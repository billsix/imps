From befed917f2d6ac2ad3854326c73d180b9f6abfad Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:39:21 +0000
Subject: [PATCH 1723/3799] soh: rename func_808B3AAC ->
 BgSpot15Rrbox_CanBePushed

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: decides whether a push should be allowed here: always inside the Lon Lon buildings, never while riding another bg actor, and otherwise only away from the ranch's fenced edge.
Defined in soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c  | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
index 9044a075c..43deb34c6 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
@@ -98,7 +98,8 @@ s32 BgSpot15Rrbox_IsOnRememberedBg(BgSpot15Rrbox* this, PlayState* play) {
     return false;
 }
 
-s32 func_808B3AAC(BgSpot15Rrbox* this, PlayState* play) {
+// LLM generated name (HIGH), was func_808B3AAC: decides whether a push should be allowed here: always inside the Lon Lon buildings, never while riding another bg actor, and otherwise only away from the ranch's fenced edge.
+s32 BgSpot15Rrbox_CanBePushed(BgSpot15Rrbox* this, PlayState* play) {
     s16 rotY;
     Actor* actor = &this->dyna.actor;
 
@@ -241,7 +242,7 @@ void func_808B40AC(BgSpot15Rrbox* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     if (this->unk_168 <= 0 && fabsf(this->dyna.unk_150) > 0.001f) {
-        if (func_808B3AAC(this, play) && !func_808B4010(this, play)) {
+        if (BgSpot15Rrbox_CanBePushed(this, play) && !func_808B4010(this, play)) {  // was func_808B3AAC [LLM:HIGH]
             this->unk_17C = this->dyna.unk_150;
             func_808B4178(this, play);
         } else {
-- 
2.55.0

