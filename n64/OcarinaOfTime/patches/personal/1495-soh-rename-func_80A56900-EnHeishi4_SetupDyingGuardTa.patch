From 1b2342abc498cb7920255feb6058d6f0813c9ee0 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:23:22 +0000
Subject: [PATCH 1495/3799] soh: rename func_80A56900 ->
 EnHeishi4_SetupDyingGuardTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: switches to the talking-while-dying animation and installs the speech state.
Defined in soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
index 6ae9c8bd5..7ca4d5b95 100644
--- a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
+++ b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
@@ -23,7 +23,8 @@ void func_80A56B40(EnHeishi4* this, PlayState* play);
 void EnHeishi4_PickMarketText(EnHeishi4* this, PlayState* play);  // was func_80A56614 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A56874: offers talk within 100 units and routes an accepted conversation either back to the idle or into the dying speech.
 void EnHeishi4_DyingGuardOfferTalk(EnHeishi4* this, PlayState* play);  // was func_80A56874 [LLM:HIGH]
-void func_80A56900(EnHeishi4* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A56900: switches to the talking-while-dying animation and installs the speech state.
+void EnHeishi4_SetupDyingGuardTalk(EnHeishi4* this, PlayState* play);  // was func_80A56900 [LLM:HIGH]
 void func_80A56994(EnHeishi4* this, PlayState* play);
 void func_80A56A50(EnHeishi4* this, PlayState* play);
 void func_80A56ACC(EnHeishi4* this, PlayState* play);
@@ -254,14 +255,14 @@ void EnHeishi4_DyingGuardOfferTalk(EnHeishi4* this, PlayState* play) {
             this->actionFunc = EnHeishi4_DyingGuardIdle;  // was func_80A5673C [LLM:HIGH]
 
         } else {
-            this->actionFunc = func_80A56900;
+            this->actionFunc = EnHeishi4_SetupDyingGuardTalk;  // was func_80A56900 [LLM:HIGH]
         }
     } else {
         Actor_OfferTalk(&this->actor, play, 100.0f);
     }
 }
 
-void func_80A56900(EnHeishi4* this, PlayState* play) {
+void EnHeishi4_SetupDyingGuardTalk(EnHeishi4* this, PlayState* play) {
     f32 frames = Animation_GetLastFrame(&gEnHeishiDyingGuardTalkAnim);
 
     Animation_Change(&this->skelAnime, &gEnHeishiDyingGuardTalkAnim, 1.0f, 0.0f, (s16)frames, ANIMMODE_LOOP, -10.0f);
-- 
2.55.0

