From 5eefb5279103f5669a0f105d1d558b442496db7a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:14:11 +0000
Subject: [PATCH 2146/3799] soh: rename func_80967FFC -> Demo6K_DrawLightBeams

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 1 draw: scrolls a tall texture through six per-sage color pairs; whether these read as beams or as a column of light is not settled by the code
Defined in soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c b/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
index 9de0e4e9b..29793614c 100644
--- a/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
+++ b/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
@@ -45,7 +45,8 @@ void Demo6K_UpdateStreaks(Demo6K* this, PlayState* play);  // was func_80967AD0
 void Demo6K_GanonLightBlast(Demo6K* this, PlayState* play);  // was func_80967DBC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80967F10: the params 12 update: spawns the params 13 blast on its first frame and shrinks away over 60
 void Demo6K_SpawnGanonBlast(Demo6K* this, PlayState* play);  // was func_80967F10 [LLM:HIGH]
-void func_80967FFC(Actor* thisx, PlayState* play);
+// LLM generated name (GUESS), was func_80967FFC: the params 1 draw: scrolls a tall texture through six per-sage color pairs; whether these read as beams or as a column of light is not settled by the code
+void Demo6K_DrawLightBeams(Actor* thisx, PlayState* play);  // was func_80967FFC [LLM:GUESS]
 void func_80968298(Actor* thisx, PlayState* play);
 void func_8096865C(Actor* thisx, PlayState* play);
 void func_809688C4(Actor* thisx, PlayState* play);
@@ -124,7 +125,7 @@ void Demo6K_Init(Actor* thisx, PlayState* play) {
             this->unk_170 = 0.0f;
             break;
         case 1:
-            this->drawFunc = func_80967FFC;
+            this->drawFunc = Demo6K_DrawLightBeams;  // was func_80967FFC [LLM:GUESS]
             this->initActionFunc = Demo6K_GatherLight;  // was func_80966E04 [LLM:HIGH]
             Actor_SetScale(&this->actor, 0.228f);
             break;
@@ -581,7 +582,7 @@ void Demo6K_Update(Actor* thisx, PlayState* play) {
     this->actionFunc(this, play);
 }
 
-void func_80967FFC(Actor* thisx, PlayState* play) {
+void Demo6K_DrawLightBeams(Actor* thisx, PlayState* play) {
     Demo6K* this = (Demo6K*)thisx;
     s32 pad;
     u16 timer1 = this->timer1;
-- 
2.55.0

