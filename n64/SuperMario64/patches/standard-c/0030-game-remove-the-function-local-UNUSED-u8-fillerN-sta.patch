From e4d8bffe47f9040e7679aefd9decd097c342d174 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 22 Sep 2026 12:27:40 +0000
Subject: [PATCH 30/46] game: remove the function-local `UNUSED u8 fillerN[]`
 stack padding
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

These arrays exist to reproduce the ROM's stack-frame layout for the
matching build; they are never read or written and the UNUSED attribute
is the only reason the compiler tolerates them. 165 declarations across
63 file(s) in game/. Struct members are untouched — the fillers
that fix a layout (save files, object rawData, audio structs) all live in
headers, which this patch does not visit.

Verified: every file compiles to byte-identical assembly before and after
with the port's own flags (asmdiff over compile_commands.json).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
---
 src/game/behaviors/activated_bf_plat.inc.c    |  1 -
 src/game/behaviors/amp.inc.c                  |  1 -
 src/game/behaviors/bbh_tilting_trap.inc.c     |  1 -
 .../behaviors/beta_fish_splash_spawner.inc.c  |  1 -
 src/game/behaviors/bird.inc.c                 |  1 -
 src/game/behaviors/bobomb.inc.c               |  3 -
 src/game/behaviors/boo_cage.inc.c             |  1 -
 src/game/behaviors/bowser.inc.c               |  6 --
 .../behaviors/bowser_falling_platform.inc.c   |  1 -
 src/game/behaviors/bowser_flame.inc.c         |  2 -
 src/game/behaviors/bubba.inc.c                |  1 -
 src/game/behaviors/cannon.inc.c               |  2 -
 src/game/behaviors/castle_floor_trap.inc.c    |  1 -
 src/game/behaviors/chuckya.inc.c              |  1 -
 src/game/behaviors/coin.inc.c                 |  1 -
 src/game/behaviors/collide_particles.inc.c    |  2 -
 src/game/behaviors/dorrie.inc.c               |  1 -
 src/game/behaviors/exclamation_box.inc.c      |  1 -
 src/game/behaviors/fish.inc.c                 |  1 -
 src/game/behaviors/flamethrower.inc.c         |  1 -
 src/game/behaviors/goomba.inc.c               |  2 -
 src/game/behaviors/grand_star.inc.c           |  1 -
 src/game/behaviors/heave_ho.inc.c             |  1 -
 src/game/behaviors/intro_lakitu.inc.c         |  3 -
 src/game/behaviors/king_bobomb.inc.c          |  1 -
 src/game/behaviors/klepto.inc.c               |  1 -
 src/game/behaviors/koopa.inc.c                |  1 -
 src/game/behaviors/koopa_shell.inc.c          |  1 -
 src/game/behaviors/lll_hexagonal_ring.inc.c   |  1 -
 .../behaviors/lll_rotating_hex_flame.inc.c    |  1 -
 src/game/behaviors/mr_i.inc.c                 |  1 -
 src/game/behaviors/mushroom_1up.inc.c         |  1 -
 src/game/behaviors/piranha_bubbles.inc.c      |  1 -
 src/game/behaviors/purple_switch.inc.c        |  1 -
 src/game/behaviors/scuttlebug.inc.c           |  1 -
 src/game/behaviors/seesaw_platform.inc.c      |  1 -
 src/game/behaviors/star_door.inc.c            |  1 -
 .../behaviors/tilting_inverted_pyramid.inc.c  |  2 -
 src/game/behaviors/tree_particles.inc.c       |  2 -
 src/game/behaviors/tuxie.inc.c                |  1 -
 src/game/behaviors/ukiki.inc.c                |  1 -
 .../behaviors/water_splashes_and_waves.inc.c  |  1 -
 src/game/camera.c                             | 73 -------------------
 src/game/debug.c                              |  1 -
 src/game/envfx_bubbles.c                      |  2 -
 src/game/game_init.c                          |  2 -
 src/game/ingame_menu.c                        |  1 -
 src/game/interaction.c                        |  2 -
 src/game/macro_special_objects.c              |  3 -
 src/game/mario.c                              |  1 -
 src/game/mario_actions_airborne.c             |  1 -
 src/game/mario_actions_automatic.c            |  2 -
 src/game/mario_actions_cutscene.c             |  1 -
 src/game/mario_actions_moving.c               |  1 -
 src/game/mario_actions_submerged.c            |  1 -
 src/game/mario_misc.c                         |  4 -
 src/game/obj_behaviors.c                      |  2 -
 src/game/obj_behaviors_2.c                    |  1 -
 src/game/object_collision.c                   |  1 -
 src/game/object_helpers.c                     |  3 -
 src/game/object_list_processor.c              |  3 -
 src/game/platform_displacement.c              |  1 -
 src/game/save_file.c                          |  2 -
 63 files changed, 165 deletions(-)

diff --git a/src/game/behaviors/activated_bf_plat.inc.c b/src/game/behaviors/activated_bf_plat.inc.c
index 7edf94e5..7c63977e 100644
--- a/src/game/behaviors/activated_bf_plat.inc.c
+++ b/src/game/behaviors/activated_bf_plat.inc.c
@@ -62,7 +62,6 @@ void bhv_activated_back_and_forth_platform_init(void) {
  * Activated back-and-forth platform update function.
  */
 void bhv_activated_back_and_forth_platform_update(void) {
-    UNUSED u8 filler[12];
 
     // oVelY is used for vertical platforms' movement and also for
     // horizontal platforms' dipping up/down when Mario gets on/off them
diff --git a/src/game/behaviors/amp.inc.c b/src/game/behaviors/amp.inc.c
index 4fbdadb7..5de97b0b 100644
--- a/src/game/behaviors/amp.inc.c
+++ b/src/game/behaviors/amp.inc.c
@@ -157,7 +157,6 @@ static void homing_amp_chase_loop(void) {
  * Give up on chasing Mario.
  */
 static void homing_amp_give_up_loop(void) {
-    UNUSED u8 filler[8];
 
     // Move forward for 152 frames
     o->oForwardVel = 15.0f;
diff --git a/src/game/behaviors/bbh_tilting_trap.inc.c b/src/game/behaviors/bbh_tilting_trap.inc.c
index d54460af..e0b0657e 100644
--- a/src/game/behaviors/bbh_tilting_trap.inc.c
+++ b/src/game/behaviors/bbh_tilting_trap.inc.c
@@ -9,7 +9,6 @@
  * Update function for bhvBbhTiltingTrapPlatform.
  */
 void bhv_bbh_tilting_trap_platform_loop(void) {
-    UNUSED u8 filler[4];
 
     // Post-JP versions use oAction for the
     // if statement, while immediately setting it over here.
diff --git a/src/game/behaviors/beta_fish_splash_spawner.inc.c b/src/game/behaviors/beta_fish_splash_spawner.inc.c
index 93a2ee12..b68dc9f6 100644
--- a/src/game/behaviors/beta_fish_splash_spawner.inc.c
+++ b/src/game/behaviors/beta_fish_splash_spawner.inc.c
@@ -13,6 +13,5 @@
  * Update function for bhvBetaFishSplashSpawner.
  */
 void bhv_beta_fish_splash_spawner_loop(void) {
-    UNUSED u8 filler[12];
     UNUSED f32 waterLevel = find_water_level(o->oPosX, o->oPosZ);
 }
diff --git a/src/game/behaviors/bird.inc.c b/src/game/behaviors/bird.inc.c
index 11d51058..12822947 100644
--- a/src/game/behaviors/bird.inc.c
+++ b/src/game/behaviors/bird.inc.c
@@ -53,7 +53,6 @@ static void bird_act_inactive(void) {
  * and the parent spawner bird if it's a spawned bird.
  */
 static void bird_act_fly(void) {
-    UNUSED u8 filler[4];
     f32 distance;
 
     // Compute forward velocity and vertical velocity from oBirdSpeed and pitch
diff --git a/src/game/behaviors/bobomb.inc.c b/src/game/behaviors/bobomb.inc.c
index 4db4137b..3d80c6be 100644
--- a/src/game/behaviors/bobomb.inc.c
+++ b/src/game/behaviors/bobomb.inc.c
@@ -65,7 +65,6 @@ void bobomb_check_interactions(void) {
 }
 
 void bobomb_act_patrol(void) {
-    UNUSED u8 filler[4];
     UNUSED s16 animFrame = o->header.gfx.animInfo.animFrame;
     s16 collisionFlags;
 
@@ -82,7 +81,6 @@ void bobomb_act_patrol(void) {
 }
 
 void bobomb_act_chase_mario(void) {
-    UNUSED u8 filler[4];
     s16 animFrame = ++o->header.gfx.animInfo.animFrame; 
     s16 collisionFlags;
 
@@ -297,7 +295,6 @@ void bhv_bobomb_buddy_init(void) {
 }
 
 void bobomb_buddy_act_idle(void) {
-    UNUSED u8 filler[4];
     s16 animFrame = o->header.gfx.animInfo.animFrame;
     UNUSED s16 collisionFlags = 0;
 
diff --git a/src/game/behaviors/boo_cage.inc.c b/src/game/behaviors/boo_cage.inc.c
index 76f1f04e..d850b1c8 100644
--- a/src/game/behaviors/boo_cage.inc.c
+++ b/src/game/behaviors/boo_cage.inc.c
@@ -26,7 +26,6 @@ static struct ObjectHitbox sBooCageHitbox = {
  * Update function for bhvBooCage.
  */
 void bhv_boo_cage_loop(void) {
-    UNUSED u8 filler[4];
 
     obj_set_hitbox(o, &sBooCageHitbox);
 
diff --git a/src/game/behaviors/bowser.inc.c b/src/game/behaviors/bowser.inc.c
index dff1102a..f00632dd 100644
--- a/src/game/behaviors/bowser.inc.c
+++ b/src/game/behaviors/bowser.inc.c
@@ -722,7 +722,6 @@ void bowser_short_second_hop(void) {
  * Makes Bowser do a big jump
  */
 void bowser_act_big_jump(void) {
-    UNUSED u8 filler[4];
 
     if (o->oSubAction == 0) {
         // Set jump animation
@@ -988,7 +987,6 @@ s32 bowser_check_hit_mine(void) {
  * Bowser's thrown act that gets called after Mario releases him
  */
 void bowser_act_thrown(void) {
-    UNUSED u8 filler[4];
 
     // Keep Bowser's timer at 0 unless he lands
     if (o->oTimer < 2) {
@@ -1035,7 +1033,6 @@ void bowser_set_goal_invisible(void) {
  */
 void bowser_act_jump_onto_stage(void) {
     s32 onDynamicFloor;
-    UNUSED u8 filler[4];
     struct Surface *floor = o->oFloor;
 
     // Set dynamic floor check (Object platforms)
@@ -1311,7 +1308,6 @@ s32 bowser_dead_default_stage_ending(void) {
  * Returns TRUE once done
  */
 s32 bowser_dead_final_stage_ending(void) {
-    UNUSED u8 filler[4];
     s32 ret = FALSE;
     s32 dialogID;
 
@@ -1450,7 +1446,6 @@ void bowser_act_tilt_lava_platform(void) {
     struct Object *platform = cur_obj_nearest_object_with_behavior(bhvTiltingBowserLavaPlatform);
     UNUSED s16 angle = o->oBowserAngleToCenter + 0x8000;
     s16 angSpeed;
-    UNUSED u8 filler[4];
 
     // If there's not platform, return to default action
     if (platform == NULL) {
@@ -1921,7 +1916,6 @@ void bowser_open_eye_switch(struct Object *obj, struct GraphNodeSwitchCase *swit
  */
 Gfx *geo_switch_bowser_eyes(s32 callContext, struct GraphNode *node, UNUSED Mat4 *mtx) {
     UNUSED s16 eyeShut;
-    UNUSED u8 filler[4];
     struct Object *obj = (struct Object *) gCurGraphNodeObject;
     struct GraphNodeSwitchCase *switchCase = (struct GraphNodeSwitchCase *) node;
 
diff --git a/src/game/behaviors/bowser_falling_platform.inc.c b/src/game/behaviors/bowser_falling_platform.inc.c
index 85da27a7..d103a2ef 100644
--- a/src/game/behaviors/bowser_falling_platform.inc.c
+++ b/src/game/behaviors/bowser_falling_platform.inc.c
@@ -30,7 +30,6 @@ void falling_bowser_plat_act_start(void) {
 }
 
 void falling_bowser_plat_act_check(void) {
-    UNUSED u8 filler[4];
     struct Object *bowser = o->oBitsPlatformBowser;
 
     if (bowser->platform == o) {
diff --git a/src/game/behaviors/bowser_flame.inc.c b/src/game/behaviors/bowser_flame.inc.c
index 08a6d670..89ce3150 100644
--- a/src/game/behaviors/bowser_flame.inc.c
+++ b/src/game/behaviors/bowser_flame.inc.c
@@ -118,7 +118,6 @@ void bhv_flame_moving_forward_growing_init(void) {
 }
 
 void bhv_flame_moving_forward_growing_loop(void) {
-    UNUSED u8 filler[4];
     UNUSED struct Object *flame;
 
     obj_set_hitbox(o, &sGrowingBowserFlameHitbox);
@@ -159,7 +158,6 @@ void bhv_flame_floating_landing_init(void) {
 f32 sFlameFloatingYLimit[] = { -8.0f, -6.0f, -3.0f };
 
 void bhv_flame_floating_landing_loop(void) {
-    UNUSED u8 filler[4];
 
     cur_obj_update_floor_and_walls();
     cur_obj_move_standard(78);
diff --git a/src/game/behaviors/bubba.inc.c b/src/game/behaviors/bubba.inc.c
index b973496d..fde1f3bf 100644
--- a/src/game/behaviors/bubba.inc.c
+++ b/src/game/behaviors/bubba.inc.c
@@ -96,7 +96,6 @@ void bubba_act_1(void) {
 }
 
 void bhv_bubba_loop(void) {
-    UNUSED u8 filler[4];
 
     o->oInteractionSubtype &= ~INT_SUBTYPE_EATS_MARIO;
     o->oBubbaUnk104 = obj_turn_pitch_toward_mario(120.0f, 0);
diff --git a/src/game/behaviors/cannon.inc.c b/src/game/behaviors/cannon.inc.c
index 2b0dac0d..cd960daf 100644
--- a/src/game/behaviors/cannon.inc.c
+++ b/src/game/behaviors/cannon.inc.c
@@ -94,7 +94,6 @@ void opened_cannon_act_5(void) {
 }
 
 void opened_cannon_act_1(void) {
-    UNUSED u8 filler[4];
 
     cur_obj_become_intangible();
     cur_obj_disable_rendering();
@@ -108,7 +107,6 @@ void opened_cannon_act_2(void) {
 }
 
 void opened_cannon_act_3(void) {
-    UNUSED u8 filler[4];
     if (o->oTimer > 3) {
         o->oAction = 0;
     }
diff --git a/src/game/behaviors/castle_floor_trap.inc.c b/src/game/behaviors/castle_floor_trap.inc.c
index ee91a020..d17ad265 100644
--- a/src/game/behaviors/castle_floor_trap.inc.c
+++ b/src/game/behaviors/castle_floor_trap.inc.c
@@ -61,7 +61,6 @@ void bhv_castle_floor_trap_rotate(void) {
 }
 
 void bhv_castle_floor_trap_loop(void) {
-    UNUSED u8 filler[12];
 
     switch (o->oAction) {
         case 0:
diff --git a/src/game/behaviors/chuckya.inc.c b/src/game/behaviors/chuckya.inc.c
index cf18ea56..5f4f9dea 100644
--- a/src/game/behaviors/chuckya.inc.c
+++ b/src/game/behaviors/chuckya.inc.c
@@ -102,7 +102,6 @@ s32 approach_forward_vel(f32 *forwardVel, f32 spC, f32 sp10) {
 
 void chuckya_act_0(void) {
     s32 sp3C = 0;
-    UNUSED u8 filler[16];
     s32 sp28;
 
     if (o->oTimer == 0) {
diff --git a/src/game/behaviors/coin.inc.c b/src/game/behaviors/coin.inc.c
index 65b7d58f..6811ff9c 100644
--- a/src/game/behaviors/coin.inc.c
+++ b/src/game/behaviors/coin.inc.c
@@ -314,7 +314,6 @@ void bhv_coin_sparkles_loop(void) {
 
 void bhv_golden_coin_sparkles_loop(void) {
     struct Object *sp2C;
-    UNUSED u8 filler[4];
     f32 sp24 = 30.0f;
 
     sp2C = spawn_object(o, MODEL_SPARKLES, bhvCoinSparkles);
diff --git a/src/game/behaviors/collide_particles.inc.c b/src/game/behaviors/collide_particles.inc.c
index 4f69d406..39e74148 100644
--- a/src/game/behaviors/collide_particles.inc.c
+++ b/src/game/behaviors/collide_particles.inc.c
@@ -26,7 +26,6 @@ void bhv_punch_tiny_triangle_loop(void) {
 
 void bhv_punch_tiny_triangle_init(void) {
     s32 i;
-    UNUSED u8 filler[4];
 
     for (i = 0; i < 6; i++) {
         struct Object *triangle = spawn_object(o, MODEL_DIRT_ANIMATION, bhvPunchTinyTriangle);
@@ -52,7 +51,6 @@ void bhv_wall_tiny_star_particle_loop(void) {
 
 void bhv_tiny_star_particles_init(void) {
     s32 i;
-    UNUSED u8 filler[4];
 
     for (i = 0; i < 7; i++) {
         struct Object *particle = spawn_object(o, MODEL_CARTOON_STAR, bhvWallTinyStarParticle);
diff --git a/src/game/behaviors/dorrie.inc.c b/src/game/behaviors/dorrie.inc.c
index b945dfbc..8ce457fe 100644
--- a/src/game/behaviors/dorrie.inc.c
+++ b/src/game/behaviors/dorrie.inc.c
@@ -108,7 +108,6 @@ void dorrie_act_raise_head(void) {
 
 void bhv_dorrie_update(void) {
     f32 boundsShift;
-    UNUSED u8 filler[8];
     f32 maxOffsetY;
 
     if (!(o->activeFlags & ACTIVE_FLAG_IN_DIFFERENT_ROOM)) {
diff --git a/src/game/behaviors/exclamation_box.inc.c b/src/game/behaviors/exclamation_box.inc.c
index a91e9638..e64f91a8 100644
--- a/src/game/behaviors/exclamation_box.inc.c
+++ b/src/game/behaviors/exclamation_box.inc.c
@@ -98,7 +98,6 @@ void exclamation_box_act_2(void) {
 }
 
 void exclamation_box_act_3(void) {
-    UNUSED u8 filler[4];
     cur_obj_move_using_fvel_and_gravity();
     if (o->oVelY < 0.0f) {
         o->oVelY = 0.0f;
diff --git a/src/game/behaviors/fish.inc.c b/src/game/behaviors/fish.inc.c
index 37ef8ecb..a5cc2104 100644
--- a/src/game/behaviors/fish.inc.c
+++ b/src/game/behaviors/fish.inc.c
@@ -258,7 +258,6 @@ static void (*sFishActions[])(void) = {
  * Main loop for fish
  */
 void bhv_fish_loop(void) {
-    UNUSED u8 filler[16];
     cur_obj_scale(1.0f);
 
     // oFishWaterLevel tracks if a fish has roamed out of water.
diff --git a/src/game/behaviors/flamethrower.inc.c b/src/game/behaviors/flamethrower.inc.c
index ae0922aa..b1255195 100644
--- a/src/game/behaviors/flamethrower.inc.c
+++ b/src/game/behaviors/flamethrower.inc.c
@@ -52,7 +52,6 @@ void bhv_flamethrower_loop(void) {
     f32 flameVel;
     s32 sp34;
     s32 model;
-    UNUSED u8 filler[8];
 
     if (o->oAction == 0) {
         if ((gCurrLevelNum != LEVEL_BBH || gMarioOnMerryGoRound == TRUE)
diff --git a/src/game/behaviors/goomba.inc.c b/src/game/behaviors/goomba.inc.c
index 4c2b8aa5..ae054fd6 100644
--- a/src/game/behaviors/goomba.inc.c
+++ b/src/game/behaviors/goomba.inc.c
@@ -67,9 +67,7 @@ static u8 sGoombaAttackHandlers[][6] = {
  * Update function for goomba triplet spawner.
  */
 void bhv_goomba_triplet_spawner_update(void) {
-    UNUSED u8 filler1[4];
     s16 goombaFlag;
-    UNUSED u8 filler2[2];
     s32 angle;
 
     // If mario is close enough and the goombas aren't currently loaded, then
diff --git a/src/game/behaviors/grand_star.inc.c b/src/game/behaviors/grand_star.inc.c
index 214b57d4..f3c0fcd6 100644
--- a/src/game/behaviors/grand_star.inc.c
+++ b/src/game/behaviors/grand_star.inc.c
@@ -24,7 +24,6 @@ void grand_star_zero_velocity(void) {
 }
 
 void bhv_grand_star_loop(void) {
-    UNUSED u8 filler[4];
     Vec3f sp28;
 
     sp28[0] = sp28[1] = sp28[2] = 0.0f;
diff --git a/src/game/behaviors/heave_ho.inc.c b/src/game/behaviors/heave_ho.inc.c
index ef5d3607..744b0251 100644
--- a/src/game/behaviors/heave_ho.inc.c
+++ b/src/game/behaviors/heave_ho.inc.c
@@ -47,7 +47,6 @@ void heave_ho_act_1(void) {
 }
 
 void heave_ho_act_2(void) {
-    UNUSED u8 filler[4];
     s16 angleVel;
 
     if (cur_obj_lateral_dist_from_mario_to_home() > 1000.0f) {
diff --git a/src/game/behaviors/intro_lakitu.inc.c b/src/game/behaviors/intro_lakitu.inc.c
index cc7859cd..c38a87c2 100644
--- a/src/game/behaviors/intro_lakitu.inc.c
+++ b/src/game/behaviors/intro_lakitu.inc.c
@@ -24,11 +24,9 @@ void intro_lakitu_set_offset_from_camera(struct Object *obj, Vec3f offset) {
 }
 
 void intro_lakitu_set_focus(struct Object *obj, Vec3f newFocus) {
-    UNUSED u8 filler1[12];
     Vec3f origin;
     f32 dist;
     s16 pitch, yaw;
-    UNUSED u8 filler2[4];
 
     // newFocus is an offset from lakitu's origin, not a point in the world.
     vec3f_set(origin, 0.0f, 0.0f, 0.0f);
@@ -73,7 +71,6 @@ s32 intro_lakitu_set_pos_and_focus(struct Object *obj, struct CutsceneSplinePoin
 
 void bhv_intro_lakitu_loop(void) {
     Vec3f sp64, sp58, sp4C;
-    UNUSED u8 filler[16];
 
     switch (o->oAction) {
         case 0:
diff --git a/src/game/behaviors/king_bobomb.inc.c b/src/game/behaviors/king_bobomb.inc.c
index af34535e..9c317fce 100644
--- a/src/game/behaviors/king_bobomb.inc.c
+++ b/src/game/behaviors/king_bobomb.inc.c
@@ -386,7 +386,6 @@ void king_bobomb_move(void) {
 void bhv_king_bobomb_loop(void) {
     f32 sp34 = 20.0f;
     f32 sp30 = 50.0f;
-    UNUSED u8 filler[8];
 
     o->oInteractionSubtype |= INT_SUBTYPE_GRABS_MARIO;
 
diff --git a/src/game/behaviors/klepto.inc.c b/src/game/behaviors/klepto.inc.c
index 9a1e10b3..73816e39 100644
--- a/src/game/behaviors/klepto.inc.c
+++ b/src/game/behaviors/klepto.inc.c
@@ -320,7 +320,6 @@ void obj_set_speed_to_zero(void) {
 }
 
 void bhv_klepto_update(void) {
-    UNUSED u8 filler[4];
 
     cur_obj_update_floor_and_walls();
 
diff --git a/src/game/behaviors/koopa.inc.c b/src/game/behaviors/koopa.inc.c
index 8e46ee7a..ed52f070 100644
--- a/src/game/behaviors/koopa.inc.c
+++ b/src/game/behaviors/koopa.inc.c
@@ -518,7 +518,6 @@ static void koopa_the_quick_act_show_init_text(void) {
         sKoopaTheQuickProperties[o->oKoopaTheQuickRaceIndex].initText);
 
     if (response == DIALOG_RESPONSE_YES) {
-        UNUSED u8 filler[4];
 
         gMarioShotFromCannon = FALSE;
         o->oAction = KOOPA_THE_QUICK_ACT_RACE;
diff --git a/src/game/behaviors/koopa_shell.inc.c b/src/game/behaviors/koopa_shell.inc.c
index dec89024..2dea881f 100644
--- a/src/game/behaviors/koopa_shell.inc.c
+++ b/src/game/behaviors/koopa_shell.inc.c
@@ -13,7 +13,6 @@ struct ObjectHitbox sKoopaShellHitbox = {
 };
 
 void koopa_shell_spawn_water_drop(void) {
-    UNUSED u8 filler[4];
 
     spawn_object(o, MODEL_WAVE_TRAIL, bhvObjectWaveTrail);
 
diff --git a/src/game/behaviors/lll_hexagonal_ring.inc.c b/src/game/behaviors/lll_hexagonal_ring.inc.c
index 28a34565..175d7a08 100644
--- a/src/game/behaviors/lll_hexagonal_ring.inc.c
+++ b/src/game/behaviors/lll_hexagonal_ring.inc.c
@@ -19,7 +19,6 @@ void hexagonal_ring_spawn_flames(void) {
 }
 
 void bhv_lll_rotating_hexagonal_ring_loop(void) {
-    UNUSED u8 filler[4];
 
     o->oCollisionDistance = 4000.0f;
     o->oDrawingDistance = 8000.0f;
diff --git a/src/game/behaviors/lll_rotating_hex_flame.inc.c b/src/game/behaviors/lll_rotating_hex_flame.inc.c
index 282bacc2..10561309 100644
--- a/src/game/behaviors/lll_rotating_hex_flame.inc.c
+++ b/src/game/behaviors/lll_rotating_hex_flame.inc.c
@@ -15,7 +15,6 @@ void bhv_lll_rotating_hex_flame_loop(void) {
 
 void fire_bar_spawn_flames(s16 a0) {
     struct Object *sp2C;
-    UNUSED u8 filler[4];
     s32 i;
     s32 sp20;
     f32 sp1C = sins(a0) * 200.0f;
diff --git a/src/game/behaviors/mr_i.inc.c b/src/game/behaviors/mr_i.inc.c
index 346a2b7e..9b68f3ed 100644
--- a/src/game/behaviors/mr_i.inc.c
+++ b/src/game/behaviors/mr_i.inc.c
@@ -85,7 +85,6 @@ void mr_i_act_3(void) {
     s16 sp34;
     f32 sp30;
     f32 sp2C;
-    UNUSED u8 filler[8];
     f32 sp20;
     f32 sp1C;
 
diff --git a/src/game/behaviors/mushroom_1up.inc.c b/src/game/behaviors/mushroom_1up.inc.c
index 861fc325..f280b92f 100644
--- a/src/game/behaviors/mushroom_1up.inc.c
+++ b/src/game/behaviors/mushroom_1up.inc.c
@@ -1,7 +1,6 @@
 // mushroom_1up.inc.c
 
 void bhv_1up_interact(void) {
-    UNUSED u8 filler[4];
 
     if (obj_check_if_collided_with_object(o, gMarioObject) == TRUE) {
         CALL_CANCELLABLE_EVENT(PlayerLivesChange, gMarioState, 1) {
diff --git a/src/game/behaviors/piranha_bubbles.inc.c b/src/game/behaviors/piranha_bubbles.inc.c
index 21e01f66..32e1ae59 100644
--- a/src/game/behaviors/piranha_bubbles.inc.c
+++ b/src/game/behaviors/piranha_bubbles.inc.c
@@ -34,7 +34,6 @@ void bhv_piranha_plant_bubble_loop(void) {
     s32 animFrame = parent->header.gfx.animInfo.animFrame;
     // TODO: rename lastFrame if it is inaccurate
     s32 lastFrame = parent->header.gfx.animInfo.curAnim->loopEnd - 2;
-    UNUSED u8 filler[4];
 
     cur_obj_set_pos_relative(parent, 0, 72.0f, 180.0f);
 
diff --git a/src/game/behaviors/purple_switch.inc.c b/src/game/behaviors/purple_switch.inc.c
index a5157746..eebac8bd 100644
--- a/src/game/behaviors/purple_switch.inc.c
+++ b/src/game/behaviors/purple_switch.inc.c
@@ -8,7 +8,6 @@
  */
 
 void bhv_purple_switch_loop(void) {
-    UNUSED u8 filler[4];
 
     switch (o->oAction) {
         /**
diff --git a/src/game/behaviors/scuttlebug.inc.c b/src/game/behaviors/scuttlebug.inc.c
index 6ec01555..5f0ab7c4 100644
--- a/src/game/behaviors/scuttlebug.inc.c
+++ b/src/game/behaviors/scuttlebug.inc.c
@@ -24,7 +24,6 @@ s32 update_angle_from_move_flags(s32 *angle) {
 }
 
 void bhv_scuttlebug_loop(void) {
-    UNUSED u8 filler[4];
     f32 sp18;
 
     cur_obj_update_floor_and_walls();
diff --git a/src/game/behaviors/seesaw_platform.inc.c b/src/game/behaviors/seesaw_platform.inc.c
index 5b2e4bc1..31cbdaf9 100644
--- a/src/game/behaviors/seesaw_platform.inc.c
+++ b/src/game/behaviors/seesaw_platform.inc.c
@@ -44,7 +44,6 @@ void bhv_seesaw_platform_update(void) {
     if (gMarioObject->platform == o) {
         // Rotate toward mario
         f32 rotation = o->oDistanceToMario * coss(o->oAngleToMario - o->oMoveAngleYaw);
-        UNUSED u8 filler[4];
 
         // Deceleration is faster than acceleration
         if (o->oSeesawPlatformPitchVel * rotation < 0) {
diff --git a/src/game/behaviors/star_door.inc.c b/src/game/behaviors/star_door.inc.c
index 23faf8b4..fadaa288 100644
--- a/src/game/behaviors/star_door.inc.c
+++ b/src/game/behaviors/star_door.inc.c
@@ -8,7 +8,6 @@ void star_door_update_pos(void) {
 }
 
 void bhv_star_door_loop(void) {
-    UNUSED u8 filler[4];
     struct Object *sp18 = cur_obj_nearest_object_with_behavior(bhvStarDoor);
 
     switch (o->oAction) {
diff --git a/src/game/behaviors/tilting_inverted_pyramid.inc.c b/src/game/behaviors/tilting_inverted_pyramid.inc.c
index 77193dc7..062a8310 100644
--- a/src/game/behaviors/tilting_inverted_pyramid.inc.c
+++ b/src/game/behaviors/tilting_inverted_pyramid.inc.c
@@ -78,9 +78,7 @@ void bhv_tilting_inverted_pyramid_loop(void) {
     f32 mz;
 
     s32 marioOnPlatform = FALSE;
-    UNUSED u8 filler1[4];
     Mat4 *transform = &o->transform;
-    UNUSED u8 filler2[28];
 
     if (gMarioObject->platform == o) {
         get_mario_pos(&mx, &my, &mz);
diff --git a/src/game/behaviors/tree_particles.inc.c b/src/game/behaviors/tree_particles.inc.c
index 7fca3654..7483fd02 100644
--- a/src/game/behaviors/tree_particles.inc.c
+++ b/src/game/behaviors/tree_particles.inc.c
@@ -45,10 +45,8 @@ void bhv_tree_snow_or_leaf_loop(void) {
 
 void bhv_snow_leaf_particle_spawn_init(void) {
     struct Object *obj; // Either snow or leaf
-    UNUSED u8 filler1[4];
     s32 isSnow;
     f32 scale;
-    UNUSED u8 filler2[4];
 
     gMarioObject->oActiveParticleFlags &= ~ACTIVE_PARTICLE_LEAF;
 
diff --git a/src/game/behaviors/tuxie.inc.c b/src/game/behaviors/tuxie.inc.c
index 49195f53..41631047 100644
--- a/src/game/behaviors/tuxie.inc.c
+++ b/src/game/behaviors/tuxie.inc.c
@@ -15,7 +15,6 @@ void play_penguin_walking_sound(s32 walk) {
 
 void tuxies_mother_act_2(void) {
     f32 sp24;
-    UNUSED u8 filler[4];
     struct Object *sp1C = cur_obj_find_nearest_object_with_behavior(bhvSmallPenguin, &sp24);
 
     if (cur_obj_find_nearby_held_actor(bhvUnused20E0, 1000.0f) != NULL) {
diff --git a/src/game/behaviors/ukiki.inc.c b/src/game/behaviors/ukiki.inc.c
index ffc3e2d5..8d1e74c5 100644
--- a/src/game/behaviors/ukiki.inc.c
+++ b/src/game/behaviors/ukiki.inc.c
@@ -171,7 +171,6 @@ void ukiki_act_idle(void) {
  * Only used for the cap ukiki.
  */
 void ukiki_act_return_home(void) {
-    UNUSED u8 filler[4];
 
     cur_obj_init_animation_with_sound(UKIKI_ANIM_RUN);
     o->oMoveAngleYaw = cur_obj_angle_to_home();
diff --git a/src/game/behaviors/water_splashes_and_waves.inc.c b/src/game/behaviors/water_splashes_and_waves.inc.c
index 89948910..53cf81b6 100644
--- a/src/game/behaviors/water_splashes_and_waves.inc.c
+++ b/src/game/behaviors/water_splashes_and_waves.inc.c
@@ -60,7 +60,6 @@ void bhv_water_splash_spawn_droplets(void) {
 }
 
 void bhv_water_droplet_loop(void) {
-    UNUSED u8 filler[4];
     f32 waterLevel = find_water_level(o->oPosX, o->oPosZ);
 
     if (o->oTimer == 0) {
diff --git a/src/game/camera.c b/src/game/camera.c
index 88c1f31b..79a6b802 100644
--- a/src/game/camera.c
+++ b/src/game/camera.c
@@ -669,7 +669,6 @@ void unused_set_camera_pitch_shake_env(s16 shake) {
 BAD_RETURN(f32) calc_y_to_curr_floor(f32 *posOff, f32 posMul, f32 posBound, f32 *focOff, f32 focMul, f32 focBound) {
     f32 floorHeight = sMarioGeometry.currFloorHeight;
     f32 waterHeight;
-    UNUSED u8 filler[4];
 
     if (!(sMarioCamState->action & ACT_FLAG_METAL_WATER)) {
         //! @bug this should use sMarioGeometry.waterHeight
@@ -751,7 +750,6 @@ void set_camera_height(struct Camera *c, f32 goalHeight) {
     f32 marioFloorHeight;
     f32 marioCeilHeight;
     f32 camFloorHeight;
-    UNUSED u8 filler[8];
     UNUSED s16 action = sMarioCamState->action;
     f32 baseOff = 125.f;
     f32 camCeilHeight = find_ceil(c->pos[0], gLakituState.goalPos[1] - 50.f, c->pos[2], &surface);
@@ -905,10 +903,8 @@ s32 update_radial_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     f32 cenDistZ = sMarioCamState->pos[2] - c->areaCenZ;
     s16 camYaw = atan2s(cenDistZ, cenDistX) + sModeOffsetYaw;
     s16 pitch = look_down_slopes(camYaw);
-    UNUSED u8 filler1[4];
     f32 posY;
     f32 focusY;
-    UNUSED u8 filler2[8];
     f32 yOff = 125.f;
     f32 baseDist = 1000.f;
 
@@ -928,7 +924,6 @@ s32 update_8_directions_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     s16 pitch = look_down_slopes(camYaw);
     f32 posY;
     f32 focusY;
-    UNUSED u8 filler[12];
     f32 yOff = 125.f;
     f32 baseDist = 1000.f;
 
@@ -1005,7 +1000,6 @@ void radial_camera_move(struct Camera *c) {
     s32 avoidStatus;
     f32 areaDistX = sMarioCamState->pos[0] - c->areaCenX;
     f32 areaDistZ = sMarioCamState->pos[2] - c->areaCenZ;
-    UNUSED u8 filler[4];
 
     // How much the camera's yaw changed
     s16 yawOffset = calculate_yaw(sMarioCamState->pos, c->pos) - atan2s(areaDistZ, areaDistX);
@@ -1192,9 +1186,7 @@ void update_yaw_and_dist_from_c_up(UNUSED struct Camera *c) {
  */
 void mode_radial_camera(struct Camera *c) {
     Vec3f pos;
-    UNUSED u8 filler1[8];
     s16 oldAreaYaw = sAreaYaw;
-    UNUSED u8 filler2[4];
 
     if (gCameraMovementFlags & CAM_MOVING_INTO_MODE) {
         update_yaw_and_dist_from_c_up(c);
@@ -1222,7 +1214,6 @@ void mode_radial_camera(struct Camera *c) {
  */
 void mode_8_directions_camera(struct Camera *c) {
     Vec3f pos;
-    UNUSED u8 filler[8];
     s16 oldAreaYaw = sAreaYaw;
 
     radial_camera_input(c, 0.f);
@@ -1398,17 +1389,13 @@ s32 update_parallel_tracking_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     Vec3f focOffset;
     s16 pathPitch;
     s16 pathYaw;
-    UNUSED u8 filler[4];
     f32 distThresh;
     f32 zoom;
     f32 camParDist;
-    UNUSED u8 filler2[8];
     f32 pathLength;
-    UNUSED u8 filler3[8];
     f32 parScale = 0.5f;
     f32 marioFloorDist;
     Vec3f marioPos;
-    UNUSED u8 filler4[12];
     Vec3s pathAngle;
     // Variables for changing to the next/prev path in the list
     Vec3f oldPos;
@@ -1578,14 +1565,12 @@ s32 update_fixed_camera(struct Camera *c, Vec3f focus, UNUSED Vec3f pos) {
     f32 ceilHeight;
     f32 heightOffset;
     f32 distCamToFocus;
-    UNUSED u8 filler2[8];
     f32 scaleToMario = 0.5f;
     s16 pitch;
     s16 yaw;
     Vec3s faceAngle;
     struct Surface *ceiling;
     Vec3f basePos;
-    UNUSED u8 filler[12];
 
     play_camera_buzz_if_c_sideways();
 
@@ -1665,20 +1650,16 @@ s32 update_fixed_camera(struct Camera *c, Vec3f focus, UNUSED Vec3f pos) {
  */
 s32 update_boss_fight_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     struct Object *o;
-    UNUSED u8 filler2[12];
     f32 focusDistance;
-    UNUSED u8 filler3[4];
     // Floor normal values
     f32 nx;
     f32 ny;
     f32 nz;
     /// Floor originOffset
     f32 oo;
-    UNUSED u8 filler4[4];
     s16 yaw;
     s16 heldState;
     struct Surface *floor;
-    UNUSED u8 filler[20];
     Vec3f secondFocus;
     Vec3f holdFocOffset = { 0.f, -150.f, -125.f };
 
@@ -1863,7 +1844,6 @@ void mode_parallel_tracking_camera(struct Camera *c) {
  * Fixed camera mode, the camera rotates around a point and looks and zooms toward Mario.
  */
 void mode_fixed_camera(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     if (gCurrLevelNum == LEVEL_BBH) {
         set_fov_function(CAM_FOV_BBH);
@@ -1882,9 +1862,7 @@ void mode_fixed_camera(struct Camera *c) {
  * The C-Buttons rotate the camera 90 degrees left/right and 67.5 degrees up/down.
  */
 s32 update_behind_mario_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
-    UNUSED u8 filler1[12];
     f32 dist;
-    UNUSED u8 filler2[4];
     s16 absPitch;
     s16 pitch;
     s16 yaw;
@@ -1893,7 +1871,6 @@ s32 update_behind_mario_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     s16 goalYawOff = 0;
     s16 yawSpeed;
     s16 pitchInc = 32;
-    UNUSED u8 filler3[12];
     f32 maxDist = 800.f;
     f32 focYOff = 125.f;
 
@@ -2189,7 +2166,6 @@ s32 update_mario_camera(UNUSED struct Camera *c, Vec3f focus, Vec3f pos) {
 s16 update_default_camera(struct Camera *c) {
     Vec3f tempPos;
     Vec3f cPos;
-    UNUSED u8 filler1[12];
     struct Surface *marioFloor;
     struct Surface *cFloor;
     struct Surface *tempFloor;
@@ -2197,7 +2173,6 @@ s16 update_default_camera(struct Camera *c) {
     f32 camFloorHeight;
     f32 tempFloorHeight;
     f32 marioFloorHeight;
-    UNUSED u8 filler2[4];
     f32 dist;
     f32 zoomDist;
     f32 waterHeight;
@@ -2212,7 +2187,6 @@ s16 update_default_camera(struct Camera *c) {
     s16 tempPitch;
     s16 tempYaw;
     f32 xzDist;
-    UNUSED u8 filler3[4];
     s16 nextYawVel;
     s16 yawVel = 0;
     f32 scale;
@@ -2549,7 +2523,6 @@ s32 update_spiral_stairs_camera(struct Camera *c, Vec3f focus, Vec3f pos) {
     s16 posPitch;
     /// The camera's yaw around the stairs
     s16 posYaw;
-    UNUSED u8 filler[4];
     Vec3f cPos;
     Vec3f checkPos;
     struct Surface *floor;
@@ -2854,7 +2827,6 @@ void move_into_c_up(struct Camera *c) {
  * The main update function for C-Up mode
  */
 s32 mode_c_up_camera(struct Camera *c) {
-    UNUSED u8 filler[12];
 
     // Play a sound when entering C-Up mode
     if (!(sCameraSoundFlags & CAM_SOUND_C_UP_PLAYED)) {
@@ -2912,7 +2884,6 @@ s32 update_in_cannon(UNUSED struct Camera *c, Vec3f focus, Vec3f pos) {
  * sCannonYOffset is used to make the camera rotate down when Mario has just entered the cannon
  */
 void mode_cannon_camera(struct Camera *c) {
-    UNUSED u8 filler[24];
 
     sLakituPitch = 0;
     gCameraMovementFlags &= ~CAM_MOVING_INTO_MODE;
@@ -3037,10 +3008,8 @@ void update_lakitu(struct Camera *c) {
     struct Surface *floor = NULL;
     Vec3f newPos;
     Vec3f newFoc;
-    UNUSED u8 filler1[12];
     f32 distToFloor;
     s16 newYaw;
-    UNUSED u8 filler2[8];
 
     if (gCameraMovementFlags & CAM_MOVE_PAUSE_SCREEN) {
     } else {
@@ -3175,7 +3144,6 @@ void manual_cam_modes(struct Camera *c) {
  * Gets controller input, checks for cutscenes, handles mode changes, and moves the camera
  */
 void update_camera(struct Camera *c) {
-    UNUSED u8 filler[24];
 
     gCamera = c;
     update_camera_hud_status(c);
@@ -3401,7 +3369,6 @@ void update_camera(struct Camera *c) {
  * Reset all the camera variables to their arcane defaults
  */
 void reset_camera(struct Camera *c) {
-    UNUSED u8 filler[16];
 
     gCamera = c;
     gCameraMovementFlags = 0;
@@ -3644,11 +3611,9 @@ void init_camera(struct Camera *c) {
  *      modulo-4's the result, because each 8-bit mask only has 4 area bits for each level
  */
 void zoom_out_if_paused_and_outside(struct GraphNodeCamera *camera) {
-    UNUSED u8 filler1[8];
     UNUSED f32 dist;
     UNUSED s16 pitch;
     s16 yaw;
-    UNUSED u8 filler2[4];
     s32 areaMaskIndex = gCurrLevelArea / 32;
     s32 areaBit = 1 << (((gCurrLevelArea & 0x10) / 4) + (((gCurrLevelArea & 0xF) - 1) & 3));
 
@@ -3705,7 +3670,6 @@ void create_camera(struct GraphNodeCamera *gc, struct AllocOnlyPool *pool) {
  * Copy Lakitu's pos and foc into `gc`
  */
 void update_graph_node_camera(struct GraphNodeCamera *gc) {
-    UNUSED u8 filler[8];
     UNUSED struct Camera *c = gc->config.camera;
     FrameInterpolation_ShouldInterpolateFrame(sFramesPaused == 0);
     gc->rollScreen = gLakituState.roll;
@@ -3772,7 +3736,6 @@ void evaluate_cubic_spline(f32 u, Vec3f Q, Vec3f a0, Vec3f a1, Vec3f a2, Vec3f a
     f32 x;
     f32 y;
     f32 z;
-    UNUSED u8 filler[16];
 
     if (u > 1.f) {
         u = 1.f;
@@ -3998,7 +3961,6 @@ void shake_camera_handheld(Vec3f pos, Vec3f focus) {
     f32 dist;
     s16 pitch;
     s16 yaw;
-    UNUSED u8 filler[8];
 
     if (sHandheldShakeMag == 0) {
         vec3f_set(shakeOffset, 0.f, 0.f, 0.f);
@@ -4435,7 +4397,6 @@ f32 camera_approach_f32_symmetric(f32 current, f32 target, f32 increment) {
  */
 void random_vec3s(Vec3s dst, s16 xRange, s16 yRange, s16 zRange) {
     f32 randomFloat;
-    UNUSED u8 filler[4];
     f32 tempXRange;
     f32 tempYRange;
     f32 tempZRange;
@@ -4567,7 +4528,6 @@ s32 is_surf_within_bounding_box(struct Surface *surf, f32 xMax, f32 yMax, f32 zM
     f32 dx;
     f32 dy;
     f32 dz;
-    UNUSED u8 filler[4];
     s32 i;
     s32 j;
     // result
@@ -4645,7 +4605,6 @@ s32 is_range_behind_surface(Vec3f from, Vec3f to, struct Surface *surf, s16 rang
     s32 behindSurface = TRUE;
     s32 leftBehind = 0;
     s32 rightBehind = 0;
-    UNUSED u8 filler[20];
     f32 checkDist;
     s16 checkPitch;
     s16 checkYaw;
@@ -4914,7 +4873,6 @@ void shake_camera_yaw(Vec3f pos, Vec3f focus) {
  * Apply a rotational shake to the camera by adjusting its roll
  */
 void shake_camera_roll(s16 *roll) {
-    UNUSED u8 filler[8];
 
     if (gLakituState.shakeMagnitude[2] != 0) {
         increment_shake_offset(&gLakituState.shakeRollPhase, gLakituState.shakeRollVel);
@@ -5309,9 +5267,7 @@ u8 open_door_cutscene(u8 pullResult, u8 pushResult) {
  * @return the cutscene that should start, 0 if none
  */
 u8 get_cutscene_from_mario_status(struct Camera *c) {
-    UNUSED u8 filler1[4];
     u8 cutscene = c->cutscene;
-    UNUSED u8 filler2[12];
 
     if (cutscene == 0) {
         // A cutscene started by an object, if any, will start if nothing else happened
@@ -5589,10 +5545,8 @@ s16 next_lakitu_state(Vec3f newPos, Vec3f newFoc, Vec3f curPos, Vec3f curFoc,
     s16 pitchVelocity;
     f32 distVelocity;
     f32 goalDist;
-    UNUSED u8 filler1[4];
     s16 goalPitch;
     s16 goalYaw;
-    UNUSED u8 filler2[4];
     f32 distTimer = sModeTransition.framesLeft;
     s16 angleTimer = sModeTransition.framesLeft;
     UNUSED s16 inTransition = FALSE;
@@ -5883,7 +5837,6 @@ BAD_RETURN(s32) cam_sl_free_roam(struct Camera *c) {
  * Warps the camera underneath the floor, used in HMC to move under the elevator platforms
  */
 void move_camera_through_floor_while_descending(struct Camera *c, f32 height) {
-    UNUSED u8 filler[4];
 
     if ((sMarioGeometry.currFloorHeight < height - 100.f)
         && (sMarioGeometry.prevFloorHeight > sMarioGeometry.currFloorHeight)) {
@@ -6921,12 +6874,9 @@ void resolve_geometry_collisions(Vec3f pos, UNUSED Vec3f lastGood) {
  * @return 3 if a wall is covering Mario, 1 if a wall is only near the camera.
  */
 s32 rotate_camera_around_walls(struct Camera *c, Vec3f cPos, s16 *avoidYaw, s16 yawRange) {
-    UNUSED u8 filler1[4];
     struct WallCollisionData colData;
     struct Surface *wall;
-    UNUSED u8 filler2[12];
     f32 dummyDist, checkDist;
-    UNUSED u8 filler3[4];
     f32 coarseRadius;
     f32 fineRadius;
     s16 wallYaw, horWallNorm;
@@ -6934,11 +6884,9 @@ s32 rotate_camera_around_walls(struct Camera *c, Vec3f cPos, s16 *avoidYaw, s16
     s16 dummyPitch;
     // The yaw of the vector from Mario to the camera.
     s16 yawFromMario;
-    UNUSED u8 filler4[2];
     s32 status = 0;
     /// The current iteration. The algorithm takes 8 equal steps from Mario back to the camera.
     s32 step = 0;
-    UNUSED u8 filler5[4];
 
     vec3f_get_dist_and_angle(sMarioCamState->pos, cPos, &dummyDist, &dummyPitch, &yawFromMario);
     sStatusFlags &= ~CAM_FLAG_CAM_NEAR_WALL;
@@ -7156,7 +7104,6 @@ void init_spline_point(struct CutsceneSplinePoint *splinePoint, s8 index, u8 spe
 void copy_spline_segment(struct CutsceneSplinePoint dst[], struct CutsceneSplinePoint src[]) {
     s32 j = 0;
     s32 i = 0;
-    UNUSED u8 filler[8];
 
     init_spline_point(&dst[i], src[j].index, src[j].speed, src[j].point);
     i++;
@@ -7346,7 +7293,6 @@ UNUSED void unused_vec3f_to_vec3s(Vec3s dst, Vec3f src) {
  * Rotate the camera's focus around the camera's position by incYaw and incPitch
  */
 void pan_camera(struct Camera *c, s16 incPitch, s16 incYaw) {
-    UNUSED u8 filler[12];
     f32 distCamToFocus;
     s16 pitch, yaw;
 
@@ -7961,7 +7907,6 @@ BAD_RETURN(s32) star_dance_bound_yaw(struct Camera *c, s16 absYaw, s16 yawMax) {
  * Store the camera's focus in cvar9.
  */
 BAD_RETURN(s32) cutscene_dance_closeup_start(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     if ((gLastCompletedStarNum == 4) && (gCurrCourseNum == COURSE_JRB)) {
         star_dance_bound_yaw(c, 0x0, 0x4000);
@@ -8607,7 +8552,6 @@ BAD_RETURN(s32) cutscene_red_coin_star_end(struct Camera *c) {
  * @param rotYaw constant yaw offset to add to the camera's focus
  */
 void cutscene_goto_cvar_pos(struct Camera *c, f32 goalDist, s16 goalPitch, s16 rotPitch, s16 rotYaw) {
-    UNUSED u8 filler[4];
     f32 nextDist;
     s16 nextPitch, nextYaw;
     // The next 2 polar coord points are only used in CUTSCENE_PREPARE_CANNON
@@ -8877,9 +8821,7 @@ BAD_RETURN(s32) cutscene_suffocation_fly_away(UNUSED struct Camera *c) {
  * Keep Lakitu above the gas level.
  */
 BAD_RETURN(s32) cutscene_suffocation_stay_above_gas(struct Camera *c) {
-    UNUSED u8 filler1[4];
     f32 gasLevel;
-    UNUSED u8 filler2[4];
 
     cutscene_goto_cvar_pos(c, 400.f, 0x2800, 0x200, 0);
     gasLevel = find_poison_gas_level(sMarioCamState->pos[0], sMarioCamState->pos[2]);
@@ -8929,7 +8871,6 @@ BAD_RETURN(s32) cutscene_enter_pool_start(struct Camera *c) {
 }
 
 BAD_RETURN(s32) cutscene_enter_pool_loop(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     cutscene_goto_cvar_pos(c, 1200.f, 0x2000, 0x200, 0);
 }
@@ -9014,7 +8955,6 @@ BAD_RETURN(s32) cutscene_pyramid_top_explode_cam_shake(UNUSED struct Camera *c)
  * Warp back to the old position, and start a heavy camera shake.
  */
 BAD_RETURN(s32) cutscene_pyramid_top_explode_warp_back(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     vec3f_copy(c->pos, sCutsceneVars[4].point);
     vec3f_copy(c->focus, sCutsceneVars[5].point);
@@ -9084,7 +9024,6 @@ UNUSED void unused_cutscene_goto_cvar(struct Camera *c) {
  * cvar9.angle[1] is the yaw between Mario and the gCutsceneFocus
  */
 BAD_RETURN(s32) cutscene_dialog_start(struct Camera *c) {
-    UNUSED u8 filler[4];
     UNUSED s16 unused;
     s16 yaw;
 
@@ -9237,7 +9176,6 @@ UNUSED void unused_cam_to_mario(struct Camera *c) {
  * Cutscene that plays when Mario is reading a message (a sign or message on the wall)
  */
 BAD_RETURN(s32) cutscene_read_message(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     cutscene_event(cutscene_read_message_start, c, 0, 0);
     sStatusFlags |= CAM_FLAG_SMOOTH_MOVEMENT;
@@ -9304,9 +9242,7 @@ BAD_RETURN(s32) cutscene_exit_succ_start(UNUSED struct Camera *c) {
  * Set the camera pos depending on which level Mario exited.
  */
 BAD_RETURN(s32) cutscene_non_painting_set_cam_pos(struct Camera *c) {
-    UNUSED u8 filler1[4];
     struct Surface *floor;
-    UNUSED u8 filler2[12];
 
     switch (gPrevLevel) {
         case LEVEL_HMC:
@@ -9490,7 +9426,6 @@ BAD_RETURN(s32) cutscene_non_painting_death(struct Camera *c) {
 BAD_RETURN(s32) cutscene_cap_switch_press_start(struct Camera *c) {
     UNUSED s16 unused;
     s16 yaw;
-    UNUSED u8 filler[8];
 
     store_info_star(c);
     yaw = calculate_yaw(sMarioCamState->pos, c->pos);
@@ -9507,7 +9442,6 @@ BAD_RETURN(s32) cutscene_cap_switch_press_rotate_around_mario(struct Camera *c)
     f32 dist;
     s16 pitch, yaw;
     UNUSED s16 unusedYaw = sMarioCamState->faceAngle[1] + 0x1000;
-    UNUSED u8 filler[2];
     UNUSED s32 cvar1Yaw = sCutsceneVars[1].angle[1];
 
     vec3f_get_dist_and_angle(sMarioCamState->pos, c->pos, &dist, &pitch, &yaw);
@@ -9807,7 +9741,6 @@ BAD_RETURN(s32) cutscene_intro_peach_fly_to_pipe(struct Camera *c) {
  * Lakitu flies around the warp pipe, then Mario jumps out.
  */
 BAD_RETURN(s32) cutscene_intro_peach_mario_appears(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     sMarioCamState->cameraEvent = 0;
     cutscene_event(cutscene_intro_peach_reset_spline, c, 0, 0);
@@ -10122,7 +10055,6 @@ BAD_RETURN(s32) cutscene_sliding_doors_fly_back_up(UNUSED struct Camera *c) {
  */
 BAD_RETURN(s32) cutscene_sliding_doors_follow_mario(struct Camera *c) {
     Vec3f pos;
-    UNUSED u8 filler[20];
 
     vec3f_copy(pos, c->pos);
     // Update cvar1 with Mario's position (the y value doesn't change)
@@ -10148,7 +10080,6 @@ BAD_RETURN(s32) cutscene_sliding_doors_follow_mario(struct Camera *c) {
  * Note: the star door unlocking event is not a cutscene, it's handled by Mario separately.
  */
 BAD_RETURN(s32) cutscene_sliding_doors_open(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     reset_pan_distance(c);
     cutscene_event(cutscene_sliding_doors_open_start, c, 0, 8);
@@ -10319,7 +10250,6 @@ BAD_RETURN(s32) cutscene_exit_painting(struct Camera *c) {
  * Unused. Warp the camera to Mario.
  */
 BAD_RETURN(s32) cutscene_unused_exit_start(struct Camera *c) {
-    UNUSED u8 filler[18];
     Vec3f offset;
     Vec3s marioAngle;
 
@@ -10369,7 +10299,6 @@ BAD_RETURN(s32) cutscene_enter_cannon_end(struct Camera *c) {
  */
 BAD_RETURN(s32) cutscene_enter_cannon_raise(struct Camera *c) {
     struct Object *o;
-    UNUSED u8 filler[8];
     f32 floorHeight;
     struct Surface *floor;
     Vec3f cannonFocus;
@@ -10522,7 +10451,6 @@ BAD_RETURN(s32) cutscene_door_end(struct Camera *c) {
  * Used for entering a room that uses a specific camera mode, like the castle lobby or BBH
  */
 BAD_RETURN(s32) cutscene_door_mode(struct Camera *c) {
-    UNUSED u8 filler[8];
 
     reset_pan_distance(c);
     camera_course_processing(c);
@@ -11346,7 +11274,6 @@ struct CutsceneSplinePoint sCcmOutsideCreditsSplineFocus[] = {
  * the duration they want the flag to be active.
  */
 void play_cutscene(struct Camera *c) {
-    UNUSED u8 filler[12];
     UNUSED s16 unusedYawFocToMario;
     s16 cutsceneDuration;
     u8 oldCutscene;
diff --git a/src/game/debug.c b/src/game/debug.c
index 900d7953..2fed9b70 100644
--- a/src/game/debug.c
+++ b/src/game/debug.c
@@ -474,7 +474,6 @@ void try_print_debug_mario_level_info(void) {
  * [5][7] (b7 in the string array) to 1 to enable debug spawn.
  */
 void try_do_mario_debug_object_spawn(void) {
-    UNUSED u8 filler[4];
 
     if (sDebugPage == DEBUG_PAGE_STAGEINFO && gDebugInfo[DEBUG_PAGE_ENEMYINFO][7] == 1) {
         if (gPlayer1Controller->buttonPressed & R_JPAD) {
diff --git a/src/game/envfx_bubbles.c b/src/game/envfx_bubbles.c
index 5e6ca09d..e8e659eb 100644
--- a/src/game/envfx_bubbles.c
+++ b/src/game/envfx_bubbles.c
@@ -204,7 +204,6 @@ void envfx_rotate_around_whirlpool(f32 *x, f32 *y, f32 *z) {
  * low or close to the center.
  */
 s32 envfx_is_whirlpool_bubble_alive(s32 index) {
-    UNUSED u8 filler[4];
 
     if ((gEnvFxBuffer + index)->bubbleY < gEnvFxBubbleConfig[ENVFX_STATE_DEST_Y] - 100) {
         return FALSE;
@@ -267,7 +266,6 @@ void envfx_update_whirlpool(void) {
  * 1000 units away from the source or 1500 units above it.
  */
 s32 envfx_is_jestream_bubble_alive(s32 index) {
-    UNUSED u8 filler[4];
 
     if (!particle_is_laterally_close(index, gEnvFxBubbleConfig[ENVFX_STATE_SRC_X],
                                      gEnvFxBubbleConfig[ENVFX_STATE_SRC_Z], 1000)
diff --git a/src/game/game_init.c b/src/game/game_init.c
index ba802509..0b5d2f3f 100644
--- a/src/game/game_init.c
+++ b/src/game/game_init.c
@@ -437,7 +437,6 @@ UNUSED void record_demo(void) {
  * Take the updated controller struct and calculate the new x, y, and distance floats.
  */
 void adjust_analog_stick(struct Controller *controller) {
-    UNUSED u8 filler[8];
 
     // Reset the controller's x and y floats.
     controller->stickX = 0;
@@ -670,7 +669,6 @@ void init_controllers(void) {
  * Setup main segments and framebuffers.
  */
 void setup_game_memory(void) {
-    UNUSED u8 filler[8];
 
     // Setup general Segment 0
     set_segment_base_addr(0, (void *) (uintptr_t) 0x80000000);
diff --git a/src/game/ingame_menu.c b/src/game/ingame_menu.c
index 38c19095..0ce46e1f 100644
--- a/src/game/ingame_menu.c
+++ b/src/game/ingame_menu.c
@@ -1123,7 +1123,6 @@ u32 ensure_nonnegative(s16 value) {
 }
 
 void handle_dialog_text_and_pages(s8 colorMode, struct DialogEntry *dialog, s8 lowerBound) {
-    UNUSED u8 filler[8];
     u8 strChar;
     u8 *str = segmented_to_virtual(dialog->str);
     s8 lineNum = 1;
diff --git a/src/game/interaction.c b/src/game/interaction.c
index debe9dcc..c666e94a 100644
--- a/src/game/interaction.c
+++ b/src/game/interaction.c
@@ -1245,7 +1245,6 @@ u32 interact_clam_or_bubba(struct MarioState *m, UNUSED u32 interactType, struct
 }
 
 u32 interact_bully(struct MarioState *m, UNUSED u32 interactType, struct Object *o) {
-    UNUSED u8 filler[4];
 
     u32 interaction;
     if (m->flags & MARIO_METAL_CAP) {
@@ -1342,7 +1341,6 @@ u32 interact_mr_blizzard(struct MarioState *m, UNUSED u32 interactType, struct O
 }
 
 u32 interact_hit_from_below(struct MarioState *m, UNUSED u32 interactType, struct Object *o) {
-    UNUSED u8 filler[4];
 
     u32 interaction;
     if (m->flags & MARIO_METAL_CAP) {
diff --git a/src/game/macro_special_objects.c b/src/game/macro_special_objects.c
index e2456323..01afab4e 100644
--- a/src/game/macro_special_objects.c
+++ b/src/game/macro_special_objects.c
@@ -108,7 +108,6 @@ struct LoadedPreset {
 #define MACRO_OBJ_PARAMS 4
 
 void spawn_macro_objects(s16 areaIndex, s16 *macroObjList) {
-    UNUSED u8 filler[4];
     s32 presetID;
 
     s16 macroObject[5]; // see the 5 #define statements above
@@ -178,7 +177,6 @@ void spawn_macro_objects(s16 areaIndex, s16 *macroObjList) {
 }
 
 void spawn_macro_objects_hardcoded(s16 areaIndex, s16 *macroObjList) {
-    UNUSED u8 filler1[8];
 
     // This version of macroObjList has the preset and Y-Rotation separated,
     // and lacks behavior params. Might be an early version of the macro object list?
@@ -188,7 +186,6 @@ void spawn_macro_objects_hardcoded(s16 areaIndex, s16 *macroObjList) {
     s16 macroObjPreset;
     s16 macroObjRY; // Y Rotation
 
-    UNUSED u8 filler2[10];
 
     gMacroObjectDefaultParent.header.gfx.areaIndex = areaIndex;
     gMacroObjectDefaultParent.header.gfx.activeAreaIndex = areaIndex;
diff --git a/src/game/mario.c b/src/game/mario.c
index cd3d2bfa..ae218941 100644
--- a/src/game/mario.c
+++ b/src/game/mario.c
@@ -536,7 +536,6 @@ struct Surface *resolve_and_return_wall_collisions(Vec3f pos, f32 offset, f32 ra
  * Finds the ceiling from a vec3f horizontally and a height (with 80 vertical buffer).
  */
 f32 vec3f_find_ceil(Vec3f pos, f32 height, struct Surface **ceil) {
-    UNUSED u8 filler[4];
 
     return find_ceil(pos[0], height + 80.0f, pos[2], ceil);
 }
diff --git a/src/game/mario_actions_airborne.c b/src/game/mario_actions_airborne.c
index 1278e9cf..a4e3c6af 100644
--- a/src/game/mario_actions_airborne.c
+++ b/src/game/mario_actions_airborne.c
@@ -328,7 +328,6 @@ void update_flying_pitch(struct MarioState *m) {
 }
 
 void update_flying(struct MarioState *m) {
-    UNUSED u8 filler[4];
 
     update_flying_pitch(m);
     update_flying_yaw(m);
diff --git a/src/game/mario_actions_automatic.c b/src/game/mario_actions_automatic.c
index b8bf6cd2..528e8869 100644
--- a/src/game/mario_actions_automatic.c
+++ b/src/game/mario_actions_automatic.c
@@ -56,7 +56,6 @@ void play_climbing_sounds(struct MarioState *m, s32 b) {
 }
 
 s32 set_pole_position(struct MarioState *m, f32 offsetY) {
-    UNUSED u8 filler[12];
     struct Surface *floor;
     struct Surface *ceil;
     f32 floorHeight;
@@ -295,7 +294,6 @@ s32 act_top_of_pole(struct MarioState *m) {
 }
 
 s32 perform_hanging_step(struct MarioState *m, Vec3f nextPos) {
-    UNUSED u8 filler[4];
     struct Surface *ceil;
     struct Surface *floor;
     f32 ceilHeight;
diff --git a/src/game/mario_actions_cutscene.c b/src/game/mario_actions_cutscene.c
index e40b4327..62dc77d0 100644
--- a/src/game/mario_actions_cutscene.c
+++ b/src/game/mario_actions_cutscene.c
@@ -1479,7 +1479,6 @@ s32 act_shocked(struct MarioState *m) {
 }
 
 s32 act_squished(struct MarioState *m) {
-    UNUSED u8 filler[4];
     f32 squishAmount;
     f32 spaceUnderCeil;
     s16 surfAngle;
diff --git a/src/game/mario_actions_moving.c b/src/game/mario_actions_moving.c
index 413201e6..cc9c88eb 100644
--- a/src/game/mario_actions_moving.c
+++ b/src/game/mario_actions_moving.c
@@ -488,7 +488,6 @@ s32 analog_stick_held_back(struct MarioState *m) {
 }
 
 s32 check_ground_dive_or_punch(struct MarioState *m) {
-    UNUSED u8 filler[4];
 
     if (m->input & INPUT_B_PRESSED) {
         //! Speed kick (shoutouts to SimpleFlips)
diff --git a/src/game/mario_actions_submerged.c b/src/game/mario_actions_submerged.c
index 368a6143..b2285d48 100644
--- a/src/game/mario_actions_submerged.c
+++ b/src/game/mario_actions_submerged.c
@@ -165,7 +165,6 @@ void apply_water_current(struct MarioState *m, Vec3f step) {
 }
 
 u32 perform_water_step(struct MarioState *m) {
-    UNUSED u8 filler[4];
     u32 stepResult;
     Vec3f nextPos;
     Vec3f step;
diff --git a/src/game/mario_misc.c b/src/game/mario_misc.c
index 4e2576f4..f4195277 100644
--- a/src/game/mario_misc.c
+++ b/src/game/mario_misc.c
@@ -242,9 +242,7 @@ void bhv_unlock_door_star_init(void) {
 }
 
 void bhv_unlock_door_star_loop(void) {
-    UNUSED u8 filler1[4];
     s16 prevYaw = gCurrentObject->oMoveAngleYaw;
-    UNUSED u8 filler2[4];
 
     // Speed up the star every frame
     if (gCurrentObject->oUnlockDoorStarYawVel < 0x2400) {
@@ -325,12 +323,10 @@ Gfx *make_gfx_mario_alpha(struct GraphNodeGenerated *node, s16 alpha) {
  * Sets the correct blend mode and color for mirror Mario.
  */
 Gfx *geo_mirror_mario_set_alpha(s32 callContext, struct GraphNode *node, UNUSED Mat4 *c) {
-    UNUSED u8 filler1[4];
     Gfx *gfx = NULL;
     struct GraphNodeGenerated *asGenerated = (struct GraphNodeGenerated *) node;
     struct MarioBodyState *bodyState = &gBodyStates[asGenerated->parameter];
     s16 alpha;
-    UNUSED u8 filler2[4];
 
     if (callContext == GEO_CONTEXT_RENDER) {
         alpha = (bodyState->modelState & 0x100) ? (bodyState->modelState & 0xFF) : 255;
diff --git a/src/game/obj_behaviors.c b/src/game/obj_behaviors.c
index f12fca90..7bdf06b7 100644
--- a/src/game/obj_behaviors.c
+++ b/src/game/obj_behaviors.c
@@ -93,7 +93,6 @@ Gfx UNUSED *geo_obj_transparency_something(s32 callContext, struct GraphNode *no
     struct Object *heldObject;
     struct Object *obj;
     UNUSED struct Object *unusedObject;
-    UNUSED u8 filler[4];
 
     gfxHead = NULL;
 
@@ -599,7 +598,6 @@ s8 obj_check_if_facing_toward_angle(u32 base, u32 goal, s16 range) {
  */
 s8 obj_find_wall_displacement(Vec3f dist, f32 x, f32 y, f32 z, f32 radius) {
     struct WallCollisionData hitbox;
-    UNUSED u8 filler[32];
 
     hitbox.x = x;
     hitbox.y = y;
diff --git a/src/game/obj_behaviors_2.c b/src/game/obj_behaviors_2.c
index 6be3cf5f..a2d7e55a 100644
--- a/src/game/obj_behaviors_2.c
+++ b/src/game/obj_behaviors_2.c
@@ -152,7 +152,6 @@ void platform_on_track_update_pos_or_spawn_ball(s32 ballIndex, f32 x, f32 y, f32
     struct Waypoint *initialPrevWaypoint;
     struct Waypoint *nextWaypoint;
     struct Waypoint *prevWaypoint;
-    UNUSED u8 filler[4];
     f32 amountToMove;
     f32 dx;
     f32 dy;
diff --git a/src/game/object_collision.c b/src/game/object_collision.c
index 779cdb79..a26f53ce 100644
--- a/src/game/object_collision.c
+++ b/src/game/object_collision.c
@@ -9,7 +9,6 @@
 
 struct Object *debug_print_obj_collision(struct Object *a) {
     struct Object *sp24;
-    UNUSED u8 filler[4];
     s32 i;
 
     for (i = 0; i < a->numCollidedObjs; i++) {
diff --git a/src/game/object_helpers.c b/src/game/object_helpers.c
index 58ab59ea..ad336821 100644
--- a/src/game/object_helpers.c
+++ b/src/game/object_helpers.c
@@ -386,7 +386,6 @@ s16 obj_angle_to_object(struct Object *obj1, struct Object *obj2) {
 
 s16 obj_turn_toward_object(struct Object *obj, struct Object *target, s16 angleIndex, s16 turnAmount) {
     f32 a, b, c, d;
-    UNUSED u8 filler[4];
     s16 targetAngle, startAngle;
 
     switch (angleIndex) {
@@ -1201,7 +1200,6 @@ s32 cur_obj_move_xz(f32 steepSlopeNormalY, s32 careAboutEdgesAndSteepSlopes) {
     f32 intendedFloorHeight = find_floor(intendedX, o->oPosY, intendedZ, &intendedFloor);
     f32 deltaFloorHeight = intendedFloorHeight - o->oFloorHeight;
 
-    UNUSED u8 filler[4];
     f32 ny;
 
     o->oMoveFlags &= ~OBJ_MOVE_HIT_EDGE;
@@ -1975,7 +1973,6 @@ s32 cur_obj_follow_path(UNUSED s32 unusedArg) {
     struct Waypoint *lastWaypoint;
     struct Waypoint *targetWaypoint;
     f32 prevToNextX, prevToNextY, prevToNextZ;
-    UNUSED u8 filler[4];
     f32 objToNextXZ;
     f32 objToNextX, objToNextY, objToNextZ;
 
diff --git a/src/game/object_list_processor.c b/src/game/object_list_processor.c
index 026a135d..bb301e0a 100644
--- a/src/game/object_list_processor.c
+++ b/src/game/object_list_processor.c
@@ -470,7 +470,6 @@ void spawn_objects_from_info(UNUSED s32 unused, struct SpawnInfo *spawnInfo) {
 
     while (spawnInfo != NULL) {
         struct Object *object;
-        UNUSED u8 filler[4];
         const BehaviorScript *script;
         UNUSED s16 arg16 = (s16)(spawnInfo->behaviorArg & 0xFFFF);
 
@@ -572,7 +571,6 @@ void update_terrain_objects(void) {
  * the order specified by sObjectListUpdateOrder.
  */
 void update_non_terrain_objects(void) {
-    UNUSED u8 filler[4];
     s32 listIndex;
 
     s32 i = 2;
@@ -586,7 +584,6 @@ void update_non_terrain_objects(void) {
  * Unload deactivated objects in any object list.
  */
 void unload_deactivated_objects(void) {
-    UNUSED u8 filler[4];
     s32 listIndex;
 
     s32 i = 0;
diff --git a/src/game/platform_displacement.c b/src/game/platform_displacement.c
index a6a15f0f..c379fbfc 100644
--- a/src/game/platform_displacement.c
+++ b/src/game/platform_displacement.c
@@ -22,7 +22,6 @@ struct Object *gMarioPlatform = NULL;
  */
 void update_mario_platform(void) {
     struct Surface *floor;
-    UNUSED u8 filler[4];
     f32 marioX;
     f32 marioY;
     f32 marioZ;
diff --git a/src/game/save_file.c b/src/game/save_file.c
index 98b1f220..50bdaed3 100644
--- a/src/game/save_file.c
+++ b/src/game/save_file.c
@@ -52,7 +52,6 @@ STATIC_ASSERT(ARRAY_COUNT(gLevelToCourseNumTable) == LEVEL_COUNT - 1,
 // This was probably used to set progress to 100% for debugging, but
 // it was removed from the release ROM.
 void stub_save_file_1(void) {
-    UNUSED u8 filler[4];
 }
 
 /**
@@ -292,7 +291,6 @@ void save_file_erase(s32 fileIndex) {
 
 //! Needs to be s32 to match on -O2, despite no return value.
 BAD_RETURN(s32) save_file_copy(s32 srcFileIndex, s32 destFileIndex) {
-    UNUSED u8 filler[4];
 
     touch_high_score_ages(destFileIndex);
     bcopy(&gSaveBuffer.files[srcFileIndex][0], &gSaveBuffer.files[destFileIndex][0],
-- 
2.55.0

