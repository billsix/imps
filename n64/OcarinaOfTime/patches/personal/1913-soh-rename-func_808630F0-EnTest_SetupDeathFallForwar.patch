From 2c7f347ae204181528e43f364f77b2a5623f8350 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:12 +0000
Subject: [PATCH 1913/3799] soh: rename func_808630F0 ->
 EnTest_SetupDeathFallForwards

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: plays gStalfosFallOverForwardsAnim with NA_SE_EN_STAL_DEAD; the forwards twin of EnTest_SetupDeathFallBackwards
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index aabbf2856..2abe6a113 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -1577,7 +1577,8 @@ void EnTest_DeathFallBackwards(EnTest* this, PlayState* play) {
     }
 }
 
-void func_808630F0(EnTest* this, PlayState* play) {
+// LLM generated name (HIGH), was func_808630F0: plays gStalfosFallOverForwardsAnim with NA_SE_EN_STAL_DEAD; the forwards twin of EnTest_SetupDeathFallBackwards
+void EnTest_SetupDeathFallForwards(EnTest* this, PlayState* play) {
     Animation_PlayOnce(&this->skelAnime, &gStalfosFallOverForwardsAnim);
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_STAL_DEAD);
     this->unk_7C8 = 6;
@@ -1706,7 +1707,7 @@ void EnTest_UpdateDamage(EnTest* this, PlayState* play) {
                         EnTest_SetupFlinchFront(this);  // was func_80862154 [LLM:HIGH]
                     }
                 } else if (Actor_ApplyDamage(&this->actor) == 0) {
-                    func_808630F0(this, play);
+                    EnTest_SetupDeathFallForwards(this, play);  // was func_808630F0 [LLM:HIGH]
                     Enemy_StartFinishingBlow(play, &this->actor);
                 } else {
                     EnTest_SetupFlinchBehind(this);  // was func_80862398 [LLM:HIGH]
-- 
2.55.0

