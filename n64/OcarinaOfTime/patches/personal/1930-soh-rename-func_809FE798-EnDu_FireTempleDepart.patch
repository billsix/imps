From ef87c3ca7887f483d50526cfb4e86effabcbecee Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:04:00 +0000
Subject: [PATCH 1930/3799] soh: rename func_809FE798 -> EnDu_FireTempleDepart

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: slides Darunia out through the door with the unlock/open/close/thud sounds, then kills the actor and sets INFTABLE_SPOKE_TO_DARUNIA_IN_FIRE_TEMPLE
Defined in soh/src/overlays/actors/ovl_En_Du/z_en_du.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Du/z_en_du.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Du/z_en_du.c b/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
index 7db6f4340..881d56d65 100644
--- a/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
+++ b/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
@@ -23,7 +23,8 @@ void EnDu_CheckOcarinaSong(EnDu* this, PlayState* play);  // was func_809FE4A4 [
 void EnDu_FireTempleWaitToTalk(EnDu* this, PlayState* play);  // was func_809FE6CC [LLM:HIGH]
 // LLM generated name (HIGH), was func_809FE740: waits for the talk state to return to idle, releases the camera and arms the 0x5A departure timer
 void EnDu_FireTempleWaitTalkDone(EnDu* this, PlayState* play);  // was func_809FE740 [LLM:HIGH]
-void func_809FE798(EnDu* this, PlayState* play);
+// LLM generated name (HIGH), was func_809FE798: slides Darunia out through the door with the unlock/open/close/thud sounds, then kills the actor and sets INFTABLE_SPOKE_TO_DARUNIA_IN_FIRE_TEMPLE
+void EnDu_FireTempleDepart(EnDu* this, PlayState* play);  // was func_809FE798 [LLM:HIGH]
 void func_809FEC14(EnDu* this, PlayState* play);
 void func_809FEC70(EnDu* this, PlayState* play);
 void func_809FECE4(EnDu* this, PlayState* play);
@@ -446,11 +447,11 @@ void EnDu_FireTempleWaitTalkDone(EnDu* this, PlayState* play) {
     if (this->interactInfo.talkState == NPC_TALK_STATE_IDLE) {
         Camera_SetFinishedFlag(GET_ACTIVE_CAM(play));
         this->unk_1E2 = 0x5A;
-        EnDu_SetupAction(this, func_809FE798);
+        EnDu_SetupAction(this, EnDu_FireTempleDepart);  // was func_809FE798 [LLM:HIGH]
     }
 }
 
-void func_809FE798(EnDu* this, PlayState* play) {
+void EnDu_FireTempleDepart(EnDu* this, PlayState* play) {
     s32 phi_v0;
 
     if (this->unk_1E2 == 0) {
-- 
2.55.0

