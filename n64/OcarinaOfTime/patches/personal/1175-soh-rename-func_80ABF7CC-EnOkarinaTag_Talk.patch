From 0d58d2af7ae86fb2bfa7a40266c0c423ffb80429 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:38 +0000
Subject: [PATCH 1175/3799] soh: rename func_80ABF7CC -> EnOkarinaTag_Talk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: closes the textbox and, if the player does not yet have the Sun's Song, triggers gSunSongGraveSunSongTeachCs.
Defined in soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
index 3ac3ebc8e..6dabd43f6 100644
--- a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
+++ b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
@@ -26,7 +26,8 @@ void EnOkarinaTag_WaitForFixedSong(EnOkarinaTag* this, PlayState* play);  // was
 void EnOkarinaTag_CheckConfiguredSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF0CC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABF4C8: on a correct song it sets the switch flag and runs the per-type consequence: opening Zora's Domain, speeding the windmill, the Door of Time cutscene, or blowing open the royal family's tomb.
 void EnOkarinaTag_CheckFixedSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF4C8 [LLM:HIGH]
-void func_80ABF7CC(EnOkarinaTag* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABF7CC: closes the textbox and, if the player does not yet have the Sun's Song, triggers gSunSongGraveSunSongTeachCs.
+void EnOkarinaTag_Talk(EnOkarinaTag* this, PlayState* play);  // was func_80ABF7CC [LLM:HIGH]
 
 const ActorInit En_Okarina_Tag_InitVars = {
     ACTOR_EN_OKARINA_TAG,
@@ -300,7 +301,7 @@ void EnOkarinaTag_WaitForTalk(EnOkarinaTag* this, PlayState* play) {
     s16 yawDiffNew;
 
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
-        this->actionFunc = func_80ABF7CC;
+        this->actionFunc = EnOkarinaTag_Talk;  // was func_80ABF7CC [LLM:HIGH]
     } else {
         yawDiff = this->actor.yawTowardsPlayer - this->actor.world.rot.y;
         this->unk_15A++;
@@ -317,7 +318,7 @@ void EnOkarinaTag_WaitForTalk(EnOkarinaTag* this, PlayState* play) {
     }
 }
 
-void func_80ABF7CC(EnOkarinaTag* this, PlayState* play) {
+void EnOkarinaTag_Talk(EnOkarinaTag* this, PlayState* play) {
     // "Open sesame sesame!"
     osSyncPrintf(VT_FGCOL(PURPLE) "☆☆☆☆☆ 開けゴマゴマゴマ！ ☆☆☆☆☆ %d\n" VT_RST, Message_GetState(&play->msgCtx));
 
-- 
2.55.0

