From 4af4c7039ddf997649b18d5b78b5ba0bedb3c962 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:38 +0000
Subject: [PATCH 1411/3799] soh: rename func_80999214 ->
 DoorWarp1_BlueCrystalFadeToBlack

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: fades the crystal in while driving the environment's ambient, fog and light colours to black over forty frames, then hands off to the wait for the boss-clear fanfare.
Defined in soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
index a3f4a7090..66437265e 100644
--- a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
+++ b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
@@ -19,7 +19,8 @@ void DoorWarp1_AwaitClearFlag(DoorWarp1* this, PlayState* play);
 void func_8099A3A4(DoorWarp1* this, PlayState* play);
 void DoorWarp1_BlueCrystal(DoorWarp1* this, PlayState* play);
 void DoorWarp1_PurpleCrystal(DoorWarp1* this, PlayState* play);
-void func_80999214(DoorWarp1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80999214: fades the crystal in while driving the environment's ambient, fog and light colours to black over forty frames, then hands off to the wait for the boss-clear fanfare.
+void DoorWarp1_BlueCrystalFadeToBlack(DoorWarp1* this, PlayState* play);  // was func_80999214 [LLM:HIGH]
 void func_80999348(DoorWarp1* this, PlayState* play);
 void func_809995D4(DoorWarp1* this, PlayState* play);
 void func_809998A4(DoorWarp1* this, PlayState* play);
@@ -304,12 +305,12 @@ void DoorWarp1_BlueCrystal(DoorWarp1* this, PlayState* play) {
     if (this->warpTimer != 0) {
         this->warpTimer--;
     } else {
-        DoorWarp1_SetupAction(this, func_80999214);
+        DoorWarp1_SetupAction(this, DoorWarp1_BlueCrystalFadeToBlack);  // was func_80999214 [LLM:HIGH]
     }
     DoorWarp1_SetPlayerPos(this, play);
 }
 
-void func_80999214(DoorWarp1* this, PlayState* play) {
+void DoorWarp1_BlueCrystalFadeToBlack(DoorWarp1* this, PlayState* play) {
     s32 temp_f4;
     f32 darkness;
     s16 i;
@@ -1112,4 +1113,4 @@ void DoorWarp1_Draw(Actor* thisx, PlayState* play) {
 void DoorWarp1_Reset(void) {
     sWarpTimerTarget = 0;
     sRutoWarpSubCamId = 0;
-}
\ No newline at end of file
+}
-- 
2.55.0

