From 3758825b794274a0b9ca6e69ba6bf1987d63e45b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:11:25 +0000
Subject: [PATCH 1343/3799] soh: rename func_80BA2610 -> ObjWarp2block_Active

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the main action: on a successful Song of Time it spawns the warp effect, arms the attention cutscene and, twelve frames later, swaps the block with its partner.
Defined in soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c b/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
index a08061ebd..8ae593df0 100644
--- a/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
+++ b/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
@@ -36,7 +36,8 @@ void ObjWarp2block_SetupFindPartner(ObjWarp2block* this);  // was func_80BA24E8
 void ObjWarp2block_FindPartner(ObjWarp2block* this, PlayState* play);  // was func_80BA24F8 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80BA2600: installs the main action once the partner has been found.
 void ObjWarp2block_SetupActive(ObjWarp2block* this);  // was func_80BA2600 [LLM:HIGH]
-void func_80BA2610(ObjWarp2block* this, PlayState* play);
+// LLM generated name (HIGH), was func_80BA2610: the main action: on a successful Song of Time it spawns the warp effect, arms the attention cutscene and, twelve frames later, swaps the block with its partner.
+void ObjWarp2block_Active(ObjWarp2block* this, PlayState* play);  // was func_80BA2610 [LLM:HIGH]
 
 const ActorInit Obj_Warp2block_InitVars = {
     ACTOR_OBJ_WARP2BLOCK,
@@ -281,10 +282,10 @@ void ObjWarp2block_FindPartner(ObjWarp2block* this, PlayState* play) {
 }
 
 void ObjWarp2block_SetupActive(ObjWarp2block* this) {
-    this->actionFunc = func_80BA2610;
+    this->actionFunc = ObjWarp2block_Active;  // was func_80BA2610 [LLM:HIGH]
 }
 
-void func_80BA2610(ObjWarp2block* this, PlayState* play) {
+void ObjWarp2block_Active(ObjWarp2block* this, PlayState* play) {
     if ((ObjWarp2block_RunOcarinaCheck(this, play) != 0) && (this->unk_16C <= 0)) {  // was func_80BA2304 [LLM:HIGH]
         ObjWarp2block_Spawn(this, play);
         this->unk_16C = 0xA0;
-- 
2.55.0

