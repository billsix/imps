From 89cdc03185335422ec792419a101081257b1eab5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:18:20 +0000
Subject: [PATCH 1425/3799] soh: rename func_80A45288 ->
 EnGo2_UpdateTrackingAndTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: tracks the player with the head at the per-variant, per-age offset and, once the Goron is awake, runs the talk update and picks Biggoron's text.
Defined in soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c b/soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c
index b6c8bf637..70e0cede1 100644
--- a/soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c
+++ b/soh/src/overlays/actors/ovl_En_Go2/z_en_go2.c
@@ -1095,7 +1095,8 @@ void EnGo2_BiggoronSetTextId(EnGo2* this, PlayState* play, Player* player) {
     }
 }
 
-void func_80A45288(EnGo2* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A45288: tracks the player with the head at the per-variant, per-age offset and, once the Goron is awake, runs the talk update and picks Biggoron's text.
+void EnGo2_UpdateTrackingAndTalk(EnGo2* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s32 linkAge;
 
@@ -2022,7 +2023,7 @@ void EnGo2_Update(Actor* thisx, PlayState* play) {
     if (this->unk_211 == true) {
         Actor_UpdateFidgetTables(play, this->fidgetTableY, this->fidgetTableZ, 18);
     }
-    func_80A45288(this, play);
+    EnGo2_UpdateTrackingAndTalk(this, play);  // was func_80A45288 [LLM:HIGH]
     EnGo2_EyeMouthTexState(this);
     EnGo2_CheckCollision(this, play);
 }
-- 
2.55.0

