From 1962575076ed75e715bd4c41ec74894156df6f75 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:32:50 +0000
Subject: [PATCH 1652/3799] soh: rename func_80A6C6B0 ->
 EnHorseNormal_SetupWaitClone

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters HORSE_WAIT_CLONE, the same stop but with culling disabled so the duplicate stays live off screen.
Defined in soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Horse_Normal/z_en_horse_normal.c       | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
index 47342e8a6..17247a243 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
@@ -41,7 +41,8 @@ void EnHorseNormal_SetupWander(EnHorseNormal* this);  // was func_80A6BC48 [LLM:
 void EnHorseNormal_PlayRunSfx(EnHorseNormal* this);  // was func_80A6BCEC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A6C4CC: enters HORSE_WAIT, stopping the horse and returning it to the idle animation.
 void EnHorseNormal_SetupWait(EnHorseNormal* this);  // was func_80A6C4CC [LLM:HIGH]
-void func_80A6C6B0(EnHorseNormal* this);
+// LLM generated name (HIGH), was func_80A6C6B0: enters HORSE_WAIT_CLONE, the same stop but with culling disabled so the duplicate stays live off screen.
+void EnHorseNormal_SetupWaitClone(EnHorseNormal* this);  // was func_80A6C6B0 [LLM:HIGH]
 
 const ActorInit En_Horse_Normal_InitVars = {
     ACTOR_EN_HORSE_NORMAL,
@@ -243,7 +244,7 @@ void EnHorseNormal_Init(Actor* thisx, PlayState* play) {
              this->actor.world.pos.z == -1100.0f) ||
             (this->actor.world.pos.x == 880.0f && this->actor.world.pos.y == 0.0f &&
              this->actor.world.pos.z == -1170.0f)) {
-            func_80A6C6B0(this);
+            EnHorseNormal_SetupWaitClone(this);  // was func_80A6C6B0 [LLM:HIGH]
             return;
         }
     } else if (play->sceneNum == SCENE_STABLE) {
@@ -253,7 +254,7 @@ void EnHorseNormal_Init(Actor* thisx, PlayState* play) {
         } else {
             Skin_Init(play, &this->skin, &gHorseNormalSkel, &gHorseNormalIdleAnim);
             Animation_PlayOnce(&this->skin.skelAnime, sAnimations[this->animationIdx]);
-            func_80A6C6B0(this);
+            EnHorseNormal_SetupWaitClone(this);  // was func_80A6C6B0 [LLM:HIGH]
             return;
         }
     } else if (play->sceneNum == SCENE_GERUDOS_FORTRESS) {
@@ -525,7 +526,7 @@ void EnHorseNormal_Wait(EnHorseNormal* this, PlayState* play) {
     }
 }
 
-void func_80A6C6B0(EnHorseNormal* this) {
+void EnHorseNormal_SetupWaitClone(EnHorseNormal* this) {
     this->action = HORSE_WAIT_CLONE;
     this->animationIdx = 0;
     this->unk_21C = 0;
-- 
2.55.0

