From e849b720f13fc1b88743132387fb232f4b64dca9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:19 +0000
Subject: [PATCH 240/488] soh: name the 4 address-named symbols defined in
 z_bg_jya_ironobj.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 4 are defined in soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c.

  * D_808994F0 -> sPillarDebrisLifetimes
    deduced, confidence HIGH: feeds the life parameter (13th argument) of
    EffectSsKakera_Spawn in the pillar debris burst, alongside
    sPillarDebrisScales.

  * D_80899500 -> sPillarDebrisArg6
    deduced, confidence GUESS: feeds the still-unidentified arg6 parameter
    (7th argument) of EffectSsKakera_Spawn in the pillar debris burst; named
    for the parameter it fills because that parameter has no name yet.

  * D_80899520 -> sThroneDebrisLifetimes
    deduced, confidence HIGH: the throne counterpart of
    sPillarDebrisLifetimes, feeding the same life parameter in
    BgJyaIronobj_SpawnThoneParticles.

  * D_80899530 -> sThroneDebrisArg6
    deduced, confidence GUESS: the throne counterpart of sPillarDebrisArg6,
    feeding the same unidentified arg6 parameter.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c     | 20 +++++++++++--------
 1 file changed, 12 insertions(+), 8 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
index d0854634c..bb47e734b 100644
--- a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
+++ b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
@@ -67,14 +67,18 @@ static s16 sDebrisYawOffsets[] = { 0x8700, 0x4000, 0xC000, 0x0000 };  // was D_8
 // Pillar
 // LLM generated name (HIGH), was D_808994E0: feeds the scale parameter of EffectSsKakera_Spawn in BgJyaIronobj_SpawnPillarParticles (10th argument); the in-tree comment already groups it under "// Pillar".
 static s16 sPillarDebrisScales[] = { 5, 8, 11, 14, 17, 20, 23, 26 };  // was D_808994E0 [LLM:HIGH]
-static s16 D_808994F0[] = { 18, 26, 34, 42, 50, 60, 70, 80 };
-static s16 D_80899500[] = { 48, 42, 36, 32, 28, 24, 20, 16 };
+// LLM generated name (HIGH), was D_808994F0: feeds the life parameter (13th argument) of EffectSsKakera_Spawn in the pillar debris burst, alongside sPillarDebrisScales.
+static s16 sPillarDebrisLifetimes[] = { 18, 26, 34, 42, 50, 60, 70, 80 };  // was D_808994F0 [LLM:HIGH]
+// LLM generated name (GUESS), was D_80899500: feeds the still-unidentified arg6 parameter (7th argument) of EffectSsKakera_Spawn in the pillar debris burst; named for the parameter it fills because that parameter has no name yet.
+static s16 sPillarDebrisArg6[] = { 48, 42, 36, 32, 28, 24, 20, 16 };  // was D_80899500 [LLM:GUESS]
 
 // Throne
 // LLM generated name (HIGH), was D_80899510: the throne counterpart of sPillarDebrisScales -- same values, feeding the scale parameter in BgJyaIronobj_SpawnThoneParticles.
 static s16 sThroneDebrisScales[] = { 5, 8, 11, 14, 17, 20, 23, 26 };  // was D_80899510 [LLM:HIGH]
-static s16 D_80899520[] = { 18, 26, 34, 42, 50, 60, 70, 80 };
-static s16 D_80899530[] = { 48, 42, 36, 32, 28, 24, 20, 16 };
+// LLM generated name (HIGH), was D_80899520: the throne counterpart of sPillarDebrisLifetimes, feeding the same life parameter in BgJyaIronobj_SpawnThoneParticles.
+static s16 sThroneDebrisLifetimes[] = { 18, 26, 34, 42, 50, 60, 70, 80 };  // was D_80899520 [LLM:HIGH]
+// LLM generated name (GUESS), was D_80899530: the throne counterpart of sPillarDebrisArg6, feeding the same unidentified arg6 parameter.
+static s16 sThroneDebrisArg6[] = { 48, 42, 36, 32, 28, 24, 20, 16 };  // was D_80899530 [LLM:GUESS]
 
 static InitChainEntry sInitChain[] = {
     ICHAIN_VEC3F_DIV1000(scale, 100, ICHAIN_CONTINUE),
@@ -151,8 +155,8 @@ void BgJyaIronobj_SpawnPillarParticles(BgJyaIronobj* this, PlayState* play, EnIk
         vel.x = 2.0f * ((Rand_ZeroOne() * 6.0f) - 3.0f) + (Rand_ZeroOne() * sins * 8.0f * temp_f22);
         vel.y = (Rand_ZeroOne() * 8.0f) - 3.0f;
         vel.z = 2.0f * ((Rand_ZeroOne() * 6.0f) - 3.0f) + (Rand_ZeroOne() * coss * 8.0f * temp_f22);
-        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899500[j & 7], 4, 0, sPillarDebrisScales[j & 7], 0, 5,  // was D_808994E0 [LLM:HIGH]
-                             D_808994F0[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);
+        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, sPillarDebrisArg6[j & 7], 4, 0, sPillarDebrisScales[j & 7], 0, 5,  // was D_808994E0 [LLM:HIGH]
+                             sPillarDebrisLifetimes[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);  // was D_808994F0 [LLM:HIGH]
         if (Rand_ZeroOne() < 0.26f) {
             func_80033480(play, &pos, 200.0f, 1, sPillarDebrisScales[j & 7] * 4 + 60, sPillarDebrisScales[j & 7] * 4 + 80, 1);  // was D_808994E0 [LLM:HIGH]
         }
@@ -209,8 +213,8 @@ void BgJyaIronobj_SpawnThoneParticles(BgJyaIronobj* this, PlayState* play, EnIk*
         vel.x = 2.0f * (Rand_ZeroOne() * 6.0f - 3.0f) + (Rand_ZeroOne() * sins * 8.0f * temp_f22);
         vel.y = Rand_ZeroOne() * 8.0f - 3.0f;
         vel.z = 2.0f * (Rand_ZeroOne() * 6.0f - 3.0f) + (Rand_ZeroOne() * coss * 8.0f * temp_f22);
-        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899530[j & 7], 4, 0, sThroneDebrisScales[j & 7], 0, 5,  // was D_80899510 [LLM:HIGH]
-                             D_80899520[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);
+        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, sThroneDebrisArg6[j & 7], 4, 0, sThroneDebrisScales[j & 7], 0, 5,  // was D_80899510 [LLM:HIGH]
+                             sThroneDebrisLifetimes[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);  // was D_80899520 [LLM:HIGH]
         if (Rand_ZeroOne() < 0.26f) {
             func_80033480(play, &pos, 200.0f, 1, sThroneDebrisScales[j & 7] * 4 + 60, sThroneDebrisScales[j & 7] * 4 + 80, 1);  // was D_80899510 [LLM:HIGH]
         }
-- 
2.55.0

