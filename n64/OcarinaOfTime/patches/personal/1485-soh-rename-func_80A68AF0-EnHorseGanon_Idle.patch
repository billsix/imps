From 29a989c9d9cb7e4c61953c65213e6e494c295f28 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:22:45 +0000
Subject: [PATCH 1485/3799] soh: rename func_80A68AF0 -> EnHorseGanon_Idle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: sActionFuncs[0]: keeps the horse stopped and just advances its animation.
Defined in soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
index 176712e92..7fe648424 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
@@ -21,7 +21,8 @@ void EnHorseGanon_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80A68AC4: installs action 0 and loops the idle animation.
 void EnHorseGanon_SetupIdle(EnHorseGanon* this);  // was func_80A68AC4 [LLM:HIGH]
-void func_80A68AF0(EnHorseGanon* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A68AF0: sActionFuncs[0]: keeps the horse stopped and just advances its animation.
+void EnHorseGanon_Idle(EnHorseGanon* this, PlayState* play);  // was func_80A68AF0 [LLM:HIGH]
 void func_80A68DB0(EnHorseGanon* this, PlayState* play);
 
 const ActorInit En_Horse_Ganon_InitVars = {
@@ -109,7 +110,7 @@ static InitChainEntry sInitChain[] = {
     ICHAIN_F32(uncullZoneScale, 1200, ICHAIN_STOP),
 };
 
-static EnHorseGanonActionFunc sActionFuncs[] = { func_80A68AF0, func_80A68DB0 };
+static EnHorseGanonActionFunc sActionFuncs[] = { EnHorseGanon_Idle, func_80A68DB0 };  // was func_80A68AF0 [LLM:HIGH]
 
 // LLM generated name (HIGH), was func_80A68660: copies one sPathPoints entry's position into a Vec3f.
 void EnHorseGanon_GetPathPoint(unk_D_80A69248* data, s32 index, Vec3f* vec) {
@@ -210,7 +211,7 @@ void EnHorseGanon_SetupIdle(EnHorseGanon* this) {
     Animation_PlayLoop(&this->skin.skelAnime, sAnimations[4]);
 }
 
-void func_80A68AF0(EnHorseGanon* this, PlayState* play) {
+void EnHorseGanon_Idle(EnHorseGanon* this, PlayState* play) {
     this->actor.speedXZ = 0.0f;
     SkelAnime_Update(&this->skin.skelAnime);
 }
-- 
2.55.0

