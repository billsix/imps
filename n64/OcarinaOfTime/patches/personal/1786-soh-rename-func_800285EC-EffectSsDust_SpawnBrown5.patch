From 904d5f53d6966e20159801ce36b32e2c8dd3ab95 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:43:29 +0000
Subject: [PATCH 1786/3799] soh: rename func_800285EC ->
 EffectSsDust_SpawnBrown5

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the same with drawFlags 5.
Defined in soh/src/code/z_effect_soft_sprite_old_init.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                      | 2 +-
 soh/src/code/z_effect_soft_sprite_old_init.c | 3 ++-
 2 files changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index ff1f00992..1845e48a3 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -246,7 +246,7 @@ void EffectSsDust_SpawnDrawFlags0Mode1(PlayState* play, Vec3f* pos, Vec3f* veloc
 void EffectSsDust_SpawnDrawFlags1Mode1(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, Color_RGBA8* primColor,
                    Color_RGBA8* envColor, s16 scale, s16 scaleStep);  // was func_80028510 [LLM:HIGH]
 void EffectSsDust_SpawnBrown4(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);  // was func_8002857C [LLM:HIGH]
-void func_800285EC(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);
+void EffectSsDust_SpawnBrown5(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);  // was func_800285EC [LLM:HIGH]
 void func_8002865C(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, s16 scale, s16 scaleStep);
 void func_800286CC(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, s16 scale, s16 scaleStep);
 void func_8002873C(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, s16 scale, s16 scaleStep,
diff --git a/soh/src/code/z_effect_soft_sprite_old_init.c b/soh/src/code/z_effect_soft_sprite_old_init.c
index b9db0dfef..3d015bfe4 100644
--- a/soh/src/code/z_effect_soft_sprite_old_init.c
+++ b/soh/src/code/z_effect_soft_sprite_old_init.c
@@ -150,7 +150,8 @@ void EffectSsDust_SpawnBrown4(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3
 }
 
 // unused
-void func_800285EC(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel) {
+// LLM generated name (HIGH), was func_800285EC: the same with drawFlags 5.
+void EffectSsDust_SpawnBrown5(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel) {
     EffectSsDust_Spawn(play, 5, pos, velocity, accel, &sDustBrownPrim, &sDustBrownEnv, 100, 5, 10, 0);
 }
 
-- 
2.55.0

