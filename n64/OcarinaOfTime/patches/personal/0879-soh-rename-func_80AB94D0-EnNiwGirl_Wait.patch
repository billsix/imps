From 77995c6ea8e5dade525143f819958c91e4c81999 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:15:50 +0000
Subject: [PATCH 0879/3799] soh: rename func_80AB94D0 -> EnNiwGirl_Wait

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: action state entered from EnNiwGirl_Talk; decelerates to a stop, offers talk, and after jumpTimer expires hands off to EnNiwGirl_Jump.
Defined in soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c b/soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c
index 22e154e81..8a47adbe0 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Girl/z_en_niw_girl.c
@@ -16,7 +16,8 @@ void EnNiwGirl_Update(Actor* thisx, PlayState* play);
 void EnNiwGirl_Draw(Actor* thisx, PlayState* play);
 
 void EnNiwGirl_Talk(EnNiwGirl* this, PlayState* play);
-void func_80AB94D0(EnNiwGirl* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB94D0: action state entered from EnNiwGirl_Talk; decelerates to a stop, offers talk, and after jumpTimer expires hands off to EnNiwGirl_Jump.
+void EnNiwGirl_Wait(EnNiwGirl* this, PlayState* play);  // was func_80AB94D0 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80AB9210: action state entered from EnNiwGirl_Jump; accelerates to speedXZ 3.0, steers chasedEnNiw's path by distance and turns to face it.
 void EnNiwGirl_ChaseCucco(EnNiwGirl* this, PlayState* play);  // was func_80AB9210 [LLM:HIGH]
 
@@ -163,10 +164,10 @@ void EnNiwGirl_Talk(EnNiwGirl* this, PlayState* play) {
             break;
     }
     this->unk_270 = 6;
-    this->actionFunc = func_80AB94D0;
+    this->actionFunc = EnNiwGirl_Wait;  // was func_80AB94D0 [LLM:HIGH]
 }
 
-void func_80AB94D0(EnNiwGirl* this, PlayState* play) {
+void EnNiwGirl_Wait(EnNiwGirl* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (Message_GetState(&play->msgCtx) != TEXT_STATE_NONE) {
         this->chasedEnNiw->path = 0;
@@ -193,7 +194,7 @@ void EnNiwGirl_Update(Actor* thisx, PlayState* play) {
 
     Actor_SetScale(&this->actor, 0.013f);
     this->unkUpTimer++;
-    tempActionFunc = func_80AB94D0;
+    tempActionFunc = EnNiwGirl_Wait;  // was func_80AB94D0 [LLM:HIGH]
     if (this->blinkTimer == 0) {
         this->eyeIndex++;
         if (this->eyeIndex >= 3) {
-- 
2.55.0

