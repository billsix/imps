From 13189c27b3c98315d88293facaec65943320aac8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:22:45 +0000
Subject: [PATCH 1486/3799] soh: rename func_80A68B20 ->
 EnHorseGanon_SetupGallop

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: installs action 1 and picks the walk, trot or gallop animation from the current speed, scaling its playback rate to match and playing the running sfx for the faster two.
Defined in soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c    | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
index 7fe648424..64980b959 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
@@ -216,7 +216,8 @@ void EnHorseGanon_Idle(EnHorseGanon* this, PlayState* play) {
     SkelAnime_Update(&this->skin.skelAnime);
 }
 
-void func_80A68B20(EnHorseGanon* this) {
+// LLM generated name (HIGH), was func_80A68B20: installs action 1 and picks the walk, trot or gallop animation from the current speed, scaling its playback rate to match and playing the running sfx for the faster two.
+void EnHorseGanon_SetupGallop(EnHorseGanon* this) {
     s32 animationChanged;
     f32 sp30;
 
@@ -272,7 +273,7 @@ void func_80A68DB0(EnHorseGanon* this, PlayState* play) {
     EnHorseGanon_FollowPath(this, play);  // was func_80A686A8 [LLM:HIGH]
 
     if (SkelAnime_Update(&this->skin.skelAnime) != 0) {
-        func_80A68B20(this);
+        EnHorseGanon_SetupGallop(this);  // was func_80A68B20 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

