From 97d1da9f03259c8859166da7210dc782e5d2b846 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:00 +0000
Subject: [PATCH 1822/3799] soh: rename func_809A9E88 ->
 EffectSsKakera_ApplyAttraction

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the first force pass: pulls the fragment back toward its origin at one of two strengths, chosen by the low bits of the behaviour flags.
Defined in soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c  | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c b/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
index c24169c5c..da5a3e50d 100644
--- a/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
+++ b/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
@@ -226,7 +226,8 @@ f32 EffectSsKakera_DragSquared2(f32 arg0, s32 arg1) {
     return EffectSsKakera_DragSquared(arg0, arg1);  // was func_809A9E28 [LLM:HIGH]
 }
 
-s32 func_809A9E88(EffectSs* this, Vec3f* diff, f32 dist) {
+// LLM generated name (HIGH), was func_809A9E88: the first force pass: pulls the fragment back toward its origin at one of two strengths, chosen by the low bits of the behaviour flags.
+s32 EffectSsKakera_ApplyAttraction(EffectSs* this, Vec3f* diff, f32 dist) {
     static f32 D_809AA558[] = { 0.05f, 1.0f };
     s32 temp_v0;
     f32 phi_f0;
@@ -329,7 +330,7 @@ s32 func_809AA0EC(EffectSs* this) {
     }
 
     if (this->rReg0 != 0) {
-        if (!func_809A9E88(this, &diff, dist)) {
+        if (!EffectSsKakera_ApplyAttraction(this, &diff, dist)) {  // was func_809A9E88 [LLM:HIGH]
             return false;
         }
 
-- 
2.55.0

