From 00dae536fab222d73a0198f8c2dfdc58cb3a980b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:52:29 +0000
Subject: [PATCH 1149/3799] soh: rename func_80B9A668 ->
 ObjMure2_WaitForPlayerInRange

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spawns the whole group and disables update culling once the player's screen-space distance drops below the per-variant threshold.
Defined in soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c b/soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c
index a38aeb652..3816cd7ba 100644
--- a/soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c
+++ b/soh/src/overlays/actors/ovl_Obj_Mure2/z_obj_mure2.c
@@ -23,7 +23,8 @@ void ObjMure2_SetPosShrubCircle(Vec3f* vec, ObjMure2* this);
 void ObjMure2_SetPosShrubScattered(Vec3f* vec, ObjMure2* this);
 void ObjMure2_SetPosRockCircle(Vec3f* vec, ObjMure2* this);
 void ObjMure2_Wait(ObjMure2* this, PlayState* play);
-void func_80B9A668(ObjMure2* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B9A668: spawns the whole group and disables update culling once the player's screen-space distance drops below the per-variant threshold.
+void ObjMure2_WaitForPlayerInRange(ObjMure2* this, PlayState* play);  // was func_80B9A668 [LLM:HIGH]
 void func_80B9A6F8(ObjMure2* this, PlayState* play);
 void ObjMure2_SetupWait(ObjMure2* this);
 // LLM generated name (HIGH), was func_80B9A658: installs the action that watches for the player approaching.
@@ -188,10 +189,10 @@ void ObjMure2_Wait(ObjMure2* this, PlayState* play) {
 }
 
 void ObjMure2_SetupWaitForPlayerInRange(ObjMure2* this) {
-    this->actionFunc = func_80B9A668;
+    this->actionFunc = ObjMure2_WaitForPlayerInRange;  // was func_80B9A668 [LLM:HIGH]
 }
 
-void func_80B9A668(ObjMure2* this, PlayState* play) {
+void ObjMure2_WaitForPlayerInRange(ObjMure2* this, PlayState* play) {
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) <
         (sDistSquared1[this->actor.params & 3] * this->unk_184)) {
         this->actor.flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED;
-- 
2.55.0

