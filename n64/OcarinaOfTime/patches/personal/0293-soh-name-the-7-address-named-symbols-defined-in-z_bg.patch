From c7e5c92b04dfe0d865e9eba017f0411988e19ddf Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:23 +0000
Subject: [PATCH 293/488] soh: name the 7 address-named symbols defined in
 z_bg_spot12_saku.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 7 are defined in soh/src/overlays/actors/ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c.

  * func_808B3420 -> BgSpot12Saku_InitDynaPoly
    deduced, confidence HIGH: registers the gate as a dyna-poly bg actor
    with the given collision header, warning when the bg actor table is
    full.

  * func_808B3550 -> BgSpot12Saku_SetupWaitForSwitch
    deduced, confidence HIGH: snaps the gate to its closed scale and home
    position, then installs the waiting action.

  * func_808B357C -> BgSpot12Saku_WaitForSwitch
    deduced, confidence HIGH: waits for the GTG gate switch flag, then arms
    the 20-frame delay and starts the one-point cutscene that shows the
    gate.

  * func_808B35E4 -> BgSpot12Saku_SetupOpen
    deduced, confidence HIGH: installs the opening action once the delay
    timer has run out.

  * func_808B3604 -> BgSpot12Saku_Open
    deduced, confidence HIGH: shrinks scale.x toward the open value while
    sliding the gate sideways along its facing, looping the metal-gate sfx
    and finishing on the stop sfx.

  * func_808B3714 -> BgSpot12Saku_SetupOpened
    deduced, confidence HIGH: snaps the gate straight to the fully-open
    scale and offset; Init uses it when the switch flag is already set.

  * func_808B37AC -> BgSpot12Saku_DoNothing
    deduced, confidence HIGH: the empty terminal action installed once the
    gate is open.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c     | 49 +++++++++++--------
 1 file changed, 28 insertions(+), 21 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c b/soh/src/overlays/actors/ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c
index 7bc14f84a..b2c56a681 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot12_Saku/z_bg_spot12_saku.c
@@ -15,12 +15,18 @@ void BgSpot12Saku_Destroy(Actor* thisx, PlayState* play);
 void BgSpot12Saku_Update(Actor* thisx, PlayState* play);
 void BgSpot12Saku_Draw(Actor* thisx, PlayState* play);
 
-void func_808B3550(BgSpot12Saku* this);
-void func_808B357C(BgSpot12Saku* this, PlayState* play);
-void func_808B35E4(BgSpot12Saku* this);
-void func_808B3604(BgSpot12Saku* this, PlayState* play);
-void func_808B3714(BgSpot12Saku* this);
-void func_808B37AC(BgSpot12Saku* this, PlayState* play);
+// LLM generated name (HIGH), was func_808B3550: snaps the gate to its closed scale and home position, then installs the waiting action.
+void BgSpot12Saku_SetupWaitForSwitch(BgSpot12Saku* this);  // was func_808B3550 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808B357C: waits for the GTG gate switch flag, then arms the 20-frame delay and starts the one-point cutscene that shows the gate.
+void BgSpot12Saku_WaitForSwitch(BgSpot12Saku* this, PlayState* play);  // was func_808B357C [LLM:HIGH]
+// LLM generated name (HIGH), was func_808B35E4: installs the opening action once the delay timer has run out.
+void BgSpot12Saku_SetupOpen(BgSpot12Saku* this);  // was func_808B35E4 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808B3604: shrinks scale.x toward the open value while sliding the gate sideways along its facing, looping the metal-gate sfx and finishing on the stop sfx.
+void BgSpot12Saku_Open(BgSpot12Saku* this, PlayState* play);  // was func_808B3604 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808B3714: snaps the gate straight to the fully-open scale and offset; Init uses it when the switch flag is already set.
+void BgSpot12Saku_SetupOpened(BgSpot12Saku* this);  // was func_808B3714 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808B37AC: the empty terminal action installed once the gate is open.
+void BgSpot12Saku_DoNothing(BgSpot12Saku* this, PlayState* play);  // was func_808B37AC [LLM:HIGH]
 
 const ActorInit Bg_Spot12_Saku_InitVars = {
     ACTOR_BG_SPOT12_SAKU,
@@ -42,7 +48,8 @@ static InitChainEntry sInitChain[] = {
     ICHAIN_F32(uncullZoneDownward, 1000, ICHAIN_STOP),
 };
 
-void func_808B3420(BgSpot12Saku* this, PlayState* play, CollisionHeader* collision, s32 flags) {
+// LLM generated name (HIGH), was func_808B3420: registers the gate as a dyna-poly bg actor with the given collision header, warning when the bg actor table is full.
+void BgSpot12Saku_InitDynaPoly(BgSpot12Saku* this, PlayState* play, CollisionHeader* collision, s32 flags) {
     s32 pad;
     CollisionHeader* colHeader = NULL;
     s32 pad2;
@@ -59,12 +66,12 @@ void func_808B3420(BgSpot12Saku* this, PlayState* play, CollisionHeader* collisi
 void BgSpot12Saku_Init(Actor* thisx, PlayState* play) {
     BgSpot12Saku* this = (BgSpot12Saku*)thisx;
 
-    func_808B3420(this, play, &gGerudoFortressGTGShutterCol, DPM_UNK);
+    BgSpot12Saku_InitDynaPoly(this, play, &gGerudoFortressGTGShutterCol, DPM_UNK);  // was func_808B3420 [LLM:HIGH]
     Actor_ProcessInitChain(&this->dyna.actor, sInitChain);
     if (GameInteractor_Should(VB_GTG_GATE_BE_OPEN, Flags_GetSwitch(play, this->dyna.actor.params & 0x3F))) {
-        func_808B3714(this);
+        BgSpot12Saku_SetupOpened(this);  // was func_808B3714 [LLM:HIGH]
     } else {
-        func_808B3550(this);
+        BgSpot12Saku_SetupWaitForSwitch(this);  // was func_808B3550 [LLM:HIGH]
     }
 }
 
@@ -74,28 +81,28 @@ void BgSpot12Saku_Destroy(Actor* thisx, PlayState* play) {
     DynaPoly_DeleteBgActor(play, &play->colCtx.dyna, this->dyna.bgId);
 }
 
-void func_808B3550(BgSpot12Saku* this) {
-    this->actionFunc = func_808B357C;
+void BgSpot12Saku_SetupWaitForSwitch(BgSpot12Saku* this) {
+    this->actionFunc = BgSpot12Saku_WaitForSwitch;  // was func_808B357C [LLM:HIGH]
     this->dyna.actor.scale.x = 0.1f;
     this->dyna.actor.world.pos.x = this->dyna.actor.home.pos.x;
     this->dyna.actor.world.pos.z = this->dyna.actor.home.pos.z;
 }
 
-void func_808B357C(BgSpot12Saku* this, PlayState* play) {
+void BgSpot12Saku_WaitForSwitch(BgSpot12Saku* this, PlayState* play) {
     if (GameInteractor_Should(VB_GTG_GATE_BE_OPEN, Flags_GetSwitch(play, this->dyna.actor.params & 0x3F))) {
-        func_808B35E4(this);
+        BgSpot12Saku_SetupOpen(this);  // was func_808B35E4 [LLM:HIGH]
         this->timer = 20;
         OnePointCutscene_Init(play, 4170, -99, &this->dyna.actor, CAM_ID_MAIN);
     }
 }
 
-void func_808B35E4(BgSpot12Saku* this) {
+void BgSpot12Saku_SetupOpen(BgSpot12Saku* this) {
     if (this->timer == 0) {
-        this->actionFunc = func_808B3604;
+        this->actionFunc = BgSpot12Saku_Open;  // was func_808B3604 [LLM:HIGH]
     }
 }
 
-void func_808B3604(BgSpot12Saku* this, PlayState* play) {
+void BgSpot12Saku_Open(BgSpot12Saku* this, PlayState* play) {
     f32 temp_ret = Math_SmoothStepToF(&this->dyna.actor.scale.x, 0.001f / 0.14f, 0.16f, 0.0022f, 0.001f);
     f32 temp_f18 = ((0.1f - this->dyna.actor.scale.x) * 840.0f);
 
@@ -104,15 +111,15 @@ void func_808B3604(BgSpot12Saku* this, PlayState* play) {
     this->dyna.actor.world.pos.z =
         this->dyna.actor.home.pos.z - (Math_CosS(this->dyna.actor.shape.rot.y + 0x4000) * temp_f18);
     if (fabsf(temp_ret) < 0.0001f) {
-        func_808B3714(this);
+        BgSpot12Saku_SetupOpened(this);  // was func_808B3714 [LLM:HIGH]
         Audio_PlayActorSound2(&this->dyna.actor, NA_SE_EV_BRIDGE_OPEN_STOP);
     } else {
         Actor_PlaySfx_Flagged(&this->dyna.actor, NA_SE_EV_METALGATE_OPEN - SFX_FLAG);
     }
 }
 
-void func_808B3714(BgSpot12Saku* this) {
-    this->actionFunc = func_808B37AC;
+void BgSpot12Saku_SetupOpened(BgSpot12Saku* this) {
+    this->actionFunc = BgSpot12Saku_DoNothing;  // was func_808B37AC [LLM:HIGH]
     this->dyna.actor.scale.x = 0.001f / 0.14f;
     this->dyna.actor.world.pos.x =
         this->dyna.actor.home.pos.x - (Math_SinS(this->dyna.actor.shape.rot.y + 0x4000) * 78.0f);
@@ -120,7 +127,7 @@ void func_808B3714(BgSpot12Saku* this) {
         this->dyna.actor.home.pos.z - (Math_CosS(this->dyna.actor.shape.rot.y + 0x4000) * 78.0f);
 }
 
-void func_808B37AC(BgSpot12Saku* this, PlayState* play) {
+void BgSpot12Saku_DoNothing(BgSpot12Saku* this, PlayState* play) {
 }
 
 void BgSpot12Saku_Update(Actor* thisx, PlayState* play) {
-- 
2.55.0

