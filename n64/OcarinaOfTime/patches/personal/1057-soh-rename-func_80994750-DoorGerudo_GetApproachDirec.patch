From 47b0206b65fc77b837a001b45a553af99ab9078a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:39:39 +0000
Subject: [PATCH 1057/3799] soh: rename func_80994750 ->
 DoorGerudo_GetApproachDirection

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: returns 1 or -1 for the side the player is standing on when they are within 40 units of the door plane and facing it, and 0 otherwise.
Defined in soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c b/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
index 9d08cd7f3..330eccaef 100644
--- a/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
+++ b/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
@@ -16,7 +16,8 @@ void DoorGerudo_Update(Actor* thisx, PlayState* play);
 void DoorGerudo_Draw(Actor* thisx, PlayState* play);
 
 void func_8099485C(DoorGerudo* this, PlayState* play);
-s32 func_80994750(DoorGerudo* this, PlayState* play);
+// LLM generated name (HIGH), was func_80994750: returns 1 or -1 for the side the player is standing on when they are within 40 units of the door plane and facing it, and 0 otherwise.
+s32 DoorGerudo_GetApproachDirection(DoorGerudo* this, PlayState* play);  // was func_80994750 [LLM:HIGH]
 void func_8099496C(DoorGerudo* this, PlayState* play);
 void func_809949C8(DoorGerudo* this, PlayState* play);
 
@@ -79,7 +80,7 @@ f32 DoorGerudo_GetPlayerLocalZ(PlayState* play, DoorGerudo* this, f32 arg2, f32
     }
 }
 
-s32 func_80994750(DoorGerudo* this, PlayState* play) {
+s32 DoorGerudo_GetApproachDirection(DoorGerudo* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     f32 temp_f0;
     s16 rotYDiff;
@@ -107,7 +108,7 @@ void func_8099485C(DoorGerudo* this, PlayState* play) {
         Audio_PlayActorSound2(&this->dyna.actor, NA_SE_EV_CHAIN_KEY_UNLOCK);
         GameInteractor_ExecuteOnDungeonKeyUsedHooks(gSaveContext.mapIndex);
     } else {
-        s32 direction = func_80994750(this, play);
+        s32 direction = DoorGerudo_GetApproachDirection(this, play);  // was func_80994750 [LLM:HIGH]
 
         if (direction != 0) {
             Player* player = GET_PLAYER(play);
-- 
2.55.0

