From f696615c3fcae88892fec6b89007f678b25aa1bc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:02 +0000
Subject: [PATCH 1408/3799] soh: rename func_80AAE294 -> EnMm_Run

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the running action: steps the animation with footstep sfx, follows the path and, once night falls, breaks off to sit down.
Defined in soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
index f5b844309..5b7d681c4 100644
--- a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
+++ b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
@@ -31,7 +31,8 @@ void EnMm_Update(Actor* thisx, PlayState* play);
 void EnMm_Draw(Actor* thisx, PlayState* play);
 
 void func_80AAE598(EnMm* this, PlayState* play);
-void func_80AAE294(EnMm* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AAE294: the running action: steps the animation with footstep sfx, follows the path and, once night falls, breaks off to sit down.
+void EnMm_Run(EnMm* this, PlayState* play);  // was func_80AAE294 [LLM:HIGH]
 void func_80AAE50C(EnMm* this, PlayState* play);
 // LLM generated name (HIGH), was func_80AAE224: waits for the current animation to finish, then parks the Running Man in the sitting-idle animation with his mouth closed and hands off to the sitting action.
 void EnMm_SitDown(EnMm* this, PlayState* play);  // was func_80AAE224 [LLM:HIGH]
@@ -191,7 +192,7 @@ void EnMm_Init(Actor* thisx, PlayState* play) {
     } else {
         this->mouthTexIndex = RM_MOUTH_CLOSED;
         EnMm_ChangeAnim(this, RM_ANIM_SIT_WAIT, &this->curAnimIndex);
-        this->actionFunc = func_80AAE294;
+        this->actionFunc = EnMm_Run;  // was func_80AAE294 [LLM:HIGH]
     }
 }
 
@@ -413,7 +414,7 @@ void EnMm_SitDown(EnMm* this, PlayState* play) {
     }
 }
 
-void func_80AAE294(EnMm* this, PlayState* play) {
+void EnMm_Run(EnMm* this, PlayState* play) {
     f32 floorYNorm;
     Vec3f dustPos;
 
@@ -480,7 +481,7 @@ void func_80AAE294(EnMm* this, PlayState* play) {
 void func_80AAE50C(EnMm* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->sitTimer = 0;
-        this->actionFunc = func_80AAE294;
+        this->actionFunc = EnMm_Run;  // was func_80AAE294 [LLM:HIGH]
 
         if (Flags_GetItemGetInf(ITEMGETINF_3B)) {
             EnMm_ChangeAnim(this, RM_ANIM_SPRINT, &this->curAnimIndex);
-- 
2.55.0

