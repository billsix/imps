From 6f1de47fead98f9fcfcf9988ea2e543cbd8c07f8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:05:51 +0000
Subject: [PATCH 1260/3799] soh: rename func_808E229C ->
 BossGanon_GatheringMagic_Draw

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the matching draw, rendering the fifteen-point trail those children record; named to pair with the update.
Defined in soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
index 952c551f5..28e3365a7 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
@@ -31,7 +31,8 @@ void BossGanon_GatheringMagic_Update(Actor* thisx, PlayState* play); // update
 // LLM generated name (HIGH), was func_808E2544: the update installed for the params 0xFA and 0x104 children, which the in-tree comments at those branches call the "big magic light ball charge" and "big magic light ball thrown"; it sits alongside the already-named BossGanon_LightBall_Update.
 void BossGanon_BigLightBall_Update(Actor* thisx, PlayState* play); // update
 void BossGanon_LightBall_Update(Actor* thisx, PlayState* play);
-void func_808E229C(Actor* thisx, PlayState* play); // draw
+// LLM generated name (GUESS), was func_808E229C: the matching draw, rendering the fifteen-point trail those children record; named to pair with the update.
+void BossGanon_GatheringMagic_Draw(Actor* thisx, PlayState* play); // draw
 // LLM generated name (HIGH), was func_808E324C: the matching draw for those same children.
 void BossGanon_BigLightBall_Draw(Actor* thisx, PlayState* play); // draw
 void BossGanon_LightBall_Draw(Actor* thisx, PlayState* play);
@@ -438,7 +439,7 @@ void BossGanon_Init(Actor* thisx, PlayState* play2) {
                 this->fwork[GDF_FWORK_1] = 0;
             } else {
                 thisx->update = BossGanon_GatheringMagic_Update;  // was func_808E1EB4 [LLM:GUESS]
-                thisx->draw = func_808E229C;
+                thisx->draw = BossGanon_GatheringMagic_Draw;  // was func_808E229C [LLM:GUESS]
                 thisx->speedXZ = 11.0f;
 
                 if (thisx->params == 0xC8) {
@@ -4357,7 +4358,7 @@ void BossGanon_GatheringMagic_Update(Actor* thisx, PlayState* play2) {
     }
 }
 
-void func_808E229C(Actor* thisx, PlayState* play2) {
+void BossGanon_GatheringMagic_Draw(Actor* thisx, PlayState* play2) {
     BossGanon* this = (BossGanon*)thisx;
     PlayState* play = play2;
     s16 i;
-- 
2.55.0

