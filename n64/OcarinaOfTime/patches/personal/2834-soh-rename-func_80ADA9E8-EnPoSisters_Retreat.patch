From 8d69a5d18a5b00aab2efb7309c7ad5778447f360 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:53:56 +0000
Subject: [PATCH 2834/3799] soh: rename func_80ADA9E8 -> EnPoSisters_Retreat

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: backs away with the float animation and a softened collider
Defined in soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c    | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
index d88c24eea..0db946a74 100644
--- a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
+++ b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
@@ -32,7 +32,8 @@ void EnPoSisters_Circle(EnPoSisters* this, PlayState* play);  // was func_80ADA6
 void EnPoSisters_Attack(EnPoSisters* this, PlayState* play);  // was func_80ADA7F0 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ADA8C0: drives straight at the player at speed 5
 void EnPoSisters_Charge(EnPoSisters* this, PlayState* play);  // was func_80ADA8C0 [LLM:HIGH]
-void func_80ADA9E8(EnPoSisters* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ADA9E8: backs away with the float animation and a softened collider
+void EnPoSisters_Retreat(EnPoSisters* this, PlayState* play);  // was func_80ADA9E8 [LLM:HIGH]
 void func_80ADAAA4(EnPoSisters* this, PlayState* play);
 void func_80ADAC70(EnPoSisters* this, PlayState* play);
 void func_80ADAD54(EnPoSisters* this, PlayState* play);
@@ -318,7 +319,7 @@ void EnPoSisters_SetupRetreat(EnPoSisters* this) {
         this->collider.base.colType = COLTYPE_HIT3;
         this->collider.base.acFlags &= ~AC_HARD;
     }
-    this->actionFunc = func_80ADA9E8;
+    this->actionFunc = EnPoSisters_Retreat;  // was func_80ADA9E8 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_80AD95D8: plays gPoeSistersDamagedAnim, aims the knockback away from the attacker and applies the red filter
@@ -724,7 +725,7 @@ void EnPoSisters_Charge(EnPoSisters* this, PlayState* play) {
     }
 }
 
-void func_80ADA9E8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Retreat(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     this->actor.shape.rot.y -= (this->actor.speedXZ * 10.0f) * 128.0f;
     if (Math_StepToF(&this->actor.speedXZ, 0.0f, 0.1f) != 0) {
-- 
2.55.0

