From 265054aec0b0be7919c050eff0517c487446f1d7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:23 +0000
Subject: [PATCH 291/488] soh: name the 7 address-named symbols defined in
 z_bg_bombwall.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 7 are defined in soh/src/overlays/actors/ovl_Bg_Bombwall/z_bg_bombwall.c; updating their references touches 2 files in total.

  * D_8086F010 -> sDustOffsets
    deduced, confidence HIGH: seven local-space offsets rotated by the
    wall's yaw and used as the spawn points for the dust clouds thrown when
    it blows apart.

  * func_8086EB5C -> BgBombwall_SpawnDust
    deduced, confidence HIGH: spawns a dust cloud with randomised size at
    each of sDustOffsets, plus one larger cloud above the wall's centre.

  * func_8086ED50 -> BgBombwall_SetupWaitForBomb
    deduced, confidence HIGH: selects the intact display list and installs
    the waiting action.

  * func_8086ED70 -> BgBombwall_WaitForBomb
    deduced, confidence HIGH: runs the AC check while the player is within
    600 units and, on a hit, sets the switch flag and starts the break.

  * func_8086EDFC -> BgBombwall_SetupBreak
    deduced, confidence HIGH: throws the dust, arms the one-frame delay and
    installs the breaking action.

  * func_8086EE40 -> BgBombwall_Break
    deduced, confidence HIGH: waits out that delay, then swaps in the broken
    wall and plays the correct-chime for the params variant that has one.

  * func_8086EE94 -> BgBombwall_SetupBroken
    deduced, confidence HIGH: selects the broken display list, tears down
    the collision and clears actionFunc so nothing runs again.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/soh/Network/Anchor/HookHandlers.cpp       |  4 +-
 .../actors/ovl_Bg_Bombwall/z_bg_bombwall.c    | 53 +++++++++++--------
 2 files changed, 32 insertions(+), 25 deletions(-)

diff --git a/soh/soh/Network/Anchor/HookHandlers.cpp b/soh/soh/Network/Anchor/HookHandlers.cpp
index 4b0532649..1adc478ea 100644
--- a/soh/soh/Network/Anchor/HookHandlers.cpp
+++ b/soh/soh/Network/Anchor/HookHandlers.cpp
@@ -32,7 +32,7 @@ extern "C" {
 #include "objects/gameplay_keep/gameplay_keep.h"
 extern PlayState* gPlayState;
 extern MapData* gMapData;
-void func_8086ED70(BgBombwall* bgBombwall, PlayState* play);
+void BgBombwall_WaitForBomb(BgBombwall* bgBombwall, PlayState* play);  // was func_8086ED70 [LLM:HIGH]
 void BgBreakwall_Wait(BgBreakwall* bgBreakwall, PlayState* play);
 void BgHakaZou_WaitForHit(BgHakaZou* bgHakaZou, PlayState* play);
 void func_808887C4(BgHidanHamstep* bgHidanHamstep, PlayState* play);
@@ -215,7 +215,7 @@ void Anchor::RegisterHooks() {
     COND_ID_HOOK(ShouldActorUpdate, ACTOR_BG_BOMBWALL, isConnected, [&](void* refActor, bool* should) {
         BgBombwall* actor = static_cast<BgBombwall*>(refActor);
 
-        if (actor->actionFunc == func_8086ED70 && Flags_GetSwitch(gPlayState, actor->dyna.actor.params & 0x3F)) {
+        if (actor->actionFunc == BgBombwall_WaitForBomb && Flags_GetSwitch(gPlayState, actor->dyna.actor.params & 0x3F)) {  // was func_8086ED70 [LLM:HIGH]
             actor->collider.base.acFlags |= AC_HIT;
         }
     });
diff --git a/soh/src/overlays/actors/ovl_Bg_Bombwall/z_bg_bombwall.c b/soh/src/overlays/actors/ovl_Bg_Bombwall/z_bg_bombwall.c
index c75d9d1a3..31cb947e5 100644
--- a/soh/src/overlays/actors/ovl_Bg_Bombwall/z_bg_bombwall.c
+++ b/soh/src/overlays/actors/ovl_Bg_Bombwall/z_bg_bombwall.c
@@ -14,11 +14,16 @@ void BgBombwall_Destroy(Actor* thisx, PlayState* play);
 void BgBombwall_Update(Actor* thisx, PlayState* play);
 void BgBombwall_Draw(Actor* thisx, PlayState* play);
 
-void func_8086ED50(BgBombwall* this, PlayState* play);
-void func_8086ED70(BgBombwall* this, PlayState* play);
-void func_8086EDFC(BgBombwall* this, PlayState* play);
-void func_8086EE40(BgBombwall* this, PlayState* play);
-void func_8086EE94(BgBombwall* this, PlayState* play);
+// LLM generated name (HIGH), was func_8086ED50: selects the intact display list and installs the waiting action.
+void BgBombwall_SetupWaitForBomb(BgBombwall* this, PlayState* play);  // was func_8086ED50 [LLM:HIGH]
+// LLM generated name (HIGH), was func_8086ED70: runs the AC check while the player is within 600 units and, on a hit, sets the switch flag and starts the break.
+void BgBombwall_WaitForBomb(BgBombwall* this, PlayState* play);  // was func_8086ED70 [LLM:HIGH]
+// LLM generated name (HIGH), was func_8086EDFC: throws the dust, arms the one-frame delay and installs the breaking action.
+void BgBombwall_SetupBreak(BgBombwall* this, PlayState* play);  // was func_8086EDFC [LLM:HIGH]
+// LLM generated name (HIGH), was func_8086EE40: waits out that delay, then swaps in the broken wall and plays the correct-chime for the params variant that has one.
+void BgBombwall_Break(BgBombwall* this, PlayState* play);  // was func_8086EE40 [LLM:HIGH]
+// LLM generated name (HIGH), was func_8086EE94: selects the broken display list, tears down the collision and clears actionFunc so nothing runs again.
+void BgBombwall_SetupBroken(BgBombwall* this, PlayState* play);  // was func_8086EE94 [LLM:HIGH]
 
 static ColliderTrisElementInit sTrisElementsInit[3] = {
     {
@@ -121,7 +126,7 @@ void BgBombwall_Init(Actor* thisx, PlayState* play) {
     Actor_SetScale(&this->dyna.actor, 0.1f);
 
     if (Flags_GetSwitch(play, this->dyna.actor.params & 0x3F)) {
-        func_8086EE94(this, play);
+        BgBombwall_SetupBroken(this, play);  // was func_8086EE94 [LLM:HIGH]
     } else {
         BgBombwall_InitDynapoly(this, play);
         this->unk_2A2 |= 2;
@@ -144,7 +149,7 @@ void BgBombwall_Init(Actor* thisx, PlayState* play) {
         }
 
         this->unk_2A2 |= 1;
-        func_8086ED50(this, play);
+        BgBombwall_SetupWaitForBomb(this, play);  // was func_8086ED50 [LLM:HIGH]
     }
 
     osSyncPrintf("(field keep 汎用爆弾壁)(arg_data 0x%04x)(angY %d)\n", this->dyna.actor.params,
@@ -169,11 +174,13 @@ void BgBombwall_Destroy(Actor* thisx, PlayState* play) {
     BgBombwall_DestroyCollision(this, play);
 }
 
-static Vec3s D_8086F010[] = {
+// LLM generated name (HIGH), was D_8086F010: seven local-space offsets rotated by the wall's yaw and used as the spawn points for the dust clouds thrown when it blows apart.
+static Vec3s sDustOffsets[] = {  // was D_8086F010 [LLM:HIGH]
     { 40, 85, 21 }, { -43, 107, 14 }, { -1, 142, 14 }, { -27, 44, 27 }, { 28, 24, 20 }, { -39, 54, 21 }, { 49, 50, 20 },
 };
 
-void func_8086EB5C(BgBombwall* this, PlayState* play) {
+// LLM generated name (HIGH), was func_8086EB5C: spawns a dust cloud with randomised size at each of sDustOffsets, plus one larger cloud above the wall's centre.
+void BgBombwall_SpawnDust(BgBombwall* this, PlayState* play) {
     s16 rand;
     s16 rand2;
     Vec3f sp88;
@@ -185,11 +192,11 @@ void func_8086EB5C(BgBombwall* this, PlayState* play) {
     f32 new_var;
 
     for (i = 0; i < 7; i++) {
-        new_var = D_8086F010[i].x;
+        new_var = sDustOffsets[i].x;  // was D_8086F010 [LLM:HIGH]
         temp = new_var * cos;
-        sp88.x = ((sin * D_8086F010[i].z) + ((f32)temp)) + pos->x;
-        sp88.y = pos->y + D_8086F010[i].y;
-        sp88.z = ((D_8086F010[i].z * cos) - (sin * D_8086F010[i].x)) + pos->z;
+        sp88.x = ((sin * sDustOffsets[i].z) + ((f32)temp)) + pos->x;  // was D_8086F010 [LLM:HIGH]
+        sp88.y = pos->y + sDustOffsets[i].y;  // was D_8086F010 [LLM:HIGH]
+        sp88.z = ((sDustOffsets[i].z * cos) - (sin * sDustOffsets[i].x)) + pos->z;  // was D_8086F010 [LLM:HIGH]
         rand = ((s16)(Rand_ZeroOne() * 120.0f)) + 0x14;
         rand2 = ((s16)(Rand_ZeroOne() * 240.0f)) + 0x14;
         func_80033480(play, &sp88, 50.0f, 2, rand, rand2, 1);
@@ -202,33 +209,33 @@ void func_8086EB5C(BgBombwall* this, PlayState* play) {
     func_80033480(play, &sp88, 40.0f, 4, 0xA, 0x32, 1);
 }
 
-void func_8086ED50(BgBombwall* this, PlayState* play) {
+void BgBombwall_SetupWaitForBomb(BgBombwall* this, PlayState* play) {
     this->dList = gBgBombwallNormalDL;
-    this->actionFunc = func_8086ED70;
+    this->actionFunc = BgBombwall_WaitForBomb;  // was func_8086ED70 [LLM:HIGH]
 }
 
-void func_8086ED70(BgBombwall* this, PlayState* play) {
+void BgBombwall_WaitForBomb(BgBombwall* this, PlayState* play) {
     if (this->collider.base.acFlags & AC_HIT) {
         this->collider.base.acFlags &= ~AC_HIT;
-        func_8086EDFC(this, play);
+        BgBombwall_SetupBreak(this, play);  // was func_8086EDFC [LLM:HIGH]
         Flags_SetSwitch(play, this->dyna.actor.params & 0x3F);
     } else if (this->dyna.actor.xzDistToPlayer < 600.0f) {
         CollisionCheck_SetAC(play, &play->colChkCtx, &this->collider.base);
     }
 }
 
-void func_8086EDFC(BgBombwall* this, PlayState* play) {
+void BgBombwall_SetupBreak(BgBombwall* this, PlayState* play) {
     this->dList = gBgBombwallNormalDL;
     this->unk_2A0 = 1;
-    func_8086EB5C(this, play);
-    this->actionFunc = func_8086EE40;
+    BgBombwall_SpawnDust(this, play);  // was func_8086EB5C [LLM:HIGH]
+    this->actionFunc = BgBombwall_Break;  // was func_8086EE40 [LLM:HIGH]
 }
 
-void func_8086EE40(BgBombwall* this, PlayState* play) {
+void BgBombwall_Break(BgBombwall* this, PlayState* play) {
     if (this->unk_2A0 > 0) {
         this->unk_2A0--;
     } else {
-        func_8086EE94(this, play);
+        BgBombwall_SetupBroken(this, play);  // was func_8086EE94 [LLM:HIGH]
 
         if (((this->dyna.actor.params >> 0xF) & 1) != 0) {
             Sfx_PlaySfxCentered(NA_SE_SY_CORRECT_CHIME);
@@ -236,7 +243,7 @@ void func_8086EE40(BgBombwall* this, PlayState* play) {
     }
 }
 
-void func_8086EE94(BgBombwall* this, PlayState* play) {
+void BgBombwall_SetupBroken(BgBombwall* this, PlayState* play) {
     this->dList = gBgBombwallBrokenDL;
     BgBombwall_DestroyCollision(this, play);
     this->actionFunc = NULL;
-- 
2.55.0

