From 7e2a907da705c4c7929b1ac3c84279354bce68f6 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:39:21 +0000
Subject: [PATCH 1726/3799] soh: rename func_808B40AC -> BgSpot15Rrbox_Idle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: watches for the player pushing: it starts a slide when the push is allowed and unobstructed, and otherwise cancels the player's push.
Defined in soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c         | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
index 3a1a05d20..c72799924 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
@@ -16,7 +16,8 @@ void BgSpot15Rrbox_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_808B4084: stops the crate dead and installs the idle action.
 void BgSpot15Rrbox_SetupIdle(BgSpot15Rrbox* this, PlayState* play);  // was func_808B4084 [LLM:HIGH]
-void func_808B40AC(BgSpot15Rrbox* this, PlayState* play);
+// LLM generated name (HIGH), was func_808B40AC: watches for the player pushing: it starts a slide when the push is allowed and unobstructed, and otherwise cancels the player's push.
+void BgSpot15Rrbox_Idle(BgSpot15Rrbox* this, PlayState* play);  // was func_808B40AC [LLM:HIGH]
 void func_808B4194(BgSpot15Rrbox* this, PlayState* play);
 void func_808B4380(BgSpot15Rrbox* this, PlayState* play);
 void func_808B43D0(BgSpot15Rrbox* this, PlayState* play);
@@ -233,14 +234,14 @@ s32 BgSpot15Rrbox_IsPathBlocked(BgSpot15Rrbox* this, PlayState* play) {
 }
 
 void BgSpot15Rrbox_SetupIdle(BgSpot15Rrbox* this, PlayState* play) {
-    this->actionFunc = func_808B40AC;
+    this->actionFunc = BgSpot15Rrbox_Idle;  // was func_808B40AC [LLM:HIGH]
     this->dyna.actor.gravity = 0.0f;
     this->dyna.actor.velocity.x = 0.0f;
     this->dyna.actor.velocity.y = 0.0f;
     this->dyna.actor.velocity.z = 0.0f;
 }
 
-void func_808B40AC(BgSpot15Rrbox* this, PlayState* play) {
+void BgSpot15Rrbox_Idle(BgSpot15Rrbox* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     if (this->unk_168 <= 0 && fabsf(this->dyna.unk_150) > 0.001f) {
-- 
2.55.0

