From 9514177cdda27ee125f1860ec6da5a62fa83a191 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:27 +0000
Subject: [PATCH 2409/3799] soh: rename func_809BDF34 -> EnBigokuta_Bounce

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: arcs the body 100 units up and back down on a sine, splashing on landing before resuming the circle
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../Enhancements/TimeSavers/timesaver_hook_handlers.cpp    | 2 +-
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c    | 7 ++++---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.h    | 2 +-
 3 files changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
index 932dc984d..8ab728b2d 100644
--- a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
+++ b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
@@ -1192,7 +1192,7 @@ void TimeSaverOnActorInitHandler(void* actorRef) {
                         enBigokutaUpdateHook = 0;
                         enBigokutaKillHook = 0;
                         // Possible action functions after taken damage
-                    } else if (enBigokuta->actionFunc == func_809BE058 || enBigokuta->actionFunc == func_809BDF34 ||
+                    } else if (enBigokuta->actionFunc == func_809BE058 || enBigokuta->actionFunc == EnBigokuta_Bounce ||  // was func_809BDF34 [LLM:GUESS]
                                enBigokuta->actionFunc == func_809BE180) {
                         enBigokuta->actor.home.pos.y = enBigokuta->actor.world.pos.y = -1025.0f;
                         Actor_ChangeCategory(gPlayState, &gPlayState->actorCtx, &enBigokuta->actor, ACTORCAT_ENEMY);
diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index b908ab698..a6516c748 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -15,7 +15,8 @@ void EnBigokuta_Draw(Actor* thisx, PlayState* play);
 void EnBigokuta_SetupIntroWait(EnBigokuta* this);  // was func_809BD318 [LLM:HIGH]
 // LLM generated name (HIGH), was func_809BD3E0: arms the 40-frame pause before the circling begins
 void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this);  // was func_809BD3E0 [LLM:HIGH]
-void func_809BDF34(EnBigokuta* this, PlayState* play);
+// LLM generated name (GUESS), was func_809BDF34: arcs the body 100 units up and back down on a sine, splashing on landing before resuming the circle
+void EnBigokuta_Bounce(EnBigokuta* this, PlayState* play);  // was func_809BDF34 [LLM:GUESS]
 // LLM generated name (HIGH), was func_809BD84C: counts the intro wait down, calling out twice and starting the miniboss music
 void EnBigokuta_IntroWait(EnBigokuta* this, PlayState* play);  // was func_809BD84C [LLM:HIGH]
 // LLM generated name (HIGH), was func_809BD8DC: arcs the octopus out of the water onto the ring, splashing and rumbling on landing
@@ -334,7 +335,7 @@ void EnBigokuta_SetupBounce(EnBigokuta* this) {
     this->unk_196 = 16;
     this->collider.base.acFlags &= ~AC_ON;
     this->actor.colorFilterTimer = 0;
-    this->actionFunc = func_809BDF34;
+    this->actionFunc = EnBigokuta_Bounce;  // was func_809BDF34 [LLM:GUESS]
 }
 
 // LLM generated name (HIGH), was func_809BD4A4: records the opposite heading in world.rot.x and morphs to the idle loop while it turns
@@ -571,7 +572,7 @@ void EnBigokuta_Circle(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BDF34(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_Bounce(EnBigokuta* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (this->unk_196 != 0) {
         this->unk_196--;
diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.h b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.h
index 4748a8ea7..c844b20c6 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.h
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.h
@@ -25,7 +25,7 @@ typedef struct EnBigokuta {
 } EnBigokuta; // size = 0x0384
 
 void func_809BE058(EnBigokuta*, PlayState*);
-void func_809BDF34(EnBigokuta*, PlayState*);
+void EnBigokuta_Bounce(EnBigokuta*, PlayState*);  // was func_809BDF34 [LLM:GUESS]
 void func_809BE180(EnBigokuta*, PlayState*);
 
 #endif
-- 
2.55.0

