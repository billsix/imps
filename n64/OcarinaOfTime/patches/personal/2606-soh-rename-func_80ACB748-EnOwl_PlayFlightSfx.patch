From 53b6e1db171efd6049a2e73c0ac304cee79a4fbb Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:11 +0000
Subject: [PATCH 2606/3799] soh: rename func_80ACB748 -> EnOwl_PlayFlightSfx

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: scales the wind, flutter and pass-by sounds by how fast the camera is moving during the travel cutscenes
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index 6e78a4911..f61c8c3ae 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -757,7 +757,8 @@ void EnOwl_WaitLWPostSaria(EnOwl* this, PlayState* play) {
     }
 }
 
-void func_80ACB748(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB748: scales the wind, flutter and pass-by sounds by how fast the camera is moving during the travel cutscenes
+void EnOwl_PlayFlightSfx(EnOwl* this, PlayState* play) {
     static Vec3f D_80ACD62C = { 0.0f, 0.0f, 0.0f };
     f32 dist;
     f32 weight;
@@ -806,7 +807,7 @@ void func_80ACB904(EnOwl* this, PlayState* play) {
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
@@ -820,7 +821,7 @@ void func_80ACB994(EnOwl* this, PlayState* play) {
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
@@ -836,7 +837,7 @@ void EnOwl_WaitDefault(EnOwl* this, PlayState* play) {
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

