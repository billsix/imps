From a1b26930015f5dabf0657a08fcadfbc270e2bd95 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:18:57 +0000
Subject: [PATCH 1430/3799] soh: rename func_809B07F8 -> EnAni_AdultIdle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the adult-era twin of EnAni_ChildIdle, choosing its line from whether Death Mountain has erupted and whether the heart piece has already been taken.
Defined in soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
index 33106b651..46eea8f94 100644
--- a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
+++ b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
@@ -22,7 +22,8 @@ void func_809B0558(EnAni* this, PlayState* play);
 void func_809B05F0(EnAni* this, PlayState* play);
 // LLM generated name (HIGH), was func_809B064C: the child-era idle on the roof: picks the day, night or face-reaction line, offers talk at two different ranges, and routes an accepted conversation to the heart-piece hand-off or to the plain talk state.
 void EnAni_ChildIdle(EnAni* this, PlayState* play);  // was func_809B064C [LLM:HIGH]
-void func_809B07F8(EnAni* this, PlayState* play);
+// LLM generated name (HIGH), was func_809B07F8: the adult-era twin of EnAni_ChildIdle, choosing its line from whether Death Mountain has erupted and whether the heart piece has already been taken.
+void EnAni_AdultIdle(EnAni* this, PlayState* play);  // was func_809B07F8 [LLM:HIGH]
 void func_809B0988(EnAni* this, PlayState* play);
 void func_809B0994(EnAni* this, PlayState* play);
 void func_809B0A28(EnAni* this, PlayState* play);
@@ -85,7 +86,7 @@ void EnAni_Init(Actor* thisx, PlayState* play) {
     if (!LINK_IS_ADULT) {
         EnAni_SetupAction(this, EnAni_ChildIdle);  // was func_809B064C [LLM:HIGH]
     } else {
-        EnAni_SetupAction(this, func_809B07F8);
+        EnAni_SetupAction(this, EnAni_AdultIdle);  // was func_809B07F8 [LLM:HIGH]
     }
     this->unk_2AA = 0;
     this->unk_2A8 = 0;
@@ -114,7 +115,7 @@ void func_809B04F0(EnAni* this, PlayState* play) {
 
 void func_809B0524(EnAni* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
-        EnAni_SetupAction(this, func_809B07F8);
+        EnAni_SetupAction(this, EnAni_AdultIdle);  // was func_809B07F8 [LLM:HIGH]
     }
 }
 
@@ -179,7 +180,7 @@ void EnAni_ChildIdle(EnAni* this, PlayState* play) {
     }
 }
 
-void func_809B07F8(EnAni* this, PlayState* play) {
+void EnAni_AdultIdle(EnAni* this, PlayState* play) {
     s16 pad;
     s16 yawDiff;
     u16 textId;
-- 
2.55.0

