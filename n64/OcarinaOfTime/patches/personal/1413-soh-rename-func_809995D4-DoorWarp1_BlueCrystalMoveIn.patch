From d34b6fdb775ce375766ba79b3bfc42a666bae7fc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:39 +0000
Subject: [PATCH 1413/3799] soh: rename func_809995D4 ->
 DoorWarp1_BlueCrystalMoveIntoPlace

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: once that wait ends it relocates the warp to its fixed chamber coordinates and re-aims both point lights at the new position.
Defined in soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
index 1212c1440..3723fdc9c 100644
--- a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
+++ b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
@@ -23,7 +23,8 @@ void DoorWarp1_PurpleCrystal(DoorWarp1* this, PlayState* play);
 void DoorWarp1_BlueCrystalFadeToBlack(DoorWarp1* this, PlayState* play);  // was func_80999214 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80999348: holds the player in place until the timer runs out, then queues NA_BGM_BOSS_CLEAR and arms the 200-frame wait.
 void DoorWarp1_BlueCrystalStartFanfare(DoorWarp1* this, PlayState* play);  // was func_80999348 [LLM:HIGH]
-void func_809995D4(DoorWarp1* this, PlayState* play);
+// LLM generated name (HIGH), was func_809995D4: once that wait ends it relocates the warp to its fixed chamber coordinates and re-aims both point lights at the new position.
+void DoorWarp1_BlueCrystalMoveIntoPlace(DoorWarp1* this, PlayState* play);  // was func_809995D4 [LLM:HIGH]
 void func_809998A4(DoorWarp1* this, PlayState* play);
 void DoorWarp1_ChildWarpIdle(DoorWarp1* this, PlayState* play);
 void DoorWarp1_RutoWarpIdle(DoorWarp1* this, PlayState* play);
@@ -406,11 +407,11 @@ void DoorWarp1_AwaitClearFlag(DoorWarp1* this, PlayState* play) {
     if (Flags_GetTempClear(play, this->actor.room)) {
         this->warpTimer = 200;
         Audio_QueueSeqCmd(SEQ_PLAYER_BGM_MAIN << 24 | NA_BGM_BOSS_CLEAR);
-        DoorWarp1_SetupAction(this, func_809995D4);
+        DoorWarp1_SetupAction(this, DoorWarp1_BlueCrystalMoveIntoPlace);  // was func_809995D4 [LLM:HIGH]
     }
 }
 
-void func_809995D4(DoorWarp1* this, PlayState* play) {
+void DoorWarp1_BlueCrystalMoveIntoPlace(DoorWarp1* this, PlayState* play) {
     if (this->warpTimer == 0) {
         if (this->actor.xzDistToPlayer < 100.0f) {
             this->actor.world.pos.x = -98.0f;
-- 
2.55.0

