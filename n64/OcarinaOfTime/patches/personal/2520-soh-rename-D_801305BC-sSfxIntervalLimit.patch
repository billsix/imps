From f5e00cb970644c55f59e31cf97913a265d4b651f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:32:58 +0000
Subject: [PATCH 2520/3799] soh: rename D_801305BC -> sSfxIntervalLimit

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: subtracted from the base in that period formula, so the fastest period is base minus this
Defined in soh/src/code/audio_general.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/audio_general.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/code/audio_general.c b/soh/src/code/audio_general.c
index 705a3caac..d00d7ffe9 100644
--- a/soh/src/code/audio_general.c
+++ b/soh/src/code/audio_general.c
@@ -121,7 +121,8 @@ f32 sFixedFreqScale = 0.7950898f;  // was D_801305B0 [LLM:HIGH]
 s8 sFixedReverb = 35;  // was D_801305B4 [LLM:HIGH]
 // LLM generated name (HIGH), was D_801305B8: the countdown Audio_PlaySfxAtInterval fires on
 s8 sSfxIntervalCounter = 20;  // was D_801305B8 [LLM:HIGH]
-s8 D_801305BC = 30;
+// LLM generated name (HIGH), was D_801305BC: subtracted from the base in that period formula, so the fastest period is base minus this
+s8 sSfxIntervalLimit = 30;  // was D_801305BC [LLM:HIGH]
 s8 D_801305C0 = 20;
 f32 sBehindScreenZ[2] = { -15.0f, -65.0f };
 u8 sAudioIncreasingTranspose = 0;
@@ -1324,7 +1325,7 @@ s32 sAudioUpdateTaskEnd;      // decomp: debug only
     F(sFixedFreqScale)                                      \
     F(sFixedReverb)                                      \
     F(sSfxIntervalCounter)                                      \
-    F(D_801305BC)                                      \
+    F(sSfxIntervalLimit)                                      \
     F(D_801305C0)                                      \
     F(sAudioIncreasingTranspose)                       \
     F(sPrevChargeLevel)                                \
@@ -4492,14 +4493,14 @@ void Audio_PlaySfxAtInterval(Vec3f* pos, u16 sfxId, f32 arg2) {
         if (arg2 > 2.0f) {
             arg2 = 2.0f;
         }
-        sSfxIntervalCounter = (s8)((D_801305C0 - D_801305BC) * (1.0f - arg2)) + D_801305C0;  // was D_801305B8 [LLM:HIGH]
+        sSfxIntervalCounter = (s8)((D_801305C0 - sSfxIntervalLimit) * (1.0f - arg2)) + D_801305C0;  // was D_801305B8 [LLM:HIGH]
     }
 }
 
 // LLM generated name (HIGH), was func_800F44EC: arms that countdown and stores the two bounds its period is computed from
 void Audio_SetSfxIntervalRange(s8 arg0, s8 arg1) {
     sSfxIntervalCounter = 1;  // was D_801305B8 [LLM:HIGH]
-    D_801305BC = arg1;
+    sSfxIntervalLimit = arg1;  // was D_801305BC [LLM:HIGH]
     D_801305C0 = arg0;
 }
 
-- 
2.55.0

