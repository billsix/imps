From a1dd3707702fdd23b6561573d294fe1834f3b242 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 20:47:44 +0000
Subject: [PATCH 0862/3799] soh: rename func_80A55BD4 -> EnHeishi3_ChasePlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the action entered from EnHeishi3_CatchStart with the walk animation and speedXZ 2.5: turns the guard toward the player and plays NA_SE_EV_KNIGHT_WALK on the two footfall frames, until caughtTimer expires and it hands off to EnHeishi3_ResetAnimationToIdle.
Defined in soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c b/soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c
index 121af3482..940258f06 100644
--- a/soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c
+++ b/soh/src/overlays/actors/ovl_En_Heishi3/z_en_heishi3.c
@@ -21,7 +21,8 @@ void EnHeishi3_StandSentinelInCastle(EnHeishi3* this, PlayState* play);
 void EnHeishi3_CatchStart(EnHeishi3* this, PlayState* play);
 void EnHeishi3_ResetAnimationToIdle(EnHeishi3* this, PlayState* play);
 void func_80A55D00(EnHeishi3* this, PlayState* play);
-void func_80A55BD4(EnHeishi3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A55BD4: the action entered from EnHeishi3_CatchStart with the walk animation and speedXZ 2.5: turns the guard toward the player and plays NA_SE_EV_KNIGHT_WALK on the two footfall frames, until caughtTimer expires and it hands off to EnHeishi3_ResetAnimationToIdle.
+void EnHeishi3_ChasePlayer(EnHeishi3* this, PlayState* play);  // was func_80A55BD4 [LLM:HIGH]
 
 static s16 sPlayerCaught = 0;
 
@@ -172,11 +173,11 @@ void EnHeishi3_CatchStart(EnHeishi3* this, PlayState* play) {
 
     Animation_Change(&this->skelAnime, &gEnHeishiWalkAnim, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
     this->caughtTimer = 20;
-    this->actionFunc = func_80A55BD4;
+    this->actionFunc = EnHeishi3_ChasePlayer;  // was func_80A55BD4 [LLM:HIGH]
     this->actor.speedXZ = 2.5f;
 }
 
-void func_80A55BD4(EnHeishi3* this, PlayState* play) {
+void EnHeishi3_ChasePlayer(EnHeishi3* this, PlayState* play) {
 
     SkelAnime_Update(&this->skelAnime);
     if (Animation_OnFrame(&this->skelAnime, 1.0f) || Animation_OnFrame(&this->skelAnime, 17.0f)) {
-- 
2.55.0

