From 513ae53acb55f968b31bc848dbcd004dfb87e049 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:19 +0000
Subject: [PATCH 2256/3799] soh: rename func_80B0E430 -> EnSw_TurnToRoll

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: holds still while the pause timer runs, then ramps the animation up and turns the roll toward its target, returning true on arrival
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index cfe4353d4..62466cda3 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -806,7 +806,8 @@ void EnSw_MoveToward(EnSw* this, Vec3f arg1, f32 arg4) {
     this->actor.world.pos.z += zDist;
 }
 
-s32 func_80B0E430(EnSw* this, f32 arg1, s16 arg2, s32 arg3, PlayState* play) {
+// LLM generated name (HIGH), was func_80B0E430: holds still while the pause timer runs, then ramps the animation up and turns the roll toward its target, returning true on arrival
+s32 EnSw_TurnToRoll(EnSw* this, f32 arg1, s16 arg2, s32 arg3, PlayState* play) {
     Camera* activeCam;
     f32 lastFrame = Animation_GetLastFrame(&object_st_Anim_000304);
 
@@ -843,7 +844,7 @@ void func_80B0E5E0(EnSw* this, PlayState* play) {
     s32 pad[2];
     f32 rand;
 
-    if (func_80B0E430(this, 6.0f, 0x3E8, 1, play)) {
+    if (EnSw_TurnToRoll(this, 6.0f, 0x3E8, 1, play)) {  // was func_80B0E430 [LLM:HIGH]
         rand = Rand_ZeroOne();
         this->unk_444 =
             ((s16)(20000.0f * rand) + 0x2EE0) * (Rand_ZeroOne() >= 0.5f ? 1.0f : -1.0f) + this->actor.world.rot.z;
@@ -866,7 +867,7 @@ void func_80B0E728(EnSw* this, PlayState* play) {
             this->unk_448 = player->actor.world.pos;
             this->unk_448.y += 30.0f;
             this->unk_444 = EnSw_GetRollToward(this, &this->unk_448);  // was func_80B0DE34 [LLM:HIGH]
-            func_80B0E430(this, 6.0f, (u16)0xFA0, 0, play);
+            EnSw_TurnToRoll(this, 6.0f, (u16)0xFA0, 0, play);  // was func_80B0E430 [LLM:HIGH]
         } else {
             this->actionFunc = func_80B0E5E0;
         }
@@ -905,7 +906,7 @@ void func_80B0E90C(EnSw* this, PlayState* play) {
 void func_80B0E9BC(EnSw* this, PlayState* play) {
     s32 pad;
 
-    if (func_80B0E430(this, 6.0f, 0x3E8, 0, play)) {
+    if (EnSw_TurnToRoll(this, 6.0f, 0x3E8, 0, play)) {  // was func_80B0E430 [LLM:HIGH]
         EnSw_MoveToward(this, this->unk_448, 2.0f);  // was func_80B0E314 [LLM:HIGH]
         if (!(Math_Vec3f_DistXYZ(&this->actor.world.pos, &this->unk_448) > 4.0f)) {
             this->actionFunc = func_80B0E5E0;
-- 
2.55.0

