From 0cb1879f4f5412fc08939761faa485ca590a508b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:56:56 +0000
Subject: [PATCH 2946/3799] soh: rename func_80B40BB4 ->
 EnXc_SetupIdleAfterNocturneFall

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: snaps onto cue 4, loops gSheikIdleAnim and enters action 65; named for the beat it follows because EnXc_SetupIdleInNocturne is already taken
Defined in soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c b/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
index 0133dbcef..fef3e390f 100644
--- a/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
+++ b/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
@@ -1987,7 +1987,8 @@ void EnXc_SetupKneelInNocturne(EnXc* this, PlayState* play) {
     this->actor.shape.shadowAlpha = 255;
 }
 
-void func_80B40BB4(EnXc* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B40BB4: snaps onto cue 4, loops gSheikIdleAnim and enters action 65; named for the beat it follows because EnXc_SetupIdleInNocturne is already taken
+void EnXc_SetupIdleAfterNocturneFall(EnXc* this, PlayState* play) {
     s32 pad[3];
     f32 frameCount = Animation_GetLastFrame(&gSheikIdleAnim);
     EnXc_ClearRootMotion(this);  // was func_80B3C9DC [LLM:HIGH]
@@ -2104,7 +2105,7 @@ s32 EnXc_SetupNocturneState(Actor* thisx, PlayState* play) {
                     EnXc_SetupKneelInNocturne(this, play);
                     break;
                 case 17:
-                    func_80B40BB4(this, play);
+                    EnXc_SetupIdleAfterNocturneFall(this, play);  // was func_80B40BB4 [LLM:HIGH]
                     break;
                 case 9:
                     Actor_Kill(thisx);
-- 
2.55.0

