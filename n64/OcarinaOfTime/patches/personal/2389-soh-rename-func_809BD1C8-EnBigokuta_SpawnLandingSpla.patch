From 74f97207a342344dda50eb431b8e03a4aafd7f16 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:25 +0000
Subject: [PATCH 2389/3799] soh: rename func_809BD1C8 ->
 EnBigokuta_SpawnLandingSplash

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: four corner splashes with the landing sounds and a quake
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 19f2fc5cd..db488dd94 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -28,7 +28,8 @@ void func_809BDFC8(EnBigokuta* this, PlayState* play);
 void func_809BE26C(EnBigokuta* this, PlayState* play);
 void func_809BE180(EnBigokuta* this, PlayState* play);
 void func_809BE058(EnBigokuta* this, PlayState* play);
-void func_809BD1C8(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BD1C8: four corner splashes with the landing sounds and a quake
+void EnBigokuta_SpawnLandingSplash(EnBigokuta* this, PlayState* play);  // was func_809BD1C8 [LLM:HIGH]
 
 static Color_RGBA8 sEffectPrimColor = { 255, 255, 255, 255 };
 static Color_RGBA8 sEffectEnvColor = { 100, 255, 255, 255 };
@@ -255,7 +256,7 @@ void EnBigokuta_SpawnWakeSplash(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BD1C8(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_SpawnLandingSplash(EnBigokuta* this, PlayState* play) {
     s32 i;
     Vec3f effectPos;
 
@@ -558,7 +559,7 @@ void func_809BDF34(EnBigokuta* this, PlayState* play) {
     }
     this->actor.world.pos.y = (sinf(this->unk_196 * (M_PI / 16)) * 100.0f) + this->actor.home.pos.y;
     if (this->unk_196 == 0) {
-        func_809BD1C8(this, play);
+        EnBigokuta_SpawnLandingSplash(this, play);  // was func_809BD1C8 [LLM:HIGH]
         func_809BD3F8(this);
     }
 }
@@ -619,7 +620,7 @@ void func_809BE180(EnBigokuta* this, PlayState* play) {
         this->actor.world.pos.y = sinf((this->unk_196 - 8) * (M_PI / 16)) * 100.0f + this->actor.home.pos.y;
         EnBigokuta_UpdateOrbitPos(this);  // was func_809BCE3C [LLM:HIGH]
         if (this->unk_196 == 8) {
-            func_809BD1C8(this, play);
+            EnBigokuta_SpawnLandingSplash(this, play);  // was func_809BD1C8 [LLM:HIGH]
         }
     }
 }
-- 
2.55.0

