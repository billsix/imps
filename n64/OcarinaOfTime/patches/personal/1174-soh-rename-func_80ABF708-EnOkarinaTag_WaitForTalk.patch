From f870bcbafc92791f2159a84ec841781548031519 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:38 +0000
Subject: [PATCH 1174/3799] soh: rename func_80ABF708 ->
 EnOkarinaTag_WaitForTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the type 5 idle: offers talk within 120 units and roughly in front, using the Sun's Song grave text.
Defined in soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c         | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
index 043cc0961..3ac3ebc8e 100644
--- a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
+++ b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
@@ -18,7 +18,8 @@ void EnOkarinaTag_Update(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80ABEF2C: the type 7 idle: when the player plays for the actor inside the configured range, it starts the ocarina check for the song number packed into params.
 void EnOkarinaTag_WaitForConfiguredSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABEF2C [LLM:HIGH]
-void func_80ABF708(EnOkarinaTag* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABF708: the type 5 idle: offers talk within 120 units and roughly in front, using the Sun's Song grave text.
+void EnOkarinaTag_WaitForTalk(EnOkarinaTag* this, PlayState* play);  // was func_80ABF708 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABF28C: the idle for the types whose song is fixed by the type itself (Zora's River, the windmill, the Door of Time, the royal tomb); it starts the matching OCARINA_ACTION_CHECK when the player plays nearby.
 void EnOkarinaTag_WaitForFixedSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF28C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABF0CC: reads msgCtx.ocarinaMode after that check; a correct song sets the switch flag and plays the chime, anything else just returns to the idle state.
@@ -104,7 +105,7 @@ void EnOkarinaTag_Init(Actor* thisx, PlayState* play) {
                 break;
             case 5:
                 this->actor.textId = 0x5021;
-                this->actionFunc = func_80ABF708;
+                this->actionFunc = EnOkarinaTag_WaitForTalk;  // was func_80ABF708 [LLM:HIGH]
                 break;
             default:
                 Actor_Kill(&this->actor);
@@ -294,7 +295,7 @@ void EnOkarinaTag_CheckFixedSong(EnOkarinaTag* this, PlayState* play) {
     }
 }
 
-void func_80ABF708(EnOkarinaTag* this, PlayState* play) {
+void EnOkarinaTag_WaitForTalk(EnOkarinaTag* this, PlayState* play) {
     s16 yawDiff;
     s16 yawDiffNew;
 
@@ -326,7 +327,7 @@ void func_80ABF7CC(EnOkarinaTag* this, PlayState* play) {
             play->csCtx.segment = SEGMENTED_TO_VIRTUAL(&gSunSongGraveSunSongTeachCs);
             gSaveContext.cutsceneTrigger = 1;
         }
-        this->actionFunc = func_80ABF708;
+        this->actionFunc = EnOkarinaTag_WaitForTalk;  // was func_80ABF708 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

