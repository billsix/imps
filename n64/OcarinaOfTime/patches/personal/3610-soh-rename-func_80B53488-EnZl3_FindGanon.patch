From d0a6fd71c6ceb9e13599b90fdfdcde7dfec378d2 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 02:01:38 +0000
Subject: [PATCH 3610/3799] soh: rename func_80B53488 -> EnZl3_FindGanon

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: walks ACTORCAT_BOSS for ACTOR_BOSS_GANON2, caches it in this->ganon and returns it
Defined in soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
index 61d2d13e0..4c304ccc0 100644
--- a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
+++ b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
@@ -112,7 +112,8 @@ void EnZl3_PlayEscapeBgm(void) {
     Audio_QueueSeqCmd(SEQ_PLAYER_BGM_MAIN << 24 | NA_BGM_ESCAPE);
 }
 
-BossGanon2* func_80B53488(EnZl3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B53488: walks ACTORCAT_BOSS for ACTOR_BOSS_GANON2, caches it in this->ganon and returns it
+BossGanon2* EnZl3_FindGanon(EnZl3* this, PlayState* play) {
     if (this->ganon == NULL) {
         Actor* actorIt = play->actorCtx.actorLists[ACTORCAT_BOSS].head;
 
@@ -1194,7 +1195,7 @@ void func_80B55F6C(EnZl3* this, PlayState* play) {
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
         this->action = 0x12;
     } else if (ABS((s16)(this->actor.yawTowardsPlayer - this->actor.shape.rot.y)) <= 0x4300) {
-        BossGanon2* bossGanon2 = func_80B53488(this, play);
+        BossGanon2* bossGanon2 = EnZl3_FindGanon(this, play);  // was func_80B53488 [LLM:HIGH]
 
         if ((bossGanon2 != NULL) && (bossGanon2->unk_324 <= (10.0f / 81.0f))) {
             this->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_FRIENDLY;
@@ -1255,7 +1256,7 @@ void func_80B56214(EnZl3* this, PlayState* play) {
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
         this->action = 21;
     } else if (ABS((s16)(this->actor.yawTowardsPlayer - this->actor.shape.rot.y)) <= 0x4300) {
-        BossGanon2* bossGanon2 = func_80B53488(this, play);
+        BossGanon2* bossGanon2 = EnZl3_FindGanon(this, play);  // was func_80B53488 [LLM:HIGH]
 
         if (bossGanon2 != NULL) {
             if (bossGanon2->unk_324 <= (10.0f / 81.0f)) {
-- 
2.55.0

