From 1342b59077157c754e50c2088dfde048f65ff96c Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:04:38 +0000
Subject: [PATCH 1252/3799] soh: rename func_8088D720 ->
 BgHidanSekizou_UpdateShort

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 0 statue's update: a bare timer that sweeps from 36 down to -36 and wraps, so its two fixed jets pulse on and off.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c       | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
index b2f155606..819a8aeb9 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
@@ -16,7 +16,8 @@ void BgHidanSekizou_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_8088D434: the params 1 statue's update: for each of the four directions it counts the flame timer down and, when the player is close and lined up with that axis, re-arms the timer and enables that direction's collider, disabling the rest.
 void BgHidanSekizou_UpdateTall(BgHidanSekizou* this, PlayState* play);  // was func_8088D434 [LLM:HIGH]
-void func_8088D720(BgHidanSekizou* this, PlayState* play);
+// LLM generated name (HIGH), was func_8088D720: the params 0 statue's update: a bare timer that sweeps from 36 down to -36 and wraps, so its two fixed jets pulse on and off.
+void BgHidanSekizou_UpdateShort(BgHidanSekizou* this, PlayState* play);  // was func_8088D720 [LLM:HIGH]
 
 const ActorInit Bg_Hidan_Sekizou_InitVars = {
     ACTOR_BG_HIDAN_SEKIZOU,
@@ -166,7 +167,7 @@ void BgHidanSekizou_Init(Actor* thisx, PlayState* play) {
             BgHidanSekizou_EnableFlameCollider(this, i, this->dyna.actor.shape.rot.y + ((i == 0) ? 0x2000 : -0x2000));  // was func_8088CEC0 [LLM:HIGH]
         }
         CollisionHeader_GetVirtual(&gFireTempleStationaryFlamethrowerShortCol, &colHeader);
-        this->updateFunc = func_8088D720;
+        this->updateFunc = BgHidanSekizou_UpdateShort;  // was func_8088D720 [LLM:HIGH]
     } else {
         this->unk_168[0] = this->unk_168[1] = this->unk_168[2] = this->unk_168[3] = 0;
         CollisionHeader_GetVirtual(&gFireTempleStationaryFlamethrowerTallCol, &colHeader);
@@ -218,7 +219,7 @@ void BgHidanSekizou_UpdateTall(BgHidanSekizou* this, PlayState* play) {
     }
 }
 
-void func_8088D720(BgHidanSekizou* this, PlayState* play) {
+void BgHidanSekizou_UpdateShort(BgHidanSekizou* this, PlayState* play) {
     this->unk_168[0]--;
     if (this->unk_168[0] <= -36) {
         this->unk_168[0] = 36;
-- 
2.55.0

