From e404f00970d4dba8eb7dcca94dc19582b74a189c Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:10:58 +0000
Subject: [PATCH 2052/3799] soh: rename func_80AB714C -> EnNiw_SwarmCall

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: throws the head and wings up with NA_SE_EV_CHICKEN_CRY_M at frame 40 and then charges the player
Defined in soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
index 8d060d897..573580b12 100644
--- a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
@@ -38,7 +38,8 @@ void EnNiw_FlutterAfterRelease(EnNiw* this, PlayState* play);  // was func_80AB6
 void EnNiw_FlyAway(EnNiw* this, PlayState* play);  // was func_80AB6EB4 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80AB70F8: holds for the cutscene timer, then arms the crow that precedes the swarm
 void EnNiw_SwarmWindUp(EnNiw* this, PlayState* play);  // was func_80AB70F8 [LLM:HIGH]
-void func_80AB714C(EnNiw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB714C: throws the head and wings up with NA_SE_EV_CHICKEN_CRY_M at frame 40 and then charges the player
+void EnNiw_SwarmCall(EnNiw* this, PlayState* play);  // was func_80AB714C [LLM:HIGH]
 void func_80AB7204(EnNiw* this, PlayState* play);
 void func_80AB7290(EnNiw* this, PlayState* play);
 void func_80AB7328(EnNiw* this, PlayState* play);
@@ -799,13 +800,13 @@ void EnNiw_SwarmWindUp(EnNiw* this, PlayState* play) {
         this->timer5 = 60;
         this->timer1 = 10;
         this->unk_2A2 = 4;
-        this->actionFunc = func_80AB714C;
+        this->actionFunc = EnNiw_SwarmCall;  // was func_80AB714C [LLM:HIGH]
     }
 
     EnNiw_AnimateLimbs(this, play, this->unk_2A2);  // was func_80AB5BF8 [LLM:HIGH]
 }
 
-void func_80AB714C(EnNiw* this, PlayState* play) {
+void EnNiw_SwarmCall(EnNiw* this, PlayState* play) {
     this->sfxTimer1 = 100;
 
     if (this->timer5 == 40) {
-- 
2.55.0

