From c67f1da9bb1c44256bdfad0e6526a3b08f71d7d4 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:21:15 +0000
Subject: [PATCH 2278/3799] soh: rename func_80AB26C8 ->
 EnNb_SetupConfrontationHidden

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters the confrontation action with nothing drawn
Defined in soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c b/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
index 330240334..d37635453 100644
--- a/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
+++ b/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
@@ -757,7 +757,8 @@ void EnNb_ApplyRootMotion(EnNb* this, PlayState* play) {
     AnimationContext_SetMoveActor(play, &this->actor, &this->skelAnime, 1.0f);
 }
 
-void func_80AB26C8(EnNb* this) {
+// LLM generated name (HIGH), was func_80AB26C8: enters the confrontation action with nothing drawn
+void EnNb_SetupConfrontationHidden(EnNb* this) {
     this->action = NB_IN_CONFRONTATION;
     this->drawMode = NB_DRAW_NOTHING;
     this->actor.shape.shadowAlpha = 0;
@@ -870,7 +871,7 @@ void EnNb_CheckConfrontationCsMode(EnNb* this, PlayState* play) {
         if (csAction != previousCsAction) {
             switch (csAction) {
                 case 1:
-                    func_80AB26C8(this);
+                    EnNb_SetupConfrontationHidden(this);  // was func_80AB26C8 [LLM:HIGH]
                     break;
                 case 10:
                     func_80AB26DC(this, play);
-- 
2.55.0

