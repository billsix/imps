From ae5721eec6d290a9da5528808db6dd3997bb9227 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:26 +0000
Subject: [PATCH 327/488] soh: name the 11 address-named symbols defined in
 z_obj_mure3.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 11 are defined in soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c.

  * func_80B9A9D0 -> ObjMure3_SpawnColumn
    deduced, confidence HIGH: the params variant that drops five blue rupees
    stacked 20 units apart straight up from the actor.

  * func_80B9AA90 -> ObjMure3_SpawnLine
    deduced, confidence HIGH: the params variant that drops five green
    rupees in a line along the actor's facing, spaced 20 units apart.

  * func_80B9ABA0 -> ObjMure3_SpawnCircle
    deduced, confidence HIGH: the params variant that drops six green rupees
    in a ring of radius 40 with a red rupee at its centre.

  * func_80B9ACE4 -> ObjMure3_DespawnRupees
    deduced, confidence HIGH: kills every rupee that is still lying around
    when the player leaves, recording the ones already collected so they are
    not respawned.

  * func_80B9ADCC -> ObjMure3_CheckCollected
    deduced, confidence HIGH: watches the spawned rupees each frame, setting
    the group's switch flag as soon as the player picks one up and marking
    each one gone when its actor dies.

  * func_80B9AF24 -> ObjMure3_SetupIdle
    deduced, confidence HIGH: installs the one-frame idle action.

  * func_80B9AF34 -> ObjMure3_Idle
    deduced, confidence HIGH: falls straight through to the waiting setup on
    its first frame.

  * func_80B9AF54 -> ObjMure3_SetupWaitForPlayer
    deduced, confidence HIGH: installs the action that watches for the
    player approaching.

  * func_80B9AF64 -> ObjMure3_WaitForPlayer
    deduced, confidence HIGH: once the player's screen-space distance drops
    below 1150 it disables update culling and runs this variant's spawn
    function.

  * func_80B9AFEC -> ObjMure3_SetupSpawned
    deduced, confidence HIGH: installs the action that runs while the rupees
    exist.

  * func_80B9AFFC -> ObjMure3_Spawned
    deduced, confidence HIGH: tracks which rupees have been collected and,
    once the player passes 1450 units away, despawns the rest and returns to
    waiting.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../actors/ovl_Obj_Mure3/z_obj_mure3.c        | 65 +++++++++++--------
 1 file changed, 38 insertions(+), 27 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
index 9766f3b7b..78713800f 100644
--- a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
+++ b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
@@ -12,12 +12,18 @@ void ObjMure3_Init(Actor* thisx, PlayState* play);
 void ObjMure3_Destroy(Actor* thisx, PlayState* play);
 void ObjMure3_Update(Actor* thisx, PlayState* play);
 
-void func_80B9AF24(ObjMure3* this);
-void func_80B9AF34(ObjMure3* this, PlayState* play);
-void func_80B9AF54(ObjMure3* this);
-void func_80B9AF64(ObjMure3* this, PlayState* play);
-void func_80B9AFEC(ObjMure3* this);
-void func_80B9AFFC(ObjMure3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B9AF24: installs the one-frame idle action.
+void ObjMure3_SetupIdle(ObjMure3* this);  // was func_80B9AF24 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80B9AF34: falls straight through to the waiting setup on its first frame.
+void ObjMure3_Idle(ObjMure3* this, PlayState* play);  // was func_80B9AF34 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80B9AF54: installs the action that watches for the player approaching.
+void ObjMure3_SetupWaitForPlayer(ObjMure3* this);  // was func_80B9AF54 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80B9AF64: once the player's screen-space distance drops below 1150 it disables update culling and runs this variant's spawn function.
+void ObjMure3_WaitForPlayer(ObjMure3* this, PlayState* play);  // was func_80B9AF64 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80B9AFEC: installs the action that runs while the rupees exist.
+void ObjMure3_SetupSpawned(ObjMure3* this);  // was func_80B9AFEC [LLM:HIGH]
+// LLM generated name (HIGH), was func_80B9AFFC: tracks which rupees have been collected and, once the player passes 1450 units away, despawns the rest and returns to waiting.
+void ObjMure3_Spawned(ObjMure3* this, PlayState* play);  // was func_80B9AFFC [LLM:HIGH]
 
 const ActorInit Obj_Mure3_InitVars = {
     ACTOR_OBJ_MURE3,
@@ -40,7 +46,8 @@ static InitChainEntry sInitChain[] = {
     ICHAIN_F32(uncullZoneDownward, 100, ICHAIN_STOP),
 };
 
-void func_80B9A9D0(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9A9D0: the params variant that drops five blue rupees stacked 20 units apart straight up from the actor.
+void ObjMure3_SpawnColumn(ObjMure3* this, PlayState* play) {
     s32 i;
     Vec3f spawnPos;
 
@@ -55,7 +62,8 @@ void func_80B9A9D0(ObjMure3* this, PlayState* play) {
     }
 }
 
-void func_80B9AA90(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9AA90: the params variant that drops five green rupees in a line along the actor's facing, spaced 20 units apart.
+void ObjMure3_SpawnLine(ObjMure3* this, PlayState* play) {
     s32 i;
     Vec3f spawnPos;
     f32 sn = Math_SinS(this->actor.world.rot.y);
@@ -76,7 +84,8 @@ void func_80B9AA90(ObjMure3* this, PlayState* play) {
     }
 }
 
-void func_80B9ABA0(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9ABA0: the params variant that drops six green rupees in a ring of radius 40 with a red rupee at its centre.
+void ObjMure3_SpawnCircle(ObjMure3* this, PlayState* play) {
     s32 i;
     Vec3f spawnPos;
     s16 yRot;
@@ -104,7 +113,8 @@ void func_80B9ABA0(ObjMure3* this, PlayState* play) {
     }
 }
 
-void func_80B9ACE4(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9ACE4: kills every rupee that is still lying around when the player leaves, recording the ones already collected so they are not respawned.
+void ObjMure3_DespawnRupees(ObjMure3* this, PlayState* play) {
     s16 count = sRupeeCounts[(this->actor.params >> 13) & 7];
     s32 i;
 
@@ -122,7 +132,8 @@ void func_80B9ACE4(ObjMure3* this, PlayState* play) {
     }
 }
 
-void func_80B9ADCC(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9ADCC: watches the spawned rupees each frame, setting the group's switch flag as soon as the player picks one up and marking each one gone when its actor dies.
+void ObjMure3_CheckCollected(ObjMure3* this, PlayState* play) {
     s16 count = sRupeeCounts[(this->actor.params >> 13) & 7];
     s32 i;
 
@@ -149,44 +160,44 @@ void ObjMure3_Init(Actor* thisx, PlayState* play) {
         return;
     }
     Actor_ProcessInitChain(&this->actor, sInitChain);
-    func_80B9AF24(this);
+    ObjMure3_SetupIdle(this);  // was func_80B9AF24 [LLM:HIGH]
 }
 
 void ObjMure3_Destroy(Actor* thisx, PlayState* play) {
 }
 
-void func_80B9AF24(ObjMure3* this) {
-    this->actionFunc = func_80B9AF34;
+void ObjMure3_SetupIdle(ObjMure3* this) {
+    this->actionFunc = ObjMure3_Idle;  // was func_80B9AF34 [LLM:HIGH]
 }
 
-void func_80B9AF34(ObjMure3* this, PlayState* play) {
-    func_80B9AF54(this);
+void ObjMure3_Idle(ObjMure3* this, PlayState* play) {
+    ObjMure3_SetupWaitForPlayer(this);  // was func_80B9AF54 [LLM:HIGH]
 }
 
-void func_80B9AF54(ObjMure3* this) {
-    this->actionFunc = func_80B9AF64;
+void ObjMure3_SetupWaitForPlayer(ObjMure3* this) {
+    this->actionFunc = ObjMure3_WaitForPlayer;  // was func_80B9AF64 [LLM:HIGH]
 }
 
-void func_80B9AF64(ObjMure3* this, PlayState* play) {
-    static ObjMure3SpawnFunc spawnFuncs[] = { func_80B9A9D0, func_80B9AA90, func_80B9ABA0 };
+void ObjMure3_WaitForPlayer(ObjMure3* this, PlayState* play) {
+    static ObjMure3SpawnFunc spawnFuncs[] = { ObjMure3_SpawnColumn, ObjMure3_SpawnLine, ObjMure3_SpawnCircle };  // was func_80B9A9D0 [LLM:HIGH]
 
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) < SQ(1150.0f)) {
         this->actor.flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED;
         spawnFuncs[(this->actor.params >> 13) & 7](this, play);
-        func_80B9AFEC(this);
+        ObjMure3_SetupSpawned(this);  // was func_80B9AFEC [LLM:HIGH]
     }
 }
 
-void func_80B9AFEC(ObjMure3* this) {
-    this->actionFunc = func_80B9AFFC;
+void ObjMure3_SetupSpawned(ObjMure3* this) {
+    this->actionFunc = ObjMure3_Spawned;  // was func_80B9AFFC [LLM:HIGH]
 }
 
-void func_80B9AFFC(ObjMure3* this, PlayState* play) {
-    func_80B9ADCC(this, play);
+void ObjMure3_Spawned(ObjMure3* this, PlayState* play) {
+    ObjMure3_CheckCollected(this, play);  // was func_80B9ADCC [LLM:HIGH]
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) >= SQ(1450.0f)) {
         this->actor.flags &= ~ACTOR_FLAG_UPDATE_CULLING_DISABLED;
-        func_80B9ACE4(this, play);
-        func_80B9AF54(this);
+        ObjMure3_DespawnRupees(this, play);  // was func_80B9ACE4 [LLM:HIGH]
+        ObjMure3_SetupWaitForPlayer(this);  // was func_80B9AF54 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

