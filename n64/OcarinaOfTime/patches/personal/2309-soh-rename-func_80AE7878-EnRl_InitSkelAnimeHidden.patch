From 74ac5b725ccb87bc91423d26ab17235e7714b759 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:22:10 +0000
Subject: [PATCH 2309/3799] soh: rename func_80AE7878 ->
 EnRl_InitSkelAnimeHidden

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 2 init: builds the skeleton, starts at action 4 and hides the shadow
Defined in soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c b/soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c
index c511e8f8c..63a879e0c 100644
--- a/soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c
+++ b/soh/src/overlays/actors/ovl_En_Rl/z_en_rl.c
@@ -204,7 +204,8 @@ void EnRl_Action3(EnRl* this, PlayState* play) {
     EnRl_GiveLightMedallion(this, play);  // was func_80AE7590 [LLM:HIGH]
 }
 
-void func_80AE7878(EnRl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AE7878: the params 2 init: builds the skeleton, starts at action 4 and hides the shadow
+void EnRl_InitSkelAnimeHidden(EnRl* this, PlayState* play) {
     SkelAnime_InitFlex(play, &this->skelAnime, &object_rl_Skel_007B38, &object_rl_Anim_000A3C, NULL, NULL, 0);
     this->action = 4;
     this->actor.shape.shadowAlpha = 0;
@@ -353,7 +354,7 @@ void EnRl_Init(Actor* thisx, PlayState* play) {
 
     ActorShape_Init(&this->actor.shape, 0.0f, ActorShadow_DrawCircle, 50.0f);
     if (this->actor.params == 2) {
-        func_80AE7878(this, play);
+        EnRl_InitSkelAnimeHidden(this, play);  // was func_80AE7878 [LLM:HIGH]
     } else {
         EnRl_InitSkelAnime(this, play);  // was func_80AE7544 [LLM:HIGH]
     }
-- 
2.55.0

