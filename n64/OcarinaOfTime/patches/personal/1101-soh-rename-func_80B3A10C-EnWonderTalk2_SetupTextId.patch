From 74efcffa4fa7bf7d486de4ca8309ce2a93c6fb5b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:48:25 +0000
Subject: [PATCH 1101/3799] soh: rename func_80B3A10C ->
 EnWonderTalk2_SetupTextId

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: builds actor.textId from 0x200 plus baseMsgId, then routes to the proximity-triggered or the talk-offer waiting state according to talkMode.
Defined in soh/src/overlays/actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c       | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c b/soh/src/overlays/actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c
index f8a7b6032..08a505dd9 100644
--- a/soh/src/overlays/actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c
+++ b/soh/src/overlays/actors/ovl_En_Wonder_Talk2/z_en_wonder_talk2.c
@@ -14,7 +14,8 @@ void EnWonderTalk2_Init(Actor* thisx, PlayState* play);
 void EnWonderTalk2_Destroy(Actor* thisx, PlayState* play);
 void EnWonderTalk2_Update(Actor* thisx, PlayState* play);
 
-void func_80B3A10C(EnWonderTalk2* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B3A10C: builds actor.textId from 0x200 plus baseMsgId, then routes to the proximity-triggered or the talk-offer waiting state according to talkMode.
+void EnWonderTalk2_SetupTextId(EnWonderTalk2* this, PlayState* play);  // was func_80B3A10C [LLM:HIGH]
 void func_80B3A4F8(EnWonderTalk2* this, PlayState* play);
 void func_80B3A15C(EnWonderTalk2* this, PlayState* play);
 void func_80B3A3D4(EnWonderTalk2* this, PlayState* play);
@@ -97,11 +98,11 @@ void EnWonderTalk2_Init(Actor* thisx, PlayState* play) {
         this->actor.flags &= ~ACTOR_FLAG_LOCK_ON_DISABLED;
         this->actionFunc = EnWonderTalk2_DoNothing;
     } else {
-        this->actionFunc = func_80B3A10C;
+        this->actionFunc = EnWonderTalk2_SetupTextId;  // was func_80B3A10C [LLM:HIGH]
     }
 }
 
-void func_80B3A10C(EnWonderTalk2* this, PlayState* play) {
+void EnWonderTalk2_SetupTextId(EnWonderTalk2* this, PlayState* play) {
     this->actor.textId = 0x200;
     this->actor.textId |= this->baseMsgId;
     if (this->talkMode == 1 || this->talkMode == 4) {
@@ -127,7 +128,7 @@ void func_80B3A15C(EnWonderTalk2* this, PlayState* play) {
             osSyncPrintf(VT_FGCOL(PURPLE) "☆☆☆☆☆ セーブしたよ！おもいっきり！ %x\n" VT_RST, this->switchFlag);
         }
 
-        this->actionFunc = func_80B3A10C;
+        this->actionFunc = EnWonderTalk2_SetupTextId;  // was func_80B3A10C [LLM:HIGH]
     } else {
         s16 yawDiff = ABS((s16)(this->actor.yawTowardsPlayer - this->actor.world.rot.y));
 
-- 
2.55.0

