From 2916843462d77a4a262b8f616c2a4d8d7268d89a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 13:35:18 +0000
Subject: [PATCH 0076/3799] soh: rename func_800C4344 ->
 GameState_UpdateDebugRegisters

Name source: deduced from the code (confidence HIGH).
Defined in soh/src/code/game.c.

Provenance comments accompany the definition and every header declaration,
so the name can be re-derived or overturned later. See
tasks/reference/ocarina/decomp-renaming.md.
---
 soh/include/functions.h | 2 +-
 soh/src/code/game.c     | 5 +++--
 2 files changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index f13991323..374cc570f 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1635,7 +1635,7 @@ void TitleSetup_Destroy(GameState* gameState);
 void TitleSetup_Init(GameState* gameState);
 void GameState_FaultPrint(void);
 void GameState_SetFBFilter(Gfx** gfx);
-// ? func_800C4344(?);
+// ? GameState_UpdateDebugRegisters(?);
 void GameState_DrawInputDisplay(u16 input, Gfx** gfx);
 void GameState_Draw(GameState* gameState, GraphicsContext* gfxCtx);
 void GameState_SetFrameBuffer(GraphicsContext* gfxCtx);
diff --git a/soh/src/code/game.c b/soh/src/code/game.c
index 9b706f15d..ed5bb042b 100644
--- a/soh/src/code/game.c
+++ b/soh/src/code/game.c
@@ -82,7 +82,8 @@ void GameState_SetFBFilter(Gfx** gfxP) {
     *gfxP = gfx;
 }
 
-void func_800C4344(GameState* gameState) {
+// LLM generated name (HIGH), was func_800C4344: reads/writes the HREG/SREG debug registers driving dev-tool behavior.
+void GameState_UpdateDebugRegisters(GameState* gameState) {
     Input* selectedInput;
     s32 hexDumpSize;
     u16 hReg82;
@@ -270,7 +271,7 @@ void GameState_Update(GameState* gameState) {
 
     gameState->main(gameState);
 
-    func_800C4344(gameState);
+    GameState_UpdateDebugRegisters(gameState);
 
     if (SREG(63) == 1u) {
         if (SREG(48) < 0) {
-- 
2.55.0

