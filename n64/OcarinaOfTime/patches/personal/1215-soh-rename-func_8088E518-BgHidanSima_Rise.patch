From 0fbb5180a1ba3378ad49ceaa52a8ef411f3d4582 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:58:38 +0000
Subject: [PATCH 1215/3799] soh: rename func_8088E518 -> BgHidanSima_Rise

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 0 platform's resting action: steps back up to home height and, when the player steps on, arms the shake or drops straight to the sink.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c  | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
index 9effb70b7..89979313d 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
@@ -14,7 +14,8 @@ void BgHidanSima_Destroy(Actor* thisx, PlayState* play);
 void BgHidanSima_Update(Actor* thisx, PlayState* play);
 void BgHidanSima_Draw(Actor* thisx, PlayState* play);
 
-void func_8088E518(BgHidanSima* this, PlayState* play);
+// LLM generated name (HIGH), was func_8088E518: the params 0 platform's resting action: steps back up to home height and, when the player steps on, arms the shake or drops straight to the sink.
+void BgHidanSima_Rise(BgHidanSima* this, PlayState* play);  // was func_8088E518 [LLM:HIGH]
 void func_8088E5D0(BgHidanSima* this, PlayState* play);
 void func_8088E6D0(BgHidanSima* this, PlayState* play);
 void func_8088E760(BgHidanSima* this, PlayState* play);
@@ -101,7 +102,7 @@ void BgHidanSima_Init(Actor* thisx, PlayState* play) {
         this->collider.elements[i].dim.worldSphere.radius = this->collider.elements[i].dim.modelSphere.radius;
     }
     if (this->dyna.actor.params == 0) {
-        this->actionFunc = func_8088E518;
+        this->actionFunc = BgHidanSima_Rise;  // was func_8088E518 [LLM:HIGH]
     } else {
         this->actionFunc = func_8088E760;
     }
@@ -114,7 +115,7 @@ void BgHidanSima_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyJntSph(play, &this->collider);
 }
 
-void func_8088E518(BgHidanSima* this, PlayState* play) {
+void BgHidanSima_Rise(BgHidanSima* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     Math_StepToF(&this->dyna.actor.world.pos.y, this->dyna.actor.home.pos.y, 3.4f);
@@ -158,7 +159,7 @@ void func_8088E6D0(BgHidanSima* this, PlayState* play) {
     }
     Math_StepToF(&this->dyna.actor.world.pos.y, this->dyna.actor.home.pos.y - 100.0f, 1.7f);
     if (this->timer == 0) {
-        this->actionFunc = func_8088E518;
+        this->actionFunc = BgHidanSima_Rise;  // was func_8088E518 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

