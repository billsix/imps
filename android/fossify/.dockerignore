# The image only needs `repos` + entrypoint/ (COPY'd explicitly). Keep the huge,
# gitignored runtime checkouts, build outputs, and image tarballs OUT of the
# build context so `podman build` isn't sent hundreds of MB it never uses.
checkout/
out/
**/build/
**/.gradle/
fossify-builder-*.tar
