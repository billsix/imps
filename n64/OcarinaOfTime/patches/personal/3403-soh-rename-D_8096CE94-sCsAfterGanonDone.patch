From 2c5fe4639aa85ef385e77e2c2f865fc957fa8610 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:19:06 +0000
Subject: [PATCH 3403/3799] soh: rename D_8096CE94 -> sCsAfterGanonDone

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: a bool DemoDu_CsAfterGanon_Reset clears; read as whether that cutscene has already run
Defined in soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c | 15 ++++++++-------
 1 file changed, 8 insertions(+), 7 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c b/soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c
index e4aee83c1..58a027c11 100644
--- a/soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c
+++ b/soh/src/overlays/actors/ovl_Demo_Du/z_demo_du.c
@@ -80,24 +80,25 @@ void DemoDu_CsAfterGanon_Reset(DemoDu* this) {
     this->unk_1A4 = 0.0f;
 }
 
-static s32 D_8096CE94 = false;
+// LLM generated name (GUESS), was D_8096CE94: a bool DemoDu_CsAfterGanon_Reset clears; read as whether that cutscene has already run
+static s32 sCsAfterGanonDone = false;  // was D_8096CE94 [LLM:GUESS]
 
-#define DEMO_DU_SHIP_SAVESTATE_FIELDS(F) F(D_8096CE94)
+#define DEMO_DU_SHIP_SAVESTATE_FIELDS(F) F(sCsAfterGanonDone)  // was D_8096CE94 [LLM:GUESS]
 
 SHIP_SAVESTATE_DEFINE(DemoDu, DEMO_DU_SHIP_SAVESTATE_FIELDS)
 
 void DemoDu_CsAfterGanon_CheckIfShouldReset(DemoDu* this, PlayState* play) {
 
     if (play->csCtx.state == CS_STATE_IDLE) {
-        if (D_8096CE94) {
+        if (sCsAfterGanonDone) {  // was D_8096CE94 [LLM:GUESS]
             if (this->actor.params == DEMO_DU_CS_CHAMBER_AFTER_GANON) {
                 DemoDu_CsAfterGanon_Reset(this);
             }
-            D_8096CE94 = false;
+            sCsAfterGanonDone = false;  // was D_8096CE94 [LLM:GUESS]
             return;
         }
-    } else if (!D_8096CE94) {
-        D_8096CE94 = true;
+    } else if (!sCsAfterGanonDone) {  // was D_8096CE94 [LLM:GUESS]
+        sCsAfterGanonDone = true;  // was D_8096CE94 [LLM:GUESS]
     }
 }
 
@@ -1062,5 +1063,5 @@ const ActorInit Demo_Du_InitVars = {
 };
 
 void DemoDu_Reset(void) {
-    D_8096CE94 = 0;
+    sCsAfterGanonDone = 0;  // was D_8096CE94 [LLM:GUESS]
 }
-- 
2.55.0

