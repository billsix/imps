From a473800e7083b1b6d73c9cb395707fe45cde9fcc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 20:20:56 +0000
Subject: [PATCH 0855/3799] soh: rename D_808994D8 -> sDebrisYawOffsets

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: binary-angle offsets added to the yaw toward the Iron Knuckle, indexed by its attack state, to aim the debris burst.
Defined in soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
index 78c664be6..b7769e227 100644
--- a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
+++ b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
@@ -61,7 +61,8 @@ static ColliderCylinderInit sCylinderInit = {
     { 30, 150, 0, { 0, 0, 0 } },
 };
 
-static s16 D_808994D8[] = { 0x8700, 0x4000, 0xC000, 0x0000 };
+// LLM generated name (HIGH), was D_808994D8: binary-angle offsets added to the yaw toward the Iron Knuckle, indexed by its attack state, to aim the debris burst.
+static s16 sDebrisYawOffsets[] = { 0x8700, 0x4000, 0xC000, 0x0000 };  // was D_808994D8 [LLM:HIGH]
 
 // Pillar
 static s16 D_808994E0[] = { 5, 8, 11, 14, 17, 20, 23, 26 };
@@ -114,7 +115,7 @@ void BgJyaIronobj_SpawnPillarParticles(BgJyaIronobj* this, PlayState* play, EnIk
         return;
     }
     osSyncPrintf("¢ attack_type(%d)\n", enIk->unk_2FF);
-    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + D_808994D8[enIk->unk_2FF - 1];
+    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + sDebrisYawOffsets[enIk->unk_2FF - 1];  // was D_808994D8 [LLM:HIGH]
 
     for (i = 0; i < 8; i++) {
         Actor* actor =
@@ -177,7 +178,7 @@ void BgJyaIronobj_SpawnThoneParticles(BgJyaIronobj* this, PlayState* play, EnIk*
         return;
     }
     osSyncPrintf("¢ attack_type(%d)\n", enIk->unk_2FF);
-    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + D_808994D8[enIk->unk_2FF - 1];
+    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + sDebrisYawOffsets[enIk->unk_2FF - 1];  // was D_808994D8 [LLM:HIGH]
     for (i = 0; i < 8; i++) {
         Actor* actor =
             Actor_Spawn(&play->actorCtx, play, ACTOR_BG_JYA_HAHENIRON, this->dyna.actor.world.pos.x,
-- 
2.55.0

