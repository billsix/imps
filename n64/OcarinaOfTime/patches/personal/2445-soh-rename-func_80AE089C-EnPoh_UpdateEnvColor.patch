From f138cabbd77008899c7cbd091ea6d0251d566fb6 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:28:10 +0000
Subject: [PATCH 2445/3799] soh: rename func_80AE089C -> EnPoh_UpdateEnvColor

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: ramps the body's env colour in during the appear animations and flickers it randomly afterwards
Defined in soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
index 1fb25f988..4829f49c0 100644
--- a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
+++ b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
@@ -996,7 +996,8 @@ void EnPoh_UpdateLightColor(EnPoh* this) {
     }
 }
 
-void func_80AE089C(EnPoh* this) {
+// LLM generated name (HIGH), was func_80AE089C: ramps the body's env colour in during the appear animations and flickers it randomly afterwards
+void EnPoh_UpdateEnvColor(EnPoh* this) {
     f32 rand;
 
     if ((this->actionFunc == EnPoh_RegularAppear || this->actionFunc == EnPoh_ComposerAppear) &&  // was func_80ADEF38 [LLM:HIGH]
@@ -1049,7 +1050,7 @@ void EnPoh_UpdateLiving(Actor* thisx, PlayState* play) {
     vec.z = this->actor.world.pos.z;
     this->actor.floorHeight =
         BgCheck_EntityRaycastFloor4(&play->colCtx, &this->actor.floorPoly, &sp38, &this->actor, &vec);
-    func_80AE089C(this);
+    EnPoh_UpdateEnvColor(this);  // was func_80AE089C [LLM:HIGH]
     this->actor.shape.shadowAlpha = this->lightColor.a;
 }
 
@@ -1184,7 +1185,7 @@ void EnPoh_UpdateDead(Actor* thisx, PlayState* play) {
     if (this->actionFunc != EnPoh_Death) {
         this->visibilityTimer++;
     }
-    func_80AE089C(this);
+    EnPoh_UpdateEnvColor(this);  // was func_80AE089C [LLM:HIGH]
 }
 
 void EnPoh_DrawSoul(Actor* thisx, PlayState* play) {
-- 
2.55.0

