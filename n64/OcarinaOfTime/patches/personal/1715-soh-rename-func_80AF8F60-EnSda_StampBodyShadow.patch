From 69df0e0768dc72b855ecb09bb9f349131438c1f8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:38:49 +0000
Subject: [PATCH 1715/3799] soh: rename func_80AF8F60 -> EnSda_StampBodyShadow

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: stamps the player's sixteen body-part positions into the shadow texture, interpolating each along its link by the caller's weight and picking the brush from its size class.
Defined in soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c b/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
index 2a5b71695..8e72df55a 100644
--- a/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
+++ b/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
@@ -15,7 +15,8 @@ void EnSda_Draw(Actor* thisx, PlayState* play);
 
 void func_80AF95C4(EnSda* this, u8* shadowTexture, Player* player, PlayState* play);
 void func_80AF9C70(u8* shadowTexture, Player* player, PlayState* play);
-void func_80AF8F60(Player* player, u8* shadowTexture, f32 arg2);
+// LLM generated name (HIGH), was func_80AF8F60: stamps the player's sixteen body-part positions into the shadow texture, interpolating each along its link by the caller's weight and picking the brush from its size class.
+void EnSda_StampBodyShadow(Player* player, u8* shadowTexture, f32 arg2);  // was func_80AF8F60 [LLM:HIGH]
 
 const ActorInit En_Sda_InitVars = {
     ACTOR_EN_SDA,
@@ -149,7 +150,7 @@ void EnSda_Draw(Actor* thisx, PlayState* play) {
     osSyncPrintf("SDA DRAW END\n");
 }
 
-void func_80AF8F60(Player* player, u8* shadowTexture, f32 arg2) {
+void EnSda_StampBodyShadow(Player* player, u8* shadowTexture, f32 arg2) {
     s16 temp_t0;
     s16 temp_t1;
     s16 temp_v1;
@@ -291,7 +292,7 @@ void func_80AF95C4(EnSda* this, u8* shadowTexture, Player* player, PlayState* pl
     sBodyPartPos[15].y = sBodyPartPos[0].y + ((sBodyPartPos[15].y - sBodyPartPos[0].y) * -1.2f);  // was D_80AFA660 [LLM:HIGH]
     sBodyPartPos[15].z = sBodyPartPos[0].z + ((sBodyPartPos[15].z - sBodyPartPos[0].z) * 1.2f);  // was D_80AFA660 [LLM:HIGH]
     for (i = 0; i < 6; i++) {
-        func_80AF8F60(player, shadowTexture, i / 5.0f);
+        EnSda_StampBodyShadow(player, shadowTexture, i / 5.0f);  // was func_80AF8F60 [LLM:HIGH]
     }
     osSyncPrintf("SDA CONT 3\n");
     if (this->actor.params != 1) {
-- 
2.55.0

