From 31657b7add3647f3abb8db64e2d1159f32a65bbf Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:17 +0000
Subject: [PATCH 1000/3799] soh: rename func_80993848 -> DemoTreLgt_Animate

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: sActionFuncs[DEMO_TRE_LGT_ACTION_ANIMATE]: fades the two limb alphas along the curve, plays the treasure-flash sfx once past frame 30 and kills the actor when the curve ends.
Defined in soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c b/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
index 9618c0b48..b302d63ec 100644
--- a/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
+++ b/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
@@ -10,7 +10,8 @@ void DemoTreLgt_Destroy(Actor* thisx, PlayState* play);
 void DemoTreLgt_Update(Actor* thisx, PlayState* play);
 void DemoTreLgt_Draw(Actor* thisx, PlayState* play);
 
-void func_80993848(DemoTreLgt* this, PlayState* play);
+// LLM generated name (HIGH), was func_80993848: sActionFuncs[DEMO_TRE_LGT_ACTION_ANIMATE]: fades the two limb alphas along the curve, plays the treasure-flash sfx once past frame 30 and kills the actor when the curve ends.
+void DemoTreLgt_Animate(DemoTreLgt* this, PlayState* play);  // was func_80993848 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80993754: sets action to DEMO_TRE_LGT_ACTION_WAIT; called from Init.
 void DemoTreLgt_SetupWait(DemoTreLgt* this);  // was func_80993754 [LLM:HIGH]
 // LLM generated name (HIGH), was func_8099375C: sActionFuncs[DEMO_TRE_LGT_ACTION_WAIT]: watches the parent chest's skelanime and starts the light animation on frame 10.
@@ -47,7 +48,7 @@ static TransformUpdateIndex* sTransformUpdIdx[] = { &gTreasureChestCurveAnim_4B6
 
 static DemoTreLgtActionFunc sActionFuncs[] = {
     DemoTreLgt_Wait,  // was func_8099375C [LLM:HIGH]
-    func_80993848,
+    DemoTreLgt_Animate,  // was func_80993848 [LLM:HIGH]
 };
 
 void DemoTreLgt_Init(Actor* thisx, PlayState* play) {
@@ -96,7 +97,7 @@ void DemoTreLgt_StartAnimation(DemoTreLgt* this, PlayState* play, f32 currentFra
     SkelCurve_Update(play, skelCurve);
 }
 
-void func_80993848(DemoTreLgt* this, PlayState* play) {
+void DemoTreLgt_Animate(DemoTreLgt* this, PlayState* play) {
     f32 currentFrame = this->skelCurve.animCurFrame;
 
     if (currentFrame < sDemoTreLgtInfo[((void)0, gSaveContext.linkAge)].endFrame) {
-- 
2.55.0

