From 51d5c24b84e0f6735a690fbf852778e719331a8c Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:11:53 +0000
Subject: [PATCH 3277/3799] soh: rename D_8094A9BC -> sTwinrovaFocusOffset

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the zero vector Twinrova records her focus position from
Defined in soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
index f1872f7ba..425fa171a 100644
--- a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
+++ b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
@@ -3643,7 +3643,7 @@ s32 BossTw_TwinrovaOverrideLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList,
 }
 
 void BossTw_TwinrovaPostLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList, Vec3s* rot, void* thisx) {
-    static Vec3f D_8094A9BC = { 0.0f, 0.0f, 0.0f };
+    static Vec3f sTwinrovaFocusOffset = { 0.0f, 0.0f, 0.0f };  // was D_8094A9BC [LLM:HIGH]
     static Vec3f D_8094A9C8 = { 0.0f, 2000.0f, -2000.0f };
     static Vec3f D_8094A9D4 = { 13000.0f, 0.0f, 0.0f };
     static Vec3f D_8094A9E0 = { 13000.0f, 0.0f, 0.0f };
@@ -3660,7 +3660,7 @@ void BossTw_TwinrovaPostLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList, Ve
             Matrix_MultVec3f(&D_8094A9E0, &this->rightScepterPos);
             break;
         case 21:
-            Matrix_MultVec3f(&D_8094A9BC, &this->actor.focus.pos);
+            Matrix_MultVec3f(&sTwinrovaFocusOffset, &this->actor.focus.pos);  // was D_8094A9BC [LLM:HIGH]
             Matrix_MultVec3f(&D_8094A9C8, &this->crownPos);
             break;
         case 15:
-- 
2.55.0

