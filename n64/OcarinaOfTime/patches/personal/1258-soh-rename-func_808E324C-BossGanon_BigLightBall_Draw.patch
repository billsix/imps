From eb50350bde26d27bc3199bcab550a8b3fcfd6c16 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:05:50 +0000
Subject: [PATCH 1258/3799] soh: rename func_808E324C ->
 BossGanon_BigLightBall_Draw

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the matching draw for those same children.
Defined in soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
index b8c23e21e..a9cd77633 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
@@ -31,7 +31,8 @@ void func_808E1EB4(Actor* thisx, PlayState* play); // update
 void BossGanon_BigLightBall_Update(Actor* thisx, PlayState* play); // update
 void BossGanon_LightBall_Update(Actor* thisx, PlayState* play);
 void func_808E229C(Actor* thisx, PlayState* play); // draw
-void func_808E324C(Actor* thisx, PlayState* play); // draw
+// LLM generated name (HIGH), was func_808E324C: the matching draw for those same children.
+void BossGanon_BigLightBall_Draw(Actor* thisx, PlayState* play); // draw
 void BossGanon_LightBall_Draw(Actor* thisx, PlayState* play);
 void BossGanon_Reset(void);
 
@@ -412,7 +413,7 @@ void BossGanon_Init(Actor* thisx, PlayState* play2) {
             } else if (thisx->params >= 0x104) {
                 // big magic light ball thrown
                 thisx->update = BossGanon_BigLightBall_Update;  // was func_808E2544 [LLM:HIGH]
-                thisx->draw = func_808E324C;
+                thisx->draw = BossGanon_BigLightBall_Draw;  // was func_808E324C [LLM:HIGH]
                 this->unk_1C2 = 10;
                 this->unk_1A2 = 520 + (-thisx->params * 2);
 
@@ -426,7 +427,7 @@ void BossGanon_Init(Actor* thisx, PlayState* play2) {
             } else if (thisx->params >= 0xFA) {
                 // big magic light ball charge
                 thisx->update = BossGanon_BigLightBall_Update;  // was func_808E2544 [LLM:HIGH]
-                thisx->draw = func_808E324C;
+                thisx->draw = BossGanon_BigLightBall_Draw;  // was func_808E324C [LLM:HIGH]
                 this->unk_1A2 = Rand_ZeroFloat(10000.0f);
 
                 for (i = 0; i < 15; i++) {
@@ -4633,7 +4634,7 @@ static Gfx* sBigMagicLightStreakDLists[] = {
     gGanondorfLightStreak4DL,  gGanondorfLightStreak3DL,  gGanondorfLightStreak2DL,  gGanondorfLightStreak1DL,
 };
 
-void func_808E324C(Actor* thisx, PlayState* play) {
+void BossGanon_BigLightBall_Draw(Actor* thisx, PlayState* play) {
     BossGanon* this = (BossGanon*)thisx;
     Mtx* mtx;
     s16 i;
-- 
2.55.0

