From 4822fa7bb621a47fb9859af2ec104cc027667db1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:38:50 +0000
Subject: [PATCH 1716/3799] soh: rename func_80AF95C4 ->
 EnSda_BuildShadowTexture

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: fills sBodyPartPos from the player, stretches the head slot outward and then runs several stamping passes at increasing weights to build the soft shadow.
Defined in soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c b/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
index 8e72df55a..36cb028ca 100644
--- a/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
+++ b/soh/src/overlays/actors/ovl_En_Sda/z_en_sda.c
@@ -13,7 +13,8 @@ void EnSda_Destroy(Actor* thisx, PlayState* play);
 void EnSda_Update(Actor* thisx, PlayState* play);
 void EnSda_Draw(Actor* thisx, PlayState* play);
 
-void func_80AF95C4(EnSda* this, u8* shadowTexture, Player* player, PlayState* play);
+// LLM generated name (HIGH), was func_80AF95C4: fills sBodyPartPos from the player, stretches the head slot outward and then runs several stamping passes at increasing weights to build the soft shadow.
+void EnSda_BuildShadowTexture(EnSda* this, u8* shadowTexture, Player* player, PlayState* play);  // was func_80AF95C4 [LLM:HIGH]
 void func_80AF9C70(u8* shadowTexture, Player* player, PlayState* play);
 // LLM generated name (HIGH), was func_80AF8F60: stamps the player's sixteen body-part positions into the shadow texture, interpolating each along its link by the caller's weight and picking the brush from its size class.
 void EnSda_StampBodyShadow(Player* player, u8* shadowTexture, f32 arg2);  // was func_80AF8F60 [LLM:HIGH]
@@ -141,7 +142,7 @@ void EnSda_Draw(Actor* thisx, PlayState* play) {
     }
 
     player->actor.shape.shadowAlpha = 0;
-    func_80AF95C4(this, shadowTexture, player, play);
+    EnSda_BuildShadowTexture(this, shadowTexture, player, play);  // was func_80AF95C4 [LLM:HIGH]
 
     if (KREG(0) < 5) {
         func_80AF9C70(shadowTexture, player, play);
@@ -249,7 +250,7 @@ void EnSda_StampBodyShadow(Player* player, u8* shadowTexture, f32 arg2) {
     }
 }
 
-void func_80AF95C4(EnSda* this, u8* shadowTexture, Player* player, PlayState* play) {
+void EnSda_BuildShadowTexture(EnSda* this, u8* shadowTexture, Player* player, PlayState* play) {
     s16 temp_t0;
     s16 temp_t1;
     s16 temp_v0;
-- 
2.55.0

