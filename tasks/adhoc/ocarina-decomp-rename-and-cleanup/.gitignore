# Fetched zeldaret/oot source, cached so re-runs need no network. It is other
# people's code under their licence and is trivially re-fetched -- never commit.
.cache/

# oracle.tsv IS tracked, deliberately: it is the verified work-list for the
# ~2,900 symbols still to name (which need a deduced name, not an adopted one),
# and it cost 187 network fetches plus a body-verification pass to produce.
