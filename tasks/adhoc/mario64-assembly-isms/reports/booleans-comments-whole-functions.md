# SM64 (Ghostship port) assembly-isms survey — booleans, decomp comments, whole-function reads

All paths relative to `Ghostship/src/`. Every claim from code the reader opened. (Reader report, 2026-09-22, verbatim in substance; the harness frame is omitted.)

# PART 1

## Class A — `bool_vs_TRUEFALSE` (202 hits, 60 files)

**True-positive ratio: ~201/202 mechanically safe.** Every function on the right of `== TRUE` returns a literal `TRUE`/`FALSE` or a normalized `(x & mask) != 0`: `game/save_file.c:431` `save_file_exists`; `game/object_helpers.c:967, 1444, 2788, 363`; `game/obj_behaviors.c:499/550/586/736` (`s8`, only TRUE/FALSE); `goddard/debug_utils.c:799` `gd_str_not_equal`; `goddard/gd_math.c:330`; `goddard/renderer.c:2487`; `IS_SEQUENCE_CHANNEL_VALID` (`audio/load.h:13`) is a comparison; audio struct operands (`enabled`, `finished`, `needsInit`, `restart`, `stopSomething`, `largeNotes`, `isFree`, `isPatched`) are `: 1` bitfields (`audio/internal.h:263,426-432,511-513,604-606,681-684,184`); all object fields in the log (`oHomingAmpLockedOn`, `oTiltingPyramidMarioOnPlatform`, `oTreasureChestUnkF8`, `oChainChompRestrictedByChain`, `oMerryGoRoundMusicShouldPlay`, `oHauntedBookshelfShouldOpen`, `oBreakableBoxSmallReleased`) only ever written TRUE/FALSE.

Sub-patterns: (1) plain redundant comparison on a 0/1 value (~175 sites) → `x` / `!x`; (2) asymmetric same-variable use — `game/behaviors/door.inc.c:143` `if (sp4 == TRUE)` then `:148` `if (!sp4)`; (3) `== TRUE` on a comparison result — `game/obj_behaviors.c:536` `bool visible = is_point_within_radius_of_mario(...) == TRUE;`; (4) `== FALSE` on a negated-predicate name — `goddard/debug_utils.c:84,256` `if (gd_str_not_equal(a, b) == FALSE)` → `if (!gd_str_not_equal(a, b))`; (5) boolean incremented instead of assigned — `game/behaviors/tilting_inverted_pyramid.inc.c:80/112/131` `marioOnPlatform++`, `game/behaviors/bowser.inc.c:329,372` `o->oBowserIsReacting++` (safe → `= TRUE` only because each is reset to FALSE before reaching 2; verified); (6) `switch` on a boolean with `case TRUE:`/`case FALSE:` — `game/behaviors/bowser.inc.c:419-435`.

**Behaviour-sensitive site — do not touch:** `game/behaviors/hoot.inc.c:229` `if (o->oInteractStatus == TRUE)` with the decomp's own `//! Note: Not a flag, treated as a TRUE/FALSE statement`. `oInteractStatus` is a flags word elsewhere (`INT_STATUS_INTERACTED` = `1 << 15`, `game/interaction.h:97`; `interaction.c:277`); for hoot the only writer is `interaction.c:1595` `= TRUE` and the clear at `:343`, so `if (o->oInteractStatus)` is preserving today, but `== TRUE` is load-bearing documentation. Same for `bowser.inc.c:423` `oBowserBitsJustJump == FALSE` (documented dead, `:409`, `:421`).

Risk: LOW for 1–4 (≈180 sites); MEDIUM for 5–6; DO-NOT-TOUCH hoot. Hot: `menu/file_select.c` (41), `audio/synthesis.c` (18), `audio/seqplayer.c` (17), `audio/playback.c` (8), `audio/external.c` (7).

## Class B — `cmp_zero` (1015 hits, 209 files)

Sample of 30: bitmask tests 8 (keep or drop, never auto-drop), counters/timers 11 (keep), loop/array index 3 (keep), genuine booleans 4 (drop), error-code returns 2 (keep), enum/multi-valued 2 (keep). Census-wide 131/1015 are `(… & …) == 0`/`!= 0`; 207 mention counter/index names; pointers use `== NULL`.

Rule: drop `!= 0`/`== 0` only when the operand is a `:1` bitfield, a `bool`, or a comparison/predicate result; keep for counters/timers/indices/lengths, enums/states where 0 is named, error-code returns, and parenthesized mask tests.

Examples: DROP `game/rendering_graph_node.c:166,182` `if (enableZBuffer != 0)` (defined `:153` as a comparison), `engine/surface_collision.c:262,461` `if (gCheckingSurfaceCollisionsForCamera != 0)` (`s16` flag); KEEP `audio/seqplayer.c:2122` `if (seq_channel_set_layer(...) == 0)` (0 = success, -1 = failure), `game/behaviors/coin.inc.c:99` / `tox_box.inc.c:55` / `bowser_key.inc.c:27` `if (o->oTimer == 0)`, `audio/external.c:1906-1908` mask pair, `goddard/renderer.c:3025` `if (view->gdDlNum != 0)` (0 = unassigned sentinel).

Risk LOW–MEDIUM (readability, not miscompilation). Cap a patch at the ~60 verifiably boolean sites. Hot: `game/camera.c` (65), `audio/seqplayer.c` (41), `audio/load_sh.c` (38), `audio/synthesis.c` (37), `game/mario_actions_cutscene.c` (31).

## Class C — `todo_bug_comments` (430 hits; 33 `@bug`, ~80 `TODO`, 9 `FIXME`)

C1 — bug-preserving, must NOT be "fixed": `engine/math_util.c:37…152` 14× `return &dest; //! warning: function returns address of local variable` (bracketed by `#pragma GCC diagnostic ignored "-Wreturn-local-addr"` at `:25-30`); `game/object_helpers.c:1246` `//! Returning FALSE but moving anyway`; `game/mario_actions_cutscene.c:1108,1111,1114` `//! fallthrough` (audible); `engine/surface_collision.c:521-522` (Surface Cucking); `engine/surface_load.c:244-247` (cell-border wall misses); `game/mario_actions_moving.c:564` (Speed Crash); `game/behaviors/chain_chomp.inc.c:316-318`; `game/camera.c:2597` `//! @bug unnecessary` (the helper has side effects; leave).

C2 — real, behaviour-preserving cleanups: `game/behaviors/tilting_inverted_pyramid.inc.c:98-109` provably dead `else` (`dy = 500.0f` literal at `:94` ⇒ `d >= 500`); `goddard/draw_objects.c:458-460` `if (FALSE) { }`; `audio/load_sh.c:231`, `audio/playback.c:473`, `audio/seqplayer.c:2046,2124` `if (1) {}` (dead arms); `goddard/renderer.c:812` duplicated `break;`; `game/camera.c:7189-7192` four `do { init_spline_point(...); } while (0);`; `audio/synthesis.c:551-554` `ra = ra + temp;` — read carefully, `ra` IS used later; treat as C1 until traced.

C3 — stale notes (comment-only): `game/camera.c:5165` `//! returning uninitialized variable` above `return dummy;` (initialised at `:5067`); `menu/file_select.c:1303`; `game/ingame_menu.c:75`, `game/mario_misc.c:608`, `audio/external.c:155` (questions, not defects); `game/camera.c:7630` `//! another double typo` — changing `1.0` to `1.0f` DOES change codegen: C1; `goddard/gd_math.c:692`.

Risk HIGH as a work list; zero-risk set = C3 comment edits + C2 items 9/10/11/12. Hot: `game/camera.c` (74), `game/object_helpers.c` (24), `game/mario_actions_moving.c` (24), `engine/math_util.c` (20), `game/obj_behaviors_2.c` (15).

# PART 2 — whole-function reads

1. `gdm_gettestdl` — `goddard/renderer.c:1199-1277`: three near-identical NULL-check `fatal_printf` guards (`:1210,1221,1244`) → helper; duplicated `apply_to_obj_types_in_group` calls (`:1215-1216`, `:1249-1250`, keep the `//! @bug` cast); `switch (id)` arms differing only in which static view/flag → a table (large restructuring); `:1207` `gddl = NULL` result temp; `:1203` `UNUSED u8 filler[8]`.
2. `new_gd_dl` — `goddard/renderer.c:1371-1423`: five verbatim-shaped per-resource blocks (`if (N == 0) N = 1; dl->curXIdx = 0; dl->totalX = N; if ((dl->x = gd_malloc_perm(N * sizeof(T))) == NULL) fatal_no_dl_mem();`) → helper taking `&count` (the `N = 1` clamp mutates the local).
3. `gd_dl_lookat` — `goddard/renderer.c:1719-1790`: two byte-identical 16-line colour-init blocks (`:1748-1763` vs `:1774-1789`) → one helper; six hand-unrolled `LOOKAT_PACK` assignments → 2×3 loop; `:1764` the same local reused for a second object; `:1722` `arg7 *= RAD_PER_DEG` mutates a parameter; unnamed `arg1…arg7` = (eyeX,eyeY,eyeZ, lookX,lookY,lookZ, rollDeg).
4. `gd_printf` — `goddard/renderer.c:765-859`: `:812` dead duplicate `break;`; three `UNUSED u8 fillerN[4]`; `:823` `//! @bug formatter 'c' uses s32` stays; the `*csr = X; csr++; *csr = '\0';` triple ×6 → `put()`; `:830` `spec[3]` read only via `default:` — do not "fix".
5. `process_level_music_dynamics` — `audio/external.c:1759-1895`: `j = condIndex + 1;` (×8, `:1808-1850`) used as `break` with the post-loop `if (j == condIndex)` doubling as "all matched" → `allMatched` flag + `break` (verified no other side effects); the 8-arm `switch (conditionTypes[j])` differs only in axis/direction → table (verify `MARIO_X_GE..MARIO_Z_LT` are 0..5 first); `tempBits` is two variables in one slot (`:1761` vs `:1870-1890`) → split; `:1881` `tempBits = 0;` dead; `:1788` `i = 2;` hand-rolled cursor; `:1790-1802` `bit = 0x8000 … bit >> 1` shift-as-index.
6. `play_music` / `stop_background_music` — `audio/external.c:2285-2346` / `2352-2396`: `:2326` `i = sBackgroundMusicQueueSize; // break` → `break;` (exact, `i` unread after); **`:2380` is NOT the same** — `stop_background_music` reads `i` after the loop at `:2390` (`sBackgroundMusicQueue[i].priority = 0;`, the `// @bug? … out of bounds` note at `:2387-2388`): a `break` changes which slot is zeroed; correct form is `break;` + explicit `i = sBackgroundMusicQueueSize;` after the loop. `:2311` `s16 seqId` shadows the `u8 seqId` at `:2287` with a different width → `firstSeqId`; `:2378` `(u8)(seqId & 0xff)` cast redundant, mask not; `foundIndex` has opposite "not found" sentinels in the two functions (`:2290` 0 vs `:2364` size).
7. `act_squished` — `game/mario_actions_cutscene.c:1481-1583`: `:1488` assignment-in-condition; `:1552` backwards condition `-0.5f < m->ceil->normal.y` (sibling `:1547` is normal); `:1547-1555` duplicated 4-line blocks (order is behaviour: floor then ceiling); `:1486` `underSteepSurf` guess-comment → rename; `:1572` `if (m->actionArg++ > 300)` — easy off-by-one, leave.
8. `act_exit_land_save_dialog` — `game/mario_actions_cutscene.c:1074-1156`: `:1092-1099` cascading overwrite instead of if/else ladder (`actionState = 3; if (!cap) 2; if (key) 1;` → `if (key) 1; else if (!cap) 2; else 3;`, preserving); `:1081-1082`/`:1097-1098` same BITDW/BITFS test twice → hoist; `:1105-1119` `//! fallthrough` ×3 preserve (comment-only); `:1127` `if (!(animFrame < 109) && animFrame < 154)` → `animFrame >= 109 && …`; `:1135-1145` `case 37: case 53:` deliberate; `:1119,1131,1149` `handle_save_menu(m)` ×3 → hoist (guard case 0); `:1153` `+= 0x8000` → `DEGREES(180)`.
9. `print_displaying_credits_entry` — `game/mario_actions_cutscene.c:112-165`: `:124` boolean used as arithmetic `(numLines == 1) * 16`; `:125-127`/`:158` `lineHeight` assigned only under `!ROM_JP`; `:120-121` pointer-bump-and-parse chains; `:128-150` switch that mutates the loop bound consumed by `while (numLines-- > 0)` at `:156`.
10. `bhv_tilting_inverted_pyramid_loop` + `approach_by_increment` — `game/behaviors/tilting_inverted_pyramid.inc.c:43-140`: `:112` `marioOnPlatform++` → `= TRUE`; `:111` `== TRUE`; `:98-109` dead `else`; `:82` `Mat4 *transform = &o->transform;` alias; `:81,:83` fillers; `:88-90` vs `:93-95` recompute the same subtractions twice; `:43-59` `newVal` result mirror → three returns; `:52` `goal - src > -inc` vs `:47` — exact only because IEEE negation is exact, keep + comment; **`:101` `d = 1.0 / d;` is a double reciprocal — changing to `1.0f` changes results; do not touch.**
11. `bowser_bits_actions` — `game/behaviors/bowser.inc.c:418-435`: `switch (o->oBowserIsReacting) { case FALSE: … case TRUE: … }` → if/else (verified every write is 0/1; note the silent no-op for other values); `:423-427` documented dead code — leave; `:1116` `o->oPosZ = 0.0f, o->oPosX = o->oPosZ;` comma-chained in register order → `o->oPosX = o->oPosZ = 0.0f;` (the `#if BUGFIX_BOWSER_FALLEN_OFF_STAGE` sibling at `:442` already writes it that way); `:1098` `onDynamicFloor == FALSE`; `:1082-1090` `cur_obj_forward_vel_approach_upward(150.0f, 2.0f);` twice → one else; `:1099/:1105` `else if` then separate `if` — intentional per comments, preserve.
12. `find_floor_from_list` — `engine/surface_collision.c:406-523`: `:407-408` `register` ×7; `:441,:453,:456` three duplicated cross-product edge tests → helper (preserve operand order); `:461-469` `!= 0` + nested single-statement if → `&&`; `:504` `if (y - (height + -78.0f) < 0.0f)` → `if (y < height - 78.0f)` (preserving in practice, not provably under flush-to-zero — caveat in the PR); `:497` `if (ny == 0.0f) continue;` leave; `:412`+`:518-519` result temp → `return surf;`; **`:509-517` REAL BUG (port-authored): inside `if (interpolate)`, `struct Surface s;` is function-local and `return &s;` returns a dangling pointer** — file separately; `:415-437` `interpolate` re-tested five times with a mid-loop downgrade at `:431`.
13. `radial_camera_input` — `game/camera.c:5066-5167`: R-button (`:5081-5107`) and L-button (`:5110-5137`) blocks are exact mirrors → one `radial_rotate(c, sign, flag)`; `:5084-5086`, `:5113-5115` redundant guard around idempotent `|=`; `:5090`, `:5117` `0x22AA` is not a whole `DEGREES()` — leave with comment; `:5094…5130` exact `==` on a clamped angle — do not "improve" to `>=`; `:5067`+`:5164-5166` `s16 dummy = 0; … return dummy;` with the stale `//! returning uninitialized variable` → `return 0;` (check function tables before changing the signature); `:5068` mixed mask tests with the `@bug` note `:5074-5078` — C1; `:5155-5158` `if(!ROM_JP)` port pattern — leave.
14. `atan2s` / `atan2_lookup` — `engine/math_util.c:702-753`: 8-way octant ladder → table in principle, **would not ship** (per-arm `>=`/`<` tie-breaks, `-atan2_lookup` on `u16` at `:747`); `:708` divide-by-zero guard returns the wrong octant (documented); `:710` unbounded `gArctanTable[(s32)(y / x * 1024 + 0.5f)]` — the known OOB read, preserve.
15. `cur_obj_move_xz` — `game/object_helpers.c:1195-1250`: `:1205` `UNUSED f32 ny;` is actually used at `:1239` (the attribute is a lie); `:1239` assignment-inside-condition on a value never read again; `:1209-1211` nested single-statement ifs → `&&`; `:1218,1228,1238` `oMoveFlags |= OBJ_MOVE_HIT_EDGE; return FALSE;` ×3 — leave or two tiny statics; `:1243-1250` `//! Returning FALSE but moving anyway` preserve.

# Constructs the regex census missed (with greps)

1. Loop exit by sentinel assignment to the induction variable — `audio/external.c:991, 2131, 2326, 2380`, `:1808-1850`. `grep -rnE '^\s*[a-zA-Z_]\w*\s*=\s*[^;]+;\s*(//|/\*)\s*"?break' src/`.
2. Boolean set via `++` — `tilting_inverted_pyramid.inc.c:112`, `bowser.inc.c:329,372`. `grep -rnE '\b(is|has|was|should|found|mario|on|near|under|negative)[A-Za-z]*\+\+;' src/` then check the declaration says `= FALSE`.
3. `switch` on a boolean — `bowser.inc.c:419`. `grep -rn 'case TRUE:\|case FALSE:' src/`.
4. Conditions written backwards / Yoda — `mario_actions_cutscene.c:1127,1552`, `bowser.inc.c:1085` (`500.0f < o->oBowserDistToCenter`), `mario_actions_cutscene.c:1158` (`15 < m->actionTimer++`). `grep -rnE 'if \(\s*!?\(?-?[0-9]+\.?[0-9]*f? *[<>]=? ' src/`; `grep -rnE 'if \(!\([^)]*[<>]' src/`.
5. Arithmetic-shaped comparison (`a - b < 0`) — `surface_collision.c:504`. `grep -rnE '\) *[<>]=? *0\.0f?\)' src/ | grep -E '[-+] '`.
6. Comma-operator chained assignment — `bowser.inc.c:1116`. `grep -rnE '^\s*\S+ = [^;,()]+, *\S+ = ' src/`.
7. Duplicated mirror-image blocks — `camera.c:5081` vs `:5110`, `renderer.c:1748` vs `:1774`, `renderer.c:1375-1419`. Needs a clone detector: `jscpd --min-lines 8 --min-tokens 60 --format c src/` or `pmd cpd --minimum-tokens 60 --language cpp`.
8. Redundant guard around an idempotent bit-set — `camera.c:5084,5113`. `grep -rnA1 -E 'if \(!\(\w+ & (\w+)\)\) \{' src/ | grep -E '\|= '`.
9. Result temp mirroring the return register — `renderer.c:2488` (`got`), `surface_collision.c:412` (`floor`), `tilting_inverted_pyramid.inc.c:44` (`newVal`), `math_util.c:703` (`ret`). `grep -rnB1 -E '^\s*return \w+;\s*$' src/ | grep -E '^\S+-\s*\w+ = '` (crude).
10. Pointer-to-array alias local for one struct field — `tilting_inverted_pyramid.inc.c:30,82`. `grep -rnE '(Mat4|Vec3f|Vec3s|Vp|Gfx) \*\w+ = &(o|obj|m)->' src/`.
11. Parameter mutated in place as scratch — `renderer.c:1722`, `math_util.c:726,738,740`. `grep -rnE '^\s*arg[0-9] [-+*/]?= ' src/`.
12. `UNUSED` on a variable that is used — `object_helpers.c:1205`. `grep -rn 'UNUSED \(f32\|s32\|s16\|u8\) \w*;' src/` then grep the name.
13. Version macro as a runtime `if` (`if (!ROM_JP)`) — `mario_actions_cutscene.c:125,158`, `camera.c:5155`. Ghostship-specific. `grep -rnE 'if\s*\(!?ROM_(JP|US|EU|SH)' src/`.
14. Exact `==` on an angle constant — `camera.c:5094,5101,5123,5130`. `grep -rnE '[!=]= *(DEGREES\(|0x[0-9A-Fa-f]{4}\))' src/`.
15. Variable slot reused for two meanings — `external.c:1761` (`tempBits`), `goddard/draw_objects.c:441` (`s32 i; // 20; also used to store mtl's gddl number`), `renderer.c:1764` (`lookat`). `grep -rniE '// *[0-9a-f]+; *also used|reused|doubles as' src/`.
16. Assignment inside an `if` condition — `mario_actions_cutscene.c:1488`, `renderer.c:1381`, `object_helpers.c:1239`, `draw_objects.c:450`. `grep -rnE 'if \(\(\w+([-.>]+\w+)* = [^=]' src/`.

**Outside the census:** `engine/surface_collision.c:509-517` returns the address of a stack-local `struct Surface` — a live dangling-pointer bug in the Ghostship interpolation path, not a decomp artifact; file separately.
