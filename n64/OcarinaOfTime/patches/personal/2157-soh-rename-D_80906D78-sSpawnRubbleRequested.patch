From 9690d56d5402784c71b998523026e47aed548256 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:15:35 +0000
Subject: [PATCH 2157/3799] soh: rename D_80906D78 -> sSpawnRubbleRequested

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: set at cutscene frame 30 alongside the rumble, and consumed by the next update to scatter the rubble effects
Defined in soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c     | 6 +++---
 .../overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c    | 3 ++-
 2 files changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
index 54fac5f2f..fddaa6279 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
@@ -484,7 +484,7 @@ void func_808FD5F4(BossGanon2* this, PlayState* play) {
         case 13:
             SkelAnime_Update(&this->skelAnime);
             if (this->csTimer == 30) {
-                D_80906D78 = 1;
+                sSpawnRubbleRequested = 1;  // was D_80906D78 [LLM:HIGH]
                 this->unk_314 = 1;
                 Rumble_Override(0.0f, 0xC8, 0x14, 0x14);
             }
@@ -2243,8 +2243,8 @@ void BossGanon2_Update(Actor* thisx, PlayState* play) {
     if (this->unk_339 >= 0) {
         this->unk_339 = 0;
     }
-    if (D_80906D78 != 0) {
-        D_80906D78 = 0;
+    if (sSpawnRubbleRequested != 0) {  // was D_80906D78 [LLM:HIGH]
+        sSpawnRubbleRequested = 0;  // was D_80906D78 [LLM:HIGH]
 
         for (i2 = 0; i2 < ARRAY_COUNT(sEffects); i2++) {
             angle = Rand_ZeroFloat(2 * M_PI);
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
index ef0c5fd04..e4c2e623b 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
@@ -8,7 +8,8 @@ static Vec3f sOrigin = { 0.0f, 0.0f, 0.0f };  // was D_80906D60 [LLM:HIGH]
 // LLM generated name (HIGH), was D_80906D6C: its only use is the position argument of the two NA_SE_EV_STONE_BOUND calls
 static Vec3f sStoneBoundSfxPos = { 0.0f, 0.0f, 500.0f };  // was D_80906D6C [LLM:HIGH]
 
-static u8 D_80906D78 = 0;
+// LLM generated name (HIGH), was D_80906D78: set at cutscene frame 30 alongside the rumble, and consumed by the next update to scatter the rubble effects
+static u8 sSpawnRubbleRequested = 0;  // was D_80906D78 [LLM:HIGH]
 
 static ColliderJntSphElementInit sJntSphItemsInit1[] = {
     {
-- 
2.55.0

