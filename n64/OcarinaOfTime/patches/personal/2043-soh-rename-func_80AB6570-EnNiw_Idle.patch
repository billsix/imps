From 511bf61cd8ed4d297de652797330d50328283e95 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:10:57 +0000
Subject: [PATCH 2043/3799] soh: rename func_80AB6570 -> EnNiw_Idle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the default idle: offers itself to be carried, or starts down its path for the params 0xA river cucco
Defined in soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
index 5f8421726..96d6cc9eb 100644
--- a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
@@ -26,7 +26,8 @@ void EnNiw_WaitToFlyOff(EnNiw* this, PlayState* play);  // was func_80AB6324 [LL
 void EnNiw_Airborne(EnNiw* this, PlayState* play);  // was func_80AB63A8 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80AB6450: the params 4 idle: offers itself with GI_NONE so the player can pick it up, and reacts to being stood on
 void EnNiw_WaitToBeLifted(EnNiw* this, PlayState* play);  // was func_80AB6450 [LLM:HIGH]
-void func_80AB6570(EnNiw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB6570: the default idle: offers itself to be carried, or starts down its path for the params 0xA river cucco
+void EnNiw_Idle(EnNiw* this, PlayState* play);  // was func_80AB6570 [LLM:HIGH]
 void func_80AB6A38(EnNiw* this, PlayState* play);
 void func_80AB6BF8(EnNiw* this, PlayState* play);
 void func_80AB6D08(EnNiw* this, PlayState* play);
@@ -430,7 +431,7 @@ void EnNiw_ResetAction(EnNiw* this, PlayState* play) {
             this->actionFunc = EnNiw_WaitToFlyOff;  // was func_80AB6324 [LLM:GUESS]
             break;
         default:
-            this->actionFunc = func_80AB6570;
+            this->actionFunc = EnNiw_Idle;  // was func_80AB6570 [LLM:HIGH]
             break;
     }
 }
@@ -454,7 +455,7 @@ void EnNiw_Airborne(EnNiw* this, PlayState* play) {
         this->unk_26C[7] = this->unk_26C[5] = this->unk_26C[6] = this->unk_26C[8] = this->actor.speedXZ =
             this->unk_2FC = this->unk_300 = 0.0f;
 
-        this->actionFunc = func_80AB6570;
+        this->actionFunc = EnNiw_Idle;  // was func_80AB6570 [LLM:HIGH]
     } else {
         EnNiw_AnimateLimbs(this, play, 2);  // was func_80AB5BF8 [LLM:HIGH]
     }
@@ -483,7 +484,7 @@ void EnNiw_WaitToBeLifted(EnNiw* this, PlayState* play) {
     }
 }
 
-void func_80AB6570(EnNiw* this, PlayState* play) {
+void EnNiw_Idle(EnNiw* this, PlayState* play) {
     s32 pad[2];
     f32 posY = Rand_CenteredFloat(100.0f);
     f32 posZ = Rand_CenteredFloat(100.0f);
@@ -908,7 +909,7 @@ void EnNiw_Update(Actor* thisx, PlayState* play) {
 
     this->unk_294++;
 
-    if (this->actionFunc != func_80AB6570) {
+    if (this->actionFunc != EnNiw_Idle) {  // was func_80AB6570 [LLM:HIGH]
         this->unk_26C[9] = 0.0f;
     }
     if (this->unk_2A6) {
@@ -1097,7 +1098,7 @@ void EnNiw_Update(Actor* thisx, PlayState* play) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_DEKU_WAKEUP);
     }
     if (this->sfxTimer1 == 0) {
-        if (this->actionFunc != func_80AB6570) {
+        if (this->actionFunc != EnNiw_Idle) {  // was func_80AB6570 [LLM:HIGH]
             this->sfxTimer1 = 30;
             Audio_PlayActorSound2(&this->actor, NA_SE_EV_CHICKEN_CRY_A);
         } else {
-- 
2.55.0

