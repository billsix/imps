From a5cbc9ec40da9899b3b2389a2e915486610519f7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:27 +0000
Subject: [PATCH 2407/3799] soh: rename func_809BDB90 ->
 EnBigokuta_WaitBeforeCircle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: counts the pause out and starts the miniboss music for the params 3 spawn
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 8de13ecd6..5ad411c2f 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -22,7 +22,8 @@ void EnBigokuta_IntroWait(EnBigokuta* this, PlayState* play);  // was func_809BD
 void EnBigokuta_JumpIn(EnBigokuta* this, PlayState* play);  // was func_809BD8DC [LLM:HIGH]
 // LLM generated name (HIGH), was func_809BDAE8: turns onto the home yaw, then becomes an enemy and moves on
 void EnBigokuta_TurnToStart(EnBigokuta* this, PlayState* play);  // was func_809BDAE8 [LLM:HIGH]
-void func_809BDB90(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BDB90: counts the pause out and starts the miniboss music for the params 3 spawn
+void EnBigokuta_WaitBeforeCircle(EnBigokuta* this, PlayState* play);  // was func_809BDB90 [LLM:HIGH]
 void func_809BDC08(EnBigokuta* this, PlayState* play);
 void func_809BE3E4(EnBigokuta* this, PlayState* play);
 void func_809BE4A4(EnBigokuta* this, PlayState* play);
@@ -312,7 +313,7 @@ void EnBigokuta_SetupTurnToStart(EnBigokuta* this) {
 
 void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this) {
     this->unk_196 = 40;
-    this->actionFunc = func_809BDB90;
+    this->actionFunc = EnBigokuta_WaitBeforeCircle;  // was func_809BDB90 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_809BD3F8: morphs to the swimming loop, arms the 350-frame circling window and turns the colliders on
@@ -486,7 +487,7 @@ void EnBigokuta_TurnToStart(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BDB90(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_WaitBeforeCircle(EnBigokuta* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (this->unk_196 != 0) {
         this->unk_196--;
-- 
2.55.0

