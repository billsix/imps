From 7aadc7b9e8b9b3a91838ee6a03fddbaa00889274 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:11:23 +0000
Subject: [PATCH 1336/3799] soh: rename func_80BA1ECC ->
 ObjWarp2block_IsPlayerInRange

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: returns 1 when the player is close enough to either block of the pair and standing clear of both footprints, and 0 otherwise; it is the gate on offering the ocarina.
Defined in soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c b/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
index 6db8ed9d0..5d6ecd909 100644
--- a/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
+++ b/soh/src/overlays/actors/ovl_Obj_Warp2block/z_obj_warp2block.c
@@ -19,7 +19,8 @@ void ObjWarp2block_Update(Actor* thisx, PlayState* play);
 void ObjWarp2block_Draw(Actor* thisx, PlayState* play);
 
 void ObjWarp2block_Spawn(ObjWarp2block* this, PlayState* play);
-s32 func_80BA1ECC(ObjWarp2block* this, PlayState* play);
+// LLM generated name (HIGH), was func_80BA1ECC: returns 1 when the player is close enough to either block of the pair and standing clear of both footprints, and 0 otherwise; it is the gate on offering the ocarina.
+s32 ObjWarp2block_IsPlayerInRange(ObjWarp2block* this, PlayState* play);  // was func_80BA1ECC [LLM:HIGH]
 void ObjWarp2block_SwapWithChild(ObjWarp2block* this, PlayState* play);
 s32 func_80BA2218(ObjWarp2block* this, PlayState* play);
 s32 func_80BA228C(ObjWarp2block* this, PlayState* play);
@@ -77,7 +78,7 @@ void ObjWarp2block_Spawn(ObjWarp2block* this, PlayState* play) {
                 sSpawnData[(this->dyna.actor.child->params >> 8) & 1].params);
 }
 
-s32 func_80BA1ECC(ObjWarp2block* this, PlayState* play) {
+s32 ObjWarp2block_IsPlayerInRange(ObjWarp2block* this, PlayState* play) {
     s32 pad;
     Actor* temp_a3;
     Player* player;
@@ -160,7 +161,7 @@ void ObjWarp2block_SwapWithChild(ObjWarp2block* this, PlayState* play) {
 s32 func_80BA2218(ObjWarp2block* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
-    if (func_80BA1ECC(this, play)) {
+    if (ObjWarp2block_IsPlayerInRange(this, play)) {  // was func_80BA1ECC [LLM:HIGH]
         if (player->stateFlags2 & PLAYER_STATE2_ATTEMPT_PLAY_FOR_ACTOR) {
             Message_StartOcarinaSunsSongEnabled(play, OCARINA_ACTION_FREE_PLAY);
             this->func_168 = func_80BA228C;
-- 
2.55.0

