From d5e69abe59dd93c12bd94970a40fdde9de473375 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:06 +0000
Subject: [PATCH 1160/3799] soh: rename func_80A90578 -> EnKakasi2_Rise

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spins the scarecrow while easing shape.yOffset up from underground, playing the swing sfx on two animation frames, and stops once it reaches the surface.
Defined in soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c b/soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c
index 35fabe1ef..4bd207f67 100644
--- a/soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c
+++ b/soh/src/overlays/actors/ovl_En_Kakasi2/z_en_kakasi2.c
@@ -44,7 +44,8 @@ void func_80A9062C(EnKakasi2* this, PlayState* play);
 void EnKakasi2_WaitToAppear(EnKakasi2* this, PlayState* play);  // was func_80A90264 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A904D8: starts the looping animation and plays the emerging sfx.
 void EnKakasi2_SetupRise(EnKakasi2* this, PlayState* play);  // was func_80A904D8 [LLM:HIGH]
-void func_80A90578(EnKakasi2* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A90578: spins the scarecrow while easing shape.yOffset up from underground, playing the swing sfx on two animation frames, and stops once it reaches the surface.
+void EnKakasi2_Rise(EnKakasi2* this, PlayState* play);  // was func_80A90578 [LLM:HIGH]
 void func_80A906C4(EnKakasi2* this, PlayState* play);
 
 const ActorInit En_Kakasi2_InitVars = {
@@ -169,10 +170,10 @@ void EnKakasi2_SetupRise(EnKakasi2* this, PlayState* play) {
 
     Animation_Change(&this->skelAnime, &object_ka_Anim_000214, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
     Audio_PlayActorSound2(&this->actor, NA_SE_EV_COME_UP_DEKU_JR);
-    this->actionFunc = func_80A90578;
+    this->actionFunc = EnKakasi2_Rise;  // was func_80A90578 [LLM:HIGH]
 }
 
-void func_80A90578(EnKakasi2* this, PlayState* play) {
+void EnKakasi2_Rise(EnKakasi2* this, PlayState* play) {
     s16 currentFrame;
 
     SkelAnime_Update(&this->skelAnime);
-- 
2.55.0

