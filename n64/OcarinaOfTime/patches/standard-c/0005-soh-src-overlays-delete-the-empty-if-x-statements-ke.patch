From 7f9d49c8dd269ceb7d16527379929f94889d8e36 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 22 Sep 2026 16:33:12 +0000
Subject: [PATCH 05/36] soh/src/overlays: delete the empty `if (x) {}`
 statements kept "to match"

An `if` with an empty body and a side-effect-free condition does nothing;
the decomp kept them because the original compiler needed the read to
reproduce the ROM's register allocation ("Needed to match", "fixes
regalloc, may be fake", "Very fake, but needed to get the s registers
right"). GCC at -O2 already drops the dead load, so the generated code is
unchanged. 6 line(s) in 37 file(s) under soh/src/overlays; the comment that
excused each one goes with it.

Verified: every file compiles to byte-identical assembly before and after
with the port's own flags (asmdiff over compile_commands.json).

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
---
 .../overlays/effects/ovl_Effect_Ss_G_Ripple/z_eff_ss_g_ripple.c | 1 -
 soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_equipment.c   | 2 --
 soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_item.c        | 2 --
 soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c   | 1 -
 4 files changed, 6 deletions(-)

diff --git a/soh/src/overlays/effects/ovl_Effect_Ss_G_Ripple/z_eff_ss_g_ripple.c b/soh/src/overlays/effects/ovl_Effect_Ss_G_Ripple/z_eff_ss_g_ripple.c
index adfa868f9..e240aa83a 100644
--- a/soh/src/overlays/effects/ovl_Effect_Ss_G_Ripple/z_eff_ss_g_ripple.c
+++ b/soh/src/overlays/effects/ovl_Effect_Ss_G_Ripple/z_eff_ss_g_ripple.c
@@ -71,7 +71,6 @@ void EffectSsGRipple_DrawRipple(PlayState* play, EffectSs* this, void* segment)
 
     OPEN_DISPS(gfxCtx);
 
-    if (play) {}
 
     radius = this->rRadius * 0.0025f;
 
diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_equipment.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_equipment.c
index a8144cc75..443fb3ce8 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_equipment.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_equipment.c
@@ -309,7 +309,6 @@ void KaleidoScope_DrawEquipment(PlayState* play) {
             cursorPoint = pauseCtx->cursorPoint[PAUSE_EQUIP];
             cursorY = pauseCtx->cursorY[PAUSE_EQUIP];
 
-            if (cursorMoveResult) {}
 
             cursorMoveResult = 0;
             while (cursorMoveResult == 0) {
@@ -878,7 +877,6 @@ void KaleidoScope_DrawEquipment(PlayState* play) {
     Gfx_SetupDL_42Opa(play->state.gfxCtx);
     KaleidoScope_DrawEquipmentImage(play, pauseCtx->playerSegment, PAUSE_EQUIP_PLAYER_WIDTH, PAUSE_EQUIP_PLAYER_HEIGHT);
 
-    if (gUpgradeMasks[0]) {}
 
     CLOSE_DISPS(play->state.gfxCtx);
 }
diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_item.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_item.c
index 2209a1e77..4d777e383 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_item.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_item.c
@@ -447,8 +447,6 @@ void KaleidoScope_DrawItemSelect(PlayState* play) {
                 osSyncPrintf("now=%d  ccc=%d\n", cursorPoint, cursorItem);
 
                 // Seem necessary to match
-                if (pauseCtx->cursorX[PAUSE_ITEM]) {}
-                if (gSaveContext.inventory.items[pauseCtx->cursorPoint[PAUSE_ITEM]]) {}
 
                 while (moveCursorResult == 0) {
                     if ((pauseCtx->stickRelX < -30) || (dpad && CHECK_BTN_ALL(input->press.button, BTN_DLEFT))) {
diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
index 990d97496..5144c8331 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
@@ -2908,7 +2908,6 @@ s16 func_80823A0C(PlayState* play, Vtx* vtx, s16 pageIndex, s16 arg3) {
 
             vtx[vtxIndex + 0].v.ob[0] = vtx[vtxIndex + 2].v.ob[0] = D_8082B060[colorIndex];
 
-            if (colorIndex) {}
 
             vtx[vtxIndex + 1].v.ob[0] = vtx[vtxIndex + 3].v.ob[0] = vtx[vtxIndex + 0].v.ob[0] + D_8082B08C[colorIndex];
 
-- 
2.55.0

