From 107a3910bc516d90af09a34e577978c204ee614f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:25 +0000
Subject: [PATCH 2388/3799] soh: rename func_809BCF68 ->
 EnBigokuta_SpawnWakeSplash

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spawns the ripple and splash trailing behind the moving octopus, with its splash sound
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 7d38c0051..19f2fc5cd 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -22,7 +22,8 @@ void func_809BDC08(EnBigokuta* this, PlayState* play);
 void func_809BE3E4(EnBigokuta* this, PlayState* play);
 void func_809BE4A4(EnBigokuta* this, PlayState* play);
 void func_809BE518(EnBigokuta* this, PlayState* play);
-void func_809BCF68(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BCF68: spawns the ripple and splash trailing behind the moving octopus, with its splash sound
+void EnBigokuta_SpawnWakeSplash(EnBigokuta* this, PlayState* play);  // was func_809BCF68 [LLM:HIGH]
 void func_809BDFC8(EnBigokuta* this, PlayState* play);
 void func_809BE26C(EnBigokuta* this, PlayState* play);
 void func_809BE180(EnBigokuta* this, PlayState* play);
@@ -222,7 +223,7 @@ void EnBigokuta_SpawnWaterRipple(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BCF68(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_SpawnWakeSplash(EnBigokuta* this, PlayState* play) {
     Vec3f effectPos;
     s16 rot;
 
@@ -499,7 +500,7 @@ void func_809BDC08(EnBigokuta* this, PlayState* play) {
         if (this->unk_196 == 0) {
             this->unk_196 = 350;
         }
-        func_809BCF68(this, play);
+        EnBigokuta_SpawnWakeSplash(this, play);  // was func_809BCF68 [LLM:HIGH]
         return;
     }
 
@@ -530,7 +531,7 @@ void func_809BDC08(EnBigokuta* this, PlayState* play) {
     }
     this->actor.shape.rot.y += phi_v1 * this->unk_194;
     EnBigokuta_UpdateOrbitPos(this);  // was func_809BCE3C [LLM:HIGH]
-    func_809BCF68(this, play);
+    EnBigokuta_SpawnWakeSplash(this, play);  // was func_809BCF68 [LLM:HIGH]
     if (this->unk_198 == 0) {
         func_809BD768(this);
     } else if (this->unk_196 == 0) {
@@ -677,7 +678,7 @@ void func_809BE3E4(EnBigokuta* this, PlayState* play) {
             this->unk_196 = -40;
         }
     }
-    func_809BCF68(this, play);
+    EnBigokuta_SpawnWakeSplash(this, play);  // was func_809BCF68 [LLM:HIGH]
 }
 
 void func_809BE4A4(EnBigokuta* this, PlayState* play) {
@@ -686,7 +687,7 @@ void func_809BE4A4(EnBigokuta* this, PlayState* play) {
     if (this->actor.world.pos.y < (this->actor.home.pos.y + -200.0f)) {
         func_809BD7F0(this, play);
     }
-    func_809BCF68(this, play);
+    EnBigokuta_SpawnWakeSplash(this, play);  // was func_809BCF68 [LLM:HIGH]
 }
 
 void func_809BE518(EnBigokuta* this, PlayState* play) {
-- 
2.55.0

