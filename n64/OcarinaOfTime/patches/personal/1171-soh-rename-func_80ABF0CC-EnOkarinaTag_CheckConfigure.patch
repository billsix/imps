From f428e90c278ea7e73cdc6c01cf9bf0b782d3fe26 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:37 +0000
Subject: [PATCH 1171/3799] soh: rename func_80ABF0CC ->
 EnOkarinaTag_CheckConfiguredSong

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: reads msgCtx.ocarinaMode after that check; a correct song sets the switch flag and plays the chime, anything else just returns to the idle state.
Defined in soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
index 26ec4dd8f..962d4e036 100644
--- a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
+++ b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
@@ -20,7 +20,8 @@ void EnOkarinaTag_Update(Actor* thisx, PlayState* play);
 void EnOkarinaTag_WaitForConfiguredSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABEF2C [LLM:HIGH]
 void func_80ABF708(EnOkarinaTag* this, PlayState* play);
 void func_80ABF28C(EnOkarinaTag* this, PlayState* play);
-void func_80ABF0CC(EnOkarinaTag* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABF0CC: reads msgCtx.ocarinaMode after that check; a correct song sets the switch flag and plays the chime, anything else just returns to the idle state.
+void EnOkarinaTag_CheckConfiguredSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF0CC [LLM:HIGH]
 void func_80ABF4C8(EnOkarinaTag* this, PlayState* play);
 void func_80ABF7CC(EnOkarinaTag* this, PlayState* play);
 
@@ -134,7 +135,7 @@ void EnOkarinaTag_WaitForConfiguredSong(EnOkarinaTag* this, PlayState* play) {
                     }
                     player->stateFlags2 |= PLAYER_STATE2_NEAR_OCARINA_ACTOR;
                     Message_StartOcarinaSunsSongEnabled(play, ocarinaSong + OCARINA_ACTION_CHECK_SARIA);
-                    this->actionFunc = func_80ABF0CC;
+                    this->actionFunc = EnOkarinaTag_CheckConfiguredSong;  // was func_80ABF0CC [LLM:HIGH]
                 } else if ((this->actor.xzDistToPlayer < (50.0f + this->interactRange) &&
                             ((fabsf(player->actor.world.pos.y - this->actor.world.pos.y) < 40.0f)))) {
                     this->unk_15A = 0;
@@ -145,7 +146,7 @@ void EnOkarinaTag_WaitForConfiguredSong(EnOkarinaTag* this, PlayState* play) {
     }
 }
 
-void func_80ABF0CC(EnOkarinaTag* this, PlayState* play) {
+void EnOkarinaTag_CheckConfiguredSong(EnOkarinaTag* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     if (play->msgCtx.ocarinaMode == OCARINA_MODE_04) {
-- 
2.55.0

