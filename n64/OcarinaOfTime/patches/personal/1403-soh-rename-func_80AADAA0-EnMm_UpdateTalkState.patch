From a912c00a3e8a6189b4aa2145a83028927dcdd715 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:02 +0000
Subject: [PATCH 1403/3799] soh: rename func_80AADAA0 -> EnMm_UpdateTalkState

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: steps the conversation by message state, resolving the yes/no choice into the matching reply text and animation, and returns the new talk state for EnMm_UpdateTalking to store.
Defined in soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
index a85f2fa1a..ec84bc0e8 100644
--- a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
+++ b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
@@ -211,7 +211,8 @@ s32 EnMm_IsDay(void) {
     return isDay;
 }
 
-s32 func_80AADAA0(EnMm* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AADAA0: steps the conversation by message state, resolving the yes/no choice into the matching reply text and animation, and returns the new talk state for EnMm_UpdateTalking to store.
+s32 EnMm_UpdateTalkState(EnMm* this, PlayState* play) {
     s32 pad;
     Player* player = GET_PLAYER(play);
     s32 sp1C = 1;
@@ -288,7 +289,7 @@ void func_80AADCD0(EnMm* this, PlayState* play) {
         Message_ContinueTextbox(play, player->actor.textId);
         this->unk_1E0 = 1;
     } else if (this->unk_1E0 == 1) {
-        this->unk_1E0 = func_80AADAA0(this, play);
+        this->unk_1E0 = EnMm_UpdateTalkState(this, play);  // was func_80AADAA0 [LLM:HIGH]
     } else {
         if (Actor_ProcessTalkRequest(&this->actor, play)) {
             this->unk_1E0 = 1;
-- 
2.55.0

