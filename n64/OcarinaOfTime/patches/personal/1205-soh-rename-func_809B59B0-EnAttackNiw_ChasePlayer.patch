From 41b5be5dc8c9a648b53981c247d2fddeac59e3cd Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:57:24 +0000
Subject: [PATCH 1205/3799] soh: rename func_809B59B0 ->
 EnAttackNiw_ChasePlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: steers toward a jittered yaw at the player, bounces along the ground under gravity, and once its timer runs out or it hits a wall it pitches up and flies off.
Defined in soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c  | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c b/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
index b7b079469..e6945a244 100644
--- a/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
@@ -18,7 +18,8 @@ void EnAttackNiw_Draw(Actor* thisx, PlayState* play);
 // LLM generated name (HIGH), was func_809B5670: the first action: flies at speed 10 toward a point offset from the camera's look-at, then hands off to the chasing state.
 void EnAttackNiw_FlyTowardCamera(EnAttackNiw* this, PlayState* play);  // was func_809B5670 [LLM:HIGH]
 void func_809B5C18(EnAttackNiw* this, PlayState* play);
-void func_809B59B0(EnAttackNiw* this, PlayState* play);
+// LLM generated name (HIGH), was func_809B59B0: steers toward a jittered yaw at the player, bounces along the ground under gravity, and once its timer runs out or it hits a wall it pitches up and flies off.
+void EnAttackNiw_ChasePlayer(EnAttackNiw* this, PlayState* play);  // was func_809B59B0 [LLM:HIGH]
 
 const ActorInit En_Attack_Niw_InitVars = {
     ACTOR_EN_ATTACK_NIW,
@@ -225,7 +226,7 @@ void EnAttackNiw_FlyTowardCamera(EnAttackNiw* this, PlayState* play) {
         this->actor.gravity = -0.2f;
         this->unk_2E0 = 5.0f;
         this->unk_288 = 0.0f;
-        this->actionFunc = func_809B59B0;
+        this->actionFunc = EnAttackNiw_ChasePlayer;  // was func_809B59B0 [LLM:HIGH]
     } else if (((this->actor.projectedPos.z > 0.0f) && (fabsf(sp34.x - this->actor.world.pos.x) < 50.0f) &&
                 (fabsf(sp34.y - this->actor.world.pos.y) < 50.0f) &&
                 (fabsf(sp34.z - this->actor.world.pos.z) < 50.0f)) ||
@@ -240,7 +241,7 @@ void EnAttackNiw_FlyTowardCamera(EnAttackNiw* this, PlayState* play) {
         this->actor.gravity = -0.2f;
         this->unk_2E0 = 5.0f;
         this->unk_288 = 0.0f;
-        this->actionFunc = func_809B59B0;
+        this->actionFunc = EnAttackNiw_ChasePlayer;  // was func_809B59B0 [LLM:HIGH]
     } else {
         this->unk_254 = 10;
         this->unk_264 = -10000.0f;
@@ -249,7 +250,7 @@ void EnAttackNiw_FlyTowardCamera(EnAttackNiw* this, PlayState* play) {
     }
 }
 
-void func_809B59B0(EnAttackNiw* this, PlayState* play) {
+void EnAttackNiw_ChasePlayer(EnAttackNiw* this, PlayState* play) {
     if (!EnAttackNiw_IsOnScreen(this, play)) {  // was func_809B55EC [LLM:HIGH]
         Actor_Kill(&this->actor);
         return;
-- 
2.55.0

