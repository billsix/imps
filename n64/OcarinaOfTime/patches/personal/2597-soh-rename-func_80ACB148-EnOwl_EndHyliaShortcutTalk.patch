From d1fdbb166a8acfd32c04ba16601488d49c35f3f1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:10 +0000
Subject: [PATCH 2597/3799] soh: rename func_80ACB148 ->
 EnOwl_EndHyliaShortcutTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: closes the Lake Hylia shortcut message, sets switch 0x23 and takes off
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index f6e584b45..8b32ca57d 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -601,7 +601,8 @@ void EnOwl_WaitZoraRiver(EnOwl* this, PlayState* play) {
     }
 }
 
-void func_80ACB148(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB148: closes the Lake Hylia shortcut message, sets switch 0x23 and takes off
+void EnOwl_EndHyliaShortcutTalk(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
         EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
@@ -618,7 +619,7 @@ void EnOwl_WaitHyliaShortcut(EnOwl* this, PlayState* play) {
     if (EnOwl_OfferTalk(this, play, textId)) {  // was func_80ACA558 [LLM:HIGH]
         Flags_SetInfTable(INFTABLE_SPOKE_TO_KAEPORA_IN_LAKE_HYLIA);
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB148;
+        this->actionFunc = EnOwl_EndHyliaShortcutTalk;  // was func_80ACB148 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

