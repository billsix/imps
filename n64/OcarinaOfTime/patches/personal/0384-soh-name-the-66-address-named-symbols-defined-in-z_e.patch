From 2b16ab973d50ecd842fd66123d4e99af9a9ba143 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:32 +0000
Subject: [PATCH 384/488] soh: name the 66 address-named symbols defined in
 z_en_po_sisters.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 66 are defined in soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c; updating their references touches 3 files in total.

  * func_80AD9240 -> EnPoSisters_UpdateTrailRing
    deduced, confidence HIGH: lays the trail points out on a ring around a
    centre, its radius growing with the caller's step

  * func_80AD9368 -> EnPoSisters_SetupSway
    deduced, confidence HIGH: loops gPoeSistersSwayAnim in place with a
    short timer

  * func_80AD93C4 -> EnPoSisters_SetupFloat
    deduced, confidence HIGH: loops gPoeSistersFloatAnim and arms the
    drifting timer

  * func_80AD943C -> EnPoSisters_SetupCircle
    deduced, confidence HIGH: installs the circling action

  * func_80AD944C -> EnPoSisters_SetupAttack
    deduced, confidence HIGH: morphs to gPoeSistersAttackAnim, hardens the
    collider for the lit sister and arms the swing

  * func_80AD94E0 -> EnPoSisters_SetupCharge
    deduced, confidence HIGH: aims at the player at speed 5 with the attack
    animation

  * func_80AD9568 -> EnPoSisters_SetupRetreat
    deduced, confidence HIGH: turns away from the player and softens the
    collider again

  * func_80AD95D8 -> EnPoSisters_SetupDamage
    deduced, confidence HIGH: plays gPoeSistersDamagedAnim, aims the
    knockback away from the attacker and applies the red filter

  * func_80AD96A4 -> EnPoSisters_SetupFlee
    deduced, confidence HIGH: loops gPoeSistersFleeAnim heading away at
    speed 5

  * func_80AD9718 -> EnPoSisters_SetupDisappear
    deduced, confidence HIGH: plays gPoeSistersAppearDisappearAnim with the
    disappear and laugh sounds

  * func_80AD97C8 -> EnPoSisters_UpdateHoverHeight
    deduced, confidence HIGH: eases the hover height between 110 and 170
    depending on whether the player is swinging on the ground

  * func_80AD98F4 -> EnPoSisters_SetupAppear
    deduced, confidence HIGH: plays the appear animation and, for Meg,
    restores the draw function at the low hover height

  * func_80AD99D4 -> EnPoSisters_SetupDeathSpiral
    deduced, confidence HIGH: drops targeting, lifts the body and opens the
    death one-point cutscene

  * func_80AD9A54 -> EnPoSisters_SetupBurnOut
    deduced, confidence HIGH: drops the body onto the first trail point and
    scatters a collectible

  * func_80AD9AA8 -> EnPoSisters_SpawnCopies
    deduced, confidence HIGH: spawns the three decoy sisters with params
    0x400, 0x800 and 0xC00

  * func_80AD9C24 -> EnPoSisters_HideCopy
    deduced, confidence GUESS: stops drawing this sister, drops targeting
    and softens the collider; named for the effect on a decoy, which the
    body alone does not confirm

  * func_80AD9D44 -> EnPoSisters_SetupReappear
    deduced, confidence HIGH: replays the appear animation, faster for the
    fourth sister, with the appear sound

  * func_80AD9DF0 -> EnPoSisters_SetupMegAppear
    deduced, confidence HIGH: plays the appear animation once and opens
    Meg's introduction one-point cutscene

  * func_80AD9E60 -> EnPoSisters_SetupCopyDance
    deduced, confidence GUESS: loops the float animation on the parent
    sister's position; the dance reading comes from the copies circling in
    step rather than from this body

  * func_80AD9F1C -> EnPoSisters_SetupMegFight
    deduced, confidence HIGH: restores full alpha and targeting and arms
    Meg's 300-frame fight window

  * func_80AD9F90 -> EnPoSisters_SetupReturnHome
    deduced, confidence HIGH: picks this sister's home coordinates and loops
    the float animation back to them

  * func_80ADA028 -> EnPoSisters_SetupWaitAtHome
    deduced, confidence HIGH: sways in place at full alpha with targeting on

  * func_80ADA094 -> EnPoSisters_SetupWaitForCs
    deduced, confidence HIGH: fades out, becomes a prop and waits for the
    introduction cutscene

  * func_80ADA10C -> EnPoSisters_SetupSpreadRing
    deduced, confidence HIGH: collapses every trail point onto the first
    before the ring spreads

  * func_80ADA1B8 -> EnPoSisters_SetupFormUp
    deduced, confidence HIGH: plays the appear animation and arms this
    sister's entrance delay

  * func_80ADA25C -> EnPoSisters_SetupGatherRing
    deduced, confidence HIGH: starts the ring at radius 32 and sways while
    it closes

  * func_80ADA2BC -> EnPoSisters_SetupTurnToPost
    deduced, confidence HIGH: faces this sister's post yaw and, for the
    first, sets switch 0x1B

  * func_80ADA35C -> EnPoSisters_UpdateTargetHeight
    deduced, confidence HIGH: picks the height this sister floats at: home
    height at the post, the player's height for two of them, a fixed 832 for
    the rest

  * func_80ADA4A8 -> EnPoSisters_Sway
    deduced, confidence HIGH: sways until the timer runs out or the player
    closes to 200 units

  * func_80ADA530 -> EnPoSisters_Float
    deduced, confidence HIGH: drifts forward, circling when the player is
    close and settling back to the sway otherwise

  * func_80ADA6A0 -> EnPoSisters_Circle
    deduced, confidence HIGH: keeps a 0x3000 offset from the player's facing
    while closing, so the sister orbits them

  * func_80ADA7F0 -> EnPoSisters_Attack
    deduced, confidence HIGH: runs the swing out and hands over to the
    charge or the retreat

  * func_80ADA8C0 -> EnPoSisters_Charge
    deduced, confidence HIGH: drives straight at the player at speed 5

  * func_80ADA9E8 -> EnPoSisters_Retreat
    deduced, confidence HIGH: backs away with the float animation and a
    softened collider

  * func_80ADAAA4 -> EnPoSisters_Damage
    deduced, confidence HIGH: rides the knockback out, then flees or dies
    according to remaining health

  * func_80ADAC70 -> EnPoSisters_Flee
    deduced, confidence HIGH: runs away at speed 5 until the flee window
    closes

  * func_80ADAD54 -> EnPoSisters_Disappear
    deduced, confidence HIGH: fades out over the disappear animation and
    hides the sister

  * func_80ADAE6C -> EnPoSisters_Appear
    deduced, confidence HIGH: fades in over the appear animation and
    restores the collider and targeting

  * func_80ADAFC0 -> EnPoSisters_DeathSpiral
    deduced, confidence HIGH: spins the trail points upward into a widening
    spiral as the sister dies

  * func_80ADB17C -> EnPoSisters_BurnOut
    deduced, confidence HIGH: at frame 64 sets this sister's switch flag and
    lights the torch with NA_SE_EV_FLAME_IGNITION

  * func_80ADB2B8 -> EnPoSisters_WaitForPlayer
    deduced, confidence HIGH: cries on the animation loop and starts Meg's
    appearance once the player is within 130 units

  * func_80ADB338 -> EnPoSisters_CopyWait
    deduced, confidence GUESS: holds a decoy near its home until the player
    comes close or the real Meg calls it; the parent field it reads is
    unnamed

  * func_80ADB4B0 -> EnPoSisters_MegAppear
    deduced, confidence HIGH: rises through the appear animation and hands
    over to the copy dance

  * func_80ADB51C -> EnPoSisters_CopyDance
    deduced, confidence GUESS: circles the copies about their parent while
    the introduction plays; named for the pattern rather than an identified
    beat

  * func_80ADB770 -> EnPoSisters_MegFight
    deduced, confidence HIGH: Meg's fight loop: counts her window down and
    picks the next attack or retreat

  * func_80ADB9F0 -> EnPoSisters_Reappear
    deduced, confidence HIGH: fades back in over the appear animation and
    restores targeting for the fourth sister

  * func_80ADBB6C -> EnPoSisters_ReturnHome
    deduced, confidence HIGH: turns onto the home position and settles once
    within 10 units

  * func_80ADBBF4 -> EnPoSisters_WaitAtHome
    deduced, confidence HIGH: faces the player and starts drifting once they
    come within 240 units

  * func_80ADBC88 -> EnPoSisters_WaitForCs
    deduced, confidence HIGH: holds until the introduction cutscene starts,
    then counts this sister into the ring

  * func_80ADBD38 -> EnPoSisters_SpreadRing
    deduced, confidence HIGH: widens the trail ring one unit a frame up to
    radius 32

  * func_80ADBD8C -> EnPoSisters_FormUp
    deduced, confidence HIGH: counts this sister's entrance delay down and
    pops her in with the appear sound

  * func_80ADBEE8 -> EnPoSisters_GatherRing
    deduced, confidence HIGH: closes the trail ring back down to nothing

  * func_80ADBF58 -> EnPoSisters_TurnToPost
    deduced, confidence HIGH: turns onto the post yaw and, for the first
    sister, hands the room lighting over

  * func_80ADC034 -> EnPoSisters_UpdateTargetedTimer
    deduced, confidence HIGH: counts a timer down while the player keeps
    this sister targeted and resets it otherwise

  * func_80ADC10C -> EnPoSisters_UpdateDamage
    deduced, confidence HIGH: applies an AC hit, setting the drop flag and
    entering the damage or death state

  * func_80ADC55C -> EnPoSisters_UpdateBodyColor
    deduced, confidence HIGH: pushes the lantern colour toward red while the
    attack animation plays and back otherwise

  * D_80ADD6F0 -> sPrimColors
    deduced, confidence HIGH: the four sisters' prim colours

  * D_80ADD700 -> sEnvColors
    deduced, confidence HIGH: the four sisters' env colours

  * D_80ADD784 -> sSistersAtPostFlags
    deduced, confidence HIGH: one bit per sister, set as each reaches her
    post; the group waits for all four at 15

  * D_80ADD79C -> sPostYaws
    deduced, confidence HIGH: the yaw each sister faces once she reaches her
    post

  * D_80ADD7A4 -> sPostPositions
    deduced, confidence HIGH: the position each sister burns out at

  * D_80ADD7BC -> sLaughSfxPos
    deduced, confidence HIGH: the fixed point NA_SE_EN_PO_LAUGH is played
    from during the introduction

  * D_80ADD7C8 -> sBodyDLists
    deduced, confidence HIGH: the four sisters' body display lists

  * D_80ADD7D8 -> sFaceDLists
    deduced, confidence HIGH: the four sisters' face display lists

  * D_80ADD7E8 -> sLanternColors
    deduced, confidence HIGH: the four sisters' lantern colours

  * D_80ADD7F8 -> sLanternOffset
    deduced, confidence HIGH: the hand-space point the lantern position is
    transformed from

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../TimeSavers/timesaver_hook_handlers.cpp    |   2 +-
 .../ovl_En_Po_Sisters/z_en_po_sisters.c       | 538 ++++++++++--------
 .../ovl_En_Po_Sisters/z_en_po_sisters.h       |   2 +-
 3 files changed, 304 insertions(+), 238 deletions(-)

diff --git a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
index ee29b761b..22f83cc2c 100644
--- a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
+++ b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
@@ -1145,7 +1145,7 @@ void TimeSaverOnActorInitHandler(void* actorRef) {
                 if (innerActor->id == ACTOR_EN_PO_SISTERS && innerActor->params == 28 &&
                     (CVarGetInteger(CVAR_ENHANCEMENT("TimeSavers.SkipMiscInteractions"), IS_RANDO))) {
                     EnPoSisters* enPoSisters = static_cast<EnPoSisters*>(innerActorRef);
-                    if (enPoSisters->actionFunc == func_80ADB338) {
+                    if (enPoSisters->actionFunc == EnPoSisters_CopyWait) {  // was func_80ADB338 [LLM:GUESS]
                         enPoSisters->unk_19C = 0;
                     }
                 }
diff --git a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
index 10d88cc56..38f960082 100644
--- a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
+++ b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
@@ -20,46 +20,77 @@ void EnPoSisters_Update(Actor* thisx, PlayState* play);
 void EnPoSisters_Draw(Actor* thisx, PlayState* play);
 void EnPoSisters_Reset(void);
 
-void func_80ADA094(EnPoSisters* this, PlayState* play);
-void func_80ADA4A8(EnPoSisters* this, PlayState* play);
-void func_80ADA530(EnPoSisters* this, PlayState* play);
-void func_80ADA6A0(EnPoSisters* this, PlayState* play);
-void func_80ADA7F0(EnPoSisters* this, PlayState* play);
-void func_80ADA8C0(EnPoSisters* this, PlayState* play);
-void func_80ADA9E8(EnPoSisters* this, PlayState* play);
-void func_80ADAAA4(EnPoSisters* this, PlayState* play);
-void func_80ADAC70(EnPoSisters* this, PlayState* play);
-void func_80ADAD54(EnPoSisters* this, PlayState* play);
-void func_80ADAE6C(EnPoSisters* this, PlayState* play);
-void func_80ADAFC0(EnPoSisters* this, PlayState* play);
-void func_80ADB17C(EnPoSisters* this, PlayState* play);
-void func_80ADB2B8(EnPoSisters* this, PlayState* play);
-void func_80ADB338(EnPoSisters* this, PlayState* play);
-void func_80ADB9F0(EnPoSisters* this, PlayState* play);
-void func_80ADB4B0(EnPoSisters* this, PlayState* play);
-void func_80ADB51C(EnPoSisters* this, PlayState* play);
-void func_80ADB770(EnPoSisters* this, PlayState* play);
-void func_80ADBB6C(EnPoSisters* this, PlayState* play);
-void func_80ADBBF4(EnPoSisters* this, PlayState* play);
-void func_80ADBC88(EnPoSisters* this, PlayState* play);
-void func_80ADBD38(EnPoSisters* this, PlayState* play);
-void func_80ADBD8C(EnPoSisters* this, PlayState* play);
-void func_80ADBEE8(EnPoSisters* this, PlayState* play);
-void func_80ADBF58(EnPoSisters* this, PlayState* play);
-
-void func_80AD9AA8(EnPoSisters* this, PlayState* play);
-void func_80AD9C24(EnPoSisters* this, PlayState* play);
-
-void func_80AD9D44(EnPoSisters* this);
-
-static Color_RGBA8 D_80ADD6F0[4] = {
+// LLM generated name (HIGH), was func_80ADA094: fades out, becomes a prop and waits for the introduction cutscene
+void EnPoSisters_SetupWaitForCs(EnPoSisters* this, PlayState* play);  // was func_80ADA094 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA4A8: sways until the timer runs out or the player closes to 200 units
+void EnPoSisters_Sway(EnPoSisters* this, PlayState* play);  // was func_80ADA4A8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA530: drifts forward, circling when the player is close and settling back to the sway otherwise
+void EnPoSisters_Float(EnPoSisters* this, PlayState* play);  // was func_80ADA530 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA6A0: keeps a 0x3000 offset from the player's facing while closing, so the sister orbits them
+void EnPoSisters_Circle(EnPoSisters* this, PlayState* play);  // was func_80ADA6A0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA7F0: runs the swing out and hands over to the charge or the retreat
+void EnPoSisters_Attack(EnPoSisters* this, PlayState* play);  // was func_80ADA7F0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA8C0: drives straight at the player at speed 5
+void EnPoSisters_Charge(EnPoSisters* this, PlayState* play);  // was func_80ADA8C0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADA9E8: backs away with the float animation and a softened collider
+void EnPoSisters_Retreat(EnPoSisters* this, PlayState* play);  // was func_80ADA9E8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADAAA4: rides the knockback out, then flees or dies according to remaining health
+void EnPoSisters_Damage(EnPoSisters* this, PlayState* play);  // was func_80ADAAA4 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADAC70: runs away at speed 5 until the flee window closes
+void EnPoSisters_Flee(EnPoSisters* this, PlayState* play);  // was func_80ADAC70 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADAD54: fades out over the disappear animation and hides the sister
+void EnPoSisters_Disappear(EnPoSisters* this, PlayState* play);  // was func_80ADAD54 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADAE6C: fades in over the appear animation and restores the collider and targeting
+void EnPoSisters_Appear(EnPoSisters* this, PlayState* play);  // was func_80ADAE6C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADAFC0: spins the trail points upward into a widening spiral as the sister dies
+void EnPoSisters_DeathSpiral(EnPoSisters* this, PlayState* play);  // was func_80ADAFC0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADB17C: at frame 64 sets this sister's switch flag and lights the torch with NA_SE_EV_FLAME_IGNITION
+void EnPoSisters_BurnOut(EnPoSisters* this, PlayState* play);  // was func_80ADB17C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADB2B8: cries on the animation loop and starts Meg's appearance once the player is within 130 units
+void EnPoSisters_WaitForPlayer(EnPoSisters* this, PlayState* play);  // was func_80ADB2B8 [LLM:HIGH]
+// LLM generated name (GUESS), was func_80ADB338: holds a decoy near its home until the player comes close or the real Meg calls it; the parent field it reads is unnamed
+void EnPoSisters_CopyWait(EnPoSisters* this, PlayState* play);  // was func_80ADB338 [LLM:GUESS]
+// LLM generated name (HIGH), was func_80ADB9F0: fades back in over the appear animation and restores targeting for the fourth sister
+void EnPoSisters_Reappear(EnPoSisters* this, PlayState* play);  // was func_80ADB9F0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADB4B0: rises through the appear animation and hands over to the copy dance
+void EnPoSisters_MegAppear(EnPoSisters* this, PlayState* play);  // was func_80ADB4B0 [LLM:HIGH]
+// LLM generated name (GUESS), was func_80ADB51C: circles the copies about their parent while the introduction plays; named for the pattern rather than an identified beat
+void EnPoSisters_CopyDance(EnPoSisters* this, PlayState* play);  // was func_80ADB51C [LLM:GUESS]
+// LLM generated name (HIGH), was func_80ADB770: Meg's fight loop: counts her window down and picks the next attack or retreat
+void EnPoSisters_MegFight(EnPoSisters* this, PlayState* play);  // was func_80ADB770 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBB6C: turns onto the home position and settles once within 10 units
+void EnPoSisters_ReturnHome(EnPoSisters* this, PlayState* play);  // was func_80ADBB6C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBBF4: faces the player and starts drifting once they come within 240 units
+void EnPoSisters_WaitAtHome(EnPoSisters* this, PlayState* play);  // was func_80ADBBF4 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBC88: holds until the introduction cutscene starts, then counts this sister into the ring
+void EnPoSisters_WaitForCs(EnPoSisters* this, PlayState* play);  // was func_80ADBC88 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBD38: widens the trail ring one unit a frame up to radius 32
+void EnPoSisters_SpreadRing(EnPoSisters* this, PlayState* play);  // was func_80ADBD38 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBD8C: counts this sister's entrance delay down and pops her in with the appear sound
+void EnPoSisters_FormUp(EnPoSisters* this, PlayState* play);  // was func_80ADBD8C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBEE8: closes the trail ring back down to nothing
+void EnPoSisters_GatherRing(EnPoSisters* this, PlayState* play);  // was func_80ADBEE8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ADBF58: turns onto the post yaw and, for the first sister, hands the room lighting over
+void EnPoSisters_TurnToPost(EnPoSisters* this, PlayState* play);  // was func_80ADBF58 [LLM:HIGH]
+
+// LLM generated name (HIGH), was func_80AD9AA8: spawns the three decoy sisters with params 0x400, 0x800 and 0xC00
+void EnPoSisters_SpawnCopies(EnPoSisters* this, PlayState* play);  // was func_80AD9AA8 [LLM:HIGH]
+// LLM generated name (GUESS), was func_80AD9C24: stops drawing this sister, drops targeting and softens the collider; named for the effect on a decoy, which the body alone does not confirm
+void EnPoSisters_HideCopy(EnPoSisters* this, PlayState* play);  // was func_80AD9C24 [LLM:GUESS]
+
+// LLM generated name (HIGH), was func_80AD9D44: replays the appear animation, faster for the fourth sister, with the appear sound
+void EnPoSisters_SetupReappear(EnPoSisters* this);  // was func_80AD9D44 [LLM:HIGH]
+
+// LLM generated name (HIGH), was D_80ADD6F0: the four sisters' prim colours
+static Color_RGBA8 sPrimColors[4] = {  // was D_80ADD6F0 [LLM:HIGH]
     { 255, 170, 255, 255 },
     { 255, 200, 0, 255 },
     { 0, 170, 255, 255 },
     { 170, 255, 0, 255 },
 };
 
-static Color_RGBA8 D_80ADD700[4] = {
+// LLM generated name (HIGH), was D_80ADD700: the four sisters' env colours
+static Color_RGBA8 sEnvColors[4] = {  // was D_80ADD700 [LLM:HIGH]
     { 100, 0, 255, 255 },
     { 255, 0, 0, 255 },
     { 0, 0, 255, 255 },
@@ -136,7 +167,8 @@ static DamageTable sDamageTable = {
     /* Unknown 2     */ DMG_ENTRY(0, 0x0),
 };
 
-static s32 D_80ADD784 = 0;
+// LLM generated name (HIGH), was D_80ADD784: one bit per sister, set as each reaches her post; the group waits for all four at 15
+static s32 sSistersAtPostFlags = 0;  // was D_80ADD784 [LLM:HIGH]
 
 static InitChainEntry sInitChain[] = {
     ICHAIN_VEC3F_DIV1000(scale, 7, ICHAIN_CONTINUE),
@@ -145,39 +177,46 @@ static InitChainEntry sInitChain[] = {
 
 static Vec3f sZeroVector = { 0.0f, 0.0f, 0.0f };
 
-static s16 D_80ADD79C[4] = { 0xB000, 0xD000, 0x5000, 0x3000 };
+// LLM generated name (HIGH), was D_80ADD79C: the yaw each sister faces once she reaches her post
+static s16 sPostYaws[4] = { 0xB000, 0xD000, 0x5000, 0x3000 };  // was D_80ADD79C [LLM:HIGH]
 
-static Vec3s D_80ADD7A4[4] = {
+// LLM generated name (HIGH), was D_80ADD7A4: the position each sister burns out at
+static Vec3s sPostPositions[4] = {  // was D_80ADD7A4 [LLM:HIGH]
     { -22, 337, -1704 },
     { -431, 879, -3410 },
     { 549, 879, -3410 },
     { 1717, 515, -1340 },
 };
 
-static Vec3f D_80ADD7BC = { 120.0f, 250.0f, -1420.0f };
+// LLM generated name (HIGH), was D_80ADD7BC: the fixed point NA_SE_EN_PO_LAUGH is played from during the introduction
+static Vec3f sLaughSfxPos = { 120.0f, 250.0f, -1420.0f };  // was D_80ADD7BC [LLM:HIGH]
 
-static Gfx* D_80ADD7C8[4] = {
+// LLM generated name (HIGH), was D_80ADD7C8: the four sisters' body display lists
+static Gfx* sBodyDLists[4] = {  // was D_80ADD7C8 [LLM:HIGH]
     gPoeSistersMegBodyDL,
     gPoeSistersJoelleBodyDL,
     gPoeSistersBethBodyDL,
     gPoeSistersAmyBodyDL,
 };
 
-static Gfx* D_80ADD7D8[4] = {
+// LLM generated name (HIGH), was D_80ADD7D8: the four sisters' face display lists
+static Gfx* sFaceDLists[4] = {  // was D_80ADD7D8 [LLM:HIGH]
     gPoeSistersMegFaceDL,
     gPoeSistersJoelleFaceDL,
     gPoeSistersBethFaceDL,
     gPoSistersAmyFaceDL,
 };
 
-static Color_RGBA8 D_80ADD7E8[4] = {
+// LLM generated name (HIGH), was D_80ADD7E8: the four sisters' lantern colours
+static Color_RGBA8 sLanternColors[4] = {  // was D_80ADD7E8 [LLM:HIGH]
     { 80, 0, 100, 0 },
     { 80, 15, 0, 0 },
     { 0, 70, 50, 0 },
     { 70, 70, 0, 0 },
 };
 
-static Vec3f D_80ADD7F8 = { 1000.0f, -1700.0f, 0.0f };
+// LLM generated name (HIGH), was D_80ADD7F8: the hand-space point the lantern position is transformed from
+static Vec3f sLanternOffset = { 1000.0f, -1700.0f, 0.0f };  // was D_80ADD7F8 [LLM:HIGH]
 
 void EnPoSisters_Init(Actor* thisx, PlayState* play) {
     EnPoSisters* this = (EnPoSisters*)thisx;
@@ -209,20 +248,20 @@ void EnPoSisters_Init(Actor* thisx, PlayState* play) {
     this->unk_294 = 110.0f;
     this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
     if (this->actor.params & 0x1000) {
-        func_80ADA094(this, play);
+        EnPoSisters_SetupWaitForCs(this, play);  // was func_80ADA094 [LLM:HIGH]
     } else if (this->unk_194 == 0) {
         if (this->unk_195 == 0) {
             this->collider.base.ocFlags1 = OC1_ON | OC1_TYPE_PLAYER;
-            func_80AD9AA8(this, play);
+            EnPoSisters_SpawnCopies(this, play);  // was func_80AD9AA8 [LLM:HIGH]
         } else {
             this->actor.flags &= ~(ACTOR_FLAG_HOOKSHOT_PULLS_ACTOR | ACTOR_FLAG_CAN_ATTACH_TO_ARROW);
             this->collider.info.elemType = ELEMTYPE_UNK4;
             this->collider.info.bumper.dmgFlags |= 1;
             this->collider.base.ocFlags1 = OC1_NONE;
-            func_80AD9C24(this, NULL);
+            EnPoSisters_HideCopy(this, NULL);  // was func_80AD9C24 [LLM:GUESS]
         }
     } else {
-        func_80AD9D44(this);
+        EnPoSisters_SetupReappear(this);  // was func_80AD9D44 [LLM:HIGH]
     }
     this->actor.params &= 0x3F;
 }
@@ -237,7 +276,8 @@ void EnPoSisters_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyCylinder(play, &this->collider);
 }
 
-void func_80AD9240(EnPoSisters* this, s32 arg1, Vec3f* arg2) {
+// LLM generated name (HIGH), was func_80AD9240: lays the trail points out on a ring around a centre, its radius growing with the caller's step
+void EnPoSisters_UpdateTrailRing(EnPoSisters* this, s32 arg1, Vec3f* arg2) {
     f32 temp_f20 = SQ(arg1) * 0.1f;
     Vec3f* vec;
     s32 i;
@@ -250,27 +290,31 @@ void func_80AD9240(EnPoSisters* this, s32 arg1, Vec3f* arg2) {
     }
 }
 
-void func_80AD9368(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD9368: loops gPoeSistersSwayAnim in place with a short timer
+void EnPoSisters_SetupSway(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersSwayAnim, -3.0f);
     this->unk_19A = Rand_S16Offset(2, 3);
-    this->actionFunc = func_80ADA4A8;
+    this->actionFunc = EnPoSisters_Sway;  // was func_80ADA4A8 [LLM:HIGH]
     this->actor.speedXZ = 0.0f;
 }
 
-void func_80AD93C4(EnPoSisters* this) {
-    if (this->actionFunc != func_80ADA6A0) {
+// LLM generated name (HIGH), was func_80AD93C4: loops gPoeSistersFloatAnim and arms the drifting timer
+void EnPoSisters_SetupFloat(EnPoSisters* this) {
+    if (this->actionFunc != EnPoSisters_Circle) {  // was func_80ADA6A0 [LLM:HIGH]
         Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFloatAnim, -3.0f);
     }
     this->unk_19A = Rand_S16Offset(0xF, 3);
     this->unk_199 |= 7;
-    this->actionFunc = func_80ADA530;
+    this->actionFunc = EnPoSisters_Float;  // was func_80ADA530 [LLM:HIGH]
 }
 
-void func_80AD943C(EnPoSisters* this) {
-    this->actionFunc = func_80ADA6A0;
+// LLM generated name (HIGH), was func_80AD943C: installs the circling action
+void EnPoSisters_SetupCircle(EnPoSisters* this) {
+    this->actionFunc = EnPoSisters_Circle;  // was func_80ADA6A0 [LLM:HIGH]
 }
 
-void func_80AD944C(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD944C: morphs to gPoeSistersAttackAnim, hardens the collider for the lit sister and arms the swing
+void EnPoSisters_SetupAttack(EnPoSisters* this) {
     if (this->unk_22E.a != 0) {
         this->collider.base.colType = COLTYPE_METAL;
         this->collider.base.acFlags |= AC_HARD;
@@ -279,10 +323,11 @@ void func_80AD944C(EnPoSisters* this) {
     this->actor.speedXZ = 0.0f;
     this->unk_19A = Animation_GetLastFrame(&gPoeSistersAttackAnim) * 3 + 3;
     this->unk_199 &= ~2;
-    this->actionFunc = func_80ADA7F0;
+    this->actionFunc = EnPoSisters_Attack;  // was func_80ADA7F0 [LLM:HIGH]
 }
 
-void func_80AD94E0(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD94E0: aims at the player at speed 5 with the attack animation
+void EnPoSisters_SetupCharge(EnPoSisters* this) {
     this->actor.speedXZ = 5.0f;
     if (this->unk_194 == 0) {
         this->collider.base.colType = COLTYPE_METAL;
@@ -292,20 +337,22 @@ void func_80AD94E0(EnPoSisters* this) {
     this->unk_19A = 5;
     this->actor.world.rot.y = this->actor.yawTowardsPlayer;
     this->unk_199 |= 8;
-    this->actionFunc = func_80ADA8C0;
+    this->actionFunc = EnPoSisters_Charge;  // was func_80ADA8C0 [LLM:HIGH]
 }
 
-void func_80AD9568(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD9568: turns away from the player and softens the collider again
+void EnPoSisters_SetupRetreat(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFloatAnim, -3.0f);
     this->actor.world.rot.y = this->actor.yawTowardsPlayer + 0x8000;
     if (this->unk_194 != 0) {
         this->collider.base.colType = COLTYPE_HIT3;
         this->collider.base.acFlags &= ~AC_HARD;
     }
-    this->actionFunc = func_80ADA9E8;
+    this->actionFunc = EnPoSisters_Retreat;  // was func_80ADA9E8 [LLM:HIGH]
 }
 
-void func_80AD95D8(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD95D8: plays gPoeSistersDamagedAnim, aims the knockback away from the attacker and applies the red filter
+void EnPoSisters_SetupDamage(EnPoSisters* this) {
     Animation_MorphToPlayOnce(&this->skelAnime, &gPoeSistersDamagedAnim, -3.0f);
     if (this->collider.base.ac != NULL) {
         this->actor.world.rot.y = (this->collider.info.acHitInfo->toucher.dmgFlags & 0x1F824)
@@ -317,19 +364,21 @@ void func_80AD95D8(EnPoSisters* this) {
     }
     this->unk_199 &= ~0xB;
     Actor_SetColorFilter(&this->actor, 0x4000, 0xFF, 0, 0x10);
-    this->actionFunc = func_80ADAAA4;
+    this->actionFunc = EnPoSisters_Damage;  // was func_80ADAAA4 [LLM:HIGH]
 }
 
-void func_80AD96A4(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD96A4: loops gPoeSistersFleeAnim heading away at speed 5
+void EnPoSisters_SetupFlee(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFleeAnim, -3.0f);
     this->actor.world.rot.y = this->actor.shape.rot.y + 0x8000;
     this->unk_19A = 5;
     this->unk_199 |= 0xB;
     this->actor.speedXZ = 5.0f;
-    this->actionFunc = func_80ADAC70;
+    this->actionFunc = EnPoSisters_Flee;  // was func_80ADAC70 [LLM:HIGH]
 }
 
-void func_80AD9718(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD9718: plays gPoeSistersAppearDisappearAnim with the disappear and laugh sounds
+void EnPoSisters_SetupDisappear(EnPoSisters* this) {
     Animation_Change(&this->skelAnime, &gPoeSistersAppearDisappearAnim, 1.5f, 0.0f,
                      Animation_GetLastFrame(&gPoeSistersAppearDisappearAnim), ANIMMODE_ONCE, -3.0f);
     this->actor.speedXZ = 0.0f;
@@ -338,14 +387,15 @@ void func_80AD9718(EnPoSisters* this) {
     this->unk_199 &= ~5;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_DISAPPEAR);
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
-    this->actionFunc = func_80ADAD54;
+    this->actionFunc = EnPoSisters_Disappear;  // was func_80ADAD54 [LLM:HIGH]
 }
 
-void func_80AD97C8(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AD97C8: eases the hover height between 110 and 170 depending on whether the player is swinging on the ground
+void EnPoSisters_UpdateHoverHeight(EnPoSisters* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     f32 sp20;
 
-    if (this->unk_195 == 0 || this->actionFunc != func_80ADAAA4) {
+    if (this->unk_195 == 0 || this->actionFunc != EnPoSisters_Damage) {  // was func_80ADAAA4 [LLM:HIGH]
         if ((player->meleeWeaponState == 0 || player->meleeWeaponAnimation >= 24) &&
             player->actor.world.pos.y - player->actor.floorHeight < 1.0f) {
             Math_StepToF(&this->unk_294, 110.0f, 3.0f);
@@ -360,12 +410,13 @@ void func_80AD97C8(EnPoSisters* this, PlayState* play) {
     this->actor.world.pos.z = (Math_CosS(this->actor.shape.rot.y + 0x8000) * sp20) + player->actor.world.pos.z;
 }
 
-void func_80AD98F4(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AD98F4: plays the appear animation and, for Meg, restores the draw function at the low hover height
+void EnPoSisters_SetupAppear(EnPoSisters* this, PlayState* play) {
     Animation_Change(&this->skelAnime, &gPoeSistersAppearDisappearAnim, 1.5f, 0.0f,
                      Animation_GetLastFrame(&gPoeSistersAppearDisappearAnim), ANIMMODE_ONCE, -3.0f);
     if (this->unk_194 == 0) {
         this->unk_294 = 110.0f;
-        func_80AD97C8(this, play);
+        EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
         this->unk_22E.a = 0;
         this->actor.draw = EnPoSisters_Draw;
     } else {
@@ -375,29 +426,31 @@ void func_80AD98F4(EnPoSisters* this, PlayState* play) {
     this->actor.speedXZ = 0.0f;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_APPEAR);
     this->unk_199 &= ~1;
-    this->actionFunc = func_80ADAE6C;
+    this->actionFunc = EnPoSisters_Appear;  // was func_80ADAE6C [LLM:HIGH]
 }
 
-void func_80AD99D4(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AD99D4: drops targeting, lifts the body and opens the death one-point cutscene
+void EnPoSisters_SetupDeathSpiral(EnPoSisters* this, PlayState* play) {
     this->unk_19A = 0;
     this->actor.speedXZ = 0.0f;
     this->actor.world.pos.y += 42.0f;
     this->actor.shape.yOffset = -6000.0f;
     this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
     this->unk_199 = 0;
-    this->actionFunc = func_80ADAFC0;
+    this->actionFunc = EnPoSisters_DeathSpiral;  // was func_80ADAFC0 [LLM:HIGH]
     OnePointCutscene_Init(play, 3190, 999, &this->actor, CAM_ID_MAIN);
 }
 
-void func_80AD9A54(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AD9A54: drops the body onto the first trail point and scatters a collectible
+void EnPoSisters_SetupBurnOut(EnPoSisters* this, PlayState* play) {
     this->unk_19A = 0;
     this->actor.world.pos.y = this->unk_234[0].y;
     Item_DropCollectibleRandom(play, &this->actor, &this->actor.world.pos, 0x80);
-    this->actionFunc = func_80ADB17C;
+    this->actionFunc = EnPoSisters_BurnOut;  // was func_80ADB17C [LLM:HIGH]
 }
 
 // Meg spawning fakes
-void func_80AD9AA8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_SpawnCopies(EnPoSisters* this, PlayState* play) {
     Actor* actor1 = Actor_Spawn(&play->actorCtx, play, ACTOR_EN_PO_SISTERS, this->actor.world.pos.x,
                                 this->actor.world.pos.y, this->actor.world.pos.z, 0, 0, 0, 0x400);
     Actor* actor2 = Actor_Spawn(&play->actorCtx, play, ACTOR_EN_PO_SISTERS, this->actor.world.pos.x,
@@ -425,11 +478,11 @@ void func_80AD9AA8(EnPoSisters* this, PlayState* play) {
         Animation_PlayLoop(&this->skelAnime, &gPoeSistersMegCryAnim);
         this->unk_198 = 0;
         this->unk_199 = 160;
-        this->actionFunc = func_80ADB2B8;
+        this->actionFunc = EnPoSisters_WaitForPlayer;  // was func_80ADB2B8 [LLM:HIGH]
     }
 }
 
-void func_80AD9C24(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_HideCopy(EnPoSisters* this, PlayState* play) {
     Vec3f vec;
 
     this->actor.draw = NULL;
@@ -446,10 +499,10 @@ void func_80AD9C24(EnPoSisters* this, PlayState* play) {
                              0);
     }
     Lights_PointSetColorAndRadius(&this->lightInfo, 0, 0, 0, 0);
-    this->actionFunc = func_80ADB338;
+    this->actionFunc = EnPoSisters_CopyWait;  // was func_80ADB338 [LLM:GUESS]
 }
 
-void func_80AD9D44(EnPoSisters* this) {
+void EnPoSisters_SetupReappear(EnPoSisters* this) {
     if (this->unk_194 == 3) {
         Animation_PlayOnce(&this->skelAnime, &gPoeSistersAppearDisappearAnim);
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_APPEAR);
@@ -459,18 +512,20 @@ void func_80AD9D44(EnPoSisters* this) {
     }
     this->unk_22E.a = 0;
     this->unk_199 = 32;
-    this->actionFunc = func_80ADB9F0;
+    this->actionFunc = EnPoSisters_Reappear;  // was func_80ADB9F0 [LLM:HIGH]
 }
 
-void func_80AD9DF0(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AD9DF0: plays the appear animation once and opens Meg's introduction one-point cutscene
+void EnPoSisters_SetupMegAppear(EnPoSisters* this, PlayState* play) {
     Animation_MorphToPlayOnce(&this->skelAnime, &gPoeSistersAppearDisappearAnim, -5.0f);
     this->unk_198 = 1;
     this->unk_199 &= ~0x80;
-    this->actionFunc = func_80ADB4B0;
+    this->actionFunc = EnPoSisters_MegAppear;  // was func_80ADB4B0 [LLM:HIGH]
     OnePointCutscene_Init(play, 3180, 156, &this->actor, CAM_ID_MAIN);
 }
 
-void func_80AD9E60(EnPoSisters* this) {
+// LLM generated name (GUESS), was func_80AD9E60: loops the float animation on the parent sister's position; the dance reading comes from the copies circling in step rather than from this body
+void EnPoSisters_SetupCopyDance(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFloatAnim, -3.0f);
     this->unk_19A = Animation_GetLastFrame(&gPoeSistersFloatAnim) * 7 + 7;
     if (this->actor.parent != NULL) {
@@ -483,20 +538,22 @@ void func_80AD9E60(EnPoSisters* this) {
     if (this->unk_195 == 0) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
     }
-    this->actionFunc = func_80ADB51C;
+    this->actionFunc = EnPoSisters_CopyDance;  // was func_80ADB51C [LLM:GUESS]
 }
 
-void func_80AD9F1C(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD9F1C: restores full alpha and targeting and arms Meg's 300-frame fight window
+void EnPoSisters_SetupMegFight(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFloatAnim, -3.0f);
     this->unk_22E.a = 255;
     this->unk_19A = 300;
     this->unk_19C = 3;
     this->unk_199 |= 9;
     this->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
-    this->actionFunc = func_80ADB770;
+    this->actionFunc = EnPoSisters_MegFight;  // was func_80ADB770 [LLM:HIGH]
 }
 
-void func_80AD9F90(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80AD9F90: picks this sister's home coordinates and loops the float animation back to them
+void EnPoSisters_SetupReturnHome(EnPoSisters* this) {
     if (this->unk_194 == 1) {
         this->actor.home.pos.x = -632.0f;
         this->actor.home.pos.z = -3440.0f;
@@ -506,40 +563,43 @@ void func_80AD9F90(EnPoSisters* this) {
     }
     Animation_PlayLoop(&this->skelAnime, &gPoeSistersFloatAnim);
     this->unk_199 |= 0xA;
-    this->actionFunc = func_80ADBB6C;
+    this->actionFunc = EnPoSisters_ReturnHome;  // was func_80ADBB6C [LLM:HIGH]
     this->actor.speedXZ = 5.0f;
 }
 
-void func_80ADA028(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80ADA028: sways in place at full alpha with targeting on
+void EnPoSisters_SetupWaitAtHome(EnPoSisters* this) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersSwayAnim, -3.0f);
     this->unk_22E.a = 255;
     this->unk_199 |= 0x15;
     this->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
-    this->actionFunc = func_80ADBBF4;
+    this->actionFunc = EnPoSisters_WaitAtHome;  // was func_80ADBBF4 [LLM:HIGH]
     this->actor.speedXZ = 0.0f;
 }
 
-void func_80ADA094(EnPoSisters* this, PlayState* play) {
-    D_80ADD784 = 0;
+void EnPoSisters_SetupWaitForCs(EnPoSisters* this, PlayState* play) {
+    sSistersAtPostFlags = 0;  // was D_80ADD784 [LLM:HIGH]
     this->unk_22E.a = 0;
     this->unk_199 = 128;
     this->unk_19A = 50;
     this->unk_234[0] = this->actor.home.pos;
     Actor_ChangeCategory(play, &play->actorCtx, &this->actor, ACTORCAT_PROP);
-    this->actionFunc = func_80ADBC88;
+    this->actionFunc = EnPoSisters_WaitForCs;  // was func_80ADBC88 [LLM:HIGH]
 }
 
-void func_80ADA10C(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80ADA10C: collapses every trail point onto the first before the ring spreads
+void EnPoSisters_SetupSpreadRing(EnPoSisters* this) {
     s32 i;
 
     this->unk_198 = ARRAY_COUNT(this->unk_234);
     for (i = 0; i < ARRAY_COUNT(this->unk_234); i++) {
         this->unk_234[i] = this->unk_234[0];
     }
-    this->actionFunc = func_80ADBD38;
+    this->actionFunc = EnPoSisters_SpreadRing;  // was func_80ADBD38 [LLM:HIGH]
 }
 
-void func_80ADA1B8(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80ADA1B8: plays the appear animation and arms this sister's entrance delay
+void EnPoSisters_SetupFormUp(EnPoSisters* this) {
     Animation_Change(&this->skelAnime, &gPoeSistersAppearDisappearAnim, 0.833f, 0.0f,
                      Animation_GetLastFrame(&gPoeSistersAppearDisappearAnim), ANIMMODE_ONCE_INTERP, 0.0f);
     if (this->unk_194 == 0 || this->unk_194 == 1) {
@@ -548,38 +608,41 @@ void func_80ADA1B8(EnPoSisters* this) {
         this->unk_19A = 76;
     }
     this->unk_198 = 0;
-    D_80ADD784 = 0;
-    this->actionFunc = func_80ADBD8C;
+    sSistersAtPostFlags = 0;  // was D_80ADD784 [LLM:HIGH]
+    this->actionFunc = EnPoSisters_FormUp;  // was func_80ADBD8C [LLM:HIGH]
 }
 
-void func_80ADA25C(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80ADA25C: starts the ring at radius 32 and sways while it closes
+void EnPoSisters_SetupGatherRing(EnPoSisters* this) {
     Animation_PlayLoop(&this->skelAnime, &gPoeSistersSwayAnim);
     this->unk_198 = 8;
     this->unk_19A = 32;
-    func_80AD9240(this, this->unk_19A, &this->actor.home.pos);
-    this->actionFunc = func_80ADBEE8;
+    EnPoSisters_UpdateTrailRing(this, this->unk_19A, &this->actor.home.pos);  // was func_80AD9240 [LLM:HIGH]
+    this->actionFunc = EnPoSisters_GatherRing;  // was func_80ADBEE8 [LLM:HIGH]
 }
 
-void func_80ADA2BC(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ADA2BC: faces this sister's post yaw and, for the first, sets switch 0x1B
+void EnPoSisters_SetupTurnToPost(EnPoSisters* this, PlayState* play) {
     Animation_MorphToLoop(&this->skelAnime, &gPoeSistersFloatAnim, -3.0f);
     this->unk_198 = 0;
     this->unk_199 = 40;
     this->unk_19A = 90;
     this->unk_196 = 32;
-    this->actor.world.rot.y = D_80ADD79C[this->unk_194];
+    this->actor.world.rot.y = sPostYaws[this->unk_194];  // was D_80ADD79C [LLM:HIGH]
     this->actor.home.pos.y = this->actor.world.pos.y;
     if (this->unk_194 == 0) {
         Flags_SetSwitch(play, 0x1B);
     }
     Audio_PlayActorSound2(&this->actor, NA_SE_EV_FLAME_IGNITION);
-    this->actionFunc = func_80ADBF58;
+    this->actionFunc = EnPoSisters_TurnToPost;  // was func_80ADBF58 [LLM:HIGH]
 }
 
-void func_80ADA35C(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ADA35C: picks the height this sister floats at: home height at the post, the player's height for two of them, a fixed 832 for the rest
+void EnPoSisters_UpdateTargetHeight(EnPoSisters* this, PlayState* play) {
     f32 targetY;
     Player* player = GET_PLAYER(play);
 
-    if (this->actionFunc == func_80ADBF58) {
+    if (this->actionFunc == EnPoSisters_TurnToPost) {  // was func_80ADBF58 [LLM:HIGH]
         targetY = this->actor.home.pos.y;
     } else if (this->unk_194 == 0 || this->unk_194 == 3) {
         targetY = player->actor.world.pos.y + 5.0f;
@@ -594,8 +657,8 @@ void func_80ADA35C(EnPoSisters* this, PlayState* play) {
         this->unk_196--;
     }
     this->actor.world.pos.y += (2.0f + 0.5f * Rand_ZeroOne()) * Math_SinS(this->unk_196 * 0x800);
-    if (this->unk_22E.a == 255 && this->actionFunc != func_80ADA8C0 && this->actionFunc != func_80ADA7F0) {
-        if (this->actionFunc == func_80ADAC70) {
+    if (this->unk_22E.a == 255 && this->actionFunc != EnPoSisters_Charge && this->actionFunc != EnPoSisters_Attack) {  // was func_80ADA7F0 [LLM:HIGH]
+        if (this->actionFunc == EnPoSisters_Flee) {  // was func_80ADAC70 [LLM:HIGH]
             Actor_PlaySfx_Flagged(&this->actor, NA_SE_EN_PO_AWAY - SFX_FLAG);
         } else {
             Actor_PlaySfx_Flagged(&this->actor, NA_SE_EN_PO_FLY - SFX_FLAG);
@@ -603,26 +666,26 @@ void func_80ADA35C(EnPoSisters* this, PlayState* play) {
     }
 }
 
-void func_80ADA4A8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Sway(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (Animation_OnFrame(&this->skelAnime, 0.0f) && this->unk_19A != 0) {
         this->unk_19A--;
     }
     if (this->unk_19A == 0 || this->actor.xzDistToPlayer < 200.0f) {
-        func_80AD93C4(this);
+        EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
     }
 }
 
-void func_80ADA530(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Float(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     Math_StepToF(&this->actor.speedXZ, 1.0f, 0.2f);
     if (Animation_OnFrame(&this->skelAnime, 0.0f) && this->unk_19A != 0) {
         this->unk_19A--;
     }
     if (this->actor.xzDistToPlayer < 200.0f && fabsf(this->actor.yDistToPlayer + 5.0f) < 30.0f) {
-        func_80AD943C(this);
+        EnPoSisters_SetupCircle(this);  // was func_80AD943C [LLM:HIGH]
     } else if (this->unk_19A == 0 && Math_StepToF(&this->actor.speedXZ, 0.0f, 0.2f) != 0) {
-        func_80AD9368(this);
+        EnPoSisters_SetupSway(this);  // was func_80AD9368 [LLM:HIGH]
     }
     if (this->actor.bgCheckFlags & BGCHECKFLAG_WALL) {
         Math_ScaledStepToS(&this->actor.world.rot.y, Actor_WorldYawTowardPoint(&this->actor, &this->actor.home.pos),
@@ -633,7 +696,7 @@ void func_80ADA530(EnPoSisters* this, PlayState* play) {
     }
 }
 
-void func_80ADA6A0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Circle(EnPoSisters* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s16 temp_v0;
 
@@ -648,13 +711,13 @@ void func_80ADA6A0(EnPoSisters* this, PlayState* play) {
         Math_ScaledStepToS(&this->actor.world.rot.y, this->actor.yawTowardsPlayer, 0x71C);
     }
     if (this->actor.xzDistToPlayer < 160.0f && fabsf(this->actor.yDistToPlayer + 5.0f) < 30.0f) {
-        func_80AD944C(this);
+        EnPoSisters_SetupAttack(this);  // was func_80AD944C [LLM:HIGH]
     } else if (this->actor.xzDistToPlayer > 240.0f) {
-        func_80AD93C4(this);
+        EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
     }
 }
 
-void func_80ADA7F0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Attack(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (this->unk_19A != 0) {
         this->unk_19A--;
@@ -664,11 +727,11 @@ void func_80ADA7F0(EnPoSisters* this, PlayState* play) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_ROLL);
     }
     if (this->unk_19A == 0) {
-        func_80AD94E0(this);
+        EnPoSisters_SetupCharge(this);  // was func_80AD94E0 [LLM:HIGH]
     }
 }
 
-void func_80ADA8C0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Charge(EnPoSisters* this, PlayState* play) {
     s32 pad;
 
     SkelAnime_Update(&this->skelAnime);
@@ -680,10 +743,10 @@ void func_80ADA8C0(EnPoSisters* this, PlayState* play) {
         if (this->unk_194 != 0) {
             this->collider.base.colType = COLTYPE_HIT3;
             this->collider.base.acFlags &= ~AC_HARD;
-            func_80AD93C4(this);
+            EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
         } else {
             Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
-            func_80AD9C24(this, play);
+            EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
         }
     }
     if (Animation_OnFrame(&this->skelAnime, 1.0f)) {
@@ -691,32 +754,32 @@ void func_80ADA8C0(EnPoSisters* this, PlayState* play) {
     }
 }
 
-void func_80ADA9E8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Retreat(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     this->actor.shape.rot.y -= (this->actor.speedXZ * 10.0f) * 128.0f;
     if (Math_StepToF(&this->actor.speedXZ, 0.0f, 0.1f) != 0) {
         this->actor.world.rot.y = this->actor.shape.rot.y;
         if (this->unk_194 != 0) {
-            func_80AD93C4(this);
+            EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
         } else {
             Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
-            func_80AD9C24(this, play);
+            EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
         }
     }
 }
 
-void func_80ADAAA4(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Damage(EnPoSisters* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime) && !(this->actor.flags & ACTOR_FLAG_ATTACHED_TO_ARROW)) {
         if (this->actor.colChkInfo.health != 0) {
             if (this->unk_194 != 0) {
-                func_80AD96A4(this);
+                EnPoSisters_SetupFlee(this);  // was func_80AD96A4 [LLM:HIGH]
             } else if (this->unk_195 != 0) {
-                func_80AD9C24(this, NULL);
+                EnPoSisters_HideCopy(this, NULL);  // was func_80AD9C24 [LLM:GUESS]
             } else {
-                func_80AD9C24(this, play);
+                EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
             }
         } else {
-            func_80AD99D4(this, play);
+            EnPoSisters_SetupDeathSpiral(this, play);  // was func_80AD99D4 [LLM:HIGH]
         }
     }
     if (this->unk_195 != 0) {
@@ -724,13 +787,13 @@ void func_80ADAAA4(EnPoSisters* this, PlayState* play) {
                            (this->unk_195 == 2) ? 0x800 : 0x400);
         this->unk_22E.a = ((this->skelAnime.endFrame - this->skelAnime.curFrame) * 255.0f) / this->skelAnime.endFrame;
         this->actor.world.pos.y = this->actor.parent->world.pos.y;
-        func_80AD97C8(this, play);
+        EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
     } else if (this->unk_194 != 0) {
         Math_StepToF(&this->actor.speedXZ, 0.0f, 0.5f);
     }
 }
 
-void func_80ADAC70(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Flee(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     Math_ScaledStepToS(&this->actor.world.rot.y, this->actor.yawTowardsPlayer + 0x8000, 1820);
     if (Animation_OnFrame(&this->skelAnime, 0.0f) && this->unk_19A != 0) {
@@ -739,27 +802,27 @@ void func_80ADAC70(EnPoSisters* this, PlayState* play) {
     if (this->actor.bgCheckFlags & BGCHECKFLAG_WALL) {
         this->actor.world.rot.y = this->actor.shape.rot.y;
         this->unk_199 |= 2;
-        func_80AD9718(this);
+        EnPoSisters_SetupDisappear(this);  // was func_80AD9718 [LLM:HIGH]
     } else if (this->unk_19A == 0 && 240.0f < this->actor.xzDistToPlayer) {
         this->actor.world.rot.y = this->actor.shape.rot.y;
-        func_80AD93C4(this);
+        EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
     }
 }
 
-void func_80ADAD54(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Disappear(EnPoSisters* this, PlayState* play) {
     s32 endFrame;
 
     if (SkelAnime_Update(&this->skelAnime)) {
         this->unk_22E.a = 0;
         this->collider.info.bumper.dmgFlags = 0x00060001;
-        func_80AD93C4(this);
+        EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
     } else {
         endFrame = this->skelAnime.endFrame;
         this->unk_22E.a = ((endFrame - this->skelAnime.curFrame) * 255.0f) / endFrame;
     }
 }
 
-void func_80ADAE6C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Appear(EnPoSisters* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->unk_22E.a = 255;
         if (this->unk_194 != 0) {
@@ -770,20 +833,20 @@ void func_80ADAE6C(EnPoSisters* this, PlayState* play) {
             }
             if (this->unk_19A == 0) {
                 this->unk_197 = 20;
-                func_80AD93C4(this);
+                EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
             }
         } else {
-            func_80AD9F1C(this);
+            EnPoSisters_SetupMegFight(this);  // was func_80AD9F1C [LLM:HIGH]
         }
     } else {
         this->unk_22E.a = (this->skelAnime.curFrame * 255.0f) / this->skelAnime.endFrame;
         if (this->unk_194 == 0) {
-            func_80AD97C8(this, play);
+            EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
         }
     }
 }
 
-void func_80ADAFC0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_DeathSpiral(EnPoSisters* this, PlayState* play) {
     s32 i;
 
     this->unk_19A++;
@@ -803,7 +866,7 @@ void func_80ADAFC0(EnPoSisters* this, PlayState* play) {
         this->unk_234[0].y = this->unk_234[1].y + 2.0f;
         if (this->unk_19A >= 16) {
             if (Math_StepToF(&this->actor.scale.x, 0.0f, 0.001f) != 0) {
-                func_80AD9A54(this, play);
+                EnPoSisters_SetupBurnOut(this, play);  // was func_80AD9A54 [LLM:HIGH]
             }
             this->actor.scale.z = this->actor.scale.x;
             this->actor.scale.y = this->actor.scale.x;
@@ -814,7 +877,7 @@ void func_80ADAFC0(EnPoSisters* this, PlayState* play) {
     }
 }
 
-void func_80ADB17C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_BurnOut(EnPoSisters* this, PlayState* play) {
     this->unk_19A++;
     if (this->unk_19A == 64) {
         Flags_SetSwitch(play, this->actor.params);
@@ -826,21 +889,21 @@ void func_80ADB17C(EnPoSisters* this, PlayState* play) {
         Sfx_PlaySfxCentered(NA_SE_SY_CORRECT_CHIME);
         Actor_Kill(&this->actor);
     } else if (this->unk_19A < 32) {
-        func_80AD9240(this, this->unk_19A, &this->actor.world.pos);
+        EnPoSisters_UpdateTrailRing(this, this->unk_19A, &this->actor.world.pos);  // was func_80AD9240 [LLM:HIGH]
     } else {
-        func_80AD9240(this, 64 - this->unk_19A, &this->actor.world.pos);
+        EnPoSisters_UpdateTrailRing(this, 64 - this->unk_19A, &this->actor.world.pos);  // was func_80AD9240 [LLM:HIGH]
     }
     if (this->unk_19A == 32) {
-        this->actor.world.pos.x = D_80ADD7A4[this->unk_194].x;
-        this->actor.world.pos.y = D_80ADD7A4[this->unk_194].y;
-        this->actor.world.pos.z = D_80ADD7A4[this->unk_194].z;
+        this->actor.world.pos.x = sPostPositions[this->unk_194].x;  // was D_80ADD7A4 [LLM:HIGH]
+        this->actor.world.pos.y = sPostPositions[this->unk_194].y;  // was D_80ADD7A4 [LLM:HIGH]
+        this->actor.world.pos.z = sPostPositions[this->unk_194].z;  // was D_80ADD7A4 [LLM:HIGH]
     }
 }
 
-void func_80ADB2B8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_WaitForPlayer(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (this->actor.xzDistToPlayer < 130.0f) {
-        func_80AD9DF0(this, play);
+        EnPoSisters_SetupMegAppear(this, play);  // was func_80AD9DF0 [LLM:HIGH]
     }
     if (Animation_OnFrame(&this->skelAnime, 0.0f)) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_CRY);
@@ -848,7 +911,7 @@ void func_80ADB2B8(EnPoSisters* this, PlayState* play) {
     this->actor.shape.rot.y = this->actor.yawTowardsPlayer;
 }
 
-void func_80ADB338(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_CopyWait(EnPoSisters* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     EnPoSisters* realMeg = (EnPoSisters*)this->actor.parent;
 
@@ -863,32 +926,32 @@ void func_80ADB338(EnPoSisters* this, PlayState* play) {
         if (this->unk_19C == 0) {
             this->actor.shape.rot.y = (s32)(4.0f * Rand_ZeroOne()) * 0x4000 + this->actor.yawTowardsPlayer;
             this->actor.world.pos.y = player->actor.world.pos.y + 5.0f;
-            func_80AD98F4(this, play);
+            EnPoSisters_SetupAppear(this, play);  // was func_80AD98F4 [LLM:HIGH]
         }
     } else {
-        if (realMeg->actionFunc == func_80ADB51C) {
+        if (realMeg->actionFunc == EnPoSisters_CopyDance) {  // was func_80ADB51C [LLM:GUESS]
             this->actor.draw = EnPoSisters_Draw;
-            func_80AD9E60(this);
-        } else if (realMeg->actionFunc == func_80ADAE6C) {
+            EnPoSisters_SetupCopyDance(this);  // was func_80AD9E60 [LLM:GUESS]
+        } else if (realMeg->actionFunc == EnPoSisters_Appear) {  // was func_80ADAE6C [LLM:HIGH]
             this->actor.shape.rot.y = this->actor.parent->shape.rot.y + this->unk_195 * 0x4000;
             this->actor.world.pos.y = player->actor.world.pos.y + 5.0f;
-            func_80AD98F4(this, play);
-        } else if (realMeg->actionFunc == func_80ADAFC0) {
+            EnPoSisters_SetupAppear(this, play);  // was func_80AD98F4 [LLM:HIGH]
+        } else if (realMeg->actionFunc == EnPoSisters_DeathSpiral) {  // was func_80ADAFC0 [LLM:HIGH]
             Actor_Kill(&this->actor);
         }
     }
 }
 
-void func_80ADB4B0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_MegAppear(EnPoSisters* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
-        func_80AD9E60(this);
+        EnPoSisters_SetupCopyDance(this);  // was func_80AD9E60 [LLM:GUESS]
     }
-    func_80AD97C8(this, play);
+    EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
     this->actor.world.pos.y += 1.0f;
     Actor_SetFocus(&this->actor, 40.0f);
 }
 
-void func_80ADB51C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_CopyDance(EnPoSisters* this, PlayState* play) {
     f32 temp_f2;
     s16 phi_v0;
     s16 phi_a2;
@@ -905,7 +968,7 @@ void func_80ADB51C(EnPoSisters* this, PlayState* play) {
         if (this->unk_195 == 0) {
             Audio_PlayMiniBossBgm(NA_BGM_MINI_BOSS);  // was func_800F5ACC [LLM:HIGH]
         }
-        func_80AD9F1C(this);
+        EnPoSisters_SetupMegFight(this);  // was func_80AD9F1C [LLM:HIGH]
     } else {
         this->actor.world.pos.y += 0.1f;
         temp = this->unk_195;
@@ -935,11 +998,11 @@ void func_80ADB51C(EnPoSisters* this, PlayState* play) {
             Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
         }
     }
-    func_80AD97C8(this, play);
+    EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
     Actor_SetFocus(&this->actor, 40.0f);
 }
 
-void func_80ADB770(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_MegFight(EnPoSisters* this, PlayState* play) {
     s32 temp_v0;
     s32 phi_a0;
 
@@ -973,31 +1036,31 @@ void func_80ADB770(EnPoSisters* this, PlayState* play) {
     }
     if (Actor_WorldDistXZToPoint(&GET_PLAYER(play)->actor, &this->actor.home.pos) > 600.0f) {
         this->unk_199 &= ~0x40;
-        func_80AD9C24(this, play);
+        EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
     } else if (this->unk_19A == 0) {
         if (this->unk_195 == 0) {
-            func_80AD94E0(this);
+            EnPoSisters_SetupCharge(this);  // was func_80AD94E0 [LLM:HIGH]
         } else {
-            func_80AD9C24(this, play);
+            EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
         }
     } else if (this->unk_195 != 0) {
         EnPoSisters* realMeg = (EnPoSisters*)this->actor.parent;
 
-        if (realMeg->actionFunc == func_80ADAAA4) {
-            func_80AD95D8(this);
+        if (realMeg->actionFunc == EnPoSisters_Damage) {  // was func_80ADAAA4 [LLM:HIGH]
+            EnPoSisters_SetupDamage(this);  // was func_80AD95D8 [LLM:HIGH]
         }
     } else if (this->unk_19C == 0) {
         this->unk_19C = -15;
     } else if (this->unk_19C < 0) {
         this->unk_19C++;
         if (this->unk_19C == 0) {
-            func_80AD94E0(this);
+            EnPoSisters_SetupCharge(this);  // was func_80AD94E0 [LLM:HIGH]
         }
     }
-    func_80AD97C8(this, play);
+    EnPoSisters_UpdateHoverHeight(this, play);  // was func_80AD97C8 [LLM:HIGH]
 }
 
-void func_80ADB9F0(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_Reappear(EnPoSisters* this, PlayState* play) {
     f32 div;
 
     if (SkelAnime_Update(&this->skelAnime)) {
@@ -1007,9 +1070,9 @@ void func_80ADB9F0(EnPoSisters* this, PlayState* play) {
             this->actor.home.pos.x = 1992.0f;
             this->actor.home.pos.z = -1440.0f;
             this->unk_199 |= 0x18;
-            func_80AD93C4(this);
+            EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
         } else {
-            func_80AD9F90(this);
+            EnPoSisters_SetupReturnHome(this);  // was func_80AD9F90 [LLM:HIGH]
         }
     } else {
         div = this->skelAnime.curFrame / this->skelAnime.endFrame;
@@ -1021,26 +1084,26 @@ void func_80ADB9F0(EnPoSisters* this, PlayState* play) {
     Actor_SetFocus(&this->actor, 40.0f);
 }
 
-void func_80ADBB6C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_ReturnHome(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (Actor_WorldDistXZToPoint(&this->actor, &this->actor.home.pos) < 10.0f) {
-        func_80ADA028(this);
+        EnPoSisters_SetupWaitAtHome(this);  // was func_80ADA028 [LLM:HIGH]
     } else {
         Math_ScaledStepToS(&this->actor.world.rot.y, Actor_WorldYawTowardPoint(&this->actor, &this->actor.home.pos),
                            1820);
     }
 }
 
-void func_80ADBBF4(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_WaitAtHome(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     Math_ScaledStepToS(&this->actor.world.rot.y, this->actor.yawTowardsPlayer, 1820);
     if (this->actor.xzDistToPlayer < 240.0f && fabsf(this->actor.yDistToPlayer + 5.0f) < 30.0f) {
-        func_80AD93C4(this);
+        EnPoSisters_SetupFloat(this);  // was func_80AD93C4 [LLM:HIGH]
     }
 }
 
-void func_80ADBC88(EnPoSisters* this, PlayState* play) {
-    if (D_80ADD784 != 0 || !Player_InCsMode(play)) {
+void EnPoSisters_WaitForCs(EnPoSisters* this, PlayState* play) {
+    if (sSistersAtPostFlags != 0 || !Player_InCsMode(play)) {  // was D_80ADD784 [LLM:HIGH]
         if (this->unk_19A != 0) {
             this->unk_19A--;
         }
@@ -1048,24 +1111,24 @@ void func_80ADBC88(EnPoSisters* this, PlayState* play) {
             if (this->unk_194 == 0) {
                 OnePointCutscene_Init(play, 3140, 999, NULL, CAM_ID_MAIN);
             }
-            D_80ADD784 = 1;
+            sSistersAtPostFlags = 1;  // was D_80ADD784 [LLM:HIGH]
         }
         if (this->unk_19A == 0) {
-            func_80ADA10C(this);
+            EnPoSisters_SetupSpreadRing(this);  // was func_80ADA10C [LLM:HIGH]
         }
     }
     Actor_PlaySfx_Flagged(&this->actor, NA_SE_EV_TORCH - SFX_FLAG);
 }
 
-void func_80ADBD38(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_SpreadRing(EnPoSisters* this, PlayState* play) {
     this->unk_19A++;
-    func_80AD9240(this, this->unk_19A, &this->actor.home.pos);
+    EnPoSisters_UpdateTrailRing(this, this->unk_19A, &this->actor.home.pos);  // was func_80AD9240 [LLM:HIGH]
     if (this->unk_19A == 32) {
-        func_80ADA1B8(this);
+        EnPoSisters_SetupFormUp(this);  // was func_80ADA1B8 [LLM:HIGH]
     }
 }
 
-void func_80ADBD8C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_FormUp(EnPoSisters* this, PlayState* play) {
     this->unk_19A--;
     if (this->unk_19A == 0) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_APPEAR);
@@ -1074,28 +1137,28 @@ void func_80ADBD8C(EnPoSisters* this, PlayState* play) {
     if (this->unk_19A <= 0) {
         if (SkelAnime_Update(&this->skelAnime)) {
             this->unk_22E.a = 255;
-            D_80ADD784 |= (1 << this->unk_194);
+            sSistersAtPostFlags |= (1 << this->unk_194);  // was D_80ADD784 [LLM:HIGH]
         } else {
             this->unk_22E.a = (this->skelAnime.curFrame * 255.0f) / this->skelAnime.endFrame;
         }
     }
-    if (D_80ADD784 == 15) {
-        func_80ADA25C(this);
+    if (sSistersAtPostFlags == 15) {  // was D_80ADD784 [LLM:HIGH]
+        EnPoSisters_SetupGatherRing(this);  // was func_80ADA25C [LLM:HIGH]
     }
 }
 
-void func_80ADBEE8(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_GatherRing(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (this->unk_19A != 0) {
         this->unk_19A--;
     }
-    func_80AD9240(this, this->unk_19A, &this->actor.home.pos);
+    EnPoSisters_UpdateTrailRing(this, this->unk_19A, &this->actor.home.pos);  // was func_80AD9240 [LLM:HIGH]
     if (this->unk_19A == 0) {
-        func_80ADA2BC(this, play);
+        EnPoSisters_SetupTurnToPost(this, play);  // was func_80ADA2BC [LLM:HIGH]
     }
 }
 
-void func_80ADBF58(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_TurnToPost(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     this->unk_19A--;
     Math_ScaledStepToS(&this->actor.shape.rot.y, this->actor.world.rot.y, 0x500);
@@ -1106,14 +1169,15 @@ void func_80ADBF58(EnPoSisters* this, PlayState* play) {
         Math_StepToF(&this->actor.speedXZ, 5.0f, 0.2f);
     }
     if (this->unk_19A == -70 && this->unk_194 == 1) {
-        SoundSource_PlaySfxAtFixedWorldPos(play, &D_80ADD7BC, 40, NA_SE_EN_PO_LAUGH);
+        SoundSource_PlaySfxAtFixedWorldPos(play, &sLaughSfxPos, 40, NA_SE_EN_PO_LAUGH);  // was D_80ADD7BC [LLM:HIGH]
     }
     if (this->unk_19A < -120) {
         Actor_Kill(&this->actor);
     }
 }
 
-void func_80ADC034(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ADC034: counts a timer down while the player keeps this sister targeted and resets it otherwise
+void EnPoSisters_UpdateTargetedTimer(EnPoSisters* this, PlayState* play) {
     if (this->actor.isTargeted && this->unk_22E.a == 255) {
         if (this->unk_197 != 0) {
             this->unk_197--;
@@ -1126,16 +1190,17 @@ void func_80ADC034(EnPoSisters* this, PlayState* play) {
             this->unk_19C--;
         }
     }
-    if (this->actionFunc != func_80ADA7F0 && this->actionFunc != func_80ADA8C0 && this->actionFunc != func_80ADAAA4) {
+    if (this->actionFunc != EnPoSisters_Attack && this->actionFunc != EnPoSisters_Charge && this->actionFunc != EnPoSisters_Damage) {  // was func_80ADA7F0 [LLM:HIGH]
         if (this->unk_197 == 0) {
-            func_80AD9718(this);
+            EnPoSisters_SetupDisappear(this);  // was func_80AD9718 [LLM:HIGH]
         } else if (this->unk_19C == 0 && this->unk_22E.a == 0) {
-            func_80AD98F4(this, play);
+            EnPoSisters_SetupAppear(this, play);  // was func_80AD98F4 [LLM:HIGH]
         }
     }
 }
 
-void func_80ADC10C(EnPoSisters* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ADC10C: applies an AC hit, setting the drop flag and entering the damage or death state
+void EnPoSisters_UpdateDamage(EnPoSisters* this, PlayState* play) {
     Vec3f sp24;
 
     if (this->collider.base.acFlags & AC_HIT) {
@@ -1144,7 +1209,7 @@ void func_80ADC10C(EnPoSisters* this, PlayState* play) {
         if (this->unk_195 != 0) {
             ((EnPoSisters*)this->actor.parent)->unk_19C--;
             Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_LAUGH2);
-            func_80AD9C24(this, play);
+            EnPoSisters_HideCopy(this, play);  // was func_80AD9C24 [LLM:GUESS]
             if (Rand_ZeroOne() < 0.2f) {
                 sp24.x = this->actor.world.pos.x;
                 sp24.y = this->actor.world.pos.y;
@@ -1159,9 +1224,9 @@ void func_80ADC10C(EnPoSisters* this, PlayState* play) {
         } else if (this->actor.colChkInfo.damageEffect == 0xF) {
             this->actor.world.rot.y = this->actor.shape.rot.y;
             this->unk_199 |= 2;
-            func_80AD98F4(this, play);
+            EnPoSisters_SetupAppear(this, play);  // was func_80AD98F4 [LLM:HIGH]
         } else if (this->unk_194 == 0 && this->actor.colChkInfo.damageEffect == 0xE &&
-                   this->actionFunc == func_80ADB770) {
+                   this->actionFunc == EnPoSisters_MegFight) {  // was func_80ADB770 [LLM:HIGH]
             if (this->unk_19C == 0) {
                 this->unk_19C = -45;
             }
@@ -1173,7 +1238,7 @@ void func_80ADC10C(EnPoSisters* this, PlayState* play) {
                 Audio_PlayActorSound2(&this->actor, NA_SE_EN_PO_SISTER_DEAD);
                 GameInteractor_ExecuteOnEnemyDefeat(&this->actor);
             }
-            func_80AD95D8(this);
+            EnPoSisters_SetupDamage(this);  // was func_80AD95D8 [LLM:HIGH]
         }
     }
 }
@@ -1185,16 +1250,16 @@ void EnPoSisters_Update(Actor* thisx, PlayState* play) {
 
     if (this->collider.base.atFlags & AT_HIT) {
         this->collider.base.atFlags &= ~AT_HIT;
-        func_80AD9568(this);
+        EnPoSisters_SetupRetreat(this);  // was func_80AD9568 [LLM:HIGH]
     }
-    func_80ADC10C(this, play);
+    EnPoSisters_UpdateDamage(this, play);  // was func_80ADC10C [LLM:HIGH]
     if (this->unk_199 & 4) {
-        func_80ADC034(this, play);
+        EnPoSisters_UpdateTargetedTimer(this, play);  // was func_80ADC034 [LLM:HIGH]
     }
     this->actionFunc(this, play);
     if (this->unk_199 & 0x1F) {
         if (this->unk_199 & 8) {
-            func_80ADA35C(this, play);
+            EnPoSisters_UpdateTargetHeight(this, play);  // was func_80ADA35C [LLM:HIGH]
         }
         Actor_MoveXZGravity(&this->actor);
 
@@ -1212,25 +1277,25 @@ void EnPoSisters_Update(Actor* thisx, PlayState* play) {
         }
 
         Collider_UpdateCylinder(&this->actor, &this->collider);
-        if (this->actionFunc == func_80ADA8C0 || this->actionFunc == func_80ADA7F0) {
+        if (this->actionFunc == EnPoSisters_Charge || this->actionFunc == EnPoSisters_Attack) {  // was func_80ADA7F0 [LLM:HIGH]
             this->unk_198++;
             this->unk_198 = CLAMP_MAX(this->unk_198, 8);
-        } else if (this->actionFunc != func_80ADAFC0) {
+        } else if (this->actionFunc != EnPoSisters_DeathSpiral) {  // was func_80ADAFC0 [LLM:HIGH]
             temp = this->unk_198 - 1;
             this->unk_198 = CLAMP_MIN(temp, 1);
         }
-        if (this->actionFunc == func_80ADA8C0) {
+        if (this->actionFunc == EnPoSisters_Charge) {  // was func_80ADA8C0 [LLM:HIGH]
             this->actor.flags |= ACTOR_FLAG_SFX_FOR_PLAYER_BODY_HIT;
             CollisionCheck_SetAT(play, &play->colChkCtx, &this->collider.base);
         }
         if (this->unk_199 & 1) {
             CollisionCheck_SetAC(play, &play->colChkCtx, &this->collider.base);
         }
-        if (this->actionFunc != func_80ADB338) {
+        if (this->actionFunc != EnPoSisters_CopyWait) {  // was func_80ADB338 [LLM:GUESS]
             CollisionCheck_SetOC(play, &play->colChkCtx, &this->collider.base);
         }
         Actor_SetFocus(&this->actor, 40.0f);
-        if (this->actionFunc == func_80ADAC70) {
+        if (this->actionFunc == EnPoSisters_Flee) {  // was func_80ADAC70 [LLM:HIGH]
             this->actor.shape.rot.y = this->actor.world.rot.y + 0x8000;
         } else if (this->unk_199 & 2) {
             this->actor.shape.rot.y = this->actor.world.rot.y;
@@ -1238,7 +1303,8 @@ void EnPoSisters_Update(Actor* thisx, PlayState* play) {
     }
 }
 
-void func_80ADC55C(EnPoSisters* this) {
+// LLM generated name (HIGH), was func_80ADC55C: pushes the lantern colour toward red while the attack animation plays and back otherwise
+void EnPoSisters_UpdateBodyColor(EnPoSisters* this) {
     s16 temp_var;
 
     if (this->skelAnime.animation == &gPoeSistersAttackAnim) {
@@ -1286,16 +1352,16 @@ s32 EnPoSisters_OverrideLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList, Ve
             rot->x += (this->unk_19A * 0x1000) + 0xFFFF1000;
         }
     }
-    if (this->unk_22E.a == 0 || limbIndex == 8 || (this->actionFunc == func_80ADAFC0 && this->unk_19A >= 8)) {
+    if (this->unk_22E.a == 0 || limbIndex == 8 || (this->actionFunc == EnPoSisters_DeathSpiral && this->unk_19A >= 8)) {  // was func_80ADAFC0 [LLM:HIGH]
         *dList = NULL;
     } else if (limbIndex == 9) {
-        *dList = D_80ADD7C8[this->unk_194];
+        *dList = sBodyDLists[this->unk_194];  // was D_80ADD7C8 [LLM:HIGH]
     } else if (limbIndex == 10) {
-        *dList = D_80ADD7D8[this->unk_194];
+        *dList = sFaceDLists[this->unk_194];  // was D_80ADD7D8 [LLM:HIGH]
         gDPPipeSync((*gfxP)++);
         gDPSetEnvColor((*gfxP)++, this->unk_22E.r, this->unk_22E.g, this->unk_22E.b, this->unk_22E.a);
     } else if (limbIndex == 11) {
-        color = &D_80ADD7E8[this->unk_194];
+        color = &sLanternColors[this->unk_194];  // was D_80ADD7E8 [LLM:HIGH]
         gDPPipeSync((*gfxP)++);
         gDPSetEnvColor((*gfxP)++, color->r, color->g, color->b, this->unk_22E.a);
     }
@@ -1307,25 +1373,25 @@ void EnPoSisters_PostLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList, Vec3s
     s32 i;
     s32 pad;
 
-    if (this->actionFunc == func_80ADAFC0 && this->unk_19A >= 8 && limbIndex == 9) {
+    if (this->actionFunc == EnPoSisters_DeathSpiral && this->unk_19A >= 8 && limbIndex == 9) {  // was func_80ADAFC0 [LLM:HIGH]
         gSPMatrix((*gfxP)++, MATRIX_NEWMTX(play->state.gfxCtx), G_MTX_NOPUSH | G_MTX_LOAD | G_MTX_MODELVIEW);
         gSPDisplayList((*gfxP)++, gPoSistersBurnDL);
     }
-    if (limbIndex == 8 && this->actionFunc != func_80ADB2B8) {
+    if (limbIndex == 8 && this->actionFunc != EnPoSisters_WaitForPlayer) {  // was func_80ADB2B8 [LLM:HIGH]
         if (this->unk_199 & 0x20) {
             for (i = this->unk_198 - 1; i > 0; i--) {
                 this->unk_234[i] = this->unk_234[i - 1];
             }
-            Matrix_MultVec3f(&D_80ADD7F8, &this->unk_234[0]);
-        } else if (this->actionFunc == func_80ADBD8C) {
-            Matrix_MultVec3f(&D_80ADD7F8, &this->actor.home.pos);
+            Matrix_MultVec3f(&sLanternOffset, &this->unk_234[0]);  // was D_80ADD7F8 [LLM:HIGH]
+        } else if (this->actionFunc == EnPoSisters_FormUp) {  // was func_80ADBD8C [LLM:HIGH]
+            Matrix_MultVec3f(&sLanternOffset, &this->actor.home.pos);  // was D_80ADD7F8 [LLM:HIGH]
         }
         if (this->unk_198 > 0) {
-            Color_RGBA8* color = &D_80ADD6F0[this->unk_194];
+            Color_RGBA8* color = &sPrimColors[this->unk_194];  // was D_80ADD6F0 [LLM:HIGH]
             f32 temp_f2 = Rand_ZeroOne() * 0.3f + 0.7f;
 
-            if (this->actionFunc == func_80ADB17C || this->actionFunc == func_80ADBD38 ||
-                this->actionFunc == func_80ADBEE8) {
+            if (this->actionFunc == EnPoSisters_BurnOut || this->actionFunc == EnPoSisters_SpreadRing ||  // was func_80ADB17C [LLM:HIGH]
+                this->actionFunc == EnPoSisters_GatherRing) {  // was func_80ADBEE8 [LLM:HIGH]
                 Lights_PointNoGlowSetInfo(&this->lightInfo, this->unk_234[0].x, this->unk_234[0].y + 15.0f,
                                           this->unk_234[0].z, color->r * temp_f2, color->g * temp_f2,
                                           color->b * temp_f2, 200);
@@ -1349,12 +1415,12 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
     f32 phi_f20;
     s32 i;
     u8 spE7 = 0;
-    Color_RGBA8* temp_s1 = &D_80ADD700[this->unk_194];
-    Color_RGBA8* temp_s7 = &D_80ADD6F0[this->unk_194];
+    Color_RGBA8* temp_s1 = &sEnvColors[this->unk_194];  // was D_80ADD700 [LLM:HIGH]
+    Color_RGBA8* temp_s7 = &sPrimColors[this->unk_194];  // was D_80ADD6F0 [LLM:HIGH]
     s32 pad;
 
     OPEN_DISPS(play->state.gfxCtx);
-    func_80ADC55C(this);
+    EnPoSisters_UpdateBodyColor(this);  // was func_80ADC55C [LLM:HIGH]
     Gfx_SetupDL_25Opa(play->state.gfxCtx);
     Gfx_SetupDL_25Xlu(play->state.gfxCtx);
     if (this->unk_22E.a == 255 || this->unk_22E.a == 0) {
@@ -1377,7 +1443,7 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
                Gfx_TwoTexScrollEx(play->state.gfxCtx, 0, 0, 0, 0x20, 0x40, 1, 0, (play->gameplayFrames * -20) % 512,
                                   0x20, 0x80, 0, 0, 0, -20));
     gDPSetEnvColor(POLY_XLU_DISP++, temp_s1->r, temp_s1->g, temp_s1->b, temp_s1->a);
-    if (this->actionFunc == func_80ADB17C) {
+    if (this->actionFunc == EnPoSisters_BurnOut) {  // was func_80ADB17C [LLM:HIGH]
         if (this->unk_19A < 32) {
             phi_s5 = ((32 - this->unk_19A) * 255) / 32;
             phi_f20 = 0.0056000003f;
@@ -1385,13 +1451,13 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
             phi_s5 = (this->unk_19A * 255 - 8160) / 32;
             phi_f20 = 0.0027f;
         }
-    } else if (this->actionFunc == func_80ADBD38) {
+    } else if (this->actionFunc == EnPoSisters_SpreadRing) {  // was func_80ADBD38 [LLM:HIGH]
         phi_s5 = ((32 - this->unk_19A) * 255) / 32;
         phi_f20 = 0.0027f;
-    } else if (this->actionFunc == func_80ADBEE8) {
+    } else if (this->actionFunc == EnPoSisters_GatherRing) {  // was func_80ADBEE8 [LLM:HIGH]
         phi_s5 = ((32 - this->unk_19A) * 255) / 32;
         phi_f20 = 0.0035f;
-    } else if (this->actionFunc == func_80ADBC88) {
+    } else if (this->actionFunc == EnPoSisters_WaitForCs) {  // was func_80ADBC88 [LLM:HIGH]
         //! @bug uninitialised spE7
         phi_s5 = spE7;
         phi_f20 = 0.0027f;
@@ -1400,8 +1466,8 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
         phi_f20 = this->actor.scale.x * 0.5f;
     }
     for (i = 0; i < this->unk_198; i++) {
-        if (this->actionFunc != func_80ADB17C && this->actionFunc != func_80ADBD38 &&
-            this->actionFunc != func_80ADBEE8) {
+        if (this->actionFunc != EnPoSisters_BurnOut && this->actionFunc != EnPoSisters_SpreadRing &&  // was func_80ADB17C [LLM:HIGH]
+            this->actionFunc != EnPoSisters_GatherRing) {  // was func_80ADBEE8 [LLM:HIGH]
             phi_s5 = -i * 31 + 248;
         }
         FrameInterpolation_RecordOpenChild(this, this->epoch + i * 25);
@@ -1409,7 +1475,7 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
         gDPSetPrimColor(POLY_XLU_DISP++, 0x80, 0x80, temp_s7->r, temp_s7->g, temp_s7->b, phi_s5);
         Matrix_Translate(this->unk_234[i].x, this->unk_234[i].y, this->unk_234[i].z, MTXMODE_NEW);
         Matrix_RotateZYX(0, (s16)(Camera_GetCamDirYaw(GET_ACTIVE_CAM(play)) + 0x8000), 0, MTXMODE_APPLY);
-        if (this->actionFunc == func_80ADAFC0) {
+        if (this->actionFunc == EnPoSisters_DeathSpiral) {  // was func_80ADAFC0 [LLM:HIGH]
             phi_f20 = (this->unk_19A - i) * 0.025f + 0.5f;
             phi_f20 = CLAMP(phi_f20, 0.5f, 0.8f) * 0.007f;
         }
@@ -1422,5 +1488,5 @@ void EnPoSisters_Draw(Actor* thisx, PlayState* play) {
 }
 
 void EnPoSisters_Reset(void) {
-    D_80ADD784 = 0;
+    sSistersAtPostFlags = 0;  // was D_80ADD784 [LLM:HIGH]
 }
diff --git a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.h b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.h
index 5f42856a4..8b3128cdf 100644
--- a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.h
+++ b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.h
@@ -32,6 +32,6 @@ typedef struct EnPoSisters {
     u32 epoch;
 } EnPoSisters; // size = 0x0338
 
-void func_80ADB338(EnPoSisters* enPoSisters, PlayState* play);
+void EnPoSisters_CopyWait(EnPoSisters* enPoSisters, PlayState* play);  // was func_80ADB338 [LLM:GUESS]
 
 #endif
-- 
2.55.0

