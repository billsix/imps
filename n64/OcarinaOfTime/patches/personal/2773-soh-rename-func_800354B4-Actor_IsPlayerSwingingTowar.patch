From 16692a03d045d448156ca80487932ed88180cfcc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:35 +0000
Subject: [PATCH 2773/3799] soh: rename func_800354B4 ->
 Actor_IsPlayerSwingingToward

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: true when the player is swinging a melee weapon within range and inside two angle windows; whether this reads as a hit or an approach is not settled by the body
Defined in soh/src/code/z_actor.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                             | 2 +-
 soh/src/code/z_actor.c                              | 3 ++-
 soh/src/overlays/actors/ovl_En_GeldB/z_en_geldb.c   | 8 ++++----
 soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c         | 2 +-
 soh/src/overlays/actors/ovl_En_Torch2/z_en_torch2.c | 2 +-
 soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c         | 8 ++++----
 soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c         | 2 +-
 7 files changed, 14 insertions(+), 13 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 65c02e0b4..1442d1e00 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -546,7 +546,7 @@ void Actor_Noop(Actor* actor, PlayState* play);
 void Gfx_DrawDListOpa(PlayState* play, Gfx* dlist);
 void Gfx_DrawDListXlu(PlayState* play, Gfx* dlist);
 Actor* Actor_FindNearby(PlayState* play, Actor* refActor, s16 actorId, u8 actorCategory, f32 range);
-s32 func_800354B4(PlayState* play, Actor* actor, f32 range, s16 arg3, s16 arg4, s16 arg5);
+s32 Actor_IsPlayerSwingingToward(PlayState* play, Actor* actor, f32 range, s16 arg3, s16 arg4, s16 arg5);  // was func_800354B4 [LLM:GUESS]
 void func_8003555C(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);
 void func_800355B8(PlayState* play, Vec3f* pos);
 u8 func_800355E4(PlayState* play, Collider* collider);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 3c903e38e..7044be2c9 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4761,7 +4761,8 @@ Actor* Actor_FindNearby(PlayState* play, Actor* refActor, s16 actorId, u8 actorC
     return NULL;
 }
 
-s32 func_800354B4(PlayState* play, Actor* actor, f32 range, s16 arg3, s16 arg4, s16 arg5) {
+// LLM generated name (GUESS), was func_800354B4: true when the player is swinging a melee weapon within range and inside two angle windows; whether this reads as a hit or an approach is not settled by the body
+s32 Actor_IsPlayerSwingingToward(PlayState* play, Actor* actor, f32 range, s16 arg3, s16 arg4, s16 arg5) {
     Player* player = GET_PLAYER(play);
     s16 var1;
     s16 var2;
diff --git a/soh/src/overlays/actors/ovl_En_GeldB/z_en_geldb.c b/soh/src/overlays/actors/ovl_En_GeldB/z_en_geldb.c
index 060c0284a..78e77c402 100644
--- a/soh/src/overlays/actors/ovl_En_GeldB/z_en_geldb.c
+++ b/soh/src/overlays/actors/ovl_En_GeldB/z_en_geldb.c
@@ -284,7 +284,7 @@ s32 EnGeldB_ReactToPlayer(PlayState* play, EnGeldB* this, s16 arg2) {
     angleToLink = thisx->yawTowardsPlayer - thisx->shape.rot.y;
     angleToLink = ABS(angleToLink);
 
-    if (func_800354B4(play, thisx, 100.0f, 0x2710, 0x3E80, thisx->shape.rot.y)) {
+    if (Actor_IsPlayerSwingingToward(play, thisx, 100.0f, 0x2710, 0x3E80, thisx->shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         if (player->meleeWeaponAnimation == 0x11) {
             EnGeldB_SetupSpinDodge(this, play);
             return true;
@@ -293,7 +293,7 @@ s32 EnGeldB_ReactToPlayer(PlayState* play, EnGeldB* this, s16 arg2) {
             return true;
         }
     }
-    if (func_800354B4(play, thisx, 100.0f, 0x5DC0, 0x2AA8, thisx->shape.rot.y)) {
+    if (Actor_IsPlayerSwingingToward(play, thisx, 100.0f, 0x5DC0, 0x2AA8, thisx->shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         thisx->shape.rot.y = thisx->world.rot.y = thisx->yawTowardsPlayer;
         if ((thisx->bgCheckFlags & 8) && (ABS(angleToWall) < 0x2EE0) && (thisx->xzDistToPlayer < 90.0f)) {
             EnGeldB_SetupJump(this);
@@ -1144,7 +1144,7 @@ void EnGeldB_Block(EnGeldB* this, PlayState* play) {
         angleToLink = this->actor.yawTowardsPlayer - this->actor.shape.rot.y;
         if ((ABS(angleToLink) <= 0x4000) && (this->actor.xzDistToPlayer < 40.0f) &&
             (ABS(this->actor.yDistToPlayer) < 50.0f)) {
-            if (func_800354B4(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y)) {
+            if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
                 if (player->meleeWeaponAnimation == 0x11) {
                     EnGeldB_SetupSpinDodge(this, play);
                 } else if (play->gameplayFrames & 1) {
@@ -1165,7 +1165,7 @@ void EnGeldB_Block(EnGeldB* this, PlayState* play) {
             EnGeldB_SetupCircle(this);
         }
     } else if ((this->timer == 0) &&
-               func_800354B4(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y)) {
+               Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         if (player->meleeWeaponAnimation == 0x11) {
             EnGeldB_SetupSpinDodge(this, play);
         } else if (!EnGeldB_DodgeRanged(play, this)) {
diff --git a/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c b/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
index 6fad4a530..d2820e5b3 100644
--- a/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
+++ b/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
@@ -285,7 +285,7 @@ void EnIk_InitImpl(Actor* thisx, PlayState* play) {
 // Name from zeldaret/oot (was func_80A745E4): adopted upstream's name for this address; body verified to match upstream at 0.90. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
 s32 EnIk_HandleBlocking(EnIk* this, PlayState* play) {
     if (((this->armorStatusFlag != 0) || (this->actor.params == 0)) &&
-        (func_800354B4(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y) != 0) &&
+        (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x2710, 0x4000, this->actor.shape.rot.y) != 0) &&  // was func_800354B4 [LLM:GUESS]
         (play->gameplayFrames & 1)) {
         EnIk_SetupStopAndBlock(this);
         return true;
diff --git a/soh/src/overlays/actors/ovl_En_Torch2/z_en_torch2.c b/soh/src/overlays/actors/ovl_En_Torch2/z_en_torch2.c
index 3d7ac776d..cc61e6330 100644
--- a/soh/src/overlays/actors/ovl_En_Torch2/z_en_torch2.c
+++ b/soh/src/overlays/actors/ovl_En_Torch2/z_en_torch2.c
@@ -396,7 +396,7 @@ void EnTorch2_Update(Actor* thisx, PlayState* play2) {
 
                     // Handles Dark Link's reaction to sword attack other than jumpslashes
 
-                    if (func_800354B4(play, &this->actor, 120.0f, 0x7FFF, 0x7FFF, this->actor.world.rot.y)) {
+                    if (Actor_IsPlayerSwingingToward(play, &this->actor, 120.0f, 0x7FFF, 0x7FFF, this->actor.world.rot.y)) {  // was func_800354B4 [LLM:GUESS]
                         if ((player->meleeWeaponAnimation == STAB_1H) && (this->actor.xzDistToPlayer < 90.0f)) {
 
                             // Handles the reaction to a one-handed stab. If the conditions are satisfied,
diff --git a/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c b/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
index 73942cff9..434be0fee 100644
--- a/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
+++ b/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
@@ -294,7 +294,7 @@ s32 EnWf_ChangeAction(PlayState* play, EnWf* this, s16 mustChoose) {
     playerYawDiff = this->actor.yawTowardsPlayer - this->actor.shape.rot.y;
     playerYawDiff = ABS(playerYawDiff);
 
-    if (func_800354B4(play, &this->actor, 100.0f, 0x2710, 0x2EE0, this->actor.shape.rot.y)) {
+    if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x2710, 0x2EE0, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         if (player->meleeWeaponAnimation == 0x11) {
             EnWf_SetupBlocking(this);
             return true;
@@ -306,7 +306,7 @@ s32 EnWf_ChangeAction(PlayState* play, EnWf* this, s16 mustChoose) {
         }
     }
 
-    if (func_800354B4(play, &this->actor, 100.0f, 0x5DC0, 0x2AA8, this->actor.shape.rot.y)) {
+    if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x5DC0, 0x2AA8, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         this->actor.shape.rot.y = this->actor.world.rot.y = this->actor.yawTowardsPlayer;
 
         if ((this->actor.bgCheckFlags & BGCHECKFLAG_WALL) && (ABS(wallYawDiff) < 0x2EE0) &&
@@ -1027,7 +1027,7 @@ void EnWf_Blocking(EnWf* this, PlayState* play) {
 
         if ((ABS(yawDiff) <= 0x4000) && (this->actor.xzDistToPlayer < 60.0f) &&
             (ABS(this->actor.yDistToPlayer) < 50.0f)) {
-            if (func_800354B4(play, &this->actor, 100.0f, 10000, 0x4000, this->actor.shape.rot.y)) {
+            if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 10000, 0x4000, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
                 if (player->meleeWeaponAnimation == 0x11) {
                     EnWf_SetupBlocking(this);
                 } else if ((play->gameplayFrames % 2) != 0) {
@@ -1050,7 +1050,7 @@ void EnWf_Blocking(EnWf* this, PlayState* play) {
             EnWf_SetupRunAroundPlayer(this);
         }
     } else if (this->actionTimer == 0) {
-        if (func_800354B4(play, &this->actor, 100.0f, 10000, 0x4000, this->actor.shape.rot.y)) {
+        if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 10000, 0x4000, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
             if (player->meleeWeaponAnimation == 0x11) {
                 EnWf_SetupBlocking(this);
             } else if ((play->gameplayFrames % 2) != 0) {
diff --git a/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c b/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
index 51ef9dffb..80b49f864 100644
--- a/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
+++ b/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
@@ -601,7 +601,7 @@ s32 EnZf_ChooseAction(PlayState* play, EnZf* this) {
     angleToWall = this->actor.wallYaw - this->actor.shape.rot.y;
     angleToWall = ABS(angleToWall);
 
-    if (func_800354B4(play, &this->actor, 100.0f, 0x5DC0, 0x2AA8, this->actor.shape.rot.y)) {
+    if (Actor_IsPlayerSwingingToward(play, &this->actor, 100.0f, 0x5DC0, 0x2AA8, this->actor.shape.rot.y)) {  // was func_800354B4 [LLM:GUESS]
         this->actor.shape.rot.y = this->actor.world.rot.y = this->actor.yawTowardsPlayer;
 
         if ((this->actor.bgCheckFlags & BGCHECKFLAG_WALL) && (ABS(angleToWall) < 0x2EE0) &&
-- 
2.55.0

