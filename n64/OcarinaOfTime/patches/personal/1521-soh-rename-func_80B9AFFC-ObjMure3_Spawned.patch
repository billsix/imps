From 65c6557516458ab75d50b122082fb438adb11c2d Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:24:26 +0000
Subject: [PATCH 1521/3799] soh: rename func_80B9AFFC -> ObjMure3_Spawned

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: tracks which rupees have been collected and, once the player passes 1450 units away, despawns the rest and returns to waiting.
Defined in soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
index b94f91fbf..c64d079b1 100644
--- a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
+++ b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
@@ -22,7 +22,8 @@ void ObjMure3_SetupWaitForPlayer(ObjMure3* this);  // was func_80B9AF54 [LLM:HIG
 void ObjMure3_WaitForPlayer(ObjMure3* this, PlayState* play);  // was func_80B9AF64 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B9AFEC: installs the action that runs while the rupees exist.
 void ObjMure3_SetupSpawned(ObjMure3* this);  // was func_80B9AFEC [LLM:HIGH]
-void func_80B9AFFC(ObjMure3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B9AFFC: tracks which rupees have been collected and, once the player passes 1450 units away, despawns the rest and returns to waiting.
+void ObjMure3_Spawned(ObjMure3* this, PlayState* play);  // was func_80B9AFFC [LLM:HIGH]
 
 const ActorInit Obj_Mure3_InitVars = {
     ACTOR_OBJ_MURE3,
@@ -189,10 +190,10 @@ void ObjMure3_WaitForPlayer(ObjMure3* this, PlayState* play) {
 }
 
 void ObjMure3_SetupSpawned(ObjMure3* this) {
-    this->actionFunc = func_80B9AFFC;
+    this->actionFunc = ObjMure3_Spawned;  // was func_80B9AFFC [LLM:HIGH]
 }
 
-void func_80B9AFFC(ObjMure3* this, PlayState* play) {
+void ObjMure3_Spawned(ObjMure3* this, PlayState* play) {
     ObjMure3_CheckCollected(this, play);  // was func_80B9ADCC [LLM:HIGH]
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) >= SQ(1450.0f)) {
         this->actor.flags &= ~ACTOR_FLAG_UPDATE_CULLING_DISABLED;
-- 
2.55.0

