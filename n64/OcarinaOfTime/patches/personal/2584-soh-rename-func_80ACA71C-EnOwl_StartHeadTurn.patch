From 52d1331a624f2a550b26d274dd22f4e04b751b44 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:09 +0000
Subject: [PATCH 2584/3799] soh: rename func_80ACA71C -> EnOwl_StartHeadTurn

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: picks a direction and arms the head-turn counters
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 31 ++++++++++---------
 1 file changed, 16 insertions(+), 15 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index 47dbeb6d3..ab3deb430 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -335,7 +335,8 @@ void EnOwl_PickHeadTurnDirection(EnOwl* this) {
     }
 }
 
-void func_80ACA71C(EnOwl* this) {
+// LLM generated name (HIGH), was func_80ACA71C: picks a direction and arms the head-turn counters
+void EnOwl_StartHeadTurn(EnOwl* this) {
     EnOwl_PickHeadTurnDirection(this);  // was func_80ACA6C0 [LLM:HIGH]
     this->unk_3F2 = 0;
     this->actionFlags |= 0x10;
@@ -365,7 +366,7 @@ void func_80ACA7E0(EnOwl* this, PlayState* play) {
             EnOwl_SetSwitchAndUnfold(this, play);  // was func_80ACA62C [LLM:HIGH]
         } else {
             this->actionFlags &= ~2;
-            func_80ACA71C(this);
+            EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
             this->actionFunc = EnOwl_WaitForHeadReturn;  // was func_80ACA690 [LLM:GUESS]
         }
         this->actor.flags &= ~ACTOR_FLAG_TALK_OFFER_AUTO_ACCEPTED;
@@ -413,7 +414,7 @@ void func_80ACA998(EnOwl* this, PlayState* play) {
                 break;
         }
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -422,7 +423,7 @@ void func_80ACAA54(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x206A);
         this->actionFunc = func_80ACA998;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -431,7 +432,7 @@ void func_80ACAAC0(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x2069);
         this->actionFunc = func_80ACAA54;
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -463,7 +464,7 @@ void func_80ACAB88(EnOwl* this, PlayState* play) {
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -472,7 +473,7 @@ void func_80ACAC6C(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x206A);
         this->actionFunc = func_80ACAB88;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -499,7 +500,7 @@ void func_80ACAD34(EnOwl* this, PlayState* play) {
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -508,7 +509,7 @@ void func_80ACADF0(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x206A);
         this->actionFunc = func_80ACAD34;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -535,7 +536,7 @@ void func_80ACAEB8(EnOwl* this, PlayState* play) {
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -544,7 +545,7 @@ void func_80ACAF74(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x206A);
         this->actionFunc = func_80ACAEB8;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -677,7 +678,7 @@ void func_80ACB440(EnOwl* this, PlayState* play) {
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -686,7 +687,7 @@ void func_80ACB4FC(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x10C2);
         this->actionFunc = func_80ACB440;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -713,7 +714,7 @@ void func_80ACB5C4(EnOwl* this, PlayState* play) {
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -722,7 +723,7 @@ void func_80ACB680(EnOwl* this, PlayState* play) {
         Message_ContinueTextbox(play, 0x10C6);
         this->actionFunc = func_80ACB5C4;
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-- 
2.55.0

