From 64bb6f712ab269f9778a389c6a30ed92cd064fec Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:33:26 +0000
Subject: [PATCH 1662/3799] soh: rename func_8008A8B8 -> Interface_SetViewport

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: points the interface view at the origin looking down -Z and gives it the caller's viewport rectangle and a 60-degree perspective. Its body is commented out in this tree.
Defined in soh/src/code/z_parameter.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/z_parameter.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/src/code/z_parameter.c b/soh/src/code/z_parameter.c
index 00d6464a4..a6aa1e3e3 100644
--- a/soh/src/code/z_parameter.c
+++ b/soh/src/code/z_parameter.c
@@ -5068,7 +5068,8 @@ void Interface_InitVertices(PlayState* play) {
 }
 
 /*
-void func_8008A8B8(PlayState* play, s32 topY, s32 bottomY, s32 leftX, s32 rightX) {
+// LLM generated name (HIGH), was func_8008A8B8: points the interface view at the origin looking down -Z and gives it the caller's viewport rectangle and a 60-degree perspective. Its body is commented out in this tree.
+void Interface_SetViewport(PlayState* play, s32 topY, s32 bottomY, s32 leftX, s32 rightX) {
     InterfaceContext* interfaceCtx = &play->interfaceCtx;
     Vec3f eye;
     Vec3f lookAt;
-- 
2.55.0

