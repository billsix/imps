From b9a23fc018e35a312fcb7e056186348fc6e1f000 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:29:26 +0000
Subject: [PATCH 1582/3799] soh: rename func_80A911F0 -> EnKakasi3_SetupIdle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: restarts the looping animation and installs the text-picking state.
Defined in soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c   | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
index a798e25cf..522bf68fc 100644
--- a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
+++ b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
@@ -15,7 +15,8 @@ void EnKakasi3_Destroy(Actor* thisx, PlayState* play);
 void EnKakasi3_Update(Actor* thisx, PlayState* play);
 void EnKakasi3_Draw(Actor* thisx, PlayState* play);
 
-void func_80A911F0(EnKakasi3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A911F0: restarts the looping animation and installs the text-picking state.
+void EnKakasi3_SetupIdle(EnKakasi3* this, PlayState* play);  // was func_80A911F0 [LLM:HIGH]
 void func_80A91284(EnKakasi3* this, PlayState* play);
 void func_80A91348(EnKakasi3* this, PlayState* play);
 void func_80A915B8(EnKakasi3* this, PlayState* play);
@@ -81,7 +82,7 @@ void EnKakasi3_Init(Actor* thisx, PlayState* play) {
     this->rot = this->actor.world.rot;
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
     Actor_SetScale(&this->actor, 0.01f);
-    this->actionFunc = func_80A911F0;
+    this->actionFunc = EnKakasi3_SetupIdle;  // was func_80A911F0 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_80A90E28: stops the dance animation and eases the scarecrow's three rotation axes back to the pose it was spawned with; every non-dancing action calls it each frame. The twin of EnKakasi_ReturnToRest.
@@ -175,7 +176,7 @@ void EnKakasi3_Dance(EnKakasi3* this, PlayState* play, s32 arg) {
     }
 }
 
-void func_80A911F0(EnKakasi3* this, PlayState* play) {
+void EnKakasi3_SetupIdle(EnKakasi3* this, PlayState* play) {
     f32 frameCount = Animation_GetLastFrame(&object_ka_Anim_000214);
 
     Animation_Change(&this->skelAnime, &object_ka_Anim_000214, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
@@ -291,7 +292,7 @@ void func_80A91620(EnKakasi3* this, PlayState* play) {
         if (this->camId != SUBCAM_NONE) {
             Camera_SetFinishedFlag(play->cameraPtrs[this->camId]);
         }
-        this->actionFunc = func_80A911F0;
+        this->actionFunc = EnKakasi3_SetupIdle;  // was func_80A911F0 [LLM:HIGH]
         return;
     }
 
@@ -329,7 +330,7 @@ void func_80A917FC(EnKakasi3* this, PlayState* play) {
         play->msgCtx.ocarinaMode = OCARINA_MODE_04;
         Message_CloseTextbox(play);
         OnePointCutscene_EndCutscene(play, this->camId);
-        this->actionFunc = func_80A911F0;
+        this->actionFunc = EnKakasi3_SetupIdle;  // was func_80A911F0 [LLM:HIGH]
     }
 }
 
@@ -402,7 +403,7 @@ void func_80A91A90(EnKakasi3* this, PlayState* play) {
         Message_CloseTextbox(play);
         play->msgCtx.ocarinaMode = OCARINA_MODE_04;
         Player_SetCsActionWithHaltedActors(play, NULL, 7);
-        this->actionFunc = func_80A911F0;
+        this->actionFunc = EnKakasi3_SetupIdle;  // was func_80A911F0 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

