From f03ccd5494a10d9494714d7f25d5b33de2edd32f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:08 +0000
Subject: [PATCH 1743/3799] soh: rename func_80B4B874 -> EnZl1_MoveActor

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enables root motion on the skeleton and queues the animation system's actor move.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index 604c1725a..626751a6b 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -358,7 +358,8 @@ void EnZl1_GetCueEndPos(CsCmdActorCue* npcAction, Vec3f* pos) {
     pos->z = npcAction->endPos.z;
 }
 
-void func_80B4B874(EnZl1* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B4B874: enables root motion on the skeleton and queues the animation system's actor move.
+void EnZl1_MoveActor(EnZl1* this, PlayState* play) {
     this->skelAnime.movementFlags |= 1;
     AnimationContext_SetMoveActor(play, &this->actor, &this->skelAnime, 1.0f);
 }
@@ -392,7 +393,7 @@ void func_80B4B8B4(EnZl1* this, PlayState* play) {
     Vec3f sp48;
 
     SkelAnime_Update(&this->skelAnime);
-    func_80B4B874(this, play);
+    EnZl1_MoveActor(this, play);  // was func_80B4B874 [LLM:HIGH]
     if (play->csCtx.state == CS_STATE_IDLE) {
         this->actionFunc = func_80B4BBC4;
         return;
@@ -473,7 +474,7 @@ void func_80B4BC78(EnZl1* this, PlayState* play) {
         frameCount = Animation_GetLastFrame(&gChildZelda1Anim_11348);
         Animation_Change(&this->skelAnime, &gChildZelda1Anim_11348, 1.0f, 0.0f, frameCount, ANIMMODE_LOOP, -10.0f);
     }
-    func_80B4B874(this, play);
+    EnZl1_MoveActor(this, play);  // was func_80B4B874 [LLM:HIGH]
     npcAction = play->csCtx.npcActions[0];
     if (npcAction != NULL) {
         EnZl1_GetCueStartPos(npcAction, &sp70);  // was func_80B4B7F4 [LLM:HIGH]
-- 
2.55.0

