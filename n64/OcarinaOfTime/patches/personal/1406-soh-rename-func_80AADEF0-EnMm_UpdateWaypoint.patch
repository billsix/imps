From d3b6c6745717b42e369b98b19011be4f6f56806f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:02 +0000
Subject: [PATCH 1406/3799] soh: rename func_80AADEF0 -> EnMm_UpdateWaypoint

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: recomputes the yaw and distance to the current waypoint and advances along the path once the Running Man is close enough to it.
Defined in soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
index d6ad3ded1..d0bdd68fa 100644
--- a/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
+++ b/soh/src/overlays/actors/ovl_En_Mm/z_en_mm.c
@@ -329,7 +329,8 @@ s32 EnMm_GetPathPoint(Path* pathList, Vec3f* pos, s32 pathNum, s32 waypoint) {
     return 0;
 }
 
-s32 func_80AADEF0(EnMm* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AADEF0: recomputes the yaw and distance to the current waypoint and advances along the path once the Running Man is close enough to it.
+s32 EnMm_UpdateWaypoint(EnMm* this, PlayState* play) {
     f32 xDiff;
     f32 zDiff;
     Vec3f waypointPos;
@@ -440,7 +441,7 @@ void func_80AAE294(EnMm* this, PlayState* play) {
             this->skelAnime.playSpeed = 1.0f;
         }
 
-        func_80AADEF0(this, play);
+        EnMm_UpdateWaypoint(this, play);  // was func_80AADEF0 [LLM:HIGH]
 
         if (EnMm_IsDay() == 0) {  // was func_80AADA70 [LLM:HIGH]
             if (this->actor.floorPoly != NULL) {
-- 
2.55.0

