From 5268f23325870c0ea41c41c15329e6fdfbfb02b5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:28:10 +0000
Subject: [PATCH 2441/3799] soh: rename func_80ADFE80 -> EnPoh_SoulWaitForTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: bobs the flame and offers the conversation while the player touches it, fading out when the timer expires
Defined in soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
index 709adf3c4..926de47dc 100644
--- a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
+++ b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
@@ -46,7 +46,8 @@ void EnPoh_Appear(EnPoh* this, PlayState* play);
 void EnPoh_Death(EnPoh* this, PlayState* play);
 // LLM generated name (HIGH), was func_80ADFE28: floats the flame upward while fading it in, then opens the conversation
 void EnPoh_SoulRise(EnPoh* this, PlayState* play);  // was func_80ADFE28 [LLM:HIGH]
-void func_80ADFE80(EnPoh* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ADFE80: bobs the flame and offers the conversation while the player touches it, fading out when the timer expires
+void EnPoh_SoulWaitForTalk(EnPoh* this, PlayState* play);  // was func_80ADFE80 [LLM:HIGH]
 void func_80AE009C(EnPoh* this, PlayState* play);
 void EnPoh_TalkRegular(EnPoh* this, PlayState* play);
 void EnPoh_TalkComposer(EnPoh* this, PlayState* play);
@@ -457,7 +458,7 @@ void EnPoh_Talk(EnPoh* this, PlayState* play) {
     this->unk_198 = 200;
     this->unk_195 = 32;
     this->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
-    this->actionFunc = func_80ADFE80;
+    this->actionFunc = EnPoh_SoulWaitForTalk;  // was func_80ADFE80 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_80ADE950: installs the fade-out action, optionally laughing as the flame goes
@@ -786,7 +787,7 @@ void EnPoh_SoulRise(EnPoh* this, PlayState* play) {
     }
 }
 
-void func_80ADFE80(EnPoh* this, PlayState* play) {
+void EnPoh_SoulWaitForTalk(EnPoh* this, PlayState* play) {
     if (this->unk_198 != 0) {
         this->unk_198--;
     }
-- 
2.55.0

