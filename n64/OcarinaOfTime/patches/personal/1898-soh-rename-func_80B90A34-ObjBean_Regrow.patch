From 21074ff779b8ab177ba33c77e30bd70e26bc6524 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:50:09 +0000
Subject: [PATCH 1898/3799] soh: rename func_80B90A34 -> ObjBean_Regrow

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: grows the plant back, re-enabling its collision, and returns it to waiting for the player once nothing is trampling it.
Defined in soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
index 52b027853..5dfed2d0a 100644
--- a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
+++ b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
@@ -63,7 +63,8 @@ void ObjBean_SetupRegrowDelay(ObjBean* this);  // was func_80B90970 [LLM:HIGH]
 void ObjBean_RegrowDelay(ObjBean* this, PlayState* play);  // was func_80B909B0 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B909F8: shows the plant again and arms the 30-frame regrow.
 void ObjBean_SetupRegrow(ObjBean* this);  // was func_80B909F8 [LLM:HIGH]
-void func_80B90A34(ObjBean* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B90A34: grows the plant back, re-enabling its collision, and returns it to waiting for the player once nothing is trampling it.
+void ObjBean_Regrow(ObjBean* this, PlayState* play);  // was func_80B90A34 [LLM:HIGH]
 void ObjBean_SetupWaitForPlayer(ObjBean* this);
 void ObjBean_GrowWaterPhase4(ObjBean* this, PlayState* play);
 void ObjBean_GrowWaterPhase5(ObjBean* this, PlayState* play);
@@ -868,12 +869,12 @@ void ObjBean_RegrowDelay(ObjBean* this, PlayState* play) {
 }
 
 void ObjBean_SetupRegrow(ObjBean* this) {
-    this->actionFunc = func_80B90A34;
+    this->actionFunc = ObjBean_Regrow;  // was func_80B90A34 [LLM:HIGH]
     ObjBean_SetDrawMode(this, BEAN_STATE_DRAW_PLANT);
     this->timer = 30;
 }
 
-void func_80B90A34(ObjBean* this, PlayState* play) {
+void ObjBean_Regrow(ObjBean* this, PlayState* play) {
     s32 trampled = ObjBean_CheckForHorseTrample(this, play);
 
     ObjBean_GrowPlant(this);  // was func_80B8EE24 [LLM:HIGH]
-- 
2.55.0

