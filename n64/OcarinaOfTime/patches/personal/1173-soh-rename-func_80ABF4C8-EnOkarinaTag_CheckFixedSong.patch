From b655105ddb85061f08e679e3ad25f338e6abdb16 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:38 +0000
Subject: [PATCH 1173/3799] soh: rename func_80ABF4C8 ->
 EnOkarinaTag_CheckFixedSong

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: on a correct song it sets the switch flag and runs the per-type consequence: opening Zora's Domain, speeding the windmill, the Door of Time cutscene, or blowing open the royal family's tomb.
Defined in soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c  | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
index d9bfd45b1..043cc0961 100644
--- a/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
+++ b/soh/src/overlays/actors/ovl_En_Okarina_Tag/z_en_okarina_tag.c
@@ -23,7 +23,8 @@ void func_80ABF708(EnOkarinaTag* this, PlayState* play);
 void EnOkarinaTag_WaitForFixedSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF28C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABF0CC: reads msgCtx.ocarinaMode after that check; a correct song sets the switch flag and plays the chime, anything else just returns to the idle state.
 void EnOkarinaTag_CheckConfiguredSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF0CC [LLM:HIGH]
-void func_80ABF4C8(EnOkarinaTag* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABF4C8: on a correct song it sets the switch flag and runs the per-type consequence: opening Zora's Domain, speeding the windmill, the Door of Time cutscene, or blowing open the royal family's tomb.
+void EnOkarinaTag_CheckFixedSong(EnOkarinaTag* this, PlayState* play);  // was func_80ABF4C8 [LLM:HIGH]
 void func_80ABF7CC(EnOkarinaTag* this, PlayState* play);
 
 const ActorInit En_Okarina_Tag_InitVars = {
@@ -227,7 +228,7 @@ void EnOkarinaTag_WaitForFixedSong(EnOkarinaTag* this, PlayState* play) {
                         break;
                 }
                 player->stateFlags2 |= PLAYER_STATE2_NEAR_OCARINA_ACTOR;
-                this->actionFunc = func_80ABF4C8;
+                this->actionFunc = EnOkarinaTag_CheckFixedSong;  // was func_80ABF4C8 [LLM:HIGH]
             } else if ((this->actor.xzDistToPlayer < (50.0f + this->interactRange)) &&
                        (fabsf(player->actor.world.pos.y - this->actor.world.pos.y) < 40.0f)) {
                 this->unk_15A = 0;
@@ -237,7 +238,7 @@ void EnOkarinaTag_WaitForFixedSong(EnOkarinaTag* this, PlayState* play) {
     }
 }
 
-void func_80ABF4C8(EnOkarinaTag* this, PlayState* play) {
+void EnOkarinaTag_CheckFixedSong(EnOkarinaTag* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     if (play->msgCtx.ocarinaMode == OCARINA_MODE_04) {
-- 
2.55.0

