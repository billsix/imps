From d814d83f4c580ddcdcefa2da09826df3270852a9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:42:37 +0000
Subject: [PATCH 1773/3799] soh: rename func_800B44E0 ->
 DbCamera_HandlePlaybackInput

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: reads the debug controller and starts playback from the first keyframe, closing the recorded spline by terminating its last point.
Defined in soh/src/code/db_camera.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h  | 2 +-
 soh/src/code/db_camera.c | 5 +++--
 2 files changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 18b6eb923..1ac37ebc0 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1494,7 +1494,7 @@ void ShrinkWindow_Update(s32 updateRate);
 // ? DbCamera_LoadPoint(?);, was func_800B41DC [LLM:HIGH]
 // ? DbCamera_StorePoint(?);, was func_800B42C0 [LLM:HIGH]
 // ? DbCamera_LookAtPoint(?);, was func_800B4370 [LLM:HIGH]
-// ? func_800B44E0(?);
+// ? DbCamera_HandlePlaybackInput(?);, was func_800B44E0 [LLM:HIGH]
 // ? DbCamera_PrintPoints(?);
 // ? DbCamera_PrintF32Bytes(?);
 // ? DbCamera_PrintU16Bytes(?);
diff --git a/soh/src/code/db_camera.c b/soh/src/code/db_camera.c
index 8ed04daec..5856941e2 100644
--- a/soh/src/code/db_camera.c
+++ b/soh/src/code/db_camera.c
@@ -283,7 +283,8 @@ s32 DbCamera_LookAtPoint(DbCamera* dbCamera, s16 idx, Camera* cam) {
     return idx;
 }
 
-void func_800B44E0(DbCamera* dbCamera, Camera* cam) {
+// LLM generated name (HIGH), was func_800B44E0: reads the debug controller and starts playback from the first keyframe, closing the recorded spline by terminating its last point.
+void DbCamera_HandlePlaybackInput(DbCamera* dbCamera, Camera* cam) {
     s32 i;
 
     if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_CRIGHT)) {
@@ -932,7 +933,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         DebugCamera_CopyText(0xC, 5, 0, cameraStrings[5]);  // was func_8006376C [LLM:HIGH]
         if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_CRIGHT) &&
             !CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_L)) {
-            func_800B44E0(dbCamera, cam);
+            DbCamera_HandlePlaybackInput(dbCamera, cam);  // was func_800B44E0 [LLM:HIGH]
         } else {
             if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_CRIGHT) &&
                 CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_L)) {
-- 
2.55.0

