From 8e6de0dace42ccf86a802180bfad87835958a18a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:29:58 +0000
Subject: [PATCH 2481/3799] soh: rename func_80A04D90 -> EnElf_UpdateShadow

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: raycasts the floor under the fairy and sets the shadow alpha
Defined in soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c b/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
index 5ceb79984..3c53afc07 100644
--- a/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
+++ b/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
@@ -1231,7 +1231,8 @@ void EnElf_SpawnSparkles(EnElf* this, PlayState* play, s32 sparkleLife) {
                                     sparkleLife);
 }
 
-void func_80A04D90(EnElf* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A04D90: raycasts the floor under the fairy and sets the shadow alpha
+void EnElf_UpdateShadow(EnElf* this, PlayState* play) {
     s32 pad;
     s32 bgId;
 
@@ -1474,7 +1475,7 @@ void func_80A053F0(Actor* thisx, PlayState* play) {
         this->unk_2C7 = 1;
     }
 
-    func_80A04D90(this, play);
+    EnElf_UpdateShadow(this, play);  // was func_80A04D90 [LLM:HIGH]
 }
 
 void EnElf_Update(Actor* thisx, PlayState* play) {
@@ -1486,7 +1487,7 @@ void EnElf_Update(Actor* thisx, PlayState* play) {
     this->timer++;
 
     if (this->fairyFlags & FAIRY_FLAG_BIG) {
-        func_80A04D90(this, play);
+        EnElf_UpdateShadow(this, play);  // was func_80A04D90 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

