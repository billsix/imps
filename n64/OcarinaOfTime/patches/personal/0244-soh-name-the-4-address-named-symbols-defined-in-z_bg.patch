From 9aed80f8e04845b640ffc1b3ab68597f47210abf Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:19 +0000
Subject: [PATCH 244/488] soh: name the 4 address-named symbols defined in
 z_bg_hidan_hrock.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 4 are defined in soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c; updating their references touches 2 files in total.

  * func_808894A4 -> BgHidanHrock_DoNothing
    deduced, confidence HIGH: empty action, installed once the pillar has
    finished falling or when it starts already lowered.

  * func_808894B0 -> BgHidanHrock_Shake
    deduced, confidence HIGH: shakes the pillar in a small circle for twenty
    frames with rumble and the block-shake sfx, then lowers home.pos by the
    per-params drop distance and starts the fall.

  * func_8088960C -> BgHidanHrock_Fall
    deduced, confidence HIGH: accelerates world.pos.y down to the lowered
    home.pos.y, plays the landing sfx and re-enables culling.

  * func_808896B8 -> BgHidanHrock_WaitForHit
    deduced, confidence HIGH: runs the AC check until the pillar is struck,
    then sets the switch flag and starts the shake; meanwhile it bobs 5
    units when the player stands on it.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/soh/Network/Anchor/HookHandlers.cpp       |  4 +--
 .../ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c     | 28 +++++++++++--------
 2 files changed, 18 insertions(+), 14 deletions(-)

diff --git a/soh/soh/Network/Anchor/HookHandlers.cpp b/soh/soh/Network/Anchor/HookHandlers.cpp
index 103016a41..915abe953 100644
--- a/soh/soh/Network/Anchor/HookHandlers.cpp
+++ b/soh/soh/Network/Anchor/HookHandlers.cpp
@@ -36,7 +36,7 @@ void func_8086ED70(BgBombwall* bgBombwall, PlayState* play);
 void BgBreakwall_Wait(BgBreakwall* bgBreakwall, PlayState* play);
 void BgHakaZou_WaitForHit(BgHakaZou* bgHakaZou, PlayState* play);
 void func_808887C4(BgHidanHamstep* bgHidanHamstep, PlayState* play);
-void func_808896B8(BgHidanHrock* bgHidanHrock, PlayState* play);
+void BgHidanHrock_WaitForHit(BgHidanHrock* bgHidanHrock, PlayState* play);  // was func_808896B8 [LLM:HIGH]
 void BgIceShelter_Idle(BgIceShelter* bgIceShelter, PlayState* play);
 void BgIceShelter_SetupMelt(BgIceShelter* bgIceShelter);
 void ObjBombiwa_Break(ObjBombiwa* objBombiwa, PlayState* play);
@@ -247,7 +247,7 @@ void Anchor::RegisterHooks() {
     COND_ID_HOOK(ShouldActorUpdate, ACTOR_BG_HIDAN_HROCK, isConnected, [&](void* refActor, bool* should) {
         BgHidanHrock* actor = static_cast<BgHidanHrock*>(refActor);
 
-        if (actor->actionFunc == func_808896B8 && Flags_GetSwitch(gPlayState, actor->unk_16A)) {
+        if (actor->actionFunc == BgHidanHrock_WaitForHit && Flags_GetSwitch(gPlayState, actor->unk_16A)) {  // was func_808896B8 [LLM:HIGH]
             actor->collider.base.acFlags |= AC_HIT;
         }
     });
diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
index 950d78ed5..2926d42cf 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
@@ -14,9 +14,12 @@ void BgHidanHrock_Destroy(Actor* thisx, PlayState* play);
 void BgHidanHrock_Update(Actor* thisx, PlayState* play);
 void BgHidanHrock_Draw(Actor* thisx, PlayState* play);
 
-void func_8088960C(BgHidanHrock* this, PlayState* play);
-void func_808896B8(BgHidanHrock* this, PlayState* play);
-void func_808894A4(BgHidanHrock* this, PlayState* play);
+// LLM generated name (HIGH), was func_8088960C: accelerates world.pos.y down to the lowered home.pos.y, plays the landing sfx and re-enables culling.
+void BgHidanHrock_Fall(BgHidanHrock* this, PlayState* play);  // was func_8088960C [LLM:HIGH]
+// LLM generated name (HIGH), was func_808896B8: runs the AC check until the pillar is struck, then sets the switch flag and starts the shake; meanwhile it bobs 5 units when the player stands on it.
+void BgHidanHrock_WaitForHit(BgHidanHrock* this, PlayState* play);  // was func_808896B8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808894A4: empty action, installed once the pillar has finished falling or when it starts already lowered.
+void BgHidanHrock_DoNothing(BgHidanHrock* this, PlayState* play);  // was func_808894A4 [LLM:HIGH]
 
 const ActorInit Bg_Hidan_Hrock_InitVars = {
     ACTOR_BG_HIDAN_HROCK,
@@ -113,7 +116,7 @@ void BgHidanHrock_Init(Actor* thisx, PlayState* play) {
     }
 
     if (Flags_GetSwitch(play, this->unk_16A)) {
-        this->actionFunc = func_808894A4;
+        this->actionFunc = BgHidanHrock_DoNothing;  // was func_808894A4 [LLM:HIGH]
         if (thisx->params == 0) {
             thisx->world.pos.y -= 2800.0f;
             thisx->uncullZoneForward = 3000.0f;
@@ -127,7 +130,7 @@ void BgHidanHrock_Init(Actor* thisx, PlayState* play) {
             thisx->flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED | ACTOR_FLAG_DRAW_CULLING_DISABLED;
             thisx->uncullZoneForward = 3000.0f;
         }
-        this->actionFunc = func_808896B8;
+        this->actionFunc = BgHidanHrock_WaitForHit;  // was func_808896B8 [LLM:HIGH]
     }
 
     if (thisx->params == 0) {
@@ -146,10 +149,11 @@ void BgHidanHrock_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyTris(play, &this->collider);
 }
 
-void func_808894A4(BgHidanHrock* this, PlayState* play) {
+void BgHidanHrock_DoNothing(BgHidanHrock* this, PlayState* play) {
 }
 
-void func_808894B0(BgHidanHrock* this, PlayState* play) {
+// LLM generated name (HIGH), was func_808894B0: shakes the pillar in a small circle for twenty frames with rumble and the block-shake sfx, then lowers home.pos by the per-params drop distance and starts the fall.
+void BgHidanHrock_Shake(BgHidanHrock* this, PlayState* play) {
     if (this->unk_168 != 0) {
         this->unk_168--;
     }
@@ -173,13 +177,13 @@ void func_808894B0(BgHidanHrock* this, PlayState* play) {
             this->dyna.actor.home.pos.y -= 240.0f;
         }
 
-        this->actionFunc = func_8088960C;
+        this->actionFunc = BgHidanHrock_Fall;  // was func_8088960C [LLM:HIGH]
         this->dyna.actor.world.pos.x = this->dyna.actor.home.pos.x;
         this->dyna.actor.world.pos.z = this->dyna.actor.home.pos.z;
     }
 }
 
-void func_8088960C(BgHidanHrock* this, PlayState* play) {
+void BgHidanHrock_Fall(BgHidanHrock* this, PlayState* play) {
     this->dyna.actor.velocity.y++;
 
     if (Math_StepToF(&this->dyna.actor.world.pos.y, this->dyna.actor.home.pos.y, this->dyna.actor.velocity.y)) {
@@ -194,14 +198,14 @@ void func_8088960C(BgHidanHrock* this, PlayState* play) {
             }
         }
 
-        this->actionFunc = func_808894A4;
+        this->actionFunc = BgHidanHrock_DoNothing;  // was func_808894A4 [LLM:HIGH]
     }
 }
 
-void func_808896B8(BgHidanHrock* this, PlayState* play) {
+void BgHidanHrock_WaitForHit(BgHidanHrock* this, PlayState* play) {
     if (this->collider.base.acFlags & 2) {
         this->collider.base.acFlags &= ~2;
-        this->actionFunc = func_808894B0;
+        this->actionFunc = BgHidanHrock_Shake;  // was func_808894B0 [LLM:HIGH]
         this->dyna.actor.flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED;
 
         if (this->dyna.actor.params == 0) {
-- 
2.55.0

