From 04e6a94bf6947dbbf27bab8f6a9fef3792f990d1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:19:08 +0000
Subject: [PATCH 3418/3799] soh: rename D_80A7DF10 -> sInsectSpawnCounts

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: a four-entry table beside the insect spawn code; read as how many each type releases
Defined in soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c b/soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c
index cdb10bf43..8c62b71fc 100644
--- a/soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c
+++ b/soh/src/overlays/actors/ovl_En_Insect/z_en_insect.c
@@ -85,7 +85,8 @@ static ColliderJntSphInit sColliderInit = {
     sColliderItemInit,
 };
 
-static u16 D_80A7DF10[] = { 0, 5, 7, 7 };
+// LLM generated name (GUESS), was D_80A7DF10: a four-entry table beside the insect spawn code; read as how many each type releases
+static u16 sInsectSpawnCounts[] = { 0, 5, 7, 7 };  // was D_80A7DF10 [LLM:GUESS]
 
 static InitChainEntry sInitChain[] = {
     ICHAIN_VEC3F_DIV1000(scale, 10, ICHAIN_CONTINUE),
@@ -95,7 +96,7 @@ static InitChainEntry sInitChain[] = {
 };
 
 void EnInsect_InitFlags(EnInsect* this) {
-    this->insectFlags = D_80A7DF10[this->actor.params & 3];
+    this->insectFlags = sInsectSpawnCounts[this->actor.params & 3];  // was D_80A7DF10 [LLM:GUESS]
 }
 
 f32 EnInsect_XZDistanceSquared(Vec3f* v1, Vec3f* v2) {
-- 
2.55.0

