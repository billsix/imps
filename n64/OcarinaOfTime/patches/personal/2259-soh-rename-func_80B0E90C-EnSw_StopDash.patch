From 57dc98a29edb5d0c53ad4bad669fd9b63856dd71 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:19 +0000
Subject: [PATCH 2259/3799] soh: rename func_80B0E90C -> EnSw_StopDash

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: brakes to a halt and turns back toward home
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index e59ca95df..81953d7c8 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -16,7 +16,8 @@ void EnSw_SetupDropIn(EnSw* this, PlayState* play);  // was func_80B0D364 [LLM:H
 void EnSw_WallIdle(EnSw* this, PlayState* play);  // was func_80B0E5E0 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0D590: the resting state: grows or shrinks a night-only gold skulltula, and alternates between still frames and a scuttling wiggle across the surface
 void EnSw_Cling(EnSw* this, PlayState* play);  // was func_80B0D590 [LLM:HIGH]
-void func_80B0E90C(EnSw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B0E90C: brakes to a halt and turns back toward home
+void EnSw_StopDash(EnSw* this, PlayState* play);  // was func_80B0E90C [LLM:HIGH]
 void func_80B0E9BC(EnSw* this, PlayState* play);
 // LLM generated name (HIGH), was func_80B0E728: tracks the player while the lunge timer runs, then dashes at the recorded point or gives up and returns home
 void EnSw_Chase(EnSw* this, PlayState* play);  // was func_80B0E728 [LLM:HIGH]
@@ -888,13 +889,13 @@ void EnSw_Chase(EnSw* this, PlayState* play) {
             }
 
             if (!(Math_Vec3f_DistXYZ(&this->actor.world.pos, &this->unk_448) > 13.0f) || func_8002DDF4(play)) {
-                this->actionFunc = func_80B0E90C;
+                this->actionFunc = EnSw_StopDash;  // was func_80B0E90C [LLM:HIGH]
             }
         }
     }
 }
 
-void func_80B0E90C(EnSw* this, PlayState* play) {
+void EnSw_StopDash(EnSw* this, PlayState* play) {
     s32 pad;
 
     EnSw_MoveToward(this, this->unk_448, 0.0f);  // was func_80B0E314 [LLM:HIGH]
-- 
2.55.0

