From fdd6324792181160b0e64d047dcbbbc437759f92 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:57:24 +0000
Subject: [PATCH 1202/3799] soh: rename func_809B5268 ->
 EnAttackNiw_UpdateFlapping

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: drives the wing and body rotation fields that the limb-draw callback reads, alternating them on a short timer; its mode argument selects between still, gentle and hard flapping.
Defined in soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Attack_Niw/z_en_attack_niw.c        | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c b/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
index 347a58da0..8ad2b15c7 100644
--- a/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Attack_Niw/z_en_attack_niw.c
@@ -75,7 +75,8 @@ void EnAttackNiw_Destroy(Actor* thisx, PlayState* play) {
     }
 }
 
-void func_809B5268(EnAttackNiw* this, PlayState* play, s16 arg2) {
+// LLM generated name (HIGH), was func_809B5268: drives the wing and body rotation fields that the limb-draw callback reads, alternating them on a short timer; its mode argument selects between still, gentle and hard flapping.
+void EnAttackNiw_UpdateFlapping(EnAttackNiw* this, PlayState* play, s16 arg2) {
     if (this->unk_254 == 0) {
         if (arg2 == 0) {
             this->unk_264 = 0.0f;
@@ -242,7 +243,7 @@ void func_809B5670(EnAttackNiw* this, PlayState* play) {
         this->unk_254 = 10;
         this->unk_264 = -10000.0f;
         this->unk_288 = -3000.0f;
-        func_809B5268(this, play, 2);
+        EnAttackNiw_UpdateFlapping(this, play, 2);  // was func_809B5268 [LLM:HIGH]
     }
 }
 
@@ -281,9 +282,9 @@ void func_809B59B0(EnAttackNiw* this, PlayState* play) {
         this->unk_2D0 = this->actor.world.rot.x - 5000.0f;
         this->actionFunc = func_809B5C18;
     } else if (this->actor.bgCheckFlags & BGCHECKFLAG_GROUND) {
-        func_809B5268(this, play, 5);
+        EnAttackNiw_UpdateFlapping(this, play, 5);  // was func_809B5268 [LLM:HIGH]
     } else {
-        func_809B5268(this, play, 2);
+        EnAttackNiw_UpdateFlapping(this, play, 2);  // was func_809B5268 [LLM:HIGH]
     }
 }
 
@@ -295,7 +296,7 @@ void func_809B5C18(EnAttackNiw* this, PlayState* play) {
     Math_SmoothStepToS(&this->actor.world.rot.x, this->unk_2D0, 5, this->unk_2DC, 0);
     Math_ApproachF(&this->unk_2DC, 5000.0f, 1.0f, 100.0f);
     Math_ApproachF(&this->actor.velocity.y, 5.0f, 0.3f, 1.0f);
-    func_809B5268(this, play, 2);
+    EnAttackNiw_UpdateFlapping(this, play, 2);  // was func_809B5268 [LLM:HIGH]
 }
 
 void EnAttackNiw_Update(Actor* thisx, PlayState* play) {
-- 
2.55.0

