From f6e81b22c95556a4e0d94415b15cbf7208ae90bc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 02:01:58 +0000
Subject: [PATCH 3792/3799] soh: rename func_80B59828 -> EnZl3_InitInCollapse

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the type-3 init: if this copy belongs at this entrance it faces down the path at action 28, otherwise it kills itself; on a genuine timer expiry it also restarts the timer, refills health and magic and clears the per-waypoint switch flags
Defined in soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
index 454c6f1c3..48c1e7313 100644
--- a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
+++ b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
@@ -2719,7 +2719,8 @@ s32 EnZl3_ShouldPlayRevengeSfx(EnZl3* this, PlayState* play) {
     return 0;
 }
 
-void func_80B59828(EnZl3* this, PlayState* play) {
+// LLM generated name (GUESS), was func_80B59828: the type-3 init: if this copy belongs at this entrance it faces down the path at action 28, otherwise it kills itself; on a genuine timer expiry it also restarts the timer, refills health and magic and clears the per-waypoint switch flags
+void EnZl3_InitInCollapse(EnZl3* this, PlayState* play) {
     if (EnZl3_ShouldStartCollapseEscape(this, play) || (!EnZl3_GetSwitchFlag(this, play) && EnZl3_IsAtExpectedSpawn(this, play))) {  // was func_80B56EE4 [LLM:HIGH]
         s16 newRotY;
 
@@ -2815,7 +2816,7 @@ void func_80B59B6C(EnZl3* this, PlayState* play) {
             EnZl3_InitInFinal2(this, play);  // was func_80B55780 [LLM:HIGH]
             break;
         case 3:
-            func_80B59828(this, play);
+            EnZl3_InitInCollapse(this, play);  // was func_80B59828 [LLM:GUESS]
             break;
         default:
             osSyncPrintf(VT_FGCOL(RED) " En_Oa3 の arg_data がおかしい!!!!!!!!!!!!!!!!!!!!!!!!!\n" VT_RST);
-- 
2.55.0

