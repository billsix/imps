From d4b724946232f85c0bc30a28622643771ceb4229 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:18 +0000
Subject: [PATCH 1006/3799] soh: rename func_80B86BC8 ->
 ItemShield_OfferUntilHit

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 0 action: offers GI_SHIELD_DEKU from its perch and, on an AC hit, launches the shield into the dropped state.
Defined in soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
index 6d778eab8..898434f27 100644
--- a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
+++ b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
@@ -18,7 +18,8 @@ void ItemShield_Update(Actor* thisx, PlayState* play);
 void ItemShield_Draw(Actor* thisx, PlayState* play);
 
 void func_80B86F68(ItemShield* this, PlayState* play);
-void func_80B86BC8(ItemShield* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B86BC8: the params 0 action: offers GI_SHIELD_DEKU from its perch and, on an AC hit, launches the shield into the dropped state.
+void ItemShield_OfferUntilHit(ItemShield* this, PlayState* play);  // was func_80B86BC8 [LLM:HIGH]
 
 static ColliderCylinderInit sCylinderInit = {
     {
@@ -71,7 +72,7 @@ void ItemShield_Init(Actor* thisx, PlayState* play) {
         case 0:
             ActorShape_Init(&this->actor.shape, 1400.0f, NULL, 0.0f);
             this->actor.shape.rot.x = 0x4000;
-            ItemShield_SetupAction(this, func_80B86BC8);
+            ItemShield_SetupAction(this, ItemShield_OfferUntilHit);  // was func_80B86BC8 [LLM:HIGH]
             break;
 
         case 1:
@@ -123,7 +124,7 @@ void ItemShield_Dropped(ItemShield* this, PlayState* play) {
     }
 }
 
-void func_80B86BC8(ItemShield* this, PlayState* play) {
+void ItemShield_OfferUntilHit(ItemShield* this, PlayState* play) {
     if (Actor_HasParent(&this->actor, play)) {
         Actor_Kill(&this->actor);
         return;
-- 
2.55.0

