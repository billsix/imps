# The upstream checkout and everything build/run produce are disposable;
# only the scripts and patches/ are versioned.  runDir/ also holds
# ROM-derived and personal state (sm64.o2r, saves) that must never be
# committed.
/Ghostship/
/build-cmake/
/bldInstall/
/runDir/
