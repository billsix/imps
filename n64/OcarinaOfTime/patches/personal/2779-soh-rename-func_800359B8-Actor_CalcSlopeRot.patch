From 2bdae0356727d8c220c64af77f0447a54ac686f3 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:36 +0000
Subject: [PATCH 2779/3799] soh: rename func_800359B8 -> Actor_CalcSlopeRot

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: derives a rotation from the floor polygon under the actor so a model can lie along the slope
Defined in soh/src/code/z_actor.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                         | 2 +-
 soh/src/code/z_actor.c                          | 3 ++-
 soh/src/overlays/actors/ovl_En_Tite/z_en_tite.c | 2 +-
 soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c     | 2 +-
 4 files changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 425bfddbe..f5ba4eb6b 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -556,7 +556,7 @@ void Actor_SetDropFlagJntSph(Actor* actor, ColliderJntSph* colBody, s32 freezeFl
 void Actor_GetPitchYawBetween(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3);  // was func_80035844 [LLM:HIGH]
 Actor* Actor_SpawnEnPart(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused,
                      PlayState* play, s16 params, s32 arg8);  // was func_800358DC [LLM:HIGH]
-void func_800359B8(Actor* actor, s16 arg1, Vec3s* arg2);
+void Actor_CalcSlopeRot(Actor* actor, s16 arg1, Vec3s* arg2);  // was func_800359B8 [LLM:HIGH]
 s32 Flags_GetEventChkInf(s32 flag);
 void Flags_SetEventChkInf(s32 flag);
 void Flags_UnsetEventChkInf(s32 flag);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index d31c20ad3..dee6b73db 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4932,7 +4932,8 @@ Actor* Actor_SpawnEnPart(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* ar
     return NULL;
 }
 
-void func_800359B8(Actor* actor, s16 arg1, Vec3s* arg2) {
+// LLM generated name (HIGH), was func_800359B8: derives a rotation from the floor polygon under the actor so a model can lie along the slope
+void Actor_CalcSlopeRot(Actor* actor, s16 arg1, Vec3s* arg2) {
     f32 sp44;
     f32 sp40;
     f32 sp3C;
diff --git a/soh/src/overlays/actors/ovl_En_Tite/z_en_tite.c b/soh/src/overlays/actors/ovl_En_Tite/z_en_tite.c
index d815e31dd..af44dc331 100644
--- a/soh/src/overlays/actors/ovl_En_Tite/z_en_tite.c
+++ b/soh/src/overlays/actors/ovl_En_Tite/z_en_tite.c
@@ -938,7 +938,7 @@ void EnTite_Update(Actor* thisx, PlayState* play) {
 
         // If on ground and currently flipped over, set tektite to be fully upside-down
         if (thisx->bgCheckFlags & 3) {
-            func_800359B8(thisx, thisx->shape.rot.y, &thisx->shape.rot);
+            Actor_CalcSlopeRot(thisx, thisx->shape.rot.y, &thisx->shape.rot);  // was func_800359B8 [LLM:HIGH]
             if (this->flipState >= TEKTITE_FLIPPED) {
                 thisx->shape.rot.z += 0x7FFF;
             }
diff --git a/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c b/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
index a0891c6df..477ad117e 100644
--- a/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
+++ b/soh/src/overlays/actors/ovl_En_Wf/z_en_wf.c
@@ -1317,7 +1317,7 @@ void EnWf_Update(Actor* thisx, PlayState* play) {
     }
 
     if (this->actor.bgCheckFlags & (1 | 2)) {
-        func_800359B8(&this->actor, this->actor.shape.rot.y, &this->actor.shape.rot);
+        Actor_CalcSlopeRot(&this->actor, this->actor.shape.rot.y, &this->actor.shape.rot);  // was func_800359B8 [LLM:HIGH]
     } else {
         Math_SmoothStepToS(&this->actor.shape.rot.x, 0, 1, 1000, 0);
         Math_SmoothStepToS(&this->actor.shape.rot.z, 0, 1, 1000, 0);
-- 
2.55.0

