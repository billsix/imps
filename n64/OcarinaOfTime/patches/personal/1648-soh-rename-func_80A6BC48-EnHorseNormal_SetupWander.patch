From 8a8e304345c6e10b3bf725c7ddee1a4614b6e832 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:32:49 +0000
Subject: [PATCH 1648/3799] soh: rename func_80A6BC48 ->
 EnHorseNormal_SetupWander

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters HORSE_WANDER, stopping the horse and returning it to the idle animation.
Defined in soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Horse_Normal/z_en_horse_normal.c         | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
index 5572f2be7..ec816ddf9 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
@@ -35,7 +35,8 @@ void EnHorseNormal_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80A6B91C: enters HORSE_FOLLOW_PATH at the first waypoint with the gallop animation and speed 7.
 void EnHorseNormal_SetupFollowPath(EnHorseNormal* this, PlayState* play);  // was func_80A6B91C [LLM:HIGH]
-void func_80A6BC48(EnHorseNormal* this);
+// LLM generated name (HIGH), was func_80A6BC48: enters HORSE_WANDER, stopping the horse and returning it to the idle animation.
+void EnHorseNormal_SetupWander(EnHorseNormal* this);  // was func_80A6BC48 [LLM:HIGH]
 void func_80A6BCEC(EnHorseNormal* this);
 void func_80A6C4CC(EnHorseNormal* this);
 void func_80A6C6B0(EnHorseNormal* this);
@@ -270,7 +271,7 @@ void EnHorseNormal_Init(Actor* thisx, PlayState* play) {
     if ((this->actor.params & 0xF0) == 0x10 && (this->actor.params & 0xF) != 0xF) {
         EnHorseNormal_SetupFollowPath(this, play);  // was func_80A6B91C [LLM:HIGH]
     } else {
-        func_80A6BC48(this);
+        EnHorseNormal_SetupWander(this);  // was func_80A6BC48 [LLM:HIGH]
     }
 }
 
@@ -338,7 +339,7 @@ void EnHorseNormal_CycleAnimations(EnHorseNormal* this, PlayState* play) {
     }
 }
 
-void func_80A6BC48(EnHorseNormal* this) {
+void EnHorseNormal_SetupWander(EnHorseNormal* this) {
     this->action = HORSE_WANDER;
     this->animationIdx = 0;
     this->unk_21C = 0;
-- 
2.55.0

