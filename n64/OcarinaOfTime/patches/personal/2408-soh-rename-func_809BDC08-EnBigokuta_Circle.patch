From d03a1338f690ddc0833636ad51fd99d1a33e0ce3 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:27 +0000
Subject: [PATCH 2408/3799] soh: rename func_809BDC08 -> EnBigokuta_Circle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: circles the ring, speeding up or slowing down with the player's distance and facing, and picks the next state when its windows expire
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 5ad411c2f..b908ab698 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -24,7 +24,8 @@ void EnBigokuta_JumpIn(EnBigokuta* this, PlayState* play);  // was func_809BD8DC
 void EnBigokuta_TurnToStart(EnBigokuta* this, PlayState* play);  // was func_809BDAE8 [LLM:HIGH]
 // LLM generated name (HIGH), was func_809BDB90: counts the pause out and starts the miniboss music for the params 3 spawn
 void EnBigokuta_WaitBeforeCircle(EnBigokuta* this, PlayState* play);  // was func_809BDB90 [LLM:HIGH]
-void func_809BDC08(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BDC08: circles the ring, speeding up or slowing down with the player's distance and facing, and picks the next state when its windows expire
+void EnBigokuta_Circle(EnBigokuta* this, PlayState* play);  // was func_809BDC08 [LLM:HIGH]
 void func_809BE3E4(EnBigokuta* this, PlayState* play);
 void func_809BE4A4(EnBigokuta* this, PlayState* play);
 void func_809BE518(EnBigokuta* this, PlayState* play);
@@ -325,7 +326,7 @@ void EnBigokuta_SetupCircle(EnBigokuta* this) {
     EnBigokuta_UpdateOrbitPos(this);  // was func_809BCE3C [LLM:HIGH]
     this->cylinder[0].base.atFlags |= AT_ON;
     this->collider.base.acFlags |= AC_ON;
-    this->actionFunc = func_809BDC08;
+    this->actionFunc = EnBigokuta_Circle;  // was func_809BDC08 [LLM:HIGH]
 }
 
 // LLM generated name (GUESS), was func_809BD47C: the damageEffect 0xF response: turns AC off and arms a 16-frame arc; whether this reads as a bounce or a recoil is not settled by the code
@@ -500,7 +501,7 @@ void EnBigokuta_WaitBeforeCircle(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BDC08(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_Circle(EnBigokuta* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s16 phi_v0;
     s16 pad;
@@ -759,7 +760,7 @@ void func_809BE798(EnBigokuta* this, PlayState* play) {
             effectRot = -0x6000;
         }
         Actor_SetPlayerKnockbackLargeNoDamage(play, &this->actor, 10.0f, this->actor.world.rot.y + effectRot, 5.0f);
-        if (this->actionFunc == func_809BDC08) {
+        if (this->actionFunc == EnBigokuta_Circle) {  // was func_809BDC08 [LLM:HIGH]
             EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
             this->unk_196 = 40;
         } else if (this->actionFunc == func_809BE3E4) {
-- 
2.55.0

