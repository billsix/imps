From 356b2a73ab02299483c95d74dfbccf4b3dc2bee7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:36 +0000
Subject: [PATCH 2777/3799] soh: rename func_80035844 ->
 Actor_GetPitchYawBetween

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: writes the pitch and yaw from one point to another into a Vec3s, optionally flipping the vertical sense
Defined in soh/src/code/z_actor.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                              |  2 +-
 soh/src/code/z_actor.c                               |  3 ++-
 .../actors/ovl_Bg_Mizu_Movebg/z_bg_mizu_movebg.c     |  2 +-
 soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c      |  4 ++--
 soh/src/overlays/actors/ovl_En_Ba/z_en_ba.c          | 12 ++++++------
 soh/src/overlays/actors/ovl_En_Dha/z_en_dha.c        |  6 +++---
 6 files changed, 15 insertions(+), 14 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 28d815563..10d95c017 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -553,7 +553,7 @@ u8 Actor_IsHitByPlayerSpinAttack(PlayState* play, Collider* collider);  // was f
 u8 Actor_ApplyDamage(Actor* actor);
 void Actor_SetDropFlag(Actor* actor, ColliderInfo* colBody, s32 freezeFlag);
 void Actor_SetDropFlagJntSph(Actor* actor, ColliderJntSph* colBody, s32 freezeFlag);
-void func_80035844(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3);
+void Actor_GetPitchYawBetween(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3);  // was func_80035844 [LLM:HIGH]
 Actor* func_800358DC(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused,
                      PlayState* play, s16 params, s32 arg8);
 void func_800359B8(Actor* actor, s16 arg1, Vec3s* arg2);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 419321b3d..0e0dde958 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4898,7 +4898,8 @@ void Actor_SetDropFlagJntSph(Actor* actor, ColliderJntSph* jntSph, s32 freezeFla
     }
 }
 
-void func_80035844(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3) {
+// LLM generated name (HIGH), was func_80035844: writes the pitch and yaw from one point to another into a Vec3s, optionally flipping the vertical sense
+void Actor_GetPitchYawBetween(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3) {
     f32 dx = arg1->x - arg0->x;
     f32 dz = arg1->z - arg0->z;
     f32 dy = arg3 ? (arg1->y - arg0->y) : (arg0->y - arg1->y);
diff --git a/soh/src/overlays/actors/ovl_Bg_Mizu_Movebg/z_bg_mizu_movebg.c b/soh/src/overlays/actors/ovl_Bg_Mizu_Movebg/z_bg_mizu_movebg.c
index 782f4d6c0..288bc1fc1 100644
--- a/soh/src/overlays/actors/ovl_Bg_Mizu_Movebg/z_bg_mizu_movebg.c
+++ b/soh/src/overlays/actors/ovl_Bg_Mizu_Movebg/z_bg_mizu_movebg.c
@@ -338,7 +338,7 @@ void BgMizuMovebg_UpdateHookshotPlatform(BgMizuMovebg* this, PlayState* play) {
     if (dist < this->dyna.actor.speedXZ) {
         this->dyna.actor.speedXZ = dist;
     }
-    func_80035844(&this->dyna.actor.world.pos, &waypoint, &this->dyna.actor.world.rot, 1);
+    Actor_GetPitchYawBetween(&this->dyna.actor.world.pos, &waypoint, &this->dyna.actor.world.rot, 1);  // was func_80035844 [LLM:HIGH]
     Actor_MoveXYZ(&this->dyna.actor);
     dx = waypoint.x - this->dyna.actor.world.pos.x;
     dy = waypoint.y - this->dyna.actor.world.pos.y;
diff --git a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
index 1a0479571..631e2e307 100644
--- a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
+++ b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
@@ -2529,7 +2529,7 @@ void BossVa_BariIntro(BossVa* this, PlayState* play) {
             if (this->timer == 0) {
                 this->timer2 = 0;
             } else {
-                func_80035844(&GET_BODY(this)->actor.world.pos, &this->actor.world.pos, &this->actor.world.rot, false);
+                Actor_GetPitchYawBetween(&GET_BODY(this)->actor.world.pos, &this->actor.world.pos, &this->actor.world.rot, false);  // was func_80035844 [LLM:HIGH]
                 this->unk_1A0 = Math_Vec3f_DistXYZ(&GET_BODY(this)->actor.world.pos, &this->actor.world.pos);
                 if (sp50 > 30.0f) {
                     BossVa_Spark(play, this, 1, 80, 15.0f, 0.0f, SPARK_BARI, 1.0f, true);
@@ -3119,7 +3119,7 @@ void BossVa_ZapperPostLimbDraw(PlayState* play, s32 limbIndex, Gfx** dList, Vec3
             Matrix_MultVec3f(&sp70, &this->effectPos[3]);
             sp70.x = 20.0f;
             Matrix_MultVec3f(&sp70, &this->effectPos[9]);
-            func_80035844(&this->effectPos[9], &this->unk_1D8, &this->headRot, false);
+            Actor_GetPitchYawBetween(&this->effectPos[9], &this->unk_1D8, &this->headRot, false);  // was func_80035844 [LLM:HIGH]
             sp3E = this->headRot.x;
             sp3C = this->headRot.y;
             Matrix_Push();
diff --git a/soh/src/overlays/actors/ovl_En_Ba/z_en_ba.c b/soh/src/overlays/actors/ovl_En_Ba/z_en_ba.c
index 38ba2bc79..4fe509858 100644
--- a/soh/src/overlays/actors/ovl_En_Ba/z_en_ba.c
+++ b/soh/src/overlays/actors/ovl_En_Ba/z_en_ba.c
@@ -167,12 +167,12 @@ void EnBa_Idle(EnBa* this, PlayState* play) {
     this->unk_2FC.z += this->unk_308.y;
     Actor_SmoothStepPosToTarget(&this->unk_2FC, &this->unk_158[13], 1.0f, this->actor.speedXZ, 0.0f, 0.0f);  // was func_80033AEC [LLM:HIGH]
     for (i = 12; i >= 0; i--) {
-        func_80035844(&this->unk_158[i + 1], &this->unk_158[i], &sp5C, 0);
+        Actor_GetPitchYawBetween(&this->unk_158[i + 1], &this->unk_158[i], &sp5C, 0);  // was func_80035844 [LLM:HIGH]
         Matrix_Translate(this->unk_158[i + 1].x, this->unk_158[i + 1].y, this->unk_158[i + 1].z, MTXMODE_NEW);
         Matrix_RotateZYX(sp5C.x, sp5C.y, 0, MTXMODE_APPLY);
         Matrix_MultVec3f(&sSegmentPosStep, &this->unk_158[i]);
     }
-    func_80035844(&this->unk_158[0], &this->unk_2FC, &sp5C, 0);
+    Actor_GetPitchYawBetween(&this->unk_158[0], &this->unk_2FC, &sp5C, 0);  // was func_80035844 [LLM:HIGH]
     Matrix_Translate(this->actor.world.pos.x, this->actor.world.pos.y, this->actor.world.pos.z, MTXMODE_NEW);
     Math_SmoothStepToS(&this->actor.shape.rot.y, this->unk_2A8[0].y, 3, this->unk_31C, 182);
     Math_SmoothStepToS(&this->actor.shape.rot.x, this->unk_2A8[0].x, 3, this->unk_31C, 182);
@@ -257,7 +257,7 @@ void EnBa_SwingAtPlayer(EnBa* this, PlayState* play) {
                 phi_fp = Math_Vec3f_Yaw(&this->actor.world.pos, &this->unk_2FC);
             }
             Math_SmoothStepToS(&this->unk_31C, 1500, 1, 30, 0);
-            func_80035844(&this->actor.world.pos, &this->unk_158[0], &sp58, 0);
+            Actor_GetPitchYawBetween(&this->actor.world.pos, &this->unk_158[0], &sp58, 0);  // was func_80035844 [LLM:HIGH]
             Math_SmoothStepToS(&this->actor.shape.rot.y, sp58.y, 1, this->unk_31C, 0);
             Math_SmoothStepToS(&this->actor.shape.rot.x, (sp58.x + 0x8000), 1, this->unk_31C, 0);
             Matrix_Translate(this->actor.world.pos.x, this->actor.world.pos.y, this->actor.world.pos.z, MTXMODE_NEW);
@@ -353,12 +353,12 @@ void EnBa_RecoilFromDamage(EnBa* this, PlayState* play) {
     this->unk_2FC.z += this->unk_308.y;
     Actor_SmoothStepPosToTarget(&this->unk_2FC, &this->unk_158[13], 1.0f, this->actor.speedXZ, 0.0f, 0.0f);  // was func_80033AEC [LLM:HIGH]
     for (i = 12; i >= 0; i--) {
-        func_80035844(&this->unk_158[i + 1], &this->unk_158[i], &sp6C, 0);
+        Actor_GetPitchYawBetween(&this->unk_158[i + 1], &this->unk_158[i], &sp6C, 0);  // was func_80035844 [LLM:HIGH]
         Matrix_Translate(this->unk_158[i + 1].x, this->unk_158[i + 1].y, this->unk_158[i + 1].z, MTXMODE_NEW);
         Matrix_RotateZYX(sp6C.x, sp6C.y, 0, MTXMODE_APPLY);
         Matrix_MultVec3f(&sSegmentPosStep, &this->unk_158[i]);
     }
-    func_80035844(&this->actor.world.pos, &this->unk_158[0], &sp6C, 0);
+    Actor_GetPitchYawBetween(&this->actor.world.pos, &this->unk_158[0], &sp6C, 0);  // was func_80035844 [LLM:HIGH]
     Matrix_Translate(this->actor.world.pos.x, this->actor.world.pos.y, this->actor.world.pos.z, MTXMODE_NEW);
     Math_SmoothStepToS(&this->actor.shape.rot.y, sp6C.y, 3, this->unk_31C, 182);
     Math_SmoothStepToS(&this->actor.shape.rot.x, sp6C.x + 0x8000, 3, this->unk_31C, 182);
@@ -366,7 +366,7 @@ void EnBa_RecoilFromDamage(EnBa* this, PlayState* play) {
     Matrix_MultVec3f(&sSegmentPosStep, &this->unk_158[0]);
 
     for (i = 0; i < 13; i++) {
-        func_80035844(&this->unk_158[i], &this->unk_158[i + 1], &sp6C, 0);
+        Actor_GetPitchYawBetween(&this->unk_158[i], &this->unk_158[i + 1], &sp6C, 0);  // was func_80035844 [LLM:HIGH]
         Matrix_Translate(this->unk_158[i].x, this->unk_158[i].y, this->unk_158[i].z, MTXMODE_NEW);
         Math_SmoothStepToS(&this->unk_2A8[i].y, sp6C.y, 3, this->unk_31C, 182);
         Math_SmoothStepToS(&this->unk_2A8[i].x, sp6C.x + 0x8000, 3, this->unk_31C, 182);
diff --git a/soh/src/overlays/actors/ovl_En_Dha/z_en_dha.c b/soh/src/overlays/actors/ovl_En_Dha/z_en_dha.c
index 7b7d1af28..5109c7b18 100644
--- a/soh/src/overlays/actors/ovl_En_Dha/z_en_dha.c
+++ b/soh/src/overlays/actors/ovl_En_Dha/z_en_dha.c
@@ -239,7 +239,7 @@ void EnDha_Wait(EnDha* this, PlayState* play) {
                 }
             }
 
-            func_80035844(&this->handPos[1], &playerPos, &this->handAngle, 0);
+            Actor_GetPitchYawBetween(&this->handPos[1], &playerPos, &this->handAngle, 0);  // was func_80035844 [LLM:HIGH]
             this->handAngle.y -= this->actor.shape.rot.y + this->limbAngleY;
             this->handAngle.x -= this->actor.shape.rot.x + this->limbAngleX[0] + this->limbAngleX[1];
         } else {
@@ -261,12 +261,12 @@ void EnDha_Wait(EnDha* this, PlayState* play) {
         Math_SmoothStepToF(&this->handPos[0].y, playerPos.y, 1.0f, 16.0f, 0.0f);
         Math_SmoothStepToF(&this->handPos[0].z, playerPos.z, 1.0f, 16.0f, 0.0f);
 
-        func_80035844(&this->armPos, &this->handPos[0], &angle, 0);
+        Actor_GetPitchYawBetween(&this->armPos, &this->handPos[0], &angle, 0);  // was func_80035844 [LLM:HIGH]
         Matrix_Translate(this->handPos[0].x, this->handPos[0].y, this->handPos[0].z, MTXMODE_NEW);
         Matrix_RotateZYX(angle.x, angle.y, 0, MTXMODE_APPLY);
         Matrix_MultVec3f(&armPosMultiplier2, &this->armPos);
         Matrix_Translate(this->actor.world.pos.x, this->actor.world.pos.y, this->actor.world.pos.z, MTXMODE_NEW);
-        func_80035844(&this->actor.world.pos, &this->armPos, &angle, 0);
+        Actor_GetPitchYawBetween(&this->actor.world.pos, &this->armPos, &angle, 0);  // was func_80035844 [LLM:HIGH]
         Matrix_RotateZYX(angle.x, angle.y, 0, MTXMODE_APPLY);
         Matrix_MultVec3f(&armPosMultiplier1, &this->armPos);
         this->limbAngleX[0] = Math_Vec3f_Pitch(&this->actor.world.pos, &this->armPos);
-- 
2.55.0

