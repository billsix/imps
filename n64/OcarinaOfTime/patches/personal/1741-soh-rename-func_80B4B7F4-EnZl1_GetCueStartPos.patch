From 3cbde9b5a1d8f64f1c5f1f80c3a49ec375911b91 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:08 +0000
Subject: [PATCH 1741/3799] soh: rename func_80B4B7F4 -> EnZl1_GetCueStartPos

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: copies an actor cue's start position into a Vec3f.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index ebdca92ae..5f5147358 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -344,7 +344,8 @@ void EnZl1_CourtyardCutscene(EnZl1* this, PlayState* play) {
     Actor_TrackPlayer(play, &this->actor, &this->unk_200, &this->unk_206, this->actor.focus.pos);
 }
 
-void func_80B4B7F4(CsCmdActorCue* npcAction, Vec3f* pos) {
+// LLM generated name (HIGH), was func_80B4B7F4: copies an actor cue's start position into a Vec3f.
+void EnZl1_GetCueStartPos(CsCmdActorCue* npcAction, Vec3f* pos) {
     pos->x = npcAction->startPos.x;
     pos->y = npcAction->startPos.y;
     pos->z = npcAction->startPos.z;
@@ -398,7 +399,7 @@ void func_80B4B8B4(EnZl1* this, PlayState* play) {
 
     npcAction = play->csCtx.npcActions[0];
     if (npcAction != NULL) {
-        func_80B4B7F4(npcAction, &sp74);
+        EnZl1_GetCueStartPos(npcAction, &sp74);  // was func_80B4B7F4 [LLM:HIGH]
         func_80B4B834(npcAction, &sp68);
         if (this->unk_1E6 == 0) {
             sp48 = sp74;
@@ -474,7 +475,7 @@ void func_80B4BC78(EnZl1* this, PlayState* play) {
     func_80B4B874(this, play);
     npcAction = play->csCtx.npcActions[0];
     if (npcAction != NULL) {
-        func_80B4B7F4(npcAction, &sp70);
+        EnZl1_GetCueStartPos(npcAction, &sp70);  // was func_80B4B7F4 [LLM:HIGH]
         func_80B4B834(npcAction, &sp64);
         if (this->unk_1E6 == 0) {
             this->actor.world.pos = this->actor.home.pos = sp70;
-- 
2.55.0

