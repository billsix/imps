From 2b4346fdcb3b15e1cc2207add0414d30fd606ff8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:18:23 +0000
Subject: [PATCH 2213/3799] soh: rename func_80A535BC ->
 EnHeishi2_StartSpearSlamFence

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: plays gEnHeishiSlamSpearAnim once for the type 2 guard's fence
Defined in soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c b/soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c
index 9ecfb6a80..d9d138c26 100644
--- a/soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c
+++ b/soh/src/overlays/actors/ovl_En_Heishi2/z_en_heishi2.c
@@ -33,7 +33,8 @@ void EnHeishi2_HandleBribeChoice(EnHeishi2* this, PlayState* play);  // was func
 void func_80A54954(EnHeishi2* this, PlayState* play);
 // LLM generated name (HIGH), was func_80A53538: waits out the paid-up textbox, halts the actors and starts the spear slam
 void EnHeishi2_WaitBribeAccepted(EnHeishi2* this, PlayState* play);  // was func_80A53538 [LLM:HIGH]
-void func_80A535BC(EnHeishi2* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A535BC: plays gEnHeishiSlamSpearAnim once for the type 2 guard's fence
+void EnHeishi2_StartSpearSlamFence(EnHeishi2* this, PlayState* play);  // was func_80A535BC [LLM:HIGH]
 void func_80A5399C(EnHeishi2* this, PlayState* play);
 void func_80A53638(EnHeishi2* this, PlayState* play);
 void func_80A5372C(EnHeishi2* this, PlayState* play);
@@ -274,11 +275,11 @@ void EnHeishi2_WaitBribeAccepted(EnHeishi2* this, PlayState* play) {
     if (this->unk_300 == Message_GetState(&play->msgCtx) && Message_ShouldAdvance(play)) {
         Player_SetCsActionWithHaltedActors(play, NULL, 8);
         play->msgCtx.msgMode = MSGMODE_PAUSED;
-        this->actionFunc = func_80A535BC;
+        this->actionFunc = EnHeishi2_StartSpearSlamFence;  // was func_80A535BC [LLM:HIGH]
     }
 }
 
-void func_80A535BC(EnHeishi2* this, PlayState* play) {
+void EnHeishi2_StartSpearSlamFence(EnHeishi2* this, PlayState* play) {
     f32 frameCount = Animation_GetLastFrame(&gEnHeishiSlamSpearAnim);
 
     this->unk_2EC = frameCount;
-- 
2.55.0

