From 11808a6000568635703025b52408ab5c1e13de99 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:29:58 +0000
Subject: [PATCH 2482/3799] soh: rename func_80A04DE4 -> EnElf_MoveToTalkPos

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: flies Navi to the talk position in front of the player, sparkling above speed 5; the file's own comment calls this moving to talk to the player
Defined in soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c b/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
index 3c53afc07..2073d59a0 100644
--- a/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
+++ b/soh/src/overlays/actors/ovl_En_Elf/z_en_elf.c
@@ -1242,7 +1242,8 @@ void EnElf_UpdateShadow(EnElf* this, PlayState* play) {
 }
 
 // move to talk to player
-void func_80A04DE4(EnElf* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A04DE4: flies Navi to the talk position in front of the player, sparkling above speed 5; the file's own comment calls this moving to talk to the player
+void EnElf_MoveToTalkPos(EnElf* this, PlayState* play) {
     Vec3f headCopy;
     Player* player = GET_PLAYER(play);
     Vec3f naviRefPos;
@@ -1288,7 +1289,7 @@ void func_80A04F94(EnElf* this, PlayState* play) {
 void func_80A05040(Actor* thisx, PlayState* play) {
     EnElf* this = (EnElf*)thisx;
 
-    func_80A04DE4(this, play);
+    EnElf_MoveToTalkPos(this, play);  // was func_80A04DE4 [LLM:HIGH]
 
     if ((Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE) && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
@@ -1311,7 +1312,7 @@ void func_80A05040(Actor* thisx, PlayState* play) {
 void func_80A05114(Actor* thisx, PlayState* play) {
     EnElf* this = (EnElf*)thisx;
 
-    func_80A04DE4(this, play);
+    EnElf_MoveToTalkPos(this, play);  // was func_80A04DE4 [LLM:HIGH]
 
     if ((Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT) && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0xE3);
@@ -1324,7 +1325,7 @@ void func_80A05114(Actor* thisx, PlayState* play) {
 void func_80A05188(Actor* thisx, PlayState* play) {
     EnElf* this = (EnElf*)thisx;
 
-    func_80A04DE4(this, play);
+    EnElf_MoveToTalkPos(this, play);  // was func_80A04DE4 [LLM:HIGH]
 
     if ((Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT) && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, ElfMessage_GetSariaText(play));
@@ -1339,7 +1340,7 @@ void func_80A05208(Actor* thisx, PlayState* play) {
     s32 naviCUpText;
     EnElf* this = (EnElf*)thisx;
 
-    func_80A04DE4(this, play);
+    EnElf_MoveToTalkPos(this, play);  // was func_80A04DE4 [LLM:HIGH]
 
     if ((Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE) && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
@@ -1370,7 +1371,7 @@ void func_80A05208(Actor* thisx, PlayState* play) {
 void func_80A052F4(Actor* thisx, PlayState* play) {
     EnElf* this = (EnElf*)thisx;
 
-    func_80A04DE4(this, play);
+    EnElf_MoveToTalkPos(this, play);  // was func_80A04DE4 [LLM:HIGH]
 
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE) {
         if (Message_ShouldAdvance(play)) {
-- 
2.55.0

