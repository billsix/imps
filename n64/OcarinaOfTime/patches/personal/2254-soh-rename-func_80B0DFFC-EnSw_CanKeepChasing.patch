From c9d040f7aa72449b636352bebbb3d3582a734e0a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:18 +0000
Subject: [PATCH 2254/3799] soh: rename func_80B0DFFC -> EnSw_CanKeepChasing

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: round-robins four line tests per frame to check the chase is still viable, and re-seats the skulltula on the wall it hits
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index 9d2520cec..1b4e81856 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -8,7 +8,8 @@ void EnSw_Init(Actor* thisx, PlayState* play);
 void EnSw_Destroy(Actor* thisx, PlayState* play);
 void EnSw_Update(Actor* thisx, PlayState* play);
 void EnSw_Draw(Actor* thisx, PlayState* play);
-s32 func_80B0DFFC(EnSw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B0DFFC: round-robins four line tests per frame to check the chase is still viable, and re-seats the skulltula on the wall it hits
+s32 EnSw_CanKeepChasing(EnSw* this, PlayState* play);  // was func_80B0DFFC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0D364: arms the pre-drop delay and installs the drop-in action
 void EnSw_SetupDropIn(EnSw* this, PlayState* play);  // was func_80B0D364 [LLM:HIGH]
 void func_80B0E5E0(EnSw* this, PlayState* play);
@@ -260,7 +261,7 @@ void EnSw_Init(Actor* thisx, PlayState* play) {
         this->unk_484.y = this->actor.world.pos.y;
         this->unk_484.x = this->actor.world.pos.x + (Math_SinS(this->actor.world.rot.y) * -60.0f);
         this->unk_484.z = this->actor.world.pos.z + (Math_CosS(this->actor.world.rot.y) * -60.0f);
-        func_80B0DFFC(this, play);
+        EnSw_CanKeepChasing(this, play);  // was func_80B0DFFC [LLM:HIGH]
         this->actor.home.pos = this->actor.world.pos;
     } else {
         this->unk_370.x = Math_SinS(thisx->shape.rot.y + 0x4000);
@@ -733,7 +734,7 @@ s32 EnSw_ShouldChasePlayer(EnSw* this, PlayState* play, s32 arg2) {
     }
 }
 
-s32 func_80B0DFFC(EnSw* this, PlayState* play) {
+s32 EnSw_CanKeepChasing(EnSw* this, PlayState* play) {
     s32 pad;
     CollisionPoly* sp60;
     s32 sp5C;
@@ -869,7 +870,7 @@ void func_80B0E728(EnSw* this, PlayState* play) {
             this->actionFunc = func_80B0E5E0;
         }
     } else {
-        if (!func_80B0DFFC(this, play)) {
+        if (!EnSw_CanKeepChasing(this, play)) {  // was func_80B0DFFC [LLM:HIGH]
             this->unk_442 = Rand_S16Offset(20, 10);
             this->unk_444 = EnSw_GetRollToward(this, &this->actor.home.pos);  // was func_80B0DE34 [LLM:HIGH]
             this->unk_448 = this->actor.home.pos;
-- 
2.55.0

