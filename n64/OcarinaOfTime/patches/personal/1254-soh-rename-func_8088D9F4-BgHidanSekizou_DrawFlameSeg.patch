From 79f5a7db56bd875182ece5c570d38adfd2fefcc9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:04:38 +0000
Subject: [PATCH 1254/3799] soh: rename func_8088D9F4 ->
 BgHidanSekizou_DrawFlameSegment

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: emits one fireball quad of a jet: binds the sFireballsTexs frame for its index and builds the matrix that places and scales it along the jet.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c       | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
index 2e69b3b1d..458d1b391 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Sekizou/z_bg_hidan_sekizou.c
@@ -299,7 +299,8 @@ void BgHidanSekizou_Update(Actor* thisx, PlayState* play2) {
     }
 }
 
-Gfx* func_8088D9F4(PlayState* play, BgHidanSekizou* this, s16 arg2, MtxF* arg3, f32 arg4, f32 arg5, s16 arg6,
+// LLM generated name (HIGH), was func_8088D9F4: emits one fireball quad of a jet: binds the sFireballsTexs frame for its index and builds the matrix that places and scales it along the jet.
+Gfx* BgHidanSekizou_DrawFlameSegment(PlayState* play, BgHidanSekizou* this, s16 arg2, MtxF* arg3, f32 arg4, f32 arg5, s16 arg6,
                    Gfx* arg7) {
     f32 temp_f0;
     f32 temp_f2;
@@ -355,11 +356,11 @@ Gfx* func_8088DC50(PlayState* play, BgHidanSekizou* this, s16 arg2, s16 arg3, Gf
 
     if (ABS(temp_v1) < 0x4000) {
         for (i = phi_s2 - 1; i >= phi_s1; i--) {
-            arg4 = func_8088D9F4(play, this, i, &sp68, temp_f20, temp_f22, arg3, arg4);
+            arg4 = BgHidanSekizou_DrawFlameSegment(play, this, i, &sp68, temp_f20, temp_f22, arg3, arg4);  // was func_8088D9F4 [LLM:HIGH]
         }
     } else {
         for (i = phi_s1; i < phi_s2; i++) {
-            arg4 = func_8088D9F4(play, this, i, &sp68, temp_f20, temp_f22, arg3, arg4);
+            arg4 = BgHidanSekizou_DrawFlameSegment(play, this, i, &sp68, temp_f20, temp_f22, arg3, arg4);  // was func_8088D9F4 [LLM:HIGH]
         }
     }
     return arg4;
-- 
2.55.0

