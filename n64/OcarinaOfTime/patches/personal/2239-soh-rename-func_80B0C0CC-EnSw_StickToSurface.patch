From cf5f044c02e755d070e3305146a4a2a144cc5f9f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:17 +0000
Subject: [PATCH 2239/3799] soh: rename func_80B0C0CC -> EnSw_StickToSurface

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: probes ahead of and behind the skulltula for a surface to cling to and, when told to, moves it onto the hit and re-bases it
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index ce878ce95..ae0c6b343 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -16,7 +16,8 @@ void func_80B0E90C(EnSw* this, PlayState* play);
 void func_80B0E9BC(EnSw* this, PlayState* play);
 void func_80B0E728(EnSw* this, PlayState* play);
 void func_80B0DC7C(EnSw* this, PlayState* play);
-s32 func_80B0C0CC(EnSw* this, PlayState* play, s32);
+// LLM generated name (HIGH), was func_80B0C0CC: probes ahead of and behind the skulltula for a surface to cling to and, when told to, moves it onto the hit and re-bases it
+s32 EnSw_StickToSurface(EnSw* this, PlayState* play, s32);  // was func_80B0C0CC [LLM:HIGH]
 void func_80B0D3AC(EnSw* this, PlayState* play);
 void func_80B0DB00(EnSw* this, PlayState* play);
 void func_80B0D878(EnSw* this, PlayState* play);
@@ -137,7 +138,7 @@ CollisionPoly* EnSw_LineTestWall(PlayState* play, Vec3f* arg1, Vec3f* arg2, Vec3
     return sp3C;
 }
 
-s32 func_80B0C0CC(EnSw* this, PlayState* play, s32 arg2) {
+s32 EnSw_StickToSurface(EnSw* this, PlayState* play, s32 arg2) {
     CollisionPoly* temp_v0_2;
     CollisionPoly* temp_s1;
     Vec3f sp9C;
@@ -265,7 +266,7 @@ void EnSw_Init(Actor* thisx, PlayState* play) {
         this->unk_37C.x = Math_SinS(thisx->shape.rot.y);
         this->unk_37C.y = 0.0f;
         this->unk_37C.z = Math_CosS(thisx->shape.rot.y);
-        func_80B0C0CC(this, play, 1);
+        EnSw_StickToSurface(this, play, 1);  // was func_80B0C0CC [LLM:HIGH]
     }
 
     if (((thisx->params & 0xE000) >> 0xD) >= 3) {
@@ -526,7 +527,7 @@ void func_80B0D3AC(EnSw* this, PlayState* play) {
         this->unk_360 = 0;
     }
 
-    if (func_80B0C0CC(this, play, 1) == 1) {
+    if (EnSw_StickToSurface(this, play, 1) == 1) {  // was func_80B0C0CC [LLM:HIGH]
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_DODO_M_GND);
         func_80B0D14C(this, play, 8);
         this->actor.scale.x = 0.02f;
-- 
2.55.0

