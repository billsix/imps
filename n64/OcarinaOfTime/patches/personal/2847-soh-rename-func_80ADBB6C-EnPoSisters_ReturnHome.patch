From f08c0f68e8a711cf2f4d2c9bde4ca55ea496a666 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:53:57 +0000
Subject: [PATCH 2847/3799] soh: rename func_80ADBB6C -> EnPoSisters_ReturnHome

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: turns onto the home position and settles once within 10 units
Defined in soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c    | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
index 1e7154517..5fd8943d1 100644
--- a/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
+++ b/soh/src/overlays/actors/ovl_En_Po_Sisters/z_en_po_sisters.c
@@ -58,7 +58,8 @@ void EnPoSisters_MegAppear(EnPoSisters* this, PlayState* play);  // was func_80A
 void EnPoSisters_CopyDance(EnPoSisters* this, PlayState* play);  // was func_80ADB51C [LLM:GUESS]
 // LLM generated name (HIGH), was func_80ADB770: Meg's fight loop: counts her window down and picks the next attack or retreat
 void EnPoSisters_MegFight(EnPoSisters* this, PlayState* play);  // was func_80ADB770 [LLM:HIGH]
-void func_80ADBB6C(EnPoSisters* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ADBB6C: turns onto the home position and settles once within 10 units
+void EnPoSisters_ReturnHome(EnPoSisters* this, PlayState* play);  // was func_80ADBB6C [LLM:HIGH]
 void func_80ADBBF4(EnPoSisters* this, PlayState* play);
 void func_80ADBC88(EnPoSisters* this, PlayState* play);
 void func_80ADBD38(EnPoSisters* this, PlayState* play);
@@ -546,7 +547,7 @@ void EnPoSisters_SetupReturnHome(EnPoSisters* this) {
     }
     Animation_PlayLoop(&this->skelAnime, &gPoeSistersFloatAnim);
     this->unk_199 |= 0xA;
-    this->actionFunc = func_80ADBB6C;
+    this->actionFunc = EnPoSisters_ReturnHome;  // was func_80ADBB6C [LLM:HIGH]
     this->actor.speedXZ = 5.0f;
 }
 
@@ -1067,7 +1068,7 @@ void EnPoSisters_Reappear(EnPoSisters* this, PlayState* play) {
     Actor_SetFocus(&this->actor, 40.0f);
 }
 
-void func_80ADBB6C(EnPoSisters* this, PlayState* play) {
+void EnPoSisters_ReturnHome(EnPoSisters* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     if (Actor_WorldDistXZToPoint(&this->actor, &this->actor.home.pos) < 10.0f) {
         EnPoSisters_SetupWaitAtHome(this);  // was func_80ADA028 [LLM:HIGH]
-- 
2.55.0

