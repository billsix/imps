From 10249f2239052ed8fc83948823d72b55aa937aed Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:39:20 +0000
Subject: [PATCH 1720/3799] soh: rename func_808B3960 ->
 BgSpot15Rrbox_InitDynaPoly

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: registers the crate as a dyna-poly bg actor with the given collision header, warning when the bg actor table is full.
Defined in soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c  | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
index eed04e494..5af5fd7fe 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
@@ -58,7 +58,8 @@ static Vec3f sBoxGroundCheckPoints[] = {
     { 29.99f, 0.01f, 29.99f },  { 0.0f, 0.01f, 0.0f },
 };
 
-void func_808B3960(BgSpot15Rrbox* this, PlayState* play, CollisionHeader* collision, s32 flags) {
+// LLM generated name (HIGH), was func_808B3960: registers the crate as a dyna-poly bg actor with the given collision header, warning when the bg actor table is full.
+void BgSpot15Rrbox_InitDynaPoly(BgSpot15Rrbox* this, PlayState* play, CollisionHeader* collision, s32 flags) {
     s32 pad;
     CollisionHeader* colHeader = NULL;
     u32 pad2;
@@ -124,7 +125,7 @@ s32 func_808B3AAC(BgSpot15Rrbox* this, PlayState* play) {
 void BgSpot15Rrbox_Init(Actor* thisx, PlayState* play) {
     BgSpot15Rrbox* this = (BgSpot15Rrbox*)thisx;
 
-    func_808B3960(this, play, &gLonLonMilkCrateCol, DPM_UNK);
+    BgSpot15Rrbox_InitDynaPoly(this, play, &gLonLonMilkCrateCol, DPM_UNK);  // was func_808B3960 [LLM:HIGH]
     Actor_ProcessInitChain(&this->dyna.actor, sInitChain);
     func_808B3A34(this);
     if (Flags_GetSwitch(play, (this->dyna.actor.params & 0x3F))) {
-- 
2.55.0

