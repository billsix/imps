From f05061917430c3a84706473950220173f4185c78 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:04 +0000
Subject: [PATCH 077/488] soh: name the 7 address-named symbols defined in
 z_demo.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 7 are defined in soh/src/code/z_demo.c; updating their references touches 18 files in total.

  * func_8006450C -> Cutscene_InitContext
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.88

  * func_80064534 -> Cutscene_StopManual
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.82

  * func_80064558 -> Cutscene_UpdateManual
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.81

  * func_80064720 -> CutsceneHandler_DoNothing
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 1.00

  * func_8006472C -> Cutscene_StepTimer
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.88

  * func_80064760 -> CutsceneHandler_StartManual
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.83

  * func_80068D84 -> CutsceneHandler_StopManual
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.86

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/include/functions.h                       |  6 +--
 soh/src/code/z_camera.c                       |  2 +-
 soh/src/code/z_demo.c                         | 39 +++++++++++--------
 soh/src/code/z_play.c                         |  4 +-
 .../actors/ovl_Boss_Dodongo/z_boss_dodongo.c  |  4 +-
 .../overlays/actors/ovl_Boss_Fd/z_boss_fd.c   |  2 +-
 .../overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c |  2 +-
 .../actors/ovl_Boss_Ganon/z_boss_ganon.c      |  4 +-
 .../actors/ovl_Boss_Ganon2/z_boss_ganon2.c    | 10 ++---
 .../ovl_Boss_Ganondrof/z_boss_ganondrof.c     |  2 +-
 .../actors/ovl_Boss_Goma/z_boss_goma.c        |  6 +--
 .../overlays/actors/ovl_Boss_Mo/z_boss_mo.c   |  6 +--
 .../overlays/actors/ovl_Boss_Sst/z_boss_sst.c |  4 +-
 .../overlays/actors/ovl_Boss_Tw/z_boss_tw.c   |  6 +--
 .../overlays/actors/ovl_Boss_Va/z_boss_va.c   |  4 +-
 .../actors/ovl_En_Clear_Tag/z_en_clear_tag.c  |  2 +-
 soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c |  2 +-
 .../overlays/actors/ovl_Fishing/z_fishing.c   |  6 +--
 18 files changed, 59 insertions(+), 52 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 5e641cdb0..9c3c70577 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -862,10 +862,10 @@ DebugDispObject* DebugDisplay_AddObject(f32 posX, f32 posY, f32 posZ, s16 rotX,
                                         f32 scaleY, f32 scaleZ, u8 red, u8 green, u8 blue, u8 alpha, s16 type,
                                         GraphicsContext* gfxCtx);
 void DebugDisplay_DrawObjects(PlayState* play);
-void func_8006450C(PlayState* play, CutsceneContext* csCtx);
+void Cutscene_InitContext(PlayState* play, CutsceneContext* csCtx);  // was func_8006450C [oot]
 void func_80064520(PlayState* play, CutsceneContext* csCtx);
-void func_80064534(PlayState* play, CutsceneContext* csCtx);
-void func_80064558(PlayState* play, CutsceneContext* csCtx);
+void Cutscene_StopManual(PlayState* play, CutsceneContext* csCtx);  // was func_80064534 [oot]
+void Cutscene_UpdateManual(PlayState* play, CutsceneContext* csCtx);  // was func_80064558 [oot]
 void func_800645A0(PlayState* play, CutsceneContext* csCtx);
 void Cutscene_HandleEntranceTriggers(PlayState* play);
 void Cutscene_HandleConditionalTriggers(PlayState* play);
diff --git a/soh/src/code/z_camera.c b/soh/src/code/z_camera.c
index 940292693..bdfa2024a 100644
--- a/soh/src/code/z_camera.c
+++ b/soh/src/code/z_camera.c
@@ -7619,7 +7619,7 @@ Vec3s Camera_Update(Camera* camera) {
         if (gDbgCamEnabled) {
             DbgCamera_Enable(&D_8015BD80, camera);
         } else if (camera->play->csCtx.state != CS_STATE_IDLE) {
-            func_80064534(camera->play, &camera->play->csCtx);
+            Cutscene_StopManual(camera->play, &camera->play->csCtx);
         }
     }
 
diff --git a/soh/src/code/z_demo.c b/soh/src/code/z_demo.c
index 0f01ad583..9f0ab1fad 100644
--- a/soh/src/code/z_demo.c
+++ b/soh/src/code/z_demo.c
@@ -41,19 +41,22 @@ u16 D_8011E1C4 = 0;
 
 typedef void (*CutsceneStateHandler)(PlayState*, CutsceneContext*);
 
-void func_80064720(PlayState* play, CutsceneContext* csCtx);
-void func_80064760(PlayState* play, CutsceneContext* csCtx);
+// Name from zeldaret/oot (was func_80064720): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void CutsceneHandler_DoNothing(PlayState* play, CutsceneContext* csCtx);
+// Name from zeldaret/oot (was func_80064760): adopted upstream's name for this address; body verified to match upstream at 0.83. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void CutsceneHandler_StartManual(PlayState* play, CutsceneContext* csCtx);
 void func_800647C0(PlayState* play, CutsceneContext* csCtx);
 void func_80068C3C(PlayState* play, CutsceneContext* csCtx);
-void func_80068D84(PlayState* play, CutsceneContext* csCtx);
+// Name from zeldaret/oot (was func_80068D84): adopted upstream's name for this address; body verified to match upstream at 0.86. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void CutsceneHandler_StopManual(PlayState* play, CutsceneContext* csCtx);
 void func_80068DC0(PlayState* play, CutsceneContext* csCtx);
 
 CutsceneStateHandler sCsStateHandlers1[] = {
-    func_80064720, func_80064760, func_80064720, func_80068D84, func_80064720,
+    CutsceneHandler_DoNothing, CutsceneHandler_StartManual, CutsceneHandler_DoNothing, CutsceneHandler_StopManual, CutsceneHandler_DoNothing,
 };
 
 CutsceneStateHandler sCsStateHandlers2[] = {
-    func_80064720, func_800647C0, func_80068C3C, func_80068DC0, func_80068C3C,
+    CutsceneHandler_DoNothing, func_800647C0, func_80068C3C, func_80068DC0, func_80068C3C,
 };
 
 u8 sTitleCsState = 0;
@@ -131,7 +134,8 @@ void Cutscene_DrawDebugInfo(PlayState* play, Gfx** dlist, CutsceneContext* csCtx
     GfxPrint_Destroy(&printer);
 }
 
-void func_8006450C(PlayState* play, CutsceneContext* csCtx) {
+// Name from zeldaret/oot (was func_8006450C): adopted upstream's name for this address; body verified to match upstream at 0.88. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void Cutscene_InitContext(PlayState* play, CutsceneContext* csCtx) {
     csCtx->state = CS_STATE_IDLE;
     csCtx->unk_0C = 0.0f;
 }
@@ -141,13 +145,15 @@ void func_80064520(PlayState* play, CutsceneContext* csCtx) {
     csCtx->linkAction = NULL;
 }
 
-void func_80064534(PlayState* play, CutsceneContext* csCtx) {
+// Name from zeldaret/oot (was func_80064534): adopted upstream's name for this address; body verified to match upstream at 0.82. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void Cutscene_StopManual(PlayState* play, CutsceneContext* csCtx) {
     if (csCtx->state != CS_STATE_UNSKIPPABLE_EXEC) {
         csCtx->state = CS_STATE_UNSKIPPABLE_INIT;
     }
 }
 
-void func_80064558(PlayState* play, CutsceneContext* csCtx) {
+// Name from zeldaret/oot (was func_80064558): adopted upstream's name for this address; body verified to match upstream at 0.81. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+void Cutscene_UpdateManual(PlayState* play, CutsceneContext* csCtx) {
     if (gSaveContext.cutsceneIndex < 0xFFF0) {
         sCsStateHandlers1[csCtx->state](play, csCtx);
     }
@@ -186,18 +192,19 @@ void func_800645A0(PlayState* play, CutsceneContext* csCtx) {
     }
 }
 
-void func_80064720(PlayState* play, CutsceneContext* csCtx) {
+void CutsceneHandler_DoNothing(PlayState* play, CutsceneContext* csCtx) {
 }
 
-u32 func_8006472C(PlayState* play, CutsceneContext* csCtx, f32 target) {
+// Name from zeldaret/oot (was func_8006472C): adopted upstream's name for this address; body verified to match upstream at 0.88. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_demo.c]
+u32 Cutscene_StepTimer(PlayState* play, CutsceneContext* csCtx, f32 target) {
     return Math_StepToF(&csCtx->unk_0C, target, 0.1f);
 }
 
-void func_80064760(PlayState* play, CutsceneContext* csCtx) {
+void CutsceneHandler_StartManual(PlayState* play, CutsceneContext* csCtx) {
     Interface_ChangeHudVisibilityMode(1);
     Letterbox_SetSizeTarget(0x20);
 
-    if (func_8006472C(play, csCtx, 1.0f)) {
+    if (Cutscene_StepTimer(play, csCtx, 1.0f)) {
         Audio_SetCutsceneFlag(1);
         csCtx->state++;
     }
@@ -208,7 +215,7 @@ void func_800647C0(PlayState* play, CutsceneContext* csCtx) {
     Interface_ChangeHudVisibilityMode(1);
     Letterbox_SetSizeTarget(0x20);
 
-    if (func_8006472C(play, csCtx, 1.0f)) {
+    if (Cutscene_StepTimer(play, csCtx, 1.0f)) {
         Audio_SetCutsceneFlag(1);
         csCtx->state++;
     }
@@ -2086,8 +2093,8 @@ void func_80068C3C(PlayState* play, CutsceneContext* csCtx) {
     }
 }
 
-void func_80068D84(PlayState* play, CutsceneContext* csCtx) {
-    if (func_8006472C(play, csCtx, 0.0f)) {
+void CutsceneHandler_StopManual(PlayState* play, CutsceneContext* csCtx) {
+    if (Cutscene_StepTimer(play, csCtx, 0.0f)) {
         Audio_SetCutsceneFlag(0);
         csCtx->state = CS_STATE_IDLE;
     }
@@ -2096,7 +2103,7 @@ void func_80068D84(PlayState* play, CutsceneContext* csCtx) {
 void func_80068DC0(PlayState* play, CutsceneContext* csCtx) {
     s16 i;
 
-    if (func_8006472C(play, csCtx, 0.0f)) {
+    if (Cutscene_StepTimer(play, csCtx, 0.0f)) {
         csCtx->linkAction = NULL;
 
         for (i = 0; i < 10; i++) {
diff --git a/soh/src/code/z_play.c b/soh/src/code/z_play.c
index f967b059b..a64f683a4 100644
--- a/soh/src/code/z_play.c
+++ b/soh/src/code/z_play.c
@@ -418,7 +418,7 @@ void Play_Init(GameState* thisx) {
     EffectSs_InitInfo(play, 0x55);
     CollisionCheck_InitContext(play, &play->colChkCtx);
     AnimationContext_Reset(&play->animationCtx);
-    func_8006450C(play, &play->csCtx);
+    Cutscene_InitContext(play, &play->csCtx);
 
     if (gSaveContext.nextCutsceneIndex != 0xFFEF) {
         gSaveContext.cutsceneIndex = gSaveContext.nextCutsceneIndex;
@@ -1170,7 +1170,7 @@ void Play_Update(PlayState* play) {
                     }
 
                     PLAY_LOG(3643);
-                    func_80064558(play, &play->csCtx);
+                    Cutscene_UpdateManual(play, &play->csCtx);
 
                     PLAY_LOG(3648);
                     func_800645A0(play, &play->csCtx);
diff --git a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
index a731d95ba..6157732d4 100644
--- a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
@@ -597,7 +597,7 @@ void BossDodongo_IntroCutscene(BossDodongo* this, PlayState* play) {
                 camera->at = this->cameraAt;
                 func_800C08AC(play, this->cutsceneCamera, 0);
                 this->cutsceneCamera = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 BossDodongo_SetupWalk(this);
                 this->unk_1DA = 50;
@@ -1856,7 +1856,7 @@ void BossDodongo_DeathCutscene(BossDodongo* this, PlayState* play) {
                 this->cutsceneCamera = CAM_ID_MAIN;
                 this->csState = 100;
                 Play_ChangeCameraStatus(play, CAM_ID_MAIN, CAM_STAT_ACTIVE);
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 if (GameInteractor_Should(VB_SPAWN_BLUE_WARP, true, this)) {
                     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_DOOR_WARP1, -890.0f, -1523.76f,
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
index 6a813ec8e..e456e0f58 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
+++ b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
@@ -546,7 +546,7 @@ void BossFd_Fly(BossFd* this, PlayState* play) {
                     mainCam->at = this->camData.at;
                     func_800C08AC(play, this->introCamera, 0);
                     this->introState = this->introFlyState = this->introCamera = BFD_CS_NONE;
-                    func_80064534(play, &play->csCtx);
+                    Cutscene_StopManual(play, &play->csCtx);
                     Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                     this->actionFunc = BossFd_Wait;
                     this->handoffSignal = FD2_SIGNAL_GROUND;
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
index 229725914..48d73e1bd 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
@@ -784,7 +784,7 @@ void BossFd2_Death(BossFd2* this, PlayState* play) {
                 mainCam->at = this->camData.at;
                 func_800C08AC(play, this->deathCamera, 0);
                 this->deathCamera = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 if (GameInteractor_Should(VB_SPAWN_BLUE_WARP, true, this)) {
                     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_DOOR_WARP1, 0.0f, 100.0f, 0.0f, 0, 0,
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
index 625349ac4..99e6cf5aa 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
@@ -1164,7 +1164,7 @@ void BossGanon_IntroCutscene(BossGanon* this, PlayState* play) {
                 mainCam->at = this->csCamAt;
                 func_800C08AC(play, this->csCamIndex, 0);
                 this->csState = this->csCamIndex = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 BossGanon_SetupWait(this, play);
             }
@@ -1864,7 +1864,7 @@ void BossGanon_DeathAndTowerCutscene(BossGanon* this, PlayState* play) {
                 func_800C08AC(play, this->csCamIndex, 0);
                 this->csState = 109;
                 this->csCamIndex = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 Flags_SetSwitch(play, 0x37);
             }
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
index 2e3913dd4..092901ff8 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
@@ -376,7 +376,7 @@ void func_808FD5F4(BossGanon2* this, PlayState* play) {
                 camera->at = this->subCamAt;
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->csState = 5;
                 this->csTimer = 0;
@@ -932,7 +932,7 @@ void func_808FD5F4(BossGanon2* this, PlayState* play) {
                 camera->at = this->subCamAt;
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->csState = 0;
                 this->unk_337 = 1;
@@ -1399,7 +1399,7 @@ void func_80900890(BossGanon2* this, PlayState* play) {
                 temp_v0->at = this->subCamAt;
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->csState = 3;
             }
@@ -1431,7 +1431,7 @@ void func_80900890(BossGanon2* this, PlayState* play) {
                 this->csState = 3;
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
             }
             break;
@@ -1686,7 +1686,7 @@ void func_8090120C(BossGanon2* this, PlayState* play) {
                 temp_v0_2->at = this->subCamAt;
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->csState = 6;
             }
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
index c405a5f5e..7e68d3470 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
@@ -1078,7 +1078,7 @@ void BossGanondrof_Death(BossGanondrof* this, PlayState* play) {
                 camera->at = this->cameraAt;
                 func_800C08AC(play, this->deathCamera, 0);
                 this->deathCamera = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 if (GameInteractor_Should(VB_SPAWN_HEART_CONTAINER, true)) {
                     Actor_Spawn(&play->actorCtx, play, ACTOR_ITEM_B_HEART, GND_BOSSROOM_CENTER_X, GND_BOSSROOM_CENTER_Y,
diff --git a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
index e74a70ff4..c9eb09231 100644
--- a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
+++ b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
@@ -772,7 +772,7 @@ void BossGoma_Encounter(BossGoma* this, PlayState* play) {
                 cam->at = this->subCameraAt;
                 func_800C08AC(play, this->subCameraId, 0);
                 this->subCameraId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->actionState = 3;
             }
@@ -968,7 +968,7 @@ void BossGoma_Encounter(BossGoma* this, PlayState* play) {
                 BossGoma_SetupFloorMain(this);
                 this->disableGameplayLogic = false;
                 this->patienceTimer = 200;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
             }
             break;
@@ -1189,7 +1189,7 @@ void BossGoma_Defeated(BossGoma* this, PlayState* play) {
                     camera->at = this->subCameraAt;
                     func_800C08AC(play, this->subCameraId, 0);
                     this->subCameraId = 0;
-                    func_80064534(play, &play->csCtx);
+                    Cutscene_StopManual(play, &play->csCtx);
                     Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                     Actor_Kill(&this->actor);
                 }
diff --git a/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c b/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
index 10b111d86..0eb28cdcf 100644
--- a/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
@@ -934,7 +934,7 @@ void BossMo_Tentacle(BossMo* this, PlayState* play) {
                     camera2->at = this->cameraAt;
                     func_800C08AC(play, this->csCamera, 0);
                     this->csCamera = 0;
-                    func_80064534(play, &play->csCtx);
+                    Cutscene_StopManual(play, &play->csCtx);
                 }
             }
             for (indS1 = 0; indS1 < 41; indS1++) {
@@ -1477,7 +1477,7 @@ void BossMo_IntroCs(BossMo* this, PlayState* play) {
                 camera2->at = this->cameraAt;
                 func_800C08AC(play, this->csCamera, 0);
                 this->csState = this->csCamera = MO_BATTLE;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
             }
             break;
@@ -1709,7 +1709,7 @@ void BossMo_DeathCs(BossMo* this, PlayState* play) {
                     camera->at = this->cameraAt;
                     func_800C08AC(play, this->csCamera, 0);
                     this->csCamera = 0;
-                    func_80064534(play, &play->csCtx);
+                    Cutscene_StopManual(play, &play->csCtx);
                     Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                     sMorphaTent1->actor.world.pos.y = -1000.0f;
                 }
diff --git a/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c b/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
index f5a213c89..0907f7f24 100644
--- a/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
+++ b/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
@@ -437,7 +437,7 @@ void BossSst_HeadIntro(BossSst* this, PlayState* play) {
         sHands[RIGHT]->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
         sHands[LEFT]->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
         player->stateFlags1 &= ~PLAYER_STATE1_INPUT_DISABLED;
-        func_80064534(play, &play->csCtx);
+        Cutscene_StopManual(play, &play->csCtx);
         Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
         sSubCamAt.y += 30.0f;
         sSubCamAt.z += 300.0f;
@@ -1212,7 +1212,7 @@ void BossSst_HeadFinish(BossSst* this, PlayState* play) {
             Play_ChangeCameraStatus(play, CAM_ID_MAIN, CAM_STAT_ACTIVE);
             Play_ClearCamera(play, sSubCamId);
             Player_SetCsActionWithHaltedActors(play, &GET_PLAYER(play)->actor, 7);
-            func_80064534(play, &play->csCtx);
+            Cutscene_StopManual(play, &play->csCtx);
             Actor_Kill(&this->actor);
             Actor_Kill(&sHands[LEFT]->actor);
             Actor_Kill(&sHands[RIGHT]->actor);
diff --git a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
index 4d305fcd8..94415aa71 100644
--- a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
+++ b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
@@ -1694,7 +1694,7 @@ void BossTw_TwinrovaMergeCS(BossTw* this, PlayState* play) {
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
                 this->csState2 = this->subCamId;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->work[TW_PLLR_IDX] = 0;
                 this->targetPos = sTwinrovaPillarPos[0];
@@ -2256,7 +2256,7 @@ void BossTw_TwinrovaIntroCS(BossTw* this, PlayState* play) {
                 func_800C08AC(play, this->subCamId, 0);
                 this->subCamId = 0;
                 this->csState2 = this->subCamId;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 BossTw_SetupWait(this, play);
             }
@@ -2792,7 +2792,7 @@ void BossTw_TwinrovaDeathCS(BossTw* this, PlayState* play) {
                 func_800C08AC(play, this->subCamId, 0);
                 this->csState2 = 4;
                 this->subCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 Audio_QueueSeqCmd(SEQ_PLAYER_BGM_MAIN << 24 | NA_BGM_BOSS_CLEAR);
                 if (GameInteractor_Should(VB_SPAWN_BLUE_WARP, true, this)) {
diff --git a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
index 0a4165aec..6db6ceff6 100644
--- a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
+++ b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
@@ -1052,7 +1052,7 @@ void BossVa_BodyIntro(BossVa* this, PlayState* play) {
             if (this->timer == 0) {
                 Play_ClearCamera(play, sSubCamId);
                 sSubCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Play_ChangeCameraStatus(play, CAM_ID_MAIN, CAM_STAT_ACTIVE);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 sCsState++;
@@ -1667,7 +1667,7 @@ void BossVa_BodyDeath(BossVa* this, PlayState* play) {
             if (this->timer == 0) {
                 Play_ClearCamera(play, sSubCamId);
                 sSubCamId = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Play_ChangeCameraStatus(play, CAM_ID_MAIN, CAM_STAT_ACTIVE);
 
                 camera->eyeNext = camera->eye = sSubCamEye;
diff --git a/soh/src/overlays/actors/ovl_En_Clear_Tag/z_en_clear_tag.c b/soh/src/overlays/actors/ovl_En_Clear_Tag/z_en_clear_tag.c
index 16dca85d6..7c4f993b0 100644
--- a/soh/src/overlays/actors/ovl_En_Clear_Tag/z_en_clear_tag.c
+++ b/soh/src/overlays/actors/ovl_En_Clear_Tag/z_en_clear_tag.c
@@ -645,7 +645,7 @@ void EnClearTag_Update(Actor* thisx, PlayState* play2) {
                 if (this->cutsceneTimer == 1) {
                     func_800C08AC(play, this->cameraId, 0);
                     this->cutsceneMode = this->cameraId = SUBCAM_FREE;
-                    func_80064534(play, &play->csCtx);
+                    Cutscene_StopManual(play, &play->csCtx);
                 }
             }
         }
diff --git a/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c b/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
index 03fdf7255..f9efc5997 100644
--- a/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
+++ b/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
@@ -395,7 +395,7 @@ void EnfHG_Intro(EnfHG* this, PlayState* play) {
                 camera->at = this->cameraAt;
                 func_800C08AC(play, this->cutsceneCamera, 0);
                 this->cutsceneCamera = 0;
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 this->actionFunc = EnfHG_Retreat;
             }
diff --git a/soh/src/overlays/actors/ovl_Fishing/z_fishing.c b/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
index c52496431..1322bd1bf 100644
--- a/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
+++ b/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
@@ -5466,7 +5466,7 @@ void Fishing_UpdateOwner(Actor* thisx, PlayState* play2) {
             mainCam->eyeNext = sCameraEye;
             mainCam->at = sCameraAt;
             func_800C08AC(play, sSubCamId, 0);
-            func_80064534(play, &play->csCtx);
+            Cutscene_StopManual(play, &play->csCtx);
             sFishingPlayerCinematicState = 0;
             sSubCamId = 0;
             Environment_EnableUnderwaterLights(play, 0);
@@ -5514,7 +5514,7 @@ void Fishing_UpdateOwner(Actor* thisx, PlayState* play2) {
                 mainCam->eyeNext = sCameraEye;
                 mainCam->at = sCameraAt;
                 func_800C08AC(play, sSubCamId, 0);
-                func_80064534(play, &play->csCtx);
+                Cutscene_StopManual(play, &play->csCtx);
                 Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                 sFishingPlayerCinematicState = 0;
                 sSubCamId = 0;
@@ -5618,7 +5618,7 @@ void Fishing_UpdateOwner(Actor* thisx, PlayState* play2) {
                         mainCam->eyeNext = sCameraEye;
                         mainCam->at = sCameraAt;
                         func_800C08AC(play, sSubCamId, 0);
-                        func_80064534(play, &play->csCtx);
+                        Cutscene_StopManual(play, &play->csCtx);
                         Player_SetCsActionWithHaltedActors(play, &this->actor, 7);
                         sFishingPlayerCinematicState = 0;
                         sSubCamId = 0;
-- 
2.55.0

