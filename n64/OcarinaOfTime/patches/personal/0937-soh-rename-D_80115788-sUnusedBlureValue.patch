From da4700c96d9929d980156ee4f49bdedc43e7fd94 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:28:10 +0000
Subject: [PATCH 0937/3799] soh: rename D_80115788 -> sUnusedBlureValue

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the in-tree comment on its declaration reads "// unused".
Defined in soh/src/code/z_eff_blure.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/z_eff_blure.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/src/code/z_eff_blure.c b/soh/src/code/z_eff_blure.c
index 56e595149..2caaec074 100644
--- a/soh/src/code/z_eff_blure.c
+++ b/soh/src/code/z_eff_blure.c
@@ -878,7 +878,8 @@ void (*sSetupHandlers[])(GraphicsContext* gfxCtx, EffectBlure* this, Vtx* vtx) =
     EffectBlure_SetupSimpleAlt,
 };
 
-s32 D_80115788 = 0; // unused
+// LLM generated name (HIGH), was D_80115788: the in-tree comment on its declaration reads "// unused".
+s32 sUnusedBlureValue = 0; // unused, was D_80115788 [LLM:HIGH]
 
 // original name: "EffectBlureInfo2_disp_makeDisplayList"
 void EffectBlure_DrawSimpleVertices(GraphicsContext* gfxCtx, EffectBlure* this, Vtx* vtx) {
-- 
2.55.0

