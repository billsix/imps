From db76e262954a096df62117f458ff61eb7fc2133a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:18:57 +0000
Subject: [PATCH 1432/3799] soh: rename func_809B0558 -> EnAni_GiveHeartPiece

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: re-offers the heart piece until the player takes it, then sets ITEMGETINF_15 and returns to the age-appropriate talk state.
Defined in soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
index fb225d555..1b5acf171 100644
--- a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
+++ b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
@@ -18,7 +18,8 @@ void EnAni_Draw(Actor* thisx, PlayState* play);
 s32 EnAni_SetText(EnAni* this, PlayState* play, u16 textId);
 void func_809B04F0(EnAni* this, PlayState* play);
 void func_809B0524(EnAni* this, PlayState* play);
-void func_809B0558(EnAni* this, PlayState* play);
+// LLM generated name (HIGH), was func_809B0558: re-offers the heart piece until the player takes it, then sets ITEMGETINF_15 and returns to the age-appropriate talk state.
+void EnAni_GiveHeartPiece(EnAni* this, PlayState* play);  // was func_809B0558 [LLM:HIGH]
 // LLM generated name (HIGH), was func_809B05F0: offers GI_HEART_PIECE every frame while the textbox is up, and moves to the hand-off state once it closes.
 void EnAni_OfferHeartPiece(EnAni* this, PlayState* play);  // was func_809B05F0 [LLM:HIGH]
 // LLM generated name (HIGH), was func_809B064C: the child-era idle on the roof: picks the day, night or face-reaction line, offers talk at two different ranges, and routes an accepted conversation to the heart-piece hand-off or to the plain talk state.
@@ -120,7 +121,7 @@ void func_809B0524(EnAni* this, PlayState* play) {
     }
 }
 
-void func_809B0558(EnAni* this, PlayState* play) {
+void EnAni_GiveHeartPiece(EnAni* this, PlayState* play) {
     if (Actor_HasParent(&this->actor, play) || !GameInteractor_Should(VB_GIVE_ITEM_FROM_MAN_ON_ROOF, true)) {
         this->actor.parent = NULL;
         if (!LINK_IS_ADULT) {
@@ -138,7 +139,7 @@ void func_809B0558(EnAni* this, PlayState* play) {
 
 void EnAni_OfferHeartPiece(EnAni* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
-        EnAni_SetupAction(this, func_809B0558);
+        EnAni_SetupAction(this, EnAni_GiveHeartPiece);  // was func_809B0558 [LLM:HIGH]
     }
 
     if (GameInteractor_Should(VB_GIVE_ITEM_FROM_MAN_ON_ROOF, true)) {
-- 
2.55.0

