From ff149ff724cfc47d2d5d5dc65b39102835a3f51e Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:21:16 +0000
Subject: [PATCH 2284/3799] soh: rename func_80AB3428 -> EnNb_CreditsSit

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the NB_CREDITS_SIT step: runs the sitting animation and eyes while waiting on the credits cutscene
Defined in soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c b/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
index 0c5ab9b1d..9d4dea2ec 100644
--- a/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
+++ b/soh/src/overlays/actors/ovl_En_Nb/z_en_nb.c
@@ -1103,7 +1103,8 @@ void EnNb_CreditsFade(EnNb* this, PlayState* play) {
     EnNb_SetupCreditsSit(this);
 }
 
-void func_80AB3428(EnNb* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AB3428: the NB_CREDITS_SIT step: runs the sitting animation and eyes while waiting on the credits cutscene
+void EnNb_CreditsSit(EnNb* this, PlayState* play) {
     EnNb_UpdateBgCheck(this, play);  // was func_80AB1284 [LLM:HIGH]
     EnNb_UpdateSkelAnime(this);
     EnNb_UpdateEyes(this);
@@ -1419,7 +1420,7 @@ static EnNbActionFunc sActionFuncs[] = {
     EnNb_ConfrontationDestroy,
     EnNb_CheckCreditsCsMode,
     EnNb_CreditsFade,
-    func_80AB3428,
+    EnNb_CreditsSit,  // was func_80AB3428 [LLM:HIGH]
     EnNb_LookUp,
     EnNb_WaitForNotice,
     EnNb_StandUpAfterNotice,
-- 
2.55.0

