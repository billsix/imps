From 3e56916c73c4b1735912fabd051da6af7d1285b9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:35 +0000
Subject: [PATCH 2771/3799] soh: rename func_80035124 -> Actor_UpdateFallenItem

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: a two-state params machine that falls under gravity and reports a hard landing; no caller survives in this tree, so the item reading comes from the shape of the states
Defined in soh/src/code/z_actor.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/z_actor.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 4b10fa1b9..cf1795d8a 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4700,7 +4700,8 @@ void Actor_UpdateFidgetTables(PlayState* play, s16* arg1, s16* arg2, s32 arg3) {
 void Actor_Noop(Actor* actor, PlayState* play) {
 }
 
-s32 func_80035124(Actor* actor, PlayState* play) {
+// LLM generated name (GUESS), was func_80035124: a two-state params machine that falls under gravity and reports a hard landing; no caller survives in this tree, so the item reading comes from the shape of the states
+s32 Actor_UpdateFallenItem(Actor* actor, PlayState* play) {
     s32 ret = 0;
 
     switch (actor->params) {
-- 
2.55.0

