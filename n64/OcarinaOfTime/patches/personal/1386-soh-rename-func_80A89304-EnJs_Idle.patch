From f4df3859506795d0d2d8056692ad8c8c961ea942 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:16:06 +0000
Subject: [PATCH 1386/3799] soh: rename func_80A89304 -> EnJs_Idle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the resting action: offers the sales pitch text and, when the player accepts, switches to the offer state.
Defined in soh/src/overlays/actors/ovl_En_Js/z_en_js.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Js/z_en_js.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Js/z_en_js.c b/soh/src/overlays/actors/ovl_En_Js/z_en_js.c
index 21673359e..86046e966 100644
--- a/soh/src/overlays/actors/ovl_En_Js/z_en_js.c
+++ b/soh/src/overlays/actors/ovl_En_Js/z_en_js.c
@@ -16,7 +16,8 @@ void EnJs_Destroy(Actor* thisx, PlayState* play);
 void EnJs_Update(Actor* thisx, PlayState* play);
 void EnJs_Draw(Actor* thisx, PlayState* play);
 
-void func_80A89304(EnJs* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A89304: the resting action: offers the sales pitch text and, when the player accepts, switches to the offer state.
+void EnJs_Idle(EnJs* this, PlayState* play);  // was func_80A89304 [LLM:HIGH]
 
 const ActorInit En_Js_InitVars = {
     ACTOR_EN_JS,
@@ -67,7 +68,7 @@ void EnJs_Init(Actor* thisx, PlayState* play) {
     Collider_SetCylinder(play, &this->collider, &this->actor, &sCylinderInit);
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
     Actor_SetScale(&this->actor, 0.01f);
-    En_Js_SetupAction(this, func_80A89304);
+    En_Js_SetupAction(this, EnJs_Idle);  // was func_80A89304 [LLM:HIGH]
     this->unk_284 = 0;
     this->actor.gravity = -1.0f;
     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_JSJUTAN, this->actor.world.pos.x,
@@ -100,7 +101,7 @@ u8 EnJs_OfferTalk(EnJs* this, PlayState* play, u16 textId) {
 
 // LLM generated name (HIGH), was func_80A89008: installs the idle action and starts the knee-slapping animation.
 void EnJs_SetupIdle(EnJs* this) {
-    En_Js_SetupAction(this, func_80A89304);
+    En_Js_SetupAction(this, EnJs_Idle);  // was func_80A89304 [LLM:HIGH]
     Animation_Change(&this->skelAnime, &gCarpetMerchantSlappingKneeAnim, 1.0f, 0.0f,
                      Animation_GetLastFrame(&gCarpetMerchantSlappingKneeAnim), ANIMMODE_ONCE, -4.0f);
 }
@@ -173,7 +174,7 @@ void func_80A89294(EnJs* this) {
                      Animation_GetLastFrame(&gCarpetMerchantIdleAnim), ANIMMODE_ONCE, -4.0f);
 }
 
-void func_80A89304(EnJs* this, PlayState* play) {
+void EnJs_Idle(EnJs* this, PlayState* play) {
     if (EnJs_OfferTalk(this, play, 0x6077)) {  // was func_80A88F64 [LLM:HIGH]
         func_80A89294(this);
     }
-- 
2.55.0

