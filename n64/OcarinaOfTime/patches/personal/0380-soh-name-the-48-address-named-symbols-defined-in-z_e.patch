From af9eb8f12ca01033ca7fb5455520720d9231d4bb Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:31 +0000
Subject: [PATCH 380/488] soh: name the 48 address-named symbols defined in
 z_en_owl.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 48 are defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c; updating their references touches 3 files in total.

  * func_80ACA558 -> EnOwl_OfferTalk
    deduced, confidence HIGH: sets the text id and offers the talk exchange
    once the player is within 120 units

  * func_80ACA5C8 -> EnOwl_SetupUnfoldWings
    deduced, confidence HIGH: switches to the perched wait on
    gOwlUnfoldWingsAnim and reseeds the blink

  * func_80ACA62C -> EnOwl_SetSwitchAndUnfold
    deduced, confidence HIGH: sets this owl's switch flag when it has one,
    then unfolds the wings

  * func_80ACA690 -> EnOwl_WaitForHeadReturn
    deduced, confidence GUESS: holds until the head-turn counter returns to
    zero before unfolding; the counter's exact meaning beyond driving the
    head is not established

  * func_80ACA6C0 -> EnOwl_PickHeadTurnDirection
    deduced, confidence HIGH: flips the head-turn direction flag at random

  * func_80ACA71C -> EnOwl_StartHeadTurn
    deduced, confidence HIGH: picks a direction and arms the head-turn
    counters

  * func_80ACA76C -> EnOwl_EndKokiriTalk
    deduced, confidence HIGH: stops the owl fanfare when the Kokiri textbox
    closes and sends the owl into its takeoff

  * func_80ACA7E0 -> EnOwl_EndTalk
    deduced, confidence HIGH: the shared conversation ending: stops the
    fanfare, then either takes off or waits for the head to come back

  * func_80ACA998 -> EnOwl_HyruleCastleChoice
    deduced, confidence HIGH: the repeat or accept choice on the Hyrule
    Castle owl's message, texts 0x2069 and 0x206B

  * func_80ACAA54 -> EnOwl_HyruleCastleAdvance
    deduced, confidence HIGH: continues into text 0x206A and hands over to
    that choice

  * func_80ACAAC0 -> EnOwl_HyruleCastleStart
    deduced, confidence HIGH: opens the Hyrule Castle message at text 0x2069

  * func_80ACAB88 -> EnOwl_KakarikoChoice
    deduced, confidence HIGH: the repeat or accept choice on the Kakariko
    owl's message, texts 0x206C-0x206E

  * func_80ACAC6C -> EnOwl_KakarikoAdvance
    deduced, confidence HIGH: continues into text 0x206A and hands over to
    that choice

  * func_80ACAD34 -> EnOwl_GerudoChoice
    deduced, confidence HIGH: the repeat or accept choice on the Gerudo
    Valley owl's message

  * func_80ACADF0 -> EnOwl_GerudoAdvance
    deduced, confidence HIGH: continues the Gerudo Valley message into its
    choice

  * func_80ACAEB8 -> EnOwl_LakeHyliaChoice
    deduced, confidence HIGH: the repeat or accept choice on the Lake Hylia
    owl's message

  * func_80ACAF74 -> EnOwl_LakeHyliaAdvance
    deduced, confidence HIGH: continues the Lake Hylia message into its
    choice

  * func_80ACB03C -> EnOwl_EndZoraRiverTalk
    deduced, confidence HIGH: closes the Zora River owl's single message and
    takes off

  * func_80ACB148 -> EnOwl_EndHyliaShortcutTalk
    deduced, confidence HIGH: closes the Lake Hylia shortcut message, sets
    switch 0x23 and takes off

  * func_80ACB22C -> EnOwl_EndShortcutTalk
    deduced, confidence HIGH: the same close and takeoff without the switch
    flag

  * func_80ACB274 -> EnOwl_EndDeathMountainTalk
    deduced, confidence HIGH: closes the Death Mountain shortcut message and
    returns to the perched wait

  * func_80ACB344 -> EnOwl_DesertColossusChoice
    deduced, confidence HIGH: the repeat or accept choice on the Desert
    Colossus owl's message, texts 0x607A and 0x607C

  * func_80ACB3E0 -> EnOwl_WaitDesertColossus
    deduced, confidence HIGH: the Desert Colossus owl's perch: offers text
    0x6079 and starts its choice

  * func_80ACB440 -> EnOwl_LWPreSariaChoice
    deduced, confidence HIGH: the repeat or accept choice on the pre-Saria
    Lost Woods message, texts 0x10C1-0x10C3

  * func_80ACB4FC -> EnOwl_LWPreSariaAdvance
    deduced, confidence HIGH: continues the pre-Saria message into its
    choice

  * func_80ACB5C4 -> EnOwl_LWPostSariaChoice
    deduced, confidence HIGH: the repeat or accept choice on the post-Saria
    Lost Woods message, texts 0x10C5-0x10C7

  * func_80ACB680 -> EnOwl_LWPostSariaAdvance
    deduced, confidence HIGH: continues the post-Saria message into its
    choice

  * func_80ACB748 -> EnOwl_PlayFlightSfx
    deduced, confidence HIGH: scales the wind, flutter and pass-by sounds by
    how fast the camera is moving during the travel cutscenes

  * func_80ACB904 -> EnOwl_CutsceneFlyStraight
    deduced, confidence HIGH: rides cue 7, re-seating the owl on each new
    action, and follows the straight-line path

  * func_80ACB994 -> EnOwl_CutsceneFlyCurve
    deduced, confidence HIGH: the same cue handling with the curved path
    update instead

  * func_80ACBAB8 -> EnOwl_SetCutsceneMode
    deduced, confidence HIGH: picks the animation and update for the current
    cue 7 action

  * func_80ACBC0C -> EnOwl_FlyAway
    deduced, confidence HIGH: climbs and accelerates away on its chosen
    heading, dying once 6000 units out

  * func_80ACBD4C -> EnOwl_Takeoff
    deduced, confidence HIGH: drives the climb through the takeoff animation
    and then switches to the flight

  * func_80ACBEA0 -> EnOwl_Perched
    deduced, confidence HIGH: waits on the perch until the animation ends,
    then starts the takeoff turned a quarter turn

  * func_80ACBF50 -> EnOwl_TurnThenFly
    deduced, confidence HIGH: turns onto the stored heading and then
    switches to the flight

  * func_80ACC00C -> EnOwl_WaitForTravelCs
    deduced, confidence HIGH: circles until the player comes within 50
    units, then starts that owl's travel cutscene

  * func_80ACC23C -> EnOwl_TravelTakeoff
    deduced, confidence HIGH: the takeoff that precedes a travel cutscene,
    halting the owl at frame 37 to wait for the player

  * func_80ACC30C -> EnOwl_PerchedBeforeTravel
    deduced, confidence HIGH: waits on the perch until the animation ends,
    then starts the travel takeoff

  * func_80ACC390 -> EnOwl_AnimGlide
    deduced, confidence HIGH: animation step: rocks the body while the glide
    counter runs, then switches to the flap cycle

  * func_80ACC460 -> EnOwl_AnimFlap
    deduced, confidence HIGH: animation step: repeats the flap the counted
    number of times, then switches to the glide

  * func_80ACC540 -> EnOwl_AnimLoopOnce
    deduced, confidence HIGH: animation step: restarts the current animation
    each time it ends and reports that in the action flags

  * func_80ACC5CC -> EnOwl_StepHeadTurn
    deduced, confidence HIGH: steps the head angle four units a frame toward
    its target and reports arrival

  * func_80ACC624 -> EnOwl_ShouldDrawInColossus
    deduced, confidence HIGH: true everywhere except the Desert Colossus
    outside the two cutscene frame windows where this owl is hidden

  * func_80ACD130 -> EnOwl_SetPosFromCue
    deduced, confidence HIGH: snaps the owl onto a cue's start position and
    rotation

  * func_80ACD1C4 -> EnOwl_GetCueWeight
    deduced, confidence HIGH: returns the cue's progress from 0 to 1

  * func_80ACD220 -> EnOwl_FlyToward
    deduced, confidence HIGH: sets the speed and heading to close on a point
    by the caller's weight

  * func_80ACD2CC -> EnOwl_FollowCueStraight
    deduced, confidence HIGH: flies the owl along the cue's straight
    segment, interpolating its roll

  * func_80ACD4D4 -> EnOwl_FollowCueCurve
    deduced, confidence HIGH: flies the owl along the cue's curved segment

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../TimeSavers/timesaver_hook_handlers.cpp    |   2 +-
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 364 ++++++++++--------
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.h |   2 +-
 3 files changed, 208 insertions(+), 160 deletions(-)

diff --git a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
index d583621fd..ee29b761b 100644
--- a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
+++ b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
@@ -557,7 +557,7 @@ void TimeSaverOnVanillaBehaviorHandler(GIVanillaBehavior id, bool* should, va_li
                     Flags_SetEventChkInf(EVENTCHKINF_SPOKE_TO_KAEPORA_BY_LOST_WOODS);
                 }
 
-                func_80ACA62C(enOwl, gPlayState);
+                EnOwl_SetSwitchAndUnfold(enOwl, gPlayState);  // was func_80ACA62C [LLM:HIGH]
                 *should = false;
             }
             break;
diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index 39f95cf78..9790abdf6 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -20,7 +20,8 @@ void EnOwl_Update(Actor* thisx, PlayState* play);
 void EnOwl_Draw(Actor* thisx, PlayState* play);
 void EnOwl_ChangeMode(EnOwl* this, EnOwlActionFunc, OwlFunc, SkelAnime*, AnimationHeader*, f32);
 void EnOwl_WaitDefault(EnOwl* this, PlayState* play);
-void func_80ACC540(EnOwl* this);
+// LLM generated name (HIGH), was func_80ACC540: animation step: restarts the current animation each time it ends and reports that in the action flags
+void EnOwl_AnimLoopOnce(EnOwl* this);  // was func_80ACC540 [LLM:HIGH]
 void EnOwl_WaitOutsideKokiri(EnOwl* this, PlayState* play);
 void EnOwl_WaitHyruleCastle(EnOwl* this, PlayState* play);
 void EnOwl_WaitKakariko(EnOwl* this, PlayState* play);
@@ -29,23 +30,38 @@ void EnOwl_WaitLakeHylia(EnOwl* this, PlayState* play);
 void EnOwl_WaitZoraRiver(EnOwl* this, PlayState* play);
 void EnOwl_WaitHyliaShortcut(EnOwl* this, PlayState* play);
 void EnOwl_WaitDeathMountainShortcut(EnOwl* this, PlayState* play);
-void func_80ACB3E0(EnOwl* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ACB3E0: the Desert Colossus owl's perch: offers text 0x6079 and starts its choice
+void EnOwl_WaitDesertColossus(EnOwl* this, PlayState* play);  // was func_80ACB3E0 [LLM:HIGH]
 void EnOwl_WaitLWPreSaria(EnOwl* this, PlayState* play);
 void EnOwl_WaitLWPostSaria(EnOwl* this, PlayState* play);
-void func_80ACD4D4(EnOwl* this, PlayState* play);
-void func_80ACD130(EnOwl* this, PlayState* play, s32 arg2);
-void func_80ACBAB8(EnOwl* this, PlayState* play);
-void func_80ACD2CC(EnOwl* this, PlayState* play);
-void func_80ACAA54(EnOwl* this, PlayState* play);
-void func_80ACAC6C(EnOwl* this, PlayState* play);
-void func_80ACADF0(EnOwl* this, PlayState* play);
-void func_80ACAF74(EnOwl* this, PlayState* play);
-void func_80ACC30C(EnOwl* this, PlayState* play);
-void func_80ACB4FC(EnOwl* this, PlayState* play);
-void func_80ACB680(EnOwl* this, PlayState* play);
-void func_80ACA62C(EnOwl* this, PlayState* play);
-void func_80ACC460(EnOwl* this);
-void func_80ACBEA0(EnOwl*, PlayState*);
+// LLM generated name (HIGH), was func_80ACD4D4: flies the owl along the cue's curved segment
+void EnOwl_FollowCueCurve(EnOwl* this, PlayState* play);  // was func_80ACD4D4 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACD130: snaps the owl onto a cue's start position and rotation
+void EnOwl_SetPosFromCue(EnOwl* this, PlayState* play, s32 arg2);  // was func_80ACD130 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACBAB8: picks the animation and update for the current cue 7 action
+void EnOwl_SetCutsceneMode(EnOwl* this, PlayState* play);  // was func_80ACBAB8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACD2CC: flies the owl along the cue's straight segment, interpolating its roll
+void EnOwl_FollowCueStraight(EnOwl* this, PlayState* play);  // was func_80ACD2CC [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACAA54: continues into text 0x206A and hands over to that choice
+void EnOwl_HyruleCastleAdvance(EnOwl* this, PlayState* play);  // was func_80ACAA54 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACAC6C: continues into text 0x206A and hands over to that choice
+void EnOwl_KakarikoAdvance(EnOwl* this, PlayState* play);  // was func_80ACAC6C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACADF0: continues the Gerudo Valley message into its choice
+void EnOwl_GerudoAdvance(EnOwl* this, PlayState* play);  // was func_80ACADF0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACAF74: continues the Lake Hylia message into its choice
+void EnOwl_LakeHyliaAdvance(EnOwl* this, PlayState* play);  // was func_80ACAF74 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACC30C: waits on the perch until the animation ends, then starts the travel takeoff
+void EnOwl_PerchedBeforeTravel(EnOwl* this, PlayState* play);  // was func_80ACC30C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACB4FC: continues the pre-Saria message into its choice
+void EnOwl_LWPreSariaAdvance(EnOwl* this, PlayState* play);  // was func_80ACB4FC [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACB680: continues the post-Saria message into its choice
+void EnOwl_LWPostSariaAdvance(EnOwl* this, PlayState* play);  // was func_80ACB680 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACA62C: sets this owl's switch flag when it has one, then unfolds the wings
+void EnOwl_SetSwitchAndUnfold(EnOwl* this, PlayState* play);  // was func_80ACA62C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACC460: animation step: repeats the flap the counted number of times, then switches to the glide
+void EnOwl_AnimFlap(EnOwl* this);  // was func_80ACC460 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80ACBEA0: waits on the perch until the animation ends, then starts the takeoff turned a quarter turn
+void EnOwl_Perched(EnOwl*, PlayState*);  // was func_80ACBEA0 [LLM:HIGH]
 
 typedef enum {
     /* 0x00 */ OWL_DEFAULT,
@@ -124,7 +140,7 @@ void EnOwl_Init(Actor* thisx, PlayState* play) {
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
     this->actor.minVelocityY = -10.0f;
     this->actor.targetArrowOffset = 500.0f;
-    EnOwl_ChangeMode(this, EnOwl_WaitDefault, func_80ACC540, &this->skelAnime2, &gOwlPerchAnim, 0.0f);
+    EnOwl_ChangeMode(this, EnOwl_WaitDefault, EnOwl_AnimLoopOnce, &this->skelAnime2, &gOwlPerchAnim, 0.0f);  // was func_80ACC540 [LLM:HIGH]
     this->actionFlags = this->unk_406 = this->unk_409 = 0;
     this->unk_405 = 4;
     this->unk_404 = this->unk_407 = 0;
@@ -205,7 +221,7 @@ void EnOwl_Init(Actor* thisx, PlayState* play) {
             this->actionFunc = EnOwl_WaitDeathMountainShortcut;
             break;
         case OWL_DESSERT_COLOSSUS:
-            this->actionFunc = func_80ACB3E0;
+            this->actionFunc = EnOwl_WaitDesertColossus;  // was func_80ACB3E0 [LLM:HIGH]
             break;
         case OWL_LOST_WOODS_PRESARIA:
             if (!CHECK_QUEST_ITEM(QUEST_SONG_LULLABY)) {
@@ -288,7 +304,8 @@ s32 EnOwl_CheckInitTalk(EnOwl* this, PlayState* play, u16 textId, f32 targetDist
     }
 }
 
-s32 func_80ACA558(EnOwl* this, PlayState* play, u16 textId) {
+// LLM generated name (HIGH), was func_80ACA558: sets the text id and offers the talk exchange once the player is within 120 units
+s32 EnOwl_OfferTalk(EnOwl* this, PlayState* play, u16 textId) {
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
         return true;
     } else {
@@ -300,29 +317,32 @@ s32 func_80ACA558(EnOwl* this, PlayState* play, u16 textId) {
     }
 }
 
-void func_80ACA5C8(EnOwl* this) {
-    EnOwl_ChangeMode(this, func_80ACBEA0, func_80ACC540, &this->skelAnime, &gOwlUnfoldWingsAnim, 0.0f);
+// LLM generated name (HIGH), was func_80ACA5C8: switches to the perched wait on gOwlUnfoldWingsAnim and reseeds the blink
+void EnOwl_SetupUnfoldWings(EnOwl* this) {
+    EnOwl_ChangeMode(this, EnOwl_Perched, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlUnfoldWingsAnim, 0.0f);  // was func_80ACBEA0 [LLM:HIGH]
     this->eyeTexIndex = 0;
     this->blinkTimer = Rand_S16Offset(60, 60);
 }
 
-void func_80ACA62C(EnOwl* this, PlayState* play) {
+void EnOwl_SetSwitchAndUnfold(EnOwl* this, PlayState* play) {
     s32 switchFlag = this->actor.params & 0x3F;
 
     if (switchFlag < 0x20) {
         Flags_SetSwitch(play, switchFlag);
         osSyncPrintf(VT_FGCOL(CYAN) " Actor_Environment_sw = %d\n" VT_RST, Flags_GetSwitch(play, switchFlag));
     }
-    func_80ACA5C8(this);
+    EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
 }
 
-void func_80ACA690(EnOwl* this, PlayState* play) {
+// LLM generated name (GUESS), was func_80ACA690: holds until the head-turn counter returns to zero before unfolding; the counter's exact meaning beyond driving the head is not established
+void EnOwl_WaitForHeadReturn(EnOwl* this, PlayState* play) {
     if ((this->unk_3EE & 0x3F) == 0) {
-        func_80ACA62C(this, play);
+        EnOwl_SetSwitchAndUnfold(this, play);  // was func_80ACA62C [LLM:HIGH]
     }
 }
 
-void func_80ACA6C0(EnOwl* this) {
+// LLM generated name (HIGH), was func_80ACA6C0: flips the head-turn direction flag at random
+void EnOwl_PickHeadTurnDirection(EnOwl* this) {
     if (Rand_CenteredFloat(1.0f) < 0.0f) {
         this->actionFlags |= 0x20;
     } else {
@@ -330,8 +350,9 @@ void func_80ACA6C0(EnOwl* this) {
     }
 }
 
-void func_80ACA71C(EnOwl* this) {
-    func_80ACA6C0(this);
+// LLM generated name (HIGH), was func_80ACA71C: picks a direction and arms the head-turn counters
+void EnOwl_StartHeadTurn(EnOwl* this) {
+    EnOwl_PickHeadTurnDirection(this);  // was func_80ACA6C0 [LLM:HIGH]
     this->unk_3F2 = 0;
     this->actionFlags |= 0x10;
     this->unk_408 = 4;
@@ -341,27 +362,29 @@ void func_80ACA71C(EnOwl* this) {
     this->unk_407 = this->unk_3F2;
 }
 
-void func_80ACA76C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACA76C: stops the owl fanfare when the Kokiri textbox closes and sends the owl into its takeoff
+void EnOwl_EndKokiriTalk(EnOwl* this, PlayState* play) {
     Player_SetCsActionWithHaltedActors(play, &this->actor, 8);
 
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA62C(this, play);
+        EnOwl_SetSwitchAndUnfold(this, play);  // was func_80ACA62C [LLM:HIGH]
         this->actor.flags &= ~ACTOR_FLAG_TALK_OFFER_AUTO_ACCEPTED;
     }
 }
 
-void func_80ACA7E0(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACA7E0: the shared conversation ending: stops the fanfare, then either takes off or waits for the head to come back
+void EnOwl_EndTalk(EnOwl* this, PlayState* play) {
     Player_SetCsActionWithHaltedActors(play, &this->actor, 8);
 
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
         if ((this->unk_3EE & 0x3F) == 0) {
-            func_80ACA62C(this, play);
+            EnOwl_SetSwitchAndUnfold(this, play);  // was func_80ACA62C [LLM:HIGH]
         } else {
             this->actionFlags &= ~2;
-            func_80ACA71C(this);
-            this->actionFunc = func_80ACA690;
+            EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
+            this->actionFunc = EnOwl_WaitForHeadReturn;  // was func_80ACA690 [LLM:GUESS]
         }
         this->actor.flags &= ~ACTOR_FLAG_TALK_OFFER_AUTO_ACCEPTED;
     }
@@ -375,7 +398,7 @@ void EnOwl_ConfirmKokiriMessage(EnOwl* this, PlayState* play) {
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x2067);
-                this->actionFunc = func_80ACA76C;
+                this->actionFunc = EnOwl_EndKokiriTalk;  // was func_80ACA76C [LLM:HIGH]
                 break;
         }
     }
@@ -394,39 +417,41 @@ void EnOwl_WaitOutsideKokiri(EnOwl* this, PlayState* play) {
     }
 }
 
-void func_80ACA998(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACA998: the repeat or accept choice on the Hyrule Castle owl's message, texts 0x2069 and 0x206B
+void EnOwl_HyruleCastleChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         // swap the order of the responses if better owl is enabled
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
                 Message_ContinueTextbox(play, 0x2069);
-                this->actionFunc = func_80ACAA54;
+                this->actionFunc = EnOwl_HyruleCastleAdvance;  // was func_80ACAA54 [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x206B);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACAA54(EnOwl* this, PlayState* play) {
+void EnOwl_HyruleCastleAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x206A);
-        this->actionFunc = func_80ACA998;
+        this->actionFunc = EnOwl_HyruleCastleChoice;  // was func_80ACA998 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACAAC0(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACAAC0: opens the Hyrule Castle message at text 0x2069
+void EnOwl_HyruleCastleStart(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x2069);
-        this->actionFunc = func_80ACAA54;
+        this->actionFunc = EnOwl_HyruleCastleAdvance;  // was func_80ACAA54 [LLM:HIGH]
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -435,11 +460,12 @@ void EnOwl_WaitHyruleCastle(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x2068, 540.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACAAC0;
+        this->actionFunc = EnOwl_HyruleCastleStart;  // was func_80ACAAC0 [LLM:HIGH]
     }
 }
 
-void func_80ACAB88(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACAB88: the repeat or accept choice on the Kakariko owl's message, texts 0x206C-0x206E
+void EnOwl_KakarikoChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
@@ -449,25 +475,25 @@ void func_80ACAB88(EnOwl* this, PlayState* play) {
                 } else {
                     Message_ContinueTextbox(play, 0x206C);
                 }
-                this->actionFunc = func_80ACAC6C;
+                this->actionFunc = EnOwl_KakarikoAdvance;  // was func_80ACAC6C [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x206E);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACAC6C(EnOwl* this, PlayState* play) {
+void EnOwl_KakarikoAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x206A);
-        this->actionFunc = func_80ACAB88;
+        this->actionFunc = EnOwl_KakarikoChoice;  // was func_80ACAB88 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -476,34 +502,35 @@ void EnOwl_WaitKakariko(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x206C, 480.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACAC6C;
+        this->actionFunc = EnOwl_KakarikoAdvance;  // was func_80ACAC6C [LLM:HIGH]
     }
 }
 
-void func_80ACAD34(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACAD34: the repeat or accept choice on the Gerudo Valley owl's message
+void EnOwl_GerudoChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
                 Message_ContinueTextbox(play, 0x206F);
-                this->actionFunc = func_80ACADF0;
+                this->actionFunc = EnOwl_GerudoAdvance;  // was func_80ACADF0 [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x2070);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACADF0(EnOwl* this, PlayState* play) {
+void EnOwl_GerudoAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x206A);
-        this->actionFunc = func_80ACAD34;
+        this->actionFunc = EnOwl_GerudoChoice;  // was func_80ACAD34 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -512,34 +539,35 @@ void EnOwl_WaitGerudo(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x206F, 360.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACADF0;
+        this->actionFunc = EnOwl_GerudoAdvance;  // was func_80ACADF0 [LLM:HIGH]
     }
 }
 
-void func_80ACAEB8(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACAEB8: the repeat or accept choice on the Lake Hylia owl's message
+void EnOwl_LakeHyliaChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
                 Message_ContinueTextbox(play, 0x2071);
-                this->actionFunc = func_80ACAF74;
+                this->actionFunc = EnOwl_LakeHyliaAdvance;  // was func_80ACAF74 [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x2072);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACAF74(EnOwl* this, PlayState* play) {
+void EnOwl_LakeHyliaAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x206A);
-        this->actionFunc = func_80ACAEB8;
+        this->actionFunc = EnOwl_LakeHyliaChoice;  // was func_80ACAEB8 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -548,16 +576,17 @@ void EnOwl_WaitLakeHylia(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x2071, 360.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACAF74;
+        this->actionFunc = EnOwl_LakeHyliaAdvance;  // was func_80ACAF74 [LLM:HIGH]
     }
 }
 
-void func_80ACB03C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB03C: closes the Zora River owl's single message and takes off
+void EnOwl_EndZoraRiverTalk(EnOwl* this, PlayState* play) {
     Player_SetCsActionWithHaltedActors(play, &this->actor, 8);
 
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA62C(this, play);
+        EnOwl_SetSwitchAndUnfold(this, play);  // was func_80ACA62C [LLM:HIGH]
         this->actor.flags &= ~ACTOR_FLAG_TALK_OFFER_AUTO_ACCEPTED;
     }
 }
@@ -579,15 +608,16 @@ void EnOwl_WaitZoraRiver(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, textId, 360.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB03C;
+        this->actionFunc = EnOwl_EndZoraRiverTalk;  // was func_80ACB03C [LLM:HIGH]
     }
 }
 
-void func_80ACB148(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB148: closes the Lake Hylia shortcut message, sets switch 0x23 and takes off
+void EnOwl_EndHyliaShortcutTalk(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA5C8(this);
-        this->actionFunc = func_80ACC30C;
+        EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
+        this->actionFunc = EnOwl_PerchedBeforeTravel;  // was func_80ACC30C [LLM:HIGH]
         Flags_SetSwitch(play, 0x23);
     }
 }
@@ -597,22 +627,24 @@ void EnOwl_WaitHyliaShortcut(EnOwl* this, PlayState* play) {
 
     // Spoke to Owl in Lake Hylia
     EnOwl_LookAtLink(this, play);
-    if (func_80ACA558(this, play, textId)) {
+    if (EnOwl_OfferTalk(this, play, textId)) {  // was func_80ACA558 [LLM:HIGH]
         Flags_SetInfTable(INFTABLE_SPOKE_TO_KAEPORA_IN_LAKE_HYLIA);
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB148;
+        this->actionFunc = EnOwl_EndHyliaShortcutTalk;  // was func_80ACB148 [LLM:HIGH]
     }
 }
 
-void func_80ACB22C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB22C: the same close and takeoff without the switch flag
+void EnOwl_EndShortcutTalk(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA5C8(this);
-        this->actionFunc = func_80ACC30C;
+        EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
+        this->actionFunc = EnOwl_PerchedBeforeTravel;  // was func_80ACC30C [LLM:HIGH]
     }
 }
 
-void func_80ACB274(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB274: closes the Death Mountain shortcut message and returns to the perched wait
+void EnOwl_EndDeathMountainTalk(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
         this->actionFunc = EnOwl_WaitDeathMountainShortcut;
@@ -623,20 +655,21 @@ void EnOwl_WaitDeathMountainShortcut(EnOwl* this, PlayState* play) {
     EnOwl_LookAtLink(this, play);
 
     if (!gSaveContext.isMagicAcquired && !IS_RANDO) {
-        if (func_80ACA558(this, play, 0x3062)) {
+        if (EnOwl_OfferTalk(this, play, 0x3062)) {  // was func_80ACA558 [LLM:HIGH]
             Audio_PlayFanfare(NA_BGM_OWL);
-            this->actionFunc = func_80ACB274;
+            this->actionFunc = EnOwl_EndDeathMountainTalk;  // was func_80ACB274 [LLM:HIGH]
             return;
         }
     } else {
-        if (func_80ACA558(this, play, 0x3063)) {
+        if (EnOwl_OfferTalk(this, play, 0x3063)) {  // was func_80ACA558 [LLM:HIGH]
             Audio_PlayFanfare(NA_BGM_OWL);
-            this->actionFunc = func_80ACB22C;
+            this->actionFunc = EnOwl_EndShortcutTalk;  // was func_80ACB22C [LLM:HIGH]
         }
     }
 }
 
-void func_80ACB344(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB344: the repeat or accept choice on the Desert Colossus owl's message, texts 0x607A and 0x607C
+void EnOwl_DesertColossusChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
@@ -644,44 +677,45 @@ void func_80ACB344(EnOwl* this, PlayState* play) {
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x607C);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
     }
 }
 
-void func_80ACB3E0(EnOwl* this, PlayState* play) {
+void EnOwl_WaitDesertColossus(EnOwl* this, PlayState* play) {
     EnOwl_LookAtLink(this, play);
 
     if (EnOwl_CheckInitTalk(this, play, 0x6079, 360.0f, 2)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB344;
+        this->actionFunc = EnOwl_DesertColossusChoice;  // was func_80ACB344 [LLM:HIGH]
     }
 }
 
-void func_80ACB440(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB440: the repeat or accept choice on the pre-Saria Lost Woods message, texts 0x10C1-0x10C3
+void EnOwl_LWPreSariaChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
                 Message_ContinueTextbox(play, 0x10C1);
-                this->actionFunc = func_80ACB4FC;
+                this->actionFunc = EnOwl_LWPreSariaAdvance;  // was func_80ACB4FC [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x10C3);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACB4FC(EnOwl* this, PlayState* play) {
+void EnOwl_LWPreSariaAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x10C2);
-        this->actionFunc = func_80ACB440;
+        this->actionFunc = EnOwl_LWPreSariaChoice;  // was func_80ACB440 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -690,34 +724,35 @@ void EnOwl_WaitLWPreSaria(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x10C0, 190.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB4FC;
+        this->actionFunc = EnOwl_LWPreSariaAdvance;  // was func_80ACB4FC [LLM:HIGH]
     }
 }
 
-void func_80ACB5C4(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB5C4: the repeat or accept choice on the post-Saria Lost Woods message, texts 0x10C5-0x10C7
+void EnOwl_LWPostSariaChoice(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_CHOICE && Message_ShouldAdvance(play)) {
         switch (play->msgCtx.choiceIndex) {
             case OWL_REPEAT:
                 Message_ContinueTextbox(play, 0x10C5);
-                this->actionFunc = func_80ACB680;
+                this->actionFunc = EnOwl_LWPostSariaAdvance;  // was func_80ACB680 [LLM:HIGH]
                 break;
             case OWL_OK:
                 Message_ContinueTextbox(play, 0x10C7);
-                this->actionFunc = func_80ACA7E0;
+                this->actionFunc = EnOwl_EndTalk;  // was func_80ACA7E0 [LLM:HIGH]
                 break;
         }
 
         this->actionFlags &= ~2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
-void func_80ACB680(EnOwl* this, PlayState* play) {
+void EnOwl_LWPostSariaAdvance(EnOwl* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_EVENT && Message_ShouldAdvance(play)) {
         Message_ContinueTextbox(play, 0x10C6);
-        this->actionFunc = func_80ACB5C4;
+        this->actionFunc = EnOwl_LWPostSariaChoice;  // was func_80ACB5C4 [LLM:HIGH]
         this->actionFlags |= 2;
-        func_80ACA71C(this);
+        EnOwl_StartHeadTurn(this);  // was func_80ACA71C [LLM:HIGH]
     }
 }
 
@@ -726,11 +761,12 @@ void EnOwl_WaitLWPostSaria(EnOwl* this, PlayState* play) {
 
     if (EnOwl_CheckInitTalk(this, play, 0x10C4, 360.0f, 0)) {
         Audio_PlayFanfare(NA_BGM_OWL);
-        this->actionFunc = func_80ACB680;
+        this->actionFunc = EnOwl_LWPostSariaAdvance;  // was func_80ACB680 [LLM:HIGH]
     }
 }
 
-void func_80ACB748(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB748: scales the wind, flutter and pass-by sounds by how fast the camera is moving during the travel cutscenes
+void EnOwl_PlayFlightSfx(EnOwl* this, PlayState* play) {
     static Vec3f D_80ACD62C = { 0.0f, 0.0f, 0.0f };
     f32 dist;
     f32 weight;
@@ -769,31 +805,33 @@ void func_80ACB748(EnOwl* this, PlayState* play) {
     }
 }
 
-void func_80ACB904(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB904: rides cue 7, re-seating the owl on each new action, and follows the straight-line path
+void EnOwl_CutsceneFlyStraight(EnOwl* this, PlayState* play) {
     if (play->csCtx.state != CS_STATE_IDLE && (play->csCtx.npcActions[7] != NULL)) {
         if (this->unk_40A != play->csCtx.npcActions[7]->action) {
-            func_80ACD130(this, play, 7);
-            func_80ACBAB8(this, play);
+            EnOwl_SetPosFromCue(this, play, 7);  // was func_80ACD130 [LLM:HIGH]
+            EnOwl_SetCutsceneMode(this, play);  // was func_80ACBAB8 [LLM:HIGH]
         }
-        func_80ACD2CC(this, play);
+        EnOwl_FollowCueStraight(this, play);  // was func_80ACD2CC [LLM:HIGH]
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
-void func_80ACB994(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACB994: the same cue handling with the curved path update instead
+void EnOwl_CutsceneFlyCurve(EnOwl* this, PlayState* play) {
     if (play->csCtx.state != CS_STATE_IDLE && (play->csCtx.npcActions[7] != NULL)) {
         if (this->unk_40A != play->csCtx.npcActions[7]->action) {
-            func_80ACD130(this, play, 7);
-            func_80ACBAB8(this, play);
+            EnOwl_SetPosFromCue(this, play, 7);  // was func_80ACD130 [LLM:HIGH]
+            EnOwl_SetCutsceneMode(this, play);  // was func_80ACBAB8 [LLM:HIGH]
         }
-        func_80ACD4D4(this, play);
+        EnOwl_FollowCueCurve(this, play);  // was func_80ACD4D4 [LLM:HIGH]
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
@@ -801,30 +839,30 @@ void EnOwl_WaitDefault(EnOwl* this, PlayState* play) {
     if (play->csCtx.state != CS_STATE_IDLE && (play->csCtx.npcActions[7] != NULL)) {
         if (this->unk_40A != play->csCtx.npcActions[7]->action) {
             this->actionFlags |= 4;
-            func_80ACD130(this, play, 7);
-            func_80ACBAB8(this, play);
+            EnOwl_SetPosFromCue(this, play, 7);  // was func_80ACD130 [LLM:HIGH]
+            EnOwl_SetCutsceneMode(this, play);  // was func_80ACBAB8 [LLM:HIGH]
         } else {
             this->actor.world.rot.z = play->csCtx.npcActions[7]->urot.y;
         }
     }
 
     if (this->actionFlags & 0x80) {
-        func_80ACB748(this, play);
+        EnOwl_PlayFlightSfx(this, play);  // was func_80ACB748 [LLM:HIGH]
     }
 }
 
-void func_80ACBAB8(EnOwl* this, PlayState* play) {
+void EnOwl_SetCutsceneMode(EnOwl* this, PlayState* play) {
     switch (play->csCtx.npcActions[7]->action - 1) {
         case 0:
-            EnOwl_ChangeMode(this, func_80ACB904, func_80ACC540, &this->skelAnime, &gOwlFlyAnim, 0.0f);
+            EnOwl_ChangeMode(this, EnOwl_CutsceneFlyStraight, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlFlyAnim, 0.0f);  // was func_80ACB904 [LLM:HIGH]
             break;
         case 1:
             this->actor.draw = EnOwl_Draw;
-            EnOwl_ChangeMode(this, EnOwl_WaitDefault, func_80ACC540, &this->skelAnime, &gOwlPerchAnim, 0.0f);
+            EnOwl_ChangeMode(this, EnOwl_WaitDefault, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlPerchAnim, 0.0f);  // was func_80ACC540 [LLM:HIGH]
             break;
         case 2:
             this->actor.draw = EnOwl_Draw;
-            EnOwl_ChangeMode(this, func_80ACB994, func_80ACC540, &this->skelAnime, &gOwlFlyAnim, 0.0f);
+            EnOwl_ChangeMode(this, EnOwl_CutsceneFlyCurve, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlFlyAnim, 0.0f);  // was func_80ACB994 [LLM:HIGH]
             break;
         case 3:
             this->actor.draw = NULL;
@@ -838,7 +876,8 @@ void func_80ACBAB8(EnOwl* this, PlayState* play) {
     this->unk_40A = play->csCtx.npcActions[7]->action;
 }
 
-void func_80ACBC0C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACBC0C: climbs and accelerates away on its chosen heading, dying once 6000 units out
+void EnOwl_FlyAway(EnOwl* this, PlayState* play) {
     this->actor.flags |= ACTOR_FLAG_DRAW_CULLING_DISABLED;
 
     if (this->actor.xzDistToPlayer > 6000.0f && !(this->actionFlags & 0x80)) {
@@ -863,7 +902,8 @@ void func_80ACBC0C(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACBD4C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACBD4C: drives the climb through the takeoff animation and then switches to the flight
+void EnOwl_Takeoff(EnOwl* this, PlayState* play) {
     if (this->skelAnime.curFrame > 10.0f) {
         Math_SmoothStepToS(&this->actor.world.rot.y, this->unk_400, 2, 0x400, 0x40);
         this->actor.shape.rot.y = this->actor.world.rot.y;
@@ -880,7 +920,7 @@ void func_80ACBD4C(EnOwl* this, PlayState* play) {
     }
 
     if (this->actionFlags & 1) {
-        EnOwl_ChangeMode(this, func_80ACBC0C, func_80ACC460, &this->skelAnime, &gOwlFlyAnim, 0.0f);
+        EnOwl_ChangeMode(this, EnOwl_FlyAway, EnOwl_AnimFlap, &this->skelAnime, &gOwlFlyAnim, 0.0f);  // was func_80ACBC0C [LLM:HIGH]
         this->unk_3FE = 6;
         if (this->actionFlags & 0x40) {
             this->unk_400 += 0x2000;
@@ -892,10 +932,10 @@ void func_80ACBD4C(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACBEA0(EnOwl* this, PlayState* PlayState) {
+void EnOwl_Perched(EnOwl* this, PlayState* PlayState) {
     if (this->actionFlags & 1) {
         this->unk_3FE = 3;
-        EnOwl_ChangeMode(this, func_80ACBD4C, func_80ACC540, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);
+        EnOwl_ChangeMode(this, EnOwl_Takeoff, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);  // was func_80ACBD4C [LLM:HIGH]
         this->unk_3F8 = this->actor.world.pos.y;
         this->actor.velocity.y = 2.0f;
         if (this->actionFlags & 0x40) {
@@ -908,12 +948,13 @@ void func_80ACBEA0(EnOwl* this, PlayState* PlayState) {
     this->actionFlags |= 8;
 }
 
-void func_80ACBF50(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACBF50: turns onto the stored heading and then switches to the flight
+void EnOwl_TurnThenFly(EnOwl* this, PlayState* play) {
     Math_SmoothStepToS(&this->actor.world.rot.y, this->unk_400, 2, 0x384, 0x258);
     this->actor.shape.rot.y = this->actor.world.rot.y;
 
     if (this->actionFlags & 1) {
-        EnOwl_ChangeMode(this, func_80ACBC0C, func_80ACC460, &this->skelAnime, &gOwlFlyAnim, 0.0f);
+        EnOwl_ChangeMode(this, EnOwl_FlyAway, EnOwl_AnimFlap, &this->skelAnime, &gOwlFlyAnim, 0.0f);  // was func_80ACBC0C [LLM:HIGH]
         this->unk_3FE = 6;
         this->actor.velocity.y = 2.0f;
         this->actor.gravity = 0.0f;
@@ -922,7 +963,8 @@ void func_80ACBF50(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACC00C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACC00C: circles until the player comes within 50 units, then starts that owl's travel cutscene
+void EnOwl_WaitForTravelCs(EnOwl* this, PlayState* play) {
     s32 owlType;
     s32 temp_v0;
     s32 temp_v0_2;
@@ -971,14 +1013,15 @@ void func_80ACC00C(EnOwl* this, PlayState* play) {
             this->skelAnime.curFrame = 21.0f;
             this->unk_3FE--;
         } else {
-            this->actionFunc = func_80ACBF50;
+            this->actionFunc = EnOwl_TurnThenFly;  // was func_80ACBF50 [LLM:HIGH]
         }
     }
 
     this->actionFlags |= 8;
 }
 
-void func_80ACC23C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACC23C: the takeoff that precedes a travel cutscene, halting the owl at frame 37 to wait for the player
+void EnOwl_TravelTakeoff(EnOwl* this, PlayState* play) {
     if (this->skelAnime.curFrame < 20.0f) {
         this->actor.speedXZ = 1.5f;
     } else {
@@ -989,7 +1032,7 @@ void func_80ACC23C(EnOwl* this, PlayState* play) {
 
     if (this->skelAnime.curFrame >= 37.0f) {
         this->skelAnime.curFrame = 21.0f;
-        this->actionFunc = func_80ACC00C;
+        this->actionFunc = EnOwl_WaitForTravelCs;  // was func_80ACC00C [LLM:HIGH]
         this->unk_3FE = 5;
         this->actor.velocity.y = 0.0f;
         this->actor.gravity = 0.0f;
@@ -999,10 +1042,10 @@ void func_80ACC23C(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACC30C(EnOwl* this, PlayState* play) {
+void EnOwl_PerchedBeforeTravel(EnOwl* this, PlayState* play) {
     if (this->actionFlags & 1) {
         this->unk_3FE = 3;
-        EnOwl_ChangeMode(this, func_80ACC23C, func_80ACC540, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);
+        EnOwl_ChangeMode(this, EnOwl_TravelTakeoff, EnOwl_AnimLoopOnce, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);  // was func_80ACC23C [LLM:HIGH]
         this->unk_3F8 = this->actor.world.pos.y;
         this->actor.velocity.y = 0.2f;
     }
@@ -1010,20 +1053,21 @@ void func_80ACC30C(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACC390(EnOwl* this) {
+// LLM generated name (HIGH), was func_80ACC390: animation step: rocks the body while the glide counter runs, then switches to the flap cycle
+void EnOwl_AnimGlide(EnOwl* this) {
     SkelAnime_Update(this->curSkelAnime);
 
     if (this->unk_3FE > 0) {
         this->unk_3FE--;
         this->actor.shape.rot.z = Math_SinS(this->unk_3FE * 0x333) * 1000.0f;
     } else {
-        this->unk_410 = func_80ACC460;
+        this->unk_410 = EnOwl_AnimFlap;  // was func_80ACC460 [LLM:HIGH]
         this->unk_3FE = 6;
         Animation_Change(this->curSkelAnime, &gOwlFlyAnim, 1.0f, 0.0f, Animation_GetLastFrame(&gOwlFlyAnim), 2, 5.0f);
     }
 }
 
-void func_80ACC460(EnOwl* this) {
+void EnOwl_AnimFlap(EnOwl* this) {
     if (SkelAnime_Update(this->curSkelAnime)) {
         if (this->unk_3FE > 0) {
             this->unk_3FE--;
@@ -1031,14 +1075,14 @@ void func_80ACC460(EnOwl* this) {
                              Animation_GetLastFrame(this->curSkelAnime->animation), ANIMMODE_ONCE, 0.0f);
         } else {
             this->unk_3FE = 0xA0;
-            this->unk_410 = func_80ACC390;
+            this->unk_410 = EnOwl_AnimGlide;  // was func_80ACC390 [LLM:HIGH]
             Animation_Change(this->curSkelAnime, &gOwlGlideAnim, 1.0f, 0.0f, Animation_GetLastFrame(&gOwlGlideAnim), 0,
                              5.0f);
         }
     }
 }
 
-void func_80ACC540(EnOwl* this) {
+void EnOwl_AnimLoopOnce(EnOwl* this) {
     if (SkelAnime_Update(this->curSkelAnime)) {
         Animation_Change(this->curSkelAnime, this->curSkelAnime->animation, 1.0f, 0.0f,
                          Animation_GetLastFrame(this->curSkelAnime->animation), ANIMMODE_ONCE, 0.0f);
@@ -1048,7 +1092,8 @@ void func_80ACC540(EnOwl* this) {
     }
 }
 
-s32 func_80ACC5CC(EnOwl* this) {
+// LLM generated name (HIGH), was func_80ACC5CC: steps the head angle four units a frame toward its target and reports arrival
+s32 EnOwl_StepHeadTurn(EnOwl* this) {
     s32 phi_v1 = (this->actionFlags & 2) ? 0x20 : 0;
 
     if (phi_v1 == (this->unk_3EE & 0x3F)) {
@@ -1064,7 +1109,8 @@ s32 func_80ACC5CC(EnOwl* this) {
     }
 }
 
-s32 func_80ACC624(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACC624: true everywhere except the Desert Colossus outside the two cutscene frame windows where this owl is hidden
+s32 EnOwl_ShouldDrawInColossus(EnOwl* this, PlayState* play) {
     s32 switchFlag = (this->actor.params & 0xFC0) >> 6;
 
     if (play->sceneNum != SCENE_DESERT_COLOSSUS) {
@@ -1096,7 +1142,7 @@ void EnOwl_Update(Actor* thisx, PlayState* play) {
         return;
     }
 
-    if (!(this->actionFlags & 0x80) && func_80ACC624(this, play)) {
+    if (!(this->actionFlags & 0x80) && EnOwl_ShouldDrawInColossus(this, play)) {  // was func_80ACC624 [LLM:HIGH]
         if ((this->skelAnime.animation == &gOwlTakeoffAnim &&
              (this->skelAnime.curFrame == 2.0f || this->skelAnime.curFrame == 9.0f ||
               this->skelAnime.curFrame == 23.0f || this->skelAnime.curFrame == 40.0f ||
@@ -1157,11 +1203,11 @@ void EnOwl_Update(Actor* thisx, PlayState* play) {
                     }
                     break;
                 case 2:
-                    if (func_80ACC5CC(this)) {
+                    if (EnOwl_StepHeadTurn(this)) {  // was func_80ACC5CC [LLM:HIGH]
                         this->actionFlags &= ~0x10;
                         this->unk_406 = (s32)Rand_ZeroFloat(20.0f) + 0x3C;
                         this->unk_404 = 0;
-                        func_80ACA6C0(this);
+                        EnOwl_PickHeadTurnDirection(this);  // was func_80ACA6C0 [LLM:HIGH]
                     }
                     break;
                 default:
@@ -1203,7 +1249,7 @@ void EnOwl_Update(Actor* thisx, PlayState* play) {
                         if (this->unk_405 <= 0) {
                             this->unk_406 = (s32)Rand_ZeroFloat(20.0f) + 0x3C;
                             this->unk_404 = 0;
-                            func_80ACA6C0(this);
+                            EnOwl_PickHeadTurnDirection(this);  // was func_80ACA6C0 [LLM:HIGH]
                         }
                         break;
                     case 4:
@@ -1211,7 +1257,7 @@ void EnOwl_Update(Actor* thisx, PlayState* play) {
                         if (this->unk_405 <= 0) {
                             this->unk_406 = (s32)Rand_ZeroFloat(20.0f) + 0x3C;
                             this->unk_404 = 0;
-                            func_80ACA6C0(this);
+                            EnOwl_PickHeadTurnDirection(this);  // was func_80ACA6C0 [LLM:HIGH]
                         }
                         break;
                     default:
@@ -1336,7 +1382,7 @@ void EnOwl_ChangeMode(EnOwl* this, EnOwlActionFunc actionFunc, OwlFunc arg2, Ske
     this->unk_410 = arg2;
 }
 
-void func_80ACD130(EnOwl* this, PlayState* play, s32 idx) {
+void EnOwl_SetPosFromCue(EnOwl* this, PlayState* play, s32 idx) {
     Vec3f startPos;
 
     startPos.x = play->csCtx.npcActions[idx]->startPos.x;
@@ -1347,7 +1393,8 @@ void func_80ACD130(EnOwl* this, PlayState* play, s32 idx) {
     this->actor.shape.rot.z = play->csCtx.npcActions[idx]->urot.z;
 }
 
-f32 func_80ACD1C4(PlayState* play, s32 idx) {
+// LLM generated name (HIGH), was func_80ACD1C4: returns the cue's progress from 0 to 1
+f32 EnOwl_GetCueWeight(PlayState* play, s32 idx) {
     f32 ret = Environment_LerpWeight(play->csCtx.npcActions[idx]->endFrame, play->csCtx.npcActions[idx]->startFrame,
                                      play->csCtx.frames);
 
@@ -1355,7 +1402,8 @@ f32 func_80ACD1C4(PlayState* play, s32 idx) {
     return ret;
 }
 
-void func_80ACD220(EnOwl* this, Vec3f* arg1, f32 arg2) {
+// LLM generated name (HIGH), was func_80ACD220: sets the speed and heading to close on a point by the caller's weight
+void EnOwl_FlyToward(EnOwl* this, Vec3f* arg1, f32 arg2) {
     Vec3f rpy;
 
     rpy.x = (arg1->x - this->actor.world.pos.x) * arg2;
@@ -1368,10 +1416,10 @@ void func_80ACD220(EnOwl* this, Vec3f* arg1, f32 arg2) {
     this->actor.shape.rot.y = this->actor.world.rot.y;
 }
 
-void func_80ACD2CC(EnOwl* this, PlayState* play) {
+void EnOwl_FollowCueStraight(EnOwl* this, PlayState* play) {
     Vec3f pos;
     s32 angle;
-    f32 t = func_80ACD1C4(play, 7);
+    f32 t = EnOwl_GetCueWeight(play, 7);  // was func_80ACD1C4 [LLM:HIGH]
 
     pos.x = play->csCtx.npcActions[7]->startPos.x;
     pos.y = play->csCtx.npcActions[7]->startPos.y;
@@ -1399,14 +1447,14 @@ void func_80ACD2CC(EnOwl* this, PlayState* play) {
     } else {
         pos.x -= Math_SinS(angle) * this->unk_3F8;
         pos.z += Math_CosS(angle) * this->unk_3F8;
-        func_80ACD220(this, &pos, 1.0f);
+        EnOwl_FlyToward(this, &pos, 1.0f);  // was func_80ACD220 [LLM:HIGH]
     }
 }
 
-void func_80ACD4D4(EnOwl* this, PlayState* play) {
+void EnOwl_FollowCueCurve(EnOwl* this, PlayState* play) {
     Vec3f pos;
     Vec3f endPosf;
-    f32 temp_ret = func_80ACD1C4(play, 7);
+    f32 temp_ret = EnOwl_GetCueWeight(play, 7);  // was func_80ACD1C4 [LLM:HIGH]
 
     pos.x = play->csCtx.npcActions[7]->startPos.x;
     pos.y = play->csCtx.npcActions[7]->startPos.y;
@@ -1417,5 +1465,5 @@ void func_80ACD4D4(EnOwl* this, PlayState* play) {
     pos.x = (endPosf.x - pos.x) * temp_ret + pos.x;
     pos.y = (endPosf.y - pos.y) * temp_ret + pos.y;
     pos.z = (endPosf.z - pos.z) * temp_ret + pos.z;
-    func_80ACD220(this, &pos, 1.0f);
+    EnOwl_FlyToward(this, &pos, 1.0f);  // was func_80ACD220 [LLM:HIGH]
 }
diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.h b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.h
index a898dce2a..0291667b8 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.h
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.h
@@ -43,6 +43,6 @@ typedef struct EnOwl {
     /* 0x0410 */ OwlFunc unk_410;
 } EnOwl; // size = 0x0414
 
-void func_80ACA62C(EnOwl* enOwl, PlayState* play);
+void EnOwl_SetSwitchAndUnfold(EnOwl* enOwl, PlayState* play);  // was func_80ACA62C [LLM:HIGH]
 
 #endif
-- 
2.55.0

