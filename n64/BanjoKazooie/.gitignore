# The upstream checkout and everything build/run produce are disposable;
# only the scripts and patches/ are versioned.  runDir/ also holds
# ROM-derived and personal state (bk.o2r, saves) that must never be
# committed.
/Lighthouse/
/build-cmake/
/bldInstall/
/runDir/

# Podman/AppImage build outputs (Makefile) — large, never commit
/out/
lighthouse-builder-*.tar
