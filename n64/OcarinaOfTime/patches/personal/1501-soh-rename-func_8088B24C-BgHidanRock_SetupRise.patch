From 8fa5edce9a3768c71da48414e89be0a12a75d534 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:23:57 +0000
Subject: [PATCH 1501/3799] soh: rename func_8088B24C -> BgHidanRock_SetupRise

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: disables culling and installs the rising action.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c  | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c
index 6b9358f1e..155bab2b2 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Rock/z_bg_hidan_rock.c
@@ -16,7 +16,8 @@ void BgHidanRock_Update(Actor* thisx, PlayState* play);
 void BgHidanRock_Draw(Actor* thisx, PlayState* play);
 void BgHidanRock_Reset(void);
 
-void func_8088B24C(BgHidanRock* this);
+// LLM generated name (HIGH), was func_8088B24C: disables culling and installs the rising action.
+void BgHidanRock_SetupRise(BgHidanRock* this);  // was func_8088B24C [LLM:HIGH]
 
 void func_8088B268(BgHidanRock* this, PlayState* play);
 void func_8088B5F4(BgHidanRock* this, PlayState* play);
@@ -117,7 +118,7 @@ void BgHidanRock_Destroy(Actor* thisx, PlayState* play) {
     Audio_StopSfxByPos(&this->unk_170);
 }
 
-void func_8088B24C(BgHidanRock* this) {
+void BgHidanRock_SetupRise(BgHidanRock* this) {
     this->dyna.actor.flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED | ACTOR_FLAG_DRAW_CULLING_DISABLED;
     this->actionFunc = func_8088B990;
 }
@@ -215,7 +216,7 @@ void func_8088B5F4(BgHidanRock* this, PlayState* play) {
     }
 
     if (this->timer == 0) {
-        func_8088B24C(this);
+        BgHidanRock_SetupRise(this);  // was func_8088B24C [LLM:HIGH]
     }
 }
 
@@ -240,7 +241,7 @@ void func_8088B69C(BgHidanRock* this, PlayState* play) {
     } else {
         this->dyna.actor.world.pos.x = this->dyna.actor.home.pos.x;
         this->dyna.actor.world.pos.z = this->dyna.actor.home.pos.z;
-        func_8088B24C(this);
+        BgHidanRock_SetupRise(this);  // was func_8088B24C [LLM:HIGH]
     }
 
     if (!(this->timer % 4)) {
-- 
2.55.0

