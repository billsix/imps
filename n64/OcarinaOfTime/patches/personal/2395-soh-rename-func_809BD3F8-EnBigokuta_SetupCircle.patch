From eea8c0efd75f554bd3ca84761ba60c0b700feae8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:25 +0000
Subject: [PATCH 2395/3799] soh: rename func_809BD3F8 -> EnBigokuta_SetupCircle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: morphs to the swimming loop, arms the 350-frame circling window and turns the colliders on
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 4c40b602e..e72c5c7e8 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -312,7 +312,8 @@ void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this) {
     this->actionFunc = func_809BDB90;
 }
 
-void func_809BD3F8(EnBigokuta* this) {
+// LLM generated name (HIGH), was func_809BD3F8: morphs to the swimming loop, arms the 350-frame circling window and turns the colliders on
+void EnBigokuta_SetupCircle(EnBigokuta* this) {
     Animation_MorphToLoop(&this->skelAnime, &object_bigokuta_Anim_001CA4, -5.0f);
     this->unk_196 = 350;
     this->unk_198 = 80;
@@ -483,7 +484,7 @@ void func_809BDB90(EnBigokuta* this, PlayState* play) {
         if (this->actor.params == 3) {
             func_800F5ACC(NA_BGM_MINI_BOSS);
         }
-        func_809BD3F8(this);
+        EnBigokuta_SetupCircle(this);  // was func_809BD3F8 [LLM:HIGH]
     }
 }
 
@@ -565,7 +566,7 @@ void func_809BDF34(EnBigokuta* this, PlayState* play) {
     this->actor.world.pos.y = (sinf(this->unk_196 * (M_PI / 16)) * 100.0f) + this->actor.home.pos.y;
     if (this->unk_196 == 0) {
         EnBigokuta_SpawnLandingSplash(this, play);  // was func_809BD1C8 [LLM:HIGH]
-        func_809BD3F8(this);
+        EnBigokuta_SetupCircle(this);  // was func_809BD3F8 [LLM:HIGH]
     }
 }
 
@@ -579,7 +580,7 @@ void func_809BDFC8(EnBigokuta* this, PlayState* play) {
     }
     if ((this->unk_196 == 0) && Math_ScaledStepToS(&this->actor.shape.rot.y, this->actor.world.rot.x, 0x800)) {
         this->unk_194 = -this->unk_194;
-        func_809BD3F8(this);
+        EnBigokuta_SetupCircle(this);  // was func_809BD3F8 [LLM:HIGH]
     }
 }
 
@@ -679,7 +680,7 @@ void func_809BE3E4(EnBigokuta* this, PlayState* play) {
             this->unk_194 = -1;
         }
 
-        func_809BD3F8(this);
+        EnBigokuta_SetupCircle(this);  // was func_809BD3F8 [LLM:HIGH]
         if (!this->unk_195) {
             this->unk_196 = -40;
         }
@@ -699,7 +700,7 @@ void func_809BE4A4(EnBigokuta* this, PlayState* play) {
 void func_809BE518(EnBigokuta* this, PlayState* play) {
     if (Math_StepToF(&this->actor.world.pos.y, this->actor.home.pos.y, 10.0f)) {
         this->actor.flags |= ACTOR_FLAG_ATTENTION_ENABLED;
-        func_809BD3F8(this);
+        EnBigokuta_SetupCircle(this);  // was func_809BD3F8 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

