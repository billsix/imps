From 1e5ad3da5ad8811f15557406ae8390fe8dfc3554 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:35 +0000
Subject: [PATCH 2770/3799] soh: rename func_80034CC4 ->
 Actor_DrawFlexSkeletonXluWithEnvAlpha

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the translucent twin, which also binds the translucent render mode list
Defined in soh/src/code/z_actor.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                     | 4 ++--
 soh/src/code/z_actor.c                      | 3 ++-
 soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c | 2 +-
 soh/src/overlays/actors/ovl_En_Md/z_en_md.c | 2 +-
 soh/src/overlays/actors/ovl_En_Sa/z_en_sa.c | 2 +-
 soh/src/overlays/actors/ovl_En_Zo/z_en_zo.c | 2 +-
 6 files changed, 8 insertions(+), 7 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 52cbc27a8..65c02e0b4 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -537,8 +537,8 @@ s16 Npc_GetTrackingPresetMaxPlayerYaw(s16 presetIndex);
 void Npc_TrackPoint(Actor* actor, NpcInteractInfo* interactInfo, s16 presetIndex, s16 trackingMode);
 void Actor_DrawFlexSkeletonOpaWithEnvAlpha(PlayState* play, SkelAnime* skelAnime, OverrideLimbDraw overrideLimbDraw,
                    PostLimbDraw postLimbDraw, Actor* actor, s16 alpha);  // was func_80034BA0 [LLM:HIGH]
-void func_80034CC4(PlayState* play, SkelAnime* skelAnime, OverrideLimbDraw overrideLimbDraw,
-                   PostLimbDraw postLimbDraw, Actor* actor, s16 alpha);
+void Actor_DrawFlexSkeletonXluWithEnvAlpha(PlayState* play, SkelAnime* skelAnime, OverrideLimbDraw overrideLimbDraw,
+                   PostLimbDraw postLimbDraw, Actor* actor, s16 alpha);  // was func_80034CC4 [LLM:HIGH]
 s16 Actor_UpdateAlphaByDistance(Actor* actor, PlayState* play, s16 arg2, f32 arg3);
 void Animation_ChangeByInfo(SkelAnime* skelAnime, AnimationInfo* animationInfo, s32 index);
 void Actor_UpdateFidgetTables(PlayState* play, s16* arg1, s16* arg2, s32 arg3);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 4c2962108..4b10fa1b9 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4634,7 +4634,8 @@ void Actor_DrawFlexSkeletonOpaWithEnvAlpha(PlayState* play, SkelAnime* skelAnime
     CLOSE_DISPS(play->state.gfxCtx);
 }
 
-void func_80034CC4(PlayState* play, SkelAnime* skelAnime, OverrideLimbDraw overrideLimbDraw, PostLimbDraw postLimbDraw,
+// LLM generated name (HIGH), was func_80034CC4: the translucent twin, which also binds the translucent render mode list
+void Actor_DrawFlexSkeletonXluWithEnvAlpha(PlayState* play, SkelAnime* skelAnime, OverrideLimbDraw overrideLimbDraw, PostLimbDraw postLimbDraw,
                    Actor* actor, s16 alpha) {
     OPEN_DISPS(play->state.gfxCtx);
 
diff --git a/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c b/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
index ac68b5829..b726979af 100644
--- a/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
+++ b/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
@@ -1409,7 +1409,7 @@ void EnKo_Draw(Actor* thisx, PlayState* play) {
                    EnKo_SetEnvColor(play->state.gfxCtx, tunicColor.r, tunicColor.g, tunicColor.b, tunicColor.a));
         gSPSegment(POLY_XLU_DISP++, 0x09,
                    EnKo_SetEnvColor(play->state.gfxCtx, bootsColor.r, bootsColor.g, bootsColor.b, bootsColor.a));
-        func_80034CC4(play, &this->skelAnime, EnKo_OverrideLimbDraw, EnKo_PostLimbDraw, &this->actor, this->modelAlpha);
+        Actor_DrawFlexSkeletonXluWithEnvAlpha(play, &this->skelAnime, EnKo_OverrideLimbDraw, EnKo_PostLimbDraw, &this->actor, this->modelAlpha);  // was func_80034CC4 [LLM:HIGH]
     }
     CLOSE_DISPS(play->state.gfxCtx);
 }
diff --git a/soh/src/overlays/actors/ovl_En_Md/z_en_md.c b/soh/src/overlays/actors/ovl_En_Md/z_en_md.c
index 8c8d52f11..e6adbf6f6 100644
--- a/soh/src/overlays/actors/ovl_En_Md/z_en_md.c
+++ b/soh/src/overlays/actors/ovl_En_Md/z_en_md.c
@@ -886,7 +886,7 @@ void EnMd_Draw(Actor* thisx, PlayState* play) {
         Actor_DrawFlexSkeletonOpaWithEnvAlpha(play, &this->skelAnime, EnMd_OverrideLimbDraw, EnMd_PostLimbDraw, &this->actor, this->alpha);  // was func_80034BA0 [LLM:HIGH]
     } else if (this->alpha != 0) {
         gSPSegment(POLY_XLU_DISP++, 0x08, SEGMENTED_TO_VIRTUAL(sEyeTextures[this->eyeIdx]));
-        func_80034CC4(play, &this->skelAnime, EnMd_OverrideLimbDraw, EnMd_PostLimbDraw, &this->actor, this->alpha);
+        Actor_DrawFlexSkeletonXluWithEnvAlpha(play, &this->skelAnime, EnMd_OverrideLimbDraw, EnMd_PostLimbDraw, &this->actor, this->alpha);  // was func_80034CC4 [LLM:HIGH]
     }
 
     CLOSE_DISPS(play->state.gfxCtx);
diff --git a/soh/src/overlays/actors/ovl_En_Sa/z_en_sa.c b/soh/src/overlays/actors/ovl_En_Sa/z_en_sa.c
index f880424f8..18218d8f1 100644
--- a/soh/src/overlays/actors/ovl_En_Sa/z_en_sa.c
+++ b/soh/src/overlays/actors/ovl_En_Sa/z_en_sa.c
@@ -847,7 +847,7 @@ void EnSa_Draw(Actor* thisx, PlayState* play) {
         gSPSegment(POLY_XLU_DISP++, 0x08, SEGMENTED_TO_VIRTUAL(eyeTextures[this->rightEyeIndex]));
         gSPSegment(POLY_XLU_DISP++, 0x09, SEGMENTED_TO_VIRTUAL(eyeTextures[this->leftEyeIndex]));
         gSPSegment(POLY_XLU_DISP++, 0x0A, SEGMENTED_TO_VIRTUAL(mouthTextures[this->mouthIndex]));
-        func_80034CC4(play, &this->skelAnime, EnSa_OverrideLimbDraw, EnSa_PostLimbDraw, &this->actor, this->alpha);
+        Actor_DrawFlexSkeletonXluWithEnvAlpha(play, &this->skelAnime, EnSa_OverrideLimbDraw, EnSa_PostLimbDraw, &this->actor, this->alpha);  // was func_80034CC4 [LLM:HIGH]
     }
 
     CLOSE_DISPS(play->state.gfxCtx);
diff --git a/soh/src/overlays/actors/ovl_En_Zo/z_en_zo.c b/soh/src/overlays/actors/ovl_En_Zo/z_en_zo.c
index 6e1a4d669..d145bda81 100644
--- a/soh/src/overlays/actors/ovl_En_Zo/z_en_zo.c
+++ b/soh/src/overlays/actors/ovl_En_Zo/z_en_zo.c
@@ -810,7 +810,7 @@ void EnZo_Draw(Actor* thisx, PlayState* play) {
             Actor_DrawFlexSkeletonOpaWithEnvAlpha(play, &this->skelAnime, EnZo_OverrideLimbDraw, EnZo_PostLimbDraw, thisx, this->alpha);  // was func_80034BA0 [LLM:HIGH]
         } else {
             gSPSegment(POLY_XLU_DISP++, 0x08, SEGMENTED_TO_VIRTUAL(eyeTextures[this->eyeTexture]));
-            func_80034CC4(play, &this->skelAnime, EnZo_OverrideLimbDraw, EnZo_PostLimbDraw, thisx, this->alpha);
+            Actor_DrawFlexSkeletonXluWithEnvAlpha(play, &this->skelAnime, EnZo_OverrideLimbDraw, EnZo_PostLimbDraw, thisx, this->alpha);  // was func_80034CC4 [LLM:HIGH]
         }
 
         CLOSE_DISPS(play->state.gfxCtx);
-- 
2.55.0

