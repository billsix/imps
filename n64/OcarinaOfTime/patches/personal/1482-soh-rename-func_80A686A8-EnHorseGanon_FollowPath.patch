From 69fea9490653a58f8c4ac1a86331fcf0fac74708 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:22:45 +0000
Subject: [PATCH 1482/3799] soh: rename func_80A686A8 ->
 EnHorseGanon_FollowPath

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: advances to the next waypoint once within 400 units, turns toward it at a capped rate, and holds that waypoint's speed except when the player is close, where it sprints instead.
Defined in soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c    | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
index 12ad2a4eb..a5912f70a 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Ganon/z_en_horse_ganon.c
@@ -117,7 +117,8 @@ void EnHorseGanon_GetPathPoint(unk_D_80A69248* data, s32 index, Vec3f* vec) {
     vec->z = data[index].unk_0.z;
 }
 
-void func_80A686A8(EnHorseGanon* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A686A8: advances to the next waypoint once within 400 units, turns toward it at a capped rate, and holds that waypoint's speed except when the player is close, where it sprints instead.
+void EnHorseGanon_FollowPath(EnHorseGanon* this, PlayState* play) {
     Vec3f* tempPos;
     Vec3f vec;
     s16 y;
@@ -265,7 +266,7 @@ void func_80A68DB0(EnHorseGanon* this, PlayState* play) {
         func_80A68870(this);
     }
 
-    func_80A686A8(this, play);
+    EnHorseGanon_FollowPath(this, play);  // was func_80A686A8 [LLM:HIGH]
 
     if (SkelAnime_Update(&this->skin.skelAnime) != 0) {
         func_80A68B20(this);
-- 
2.55.0

