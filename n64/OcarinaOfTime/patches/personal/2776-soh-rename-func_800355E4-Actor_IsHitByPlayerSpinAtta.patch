From 9f07eb8381e316890760cb0ae525dd9487601865 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:36 +0000
Subject: [PATCH 2776/3799] soh: rename func_800355E4 ->
 Actor_IsHitByPlayerSpinAttack

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: true for a player-type AC collider while meleeWeaponAnimation is 0x16; that animation number is not otherwise named
Defined in soh/src/code/z_actor.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                     | 2 +-
 soh/src/code/z_actor.c                      | 3 ++-
 soh/src/overlays/actors/ovl_En_Bw/z_en_bw.c | 4 ++--
 3 files changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index e0d06ed0d..28d815563 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -549,7 +549,7 @@ Actor* Actor_FindNearby(PlayState* play, Actor* refActor, s16 actorId, u8 actorC
 s32 Actor_IsPlayerSwingingToward(PlayState* play, Actor* actor, f32 range, s16 arg3, s16 arg4, s16 arg5);  // was func_800354B4 [LLM:GUESS]
 void Actor_SpawnWoodDust(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);  // was func_8003555C [LLM:GUESS]
 void Actor_SpawnWoodDustAtPos(PlayState* play, Vec3f* pos);  // was func_800355B8 [LLM:GUESS]
-u8 func_800355E4(PlayState* play, Collider* collider);
+u8 Actor_IsHitByPlayerSpinAttack(PlayState* play, Collider* collider);  // was func_800355E4 [LLM:GUESS]
 u8 Actor_ApplyDamage(Actor* actor);
 void Actor_SetDropFlag(Actor* actor, ColliderInfo* colBody, s32 freezeFlag);
 void Actor_SetDropFlagJntSph(Actor* actor, ColliderJntSph* colBody, s32 freezeFlag);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 52a50056b..419321b3d 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4810,7 +4810,8 @@ void Actor_SpawnWoodDustAtPos(PlayState* play, Vec3f* pos) {
     Actor_SpawnWoodDust(play, pos, &D_80116268, &D_80116274);  // was func_8003555C [LLM:GUESS]
 }
 
-u8 func_800355E4(PlayState* play, Collider* collider) {
+// LLM generated name (GUESS), was func_800355E4: true for a player-type AC collider while meleeWeaponAnimation is 0x16; that animation number is not otherwise named
+u8 Actor_IsHitByPlayerSpinAttack(PlayState* play, Collider* collider) {
     Player* player = GET_PLAYER(play);
 
     if ((collider->acFlags & AC_TYPE_PLAYER) && (player->meleeWeaponState != 0) &&
diff --git a/soh/src/overlays/actors/ovl_En_Bw/z_en_bw.c b/soh/src/overlays/actors/ovl_En_Bw/z_en_bw.c
index 20713f3e1..7f6ef657e 100644
--- a/soh/src/overlays/actors/ovl_En_Bw/z_en_bw.c
+++ b/soh/src/overlays/actors/ovl_En_Bw/z_en_bw.c
@@ -687,7 +687,7 @@ void EnBw_Stunned(EnBw* this, PlayState* play) {
                 EnBw_SetupFlipBack(this);  // was func_809CFF10 [LLM:HIGH]
             }
         } else {
-            if (func_800355E4(play, &this->collider2.base)) {
+            if (Actor_IsHitByPlayerSpinAttack(play, &this->collider2.base)) {  // was func_800355E4 [LLM:GUESS]
                 this->unk_230 = 0;
                 this->actor.scale.y -= 0.009f;
                 Actor_SpawnFloorDustRing(play, &this->actor, &this->actor.world.pos, 30.0f, 11, 4.0f, 0, 0, false);
@@ -730,7 +730,7 @@ void EnBw_CheckDamage(EnBw* this, PlayState* play) {
             if (((this->unk_221 == 1) || (this->unk_221 == 4)) && (this->actor.colChkInfo.health == 0)) {
                 if (this->unk_220 != 0) {
                     Actor_SetColorFilter(&this->actor, 0x4000, 0xFF, 0, 8);
-                    if (func_800355E4(play, &this->collider2.base)) {
+                    if (Actor_IsHitByPlayerSpinAttack(play, &this->collider2.base)) {  // was func_800355E4 [LLM:GUESS]
                         this->unk_230 = 0;
                         this->actor.scale.y -= 0.009f;
                         Actor_SpawnFloorDustRing(play, &this->actor, &this->actor.world.pos, 30.0f, 11, 4.0f, 0, 0,
-- 
2.55.0

