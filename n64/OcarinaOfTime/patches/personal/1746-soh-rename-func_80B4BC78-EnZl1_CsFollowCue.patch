From 3fadc72f8f370d62880894f401fc6af62c3f4084 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:08 +0000
Subject: [PATCH 1746/3799] soh: rename func_80B4BC78 -> EnZl1_CsFollowCue

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the cutscene action that keeps playing the cue's animation and following its path.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index 3912bdeac..80dbf4335 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -26,7 +26,8 @@ void EnZl1_CourtyardCutscene(EnZl1* this, PlayState* play);  // was func_80B4B24
 void EnZl1_CsPlayCueAnimation(EnZl1* this, PlayState* play);  // was func_80B4B8B4 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B4BBC4: starts her looping surprised animation, halts the actors, plays Link's surprise voice and opens the first line of the window conversation.
 void EnZl1_StartPeepholeTalk(EnZl1* this, PlayState* play);  // was func_80B4BBC4 [LLM:HIGH]
-void func_80B4BC78(EnZl1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B4BC78: the cutscene action that keeps playing the cue's animation and following its path.
+void EnZl1_CsFollowCue(EnZl1* this, PlayState* play);  // was func_80B4BC78 [LLM:HIGH]
 void func_80B4BF2C(EnZl1* this, PlayState* play);
 
 // Name from zeldaret/oot (was D_80B4C5D0): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c]
@@ -103,7 +104,7 @@ void EnZl1_Init(Actor* thisx, PlayState* play) {
         frameCount = Animation_GetLastFrame(&gChildZelda1Anim_00438);
         Animation_Change(&this->skelAnime, &gChildZelda1Anim_00438, 1.0f, 0.0f, frameCount, ANIMMODE_LOOP, 0.0f);
         this->unk_1E6 = 0;
-        this->actionFunc = func_80B4BC78;
+        this->actionFunc = EnZl1_CsFollowCue;  // was func_80B4BC78 [LLM:HIGH]
     } else if (Flags_GetEventChkInf(EVENTCHKINF_USED_DEKU_TREE_BLUE_WARP) &&
                Flags_GetEventChkInf(EVENTCHKINF_USED_DODONGOS_CAVERN_BLUE_WARP) &&
                Flags_GetEventChkInf(EVENTCHKINF_USED_JABU_JABUS_BELLY_BLUE_WARP)) {
@@ -447,7 +448,7 @@ void EnZl1_StartPeepholeTalk(EnZl1* this, PlayState* play) {
     this->actionFunc = func_80B4BF2C;
 }
 
-void func_80B4BC78(EnZl1* this, PlayState* play) {
+void EnZl1_CsFollowCue(EnZl1* this, PlayState* play) {
     AnimationHeader* sp90[] = {
         NULL,
         NULL,
@@ -586,7 +587,7 @@ void EnZl1_Update(Actor* thisx, PlayState* play) {
     s32 pad;
     EnZl1* this = (EnZl1*)thisx;
 
-    if ((this->actionFunc != EnZl1_CsPlayCueAnimation) && (this->actionFunc != func_80B4BC78)) {  // was func_80B4B8B4 [LLM:HIGH]
+    if ((this->actionFunc != EnZl1_CsPlayCueAnimation) && (this->actionFunc != EnZl1_CsFollowCue)) {  // was func_80B4B8B4 [LLM:HIGH]
         SkelAnime_Update(&this->skelAnime);
     }
     Actor_UpdateBgCheckInfo(play, &this->actor, 0.0f, 0.0f, 0.0f, 5);
-- 
2.55.0

