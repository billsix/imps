From fd0e1aa45a561a415173b39af5a6b83416d49752 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:08:48 +0000
Subject: [PATCH 3226/3799] soh: rename D_8082AD4C -> sStickXDir

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the horizontal direction that repeat is currently firing in
Defined in soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../ovl_kaleido_scope/z_kaleido_scope_PAL.c   | 20 +++++++++----------
 1 file changed, 10 insertions(+), 10 deletions(-)

diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
index ca6166c8a..5daececb2 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
@@ -1458,7 +1458,7 @@ void KaleidoScope_DrawPages(PlayState* play, GraphicsContext* gfxCtx) {
     static s16 sCursorColorPhase = 0;  // was D_8082AD40 [LLM:HIGH]
     static s16 sStickXRepeatTimer = 0;  // was D_8082AD44 [LLM:HIGH]
     static s16 sStickYRepeatTimer = 0;  // was D_8082AD48 [LLM:HIGH]
-    static s16 D_8082AD4C = 0;
+    static s16 sStickXDir = 0;  // was D_8082AD4C [LLM:HIGH]
     static s16 D_8082AD50 = 0;
     PauseContext* pauseCtx = &play->pauseCtx;
     Input* input = &play->state.input[0];
@@ -1506,23 +1506,23 @@ void KaleidoScope_DrawPages(PlayState* play, GraphicsContext* gfxCtx) {
                 if (CHECK_BTN_ALL(input->cur.button, BTN_DLEFT)) {
                     if (CHECK_BTN_ALL(input->press.button, BTN_DLEFT)) {
                         sStickXRepeatTimer = XREG(8);  // was D_8082AD44 [LLM:HIGH]
-                        D_8082AD4C = -1;
+                        sStickXDir = -1;  // was D_8082AD4C [LLM:HIGH]
                     } else if (--sStickXRepeatTimer < 0) {  // was D_8082AD44 [LLM:HIGH]
                         sStickXRepeatTimer = XREG(6);  // was D_8082AD44 [LLM:HIGH]
                         input->press.button |= BTN_DLEFT;
                     }
                 } else if (CHECK_BTN_ALL(input->rel.button, BTN_DLEFT)) {
-                    D_8082AD4C = 0;
+                    sStickXDir = 0;  // was D_8082AD4C [LLM:HIGH]
                 } else if (CHECK_BTN_ALL(input->cur.button, BTN_DRIGHT)) {
                     if (CHECK_BTN_ALL(input->press.button, BTN_DRIGHT)) {
                         sStickXRepeatTimer = XREG(8);  // was D_8082AD44 [LLM:HIGH]
-                        D_8082AD4C = 1;
+                        sStickXDir = 1;  // was D_8082AD4C [LLM:HIGH]
                     } else if (--sStickXRepeatTimer < 0) {  // was D_8082AD44 [LLM:HIGH]
                         sStickXRepeatTimer = XREG(6);  // was D_8082AD44 [LLM:HIGH]
                         input->press.button |= BTN_DRIGHT;
                     }
                 } else if (CHECK_BTN_ALL(input->rel.button, BTN_DRIGHT)) {
-                    D_8082AD4C = 0;
+                    sStickXDir = 0;  // was D_8082AD4C [LLM:HIGH]
                 }
 
                 if (CHECK_BTN_ALL(input->cur.button, BTN_DDOWN)) {
@@ -1549,7 +1549,7 @@ void KaleidoScope_DrawPages(PlayState* play, GraphicsContext* gfxCtx) {
             }
 
             if (pauseCtx->stickRelX < -30) {
-                if (D_8082AD4C == -1) {
+                if (sStickXDir == -1) {  // was D_8082AD4C [LLM:HIGH]
                     if (--sStickXRepeatTimer < 0) {  // was D_8082AD44 [LLM:HIGH]
                         sStickXRepeatTimer = XREG(6);  // was D_8082AD44 [LLM:HIGH]
                     } else {
@@ -1557,10 +1557,10 @@ void KaleidoScope_DrawPages(PlayState* play, GraphicsContext* gfxCtx) {
                     }
                 } else {
                     sStickXRepeatTimer = XREG(8);  // was D_8082AD44 [LLM:HIGH]
-                    D_8082AD4C = -1;
+                    sStickXDir = -1;  // was D_8082AD4C [LLM:HIGH]
                 }
             } else if (pauseCtx->stickRelX > 30) {
-                if (D_8082AD4C == 1) {
+                if (sStickXDir == 1) {  // was D_8082AD4C [LLM:HIGH]
                     if (--sStickXRepeatTimer < 0) {  // was D_8082AD44 [LLM:HIGH]
                         sStickXRepeatTimer = XREG(6);  // was D_8082AD44 [LLM:HIGH]
                     } else {
@@ -1568,10 +1568,10 @@ void KaleidoScope_DrawPages(PlayState* play, GraphicsContext* gfxCtx) {
                     }
                 } else {
                     sStickXRepeatTimer = XREG(8);  // was D_8082AD44 [LLM:HIGH]
-                    D_8082AD4C = 1;
+                    sStickXDir = 1;  // was D_8082AD4C [LLM:HIGH]
                 }
             } else {
-                D_8082AD4C = 0;
+                sStickXDir = 0;  // was D_8082AD4C [LLM:HIGH]
             }
 
             if (pauseCtx->stickRelY < -30) {
-- 
2.55.0

