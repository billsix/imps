From 0f611d442322f42c5a7f9ec439e30f1c55f759a9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:39:39 +0000
Subject: [PATCH 1062/3799] soh: rename func_808C1554_Raw ->
 BossDodongo_UpdateLavaTextureRaw

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the in-tree comment above it reads "Same as func_808C1554 but works with u32 values for RGBA32 raw textures"; renamed alongside its sibling so the pair keeps matching names.
Defined in soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
index 656d14087..10b020716 100644
--- a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
@@ -193,7 +193,8 @@ void func_808C12C4(u8* arg1, s16 arg2) {
 }
 
 // Same as BossDodongo_UpdateLavaTexture but works with u32 values for RGBA32 raw textures, was func_808C1554 [LLM:HIGH]
-void func_808C1554_Raw(void* arg0, void* floorTex, s32 arg2, f32 arg3) {
+// LLM generated name (HIGH), was func_808C1554_Raw: the in-tree comment above it reads "Same as func_808C1554 but works with u32 values for RGBA32 raw textures"; renamed alongside its sibling so the pair keeps matching names.
+void BossDodongo_UpdateLavaTextureRaw(void* arg0, void* floorTex, s32 arg2, f32 arg3) {
     // Raw lava not registered, so abort the wave modification
     if (sLavaWavyTexRaw == NULL || sLavaFloorModifiedTexRaw == NULL) {
         return;
@@ -1184,7 +1185,7 @@ void BossDodongo_Update(Actor* thisx, PlayState* play2) {
         //
         // Apply the corresponding wavy effect based on the texture being raw or not
         if (ResourceMgr_TexIsRaw(gDodongosCavernBossLavaFloorTex)) {
-            func_808C1554_Raw(gDodongosCavernBossLavaFloorTex, sLavaFloorLavaTex, this->unk_19E, this->unk_224);
+            BossDodongo_UpdateLavaTextureRaw(gDodongosCavernBossLavaFloorTex, sLavaFloorLavaTex, this->unk_19E, this->unk_224);  // was func_808C1554_Raw [LLM:HIGH]
         } else {
             BossDodongo_UpdateLavaTexture(gDodongosCavernBossLavaFloorTex, sLavaFloorLavaTex, this->unk_19E, this->unk_224);  // was func_808C1554 [LLM:HIGH]
         }
-- 
2.55.0

