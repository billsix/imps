From e7690fd3338eb630b7e573ced187010f683c37b0 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:19:23 +0000
Subject: [PATCH 1445/3799] soh: rename func_80978344 -> DemoGeff_DrawRubble

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: sDrawFuncs[1]: draws gGanonRubbleDL through the shared draw helper.
Defined in soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
index ef5c21370..cdfa63b99 100644
--- a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
+++ b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
@@ -23,7 +23,8 @@ void func_809783D4(DemoGeff* this, PlayState* play);
 void DemoGeff_Active(DemoGeff* this, PlayState* play);  // was func_80978308 [LLM:HIGH]
 
 void func_809784D4(DemoGeff* this, PlayState* play);
-void func_80978344(DemoGeff* this, PlayState* play);
+// LLM generated name (HIGH), was func_80978344: sDrawFuncs[1]: draws gGanonRubbleDL through the shared draw helper.
+void DemoGeff_DrawRubble(DemoGeff* this, PlayState* play);  // was func_80978344 [LLM:HIGH]
 
 static s16 sObjectIDs[] = {
     OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF, OBJECT_GEFF,
@@ -41,7 +42,7 @@ static DemoGeffActionFunc sActionFuncs[] = {
 
 static DemoGeffDrawFunc sDrawFuncs[] = {
     func_809784D4,
-    func_80978344,
+    DemoGeff_DrawRubble,  // was func_80978344 [LLM:HIGH]
 };
 
 const ActorInit Demo_Geff_InitVars = {
@@ -172,7 +173,7 @@ void DemoGeff_Active(DemoGeff* this, PlayState* play) {
     DemoGeff_SetupRubble(this, play);  // was func_80978030 [LLM:HIGH]
 }
 
-void func_80978344(DemoGeff* this, PlayState* play) {
+void DemoGeff_DrawRubble(DemoGeff* this, PlayState* play) {
     DemoGeff_DrawDList(play, gGanonRubbleDL);  // was func_80977EA8 [LLM:HIGH]
 }
 
-- 
2.55.0

