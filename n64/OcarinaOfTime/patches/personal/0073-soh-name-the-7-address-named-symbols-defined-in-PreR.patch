From 736c7f5daa356c1e28d2a0c453f5cc64a979ffc7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:03 +0000
Subject: [PATCH 073/488] soh: name the 7 address-named symbols defined in
 PreRender.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 7 are defined in soh/src/code/PreRender.c; updating their references touches 3 files in total.

  * func_800C0F28 -> PreRender_CopyImage
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.86

  * func_800C1258 -> PreRender_CopyImageRegionImpl
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.87

  * func_800C1B24 -> PreRender_CoverageRgba16ToI8
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.86

  * func_800C1E9C -> PreRender_SaveZBuffer
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.83

  * func_800C1F20 -> PreRender_SaveFramebuffer
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.88

  * func_800C1FA4 -> PreRender_FetchFbufCoverage
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.98

  * func_800C2500 -> PreRender_AntiAliasFilter
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.87

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/include/functions.h  | 14 +++++++-------
 soh/src/code/PreRender.c | 35 +++++++++++++++++++++--------------
 soh/src/code/z_play.c    |  2 +-
 3 files changed, 29 insertions(+), 22 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 60f5c3f53..67e9f1fa6 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1578,20 +1578,20 @@ void PreRender_SetValuesSave(PreRender* this, u32 width, u32 height, void* fbuf,
 void PreRender_Init(PreRender* this);
 void PreRender_SetValues(PreRender* this, u32 width, u32 height, void* fbuf, void* zbuf);
 void PreRender_Destroy(PreRender* this);
-void func_800C0F28(PreRender* this, Gfx** gfxp, void* buf, void* bufSave);
-void func_800C1258(PreRender* this, Gfx** gfxp);
+void PreRender_CopyImage(PreRender* this, Gfx** gfxp, void* buf, void* bufSave);  // was func_800C0F28 [oot]
+void PreRender_CopyImageRegionImpl(PreRender* this, Gfx** gfxp);  // was func_800C1258 [oot]
 void func_800C170C(PreRender* this, Gfx** gfxp, void* fbuf, void* fbufSave, u32 r, u32 g, u32 b, u32 a);
 void func_800C1AE8(PreRender* this, Gfx** gfxp, void* fbuf, void* fbufSave);
-void func_800C1B24(PreRender* this, Gfx** gfxp, void* fbuf, void* cvgSave);
-void func_800C1E9C(PreRender* this, Gfx** gfxp);
-void func_800C1F20(PreRender* this, Gfx** gfxp);
-void func_800C1FA4(PreRender* this, Gfx** gfxp);
+void PreRender_CoverageRgba16ToI8(PreRender* this, Gfx** gfxp, void* fbuf, void* cvgSave);  // was func_800C1B24 [oot]
+void PreRender_SaveZBuffer(PreRender* this, Gfx** gfxp);  // was func_800C1E9C [oot]
+void PreRender_SaveFramebuffer(PreRender* this, Gfx** gfxp);  // was func_800C1F20 [oot]
+void PreRender_FetchFbufCoverage(PreRender* this, Gfx** gfxp);  // was func_800C1FA4 [oot]
 void func_800C20B4(PreRender* this, Gfx** gfxp);
 void func_800C2118(PreRender* this, Gfx** gfxp);
 void func_800C213C(PreRender* this, Gfx** gfxp);
 void func_800C24BC(PreRender* this, Gfx** gfxp);
 void func_800C24E0(PreRender* this, Gfx** gfxp);
-void func_800C2500(PreRender* this, s32 x, s32 y);
+void PreRender_AntiAliasFilter(PreRender* this, s32 x, s32 y);  // was func_800C2500 [oot]
 void func_800C2FE4(PreRender* this);
 void PreRender_Calc(PreRender* this);
 void THGA_Ct(TwoHeadGfxArena* thga, Gfx* start, size_t size);
diff --git a/soh/src/code/PreRender.c b/soh/src/code/PreRender.c
index e79b4cfe0..49efabd30 100644
--- a/soh/src/code/PreRender.c
+++ b/soh/src/code/PreRender.c
@@ -36,7 +36,8 @@ void PreRender_Destroy(PreRender* this) {
     ListAlloc_FreeAll(&this->alloc);
 }
 
-void func_800C0F28(PreRender* this, Gfx** gfxp, void* buf, void* bufSave) {
+// Name from zeldaret/oot (was func_800C0F28): adopted upstream's name for this address; body verified to match upstream at 0.86. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_CopyImage(PreRender* this, Gfx** gfxp, void* buf, void* bufSave) {
     Gfx* gfx;
     s32 x;
     s32 x2;
@@ -84,7 +85,8 @@ void func_800C0F28(PreRender* this, Gfx** gfxp, void* buf, void* bufSave) {
     *gfxp = gfx;
 }
 
-void func_800C1258(PreRender* this, Gfx** gfxp) {
+// Name from zeldaret/oot (was func_800C1258): adopted upstream's name for this address; body verified to match upstream at 0.87. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_CopyImageRegionImpl(PreRender* this, Gfx** gfxp) {
     Gfx* gfx;
     s32 y;
     s32 y2;
@@ -190,7 +192,8 @@ void func_800C1AE8(PreRender* this, Gfx** gfxp, void* fbuf, void* fbufSave) {
     func_800C170C(this, gfxp, fbuf, fbufSave, 255, 255, 255, 255);
 }
 
-void func_800C1B24(PreRender* this, Gfx** gfxp, void* fbuf, void* cvgSave) {
+// Name from zeldaret/oot (was func_800C1B24): adopted upstream's name for this address; body verified to match upstream at 0.86. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_CoverageRgba16ToI8(PreRender* this, Gfx** gfxp, void* fbuf, void* cvgSave) {
     Gfx* gfx;
     s32 x;
     s32 x2;
@@ -239,16 +242,18 @@ void func_800C1B24(PreRender* this, Gfx** gfxp, void* fbuf, void* cvgSave) {
     *gfxp = gfx;
 }
 
-void func_800C1E9C(PreRender* this, Gfx** gfxp) {
+// Name from zeldaret/oot (was func_800C1E9C): adopted upstream's name for this address; body verified to match upstream at 0.83. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_SaveZBuffer(PreRender* this, Gfx** gfxp) {
     LOG_CHECK_NULL_POINTER("this->zbuf_save", this->zbufSave);
     LOG_CHECK_NULL_POINTER("this->zbuf", this->zbuf);
 
     if ((this->zbufSave != NULL) && (this->zbuf != NULL)) {
-        func_800C0F28(this, gfxp, this->zbuf, this->zbufSave);
+        PreRender_CopyImage(this, gfxp, this->zbuf, this->zbufSave);
     }
 }
 
-void func_800C1F20(PreRender* this, Gfx** gfxp) {
+// Name from zeldaret/oot (was func_800C1F20): adopted upstream's name for this address; body verified to match upstream at 0.88. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_SaveFramebuffer(PreRender* this, Gfx** gfxp) {
     LOG_CHECK_NULL_POINTER("this->fbuf_save", this->fbufSave);
     LOG_CHECK_NULL_POINTER("this->fbuf", this->fbuf);
 
@@ -257,7 +262,8 @@ void func_800C1F20(PreRender* this, Gfx** gfxp) {
     }
 }
 
-void func_800C1FA4(PreRender* this, Gfx** gfxp) {
+// Name from zeldaret/oot (was func_800C1FA4): adopted upstream's name for this address; body verified to match upstream at 0.98. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_FetchFbufCoverage(PreRender* this, Gfx** gfxp) {
     Gfx* gfx = *gfxp;
 
     gDPPipeSync(gfx++);
@@ -275,15 +281,15 @@ void func_800C1FA4(PreRender* this, Gfx** gfxp) {
 }
 
 void func_800C20B4(PreRender* this, Gfx** gfxp) {
-    func_800C1FA4(this, gfxp);
+    PreRender_FetchFbufCoverage(this, gfxp);
     LOG_CHECK_NULL_POINTER("this->cvg_save", this->cvgSave);
     if (this->cvgSave != NULL) {
-        func_800C1B24(this, gfxp, this->fbuf, this->cvgSave);
+        PreRender_CoverageRgba16ToI8(this, gfxp, this->fbuf, this->cvgSave);
     }
 }
 
 void func_800C2118(PreRender* this, Gfx** gfxp) {
-    func_800C0F28(this, gfxp, this->zbufSave, this->zbuf);
+    PreRender_CopyImage(this, gfxp, this->zbufSave, this->zbuf);
 }
 
 void func_800C213C(PreRender* this, Gfx** gfxp) {
@@ -345,14 +351,15 @@ void func_800C213C(PreRender* this, Gfx** gfxp) {
 }
 
 void func_800C24BC(PreRender* this, Gfx** gfxp) {
-    func_800C0F28(this, gfxp, this->fbufSave, this->fbuf);
+    PreRender_CopyImage(this, gfxp, this->fbufSave, this->fbuf);
 }
 
 void func_800C24E0(PreRender* this, Gfx** gfxp) {
-    func_800C1258(this, gfxp);
+    PreRender_CopyImageRegionImpl(this, gfxp);
 }
 
-void func_800C2500(PreRender* this, s32 x, s32 y) {
+// Name from zeldaret/oot (was func_800C2500): adopted upstream's name for this address; body verified to match upstream at 0.87. [oot: https://github.com/zeldaret/oot/blob/main/src/code/PreRender.c]
+void PreRender_AntiAliasFilter(PreRender* this, s32 x, s32 y) {
     s32 i;
     s32 j;
     s32 buffA[3 * 5];
@@ -560,7 +567,7 @@ void PreRender_Calc(PreRender* this) {
                 a >>= 5;
                 a++;
                 if (a != 8) {
-                    func_800C2500(this, x, y);
+                    PreRender_AntiAliasFilter(this, x, y);
                 }
             }
         }
diff --git a/soh/src/code/z_play.c b/soh/src/code/z_play.c
index 55e947eff..8337cbd15 100644
--- a/soh/src/code/z_play.c
+++ b/soh/src/code/z_play.c
@@ -1571,7 +1571,7 @@ void Play_Draw(PlayState* play) {
             play->pauseBgPreRender.fbuf = gfxCtx->curFrameBuffer;
             play->pauseBgPreRender.fbufSave = (u16*)gZBuffer;
             // SOH [Port] Use our custom copy method instead of the prerender system
-            // func_800C1F20(&play->pauseBgPreRender, &gfxP);
+            // PreRender_SaveFramebuffer(&play->pauseBgPreRender, &gfxP);
             if (R_PAUSE_MENU_MODE == 1) {
                 play->pauseBgPreRender.cvgSave = (u8*)gfxCtx->curFrameBuffer;
                 // func_800C20B4(&play->pauseBgPreRender, &gfxP);
-- 
2.55.0

