From 3ba1afb4b7a7e6605c9fed8c39dac1d28d642a74 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 15:21:30 +0000
Subject: [PATCH 0685/3799] soh: rename func_80A7A304 -> EnIn_Racing

Name source: zeldaret/oot (same decomp, same ROM address).
Structure verified against upstream at 0.95.
Defined in soh/src/overlays/actors/ovl_En_In/z_en_in.c.
---
 soh/src/overlays/actors/ovl_En_In/z_en_in.c | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_In/z_en_in.c b/soh/src/overlays/actors/ovl_En_In/z_en_in.c
index 974397af1..96ff24860 100644
--- a/soh/src/overlays/actors/ovl_En_In/z_en_in.c
+++ b/soh/src/overlays/actors/ovl_En_In/z_en_in.c
@@ -12,7 +12,8 @@ void EnIn_Draw(Actor* thisx, PlayState* play);
 void EnIn_Reset(void);
 
 void EnIn_WaitForObject(EnIn* this, PlayState* play);
-void func_80A7A304(EnIn* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A7A304): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_In/z_en_in.c]
+void EnIn_Racing(EnIn* this, PlayState* play);
 void func_80A7A4C8(EnIn* this, PlayState* play);
 void func_80A7A568(EnIn* this, PlayState* play);
 void func_80A7A848(EnIn* this, PlayState* play);
@@ -370,7 +371,7 @@ s32 EnIn_StartingRace(EnIn* this, PlayState* play) {
     this->collider.base.ocFlags1 &= ~OC1_ON;
     Animation_Change(&this->skelAnime, D_80A7B918[this->animationIdx], 1.0f, 0.0f,
                      Animation_GetLastFrame(D_80A7B918[this->animationIdx]), 2, 0.0f);
-    this->actionFunc = func_80A7A304;
+    this->actionFunc = EnIn_Racing;
     return 1;
 }
 
@@ -622,7 +623,7 @@ void EnIn_WaitForObject(EnIn* this, PlayState* play) {
     }
 }
 
-void func_80A7A304(EnIn* this, PlayState* play) {
+void EnIn_Racing(EnIn* this, PlayState* play) {
     if (this->skelAnime.animation == &object_in_Anim_015814 || this->skelAnime.animation == &object_in_Anim_01646C) {
         if (this->skelAnime.curFrame == 8.0f) {
             Audio_PlaySoundRandom(&this->actor.projectedPos, NA_SE_VO_IN_LASH_0,
@@ -936,7 +937,7 @@ void EnIn_Update(Actor* thisx, PlayState* play) {
     collider = &this->collider;
     Collider_UpdateCylinder(&this->actor, collider);
     CollisionCheck_SetOC(play, &play->colChkCtx, &collider->base);
-    if (this->actionFunc != func_80A7A304) {
+    if (this->actionFunc != EnIn_Racing) {
         SkelAnime_Update(&this->skelAnime);
         if (this->skelAnime.animation == &object_in_Anim_001BE0 && ((gSaveContext.eventInf[0] & 0xF) != 6)) {
             EnIn_Stagger(&this->skelAnime, this, play);
@@ -945,7 +946,7 @@ void EnIn_Update(Actor* thisx, PlayState* play) {
     }
     EnIn_UpdateEyes(this);
     this->actionFunc(this, play);
-    if (this->actionFunc != func_80A7A304) {
+    if (this->actionFunc != EnIn_Racing) {
         EnIn_Fidget(this, play);
         if (gSaveContext.subTimerSeconds < 6 && gSaveContext.subTimerState != SUBTIMER_STATE_OFF &&
             this->interactInfo.talkState == NPC_TALK_STATE_IDLE) {
-- 
2.55.0

