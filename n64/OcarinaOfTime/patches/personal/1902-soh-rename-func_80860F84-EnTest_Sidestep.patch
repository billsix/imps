From 5e769020a9d46deb31ecddad483d2de734e5e564 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:10 +0000
Subject: [PATCH 1902/3799] soh: rename func_80860F84 -> EnTest_Sidestep

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: strafes at shape.rot.y + 0x3E80 while smooth-turning to face the player, bouncing off walls; the untargeted counterpart of EnTest_CircleAroundPlayer
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index e10177936..6b8f961ca 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -43,7 +43,8 @@ void EnTest_Idle(EnTest* this, PlayState* play);
 void EnTest_WalkAndBlock(EnTest* this, PlayState* play);
 // LLM generated name (HIGH), was func_80860C24: rotates shape.rot.y toward yawTowardsPlayer, scaling anim playSpeed with the turn rate, until Actor_IsFacingPlayer
 void EnTest_TurnToPlayer(EnTest* this, PlayState* play);  // was func_80860C24 [LLM:HIGH]
-void func_80860F84(EnTest* this, PlayState* play);
+// LLM generated name (GUESS), was func_80860F84: strafes at shape.rot.y + 0x3E80 while smooth-turning to face the player, bouncing off walls; the untargeted counterpart of EnTest_CircleAroundPlayer
+void EnTest_Sidestep(EnTest* this, PlayState* play);  // was func_80860F84 [LLM:GUESS]
 void EnTest_SlashDown(EnTest* this, PlayState* play);
 void EnTest_SlashDownEnd(EnTest* this, PlayState* play);
 void EnTest_SlashUp(EnTest* this, PlayState* play);
@@ -787,11 +788,11 @@ void EnTest_SetupSidestep(EnTest* this) {
     this->timer = (s16)((Rand_ZeroOne() * 15.0f) + 25.0f);
     this->unk_7EC = 0.0f;
     this->actor.world.rot.y = this->actor.shape.rot.y;
-    EnTest_SetupAction(this, func_80860F84);
+    EnTest_SetupAction(this, EnTest_Sidestep);  // was func_80860F84 [LLM:GUESS]
 }
 
 // a variation of sidestep
-void func_80860F84(EnTest* this, PlayState* play) {
+void EnTest_Sidestep(EnTest* this, PlayState* play) {
     s16 playerYaw180;
     s32 pad;
     s32 prevFrame;
-- 
2.55.0

