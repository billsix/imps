From 080def70b61277acf5f25cfb67af7e66357e1ccf Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:30:03 +0000
Subject: [PATCH 1600/3799] soh: rename func_80A4F13C -> EnGs_SpinAndRise

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spins the stone up to speed about its own axis and then stretches it, the multi-stage reaction used when it is launched.
Defined in soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
index 73f7e6494..81e9852c6 100644
--- a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
+++ b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
@@ -395,7 +395,8 @@ void EnGs_Explode(EnGs* this, PlayState* play) {
     }
 }
 
-void func_80A4F13C(EnGs* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A4F13C: spins the stone up to speed about its own axis and then stretches it, the multi-stage reaction used when it is launched.
+void EnGs_SpinAndRise(EnGs* this, PlayState* play) {
     f32 tmpf1;
     f32 tmpf2;
     f32 tmpf3;
@@ -559,7 +560,7 @@ void EnGs_Update(Actor* thisx, PlayState* play2) {
                 case 11:
                     this->unk_19E |= 1;
                     func_80A4F77C(this);
-                    this->actionFunc = func_80A4F13C;
+                    this->actionFunc = EnGs_SpinAndRise;  // was func_80A4F13C [LLM:HIGH]
                     break;
             }
         }
-- 
2.55.0

