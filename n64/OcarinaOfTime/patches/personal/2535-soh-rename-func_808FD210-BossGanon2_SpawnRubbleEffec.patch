From 0d5d0d362e8d90bf21767240688403b0f412e533 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:35:35 +0000
Subject: [PATCH 2535/3799] soh: rename func_808FD210 ->
 BossGanon2_SpawnRubbleEffect

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: fills the first effect slot with a type 1 arc thrown up and to the right
Defined in soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
index 1cf930b9d..cb48eb6e7 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
@@ -135,7 +135,8 @@ void BossGanon2_SetObjectSegment(BossGanon2* this, PlayState* play, s32 objectId
     }
 }
 
-void func_808FD210(PlayState* play, Vec3f* arg1) {
+// LLM generated name (HIGH), was func_808FD210: fills the first effect slot with a type 1 arc thrown up and to the right
+void BossGanon2_SpawnRubbleEffect(PlayState* play, Vec3f* arg1) {
     BossGanon2Effect* effect = play->specialEffects;
 
     effect->type = 1;
@@ -797,7 +798,7 @@ void func_808FD5F4(BossGanon2* this, PlayState* play) {
                     this->csTimer = 0;
                     sp68 = player->actor.world.pos;
                     sp68.y += 60.0f;
-                    func_808FD210(play, &sp68);
+                    BossGanon2_SpawnRubbleEffect(play, &sp68);  // was func_808FD210 [LLM:HIGH]
                     play->envCtx.unk_D8 = 0.0f;
                     play->envCtx.unk_BE = 0;
                     this->unk_339 = 0;
-- 
2.55.0

