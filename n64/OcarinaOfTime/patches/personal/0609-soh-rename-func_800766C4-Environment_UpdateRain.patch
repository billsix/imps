From 1e3c463ad91254ea9f3ef2bb3334f3232a7b6fcb Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 15:21:11 +0000
Subject: [PATCH 609/784] soh: rename func_800766C4 -> Environment_UpdateRain

Name source: zeldaret/oot (same decomp, same ROM address).
Structure verified against upstream at 1.00.
Defined in soh/src/code/z_kankyo.c.
---
 soh/src/code/z_kankyo.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/code/z_kankyo.c b/soh/src/code/z_kankyo.c
index aee405257..98808410f 100644
--- a/soh/src/code/z_kankyo.c
+++ b/soh/src/code/z_kankyo.c
@@ -883,7 +883,8 @@ void Environment_PrintDebugInfo(PlayState* play, Gfx** gfx) {
 
 // Name from zeldaret/oot (was func_80075B44): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.94. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_kankyo.c]
 void Environment_PlayTimeBasedSequence(PlayState* play);
-void func_800766C4(PlayState* play);
+// Name from zeldaret/oot (was func_800766C4): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_kankyo.c]
+void Environment_UpdateRain(PlayState* play);
 
 void Environment_Update(PlayState* play, EnvironmentContext* envCtx, LightContext* lightCtx, PauseContext* pauseCtx,
                         MessageContext* msgCtx, GameOverContext* gameOverCtx, GraphicsContext* gfxCtx) {
@@ -909,7 +910,7 @@ void Environment_Update(PlayState* play, EnvironmentContext* envCtx, LightContex
             }
         }
 
-        func_800766C4(play); // increments or decrements unk_EE[1] depending on some condition
+        Environment_UpdateRain(play); // increments or decrements unk_EE[1] depending on some condition
         Environment_PlayTimeBasedSequence(play); // updates bgm/sfx and other things as the day progresses
 
         if (((void)0, gSaveContext.nextDayTime) >= 0xFF00 && ((void)0, gSaveContext.nextDayTime) != 0xFFFF) {
@@ -2238,7 +2239,7 @@ void Environment_FadeOutGameOverLights(PlayState* play) {
     }
 }
 
-void func_800766C4(PlayState* play) {
+void Environment_UpdateRain(PlayState* play) {
     u8 max = MAX(play->envCtx.unk_EE[0], play->envCtx.unk_F2[0]);
 
     if (play->envCtx.unk_EE[1] != max && ((play->state.frames % 8) == 0)) {
-- 
2.55.0

