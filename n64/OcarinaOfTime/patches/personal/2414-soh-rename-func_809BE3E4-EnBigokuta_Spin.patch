From b08868c4dd8cac49b7c9fd326ade547fb50df31a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:27 +0000
Subject: [PATCH 2414/3799] soh: rename func_809BE3E4 -> EnBigokuta_Spin

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spins in place, then picks the direction it was turned toward and resumes circling
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c   | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index 562edd069..e6d014713 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -27,7 +27,8 @@ void EnBigokuta_TurnToStart(EnBigokuta* this, PlayState* play);  // was func_809
 void EnBigokuta_WaitBeforeCircle(EnBigokuta* this, PlayState* play);  // was func_809BDB90 [LLM:HIGH]
 // LLM generated name (HIGH), was func_809BDC08: circles the ring, speeding up or slowing down with the player's distance and facing, and picks the next state when its windows expire
 void EnBigokuta_Circle(EnBigokuta* this, PlayState* play);  // was func_809BDC08 [LLM:HIGH]
-void func_809BE3E4(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BE3E4: spins in place, then picks the direction it was turned toward and resumes circling
+void EnBigokuta_Spin(EnBigokuta* this, PlayState* play);  // was func_809BE3E4 [LLM:HIGH]
 void func_809BE4A4(EnBigokuta* this, PlayState* play);
 void func_809BE518(EnBigokuta* this, PlayState* play);
 // LLM generated name (HIGH), was func_809BCF68: spawns the ripple and splash trailing behind the moving octopus, with its splash sound
@@ -246,7 +247,7 @@ void EnBigokuta_SpawnWakeSplash(EnBigokuta* this, PlayState* play) {
         rot = this->actor.shape.rot.y - this->unk_194 * 0xA00 - Rand_S16Offset(0x1200, 0xC00);
     }
     if (this->actionFunc != func_809BE4A4) {
-        if (this->actionFunc == func_809BE3E4 || (play->gameplayFrames & 2)) {
+        if (this->actionFunc == EnBigokuta_Spin || (play->gameplayFrames & 2)) {  // was func_809BE3E4 [LLM:HIGH]
             effectPos.x = this->actor.world.pos.x - Math_SinS(rot) * 80.0f;
             effectPos.z = this->actor.world.pos.z - Math_CosS(rot) * 80.0f;
             effectPos.y = this->actor.home.pos.y + 1.0f;
@@ -407,7 +408,7 @@ void EnBigokuta_SetupSpin(EnBigokuta* this) {
     }
     this->unk_19A = 0;
     this->collider.base.acFlags &= ~AC_ON;
-    this->actionFunc = func_809BE3E4;
+    this->actionFunc = EnBigokuta_Spin;  // was func_809BE3E4 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_809BD768: picks a new circling direction, drops targeting and plays NA_SE_EN_DAIOCTA_SINK
@@ -682,7 +683,7 @@ void EnBigokuta_Death(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BE3E4(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_Spin(EnBigokuta* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
 
     if (this->unk_196 != 0) {
@@ -768,7 +769,7 @@ void func_809BE798(EnBigokuta* this, PlayState* play) {
         if (this->actionFunc == EnBigokuta_Circle) {  // was func_809BDC08 [LLM:HIGH]
             EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
             this->unk_196 = 40;
-        } else if (this->actionFunc == func_809BE3E4) {
+        } else if (this->actionFunc == EnBigokuta_Spin) {  // was func_809BE3E4 [LLM:HIGH]
             if ((effectRot * this->unk_194) > 0) {
                 this->unk_194 = 0 - this->unk_194;
                 this->unk_196 += 4;
-- 
2.55.0

