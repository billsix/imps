From c30c8bfee993fb6a7c3ea46b720d1524ae4724e7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:17 +0000
Subject: [PATCH 2238/3799] soh: rename func_80B0C020 -> EnSw_LineTestWall

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: line-tests for a wall, rejecting crawlspaces and surfaces projectiles pass through
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index b294e54af..ce878ce95 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -117,7 +117,8 @@ s32 EnSw_SetSurfaceBasis(EnSw* this, CollisionPoly* poly) {
     //! @bug: Does not return.
 }
 
-CollisionPoly* func_80B0C020(PlayState* play, Vec3f* arg1, Vec3f* arg2, Vec3f* arg3, s32* arg4) {
+// LLM generated name (HIGH), was func_80B0C020: line-tests for a wall, rejecting crawlspaces and surfaces projectiles pass through
+CollisionPoly* EnSw_LineTestWall(PlayState* play, Vec3f* arg1, Vec3f* arg2, Vec3f* arg3, s32* arg4) {
     CollisionPoly* sp3C;
     s32 pad;
 
@@ -158,13 +159,13 @@ s32 func_80B0C0CC(EnSw* this, PlayState* play, s32 arg2) {
     sp78.x -= this->unk_364.x * 18.0f;
     sp78.y -= this->unk_364.y * 18.0f;
     sp78.z -= this->unk_364.z * 18.0f;
-    temp_s1 = func_80B0C020(play, &sp84, &sp78, &sp90, &sp70);
+    temp_s1 = EnSw_LineTestWall(play, &sp84, &sp78, &sp90, &sp70);  // was func_80B0C020 [LLM:HIGH]
 
     if ((temp_s1 != NULL) && (this->unk_360 == 0)) {
         sp78.x = sp84.x + (this->unk_37C.x * 24);
         sp78.y = sp84.y + (this->unk_37C.y * 24);
         sp78.z = sp84.z + (this->unk_37C.z * 24);
-        temp_v0_2 = func_80B0C020(play, &sp84, &sp78, &sp9C, &sp6C);
+        temp_v0_2 = EnSw_LineTestWall(play, &sp84, &sp78, &sp9C, &sp6C);  // was func_80B0C020 [LLM:HIGH]
         if (temp_v0_2 != NULL) {
             if (arg2 == 1) {
                 EnSw_SetSurfaceBasis(this, temp_v0_2);  // was func_80B0BE20 [LLM:HIGH]
@@ -195,7 +196,7 @@ s32 func_80B0C0CC(EnSw* this, PlayState* play, s32 arg2) {
                 sp78.y = sp84.y - (this->unk_370.y * 24.0f);
                 sp78.z = sp84.z - (this->unk_370.z * 24.0f);
             }
-            temp_v0_2 = func_80B0C020(play, &sp84, &sp78, &sp9C, &sp6C);
+            temp_v0_2 = EnSw_LineTestWall(play, &sp84, &sp78, &sp9C, &sp6C);  // was func_80B0C020 [LLM:HIGH]
             if (temp_v0_2 != NULL) {
                 if (arg2 == 1) {
                     EnSw_SetSurfaceBasis(this, temp_v0_2);  // was func_80B0BE20 [LLM:HIGH]
-- 
2.55.0

