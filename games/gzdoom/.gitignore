# The upstream checkouts and everything build/run produce are disposable; only
# the scripts, patches/, and docs are versioned. WADs/IWADs are the maintainer's
# own game data and live in runDir/ — never committed.
/checkout/
/build/
/install/
/build-cmake/
/bldInstall/
/runDir/

# Podman image export tarballs (Makefile) — large, never commit.
gzdoom-builder-*.tar
