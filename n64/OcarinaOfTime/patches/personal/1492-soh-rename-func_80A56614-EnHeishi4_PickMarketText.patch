From b80d6fe7ef943c203851da343681ecc35396f023 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:23:22 +0000
Subject: [PATCH 1492/3799] soh: rename func_80A56614 ->
 EnHeishi4_PickMarketText

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the night-market guard's line picker, choosing from the face-reaction set, the scene and the time of day.
Defined in soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/soh/Enhancements/TimeSavers/MarketSneak.cpp     |  6 +++---
 .../overlays/actors/ovl_En_Heishi4/z_en_heishi4.c   | 13 +++++++------
 2 files changed, 10 insertions(+), 9 deletions(-)

diff --git a/soh/soh/Enhancements/TimeSavers/MarketSneak.cpp b/soh/soh/Enhancements/TimeSavers/MarketSneak.cpp
index bd74d0d59..bfc8295e0 100644
--- a/soh/soh/Enhancements/TimeSavers/MarketSneak.cpp
+++ b/soh/soh/Enhancements/TimeSavers/MarketSneak.cpp
@@ -10,7 +10,7 @@ extern "C" {
 #include "overlays/actors/ovl_En_Heishi4/z_en_heishi4.h"
 extern PlayState* gPlayState;
 // the guard's idle action func, restored once the sneak prompt is answered
-void func_80A56614(EnHeishi4* heishi, PlayState* play);
+void EnHeishi4_PickMarketText(EnHeishi4* heishi, PlayState* play);  // was func_80A56614 [LLM:HIGH]
 u8 Randomizer_GetSettingValue(RandomizerSettingKey randoSettingKey);
 }
 
@@ -44,10 +44,10 @@ extern "C" void EnHeishi4_MarketSneak(EnHeishi4* heishi, PlayState* play) {
                 play->transitionTrigger = TRANS_TRIGGER_START;
                 play->transitionType = TRANS_TYPE_CIRCLE(TCA_STARBURST, TCC_WHITE, TCS_FAST);
                 gSaveContext.nextTransitionType = TRANS_TYPE_CIRCLE(TCA_STARBURST, TCC_WHITE, TCS_FAST);
-                heishi->actionFunc = func_80A56614;
+                heishi->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
                 break;
             case 1: // no
-                heishi->actionFunc = func_80A56614;
+                heishi->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
                 break;
         }
     }
diff --git a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
index 430addf00..7c02ec642 100644
--- a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
+++ b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
@@ -18,7 +18,8 @@ void EnHeishi4_SetupKakarikoIdle(EnHeishi4* this, PlayState* play);  // was func
 // LLM generated name (HIGH), was func_80A563BC: chooses the Kakariko guard's line from the mask face-reaction set, the story flags, the time of day and whether he has already been spoken to, then hands off to the shared talk state.
 void EnHeishi4_PickKakarikoText(EnHeishi4* this, PlayState* play);  // was func_80A563BC [LLM:HIGH]
 void func_80A56B40(EnHeishi4* this, PlayState* play);
-void func_80A56614(EnHeishi4* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A56614: the night-market guard's line picker, choosing from the face-reaction set, the scene and the time of day.
+void EnHeishi4_PickMarketText(EnHeishi4* this, PlayState* play);  // was func_80A56614 [LLM:HIGH]
 void func_80A56874(EnHeishi4* this, PlayState* play);
 void func_80A56900(EnHeishi4* this, PlayState* play);
 void func_80A56994(EnHeishi4* this, PlayState* play);
@@ -175,11 +176,11 @@ void EnHeishi4_SetupMarketIdle(EnHeishi4* this, PlayState* play) {
         osSyncPrintf(VT_FGCOL(GREEN) " ☆☆☆☆☆ ぎゃぁ！オトナだー ☆☆☆☆☆ \n" VT_RST);
         Actor_Kill(&this->actor);
     } else {
-        this->actionFunc = func_80A56614;
+        this->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
     }
 }
 
-void func_80A56614(EnHeishi4* this, PlayState* play) {
+void EnHeishi4_PickMarketText(EnHeishi4* this, PlayState* play) {
     s16 reactionOffset;
 
     reactionOffset = this->type - 4;
@@ -312,7 +313,7 @@ void func_80A56B40(EnHeishi4* this, PlayState* play) {
                 return;
             }
             if (this->type == HEISHI4_AT_MARKET_NIGHT) {
-                this->actionFunc = func_80A56614;
+                this->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
                 return;
             }
         }
@@ -323,7 +324,7 @@ void func_80A56B40(EnHeishi4* this, PlayState* play) {
                 return;
             }
             if (this->type == HEISHI4_AT_MARKET_NIGHT) {
-                this->actionFunc = func_80A56614;
+                this->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
                 return;
             }
         }
@@ -336,7 +337,7 @@ void func_80A56B40(EnHeishi4* this, PlayState* play) {
         }
         if (this->type == HEISHI4_AT_MARKET_NIGHT) {
             if (GameInteractor_Should(VB_MARKET_NIGHT_GUARD_SET_ACTION_AFTER_TALK, true, this, play)) {
-                this->actionFunc = func_80A56614;
+                this->actionFunc = EnHeishi4_PickMarketText;  // was func_80A56614 [LLM:HIGH]
                 return;
             }
         }
-- 
2.55.0

