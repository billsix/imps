From 8595827b0f8114c3b7db30903c6f6d4973ea1934 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:39:38 +0000
Subject: [PATCH 1056/3799] soh: rename func_809946BC ->
 DoorGerudo_GetPlayerLocalZ

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: transforms the player's position into the door's local frame and returns the Z component, or FLT_MAX when the player is outside the given X and Y window.
Defined in soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c b/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
index 29eccfdd7..9d08cd7f3 100644
--- a/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
+++ b/soh/src/overlays/actors/ovl_Door_Gerudo/z_door_gerudo.c
@@ -61,7 +61,8 @@ void DoorGerudo_Destroy(Actor* thisx, PlayState* play) {
     DynaPoly_DeleteBgActor(play, &play->colCtx.dyna, this->dyna.bgId);
 }
 
-f32 func_809946BC(PlayState* play, DoorGerudo* this, f32 arg2, f32 arg3, f32 arg4) {
+// LLM generated name (HIGH), was func_809946BC: transforms the player's position into the door's local frame and returns the Z component, or FLT_MAX when the player is outside the given X and Y window.
+f32 DoorGerudo_GetPlayerLocalZ(PlayState* play, DoorGerudo* this, f32 arg2, f32 arg3, f32 arg4) {
     Player* player = GET_PLAYER(play);
     Vec3f playerPos;
     Vec3f sp1C;
@@ -84,7 +85,7 @@ s32 func_80994750(DoorGerudo* this, PlayState* play) {
     s16 rotYDiff;
 
     if (!Player_InCsMode(play)) {
-        temp_f0 = func_809946BC(play, this, 0.0f, 20.0f, 15.0f);
+        temp_f0 = DoorGerudo_GetPlayerLocalZ(play, this, 0.0f, 20.0f, 15.0f);  // was func_809946BC [LLM:HIGH]
         if (fabsf(temp_f0) < 40.0f) {
             rotYDiff = player->actor.shape.rot.y - this->dyna.actor.shape.rot.y;
             if (temp_f0 > 0.0f) {
-- 
2.55.0

