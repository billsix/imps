From 88173b3baad1e66e3696e5e5633973d038f1ae98 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:13:36 +0000
Subject: [PATCH 3335/3799] soh: rename D_80161144 -> sDbCameraFlagB

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the second such bool
Defined in soh/src/code/db_camera.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/db_camera.c | 68 ++++++++++++++++++++--------------------
 1 file changed, 34 insertions(+), 34 deletions(-)

diff --git a/soh/src/code/db_camera.c b/soh/src/code/db_camera.c
index d3a1e58ad..56c92421c 100644
--- a/soh/src/code/db_camera.c
+++ b/soh/src/code/db_camera.c
@@ -501,7 +501,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
     static s32 sDbCameraHoldTimer = 100;  // was D_8012D10C [LLM:GUESS]
     static s32 sDbCameraHoldState = 0;  // was D_8012D110 [LLM:GUESS]
     static s32 sDbCameraFlagA; // bool, was D_80161140 [LLM:GUESS]
-    static s32 D_80161144; // bool
+    static s32 sDbCameraFlagB; // bool, was D_80161144 [LLM:GUESS]
     Vec3f* sp124;
     f32 temp_f0_5;
     s16 yaw;
@@ -559,17 +559,17 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
             switch (dbCamera->unk_78) {
                 case 0:
                     sp124 = &dbCamera->unk_60;
-                    D_80161144 = false;
+                    sDbCameraFlagB = false;  // was D_80161144 [LLM:GUESS]
                     sDbCameraFlagA = false;  // was D_80161140 [LLM:GUESS]
                     break;
                 case 1:
                     sp124 = &dbCamera->unk_6C;
-                    D_80161144 = false;
+                    sDbCameraFlagB = false;  // was D_80161144 [LLM:GUESS]
                     sDbCameraFlagA = false;  // was D_80161140 [LLM:GUESS]
                     break;
                 case 2:
                     sp124 = &dbCamera->unk_54;
-                    D_80161144 = false;
+                    sDbCameraFlagB = false;  // was D_80161144 [LLM:GUESS]
                     sDbCameraFlagA = true;  // was D_80161140 [LLM:GUESS]
                     break;
             }
@@ -577,8 +577,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         case 1:
             switch (dbCamera->sub.unk_08) {
                 case 0:
-                    D_80161144 = dbCamera->sub.unk_0C;
-                    if (D_80161144) {
+                    sDbCameraFlagB = dbCamera->sub.unk_0C;  // was D_80161144 [LLM:GUESS]
+                    if (sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
                         sp124 = sp80;
                     } else {
                         sp124 = sp7C;
@@ -586,8 +586,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                     sDbCameraFlagA = false;  // was D_80161140 [LLM:GUESS]
                     break;
                 case 1:
-                    D_80161144 = dbCamera->sub.unk_0C;
-                    if (D_80161144) {
+                    sDbCameraFlagB = dbCamera->sub.unk_0C;  // was D_80161144 [LLM:GUESS]
+                    if (sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
                         sp124 = sp80;
                     } else {
                         sp124 = sp7C;
@@ -595,7 +595,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                     sDbCameraFlagA = false;  // was D_80161140 [LLM:GUESS]
                     break;
                 case 2:
-                    D_80161144 = false;
+                    sDbCameraFlagB = false;  // was D_80161144 [LLM:GUESS]
                     sDbCameraFlagA = true;  // was D_80161140 [LLM:GUESS]
                     sp124 = sp7C;
                     break;
@@ -609,7 +609,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
     }
     phi_s0 = sp124;
 
-    if (!D_80161144) {
+    if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
         OLib_Vec3fDiffToVecSphGeo(&sp104, sp7C, sp80);
     } else {
         OLib_Vec3fDiffToVecSphGeo(&sp104, sp80, sp7C);
@@ -622,7 +622,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
     new_var2 = ((dbCamera->unk_44 * 0.15f) + 0.2f);
     temp_f2 = new_var2 * (sp104.r / 100.0f);
     if ((dbCamera->unk_38 != 0) || dbCamera->unk_3C) {
-        if (D_80161144) {
+        if (sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             *sp80 = *phi_s0;
         } else {
             *sp7C = *phi_s0;
@@ -646,7 +646,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         } else if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_B)) {
             spFC = sp104;
             spFC.r = temp_f2;
-            if (!D_80161144) {
+            if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
                 spFC.yaw = sp104.yaw;
                 DbCamera_AddVecSph(sp7C, sp7C, &spFC);
             } else {
@@ -674,7 +674,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         } else if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_A)) {
             spFC = sp104;
             spFC.r = -temp_f2;
-            if (!D_80161144) {
+            if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
                 spFC.yaw = sp104.yaw;
                 DbCamera_AddVecSph(sp7C, sp7C, &spFC);
             } else {
@@ -697,7 +697,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC = sp104;
         spFC.r = temp_f2;
         spFC.pitch = 0;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -715,7 +715,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC = sp104;
         spFC.r = -temp_f2;
         spFC.pitch = 0;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -733,7 +733,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC.r = temp_f2;
         spFC.pitch = 0x3FFF;
         spFC.yaw = sp104.yaw;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
             DbCamera_AddVecSph(sp80, sp80, &spFC);
@@ -749,7 +749,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC.r = temp_f2;
         spFC.pitch = -0x3FFF;
         spFC.yaw = sp104.yaw;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
             DbCamera_AddVecSph(sp80, sp80, &spFC);
@@ -765,7 +765,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC = sp104;
         spFC.r = temp_f2;
         spFC.pitch = 0;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw + 0x3FFF;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -783,7 +783,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         spFC = sp104;
         spFC.r = temp_f2;
         spFC.pitch = 0;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw - 0x3FFF;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -810,7 +810,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
     } else if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_B)) {
         spFC = sp104;
         spFC.r = temp_f2;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -839,7 +839,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
     } else if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_A)) {
         spFC = sp104;
         spFC.r = -temp_f2;
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             spFC.yaw = sp104.yaw;
             DbCamera_AddVecSph(sp7C, sp7C, &spFC);
         } else {
@@ -900,7 +900,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         temp_f2_2 = sPlayState->state.input[2].rel.stick_x;
         pitch = DEGF_TO_BINANG((SQ(temp_f0_5) / 600.0f) * 0.8f);
         yaw = DEGF_TO_BINANG((SQ(temp_f2_2) / 600.0f) * 0.8f);
-        if (!D_80161144) {
+        if (!sDbCameraFlagB) {  // was D_80161144 [LLM:GUESS]
             sp104.pitch += (s16)((temp_f0_5 >= 0.0f) ? pitch : -pitch);
             sp104.yaw += (s16)((temp_f2_2 >= 0.0f) ? yaw : -yaw);
             DbCamera_AddVecSph(sp80, sp7C, &sp104);
@@ -1040,7 +1040,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 case 0:
                     dbCamera->unk_3C = false;
                     if (dbCamera->sub.mode != 1) {
-                        DebugCamera_CopyText(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0xD, 0x18, 3, !sDbCameraFlagB ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(sp104.pitch * 0.00549325f, &cameraStrings[69][6], 4);
                         DebugCamera_CopyText(0xF, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(sp104.yaw * 0.00549325f, &cameraStrings[70][6], 4);
@@ -1281,8 +1281,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
 
             if (dbCamera->sub.mode != 1) {
                 DebugCamera_CopyText(3, 0x16,  // was func_8006376C [LLM:HIGH]
-                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !D_80161144) ? 7
-                              : !D_80161144                                                               ? 4
+                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !sDbCameraFlagB) ? 7  // was D_80161144 [LLM:GUESS]
+                              : !sDbCameraFlagB                                                               ? 4  // was D_80161144 [LLM:GUESS]
                                                                                                           : 3,
                               cameraStrings[20]);
                 sp110 = 'X';
@@ -1295,8 +1295,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 DbCamera_SetTextValue(dbCamera->at.z, &sp111, 6);
                 DebugCamera_CopyText(3, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
                 DebugCamera_CopyText(0x1E, 0x16,  // was func_8006376C [LLM:HIGH]
-                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && D_80161144) ? 7
-                              : D_80161144                                                               ? 4
+                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && sDbCameraFlagB) ? 7  // was D_80161144 [LLM:GUESS]
+                              : sDbCameraFlagB                                                               ? 4  // was D_80161144 [LLM:GUESS]
                                                                                                          : 3,
                               cameraStrings[21]);
                 sp110 = 'X';
@@ -1312,8 +1312,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 OLib_Vec3fDiffToVecSphGeo(&spFC, sp90, sp7C);
                 spFC.yaw -= cam->playerPosRot.rot.y;
                 DebugCamera_CopyText(3, 0x16,  // was func_8006376C [LLM:HIGH]
-                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !D_80161144) ? 7
-                              : !D_80161144                                                               ? 4
+                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !sDbCameraFlagB) ? 7  // was D_80161144 [LLM:GUESS]
+                              : !sDbCameraFlagB                                                               ? 4  // was D_80161144 [LLM:GUESS]
                                                                                                           : 3,
                               cameraStrings[20]);
                 DbCamera_SetTextValue(spFC.pitch * 0.00549325f, &cameraStrings[69][5], 4);
@@ -1325,8 +1325,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 OLib_Vec3fDiffToVecSphGeo(&spFC, sp90, sp80);
                 spFC.yaw -= cam->playerPosRot.rot.y;
                 DebugCamera_CopyText(0x1E, 0x16,  // was func_8006376C [LLM:HIGH]
-                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && D_80161144) ? 7
-                              : D_80161144                                                               ? 4
+                              ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && sDbCameraFlagB) ? 7  // was D_80161144 [LLM:GUESS]
+                              : sDbCameraFlagB                                                               ? 4  // was D_80161144 [LLM:GUESS]
                                                                                                          : 3,
                               cameraStrings[21]);
                 DbCamera_SetTextValue(spFC.pitch * 0.00549325f, &cameraStrings[69][5], 4);
@@ -1384,7 +1384,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         DebugCamera_CopyText(9, 6, (dbCamera->unk_78 == 0) ? 7 : 4, cameraStrings[55]);  // was func_8006376C [LLM:HIGH]
         DebugCamera_CopyText(0x11, 6, (dbCamera->unk_78 == 1) ? 7 : 4, cameraStrings[56]);  // was func_8006376C [LLM:HIGH]
         DebugCamera_CopyText(0x18, 6, (dbCamera->unk_78 == 2) ? 7 : 4, cameraStrings[57]);  // was func_8006376C [LLM:HIGH]
-        DebugCamera_CopyText(3, 0x16, D_80161144 ? 3 : 4, cameraStrings[20]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x16, sDbCameraFlagB ? 3 : 4, cameraStrings[20]);  // was func_8006376C [LLM:HIGH]
         sp110 = 'X';
         DbCamera_SetTextValue(dbCamera->at.x, &sp111, 6);
         DebugCamera_CopyText(3, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
@@ -1394,7 +1394,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         sp110 = 'Z';
         DbCamera_SetTextValue(dbCamera->at.z, &sp111, 6);
         DebugCamera_CopyText(3, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
-        DebugCamera_CopyText(0x1E, 0x16, D_80161144 ? 4 : 3, cameraStrings[21]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x1E, 0x16, sDbCameraFlagB ? 4 : 3, cameraStrings[21]);  // was func_8006376C [LLM:HIGH]
         sp110 = 'X';
         DbCamera_SetTextValue(dbCamera->eye.x, &sp111, 6);
         DebugCamera_CopyText(0x1E, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
@@ -1404,7 +1404,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
         sp110 = 'Z';
         DbCamera_SetTextValue(dbCamera->eye.z, &sp111, 6);
         DebugCamera_CopyText(0x1E, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
-        DebugCamera_CopyText(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0xD, 0x18, 3, !sDbCameraFlagB ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sp104.pitch * 0.00549325f, &cameraStrings[69][6], 4);
         DebugCamera_CopyText(0xF, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sp104.yaw * 0.00549325f, &cameraStrings[70][6], 4);
-- 
2.55.0

