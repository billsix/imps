From ecb64f51ec63710b5b2c5cc7f3856fc6f91d6fd5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:36:45 +0000
Subject: [PATCH 1688/3799] soh: rename func_80ABAC00 -> EnNiwLady_GiveItem

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: re-offers the current trade item until the player takes it.
Defined in soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
index 661390b5a..0911a70c9 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
@@ -25,7 +25,8 @@ void EnNiwLady_AdultTalk(EnNiwLady* this, PlayState* play);  // was func_80ABA87
 void EnNiwLady_OfferPocketEgg(EnNiwLady* this, PlayState* play);  // was func_80ABA9B8 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABAB08: resolves the second trade prompt: yes hands over GI_COJIRO, no shows the refusal line and records it.
 void EnNiwLady_OfferCojiro(EnNiwLady* this, PlayState* play);  // was func_80ABAB08 [LLM:HIGH]
-void func_80ABAC00(EnNiwLady* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABAC00: re-offers the current trade item until the player takes it.
+void EnNiwLady_GiveItem(EnNiwLady* this, PlayState* play);  // was func_80ABAC00 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80ABAA9C: waits out the refusal line and returns to the adult talk setup.
 void EnNiwLady_DeclineTrade(EnNiwLady* this, PlayState* play);  // was func_80ABAA9C [LLM:HIGH]
 void func_80ABAC84(EnNiwLady* this, PlayState* play);
@@ -328,13 +329,13 @@ void EnNiwLady_FinishCuccoTalk(EnNiwLady* this, PlayState* play) {
                 return;
             }
 
-            this->actionFunc = func_80ABAC00;
+            this->actionFunc = EnNiwLady_GiveItem;  // was func_80ABAC00 [LLM:HIGH]
             return;
         }
         if (this->unk_26C == 1) {
             this->getItemId = GI_RUPEE_PURPLE;
             Actor_OfferGetItem(&this->actor, play, GI_RUPEE_PURPLE, 100.0f, 50.0f);
-            this->actionFunc = func_80ABAC00;
+            this->actionFunc = EnNiwLady_GiveItem;  // was func_80ABAC00 [LLM:HIGH]
         }
         this->actionFunc = EnNiwLady_CountCuccos;  // was func_80ABA244 [LLM:HIGH]
     }
@@ -411,7 +412,7 @@ void EnNiwLady_OfferPocketEgg(EnNiwLady* this, PlayState* play) {
 
                 if (GameInteractor_Should(VB_GIVE_ITEM_FROM_ANJU_AS_ADULT, true, this)) {
                     Actor_OfferGetItem(&this->actor, play, GI_POCKET_EGG, 200.0f, 100.0f);
-                    this->actionFunc = func_80ABAC00;
+                    this->actionFunc = EnNiwLady_GiveItem;  // was func_80ABAC00 [LLM:HIGH]
                 }
 
                 break;
@@ -442,7 +443,7 @@ void EnNiwLady_OfferCojiro(EnNiwLady* this, PlayState* play) {
                 this->actor.parent = NULL;
                 if (GameInteractor_Should(VB_TRADE_POCKET_CUCCO, true, this)) {
                     Actor_OfferGetItem(&this->actor, play, GI_COJIRO, 200.0f, 100.0f);
-                    this->actionFunc = func_80ABAC00;
+                    this->actionFunc = EnNiwLady_GiveItem;  // was func_80ABAC00 [LLM:HIGH]
                 }
                 break;
             case 1:
@@ -458,7 +459,7 @@ void EnNiwLady_OfferCojiro(EnNiwLady* this, PlayState* play) {
     }
 }
 
-void func_80ABAC00(EnNiwLady* this, PlayState* play) {
+void EnNiwLady_GiveItem(EnNiwLady* this, PlayState* play) {
     s32 getItemId;
 
     if (Actor_HasParent(&this->actor, play)) {
-- 
2.55.0

