From 5890eaf769baa5e849a9a3d3eb84de2f83a6efed Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:08:22 +0000
Subject: [PATCH 1288/3799] soh: rename D_80889E40 -> sTravelDistances

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: how far each fence variant slides from home; used to place it at its final position and as the arrival test while it moves.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c         | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c
index e992b1a8b..4b3638f2a 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Kousi/z_bg_hidan_kousi.c
@@ -21,7 +21,8 @@ void func_80889C18(BgHidanKousi* this, PlayState* play);
 void func_80889C90(BgHidanKousi* this, PlayState* play);
 void func_80889D28(BgHidanKousi* this, PlayState* play);
 
-static f32 D_80889E40[] = { 120.0f, 150.0f, 150.0f };
+// LLM generated name (HIGH), was D_80889E40: how far each fence variant slides from home; used to place it at its final position and as the arrival test while it moves.
+static f32 sTravelDistances[] = { 120.0f, 150.0f, 150.0f };  // was D_80889E40 [LLM:HIGH]
 
 const ActorInit Bg_Hidan_Kousi_InitVars = {
     ACTOR_BG_HIDAN_KOUSI,
@@ -97,8 +98,8 @@ void BgHidanKousi_Destroy(Actor* thisx, PlayState* play) {
 void func_80889ACC(BgHidanKousi* this) {
     s32 pad[2];
     Vec3s* rot = &this->dyna.actor.world.rot;
-    f32 temp1 = D_80889E40[this->dyna.actor.params & 0xFF] * Math_SinS(rot->y);
-    f32 temp2 = D_80889E40[this->dyna.actor.params & 0xFF] * Math_CosS(rot->y);
+    f32 temp1 = sTravelDistances[this->dyna.actor.params & 0xFF] * Math_SinS(rot->y);  // was D_80889E40 [LLM:HIGH]
+    f32 temp2 = sTravelDistances[this->dyna.actor.params & 0xFF] * Math_CosS(rot->y);  // was D_80889E40 [LLM:HIGH]
 
     this->dyna.actor.world.pos.x = this->dyna.actor.home.pos.x + temp1;
     this->dyna.actor.world.pos.z = this->dyna.actor.home.pos.z + temp2;
@@ -131,7 +132,7 @@ void func_80889C18(BgHidanKousi* this, PlayState* play) {
 
 void func_80889C90(BgHidanKousi* this, PlayState* play) {
     Actor_UpdatePos(&this->dyna.actor);
-    if (D_80889E40[this->dyna.actor.params & 0xFF] <
+    if (sTravelDistances[this->dyna.actor.params & 0xFF] <  // was D_80889E40 [LLM:HIGH]
         Math_Vec3f_DistXYZ(&this->dyna.actor.home.pos, &this->dyna.actor.world.pos)) {
         func_80889ACC(this);
         BgHidanKousi_SetupAction(this, func_80889D28);
-- 
2.55.0

