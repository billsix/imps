From e6678244d3270a4805b0017d06025808f2f3b1c6 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:25 +0000
Subject: [PATCH 2394/3799] soh: rename func_809BD3E0 ->
 EnBigokuta_SetupWaitBeforeCircle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: arms the 40-frame pause before the circling begins
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index c1e4e56ac..4c40b602e 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -13,7 +13,8 @@ void EnBigokuta_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_809BD318: loops the idle animation underwater and arms the 121-frame intro wait
 void EnBigokuta_SetupIntroWait(EnBigokuta* this);  // was func_809BD318 [LLM:HIGH]
-void func_809BD3E0(EnBigokuta* this);
+// LLM generated name (HIGH), was func_809BD3E0: arms the 40-frame pause before the circling begins
+void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this);  // was func_809BD3E0 [LLM:HIGH]
 void func_809BDF34(EnBigokuta* this, PlayState* play);
 void func_809BD84C(EnBigokuta* this, PlayState* play);
 void func_809BD8DC(EnBigokuta* this, PlayState* play);
@@ -187,7 +188,7 @@ void EnBigokuta_Init(Actor* thisx, PlayState* play) {
         Actor_ChangeCategory(play, &play->actorCtx, &this->actor, ACTORCAT_PROP);
         EnBigokuta_SetupIntroWait(this);  // was func_809BD318 [LLM:HIGH]
     } else {
-        func_809BD3E0(this);
+        EnBigokuta_SetupWaitBeforeCircle(this);  // was func_809BD3E0 [LLM:HIGH]
         this->unk_19A = 0;
         this->unk_196 = 1;
         this->actor.home.pos.y = -1025.0f;
@@ -306,7 +307,7 @@ void EnBigokuta_SetupTurnToStart(EnBigokuta* this) {
     this->actor.world.pos.y = this->actor.home.pos.y;
 }
 
-void func_809BD3E0(EnBigokuta* this) {
+void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this) {
     this->unk_196 = 40;
     this->actionFunc = func_809BDB90;
 }
@@ -468,7 +469,7 @@ void func_809BDAE8(EnBigokuta* this, PlayState* play) {
             Actor_ChangeCategory(play, &play->actorCtx, &this->actor, ACTORCAT_ENEMY);
             this->actor.params = 2;
             Audio_PlayActorSound2(&this->actor, NA_SE_EN_DAIOCTA_VOICE);
-            func_809BD3E0(this);
+            EnBigokuta_SetupWaitBeforeCircle(this);  // was func_809BD3E0 [LLM:HIGH]
         }
     }
 }
-- 
2.55.0

