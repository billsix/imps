From 85cd4f7b23c3e80e4b6c058c87bed203f879a6ee Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:29:26 +0000
Subject: [PATCH 1583/3799] soh: rename func_80A91284 -> EnKakasi3_PickTextId

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: chooses Pierre's line from Link's age and whether the scarecrow's song has been recorded and replayed, then installs the idle.
Defined in soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
index 522bf68fc..642d0e912 100644
--- a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
+++ b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
@@ -17,7 +17,8 @@ void EnKakasi3_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80A911F0: restarts the looping animation and installs the text-picking state.
 void EnKakasi3_SetupIdle(EnKakasi3* this, PlayState* play);  // was func_80A911F0 [LLM:HIGH]
-void func_80A91284(EnKakasi3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A91284: chooses Pierre's line from Link's age and whether the scarecrow's song has been recorded and replayed, then installs the idle.
+void EnKakasi3_PickTextId(EnKakasi3* this, PlayState* play);  // was func_80A91284 [LLM:HIGH]
 void func_80A91348(EnKakasi3* this, PlayState* play);
 void func_80A915B8(EnKakasi3* this, PlayState* play);
 void func_80A91620(EnKakasi3* this, PlayState* play);
@@ -180,10 +181,10 @@ void EnKakasi3_SetupIdle(EnKakasi3* this, PlayState* play) {
     f32 frameCount = Animation_GetLastFrame(&object_ka_Anim_000214);
 
     Animation_Change(&this->skelAnime, &object_ka_Anim_000214, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
-    this->actionFunc = func_80A91284;
+    this->actionFunc = EnKakasi3_PickTextId;  // was func_80A91284 [LLM:HIGH]
 }
 
-void func_80A91284(EnKakasi3* this, PlayState* play) {
+void EnKakasi3_PickTextId(EnKakasi3* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
 
     this->actor.textId = 0x40A1;
@@ -219,12 +220,12 @@ void func_80A91348(EnKakasi3* this, PlayState* play) {
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
         if (!this->unk_194) {
             if (this->unk_1A8 == 0) {
-                this->actionFunc = func_80A91284;
+                this->actionFunc = EnKakasi3_PickTextId;  // was func_80A91284 [LLM:HIGH]
             } else {
                 this->actionFunc = func_80A91760;
             }
         } else {
-            this->actionFunc = func_80A91284;
+            this->actionFunc = EnKakasi3_PickTextId;  // was func_80A91284 [LLM:HIGH]
         }
     } else {
         s16 angleTowardsLink = this->actor.yawTowardsPlayer - this->actor.shape.rot.y;
-- 
2.55.0

