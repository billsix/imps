From e8cc8c3b143a8bd6eee572032b04fb20c4204c81 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:01 +0000
Subject: [PATCH 1828/3799] soh: rename func_809AA230 ->
 EffectSsKakera_UpdateGrounded

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: stops the fragment dead once it has fallen below the floor, clearing its velocity, acceleration, gravity and spin.
Defined in soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c  | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c b/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
index d0252044e..a2b82a770 100644
--- a/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
+++ b/soh/src/overlays/effects/ovl_Effect_Ss_Kakera/z_eff_ss_kakera.c
@@ -359,7 +359,8 @@ s32 EffectSsKakera_UpdateAccel(EffectSs* this) {
     return true;
 }
 
-void func_809AA230(EffectSs* this, PlayState* play) {
+// LLM generated name (HIGH), was func_809AA230: stops the fragment dead once it has fallen below the floor, clearing its velocity, acceleration, gravity and spin.
+void EffectSsKakera_UpdateGrounded(EffectSs* this, PlayState* play) {
     static f32 D_809AA5B0[] = { 10.0f, 20.0f, 40.0f };
     Player* player = GET_PLAYER(play);
 
@@ -426,7 +427,7 @@ void EffectSsKakera_Update(PlayState* play, u32 index, EffectSs* this) {
         this->life = 0;
     }
 
-    func_809AA230(this, play);
+    EffectSsKakera_UpdateGrounded(this, play);  // was func_809AA230 [LLM:HIGH]
 
     if (this->rObjId != KAKERA_OBJECT_DEFAULT) {
         EffectSsKakera_CheckObjectLoaded(this, play);  // was func_809A9BA8 [LLM:HIGH]
-- 
2.55.0

