From 2405c899ca897e1a3f01f21f706b63d0d6a32d9b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:18 +0000
Subject: [PATCH 1009/3799] soh: rename func_80AA2E54 -> EnMa3_TrackPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: feeds the player's position to Npc_TrackPoint, using NPC_TRACKING_NONE while Malon is idly singing and auto-turn tracking otherwise.
Defined in soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c b/soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c
index fcc6f5e44..3f2c84e44 100644
--- a/soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c
+++ b/soh/src/overlays/actors/ovl_En_Ma3/z_en_ma3.c
@@ -19,7 +19,8 @@ void EnMa3_Draw(Actor* thisx, PlayState* play);
 u16 EnMa3_GetTextId(PlayState* play, Actor* this);
 s16 EnMa3_UpdateTalkState(PlayState* play, Actor* this);
 
-void func_80AA2E54(EnMa3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AA2E54: feeds the player's position to Npc_TrackPoint, using NPC_TRACKING_NONE while Malon is idly singing and auto-turn tracking otherwise.
+void EnMa3_TrackPlayer(EnMa3* this, PlayState* play);  // was func_80AA2E54 [LLM:HIGH]
 s32 func_80AA2EC8(EnMa3* this, PlayState* play);
 s32 func_80AA2F28(EnMa3* this);
 void EnMa3_UpdateEyes(EnMa3* this);
@@ -181,7 +182,7 @@ s16 EnMa3_UpdateTalkState(PlayState* play, Actor* thisx) {
     return ret;
 }
 
-void func_80AA2E54(EnMa3* this, PlayState* play) {
+void EnMa3_TrackPlayer(EnMa3* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s16 trackingMode;
 
@@ -293,7 +294,7 @@ void EnMa3_Update(Actor* thisx, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     EnMa3_UpdateEyes(this);
     this->actionFunc(this, play);
-    func_80AA2E54(this, play);
+    EnMa3_TrackPlayer(this, play);  // was func_80AA2E54 [LLM:HIGH]
     Npc_UpdateTalking(play, &this->actor, &this->interactInfo.talkState, (f32)this->collider.dim.radius + 150.0f,
                       EnMa3_GetTextId, EnMa3_UpdateTalkState);
     if (this->interactInfo.talkState == NPC_TALK_STATE_IDLE) {
-- 
2.55.0

