# The upstream checkout and everything build/run produce are disposable;
# only the scripts (and, later, patches/) are versioned.  runDir/ also
# holds ROM-derived and personal state (oot.o2r, saves) that must never
# be committed.
/Shipwright/
/build-cmake/
/bldInstall/
/runDir/
