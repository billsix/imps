From 429f2d1579c4a13386abff31d04b57ee0a739463 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:36:45 +0000
Subject: [PATCH 1687/3799] soh: rename func_80ABAA9C -> EnNiwLady_DeclineTrade

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: waits out the refusal line and returns to the adult talk setup.
Defined in soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
index f33f09ff6..661390b5a 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
@@ -26,7 +26,8 @@ void EnNiwLady_OfferPocketEgg(EnNiwLady* this, PlayState* play);  // was func_80
 // LLM generated name (HIGH), was func_80ABAB08: resolves the second trade prompt: yes hands over GI_COJIRO, no shows the refusal line and records it.
 void EnNiwLady_OfferCojiro(EnNiwLady* this, PlayState* play);  // was func_80ABAB08 [LLM:HIGH]
 void func_80ABAC00(EnNiwLady* this, PlayState* play);
-void func_80ABAA9C(EnNiwLady* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABAA9C: waits out the refusal line and returns to the adult talk setup.
+void EnNiwLady_DeclineTrade(EnNiwLady* this, PlayState* play);  // was func_80ABAA9C [LLM:HIGH]
 void func_80ABAC84(EnNiwLady* this, PlayState* play);
 // LLM generated name (HIGH), was func_80ABA244: walks the prop actor list each frame, counting the Cuccos inside the pen and recording each one's infTable bit, and picks Anju's line from the total.
 void EnNiwLady_CountCuccos(EnNiwLady* this, PlayState* play);  // was func_80ABA244 [LLM:HIGH]
@@ -419,13 +420,13 @@ void EnNiwLady_OfferPocketEgg(EnNiwLady* this, PlayState* play) {
                 this->unk_26E = this->unk_27A + 21;
                 Message_ContinueTextbox(play, this->actor.textId);
                 this->unk_262 = TEXT_STATE_EVENT;
-                this->actionFunc = func_80ABAA9C;
+                this->actionFunc = EnNiwLady_DeclineTrade;  // was func_80ABAA9C [LLM:HIGH]
                 break;
         }
     }
 }
 
-void func_80ABAA9C(EnNiwLady* this, PlayState* play) {
+void EnNiwLady_DeclineTrade(EnNiwLady* this, PlayState* play) {
     this->unk_26E = 11;
     if ((this->unk_262 == Message_GetState(&play->msgCtx)) && Message_ShouldAdvance(play)) {
         Message_CloseTextbox(play);
@@ -451,7 +452,7 @@ void EnNiwLady_OfferCojiro(EnNiwLady* this, PlayState* play) {
                 this->unk_26E = this->unk_27A + 21;
                 Message_ContinueTextbox(play, this->actor.textId);
                 this->unk_262 = TEXT_STATE_EVENT;
-                this->actionFunc = func_80ABAA9C;
+                this->actionFunc = EnNiwLady_DeclineTrade;  // was func_80ABAA9C [LLM:HIGH]
                 break;
         }
     }
-- 
2.55.0

