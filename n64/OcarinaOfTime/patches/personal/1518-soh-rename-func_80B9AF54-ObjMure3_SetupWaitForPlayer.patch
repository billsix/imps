From 0a951862936a2411861d7b7072fdacb075c7604a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:24:26 +0000
Subject: [PATCH 1518/3799] soh: rename func_80B9AF54 ->
 ObjMure3_SetupWaitForPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: installs the action that watches for the player approaching.
Defined in soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
index 28f5a22de..c4e4a6128 100644
--- a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
+++ b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
@@ -16,7 +16,8 @@ void ObjMure3_Update(Actor* thisx, PlayState* play);
 void ObjMure3_SetupIdle(ObjMure3* this);  // was func_80B9AF24 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B9AF34: falls straight through to the waiting setup on its first frame.
 void ObjMure3_Idle(ObjMure3* this, PlayState* play);  // was func_80B9AF34 [LLM:HIGH]
-void func_80B9AF54(ObjMure3* this);
+// LLM generated name (HIGH), was func_80B9AF54: installs the action that watches for the player approaching.
+void ObjMure3_SetupWaitForPlayer(ObjMure3* this);  // was func_80B9AF54 [LLM:HIGH]
 void func_80B9AF64(ObjMure3* this, PlayState* play);
 void func_80B9AFEC(ObjMure3* this);
 void func_80B9AFFC(ObjMure3* this, PlayState* play);
@@ -168,10 +169,10 @@ void ObjMure3_SetupIdle(ObjMure3* this) {
 }
 
 void ObjMure3_Idle(ObjMure3* this, PlayState* play) {
-    func_80B9AF54(this);
+    ObjMure3_SetupWaitForPlayer(this);  // was func_80B9AF54 [LLM:HIGH]
 }
 
-void func_80B9AF54(ObjMure3* this) {
+void ObjMure3_SetupWaitForPlayer(ObjMure3* this) {
     this->actionFunc = func_80B9AF64;
 }
 
@@ -194,7 +195,7 @@ void func_80B9AFFC(ObjMure3* this, PlayState* play) {
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) >= SQ(1450.0f)) {
         this->actor.flags &= ~ACTOR_FLAG_UPDATE_CULLING_DISABLED;
         ObjMure3_DespawnRupees(this, play);  // was func_80B9ACE4 [LLM:HIGH]
-        func_80B9AF54(this);
+        ObjMure3_SetupWaitForPlayer(this);  // was func_80B9AF54 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

