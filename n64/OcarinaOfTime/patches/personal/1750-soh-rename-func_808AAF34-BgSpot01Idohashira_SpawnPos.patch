From 8186b62b7a94eb7d06797908c8c4a4e071c01902 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:42 +0000
Subject: [PATCH 1750/3799] soh: rename func_808AAF34 ->
 BgSpot01Idohashira_SpawnPostEffects

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: while the arch is falling, throws a smaller dust cloud and spark burst at each of its two posts, transformed into world space by the current matrix.
Defined in soh/src/overlays/actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c b/soh/src/overlays/actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c
index 8f8282849..800038b49 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot01_Idohashira/z_bg_spot01_idohashira.c
@@ -118,7 +118,8 @@ void BgSpot01Idohashira_SpawnBreakEffects(BgSpot01Idohashira* this, PlayState* p
     BgSpot01Idohashira_PlayBreakSfx2(this, play);
 }
 
-void func_808AAF34(BgSpot01Idohashira* this, PlayState* play) {
+// LLM generated name (HIGH), was func_808AAF34: while the arch is falling, throws a smaller dust cloud and spark burst at each of its two posts, transformed into world space by the current matrix.
+void BgSpot01Idohashira_SpawnPostEffects(BgSpot01Idohashira* this, PlayState* play) {
     s32 pad[2];
     Vec3f dest;
     Vec3f src;
@@ -327,7 +328,7 @@ void func_808AB700(BgSpot01Idohashira* this, PlayState* play) {
     Gfx_SetupDL_25Opa(localGfxCtx);
 
     gSPMatrix(POLY_OPA_DISP++, MATRIX_NEWMTX(localGfxCtx), G_MTX_NOPUSH | G_MTX_LOAD | G_MTX_MODELVIEW);
-    func_808AAF34(this, play);
+    BgSpot01Idohashira_SpawnPostEffects(this, play);  // was func_808AAF34 [LLM:HIGH]
     gSPDisplayList(POLY_OPA_DISP++, gKakarikoWellArchDL);
 
     CLOSE_DISPS(localGfxCtx);
-- 
2.55.0

