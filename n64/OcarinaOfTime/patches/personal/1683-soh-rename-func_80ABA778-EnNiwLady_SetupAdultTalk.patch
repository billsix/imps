From ca7991ddb83167ee30bbf0611ed549e6456e90cf Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:36:45 +0000
Subject: [PATCH 1683/3799] soh: rename func_80ABA778 ->
 EnNiwLady_SetupAdultTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: picks the adult-era line from how far the Pocket Egg and Cojiro trade has progressed and installs the adult talk action.
Defined in soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/soh/Enhancements/randomizer/hook_handlers.cpp   |  6 +++---
 .../overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c | 13 +++++++------
 2 files changed, 10 insertions(+), 9 deletions(-)

diff --git a/soh/soh/Enhancements/randomizer/hook_handlers.cpp b/soh/soh/Enhancements/randomizer/hook_handlers.cpp
index add4baa40..5f0ec83d3 100644
--- a/soh/soh/Enhancements/randomizer/hook_handlers.cpp
+++ b/soh/soh/Enhancements/randomizer/hook_handlers.cpp
@@ -76,7 +76,7 @@ extern void Player_SetupActionPreserveAnimMovement(PlayState* play, Player* play
 extern s32 Player_SetupWaitForPutAway(PlayState* play, Player* player, AfterPutAwayFunc func);
 extern void Play_InitEnvironment(PlayState* play, s16 skyboxId);
 extern void EnMk_Wait(EnMk* enMk, PlayState* play);
-extern void func_80ABA778(EnNiwLady* enNiwLady, PlayState* play);
+extern void EnNiwLady_SetupAdultTalk(EnNiwLady* enNiwLady, PlayState* play);  // was func_80ABA778 [LLM:HIGH]
 extern void EnDntJiji_GivePrize(EnDntJiji* enDntJiji, PlayState* play);
 extern void EnGe1_Wait_Archery(EnGe1* enGe1, PlayState* play);
 extern void EnGe1_SetAnimationIdle(EnGe1* enGe1);
@@ -1409,7 +1409,7 @@ void RandomizerOnVanillaBehaviorHandler(GIVanillaBehavior id, bool* should, va_l
         case VB_GIVE_ITEM_FROM_ANJU_AS_ADULT: {
             EnNiwLady* enNiwLady = va_arg(args, EnNiwLady*);
             Flags_SetItemGetInf(ITEMGETINF_2C);
-            enNiwLady->actionFunc = func_80ABA778;
+            enNiwLady->actionFunc = EnNiwLady_SetupAdultTalk;  // was func_80ABA778 [LLM:HIGH]
             *should = false;
             break;
         }
@@ -1532,7 +1532,7 @@ void RandomizerOnVanillaBehaviorHandler(GIVanillaBehavior id, bool* should, va_l
             Inventory_ReplaceItem(gPlayState, ITEM_POCKET_CUCCO, Randomizer_GetNextAdultTradeItem());
             // Trigger the reward now
             Flags_SetItemGetInf(ITEMGETINF_2E);
-            enNiwLady->actionFunc = func_80ABA778;
+            enNiwLady->actionFunc = EnNiwLady_SetupAdultTalk;  // was func_80ABA778 [LLM:HIGH]
 
             *should = false;
             break;
diff --git a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
index 70d7bf03d..314f1876a 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
@@ -17,7 +17,8 @@ void EnNiwLady_Draw(Actor* thisx, PlayState* play);
 // LLM generated name (HIGH), was func_80ABA21C: arms the missing-Cucco line and installs the counting action.
 void EnNiwLady_SetupCuccoCount(EnNiwLady* this, PlayState* play);  // was func_80ABA21C [LLM:HIGH]
 void func_80ABAD38(EnNiwLady* this, PlayState* play);
-void func_80ABA778(EnNiwLady* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABA778: picks the adult-era line from how far the Pocket Egg and Cojiro trade has progressed and installs the adult talk action.
+void EnNiwLady_SetupAdultTalk(EnNiwLady* this, PlayState* play);  // was func_80ABA778 [LLM:HIGH]
 void func_80ABA878(EnNiwLady* this, PlayState* play);
 void func_80ABA9B8(EnNiwLady* this, PlayState* play);
 void func_80ABAB08(EnNiwLady* this, PlayState* play);
@@ -183,7 +184,7 @@ void EnNiwLady_WaitForObjects(EnNiwLady* this, PlayState* play) {
                     Animation_Change(&this->skelAnime, &gObjOsAnim_07D0, 1.0f, 0.0f, (s16)frames, ANIMMODE_LOOP, 0.0f);
                 }
                 if (LINK_IS_ADULT) {
-                    this->actionFunc = func_80ABA778;
+                    this->actionFunc = EnNiwLady_SetupAdultTalk;  // was func_80ABA778 [LLM:HIGH]
                 } else {
                     this->actionFunc = EnNiwLady_SetupCuccoCount;  // was func_80ABA21C [LLM:HIGH]
                 }
@@ -338,7 +339,7 @@ void EnNiwLady_FinishCuccoTalk(EnNiwLady* this, PlayState* play) {
 static s16 sTradeItemTextIds[] = { 0x503E, 0x503F, 0x5047, 0x5040, 0x5042, 0x5043,
                                    0x5044, 0x00CF, 0x5045, 0x5042, 0x5027 };
 
-void func_80ABA778(EnNiwLady* this, PlayState* play) {
+void EnNiwLady_SetupAdultTalk(EnNiwLady* this, PlayState* play) {
     // "☆☆☆☆☆ Adult message check ☆☆☆☆☆"
     osSyncPrintf(VT_FGCOL(GREEN) "☆☆☆☆☆ アダルトメッセージチェック ☆☆☆☆☆ \n" VT_RST);
     this->unk_262 = TEXT_STATE_DONE;
@@ -390,7 +391,7 @@ void func_80ABA878(EnNiwLady* this, PlayState* play) {
         } else {
             this->unk_274 = 1;
             this->unk_26E = this->unk_27A + 21;
-            this->actionFunc = !this->unk_273 ? func_80ABA778 : func_80ABA9B8;
+            this->actionFunc = !this->unk_273 ? EnNiwLady_SetupAdultTalk : func_80ABA9B8;  // was func_80ABA778 [LLM:HIGH]
         }
     } else {
         Actor_OfferTalkExchangeEquiCylinder(&this->actor, play, 50.0f, 6);
@@ -425,7 +426,7 @@ void func_80ABAA9C(EnNiwLady* this, PlayState* play) {
     this->unk_26E = 11;
     if ((this->unk_262 == Message_GetState(&play->msgCtx)) && Message_ShouldAdvance(play)) {
         Message_CloseTextbox(play);
-        this->actionFunc = func_80ABA778;
+        this->actionFunc = EnNiwLady_SetupAdultTalk;  // was func_80ABA778 [LLM:HIGH]
     }
 }
 
@@ -478,7 +479,7 @@ void func_80ABAC84(EnNiwLady* this, PlayState* play) {
         } else {
             Flags_SetItemGetInf(ITEMGETINF_2E);
         }
-        this->actionFunc = func_80ABA778;
+        this->actionFunc = EnNiwLady_SetupAdultTalk;  // was func_80ABA778 [LLM:HIGH]
     } else {
         Flags_SetItemGetInf(ITEMGETINF_0C);
         this->unk_262 = TEXT_STATE_DONE;
-- 
2.55.0

