From a1823d121bd45bccc4f044500ffe73470ebd33fa Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:42:36 +0000
Subject: [PATCH 1768/3799] soh: rename func_800B404C ->
 DbCamera_PlayerRelSToWorld

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the Vec3s-taking wrapper for DbCamera_PlayerRelToWorld.
Defined in soh/src/code/db_camera.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h  |  2 +-
 soh/src/code/db_camera.c | 23 ++++++++++++-----------
 2 files changed, 13 insertions(+), 12 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 8ebedf8b0..b15b887b0 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1489,7 +1489,7 @@ void ShrinkWindow_Update(s32 updateRate);
 // ? DbCamera_Vec3SToF2(?);
 // ? DbCamera_WorldToPlayerRelS(?);, was func_800B3F94 [LLM:HIGH]
 // ? DbCamera_PlayerRelToWorld(?);, was func_800B3FF4 [LLM:HIGH]
-// ? func_800B404C(?);
+// ? DbCamera_PlayerRelSToWorld(?);, was func_800B404C [LLM:HIGH]
 // ? func_800B4088(?);
 // ? func_800B41DC(?);
 // ? func_800B42C0(?);
diff --git a/soh/src/code/db_camera.c b/soh/src/code/db_camera.c
index a050c3ee4..a8e62ebe4 100644
--- a/soh/src/code/db_camera.c
+++ b/soh/src/code/db_camera.c
@@ -174,7 +174,8 @@ void DbCamera_PlayerRelToWorld(PosRot* posRot, Vec3f* vec, Vec3f* out) {
     DbCamera_AddVecSph(out, &posRot->pos, &sph);
 }
 
-void func_800B404C(PosRot* posRot, Vec3s* vec, Vec3f* out) {
+// LLM generated name (HIGH), was func_800B404C: the Vec3s-taking wrapper for DbCamera_PlayerRelToWorld.
+void DbCamera_PlayerRelSToWorld(PosRot* posRot, Vec3s* vec, Vec3f* out) {
     Vec3f tempVec;
     DbCamera_Vec3SToF(vec, &tempVec);
     DbCamera_PlayerRelToWorld(posRot, &tempVec, out);  // was func_800B3FF4 [LLM:HIGH]
@@ -220,8 +221,8 @@ s16 func_800B41DC(DbCamera* dbCamera, s16 idx, Camera* cameraPtr) {
         DbCamera_Vec3SToF2(&position->pos, &dbCamera->eye);
         DbCamera_Vec3SToF2(&lookAt->pos, &dbCamera->at);
     } else {
-        func_800B404C(&cameraPtr->playerPosRot, &lookAt->pos, &dbCamera->at);
-        func_800B404C(&cameraPtr->playerPosRot, &position->pos, &dbCamera->eye);
+        DbCamera_PlayerRelSToWorld(&cameraPtr->playerPosRot, &lookAt->pos, &dbCamera->at);  // was func_800B404C [LLM:HIGH]
+        DbCamera_PlayerRelSToWorld(&cameraPtr->playerPosRot, &position->pos, &dbCamera->eye);  // was func_800B404C [LLM:HIGH]
     }
 
     dbCamera->roll = lookAt->cameraRoll;
@@ -262,9 +263,9 @@ s32 func_800B4370(DbCamera* dbCamera, s16 idx, Camera* cam) {
         }
     } else {
         if (dbCamera->sub.unk_0C) {
-            func_800B404C(&cam->playerPosRot, &position->pos, &at);
+            DbCamera_PlayerRelSToWorld(&cam->playerPosRot, &position->pos, &at);  // was func_800B404C [LLM:HIGH]
         } else {
-            func_800B404C(&cam->playerPosRot, &lookAt->pos, &at);
+            DbCamera_PlayerRelSToWorld(&cam->playerPosRot, &lookAt->pos, &at);  // was func_800B404C [LLM:HIGH]
         }
         dbCamera->at = at;
     }
@@ -1101,9 +1102,9 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                                     }
                                 } else {
                                     for (i = 0; i < dbCamera->sub.nPoints; i++) {
-                                        func_800B404C(temp_s6, &dbCamera->sub.lookAt[i].pos, &spD8);
+                                        DbCamera_PlayerRelSToWorld(temp_s6, &dbCamera->sub.lookAt[i].pos, &spD8);  // was func_800B404C [LLM:HIGH]
                                         DbCamera_Vec3FToS(&spD8, &dbCamera->sub.lookAt[i].pos);
-                                        func_800B404C(temp_s6, &dbCamera->sub.position[i].pos, &spD8);
+                                        DbCamera_PlayerRelSToWorld(temp_s6, &dbCamera->sub.position[i].pos, &spD8);  // was func_800B404C [LLM:HIGH]
                                         DbCamera_Vec3FToS(&spD8, &dbCamera->sub.position[i].pos);
                                     }
                                 }
@@ -1190,9 +1191,9 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                                     }
                                 } else {
                                     for (i = 0; i < dbCamera->sub.nPoints; i++) {
-                                        func_800B404C(temp_s6, &dbCamera->sub.lookAt[i].pos, &spD8);
+                                        DbCamera_PlayerRelSToWorld(temp_s6, &dbCamera->sub.lookAt[i].pos, &spD8);  // was func_800B404C [LLM:HIGH]
                                         DbCamera_Vec3FToS(&spD8, &dbCamera->sub.lookAt[i].pos);
-                                        func_800B404C(temp_s6, &dbCamera->sub.position[i].pos, &spD8);
+                                        DbCamera_PlayerRelSToWorld(temp_s6, &dbCamera->sub.position[i].pos, &spD8);  // was func_800B404C [LLM:HIGH]
                                         DbCamera_Vec3FToS(&spD8, &dbCamera->sub.position[i].pos);
                                     }
                                 }
@@ -1343,8 +1344,8 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                         DbCamera_Vec3SToF2(&(dbCamera->sub.position + i)->pos, &spAC);
                         DbCamera_Vec3SToF2(&(dbCamera->sub.lookAt + i)->pos, &spB8);
                     } else {
-                        func_800B404C(temp_s6, &(dbCamera->sub.lookAt + i)->pos, &spB8);
-                        func_800B404C(temp_s6, &(dbCamera->sub.position + i)->pos, &spAC);
+                        DbCamera_PlayerRelSToWorld(temp_s6, &(dbCamera->sub.lookAt + i)->pos, &spB8);  // was func_800B404C [LLM:HIGH]
+                        DbCamera_PlayerRelSToWorld(temp_s6, &(dbCamera->sub.position + i)->pos, &spAC);  // was func_800B404C [LLM:HIGH]
                     }
                     OLib_Vec3fDiffToVecSphGeo(&spFC, &spAC, &spB8);
                     spAA = dbCamera->sub.lookAt[i].cameraRoll * 0xB6;
-- 
2.55.0

