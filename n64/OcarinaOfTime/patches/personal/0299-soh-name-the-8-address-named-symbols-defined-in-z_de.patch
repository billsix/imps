From 9f0fb5a95b2541caf1cb7b1bb4a90ed59a45f515 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:24 +0000
Subject: [PATCH 299/488] soh: name the 8 address-named symbols defined in
 z_debug.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 8 are defined in soh/src/code/z_debug.c; updating their references touches 5 files in total.

  * func_8006376C -> DebugCamera_CopyText
    deduced, confidence HIGH: the in-tree comment above it reads "Copy
    Camera Debugger Text"; it appends a position, colour and up to 21
    characters into the print-text buffer for the draw pass to flush.

  * func_80063828 -> DebugCamera_DrawText
    deduced, confidence HIGH: the in-tree comment above it reads "Draw
    Text"; it walks exactly the buffer DebugCamera_CopyText fills and prints
    each entry in its colour at its position.

  * func_8006390C -> Debug_EditRegs
    deduced, confidence HIGH: the in-tree comment above it reads "Edit REG";
    it reads the L/R plus C-button combos in inputCombos to pick a register
    group and adjust the selected register.

  * func_80063C04 -> Debug_DrawRegEditor
    deduced, confidence HIGH: prints the current page of the register
    editor, one line per register, highlighting the selected one.

  * func_80063D7C -> Debug_DrawOverlay
    deduced, confidence HIGH: the top-level debug draw: it sets up a
    GfxPrint and emits the register editor and the buffered camera-debugger
    text, gated on the DebugEnabled developer CVar.

  * D_8015FA94 -> sUnusedDebugValue
    deduced, confidence HIGH: the in-tree comment on its declaration reads
    "// no known symbols"; nothing reads or writes it.

  * D_8015FA98 -> sPrintTextBuffers
    deduced, confidence HIGH: the 0x16-entry PrintTextBuffer array that
    DebugCamera_CopyText fills and DebugCamera_DrawText prints.

  * D_8011E0B0 -> sPrintTextIndex
    deduced, confidence HIGH: the in-tree comment on its declaration reads
    "PrintTextBuffer index"; it is the write cursor into sPrintTextBuffers
    and the count the draw pass stops at.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/include/functions.h  |  10 +-
 soh/src/code/db_camera.c | 228 +++++++++++++++++++--------------------
 soh/src/code/game.c      |   4 +-
 soh/src/code/z_camera.c  |  20 ++--
 soh/src/code/z_debug.c   |  42 +++++---
 5 files changed, 156 insertions(+), 148 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index fe7b8c161..2c2265750 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -852,11 +852,11 @@ void SaveContext_Init(void);
 s32 func_800635D0(s32);
 void Regs_Init(void);  // was func_800636C0 [oot]
 void DebugCamera_ScreenText(s32 arg0, s32 arg1, const char* text);  // was func_8006375C [oot]
-void func_8006376C(u8 x, u8 y, u8 colorId, const char* text);
-// ? func_80063828(?);
-void func_8006390C(Input* input);
-// ? func_80063C04(?);
-void func_80063D7C(GraphicsContext* gfxCtx);
+void DebugCamera_CopyText(u8 x, u8 y, u8 colorId, const char* text);  // was func_8006376C [LLM:HIGH]
+// ? DebugCamera_DrawText(?);, was func_80063828 [LLM:HIGH]
+void Debug_EditRegs(Input* input);  // was func_8006390C [LLM:HIGH]
+// ? Debug_DrawRegEditor(?);, was func_80063C04 [LLM:HIGH]
+void Debug_DrawOverlay(GraphicsContext* gfxCtx);  // was func_80063D7C [LLM:HIGH]
 void DebugDisplay_Init(void);
 DebugDispObject* DebugDisplay_AddObject(f32 posX, f32 posY, f32 posZ, s16 rotX, s16 rotY, s16 rotZ, f32 scaleX,
                                         f32 scaleY, f32 scaleZ, u8 red, u8 green, u8 blue, u8 alpha, s16 type,
diff --git a/soh/src/code/db_camera.c b/soh/src/code/db_camera.c
index 9982ad570..c90ec8d9c 100644
--- a/soh/src/code/db_camera.c
+++ b/soh/src/code/db_camera.c
@@ -297,9 +297,9 @@ void func_800B44E0(DbCamera* dbCamera, Camera* cam) {
                                  &gSfxDefaultFreqAndVolScale, &gSfxDefaultReverb);
             sDbCamAnim.unk_0A = 0;
         }
-        func_8006376C(0x0D, 0x17, 3, cameraStrings[0]);
-        func_8006376C(0x0E, 0x18, 3, cameraStrings[1]);
-        func_8006376C(0x0B, 0x1A, 1, cameraStrings[2]);
+        DebugCamera_CopyText(0x0D, 0x17, 3, cameraStrings[0]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x0E, 0x18, 3, cameraStrings[1]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x0B, 0x1A, 1, cameraStrings[2]);  // was func_8006376C [LLM:HIGH]
         return;
     }
 
@@ -314,7 +314,7 @@ void func_800B44E0(DbCamera* dbCamera, Camera* cam) {
 
         if (dbCamera->sub.nFrames > 0 && dbCamera->sub.nFrames < sDbCamAnim.unk_04) {
             sDbCamAnim.unk_0A = 0;
-            func_8006376C(0xF, 0x1A, 1, cameraStrings[3]);
+            DebugCamera_CopyText(0xF, 0x1A, 1, cameraStrings[3]);  // was func_8006376C [LLM:HIGH]
         }
 
         if (dbCamera->sub.mode != 1) {
@@ -330,18 +330,18 @@ void func_800B44E0(DbCamera* dbCamera, Camera* cam) {
         dbCamera->rollDegrees = sDbCamAnim.roll * (360.0f / 256.0f);
 
         DbCamera_SetTextValue(sDbCamAnim.unk_04, &cameraStrings[53][6], 4);
-        func_8006376C(0x10, 0x17, 3, cameraStrings[53]);
+        DebugCamera_CopyText(0x10, 0x17, 3, cameraStrings[53]);  // was func_8006376C [LLM:HIGH]
         cameraStrings[54][4] = ((sDbCamAnim.keyframe + 1) / 10) + '0';
         cameraStrings[54][5] = ((sDbCamAnim.keyframe + 1) % 10) + '0';
         cameraStrings[54][7] = ((dbCamera->sub.nPoints - 5) / 10) + '0';
         cameraStrings[54][8] = ((dbCamera->sub.nPoints - 5) % 10) + '0';
-        func_8006376C(0x10, 0x18, 3, cameraStrings[54]);
-        func_8006376C(0x10, 0x1A, 1, cameraStrings[4]);
+        DebugCamera_CopyText(0x10, 0x18, 3, cameraStrings[54]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x10, 0x1A, 1, cameraStrings[4]);  // was func_8006376C [LLM:HIGH]
         return;
     }
 
     sDbCamAnim.unk_0A = 0;
-    func_8006376C(0xF, 0x1A, 1, cameraStrings[3]);
+    DebugCamera_CopyText(0xF, 0x1A, 1, cameraStrings[3]);  // was func_8006376C [LLM:HIGH]
 }
 
 void DbCamera_PrintPoints(const char* name, s16 count, CutsceneCameraPoint* points) {
@@ -921,7 +921,7 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
 
     if (dbCamera->unk_00 == 1) {
         OREG(0) = 8;
-        func_8006376C(0xC, 5, 0, cameraStrings[5]);
+        DebugCamera_CopyText(0xC, 5, 0, cameraStrings[5]);  // was func_8006376C [LLM:HIGH]
         if (CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_CRIGHT) &&
             !CHECK_BTN_ALL(sPlayState->state.input[2].cur.button, BTN_L)) {
             func_800B44E0(dbCamera, cam);
@@ -1007,21 +1007,21 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 }
             }
 
-            func_8006376C(0xA, 6, (dbCamera->sub.unk_08 == 0) ? 7 : 4, cameraStrings[58]);
-            func_8006376C(0x11, 6, (dbCamera->sub.unk_08 == 1) ? 7 : 4, cameraStrings[59]);
-            func_8006376C(0x17, 6, (dbCamera->sub.unk_08 == 2) ? 7 : 4, cameraStrings[60]);
+            DebugCamera_CopyText(0xA, 6, (dbCamera->sub.unk_08 == 0) ? 7 : 4, cameraStrings[58]);  // was func_8006376C [LLM:HIGH]
+            DebugCamera_CopyText(0x11, 6, (dbCamera->sub.unk_08 == 1) ? 7 : 4, cameraStrings[59]);  // was func_8006376C [LLM:HIGH]
+            DebugCamera_CopyText(0x17, 6, (dbCamera->sub.unk_08 == 2) ? 7 : 4, cameraStrings[60]);  // was func_8006376C [LLM:HIGH]
             if (dbCamera->sub.unkIdx == 0x80) {
-                func_8006376C(0x10, 0x1A, 1, cameraStrings[6]);
+                DebugCamera_CopyText(0x10, 0x1A, 1, cameraStrings[6]);  // was func_8006376C [LLM:HIGH]
             } else if (dbCamera->sub.unkIdx == (dbCamera->sub.nPoints - 1)) {
                 cameraStrings[7][8] = (dbCamera->sub.nPoints / 10) + '0';
                 cameraStrings[7][9] = (dbCamera->sub.nPoints % 10) + '0';
-                func_8006376C(0xF, 0x1A, 1, cameraStrings[7]);
+                DebugCamera_CopyText(0xF, 0x1A, 1, cameraStrings[7]);  // was func_8006376C [LLM:HIGH]
             } else {
                 cameraStrings[8][4] = ((dbCamera->sub.unkIdx + 1) / 10) + '0';
                 cameraStrings[8][5] = ((dbCamera->sub.unkIdx + 1) % 10) + '0';
                 cameraStrings[8][7] = ((dbCamera->sub.nPoints - 1) / 10) + '0';
                 cameraStrings[8][8] = ((dbCamera->sub.nPoints - 1) % 10) + '0';
-                func_8006376C(0xF, 0x1A, 1, cameraStrings[8]);
+                DebugCamera_CopyText(0xF, 0x1A, 1, cameraStrings[8]);  // was func_8006376C [LLM:HIGH]
             }
 
             switch (dbCamera->sub.unk_08) {
@@ -1031,25 +1031,25 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                 case 0:
                     dbCamera->unk_3C = false;
                     if (dbCamera->sub.mode != 1) {
-                        func_8006376C(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);
+                        DebugCamera_CopyText(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(sp104.pitch * 0.00549325f, &cameraStrings[69][6], 4);
-                        func_8006376C(0xF, 0x17, 3, cameraStrings[69]);
+                        DebugCamera_CopyText(0xF, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(sp104.yaw * 0.00549325f, &cameraStrings[70][6], 4);
-                        func_8006376C(0xF, 0x18, 3, cameraStrings[70]);
+                        DebugCamera_CopyText(0xF, 0x18, 3, cameraStrings[70]);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(sp104.r, &cameraStrings[68][5], 5);
-                        func_8006376C(0xF, 0x19, 3, cameraStrings[68]);
+                        DebugCamera_CopyText(0xF, 0x19, 3, cameraStrings[68]);  // was func_8006376C [LLM:HIGH]
                     } else {
-                        func_8006376C(0xE, 0x18, 3, cameraStrings[11]);
-                        func_8006376C(0x10, 0x16, 3, cameraStrings[12]);
+                        DebugCamera_CopyText(0xE, 0x18, 3, cameraStrings[11]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x10, 0x16, 3, cameraStrings[12]);  // was func_8006376C [LLM:HIGH]
                         sp110 = 'X';
                         DbCamera_SetTextValue(temp_s6->pos.x, &sp111, 7);
-                        func_8006376C(0x10, 0x17, 3, &sp110);
+                        DebugCamera_CopyText(0x10, 0x17, 3, &sp110);  // was func_8006376C [LLM:HIGH]
                         sp110 = 'Y';
                         DbCamera_SetTextValue(temp_s6->pos.y, &sp111, 7);
-                        func_8006376C(0x10, 0x18, 3, &sp110);
+                        DebugCamera_CopyText(0x10, 0x18, 3, &sp110);  // was func_8006376C [LLM:HIGH]
                         sp110 = 'Z';
                         DbCamera_SetTextValue(temp_s6->pos.z, &sp111, 7);
-                        func_8006376C(0x10, 0x19, 3, &sp110);
+                        DebugCamera_CopyText(0x10, 0x19, 3, &sp110);  // was func_8006376C [LLM:HIGH]
                     }
                     break;
                 case 1:
@@ -1237,14 +1237,14 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
 
                     DbCamera_SetTextValue(dbCamera->sub.lookAt[dbCamera->sub.unkIdx].viewAngle, &cameraStrings[62][10],
                                           3);
-                    func_8006376C(0x0E, 0x14, (dbCamera->sub.unk_0A == 0) ? 7 : 4, cameraStrings[62]);
+                    DebugCamera_CopyText(0x0E, 0x14, (dbCamera->sub.unk_0A == 0) ? 7 : 4, cameraStrings[62]);  // was func_8006376C [LLM:HIGH]
                     DbCamera_SetTextValue(dbCamera->sub.lookAt[dbCamera->sub.unkIdx].nextPointFrame,
                                           &cameraStrings[63][10], 3);
-                    func_8006376C(0x0E, 0x15, (dbCamera->sub.unk_0A == 1) ? 7 : 4, cameraStrings[63]);
+                    DebugCamera_CopyText(0x0E, 0x15, (dbCamera->sub.unk_0A == 1) ? 7 : 4, cameraStrings[63]);  // was func_8006376C [LLM:HIGH]
                     DbCamera_SetTextValue(dbCamera->sub.lookAt[dbCamera->sub.unkIdx].cameraRoll, &cameraStrings[64][10],
                                           3);
-                    func_8006376C(0x0E, 0x16, (dbCamera->sub.unk_0A == 2) ? 7 : 4, cameraStrings[64]);
-                    func_8006376C(0xF, 0x17, (dbCamera->sub.unk_0A == 3) ? 7 : 4,
+                    DebugCamera_CopyText(0x0E, 0x16, (dbCamera->sub.unk_0A == 2) ? 7 : 4, cameraStrings[64]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0xF, 0x17, (dbCamera->sub.unk_0A == 3) ? 7 : 4,  // was func_8006376C [LLM:HIGH]
                                   (dbCamera->sub.mode == 1)   ? cameraStrings[13]
                                   : (dbCamera->sub.mode == 0) ? cameraStrings[14]
                                                               : cameraStrings[52]);
@@ -1263,69 +1263,69 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
                         cameraStrings[66][10] = '<';
                     }
                     cameraStrings[66][11] = '\0';
-                    func_8006376C(0xF, 0x18, (dbCamera->sub.unk_0A == 4) ? 7 : 4, cameraStrings[66]);
+                    DebugCamera_CopyText(0xF, 0x18, (dbCamera->sub.unk_0A == 4) ? 7 : 4, cameraStrings[66]);  // was func_8006376C [LLM:HIGH]
                     DbCamera_SetTextValue(dbCamera->sub.nFrames, &cameraStrings[67][10], 5);
-                    func_8006376C(0xF, 0x19, (dbCamera->sub.unk_0A == 5) ? 7 : 4,
+                    DebugCamera_CopyText(0xF, 0x19, (dbCamera->sub.unk_0A == 5) ? 7 : 4,  // was func_8006376C [LLM:HIGH]
                                   (dbCamera->sub.nFrames == -1) ? cameraStrings[17] : cameraStrings[67]);
                     break;
             }
 
             if (dbCamera->sub.mode != 1) {
-                func_8006376C(3, 0x16,
+                DebugCamera_CopyText(3, 0x16,  // was func_8006376C [LLM:HIGH]
                               ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !D_80161144) ? 7
                               : !D_80161144                                                               ? 4
                                                                                                           : 3,
                               cameraStrings[20]);
                 sp110 = 'X';
                 DbCamera_SetTextValue(dbCamera->at.x, &sp111, 6);
-                func_8006376C(3, 0x17, 2, &sp110);
+                DebugCamera_CopyText(3, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
                 sp110 = 'Y';
                 DbCamera_SetTextValue(dbCamera->at.y, &sp111, 6);
-                func_8006376C(3, 0x18, 2, &sp110);
+                DebugCamera_CopyText(3, 0x18, 2, &sp110);  // was func_8006376C [LLM:HIGH]
                 sp110 = 'Z';
                 DbCamera_SetTextValue(dbCamera->at.z, &sp111, 6);
-                func_8006376C(3, 0x19, 2, &sp110);
-                func_8006376C(0x1E, 0x16,
+                DebugCamera_CopyText(3, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
+                DebugCamera_CopyText(0x1E, 0x16,  // was func_8006376C [LLM:HIGH]
                               ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && D_80161144) ? 7
                               : D_80161144                                                               ? 4
                                                                                                          : 3,
                               cameraStrings[21]);
                 sp110 = 'X';
                 DbCamera_SetTextValue(dbCamera->eye.x, &sp111, 6);
-                func_8006376C(0x1E, 0x17, 2, &sp110);
+                DebugCamera_CopyText(0x1E, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
                 sp110 = 'Y';
                 DbCamera_SetTextValue(dbCamera->eye.y, &sp111, 6);
-                func_8006376C(0x1E, 0x18, 2, &sp110);
+                DebugCamera_CopyText(0x1E, 0x18, 2, &sp110);  // was func_8006376C [LLM:HIGH]
                 sp110 = 'Z';
                 DbCamera_SetTextValue(dbCamera->eye.z, &sp111, 6);
-                func_8006376C(0x1E, 0x19, 2, &sp110);
+                DebugCamera_CopyText(0x1E, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
             } else {
                 OLib_Vec3fDiffToVecSphGeo(&spFC, sp90, sp7C);
                 spFC.yaw -= cam->playerPosRot.rot.y;
-                func_8006376C(3, 0x16,
+                DebugCamera_CopyText(3, 0x16,  // was func_8006376C [LLM:HIGH]
                               ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && !D_80161144) ? 7
                               : !D_80161144                                                               ? 4
                                                                                                           : 3,
                               cameraStrings[20]);
                 DbCamera_SetTextValue(spFC.pitch * 0.00549325f, &cameraStrings[69][5], 4);
-                func_8006376C(3, 0x17, 3, cameraStrings[69]);
+                DebugCamera_CopyText(3, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
                 DbCamera_SetTextValue(spFC.yaw * 0.00549325f, &cameraStrings[70][5], 4);
-                func_8006376C(3, 0x18, 3, cameraStrings[70]);
+                DebugCamera_CopyText(3, 0x18, 3, cameraStrings[70]);  // was func_8006376C [LLM:HIGH]
                 DbCamera_SetTextValue(spFC.r, &cameraStrings[68][5], 5);
-                func_8006376C(3, 0x19, 3, cameraStrings[68]);
+                DebugCamera_CopyText(3, 0x19, 3, cameraStrings[68]);  // was func_8006376C [LLM:HIGH]
                 OLib_Vec3fDiffToVecSphGeo(&spFC, sp90, sp80);
                 spFC.yaw -= cam->playerPosRot.rot.y;
-                func_8006376C(0x1E, 0x16,
+                DebugCamera_CopyText(0x1E, 0x16,  // was func_8006376C [LLM:HIGH]
                               ((dbCamera->sub.unk_08 == 1) && (dbCamera->sub.unk_0A == 4) && D_80161144) ? 7
                               : D_80161144                                                               ? 4
                                                                                                          : 3,
                               cameraStrings[21]);
                 DbCamera_SetTextValue(spFC.pitch * 0.00549325f, &cameraStrings[69][5], 4);
-                func_8006376C(0x1C, 0x17, 3, cameraStrings[69]);
+                DebugCamera_CopyText(0x1C, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
                 DbCamera_SetTextValue(spFC.yaw * 0.00549325f, &cameraStrings[70][5], 4);
-                func_8006376C(0x1C, 0x18, 3, cameraStrings[70]);
+                DebugCamera_CopyText(0x1C, 0x18, 3, cameraStrings[70]);  // was func_8006376C [LLM:HIGH]
                 DbCamera_SetTextValue(spFC.r, &cameraStrings[68][5], 5);
-                func_8006376C(0x1C, 0x19, 3, cameraStrings[68]);
+                DebugCamera_CopyText(0x1C, 0x19, 3, cameraStrings[68]);  // was func_8006376C [LLM:HIGH]
             }
 
             DebugDisplay_AddObject(dbCamera->at.x, dbCamera->at.y + 1.0f, dbCamera->at.z, 0, 0, 0, 0.02f, 2.0f, 0.02f,
@@ -1371,41 +1371,41 @@ void DbCamera_Update(DbCamera* dbCamera, Camera* cam) {
             dbCamera->unk_38 = -1;
         }
 
-        func_8006376C(0xE, 5, 0, cameraStrings[22]);
-        func_8006376C(9, 6, (dbCamera->unk_78 == 0) ? 7 : 4, cameraStrings[55]);
-        func_8006376C(0x11, 6, (dbCamera->unk_78 == 1) ? 7 : 4, cameraStrings[56]);
-        func_8006376C(0x18, 6, (dbCamera->unk_78 == 2) ? 7 : 4, cameraStrings[57]);
-        func_8006376C(3, 0x16, D_80161144 ? 3 : 4, cameraStrings[20]);
+        DebugCamera_CopyText(0xE, 5, 0, cameraStrings[22]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(9, 6, (dbCamera->unk_78 == 0) ? 7 : 4, cameraStrings[55]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x11, 6, (dbCamera->unk_78 == 1) ? 7 : 4, cameraStrings[56]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x18, 6, (dbCamera->unk_78 == 2) ? 7 : 4, cameraStrings[57]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x16, D_80161144 ? 3 : 4, cameraStrings[20]);  // was func_8006376C [LLM:HIGH]
         sp110 = 'X';
         DbCamera_SetTextValue(dbCamera->at.x, &sp111, 6);
-        func_8006376C(3, 0x17, 2, &sp110);
+        DebugCamera_CopyText(3, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
         sp110 = 'Y';
         DbCamera_SetTextValue(dbCamera->at.y, &sp111, 6);
-        func_8006376C(3, 0x18, 2, &sp110);
+        DebugCamera_CopyText(3, 0x18, 2, &sp110);  // was func_8006376C [LLM:HIGH]
         sp110 = 'Z';
         DbCamera_SetTextValue(dbCamera->at.z, &sp111, 6);
-        func_8006376C(3, 0x19, 2, &sp110);
-        func_8006376C(0x1E, 0x16, D_80161144 ? 4 : 3, cameraStrings[21]);
+        DebugCamera_CopyText(3, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x1E, 0x16, D_80161144 ? 4 : 3, cameraStrings[21]);  // was func_8006376C [LLM:HIGH]
         sp110 = 'X';
         DbCamera_SetTextValue(dbCamera->eye.x, &sp111, 6);
-        func_8006376C(0x1E, 0x17, 2, &sp110);
+        DebugCamera_CopyText(0x1E, 0x17, 2, &sp110);  // was func_8006376C [LLM:HIGH]
         sp110 = 'Y';
         DbCamera_SetTextValue(dbCamera->eye.y, &sp111, 6);
-        func_8006376C(0x1E, 0x18, 2, &sp110);
+        DebugCamera_CopyText(0x1E, 0x18, 2, &sp110);  // was func_8006376C [LLM:HIGH]
         sp110 = 'Z';
         DbCamera_SetTextValue(dbCamera->eye.z, &sp111, 6);
-        func_8006376C(0x1E, 0x19, 2, &sp110);
-        func_8006376C(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);
+        DebugCamera_CopyText(0x1E, 0x19, 2, &sp110);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0xD, 0x18, 3, !D_80161144 ? cameraStrings[9] : cameraStrings[10]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sp104.pitch * 0.00549325f, &cameraStrings[69][6], 4);
-        func_8006376C(0xF, 0x17, 3, cameraStrings[69]);
+        DebugCamera_CopyText(0xF, 0x17, 3, cameraStrings[69]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sp104.yaw * 0.00549325f, &cameraStrings[70][6], 4);
-        func_8006376C(0xF, 0x18, 3, cameraStrings[70]);
+        DebugCamera_CopyText(0xF, 0x18, 3, cameraStrings[70]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sp104.r, &cameraStrings[68][5], 5);
-        func_8006376C(0xF, 0x19, 3, cameraStrings[68]);
+        DebugCamera_CopyText(0xF, 0x19, 3, cameraStrings[68]);  // was func_8006376C [LLM:HIGH]
         if (dbCamera->unk_3C) {
-            func_8006376C(0x0F, 0x1A, 1, cameraStrings[23]);
+            DebugCamera_CopyText(0x0F, 0x1A, 1, cameraStrings[23]);  // was func_8006376C [LLM:HIGH]
         } else {
-            func_8006376C(0x0F, 0x1A, 1, cameraStrings[24]);
+            DebugCamera_CopyText(0x0F, 0x1A, 1, cameraStrings[24]);  // was func_8006376C [LLM:HIGH]
         }
 
         D_8012D110++;
@@ -1639,9 +1639,9 @@ void DbCamera_DrawSlotLetters(char* str, s16 y, s16 x, s32 colorId) {
     }
 
     str[0x14] = str[i * 2 + 1] = '\0';
-    func_8006376C(x, y, colorId, str);
+    DebugCamera_CopyText(x, y, colorId, str);  // was func_8006376C [LLM:HIGH]
     str[0x14] = str[i * 2 + 0] = '-';
-    func_8006376C(x + 0x14, y, colorId, str + 0x14);
+    DebugCamera_CopyText(x + 0x14, y, colorId, str + 0x14);  // was func_8006376C [LLM:HIGH]
 }
 
 void DbCamera_PrintAllCuts(Camera* cam) {
@@ -1695,11 +1695,11 @@ s32 func_800B91B0(Camera* cam, DbCamera* dbCamera) {
         cameraStrings[73][10] = ((sDbCameraCuts[D_8016110C].nPoints - 5) / 10) + '0';
         cameraStrings[73][11] = ((sDbCameraCuts[D_8016110C].nPoints - 5) % 10) + '0';
         DbCamera_SetTextValue(sDbCamAnim.unk_04, &cameraStrings[71][10], 4);
-        func_8006376C(0xF, 0x16, 3, cameraStrings[71]);
+        DebugCamera_CopyText(0xF, 0x16, 3, cameraStrings[71]);  // was func_8006376C [LLM:HIGH]
         DbCamera_SetTextValue(sDbCamAnim.unk_0C, &cameraStrings[72][10], 4);
-        func_8006376C(0xF, 0x17, 3, cameraStrings[72]);
-        func_8006376C(0xF, 0x18, 3, cameraStrings[73]);
-        func_8006376C(0x10, 0x1A, 1, cameraStrings[4]);
+        DebugCamera_CopyText(0xF, 0x17, 3, cameraStrings[72]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0xF, 0x18, 3, cameraStrings[73]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(0x10, 0x1A, 1, cameraStrings[4]);  // was func_8006376C [LLM:HIGH]
 
         sDbCamAnim.unk_04++;
         sDbCamAnim.unk_0C++;
@@ -1774,7 +1774,7 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
     VecSph sp5C;
     s32 (*callbacks[])(char*) = { DbCamera_SaveCallback, DbCamera_LoadCallback, DbCamera_ClearCallback };
 
-    func_8006376C(0xE, 5, 0, cameraStrings[25]); // DEMO CONTROL
+    DebugCamera_CopyText(0xE, 5, 0, cameraStrings[25]); // DEMO CONTROL, was func_8006376C [LLM:HIGH]
 
     idx1 = sCurFileIdx >> 1;
     idx2 = sLastFileIdx >> 1;
@@ -1796,12 +1796,12 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                             dbCamera->sub.demoCtrlToggleSwitch ^= 1;
                         }
                         cameraStrings[41][9] = sCurFileIdx + 'A';
-                        func_8006376C(0xA, 7, 5, cameraStrings[41]);
-                        func_8006376C(0x10, 7, 5, cameraStrings[dbCamera->sub.demoCtrlActionIdx + 32]);
-                        func_8006376C(0x14, 7, 5, cameraStrings[42]);
+                        DebugCamera_CopyText(0xA, 7, 5, cameraStrings[41]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x10, 7, 5, cameraStrings[dbCamera->sub.demoCtrlActionIdx + 32]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x14, 7, 5, cameraStrings[42]);  // was func_8006376C [LLM:HIGH]
 
-                        func_8006376C(0x11, 8, dbCamera->sub.demoCtrlToggleSwitch ? 4 : 7, cameraStrings[45]);
-                        func_8006376C(0x15, 8, dbCamera->sub.demoCtrlToggleSwitch ? 7 : 4, cameraStrings[46]);
+                        DebugCamera_CopyText(0x11, 8, dbCamera->sub.demoCtrlToggleSwitch ? 4 : 7, cameraStrings[45]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x15, 8, dbCamera->sub.demoCtrlToggleSwitch ? 7 : 4, cameraStrings[46]);  // was func_8006376C [LLM:HIGH]
 
                         if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_A)) {
                             if (dbCamera->sub.demoCtrlToggleSwitch == 0) {
@@ -1820,9 +1820,9 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                         } else {
                             dbCamera->sub.demoCtrlToggleSwitch ^= 1;
                             cameraStrings[41][9] = sCurFileIdx + 'A';
-                            func_8006376C(0xD, 7, 5, cameraStrings[41]); // todo: find something better
-                            func_8006376C(0x12, 7, 5, cameraStrings[40]);
-                            func_8006376C(0xD, 9, dbCamera->sub.demoCtrlToggleSwitch ? 1 : 6, "PRESS B BUTTON");
+                            DebugCamera_CopyText(0xD, 7, 5, cameraStrings[41]); // todo: find something better, was func_8006376C [LLM:HIGH]
+                            DebugCamera_CopyText(0x12, 7, 5, cameraStrings[40]);  // was func_8006376C [LLM:HIGH]
+                            DebugCamera_CopyText(0xD, 9, dbCamera->sub.demoCtrlToggleSwitch ? 1 : 6, "PRESS B BUTTON");  // was func_8006376C [LLM:HIGH]
                         }
                     }
                     if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_B)) {
@@ -1838,9 +1838,9 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                 case DEMO_CTRL_MENU(ACTION_LOAD, MENU_CALLBACK):
                 case DEMO_CTRL_MENU(ACTION_CLEAR, MENU_CALLBACK): {
                     cameraStrings[41][9] = sCurFileIdx + 'A';
-                    func_8006376C(0xC, 7, 5, cameraStrings[41]);
-                    func_8006376C(0x12, 7, 5, cameraStrings[dbCamera->sub.demoCtrlActionIdx + 32]);
-                    func_8006376C(0x16, 7, 5, cameraStrings[47]);
+                    DebugCamera_CopyText(0xC, 7, 5, cameraStrings[41]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x12, 7, 5, cameraStrings[dbCamera->sub.demoCtrlActionIdx + 32]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x16, 7, 5, cameraStrings[47]);  // was func_8006376C [LLM:HIGH]
 
                     if (callbacks[dbCamera->sub.demoCtrlActionIdx - 1](&cameraStrings[41][9])) {
                         dbCamera->sub.demoCtrlMenu++;
@@ -1856,10 +1856,10 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                 case DEMO_CTRL_MENU(ACTION_CLEAR, MENU_SUCCESS): {
                     dbCamera->sub.demoCtrlToggleSwitch ^= 1;
                     cameraStrings[41][9] = sCurFileIdx + 'A';
-                    func_8006376C(0xD, 7, 5, cameraStrings[41]);
-                    func_8006376C(0x13, 7, 5, cameraStrings[(dbCamera->sub.demoCtrlMenu / 100) + 32]);
-                    func_8006376C(0x17, 7, 5, cameraStrings[49]);
-                    func_8006376C(0xD, 9, (dbCamera->sub.demoCtrlToggleSwitch != 0) ? 1 : 6, "PRESS B BUTTON");
+                    DebugCamera_CopyText(0xD, 7, 5, cameraStrings[41]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x13, 7, 5, cameraStrings[(dbCamera->sub.demoCtrlMenu / 100) + 32]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x17, 7, 5, cameraStrings[49]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0xD, 9, (dbCamera->sub.demoCtrlToggleSwitch != 0) ? 1 : 6, "PRESS B BUTTON");  // was func_8006376C [LLM:HIGH]
 
                     if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_A) ||
                         CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_B)) {
@@ -1879,10 +1879,10 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                 case DEMO_CTRL_MENU(ACTION_CLEAR, MENU_ERROR): {
                     dbCamera->sub.demoCtrlToggleSwitch ^= 1;
                     cameraStrings[41][9] = sCurFileIdx + 'A';
-                    func_8006376C(0xD, 7, 5, cameraStrings[(dbCamera->sub.demoCtrlMenu / 100) + 32]);
-                    func_8006376C(0x11, 7, 5, cameraStrings[51]);
-                    func_8006376C(0x17, 7, 5, cameraStrings[49]);
-                    func_8006376C(0xD, 9, (dbCamera->sub.demoCtrlToggleSwitch != 0) ? 1 : 6, "PRESS B BUTTON");
+                    DebugCamera_CopyText(0xD, 7, 5, cameraStrings[(dbCamera->sub.demoCtrlMenu / 100) + 32]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x11, 7, 5, cameraStrings[51]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0x17, 7, 5, cameraStrings[49]);  // was func_8006376C [LLM:HIGH]
+                    DebugCamera_CopyText(0xD, 9, (dbCamera->sub.demoCtrlToggleSwitch != 0) ? 1 : 6, "PRESS B BUTTON");  // was func_8006376C [LLM:HIGH]
 
                     if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_A) ||
                         CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_B)) {
@@ -1955,31 +1955,31 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                             }
                         }
                         idx3 = dbCamera->sub.demoCtrlActionIdx;
-                        func_8006376C(0xE, 7, 5, cameraStrings[idx3 + 28]);
-                        func_8006376C(0xF, 7, 4, sp74);
+                        DebugCamera_CopyText(0xE, 7, 5, cameraStrings[idx3 + 28]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0xF, 7, 4, sp74);  // was func_8006376C [LLM:HIGH]
 
-                        func_8006376C((sCurFileIdx * 2) + 0x10, 7, 7, "_"); // cursor
+                        DebugCamera_CopyText((sCurFileIdx * 2) + 0x10, 7, 7, "_"); // cursor, was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(DbCamera_GetMempakAllocSize(), sp74, 6);
-                        func_8006376C(0xD, 9, 6, cameraStrings[38]); // NEED      BYTE
-                        func_8006376C(0x11, 9, 4, sp74);
+                        DebugCamera_CopyText(0xD, 9, 6, cameraStrings[38]); // NEED      BYTE, was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x11, 9, 4, sp74);  // was func_8006376C [LLM:HIGH]
                         DbCamera_SetTextValue(Mempak_GetFreeBytes(2), sp74, 6);
-                        func_8006376C(0xD, 0xA, 6, cameraStrings[37]); // FREE      BYTE
-                        func_8006376C(0x11, 0xA, 4, sp74);
+                        DebugCamera_CopyText(0xD, 0xA, 6, cameraStrings[37]); // FREE      BYTE, was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x11, 0xA, 4, sp74);  // was func_8006376C [LLM:HIGH]
                         if (sMempakFilesize != 0) {
                             DbCamera_SetTextValue(sMempakFilesize, sp74, 6);
-                            func_8006376C(0xD, 0xB, 7, cameraStrings[50]);
-                            func_8006376C(0x11, 0xB, 4, sp74);
+                            DebugCamera_CopyText(0xD, 0xB, 7, cameraStrings[50]);  // was func_8006376C [LLM:HIGH]
+                            DebugCamera_CopyText(0x11, 0xB, 4, sp74);  // was func_8006376C [LLM:HIGH]
                         }
 
                         idx1 = (dbCamera->sub.demoCtrlActionIdx + 2);
-                        func_8006376C(0xF, 0x16, 1, cameraStrings[39]);
-                        func_8006376C(0x12, 0x17, sDbCameraColors[idx1], cameraStrings[33]);
-                        func_8006376C(0x12, 0x18, sDbCameraColors[idx1 - 1], cameraStrings[34]);
-                        func_8006376C(0x12, 0x19, sDbCameraColors[idx1 - 2], cameraStrings[35]);
-                        func_8006376C(0xE, dbCamera->sub.demoCtrlActionIdx + 0x16, 7,
+                        DebugCamera_CopyText(0xF, 0x16, 1, cameraStrings[39]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x12, 0x17, sDbCameraColors[idx1], cameraStrings[33]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x12, 0x18, sDbCameraColors[idx1 - 1], cameraStrings[34]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x12, 0x19, sDbCameraColors[idx1 - 2], cameraStrings[35]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0xE, dbCamera->sub.demoCtrlActionIdx + 0x16, 7,  // was func_8006376C [LLM:HIGH]
                                       cameraStrings[11]); // current selection
-                        func_8006376C(0xD, 0x1A, 5, cameraStrings[32]);
-                        func_8006376C(0x14, 0x1A, 5, cameraStrings[36]);
+                        DebugCamera_CopyText(0xD, 0x1A, 5, cameraStrings[32]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x14, 0x1A, 5, cameraStrings[36]);  // was func_8006376C [LLM:HIGH]
 
                         if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_DUP)) {
                             Audio_PlaySfxGeneral(NA_SE_SY_CURSOR, &gSfxDefaultPos, 4, &gSfxDefaultFreqAndVolScale,
@@ -2006,8 +2006,8 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                         }
                         goto block_2;
                     } else {
-                        func_8006376C(0xC, 0x1A, 4, cameraStrings[32]);
-                        func_8006376C(0x13, 0x1A, 4, cameraStrings[40]);
+                        DebugCamera_CopyText(0xC, 0x1A, 4, cameraStrings[32]);  // was func_8006376C [LLM:HIGH]
+                        DebugCamera_CopyText(0x13, 0x1A, 4, cameraStrings[40]);  // was func_8006376C [LLM:HIGH]
                         if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_B) ||
                             CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_DUP) ||
                             CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_DDOWN)) {
@@ -2042,8 +2042,8 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
             DbCamera_DrawSlotLetters(sp74, 7, 5, 4);
 
             if (sDbCamAnim.unk_0A != 0) {
-                func_8006376C(4, 7, 5, cameraStrings[27]);
-                func_8006376C(D_8016110C * 2 + 6, 7, 7, ">");
+                DebugCamera_CopyText(4, 7, 5, cameraStrings[27]);  // was func_8006376C [LLM:HIGH]
+                DebugCamera_CopyText(D_8016110C * 2 + 6, 7, 7, ">");  // was func_8006376C [LLM:HIGH]
 
                 if (CHECK_BTN_ALL(sPlayState->state.input[2].press.button, BTN_CUP)) {
                     if (D_8016110C > 0) {
@@ -2151,7 +2151,7 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                                          &gSfxDefaultFreqAndVolScale, &gSfxDefaultReverb);
                     sp74[sCurFileIdx] = DbCamera_InitCut(idx1, &dbCamera->sub);
                     if (sp74[sCurFileIdx] == '?') {
-                        func_8006376C(0xF, 0x18, 7, cameraStrings[26]);
+                        DebugCamera_CopyText(0xF, 0x18, 7, cameraStrings[26]);  // was func_8006376C [LLM:HIGH]
                     }
                 }
             }
@@ -2235,15 +2235,15 @@ s32 DbCamera_UpdateDemoControl(DbCamera* dbCamera, Camera* cam) {
                                      &gSfxDefaultFreqAndVolScale, &gSfxDefaultReverb);
             }
 
-            func_8006376C(4, 7, 5, cameraStrings[28]);
+            DebugCamera_CopyText(4, 7, 5, cameraStrings[28]);  // was func_8006376C [LLM:HIGH]
             sp74[1] = 0;
             if (sLastFileIdx != -1) {
                 sp74[0] = D_801612EA;
-                func_8006376C(sLastFileIdx + 5, 7, 2, sp74);
+                DebugCamera_CopyText(sLastFileIdx + 5, 7, 2, sp74);  // was func_8006376C [LLM:HIGH]
             } else {
                 sp74[0] = '_';
             }
-            func_8006376C(sCurFileIdx + 5, 7, 7, sp74);
+            DebugCamera_CopyText(sCurFileIdx + 5, 7, 7, sp74);  // was func_8006376C [LLM:HIGH]
 
             break;
         }
diff --git a/soh/src/code/game.c b/soh/src/code/game.c
index 5133742ec..ea4eab006 100644
--- a/soh/src/code/game.c
+++ b/soh/src/code/game.c
@@ -109,7 +109,7 @@ void GameState_UpdateDebugRegisters(GameState* gameState) {
     }
 
     if (CVarGetInteger(CVAR_DEVELOPER_TOOLS("RegEditEnabled"), 0) || gIsCtrlr2Valid) {
-        func_8006390C(&gameState->input[1]);
+        Debug_EditRegs(&gameState->input[1]);  // was func_8006390C [LLM:HIGH]
     }
 
     gDmaMgrVerbose = HREG(60);
@@ -208,7 +208,7 @@ void GameState_Draw(GameState* gameState, GraphicsContext* gfxCtx) {
 
     CLOSE_DISPS(gfxCtx);
 
-    func_80063D7C(gfxCtx);
+    Debug_DrawOverlay(gfxCtx);  // was func_80063D7C [LLM:HIGH]
 
     if (R_ENABLE_ARENA_DBG != 0) {
         SpeedMeter_DrawTimeEntries(&sGameStateSpeedMeter, gfxCtx);
diff --git a/soh/src/code/z_camera.c b/soh/src/code/z_camera.c
index a09a00020..6485dfcb8 100644
--- a/soh/src/code/z_camera.c
+++ b/soh/src/code/z_camera.c
@@ -7231,14 +7231,14 @@ void Camera_PrintSettings(Camera* camera) {
         sp48[i] = '\0';
 
         sp48[camera->play->activeCamera] = 'a';
-        func_8006376C(3, 0x16, 5, sp58);
-        func_8006376C(3, 0x16, 1, sp48);
-        func_8006376C(3, 0x17, 5, "S:");
-        func_8006376C(5, 0x17, 4, sCameraSettingNames[camera->setting]);
-        func_8006376C(3, 0x18, 5, "M:");
-        func_8006376C(5, 0x18, 4, sCameraModeNames[camera->mode]);
-        func_8006376C(3, 0x19, 5, "F:");
-        func_8006376C(5, 0x19, 4,
+        DebugCamera_CopyText(3, 0x16, 5, sp58);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x16, 1, sp48);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x17, 5, "S:");  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(5, 0x17, 4, sCameraSettingNames[camera->setting]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x18, 5, "M:");  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(5, 0x18, 4, sCameraModeNames[camera->mode]);  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(3, 0x19, 5, "F:");  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(5, 0x19, 4,  // was func_8006376C [LLM:HIGH]
                       sCameraFunctionNames[sCameraSettings[camera->setting].cameraModes[camera->mode].funcIdx]);
 
         i = 0;
@@ -7263,8 +7263,8 @@ void Camera_PrintSettings(Camera* camera) {
         sp50[i++] = ' ';
         sp50[i++] = ' ';
         sp50[i] = '\0';
-        func_8006376C(3, 26, 5, "I:");
-        func_8006376C(5, 26, 4, sp50);
+        DebugCamera_CopyText(3, 26, 5, "I:");  // was func_8006376C [LLM:HIGH]
+        DebugCamera_CopyText(5, 26, 4, sp50);  // was func_8006376C [LLM:HIGH]
     }
 }
 
diff --git a/soh/src/code/z_debug.c b/soh/src/code/z_debug.c
index b11d52b8a..8c2bb8935 100644
--- a/soh/src/code/z_debug.c
+++ b/soh/src/code/z_debug.c
@@ -14,10 +14,13 @@ typedef struct {
 } InputCombo;
 
 GameInfo* gGameInfo;
-s32 D_8015FA94; // no known symbols
-PrintTextBuffer D_8015FA98[0x16];
+// LLM generated name (HIGH), was D_8015FA94: the in-tree comment on its declaration reads "// no known symbols"; nothing reads or writes it.
+s32 sUnusedDebugValue; // no known symbols, was D_8015FA94 [LLM:HIGH]
+// LLM generated name (HIGH), was D_8015FA98: the 0x16-entry PrintTextBuffer array that DebugCamera_CopyText fills and DebugCamera_DrawText prints.
+PrintTextBuffer sPrintTextBuffers[0x16];  // was D_8015FA98 [LLM:HIGH]
 
-s16 D_8011E0B0 = 0; // PrintTextBuffer index
+// LLM generated name (HIGH), was D_8011E0B0: the in-tree comment on its declaration reads "PrintTextBuffer index"; it is the write cursor into sPrintTextBuffers and the count the draw pass stops at.
+s16 sPrintTextIndex = 0; // PrintTextBuffer index, was D_8011E0B0 [LLM:HIGH]
 Color_RGBA8 printTextColors[] = {
     { 255, 255, 32, 192 }, { 255, 150, 128, 192 }, { 128, 96, 0, 64 },     { 192, 128, 16, 128 },
     { 255, 192, 32, 128 }, { 230, 230, 220, 64 },  { 128, 150, 255, 128 }, { 128, 255, 32, 128 },
@@ -80,13 +83,14 @@ void DebugCamera_ScreenText(s32 arg0, s32 arg1, const char* text) {
 }
 
 // Copy Camera Debugger Text
-void func_8006376C(u8 x, u8 y, u8 colorId, const char* text) {
+// LLM generated name (HIGH), was func_8006376C: the in-tree comment above it reads "Copy Camera Debugger Text"; it appends a position, colour and up to 21 characters into the print-text buffer for the draw pass to flush.
+void DebugCamera_CopyText(u8 x, u8 y, u8 colorId, const char* text) {
     PrintTextBuffer* buf;
     char* bufText;
     s16 i;
 
-    buf = &D_8015FA98[D_8011E0B0];
-    if (D_8011E0B0 < 0x16) {
+    buf = &sPrintTextBuffers[sPrintTextIndex];  // was D_8015FA98 [LLM:HIGH]
+    if (sPrintTextIndex < 0x16) {  // was D_8011E0B0 [LLM:HIGH]
         buf->x = x;
         buf->y = y;
         buf->colorId = colorId;
@@ -100,21 +104,22 @@ void func_8006376C(u8 x, u8 y, u8 colorId, const char* text) {
         }
 
         *bufText = '\0';
-        D_8011E0B0++;
+        sPrintTextIndex++;  // was D_8011E0B0 [LLM:HIGH]
     }
 }
 
 // Draw Text
-void func_80063828(GfxPrint* printer) {
+// LLM generated name (HIGH), was func_80063828: the in-tree comment above it reads "Draw Text"; it walks exactly the buffer DebugCamera_CopyText fills and prints each entry in its colour at its position.
+void DebugCamera_DrawText(GfxPrint* printer) {
     s32 i;
     Color_RGBA8* color;
     PrintTextBuffer* buffer;
     char* text;
 
     i = 0;
-    if (D_8011E0B0 > 0) {
+    if (sPrintTextIndex > 0) {  // was D_8011E0B0 [LLM:HIGH]
         do {
-            buffer = &D_8015FA98[i];
+            buffer = &sPrintTextBuffers[i];  // was D_8015FA98 [LLM:HIGH]
             text = buffer->text;
 
             color = &printTextColors[buffer->colorId];
@@ -122,12 +127,13 @@ void func_80063828(GfxPrint* printer) {
             GfxPrint_SetPos(printer, buffer->x, buffer->y);
             GfxPrint_Printf(printer, "%s", text);
             i += 1;
-        } while (i < D_8011E0B0);
+        } while (i < sPrintTextIndex);  // was D_8011E0B0 [LLM:HIGH]
     }
 }
 
 // Edit REG
-void func_8006390C(Input* input) {
+// LLM generated name (HIGH), was func_8006390C: the in-tree comment above it reads "Edit REG"; it reads the L/R plus C-button combos in inputCombos to pick a register group and adjust the selected register.
+void Debug_EditRegs(Input* input) {
 
     s32 dpad;
     s32 regGroup;
@@ -211,7 +217,8 @@ void func_8006390C(Input* input) {
 }
 
 // Draw Memory Viewer
-void func_80063C04(GfxPrint* printer) {
+// LLM generated name (HIGH), was func_80063C04: prints the current page of the register editor, one line per register, highlighting the selected one.
+void Debug_DrawRegEditor(GfxPrint* printer) {
     s32 i;
     s32 page = (gGameInfo->regPage * REG_PER_PAGE) - REG_PER_PAGE;
     s32 regGroup = (gGameInfo->regGroup * REG_PAGES + gGameInfo->regPage) * REG_PER_PAGE - REG_PER_PAGE;
@@ -239,7 +246,8 @@ void func_80063C04(GfxPrint* printer) {
     }
 }
 
-void func_80063D7C(GraphicsContext* gfxCtx) {
+// LLM generated name (HIGH), was func_80063D7C: the top-level debug draw: it sets up a GfxPrint and emits the register editor and the buffered camera-debugger text, gated on the DebugEnabled developer CVar.
+void Debug_DrawOverlay(GraphicsContext* gfxCtx) {
     Gfx* sp7C;
     Gfx* sp78;
     GfxPrint printer;
@@ -258,14 +266,14 @@ void func_80063D7C(GraphicsContext* gfxCtx) {
     GfxPrint_Open(&printer, tempRet);
 
     if ((OREG(0) == 1) || (OREG(0) == 8)) {
-        func_80063828(&printer);
+        DebugCamera_DrawText(&printer);  // was func_80063828 [LLM:HIGH]
     }
 
     if (gGameInfo->regPage != 0) {
-        func_80063C04(&printer);
+        Debug_DrawRegEditor(&printer);  // was func_80063C04 [LLM:HIGH]
     }
 
-    D_8011E0B0 = 0;
+    sPrintTextIndex = 0;  // was D_8011E0B0 [LLM:HIGH]
     sp7C = GfxPrint_Close(&printer);
     gSPEndDisplayList(sp7C++);
     Graph_BranchDlist(sp78, sp7C);
-- 
2.55.0

