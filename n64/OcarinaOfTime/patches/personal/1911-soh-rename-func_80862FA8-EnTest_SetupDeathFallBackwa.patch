From 030fa8547c36fcaad7bceec28c65b25d8f7510a8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1911/3799] soh: rename func_80862FA8 ->
 EnTest_SetupDeathFallBackwards

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: plays gStalfosFallOverBackwardsAnim with NA_SE_EN_STAL_DEAD; the higher params fall through to EnTest_SetupBreakApart instead
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index 9dd79e1c6..6ba90c2bf 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -69,7 +69,8 @@ void func_80863044(EnTest* this, PlayState* play);
 void func_8086318C(EnTest* this, PlayState* play);
 void EnTest_Recoil(EnTest* this, PlayState* play);
 void func_808633E8(EnTest* this, PlayState* play);
-void func_80862FA8(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_80862FA8: plays gStalfosFallOverBackwardsAnim with NA_SE_EN_STAL_DEAD; the higher params fall through to EnTest_SetupBreakApart instead
+void EnTest_SetupDeathFallBackwards(EnTest* this, PlayState* play);  // was func_80862FA8 [LLM:HIGH]
 
 s32 EnTest_ReactToProjectile(PlayState* play, EnTest* this);
 
@@ -1332,7 +1333,7 @@ void EnTest_Stunned(EnTest* this, PlayState* play) {
 
     if (this->actor.colorFilterTimer == 0) {
         if (this->actor.colChkInfo.health == 0) {
-            func_80862FA8(this, play);
+            EnTest_SetupDeathFallBackwards(this, play);  // was func_80862FA8 [LLM:HIGH]
         } else if (player->meleeWeaponState != 0) {
             if ((this->actor.bgCheckFlags & BGCHECKFLAG_WALL) &&
                 ((ABS((s16)(this->actor.wallYaw - this->actor.shape.rot.y)) < 0x38A4) &&
@@ -1546,7 +1547,7 @@ void EnTest_BreakApart(EnTest* this, PlayState* play) {
     }
 }
 
-void func_80862FA8(EnTest* this, PlayState* play) {
+void EnTest_SetupDeathFallBackwards(EnTest* this, PlayState* play) {
     Animation_PlayOnce(&this->skelAnime, &gStalfosFallOverBackwardsAnim);
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_STAL_DEAD);
     this->unk_7DE = 0;
@@ -1699,7 +1700,7 @@ void EnTest_UpdateDamage(EnTest* this, PlayState* play) {
                 if (Actor_IsFacingPlayer(&this->actor, 0x4000)) {
                     if (Actor_ApplyDamage(&this->actor) == 0) {
                         Enemy_StartFinishingBlow(play, &this->actor);
-                        func_80862FA8(this, play);
+                        EnTest_SetupDeathFallBackwards(this, play);  // was func_80862FA8 [LLM:HIGH]
                     } else {
                         EnTest_SetupFlinchFront(this);  // was func_80862154 [LLM:HIGH]
                     }
-- 
2.55.0

