From 1becdc8f09fa12cb5ea350c47d75b40d6a8ba42c Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:27:41 +0000
Subject: [PATCH 1574/3799] soh: rename func_80A408D8 -> EnGo_Sit

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: winds the animation back to the seated frame and then either curls up when its timer runs out, or stands again when the camera settles on the Goron.
Defined in soh/src/overlays/actors/ovl_En_Go/z_en_go.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Go/z_en_go.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
index a290d972f..c7210594c 100644
--- a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
+++ b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
@@ -28,7 +28,8 @@ void EnGo_CurlUp(EnGo* this, PlayState* play);  // was func_80A40494 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A405CC: plays the uncurl animation forward to its last frame, then installs either the standing idle or the Goron-City-doors variant.
 void EnGo_Uncurl(EnGo* this, PlayState* play);  // was func_80A405CC [LLM:HIGH]
 void EnGo_BiggoronActionFunc(EnGo* this, PlayState* play);
-void func_80A408D8(EnGo* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A408D8: winds the animation back to the seated frame and then either curls up when its timer runs out, or stands again when the camera settles on the Goron.
+void EnGo_Sit(EnGo* this, PlayState* play);  // was func_80A408D8 [LLM:HIGH]
 
 void func_80A40B1C(EnGo* this, PlayState* play);
 void EnGo_GetItem(EnGo* this, PlayState* play);
@@ -894,12 +895,12 @@ void EnGo_BiggoronActionFunc(EnGo* this, PlayState* play) {
             EnGo_ReverseAnimation(this);
             this->skelAnime.playSpeed = -0.1f;
             this->skelAnime.playSpeed *= (this->actor.params & 0xF0) == 0x90 ? 0.5f : 1.0f;
-            EnGo_SetupAction(this, func_80A408D8);
+            EnGo_SetupAction(this, EnGo_Sit);  // was func_80A408D8 [LLM:HIGH]
         }
     }
 }
 
-void func_80A408D8(EnGo* this, PlayState* play) {
+void EnGo_Sit(EnGo* this, PlayState* play) {
     f32 frame;
 
     if (this->skelAnime.playSpeed != 0.0f) {
-- 
2.55.0

