From f82f6904b7a2c7f79642c538fbc5e54dc7bc2e9b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:11 +0000
Subject: [PATCH 2601/3799] soh: rename func_80ACB3E0 ->
 EnOwl_WaitDesertColossus

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the Desert Colossus owl's perch: offers text 0x6079 and starts its choice
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index b77b2c157..ac750a129 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -29,7 +29,8 @@ void EnOwl_WaitLakeHylia(EnOwl* this, PlayState* play);
 void EnOwl_WaitZoraRiver(EnOwl* this, PlayState* play);
 void EnOwl_WaitHyliaShortcut(EnOwl* this, PlayState* play);
 void EnOwl_WaitDeathMountainShortcut(EnOwl* this, PlayState* play);
-void func_80ACB3E0(EnOwl* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ACB3E0: the Desert Colossus owl's perch: offers text 0x6079 and starts its choice
+void EnOwl_WaitDesertColossus(EnOwl* this, PlayState* play);  // was func_80ACB3E0 [LLM:HIGH]
 void EnOwl_WaitLWPreSaria(EnOwl* this, PlayState* play);
 void EnOwl_WaitLWPostSaria(EnOwl* this, PlayState* play);
 void func_80ACD4D4(EnOwl* this, PlayState* play);
@@ -210,7 +211,7 @@ void EnOwl_Init(Actor* thisx, PlayState* play) {
             this->actionFunc = EnOwl_WaitDeathMountainShortcut;
             break;
         case OWL_DESSERT_COLOSSUS:
-            this->actionFunc = func_80ACB3E0;
+            this->actionFunc = EnOwl_WaitDesertColossus;  // was func_80ACB3E0 [LLM:HIGH]
             break;
         case OWL_LOST_WOODS_PRESARIA:
             if (!CHECK_QUEST_ITEM(QUEST_SONG_LULLABY)) {
@@ -672,7 +673,7 @@ void EnOwl_DesertColossusChoice(EnOwl* this, PlayState* play) {
     }
 }
 
-void func_80ACB3E0(EnOwl* this, PlayState* play) {
+void EnOwl_WaitDesertColossus(EnOwl* this, PlayState* play) {
     EnOwl_LookAtLink(this, play);
 
     if (EnOwl_CheckInitTalk(this, play, 0x6079, 360.0f, 2)) {
-- 
2.55.0

