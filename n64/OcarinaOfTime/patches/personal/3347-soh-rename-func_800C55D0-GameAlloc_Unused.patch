From 4accaaedfc4562d5959368581403911afb378775 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:14:48 +0000
Subject: [PATCH 3347/3799] soh: rename func_800C55D0 -> GameAlloc_Unused

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: declared taking a GameAlloc with no definition and no caller in the tree
Defined in soh/include/functions.h.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 50f784907..9a17c7d48 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1650,7 +1650,8 @@ void GameState_Destroy(GameState* gameState);
 GameStateFunc GameState_GetInit(GameState* gameState);
 u32 GameState_IsRunning(GameState* gameState);
 void* GameState_Alloc(GameState* gameState, size_t size, char* file, s32 line);
-void func_800C55D0(GameAlloc* this);
+// LLM generated name (GUESS), was func_800C55D0: declared taking a GameAlloc with no definition and no caller in the tree
+void GameAlloc_Unused(GameAlloc* this);  // was func_800C55D0 [LLM:GUESS]
 void* GameAlloc_MallocDebug(GameAlloc* this, size_t size, const char* file, s32 line);
 void* GameAlloc_Malloc(GameAlloc* this, size_t size);
 void GameAlloc_Free(GameAlloc* this, void* data);
-- 
2.55.0

