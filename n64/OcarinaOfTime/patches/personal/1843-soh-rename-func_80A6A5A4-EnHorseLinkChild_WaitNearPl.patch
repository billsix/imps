From a1830a0c05a8c43f62a2f899788a021cfe0b59da Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:48 +0000
Subject: [PATCH 1843/3799] soh: rename func_80A6A5A4 ->
 EnHorseLinkChild_WaitNearPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: keeps the foal facing the player while it stands next to them, and breaks into the escape run when the debug register fires.
Defined in soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c   | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
index f7529c3db..7f18ea14e 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
@@ -454,7 +454,8 @@ void EnHorseLinkChild_SetupWaitNearPlayer(EnHorseLinkChild* this) {
                      Animation_GetLastFrame(sAnimations[this->animationIdx]), ANIMMODE_ONCE, 0.0f);
 }
 
-void func_80A6A5A4(EnHorseLinkChild* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A6A5A4: keeps the foal facing the player while it stands next to them, and breaks into the escape run when the debug register fires.
+void EnHorseLinkChild_WaitNearPlayer(EnHorseLinkChild* this, PlayState* play) {
     s16 yawDiff;
 
     if (DREG(53) != 0) {
@@ -558,7 +559,7 @@ void func_80A6A7D0(EnHorseLinkChild* this, PlayState* play) {
 }
 
 static EnHorseLinkChildActionFunc sActionFuncs[] = {
-    EnHorseLinkChild_CycleAnim, EnHorseLinkChild_Approach, EnHorseLinkChild_Idle, EnHorseLinkChild_Follow, func_80A6A7D0, func_80A6A5A4,  // was func_80A698F4 [LLM:HIGH]
+    EnHorseLinkChild_CycleAnim, EnHorseLinkChild_Approach, EnHorseLinkChild_Idle, EnHorseLinkChild_Follow, func_80A6A7D0, EnHorseLinkChild_WaitNearPlayer,  // was func_80A698F4 [LLM:HIGH]
 };
 
 static void* sEyeTextures[] = { gChildEponaEyeOpenTex, gChildEponaEyeHalfTex, gChildEponaEyeCloseTex };
-- 
2.55.0

