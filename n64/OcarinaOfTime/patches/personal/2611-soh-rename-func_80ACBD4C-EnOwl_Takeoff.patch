From 3ab009fa1d468d93620aba580f4df63dfb8a8098 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:11 +0000
Subject: [PATCH 2611/3799] soh: rename func_80ACBD4C -> EnOwl_Takeoff

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: drives the climb through the takeoff animation and then switches to the flight
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index f0124884a..90afbdb4f 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -895,7 +895,8 @@ void EnOwl_FlyAway(EnOwl* this, PlayState* play) {
     this->actionFlags |= 8;
 }
 
-void func_80ACBD4C(EnOwl* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80ACBD4C: drives the climb through the takeoff animation and then switches to the flight
+void EnOwl_Takeoff(EnOwl* this, PlayState* play) {
     if (this->skelAnime.curFrame > 10.0f) {
         Math_SmoothStepToS(&this->actor.world.rot.y, this->unk_400, 2, 0x400, 0x40);
         this->actor.shape.rot.y = this->actor.world.rot.y;
@@ -927,7 +928,7 @@ void func_80ACBD4C(EnOwl* this, PlayState* play) {
 void func_80ACBEA0(EnOwl* this, PlayState* PlayState) {
     if (this->actionFlags & 1) {
         this->unk_3FE = 3;
-        EnOwl_ChangeMode(this, func_80ACBD4C, func_80ACC540, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);
+        EnOwl_ChangeMode(this, EnOwl_Takeoff, func_80ACC540, &this->skelAnime, &gOwlTakeoffAnim, 0.0f);  // was func_80ACBD4C [LLM:HIGH]
         this->unk_3F8 = this->actor.world.pos.y;
         this->actor.velocity.y = 2.0f;
         if (this->actionFlags & 0x40) {
-- 
2.55.0

