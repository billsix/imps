From 8f9260983258ec2c43482511315ea51415870d34 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:10 +0000
Subject: [PATCH 1899/3799] soh: rename func_80860BDC ->
 EnTest_SetupTurnToPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: loops gStalfosSidestepAnim and hands off to the action that swings shape.rot.y onto yawTowardsPlayer with NA_SE_EN_STAL_WALK footsteps
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index 9fb3c456c..711a555d6 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -24,7 +24,8 @@ void EnTest_SetupJumpslash(EnTest* this);
 void EnTest_SetupWalkAndBlock(EnTest* this);
 void func_80860EC0(EnTest* this);
 void EnTest_SetupSlashDown(EnTest* this);
-void func_80860BDC(EnTest* this);
+// LLM generated name (HIGH), was func_80860BDC: loops gStalfosSidestepAnim and hands off to the action that swings shape.rot.y onto yawTowardsPlayer with NA_SE_EN_STAL_WALK footsteps
+void EnTest_SetupTurnToPlayer(EnTest* this);  // was func_80860BDC [LLM:HIGH]
 void EnTest_SetupIdleFromBlock(EnTest* this);
 void EnTest_SetupRecoil(EnTest* this);
 void func_80862398(EnTest* this);
@@ -526,7 +527,7 @@ void EnTest_Idle(EnTest* this, PlayState* play) {
                 }
             } else {
                 if (Rand_ZeroOne() > 0.7f) {
-                    func_80860BDC(this);
+                    EnTest_SetupTurnToPlayer(this);  // was func_80860BDC [LLM:HIGH]
                 } else {
                     EnTest_ChooseAction(this, play);
                 }
@@ -709,7 +710,7 @@ void EnTest_WalkAndBlock(EnTest* this, PlayState* play) {
 }
 
 // a variation of sidestep
-void func_80860BDC(EnTest* this) {
+void EnTest_SetupTurnToPlayer(EnTest* this) {
     Animation_PlayLoop(&this->skelAnime, &gStalfosSidestepAnim);
     this->unk_7C8 = 0xE;
     EnTest_SetupAction(this, func_80860C24);
-- 
2.55.0

