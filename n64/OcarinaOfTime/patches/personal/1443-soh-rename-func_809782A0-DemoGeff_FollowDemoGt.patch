From 5d0a29e5aaad04a431cbf2fc94f8ab9d7c4ab842 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:19:22 +0000
Subject: [PATCH 1443/3799] soh: rename func_809782A0 -> DemoGeff_FollowDemoGt

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: keeps the piece at its recorded offset from the Demo_Gt it found, except for the three params variants that stay put.
Defined in soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
index b3b749304..48989598d 100644
--- a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
+++ b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
@@ -153,7 +153,8 @@ void DemoGeff_FindDemoGt(DemoGeff* this, PlayState* play) {
     }
 }
 
-void func_809782A0(DemoGeff* this, PlayState* play) {
+// LLM generated name (HIGH), was func_809782A0: keeps the piece at its recorded offset from the Demo_Gt it found, except for the three params variants that stay put.
+void DemoGeff_FollowDemoGt(DemoGeff* this, PlayState* play) {
     DemoGt* demoGt = this->demoGt;
     s16 params = this->actor.params;
 
@@ -166,7 +167,7 @@ void func_809782A0(DemoGeff* this, PlayState* play) {
 
 void func_80978308(DemoGeff* this, PlayState* play) {
     DemoGeff_FindDemoGt(this, play);  // was func_809781FC [LLM:HIGH]
-    func_809782A0(this, play);
+    DemoGeff_FollowDemoGt(this, play);  // was func_809782A0 [LLM:HIGH]
     DemoGeff_SetupRubble(this, play);  // was func_80978030 [LLM:HIGH]
 }
 
-- 
2.55.0

