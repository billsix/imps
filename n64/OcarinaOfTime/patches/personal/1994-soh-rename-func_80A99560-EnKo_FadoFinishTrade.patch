From f2efc1c9bda75cdfd92d7891a82de2a4698d50cc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:07:53 +0000
Subject: [PATCH 1994/3799] soh: rename func_80A99560 -> EnKo_FadoFinishTrade

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: continues into text 0x10B9, sets ITEMGETINF_31 and returns to Fado's idle
Defined in soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c b/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
index 6c62a3c75..f0cdb3d74 100644
--- a/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
+++ b/soh/src/overlays/actors/ovl_En_Ko/z_en_ko.c
@@ -31,7 +31,8 @@ void EnKo_FadoWaitToTrade(EnKo* this, PlayState* play);  // was func_80A99384 [L
 void EnKo_FadoLaugh(EnKo* this, PlayState* play);  // was func_80A99438 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A99504: offers GI_SAW until the player takes it
 void EnKo_FadoGiveSaw(EnKo* this, PlayState* play);  // was func_80A99504 [LLM:HIGH]
-void func_80A99560(EnKo* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A99560: continues into text 0x10B9, sets ITEMGETINF_31 and returns to Fado's idle
+void EnKo_FadoFinishTrade(EnKo* this, PlayState* play);  // was func_80A99560 [LLM:HIGH]
 
 // LLM generated name (HIGH), was func_80A98ECC: dispatches to the per-quest-state update, short-circuiting to EnKo_UpdateTrackNearbyPlayer for Fado in the Lost Woods
 s32 EnKo_UpdateByForestQuestState(EnKo* this, PlayState* play);  // was func_80A98ECC [LLM:HIGH]
@@ -1247,13 +1248,13 @@ void EnKo_FadoLaugh(EnKo* this, PlayState* play) {
 void EnKo_FadoGiveSaw(EnKo* this, PlayState* play) {
     if (Actor_HasParent(&this->actor, play) || !GameInteractor_Should(VB_TRADE_ODD_POTION, true, this)) {
         this->actor.parent = NULL;
-        this->actionFunc = func_80A99560;
+        this->actionFunc = EnKo_FadoFinishTrade;  // was func_80A99560 [LLM:HIGH]
     } else {
         Actor_OfferGetItem(&this->actor, play, GI_SAW, 120.0f, 10.0f);
     }
 }
 
-void func_80A99560(EnKo* this, PlayState* play) {
+void EnKo_FadoFinishTrade(EnKo* this, PlayState* play) {
     if (this->interactInfo.talkState == NPC_TALK_STATE_ITEM_GIVEN) {
         this->actor.textId = 0x10B9;
         Message_ContinueTextbox(play, this->actor.textId);
-- 
2.55.0

