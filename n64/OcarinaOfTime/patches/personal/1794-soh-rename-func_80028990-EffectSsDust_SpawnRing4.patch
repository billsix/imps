From d1e16e292bf1ea9be70733dc74719fadd7cd0675 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:43:30 +0000
Subject: [PATCH 1794/3799] soh: rename func_80028990 ->
 EffectSsDust_SpawnRing4

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spawns twenty brown drawFlags 4 puffs scattered around the source point.
Defined in soh/src/code/z_effect_soft_sprite_old_init.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                       | 2 +-
 soh/src/code/z_effect_soft_sprite_old_init.c  | 3 ++-
 soh/src/overlays/actors/ovl_En_Dns/z_en_dns.c | 2 +-
 3 files changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 9b271d966..7de348dee 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -257,7 +257,7 @@ void EffectSsDust_SpawnDrawFlags0Default(PlayState* play, Vec3f* pos, Vec3f* vel
                    Color_RGBA8* envColor);  // was func_8002881C [LLM:HIGH]
 void EffectSsDust_SpawnDrawFlags1Default(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, Color_RGBA8* primColor,
                    Color_RGBA8* envColor);  // was func_80028858 [LLM:HIGH]
-void func_80028990(PlayState* play, f32 randScale, Vec3f* srcPos);
+void EffectSsDust_SpawnRing4(PlayState* play, f32 randScale, Vec3f* srcPos);  // was func_80028990 [LLM:HIGH]
 void func_80028A54(PlayState* play, f32 randScale, Vec3f* srcPos);
 void EffectSsKiraKira_SpawnSmallYellow(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);
 void EffectSsKiraKira_SpawnSmall(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel,
diff --git a/soh/src/code/z_effect_soft_sprite_old_init.c b/soh/src/code/z_effect_soft_sprite_old_init.c
index 7cf37feb8..3da87d0a1 100644
--- a/soh/src/code/z_effect_soft_sprite_old_init.c
+++ b/soh/src/code/z_effect_soft_sprite_old_init.c
@@ -210,7 +210,8 @@ void EffectSsDust_MakeRingParams(Vec3f* srcPos, f32 randScale, Vec3f* newPos, Ve
     accel->z = 0.0f;
 }
 
-void func_80028990(PlayState* play, f32 randScale, Vec3f* srcPos) {
+// LLM generated name (HIGH), was func_80028990: spawns twenty brown drawFlags 4 puffs scattered around the source point.
+void EffectSsDust_SpawnRing4(PlayState* play, f32 randScale, Vec3f* srcPos) {
     s32 i;
     Vec3f pos;
     Vec3f velocity;
diff --git a/soh/src/overlays/actors/ovl_En_Dns/z_en_dns.c b/soh/src/overlays/actors/ovl_En_Dns/z_en_dns.c
index 597f0bc72..cd00a3cb5 100644
--- a/soh/src/overlays/actors/ovl_En_Dns/z_en_dns.c
+++ b/soh/src/overlays/actors/ovl_En_Dns/z_en_dns.c
@@ -449,7 +449,7 @@ void EnDns_PostBurrow(EnDns* this, PlayState* play) {
         initPos.x = this->actor.world.pos.x;
         initPos.y = this->yInitPos;
         initPos.z = this->actor.world.pos.z;
-        func_80028990(play, 20.0f, &initPos);
+        EffectSsDust_SpawnRing4(play, 20.0f, &initPos);  // was func_80028990 [LLM:HIGH]
     }
     this->actor.shape.rot.y += 0x2000;
     // Drops only if you bought its item
-- 
2.55.0

