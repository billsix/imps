From 7783bbcf88cf3334c908e547eaf35abd6a29a6da Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:36:44 +0000
Subject: [PATCH 1679/3799] soh: rename func_80AB9F24 ->
 EnNiwLady_WaitForObjects

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: waits for both of Anju's object banks, then builds the skeleton, collider and shape, picks her starting animation and installs the age-appropriate action.
Defined in soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
index e1c2708ef..fe09eed71 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
@@ -11,7 +11,8 @@ void EnNiwLady_Init(Actor* thisx, PlayState* play);
 void EnNiwLady_Destroy(Actor* thisx, PlayState* play);
 void EnNiwLady_Update(Actor* thisx, PlayState* play);
 
-void func_80AB9F24(EnNiwLady* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB9F24: waits for both of Anju's object banks, then builds the skeleton, collider and shape, picks her starting animation and installs the age-appropriate action.
+void EnNiwLady_WaitForObjects(EnNiwLady* this, PlayState* play);  // was func_80AB9F24 [LLM:HIGH]
 void EnNiwLady_Draw(Actor* thisx, PlayState* play);
 void func_80ABA21C(EnNiwLady* this, PlayState* play);
 void func_80ABAD38(EnNiwLady* this, PlayState* play);
@@ -88,7 +89,7 @@ void EnNiwLady_Init(Actor* thisx, PlayState* play) {
     }
     osSyncPrintf(VT_FGCOL(GREEN) "☆☆☆☆☆ ねぇちゃんうっふん ☆☆☆☆☆ %d\n" VT_RST, this->unk_278);
     osSyncPrintf("\n\n");
-    this->actionFunc = func_80AB9F24;
+    this->actionFunc = EnNiwLady_WaitForObjects;  // was func_80AB9F24 [LLM:HIGH]
     thisx->uncullZoneForward = 600.0f;
     this->getItemEntry = (GetItemEntry)GET_ITEM_NONE;
 }
@@ -151,7 +152,7 @@ void EnNiwLady_ChoseAnimation(EnNiwLady* this, PlayState* play, s32 arg2) {
     }
 }
 
-void func_80AB9F24(EnNiwLady* this, PlayState* play) {
+void EnNiwLady_WaitForObjects(EnNiwLady* this, PlayState* play) {
     f32 frames;
     s32 pad;
 
-- 
2.55.0

