From d9fa6c5ce096ca1da454a927b035af6d80f3f3e3 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:30:03 +0000
Subject: [PATCH 1597/3799] soh: rename func_80A4EA08 -> EnGs_HitSpin

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: a second hit reaction: the same settling wobble plus a rocking rotation on the stone's two halves.
Defined in soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
index 4d693c989..5a500b210 100644
--- a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
+++ b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
@@ -237,7 +237,8 @@ void EnGs_HitWobble(EnGs* this, PlayState* play) {
     }
 }
 
-void func_80A4EA08(EnGs* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A4EA08: a second hit reaction: the same settling wobble plus a rocking rotation on the stone's two halves.
+void EnGs_HitSpin(EnGs* this, PlayState* play) {
     if (this->unk_19F == 0) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_STALKID_ATTACK);
         this->unk_1E8 = 0.3f;
@@ -541,7 +542,7 @@ void EnGs_Update(Actor* thisx, PlayState* play2) {
                 case 14:
                     this->unk_19E |= 1;
                     func_80A4F77C(this);
-                    this->actionFunc = func_80A4EA08;
+                    this->actionFunc = EnGs_HitSpin;  // was func_80A4EA08 [LLM:HIGH]
                     break;
                 case 13:
                     this->unk_19E |= 1;
-- 
2.55.0

