From 30fac3f8703e50c4bb2b950ea9824fb387197244 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:33 +0000
Subject: [PATCH 393/488] soh: name the 10 address-named symbols defined in
 z_view.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 10 are defined in soh/src/code/z_view.c; updating their references touches 13 files in total.

  * func_800AA358 -> View_LookAt
    deduced, confidence HIGH: stores the eye, look-at and up vectors,
    nudging the eye off a perfectly vertical line first

  * func_800AA3F0 -> View_LookAtUnsafe
    deduced, confidence HIGH: the same store without that nudge

  * func_800AAA50 -> View_Apply
    deduced, confidence HIGH: dispatches to the orthographic or perspective
    apply by the caller's flags

  * func_800AAA9C -> View_ApplyPerspective
    deduced, confidence HIGH: builds the viewport, guPerspective projection
    and viewing matrix and binds them

  * func_800AB0A8 -> View_ApplyOrtho
    deduced, confidence HIGH: builds the viewport and a guOrtho projection
    and binds them

  * func_800AB2C4 -> View_ApplyOrthoToOverlay
    deduced, confidence HIGH: the same orthographic setup written into the
    overlay display list, with the flipped projection kept

  * func_800AB560 -> View_ApplyPerspectiveToOverlay
    deduced, confidence HIGH: the perspective counterpart that writes into
    the overlay display list

  * func_800AB944 -> View_UpdateViewingMatrix
    deduced, confidence HIGH: rebuilds the viewing matrix from the current
    eye, look-at and up

  * func_800AB9EC -> View_ApplyTo
    deduced, confidence HIGH: the flag-driven apply that writes into a
    caller-supplied display list rather than the context

  * func_800ABE74 -> View_ErrorCheckEyePosition
    deduced, confidence HIGH: reports whether an eye position is beyond the
    coordinate range guLookAt can take

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/include/functions.h                       | 20 ++++-----
 soh/src/code/speed_meter.c                    |  4 +-
 soh/src/code/z_actor.c                        |  2 +-
 soh/src/code/z_camera.c                       |  4 +-
 soh/src/code/z_message_PAL.c                  |  2 +-
 soh/src/code/z_parameter.c                    |  6 +--
 soh/src/code/z_play.c                         |  6 +--
 soh/src/code/z_sample.c                       |  4 +-
 soh/src/code/z_view.c                         | 42 ++++++++++++-------
 .../ovl_file_choose/z_file_choose.c           |  4 +-
 .../overlays/gamestates/ovl_select/z_select.c |  6 +--
 .../overlays/gamestates/ovl_title/z_title.c   |  4 +-
 .../ovl_kaleido_scope/z_kaleido_scope_PAL.c   |  6 +--
 13 files changed, 60 insertions(+), 50 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index a43fc36b6..b99ade68e 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -1394,8 +1394,8 @@ void Rumble_SetUpdateEnabled(u32);  // was func_800AA178 [oot]
 View* View_New(GraphicsContext* gfxCtx);
 void View_Free(View* view);
 void View_Init(View*, GraphicsContext*);
-void func_800AA358(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up);
-void func_800AA3F0(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up);
+void View_LookAt(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up);  // was func_800AA358 [LLM:HIGH]
+void View_LookAtUnsafe(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up);  // was func_800AA3F0 [LLM:HIGH]
 void View_SetScale(View* view, f32 scale);
 void View_GetScale(View* view, f32* scale);
 void View_SetPerspective(View* view, f32 fovy, f32 near, f32 far);  // was func_800AA460 [oot]
@@ -1411,14 +1411,14 @@ void View_InitDistortion(View* view);
 void View_ClearDistortion(View* view);
 void View_SetDistortion(View* view, Vec3f orientation, Vec3f scale, f32 speed);
 s32 View_StepDistortion(View* view, Mtx* projectionMtx);
-void func_800AAA50(View* view, s32 arg1);
-s32 func_800AAA9C(View* view);
-s32 func_800AB0A8(View* view);
-s32 func_800AB2C4(View* view);
-s32 func_800AB560(View* view);
-s32 func_800AB944(View* view);
-s32 func_800AB9EC(View* view, s32 arg1, Gfx** p);
-s32 func_800ABE74(f32 eyeX, f32 eyeY, f32 eyeZ);
+void View_Apply(View* view, s32 arg1);  // was func_800AAA50 [LLM:HIGH]
+s32 View_ApplyPerspective(View* view);  // was func_800AAA9C [LLM:HIGH]
+s32 View_ApplyOrtho(View* view);  // was func_800AB0A8 [LLM:HIGH]
+s32 View_ApplyOrthoToOverlay(View* view);  // was func_800AB2C4 [LLM:HIGH]
+s32 View_ApplyPerspectiveToOverlay(View* view);  // was func_800AB560 [LLM:HIGH]
+s32 View_UpdateViewingMatrix(View* view);  // was func_800AB944 [LLM:HIGH]
+s32 View_ApplyTo(View* view, s32 arg1, Gfx** p);  // was func_800AB9EC [LLM:HIGH]
+s32 View_ErrorCheckEyePosition(f32 eyeX, f32 eyeY, f32 eyeZ);  // was func_800ABE74 [LLM:HIGH]
 void ViMode_LogPrint(OSViMode* viMode);
 void ViMode_Configure(ViMode* viMode, s32 mode, s32 type, s32 unk_70, s32 unk_74, s32 unk_78, s32 unk_7C, s32 width,
                       s32 height, s32 unk_left, s32 unk_right, s32 unk_top, s32 unk_bottom);
diff --git a/soh/src/code/speed_meter.c b/soh/src/code/speed_meter.c
index 8717ae4a5..556f9aa71 100644
--- a/soh/src/code/speed_meter.c
+++ b/soh/src/code/speed_meter.c
@@ -85,7 +85,7 @@ void SpeedMeter_DrawTimeEntries(SpeedMeter* this, GraphicsContext* gfxCtx) {
     SET_FULLSCREEN_VIEWPORT(&view);
 
     gfx = OVERLAY_DISP;
-    func_800AB9EC(&view, 0xF, &gfx);
+    View_ApplyTo(&view, 0xF, &gfx);  // was func_800AB9EC [LLM:HIGH]
 
     gDPPipeSync(gfx++);
     gDPSetOtherMode(gfx++,
@@ -141,7 +141,7 @@ void SpeedMeter_DrawAllocEntry(SpeedMeterAllocEntry* this, GraphicsContext* gfxC
         SET_FULLSCREEN_VIEWPORT(&view);
 
         gfx = OVERLAY_DISP;
-        func_800AB9EC(&view, 0xF, &gfx);
+        View_ApplyTo(&view, 0xF, &gfx);  // was func_800AB9EC [LLM:HIGH]
 
         gDPPipeSync(gfx++);
         gDPSetOtherMode(gfx++,
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 90945fda1..2a52b5fb6 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -1785,7 +1785,7 @@ Gfx* Actor_SetupHilite(Vec3f* object, Vec3f* eye, Vec3f* lightDir, GraphicsConte
                      eye->x, eye->y, eye->z, object->x, object->y, object->z, lightDir->x, lightDir->y, lightDir->z);
     }
 
-    func_800ABE74(correctedEyeX, eye->y, eye->z);
+    View_ErrorCheckEyePosition(correctedEyeX, eye->y, eye->z);  // was func_800ABE74 [LLM:HIGH]
     guLookAtHilite(&sHiliteLookAtMtx, lookAt, *hilite, correctedEyeX, eye->y, eye->z, object->x, object->y, object->z, 0.0f,  // was D_8015BBA8 [LLM:HIGH]
                    1.0f, 0.0f, lightDir->x, lightDir->y, lightDir->z, lightDir->x, lightDir->y, lightDir->z, 0x10,
                    0x10);
diff --git a/soh/src/code/z_camera.c b/soh/src/code/z_camera.c
index c341e7261..82d9baffc 100644
--- a/soh/src/code/z_camera.c
+++ b/soh/src/code/z_camera.c
@@ -7684,7 +7684,7 @@ Vec3s Camera_Update(Camera* camera) {
     if (gDbgCamEnabled) {
         camera->play->view.fovy = sDbCamera.fov;  // was D_8015BD80 [LLM:HIGH]
         DbCamera_Update(&sDbCamera, camera);  // was D_8015BD80 [LLM:HIGH]
-        func_800AA358(&camera->play->view, &sDbCamera.eye, &sDbCamera.at, &sDbCamera.unk_1C);  // was D_8015BD80 [LLM:HIGH]
+        View_LookAt(&camera->play->view, &sDbCamera.eye, &sDbCamera.at, &sDbCamera.unk_1C);  // was D_8015BD80 [LLM:HIGH]
         if (R_DBG_CAM_UPDATE) {
             osSyncPrintf("camera: debug out\n");
         }
@@ -7735,7 +7735,7 @@ Vec3s Camera_Update(Camera* camera) {
         View_SetScale(&camera->play->view, 1.0f);
     }
     camera->play->view.fovy = viewFov;
-    func_800AA358(&camera->play->view, &viewEye, &viewAt, &viewUp);
+    View_LookAt(&camera->play->view, &viewEye, &viewAt, &viewUp);  // was func_800AA358 [LLM:HIGH]
     camera->camDir.x = eyeAtAngle.pitch;
     camera->camDir.y = eyeAtAngle.yaw;
     camera->camDir.z = 0;
diff --git a/soh/src/code/z_message_PAL.c b/soh/src/code/z_message_PAL.c
index 4f2c5f362..8629b8f23 100644
--- a/soh/src/code/z_message_PAL.c
+++ b/soh/src/code/z_message_PAL.c
@@ -3126,7 +3126,7 @@ void Message_DrawTextBox(PlayState* play, Gfx** p) {
 
 void Message_SetView(View* view) {
     SET_FULLSCREEN_VIEWPORT(view);
-    func_800AB2C4(view);
+    View_ApplyOrthoToOverlay(view);  // was func_800AB2C4 [LLM:HIGH]
 }
 
 #define FLASH_NOTE_COLORS(target, source)                                             \
diff --git a/soh/src/code/z_parameter.c b/soh/src/code/z_parameter.c
index 298adfad6..75eb3ac7e 100644
--- a/soh/src/code/z_parameter.c
+++ b/soh/src/code/z_parameter.c
@@ -5081,7 +5081,7 @@ void Interface_SetViewport(PlayState* play, s32 topY, s32 bottomY, s32 leftX, s3
     up.x = up.z = 0.0f;
     up.y = 1.0f;
 
-    func_800AA358(&interfaceCtx->view, &eye, &lookAt, &up);
+    View_LookAt(&interfaceCtx->view, &eye, &lookAt, &up);  // was func_800AA358 [LLM:HIGH]
 
     interfaceCtx->viewport.topY = topY;
     interfaceCtx->viewport.bottomY = bottomY;
@@ -5090,14 +5090,14 @@ void Interface_SetViewport(PlayState* play, s32 topY, s32 bottomY, s32 leftX, s3
     View_SetViewport(&interfaceCtx->view, &interfaceCtx->viewport);
 
     View_SetPerspective(&interfaceCtx->view, 60.0f, 10.0f, 60.0f);
-    func_800AB560(&interfaceCtx->view);
+    View_ApplyPerspectiveToOverlay(&interfaceCtx->view);  // was func_800AB560 [LLM:HIGH]
 }
 */
 
 // LLM generated name (HIGH), was func_8008A994: resets the interface view to the full-screen viewport and applies it.
 void Interface_SetFullscreenViewport(InterfaceContext* interfaceCtx) {
     SET_FULLSCREEN_VIEWPORT(&interfaceCtx->view);
-    func_800AB2C4(&interfaceCtx->view);
+    View_ApplyOrthoToOverlay(&interfaceCtx->view);  // was func_800AB2C4 [LLM:HIGH]
 }
 
 const char* digitTextures[] = { gCounterDigit0Tex, gCounterDigit1Tex, gCounterDigit2Tex, gCounterDigit3Tex,
diff --git a/soh/src/code/z_play.c b/soh/src/code/z_play.c
index b2ff9233f..9eb82edd7 100644
--- a/soh/src/code/z_play.c
+++ b/soh/src/code/z_play.c
@@ -1361,7 +1361,7 @@ void Play_Draw(PlayState* play) {
         POLY_XLU_DISP = Play_SetFog(play, POLY_XLU_DISP);
 
         View_SetPerspective(&play->view, play->view.fovy, play->view.zNear, play->lightCtx.fogFar);
-        func_800AAA50(&play->view, 15);
+        View_Apply(&play->view, 15);  // was func_800AAA50 [LLM:HIGH]
 
         // Flip the projections and invert culling for the OPA and XLU display buffers
         // These manage the world and effects when we are not drawing kaleido
@@ -1408,7 +1408,7 @@ void Play_Draw(PlayState* play) {
 
                 SET_FULLSCREEN_VIEWPORT(&view);
 
-                func_800AB9EC(&view, 15, &gfxP);
+                View_ApplyTo(&view, 15, &gfxP);  // was func_800AB9EC [LLM:HIGH]
                 play->transitionCtx.draw(&play->transitionCtx.data, &gfxP);
             }
 
@@ -1626,7 +1626,7 @@ Play_Draw_skip:
 
     if (play->view.unk_124 != 0) {
         Camera_Update(GET_ACTIVE_CAM(play));
-        func_800AB944(&play->view);
+        View_UpdateViewingMatrix(&play->view);  // was func_800AB944 [LLM:HIGH]
         play->view.unk_124 = 0;
         if (play->skyboxId && (play->skyboxId != SKYBOX_UNSET_1D) && !play->envCtx.skyboxDisabled) {
             SkyboxDraw_UpdateMatrix(&play->skyboxCtx, play->view.eye.x, play->view.eye.y, play->view.eye.z);
diff --git a/soh/src/code/z_sample.c b/soh/src/code/z_sample.c
index 21d5b441f..cd1e3c3eb 100644
--- a/soh/src/code/z_sample.c
+++ b/soh/src/code/z_sample.c
@@ -19,7 +19,7 @@ void Sample_Draw(SampleContext* this) {
     Gfx_SetupFrame(gfxCtx, 0, 0, 0);
 
     view->flags = 1 | 2 | 4;
-    func_800AAA50(view, 15);
+    View_Apply(view, 15);  // was func_800AAA50 [LLM:HIGH]
 
     {
         Mtx* mtx = Graph_Alloc(gfxCtx, sizeof(Mtx));
@@ -72,7 +72,7 @@ void Sample_SetupView(SampleContext* this) {
         up.z = 0.0f;
         up.y = 1.0f;
 
-        func_800AA358(view, &eye, &lookAt, &up);
+        View_LookAt(view, &eye, &lookAt, &up);  // was func_800AA358 [LLM:HIGH]
     }
 }
 
diff --git a/soh/src/code/z_view.c b/soh/src/code/z_view.c
index a3515bec6..a5d7dc984 100644
--- a/soh/src/code/z_view.c
+++ b/soh/src/code/z_view.c
@@ -80,7 +80,8 @@ void View_GetParams(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
     // view->flags |= 1;
 }
 
-void func_800AA358(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
+// LLM generated name (HIGH), was func_800AA358: stores the eye, look-at and up vectors, nudging the eye off a perfectly vertical line first
+void View_LookAt(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
     if (eye->x == lookAt->x && eye->z == lookAt->z) {
         eye->x += 0.1f;
     }
@@ -91,7 +92,8 @@ void func_800AA358(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
     view->flags |= 1;
 }
 
-void func_800AA3F0(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
+// LLM generated name (HIGH), was func_800AA3F0: the same store without that nudge
+void View_LookAtUnsafe(View* view, Vec3f* eye, Vec3f* lookAt, Vec3f* up) {
     view->eye = *eye;
     view->lookAt = *lookAt;
     view->up = *up;
@@ -280,13 +282,14 @@ s32 View_StepDistortion(View* view, Mtx* projectionMtx) {
     return true;
 }
 
-void func_800AAA50(View* view, s32 arg1) {
+// LLM generated name (HIGH), was func_800AAA50: dispatches to the orthographic or perspective apply by the caller's flags
+void View_Apply(View* view, s32 arg1) {
     arg1 = (view->flags & arg1) | (arg1 >> 4);
 
     if (arg1 & 8) {
-        func_800AB0A8(view);
+        View_ApplyOrtho(view);  // was func_800AB0A8 [LLM:HIGH]
     } else {
-        func_800AAA9C(view);
+        View_ApplyPerspective(view);  // was func_800AAA9C [LLM:HIGH]
     }
 }
 
@@ -294,7 +297,8 @@ static float sqr(float a) {
     return a * a;
 }
 
-s32 func_800AAA9C(View* view) {
+// LLM generated name (HIGH), was func_800AAA9C: builds the viewport, guPerspective projection and viewing matrix and binds them
+s32 View_ApplyPerspective(View* view) {
     f32 aspect;
     s32 width;
     s32 height;
@@ -337,7 +341,7 @@ s32 func_800AAA9C(View* view) {
         view->eye.z += 1.0f;
     }
 
-    func_800ABE74(view->eye.x, view->eye.y, view->eye.z);
+    View_ErrorCheckEyePosition(view->eye.x, view->eye.y, view->eye.z);  // was func_800ABE74 [LLM:HIGH]
     MtxF viewingF;
     guLookAtF(viewingF.mf, view->eye.x, view->eye.y, view->eye.z, view->lookAt.x, view->lookAt.y, view->lookAt.z,
               view->up.x, view->up.y, view->up.z);
@@ -488,7 +492,8 @@ s32 func_800AAA9C(View* view) {
     return 1;
 }
 
-s32 func_800AB0A8(View* view) {
+// LLM generated name (HIGH), was func_800AB0A8: builds the viewport and a guOrtho projection and binds them
+s32 View_ApplyOrtho(View* view) {
     Vp* vp;
     Mtx* projection;
     GraphicsContext* gfxCtx = view->gfxCtx;
@@ -523,7 +528,8 @@ s32 func_800AB0A8(View* view) {
     return 1;
 }
 
-s32 func_800AB2C4(View* view) {
+// LLM generated name (HIGH), was func_800AB2C4: the same orthographic setup written into the overlay display list, with the flipped projection kept
+s32 View_ApplyOrthoToOverlay(View* view) {
     Vp* vp;
     Mtx* projection;
     Mtx* projectionFlipped;
@@ -573,7 +579,8 @@ s32 func_800AB2C4(View* view) {
     return 1;
 }
 
-s32 func_800AB560(View* view) {
+// LLM generated name (HIGH), was func_800AB560: the perspective counterpart that writes into the overlay display list
+s32 View_ApplyPerspectiveToOverlay(View* view) {
     s32 pad[2];
     f32 aspect;
     s32 width;
@@ -620,7 +627,7 @@ s32 func_800AB560(View* view) {
         view->eye.z += 1.0f;
     }
 
-    func_800ABE74(view->eye.x, view->eye.y, view->eye.z);
+    View_ErrorCheckEyePosition(view->eye.x, view->eye.y, view->eye.z);  // was func_800ABE74 [LLM:HIGH]
     guLookAt(viewing, view->eye.x, view->eye.y, view->eye.z, view->lookAt.x, view->lookAt.y, view->lookAt.z, view->up.x,
              view->up.y, view->up.z);
 
@@ -633,10 +640,11 @@ s32 func_800AB560(View* view) {
     return 1;
 }
 
-s32 func_800AB944(View* view) {
+// LLM generated name (HIGH), was func_800AB944: rebuilds the viewing matrix from the current eye, look-at and up
+s32 View_UpdateViewingMatrix(View* view) {
     OPEN_DISPS(view->gfxCtx);
 
-    func_800ABE74(view->eye.x, view->eye.y, view->eye.z);
+    View_ErrorCheckEyePosition(view->eye.x, view->eye.y, view->eye.z);  // was func_800ABE74 [LLM:HIGH]
     guLookAt(view->viewingPtr, view->eye.x, view->eye.y, view->eye.z, view->lookAt.x, view->lookAt.y, view->lookAt.z,
              view->up.x, view->up.y, view->up.z);
 
@@ -645,7 +653,8 @@ s32 func_800AB944(View* view) {
     return 1;
 }
 
-s32 func_800AB9EC(View* view, s32 arg1, Gfx** gfxp) {
+// LLM generated name (HIGH), was func_800AB9EC: the flag-driven apply that writes into a caller-supplied display list rather than the context
+s32 View_ApplyTo(View* view, s32 arg1, Gfx** gfxp) {
     Gfx* gfx = *gfxp;
     GraphicsContext* gfxCtx = view->gfxCtx;
     s32 width;
@@ -702,7 +711,7 @@ s32 func_800AB9EC(View* view, s32 arg1, Gfx** gfxp) {
         LOG_CHECK_NULL_POINTER("viewing", viewing);
         view->viewingPtr = viewing;
 
-        func_800ABE74(view->eye.x, view->eye.y, view->eye.z);
+        View_ErrorCheckEyePosition(view->eye.x, view->eye.y, view->eye.z);  // was func_800ABE74 [LLM:HIGH]
         guLookAt(viewing, view->eye.x, view->eye.y, view->eye.z, view->lookAt.x, view->lookAt.y, view->lookAt.z,
                  view->up.x, view->up.y, view->up.z);
 
@@ -717,7 +726,8 @@ s32 func_800AB9EC(View* view, s32 arg1, Gfx** gfxp) {
     return 1;
 }
 
-s32 func_800ABE74(f32 eyeX, f32 eyeY, f32 eyeZ) {
+// LLM generated name (HIGH), was func_800ABE74: reports whether an eye position is beyond the coordinate range guLookAt can take
+s32 View_ErrorCheckEyePosition(f32 eyeX, f32 eyeY, f32 eyeZ) {
     s32 error = 0;
 
     if (SQ(eyeX) + SQ(eyeY) + SQ(eyeZ) > SQ(32767.0f)) {
diff --git a/soh/src/overlays/gamestates/ovl_file_choose/z_file_choose.c b/soh/src/overlays/gamestates/ovl_file_choose/z_file_choose.c
index 51548ab05..7e26d7f09 100644
--- a/soh/src/overlays/gamestates/ovl_file_choose/z_file_choose.c
+++ b/soh/src/overlays/gamestates/ovl_file_choose/z_file_choose.c
@@ -176,8 +176,8 @@ void FileChoose_SetView(FileChooseContext* this, f32 eyeX, f32 eyeY, f32 eyeZ) {
 
     up.y = 1.0f;
 
-    func_800AA358(&this->view, &eye, &lookAt, &up);
-    func_800AAA50(&this->view, 0x7F);
+    View_LookAt(&this->view, &eye, &lookAt, &up);  // was func_800AA358 [LLM:HIGH]
+    View_Apply(&this->view, 0x7F);  // was func_800AAA50 [LLM:HIGH]
 }
 
 Gfx* FileChoose_QuadTextureIA8(Gfx* gfx, void* texture, s16 width, s16 height, s16 point) {
diff --git a/soh/src/overlays/gamestates/ovl_select/z_select.c b/soh/src/overlays/gamestates/ovl_select/z_select.c
index ff69ada90..dc659ce07 100644
--- a/soh/src/overlays/gamestates/ovl_select/z_select.c
+++ b/soh/src/overlays/gamestates/ovl_select/z_select.c
@@ -1729,7 +1729,7 @@ void Select_DrawMenu(SelectContext* this) {
     gSPSegment(POLY_OPA_DISP++, 0x00, NULL);
     Gfx_SetupFrame(gfxCtx, 0, 0, 0);
     SET_FULLSCREEN_VIEWPORT(&this->view);
-    func_800AAA50(&this->view, 0xF);
+    View_Apply(&this->view, 0xF);  // was func_800AAA50 [LLM:HIGH]
     Gfx_SetupDL_28Opa(gfxCtx);
 
     // printer = alloca(sizeof(GfxPrint));
@@ -1760,7 +1760,7 @@ void Select_DrawLoadingScreen(SelectContext* this) {
     gSPSegment(POLY_OPA_DISP++, 0x00, NULL);
     Gfx_SetupFrame(gfxCtx, 0, 0, 0);
     SET_FULLSCREEN_VIEWPORT(&this->view);
-    func_800AAA50(&this->view, 0xF);
+    View_Apply(&this->view, 0xF);  // was func_800AAA50 [LLM:HIGH]
     Gfx_SetupDL_28Opa(gfxCtx);
 
     // printer = alloca(sizeof(GfxPrint));
@@ -1781,7 +1781,7 @@ void Select_Draw(SelectContext* this) {
     gSPSegment(POLY_OPA_DISP++, 0x00, NULL);
     Gfx_SetupFrame(gfxCtx, 0, 0, 0);
     SET_FULLSCREEN_VIEWPORT(&this->view);
-    func_800AAA50(&this->view, 0xF);
+    View_Apply(&this->view, 0xF);  // was func_800AAA50 [LLM:HIGH]
 
     if (!this->state.running) {
         Select_DrawLoadingScreen(this);
diff --git a/soh/src/overlays/gamestates/ovl_title/z_title.c b/soh/src/overlays/gamestates/ovl_title/z_title.c
index f94ce99cf..4a318faa3 100644
--- a/soh/src/overlays/gamestates/ovl_title/z_title.c
+++ b/soh/src/overlays/gamestates/ovl_title/z_title.c
@@ -54,8 +54,8 @@ void Title_SetupView(TitleContext* this, f32 x, f32 y, f32 z) {
     up.y = 1.0f;
 
     View_SetPerspective(view, 30.0f, 10.0f, 12800.0f);
-    func_800AA358(view, &eye, &lookAt, &up);
-    func_800AAA50(view, 0xF);
+    View_LookAt(view, &eye, &lookAt, &up);  // was func_800AA358 [LLM:HIGH]
+    View_Apply(view, 0xF);  // was func_800AAA50 [LLM:HIGH]
 }
 
 void Title_Draw(TitleContext* this) {
diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
index 52397f710..8c49d6f64 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
@@ -2580,8 +2580,8 @@ void KaleidoScope_SetView(PauseContext* pauseCtx, f32 x, f32 y, f32 z) {
     up.x = up.z = 0.0f;
     up.y = 1.0f;
 
-    func_800AA358(&pauseCtx->view, &eye, &lookAt, &up);
-    func_800AAA50(&pauseCtx->view, 127);
+    View_LookAt(&pauseCtx->view, &eye, &lookAt, &up);  // was func_800AA358 [LLM:HIGH]
+    View_Apply(&pauseCtx->view, 127);  // was func_800AAA50 [LLM:HIGH]
 }
 
 static u8 sPageVtxColorR[][4] = {
@@ -3591,7 +3591,7 @@ void KaleidoScope_Draw(PlayState* play) {
         KaleidoScope_DrawDebugEditor(play);
     }
 
-    func_800AAA50(&play->view, 15);
+    View_Apply(&play->view, 15);  // was func_800AAA50 [LLM:HIGH]
 
     CLOSE_DISPS(play->state.gfxCtx);
 }
-- 
2.55.0

