From 601f3b8e061957ad8f7c1f7f0c7ed0222dca2b19 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:08:49 +0000
Subject: [PATCH 3231/3799] soh: rename D_8082ADE0 -> sInfoPanelRightWidths

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the width of the right info panel text in each language
Defined in soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
index 929d31ddb..6380f151c 100644
--- a/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
+++ b/soh/src/overlays/misc/ovl_kaleido_scope/z_kaleido_scope_PAL.c
@@ -1955,7 +1955,7 @@ void KaleidoScope_DrawInfoPanel(PlayState* play) {
         { gPauseToSelectItemENGTex, gPauseToSelectItemGERTex, gPauseToSelectItemFRATex, gPauseToSelectItemJPNTex },
     };
     static u16 sInfoPanelLeftWidths[4] = { 56, 88, 80, 56 };  // was D_8082ADD8 [LLM:HIGH]
-    static u16 D_8082ADE0[4] = { 64, 88, 72, 48 };
+    static u16 sInfoPanelRightWidths[4] = { 64, 88, 72, 48 };  // was D_8082ADE0 [LLM:HIGH]
     static u16 D_8082ADE8[4] = { 80, 104, 112, 80 };
     static s16 D_8082ADF0[][4] = {
         { 180, 210, 255, 220 },
@@ -2260,11 +2260,11 @@ void KaleidoScope_DrawInfoPanel(PlayState* play) {
                 pauseCtx->infoPanelVtx[16].v.ob[0] + WREG(52 + languageOffset);
 
             pauseCtx->infoPanelVtx[21].v.ob[0] = pauseCtx->infoPanelVtx[23].v.ob[0] =
-                pauseCtx->infoPanelVtx[20].v.ob[0] + D_8082ADE0[gSaveContext.language];
+                pauseCtx->infoPanelVtx[20].v.ob[0] + sInfoPanelRightWidths[gSaveContext.language];  // was D_8082ADE0 [LLM:HIGH]
 
             pauseCtx->infoPanelVtx[17].v.tc[0] = pauseCtx->infoPanelVtx[19].v.tc[0] = 0x300;
 
-            pauseCtx->infoPanelVtx[21].v.tc[0] = pauseCtx->infoPanelVtx[23].v.tc[0] = D_8082ADE0[gSaveContext.language]
+            pauseCtx->infoPanelVtx[21].v.tc[0] = pauseCtx->infoPanelVtx[23].v.tc[0] = sInfoPanelRightWidths[gSaveContext.language]  // was D_8082ADE0 [LLM:HIGH]
                                                                                       << 5;
             gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, aButtonColor.r, aButtonColor.g, aButtonColor.b, 255);
             // gSPDisplayList(POLY_OPA_DISP++, gAButtonIconDL);//This is changed to load the texture only so we can prim
@@ -2277,7 +2277,7 @@ void KaleidoScope_DrawInfoPanel(PlayState* play) {
             gDPSetPrimColor(POLY_OPA_DISP++, 0, 0, 255, 255, 255, 255);
 
             POLY_OPA_DISP = KaleidoScope_QuadTextureIA8(POLY_OPA_DISP, sToDecideTextures[gSaveContext.language],
-                                                        D_8082ADE0[gSaveContext.language], 16, 4);
+                                                        sInfoPanelRightWidths[gSaveContext.language], 16, 4);  // was D_8082ADE0 [LLM:HIGH]
         } else if (pauseCtx->cursorSpecialPos != 0) {
             if ((pauseCtx->state == 6) && (pauseCtx->unk_1E4 == 0)) {
                 pauseCtx->infoPanelVtx[16].v.ob[0] = pauseCtx->infoPanelVtx[18].v.ob[0] = -63;
-- 
2.55.0

