From 0f092b1a6be9bf99be8cb3cb6105d78a8d197bc3 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:41:08 +0000
Subject: [PATCH 2580/3799] soh: rename func_80ACA5C8 -> EnOwl_SetupUnfoldWings

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: switches to the perched wait on gOwlUnfoldWingsAnim and reseeds the blink
Defined in soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
index c155e2dac..8cb94496f 100644
--- a/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
+++ b/soh/src/overlays/actors/ovl_En_Owl/z_en_owl.c
@@ -301,7 +301,8 @@ s32 EnOwl_OfferTalk(EnOwl* this, PlayState* play, u16 textId) {
     }
 }
 
-void func_80ACA5C8(EnOwl* this) {
+// LLM generated name (HIGH), was func_80ACA5C8: switches to the perched wait on gOwlUnfoldWingsAnim and reseeds the blink
+void EnOwl_SetupUnfoldWings(EnOwl* this) {
     EnOwl_ChangeMode(this, func_80ACBEA0, func_80ACC540, &this->skelAnime, &gOwlUnfoldWingsAnim, 0.0f);
     this->eyeTexIndex = 0;
     this->blinkTimer = Rand_S16Offset(60, 60);
@@ -314,7 +315,7 @@ void func_80ACA62C(EnOwl* this, PlayState* play) {
         Flags_SetSwitch(play, switchFlag);
         osSyncPrintf(VT_FGCOL(CYAN) " Actor_Environment_sw = %d\n" VT_RST, Flags_GetSwitch(play, switchFlag));
     }
-    func_80ACA5C8(this);
+    EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
 }
 
 void func_80ACA690(EnOwl* this, PlayState* play) {
@@ -587,7 +588,7 @@ void EnOwl_WaitZoraRiver(EnOwl* this, PlayState* play) {
 void func_80ACB148(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA5C8(this);
+        EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
         this->actionFunc = func_80ACC30C;
         Flags_SetSwitch(play, 0x23);
     }
@@ -608,7 +609,7 @@ void EnOwl_WaitHyliaShortcut(EnOwl* this, PlayState* play) {
 void func_80ACB22C(EnOwl* this, PlayState* play) {
     if (Actor_TextboxIsClosing(&this->actor, play)) {
         Audio_QueueSeqCmd(0x1 << 28 | SEQ_PLAYER_FANFARE << 24 | 0xFF);
-        func_80ACA5C8(this);
+        EnOwl_SetupUnfoldWings(this);  // was func_80ACA5C8 [LLM:HIGH]
         this->actionFunc = func_80ACC30C;
     }
 }
-- 
2.55.0

