From 8008a958c8687c558b34740f0d53a14c240bb050 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1908/3799] soh: rename func_808628C8 ->
 EnTest_CircleAroundPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: strafes sideways around the player, reversing off walls and off missing floor for the ceiling type
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index 598764019..bae33a000 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -61,7 +61,8 @@ void EnTest_FlinchFront(EnTest* this, PlayState* play);  // was func_808621D4 [L
 // LLM generated name (HIGH), was func_80862418: the behind-hit twin of EnTest_FlinchFront, minus its jump-up-at-a-wall escape
 void EnTest_FlinchBehind(EnTest* this, PlayState* play);  // was func_80862418 [LLM:HIGH]
 void EnTest_Stunned(EnTest* this, PlayState* play);
-void func_808628C8(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_808628C8: strafes sideways around the player, reversing off walls and off missing floor for the ceiling type
+void EnTest_CircleAroundPlayer(EnTest* this, PlayState* play);  // was func_808628C8 [LLM:HIGH]
 void func_80862E6C(EnTest* this, PlayState* play);
 void func_80863044(EnTest* this, PlayState* play);
 void func_8086318C(EnTest* this, PlayState* play);
@@ -1365,12 +1366,12 @@ void EnTest_SetupCircleAroundPlayer(EnTest* this, PlayState* play) {
     this->actor.world.rot.y = this->actor.shape.rot.y + 0x3FFF;
     this->timer = (Rand_ZeroOne() * 20.0f) + 20.0f;
     this->unk_7C8 = 0x18;
-    EnTest_SetupAction(this, func_808628C8);
+    EnTest_SetupAction(this, EnTest_CircleAroundPlayer);  // was func_808628C8 [LLM:HIGH]
     this->unk_7EC = 0.0f;
 }
 
 // a variation of sidestep
-void func_808628C8(EnTest* this, PlayState* play) {
+void EnTest_CircleAroundPlayer(EnTest* this, PlayState* play) {
     s32 pad;
     Player* player = GET_PLAYER(play);
     s32 pad1;
@@ -1990,7 +1991,7 @@ void func_80864158(EnTest* this, f32 xzSpeed) {
     this->actor.world.rot.y = this->actor.shape.rot.y + 0x3FFF;
     this->timer = (Rand_ZeroOne() * 20.0f) + 15.0f;
     this->unk_7C8 = 0x18;
-    EnTest_SetupAction(this, func_808628C8);
+    EnTest_SetupAction(this, EnTest_CircleAroundPlayer);  // was func_808628C8 [LLM:HIGH]
 }
 
 /**
-- 
2.55.0

