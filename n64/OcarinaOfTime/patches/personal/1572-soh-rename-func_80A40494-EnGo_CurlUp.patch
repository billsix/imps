From 25103d8f747f99c021d9e5c39964418b1af8be74 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:27:41 +0000
Subject: [PATCH 1572/3799] soh: rename func_80A40494 -> EnGo_CurlUp

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: winds the uncurl animation backwards to frame 0, then plays the landing sfx, throws dust and installs the curled-up idle.
Defined in soh/src/overlays/actors/ovl_En_Go/z_en_go.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Go/z_en_go.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
index d4997a179..dd576b464 100644
--- a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
+++ b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
@@ -23,7 +23,8 @@ void EnGo_FireGenericActionFunc(EnGo* this, PlayState* play);
 void EnGo_CurledUp(EnGo* this, PlayState* play);
 void EnGo_WakeUp(EnGo* this, PlayState* play);
 
-void func_80A40494(EnGo* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A40494: winds the uncurl animation backwards to frame 0, then plays the landing sfx, throws dust and installs the curled-up idle.
+void EnGo_CurlUp(EnGo* this, PlayState* play);  // was func_80A40494 [LLM:HIGH]
 void func_80A405CC(EnGo* this, PlayState* play);
 void EnGo_BiggoronActionFunc(EnGo* this, PlayState* play);
 void func_80A408D8(EnGo* this, PlayState* play);
@@ -816,11 +817,11 @@ void EnGo_WakeUp(EnGo* this, PlayState* play) {
     } else if (!EnGo_IsCameraModified(this, play)) {
         EnGo_ReverseAnimation(this);
         this->skelAnime.playSpeed = 0.0f;
-        EnGo_SetupAction(this, func_80A40494);
+        EnGo_SetupAction(this, EnGo_CurlUp);  // was func_80A40494 [LLM:HIGH]
     }
 }
 
-void func_80A40494(EnGo* this, PlayState* play) {
+void EnGo_CurlUp(EnGo* this, PlayState* play) {
     f32 frame;
 
     Math_SmoothStepToF(&this->skelAnime.playSpeed, ((this->actor.params & 0xF0) == 0x90 ? 0.5f : 1.0f) * -0.5f, 0.1f,
@@ -918,7 +919,7 @@ void func_80A408D8(EnGo* this, PlayState* play) {
     }
 
     if (DECR(this->unk_212) == 0) {
-        EnGo_SetupAction(this, func_80A40494);
+        EnGo_SetupAction(this, EnGo_CurlUp);  // was func_80A40494 [LLM:HIGH]
     } else if (EnGo_IsCameraModified(this, play)) {
         EnGo_ReverseAnimation(this);
         Audio_PlaySfxGeneral(NA_SE_EN_GOLON_SIT_DOWN, &this->actor.projectedPos, 4, &gSfxDefaultFreqAndVolScale,
-- 
2.55.0

