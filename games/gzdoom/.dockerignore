# The image bakes no source (checkout/ is bind-mounted at run time), so keep the
# build context tiny — never ship the huge upstream checkout or build outputs.
checkout/
build/
install/
runDir/
*.tar
