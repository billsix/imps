From c14f89e35c704281e6d73afe07c8aed19e74f8e6 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 19:26:32 +0000
Subject: [PATCH 0809/3799] soh: rename func_809BC2A4 -> EnBdfire_BreatheFire

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params < 0 action: keeps the flame pinned to King Dodongo's firePos, fades its scale and alpha out when the boss stops breathing (unk_1E2 == 0), and kills the actor once faded. Paired with a point light set up alongside it.
Defined in soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c b/soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c
index 7c33e72b2..ddbae170f 100644
--- a/soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c
+++ b/soh/src/overlays/actors/ovl_En_Bdfire/z_en_bdfire.c
@@ -15,7 +15,8 @@ void EnBdfire_Update(Actor* thisx, PlayState* play);
 void EnBdfire_Draw(Actor* thisx, PlayState* play);
 
 void EnBdfire_DrawFire(EnBdfire* this, PlayState* play);
-void func_809BC2A4(EnBdfire* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BC2A4: the params < 0 action: keeps the flame pinned to King Dodongo's firePos, fades its scale and alpha out when the boss stops breathing (unk_1E2 == 0), and kills the actor once faded. Paired with a point light set up alongside it.
+void EnBdfire_BreatheFire(EnBdfire* this, PlayState* play);  // was func_809BC2A4 [LLM:HIGH]
 void func_809BC598(EnBdfire* this, PlayState* play);
 
 const ActorInit En_Bdfire_InitVars = {
@@ -46,7 +47,7 @@ void EnBdfire_Init(Actor* thisx, PlayState* play) {
     Actor_SetScale(&this->actor, 0.6f);
     EnbdFire_SetupDraw(this, EnBdfire_DrawFire);
     if (this->actor.params < 0) {
-        EnBdfire_SetupAction(this, func_809BC2A4);
+        EnBdfire_SetupAction(this, EnBdfire_BreatheFire);  // was func_809BC2A4 [LLM:HIGH]
         this->actor.scale.x = 2.8f;
         this->unk_154 = 90;
         Lights_PointNoGlowSetInfo(&this->lightInfoNoGlow, this->actor.world.pos.x, this->actor.world.pos.y,
@@ -81,7 +82,7 @@ void EnBdfire_Destroy(Actor* thisx, PlayState* play) {
     }
 }
 
-void func_809BC2A4(EnBdfire* this, PlayState* play) {
+void EnBdfire_BreatheFire(EnBdfire* this, PlayState* play) {
     BossDodongo* kingDodongo;
     s32 temp;
 
-- 
2.55.0

