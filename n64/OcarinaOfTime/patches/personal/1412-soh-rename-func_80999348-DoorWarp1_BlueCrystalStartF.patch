From d012fc328726d8d6b4da29dbdd75639662590092 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:39 +0000
Subject: [PATCH 1412/3799] soh: rename func_80999348 ->
 DoorWarp1_BlueCrystalStartFanfare

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: holds the player in place until the timer runs out, then queues NA_BGM_BOSS_CLEAR and arms the 200-frame wait.
Defined in soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
index 66437265e..1212c1440 100644
--- a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
+++ b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
@@ -21,7 +21,8 @@ void DoorWarp1_BlueCrystal(DoorWarp1* this, PlayState* play);
 void DoorWarp1_PurpleCrystal(DoorWarp1* this, PlayState* play);
 // LLM generated name (HIGH), was func_80999214: fades the crystal in while driving the environment's ambient, fog and light colours to black over forty frames, then hands off to the wait for the boss-clear fanfare.
 void DoorWarp1_BlueCrystalFadeToBlack(DoorWarp1* this, PlayState* play);  // was func_80999214 [LLM:HIGH]
-void func_80999348(DoorWarp1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80999348: holds the player in place until the timer runs out, then queues NA_BGM_BOSS_CLEAR and arms the 200-frame wait.
+void DoorWarp1_BlueCrystalStartFanfare(DoorWarp1* this, PlayState* play);  // was func_80999348 [LLM:HIGH]
 void func_809995D4(DoorWarp1* this, PlayState* play);
 void func_809998A4(DoorWarp1* this, PlayState* play);
 void DoorWarp1_ChildWarpIdle(DoorWarp1* this, PlayState* play);
@@ -332,13 +333,13 @@ void DoorWarp1_BlueCrystalFadeToBlack(DoorWarp1* this, PlayState* play) {
 
     this->warpTimer++;
     if (darkness <= 0) {
-        DoorWarp1_SetupAction(this, func_80999348);
+        DoorWarp1_SetupAction(this, DoorWarp1_BlueCrystalStartFanfare);  // was func_80999348 [LLM:HIGH]
     }
     this->actor.shape.rot.y += 0x320;
     DoorWarp1_SetPlayerPos(this, play);
 }
 
-void func_80999348(DoorWarp1* this, PlayState* play) {
+void DoorWarp1_BlueCrystalStartFanfare(DoorWarp1* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     DoorWarp1_SetPlayerPos(this, play);
-- 
2.55.0

