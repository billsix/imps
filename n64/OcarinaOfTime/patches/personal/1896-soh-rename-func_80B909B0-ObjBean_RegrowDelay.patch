From 901412dd446765a54ca481ccbb78f44c0d14e781 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:50:09 +0000
Subject: [PATCH 1896/3799] soh: rename func_80B909B0 -> ObjBean_RegrowDelay

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: holds that pause, restarting it whenever a horse tramples the patch.
Defined in soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
index 90f5d1ec1..fec26965f 100644
--- a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
+++ b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
@@ -59,7 +59,8 @@ void ObjBean_SetupWaitToFly(ObjBean* this);  // was func_80B908EC [LLM:HIGH]
 void ObjBean_WaitToFly(ObjBean* this, PlayState* play);  // was func_80B90918 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B90970: hides the plant, resets its scale and arms the 100-frame pause before it grows back.
 void ObjBean_SetupRegrowDelay(ObjBean* this);  // was func_80B90970 [LLM:HIGH]
-void func_80B909B0(ObjBean* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B909B0: holds that pause, restarting it whenever a horse tramples the patch.
+void ObjBean_RegrowDelay(ObjBean* this, PlayState* play);  // was func_80B909B0 [LLM:HIGH]
 void func_80B909F8(ObjBean* this);
 void func_80B90A34(ObjBean* this, PlayState* play);
 void ObjBean_SetupWaitForPlayer(ObjBean* this);
@@ -851,13 +852,13 @@ void ObjBean_WaitToFly(ObjBean* this, PlayState* play) {
 }
 
 void ObjBean_SetupRegrowDelay(ObjBean* this) {
-    this->actionFunc = func_80B909B0;
+    this->actionFunc = ObjBean_RegrowDelay;  // was func_80B909B0 [LLM:HIGH]
     ObjBean_SetDrawMode(this, 0);
     this->timer = 100;
     ObjBean_HidePlant(this);  // was func_80B8EDF4 [LLM:HIGH]
 }
 
-void func_80B909B0(ObjBean* this, PlayState* play) {
+void ObjBean_RegrowDelay(ObjBean* this, PlayState* play) {
     if (ObjBean_CheckForHorseTrample(this, play)) {
         this->timer = 100;
     } else if (this->timer <= 0) {
-- 
2.55.0

