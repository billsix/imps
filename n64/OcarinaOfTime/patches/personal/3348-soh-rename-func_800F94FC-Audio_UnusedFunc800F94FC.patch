From 70cc08b40f1802c6b081f804a53e8ad5a248179a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:14:49 +0000
Subject: [PATCH 3348/3799] soh: rename func_800F94FC ->
 Audio_UnusedFunc800F94FC

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: declared beside the audio entry points with no definition and no caller in the tree
Defined in soh/include/functions.h.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 9a17c7d48..dd4cae794 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -2162,7 +2162,8 @@ void Audio_UpdateActiveSounds(void);  // was func_800F8F88 [LLM:HIGH]
 u8 Audio_IsSfxPlaying(u32 sfxId);
 void Audio_ResetSounds(void);
 void Audio_StopSequence(u8, u16);  // was func_800F9474 [oot]
-void func_800F94FC(u32);
+// LLM generated name (GUESS), was func_800F94FC: declared beside the audio entry points with no definition and no caller in the tree
+void Audio_UnusedFunc800F94FC(u32);  // was func_800F94FC [LLM:GUESS]
 void Audio_ProcessSeqCmd(u32);
 void Audio_ProcessSeqCmds(void);
 u16 Audio_GetActiveSeqId(u8 a0);  // was func_800FA0B4 [oot]
-- 
2.55.0

