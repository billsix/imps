From 8c85a96a402c8d035fc993d0a0d8e25edd0c26fd Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:29:27 +0000
Subject: [PATCH 1588/3799] soh: rename func_80A917FC ->
 EnKakasi3_WaitForPlayback

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: dances through the playback and, once it finishes, closes the textbox, ends the camera shot and returns to the idle.
Defined in soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
index d16281d48..9e3d49444 100644
--- a/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
+++ b/soh/src/overlays/actors/ovl_En_Kakasi3/z_en_kakasi3.c
@@ -27,7 +27,8 @@ void EnKakasi3_StartRecording(EnKakasi3* this, PlayState* play);  // was func_80
 void EnKakasi3_WaitForRecording(EnKakasi3* this, PlayState* play);  // was func_80A91620 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A91760: waits out the current line, then opens the scarecrow's-song playback prompt and its one-point cutscene.
 void EnKakasi3_StartPlayback(EnKakasi3* this, PlayState* play);  // was func_80A91760 [LLM:HIGH]
-void func_80A917FC(EnKakasi3* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A917FC: dances through the playback and, once it finishes, closes the textbox, ends the camera shot and returns to the idle.
+void EnKakasi3_WaitForPlayback(EnKakasi3* this, PlayState* play);  // was func_80A917FC [LLM:HIGH]
 void func_80A9187C(EnKakasi3* this, PlayState* play);
 void func_80A918E4(EnKakasi3* this, PlayState* play);
 void func_80A91A90(EnKakasi3* this, PlayState* play);
@@ -322,12 +323,12 @@ void EnKakasi3_StartPlayback(EnKakasi3* this, PlayState* play) {
     if (this->dialogState == Message_GetState(&play->msgCtx) && Message_ShouldAdvance(play)) {
         play->msgCtx.msgMode = MSGMODE_PAUSED;
         Message_StartOcarinaSunsSongEnabled(play, OCARINA_ACTION_SCARECROW_PLAYBACK);
-        this->actionFunc = func_80A917FC;
+        this->actionFunc = EnKakasi3_WaitForPlayback;  // was func_80A917FC [LLM:HIGH]
         this->camId = OnePointCutscene_Init(play, 2280, -99, &this->actor, CAM_ID_MAIN);
     }
 }
 
-void func_80A917FC(EnKakasi3* this, PlayState* play) {
+void EnKakasi3_WaitForPlayback(EnKakasi3* this, PlayState* play) {
 
     if (play->msgCtx.ocarinaMode != OCARINA_MODE_0F) {
         EnKakasi3_Dance(this, play, 1);  // was func_80A90EBC [LLM:HIGH]
-- 
2.55.0

