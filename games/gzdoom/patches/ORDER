# Stream apply order for the GZDoom game-tree lane (apply.sh reads this).
# One stream name per line; listed streams apply first in this order, the rest
# follow alphabetically. `#` comments and blank lines are ignored.
#
# Streams commute here (they touch disjoint files), so this order is not load-
# bearing today; `book` is listed for explicitness. A future Phase-C CLI-WAD
# fix will add an `upstream-candidates` stream (unlisted → applies after `book`
# alphabetically); name it here if it ever must precede another stream.
book
