#!/usr/bin/env bash
# fake zenity for portable-file-dialogs: log the request, answer with the ROM the harness chose
printf "%s\n" "$*" >> "${FAKE_ZENITY_LOG:-/dev/null}"
case " $* " in *" --file-selection"*) echo "$FAKE_ZENITY_PICK"; exit 0;; esac
exit 0
