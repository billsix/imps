From f1b7e4fdeb8ceb1c4f5298fb822571799eee317e Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:31:18 +0000
Subject: [PATCH 1623/3799] soh: rename func_80AD587C -> EnPoField_SoulAppear

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: drifts the soul upward while fading its light in, then hands off to the hovering state.
Defined in soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c b/soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c
index b5ffb47f1..9d2cdb81c 100644
--- a/soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c
+++ b/soh/src/overlays/actors/ovl_En_Po_Field/z_en_po_field.c
@@ -34,7 +34,8 @@ void EnPoField_Flee(EnPoField* this, PlayState* play);
 void EnPoField_Death(EnPoField* this, PlayState* play);
 void EnPoField_Disappear(EnPoField* this, PlayState* play);
 void EnPoField_SoulIdle(EnPoField* this, PlayState* play);
-void func_80AD587C(EnPoField* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AD587C: drifts the soul upward while fading its light in, then hands off to the hovering state.
+void EnPoField_SoulAppear(EnPoField* this, PlayState* play);  // was func_80AD587C [LLM:HIGH]
 void func_80AD58D4(EnPoField* this, PlayState* play);
 void EnPoField_SoulDisappear(EnPoField* this, PlayState* play);
 void EnPoField_SoulInteract(EnPoField* this, PlayState* play);
@@ -348,7 +349,7 @@ void EnPoField_SetupSoulAppear(EnPoField* this) {
     if (this->actor.params == EN_PO_FIELD_BIG) {
         Sfx_PlaySfxCentered(NA_SE_SY_TRE_BOX_APPEAR);
     }
-    this->actionFunc = func_80AD587C;
+    this->actionFunc = EnPoField_SoulAppear;  // was func_80AD587C [LLM:HIGH]
 }
 
 void func_80AD4384(EnPoField* this) {
@@ -658,7 +659,7 @@ void EnPoField_SoulUpdateProperties(EnPoField* this, s32 arg1) {
                               this->lightColor.a * (200.0f / 255));
 }
 
-void func_80AD587C(EnPoField* this, PlayState* play) {
+void EnPoField_SoulAppear(EnPoField* this, PlayState* play) {
     this->actor.home.pos.y += 2.0f;
     EnPoField_SoulUpdateProperties(this, 20);
     if (this->lightColor.a == 255) {
-- 
2.55.0

