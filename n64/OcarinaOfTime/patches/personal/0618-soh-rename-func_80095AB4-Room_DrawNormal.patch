From 404e1a1a6d3b38b13e65549406c7a2d4cd8becdc Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 15:21:13 +0000
Subject: [PATCH 0618/3799] soh: rename func_80095AB4 -> Room_DrawNormal

Name source: zeldaret/oot (same decomp, same ROM address).
Structure verified against upstream at 0.95.
Defined in soh/src/code/z_room.c.
---
 soh/src/code/z_room.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/code/z_room.c b/soh/src/code/z_room.c
index cb9d4358a..d48c31cd4 100644
--- a/soh/src/code/z_room.c
+++ b/soh/src/code/z_room.c
@@ -13,7 +13,8 @@
 #include "soh/OTRGlobals.h"
 #include "soh/ResourceManagerHelpers.h"
 
-void func_80095AB4(PlayState* play, Room* room, u32 flags);
+// Name from zeldaret/oot (was func_80095AB4): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_room.c]
+void Room_DrawNormal(PlayState* play, Room* room, u32 flags);
 void func_80095D04(PlayState* play, Room* room, u32 flags);
 void func_80096F6C(PlayState* play, Room* room, u32 flags);
 
@@ -38,7 +39,7 @@ s32 OTRRoom_RequestNewRoom(PlayState* play, RoomContext* roomCtx, s32 roomNum);
 s32 OTRfunc_800973FC(PlayState* play, RoomContext* roomCtx);
 
 void (*sRoomDrawHandlers[])(PlayState* play, Room* room, u32 flags) = {
-    func_80095AB4,
+    Room_DrawNormal,
     func_80096F6C,
     func_80095D04,
 };
@@ -47,7 +48,7 @@ void func_80095AA0(PlayState* play, Room* room, Input* arg2, UNK_TYPE arg3) {
 }
 
 // Room Draw Polygon Type 0
-void func_80095AB4(PlayState* play, Room* room, u32 flags) {
+void Room_DrawNormal(PlayState* play, Room* room, u32 flags) {
     s32 i;
     PolygonType0* polygon0;
     PolygonDlist* polygonDlist;
-- 
2.55.0

