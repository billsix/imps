From 9c4c4df04db0fd57777761b2f79ab63469e8f3a9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:26 +0000
Subject: [PATCH 2405/3799] soh: rename func_809BD8DC -> EnBigokuta_JumpIn

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: arcs the octopus out of the water onto the ring, splashing and rumbling on landing
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index f250db2a1..ebb1ff73f 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -18,7 +18,8 @@ void EnBigokuta_SetupWaitBeforeCircle(EnBigokuta* this);  // was func_809BD3E0 [
 void func_809BDF34(EnBigokuta* this, PlayState* play);
 // LLM generated name (HIGH), was func_809BD84C: counts the intro wait down, calling out twice and starting the miniboss music
 void EnBigokuta_IntroWait(EnBigokuta* this, PlayState* play);  // was func_809BD84C [LLM:HIGH]
-void func_809BD8DC(EnBigokuta* this, PlayState* play);
+// LLM generated name (HIGH), was func_809BD8DC: arcs the octopus out of the water onto the ring, splashing and rumbling on landing
+void EnBigokuta_JumpIn(EnBigokuta* this, PlayState* play);  // was func_809BD8DC [LLM:HIGH]
 void func_809BDAE8(EnBigokuta* this, PlayState* play);
 void func_809BDB90(EnBigokuta* this, PlayState* play);
 void func_809BDC08(EnBigokuta* this, PlayState* play);
@@ -297,7 +298,7 @@ void EnBigokuta_SetupIntroWait(EnBigokuta* this) {
 void EnBigokuta_SetupJumpIn(EnBigokuta* this) {
     this->unk_196 = 21;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_STAL_JUMP);
-    this->actionFunc = func_809BD8DC;
+    this->actionFunc = EnBigokuta_JumpIn;  // was func_809BD8DC [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_809BD3AC: drops the octopus onto its starting point on the ring and arms the turn
@@ -435,7 +436,7 @@ void EnBigokuta_IntroWait(EnBigokuta* this, PlayState* play) {
     }
 }
 
-void func_809BD8DC(EnBigokuta* this, PlayState* play) {
+void EnBigokuta_JumpIn(EnBigokuta* this, PlayState* play) {
     Vec3f effectPos;
 
     this->unk_196--;
-- 
2.55.0

