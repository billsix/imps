From 6e38b07de7a87d5e52dca29ba05b88719f6cdec1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:25 +0000
Subject: [PATCH 315/488] soh: name the 9 address-named symbols defined in
 z_en_skb.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 9 are defined in soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c.

  * func_80AFCD60 -> EnSkb_ChooseAction
    deduced, confidence HIGH: the shared dispatch every finished action
    returns to: it burrows away in daylight, attacks when the player is
    close and in front, and otherwise walks.

  * func_80AFCDF8 -> EnSkb_SetupEmerge
    deduced, confidence HIGH: starts the uncurling animation with the
    appearing sfx, hides the actor from targeting and installs the emerging
    action.

  * func_80AFCE5C -> EnSkb_Emerge
    deduced, confidence HIGH: raises the Stalchild out of the ground,
    growing its shadow and throwing debris, and once it is fully up hands
    over to EnSkb_ChooseAction.

  * func_80AFCF48 -> EnSkb_SetupBurrow
    deduced, confidence HIGH: plays the uncurling animation in reverse with
    the hiding sfx and installs the burrowing action.

  * func_80AFCFF0 -> EnSkb_Burrow
    deduced, confidence HIGH: sinks the Stalchild back underground with
    debris and a shrinking shadow, then kills the actor.

  * func_80AFD0A4 -> EnSkb_SetupAdvance
    deduced, confidence HIGH: starts the walking animation, clears the
    headless yaw wobble and sets the walk speed before installing
    EnSkb_Advance.

  * func_80AFD33C -> EnSkb_SetupSwing
    deduced, confidence HIGH: starts the attacking animation, stops the
    Stalchild and installs EnSkb_SetupAttack.

  * func_80AFD47C -> EnSkb_SetupRecoil
    deduced, confidence HIGH: rewinds the attack animation from the frame it
    connected on and installs the recoil action.

  * func_80AFD508 -> EnSkb_Recoil
    deduced, confidence HIGH: plays that rewind out and returns to
    EnSkb_ChooseAction when it finishes.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c | 75 +++++++++++--------
 1 file changed, 42 insertions(+), 33 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c b/soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c
index 5c175682c..8bb9c8508 100644
--- a/soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c
+++ b/soh/src/overlays/actors/ovl_En_Skb/z_en_skb.c
@@ -10,17 +10,26 @@ void EnSkb_Destroy(Actor* thisx, PlayState* play);
 void EnSkb_Update(Actor* thisx, PlayState* play);
 void EnSkb_Draw(Actor* thisx, PlayState* play);
 
-void func_80AFCD60(EnSkb* this);
-void func_80AFCDF8(EnSkb* this);
-void func_80AFCE5C(EnSkb* this, PlayState* play);
-void func_80AFCF48(EnSkb* this);
-void func_80AFCFF0(EnSkb* this, PlayState* play);
-void func_80AFD0A4(EnSkb* this);
+// LLM generated name (HIGH), was func_80AFCD60: the shared dispatch every finished action returns to: it burrows away in daylight, attacks when the player is close and in front, and otherwise walks.
+void EnSkb_ChooseAction(EnSkb* this);  // was func_80AFCD60 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFCDF8: starts the uncurling animation with the appearing sfx, hides the actor from targeting and installs the emerging action.
+void EnSkb_SetupEmerge(EnSkb* this);  // was func_80AFCDF8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFCE5C: raises the Stalchild out of the ground, growing its shadow and throwing debris, and once it is fully up hands over to EnSkb_ChooseAction.
+void EnSkb_Emerge(EnSkb* this, PlayState* play);  // was func_80AFCE5C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFCF48: plays the uncurling animation in reverse with the hiding sfx and installs the burrowing action.
+void EnSkb_SetupBurrow(EnSkb* this);  // was func_80AFCF48 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFCFF0: sinks the Stalchild back underground with debris and a shrinking shadow, then kills the actor.
+void EnSkb_Burrow(EnSkb* this, PlayState* play);  // was func_80AFCFF0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFD0A4: starts the walking animation, clears the headless yaw wobble and sets the walk speed before installing EnSkb_Advance.
+void EnSkb_SetupAdvance(EnSkb* this);  // was func_80AFD0A4 [LLM:HIGH]
 void EnSkb_Advance(EnSkb* this, PlayState* play);
-void func_80AFD33C(EnSkb* this);
+// LLM generated name (HIGH), was func_80AFD33C: starts the attacking animation, stops the Stalchild and installs EnSkb_SetupAttack.
+void EnSkb_SetupSwing(EnSkb* this);  // was func_80AFD33C [LLM:HIGH]
 void EnSkb_SetupAttack(EnSkb* this, PlayState* play);
-void func_80AFD47C(EnSkb* this);
-void func_80AFD508(EnSkb* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AFD47C: rewinds the attack animation from the frame it connected on and installs the recoil action.
+void EnSkb_SetupRecoil(EnSkb* this);  // was func_80AFD47C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFD508: plays that rewind out and returns to EnSkb_ChooseAction when it finishes.
+void EnSkb_Recoil(EnSkb* this, PlayState* play);  // was func_80AFD508 [LLM:HIGH]
 void EnSkb_SetupStunned(EnSkb* this);
 // Name from zeldaret/oot (was func_80AFD59C): adopted upstream's name for this address; body verified to match upstream at 0.92. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Skb/z_en_skb.c]
 void EnSkb_Stunned(EnSkb* this, PlayState* play);
@@ -175,7 +184,7 @@ void EnSkb_Init(Actor* thisx, PlayState* play) {
     this->collider.elements[1].dim.modelSphere.radius = paramOffsetArm;
     this->actor.home.pos = this->actor.world.pos;
     this->actor.floorHeight = this->actor.world.pos.y;
-    func_80AFCDF8(this);
+    EnSkb_SetupEmerge(this);  // was func_80AFCDF8 [LLM:HIGH]
 }
 
 void EnSkb_Destroy(Actor* thisx, PlayState* play) {
@@ -193,27 +202,27 @@ void EnSkb_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyJntSph(play, &this->collider);
 }
 
-void func_80AFCD60(EnSkb* this) {
+void EnSkb_ChooseAction(EnSkb* this) {
     // Don't despawn stallchildren during daytime when enemy randomizer is enabled.
     if (IS_DAY && !CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0)) {
-        func_80AFCF48(this);
+        EnSkb_SetupBurrow(this);  // was func_80AFCF48 [LLM:HIGH]
     } else if (Actor_IsFacingPlayer(&this->actor, 0x11C7) &&
                (this->actor.xzDistToPlayer < (60.0f + (this->actor.params * 6.0f)))) {
-        func_80AFD33C(this);
+        EnSkb_SetupSwing(this);  // was func_80AFD33C [LLM:HIGH]
     } else {
-        func_80AFD0A4(this);
+        EnSkb_SetupAdvance(this);  // was func_80AFD0A4 [LLM:HIGH]
     }
 }
 
-void func_80AFCDF8(EnSkb* this) {
+void EnSkb_SetupEmerge(EnSkb* this) {
     Animation_PlayOnceSetSpeed(&this->skelAnime, &gStalchildUncurlingAnim, 1.0f);
     this->actionState = 0;
     this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_RIVA_APPEAR);
-    EnSkb_SetupAction(this, func_80AFCE5C);
+    EnSkb_SetupAction(this, EnSkb_Emerge);  // was func_80AFCE5C [LLM:HIGH]
 }
 
-void func_80AFCE5C(EnSkb* this, PlayState* play) {
+void EnSkb_Emerge(EnSkb* this, PlayState* play) {
     if (this->skelAnime.curFrame < 4.0f) {
         this->actor.world.rot.y = this->actor.yawTowardsPlayer;
         this->actor.shape.rot.y = this->actor.yawTowardsPlayer;
@@ -226,11 +235,11 @@ void func_80AFCE5C(EnSkb* this, PlayState* play) {
         EnSkb_SpawnDebris(play, this, &this->actor.world.pos);
     }
     if ((SkelAnime_Update(&this->skelAnime) != 0) && (0.0f == this->actor.shape.yOffset)) {
-        func_80AFCD60(this);
+        EnSkb_ChooseAction(this);  // was func_80AFCD60 [LLM:HIGH]
     }
 }
 
-void func_80AFCF48(EnSkb* this) {
+void EnSkb_SetupBurrow(EnSkb* this) {
     Animation_Change(&this->skelAnime, &gStalchildUncurlingAnim, -1.0f,
                      Animation_GetLastFrame(&gStalchildUncurlingAnim), 0.0f, ANIMMODE_ONCE, -4.0f);
     this->actionState = 0;
@@ -238,10 +247,10 @@ void func_80AFCF48(EnSkb* this) {
     this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
     this->actor.speedXZ = 0.0f;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_AKINDONUTS_HIDE);
-    EnSkb_SetupAction(this, func_80AFCFF0);
+    EnSkb_SetupAction(this, EnSkb_Burrow);  // was func_80AFCFF0 [LLM:HIGH]
 }
 
-void func_80AFCFF0(EnSkb* this, PlayState* play) {
+void EnSkb_Burrow(EnSkb* this, PlayState* play) {
     if ((Math_SmoothStepToF(&this->actor.shape.yOffset, -8000.0f, 1.0f, 500.0f, 0.0f) != 0.0f) &&
         (play->gameplayFrames & 1)) {
         EnSkb_SpawnDebris(play, this, &this->actor.world.pos);
@@ -252,7 +261,7 @@ void func_80AFCFF0(EnSkb* this, PlayState* play) {
     }
 }
 
-void func_80AFD0A4(EnSkb* this) {
+void EnSkb_SetupAdvance(EnSkb* this) {
     Animation_Change(&this->skelAnime, &gStalchildWalkingAnim, 0.96000004f, 0.0f,
                      Animation_GetLastFrame(&gStalchildWalkingAnim), ANIMMODE_LOOP, -4.0f);
     this->actionState = 4;
@@ -296,14 +305,14 @@ void EnSkb_Advance(EnSkb* this, PlayState* play) {
     // randomizer is enabled.
     if ((Math_Vec3f_DistXZ(&this->actor.home.pos, &player->actor.world.pos) > 800.0f || IS_DAY) &&
         !CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0)) {
-        func_80AFCF48(this);
+        EnSkb_SetupBurrow(this);  // was func_80AFCF48 [LLM:HIGH]
     } else if (Actor_IsFacingPlayer(&this->actor, 0x11C7) &&
                (this->actor.xzDistToPlayer < (60.0f + (this->actor.params * 6.0f)))) {
-        func_80AFD33C(this);
+        EnSkb_SetupSwing(this);  // was func_80AFD33C [LLM:HIGH]
     }
 }
 
-void func_80AFD33C(EnSkb* this) {
+void EnSkb_SetupSwing(EnSkb* this) {
     Animation_Change(&this->skelAnime, &gStalchildAttackingAnim, 0.6f, 0.0f,
                      Animation_GetLastFrame(&gStalchildAttackingAnim), ANIMMODE_ONCE_INTERP, 4.0f);
     this->collider.base.atFlags &= ~4;
@@ -324,24 +333,24 @@ void EnSkb_SetupAttack(EnSkb* this, PlayState* play) {
     }
     if (this->collider.base.atFlags & 4) {
         this->collider.base.atFlags &= ~6;
-        func_80AFD47C(this);
+        EnSkb_SetupRecoil(this);  // was func_80AFD47C [LLM:HIGH]
     } else if (SkelAnime_Update(&this->skelAnime) != 0) {
-        func_80AFCD60(this);
+        EnSkb_ChooseAction(this);  // was func_80AFCD60 [LLM:HIGH]
     }
 }
 
-void func_80AFD47C(EnSkb* this) {
+void EnSkb_SetupRecoil(EnSkb* this) {
     Animation_Change(&this->skelAnime, &gStalchildAttackingAnim, -0.4f, this->skelAnime.curFrame - 1.0f, 0.0f,
                      ANIMMODE_ONCE_INTERP, 0.0f);
     this->collider.base.atFlags &= ~4;
     this->actionState = 5;
     this->setColliderAT = 0;
-    EnSkb_SetupAction(this, func_80AFD508);
+    EnSkb_SetupAction(this, EnSkb_Recoil);  // was func_80AFD508 [LLM:HIGH]
 }
 
-void func_80AFD508(EnSkb* this, PlayState* play) {
+void EnSkb_Recoil(EnSkb* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime) != 0) {
-        func_80AFCD60(this);
+        EnSkb_ChooseAction(this);  // was func_80AFCD60 [LLM:HIGH]
     }
 }
 
@@ -368,7 +377,7 @@ void EnSkb_Stunned(EnSkb* this, PlayState* play) {
         if (this->actor.colChkInfo.health == 0) {
             EnSkb_SetupDeath(this, play);
         } else {
-            func_80AFCD60(this);
+            EnSkb_ChooseAction(this);  // was func_80AFCD60 [LLM:HIGH]
         }
     }
 }
@@ -405,7 +414,7 @@ void EnSkb_TakeDamage(EnSkb* this, PlayState* play) {
 
         Math_SmoothStepToS(&this->actor.shape.rot.y, this->actor.yawTowardsPlayer, 1, 0x1194, 0);
         if (SkelAnime_Update(&this->skelAnime) && (this->actor.bgCheckFlags & BGCHECKFLAG_GROUND)) {
-            func_80AFCD60(this);
+            EnSkb_ChooseAction(this);  // was func_80AFCD60 [LLM:HIGH]
         }
     }
 }
-- 
2.55.0

