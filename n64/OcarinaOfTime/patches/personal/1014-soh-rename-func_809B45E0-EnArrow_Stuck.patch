From caf406f31447ce528a7853e61a2f4d3c80999b97 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:18 +0000
Subject: [PATCH 1014/3799] soh: rename func_809B45E0 -> EnArrow_Stuck

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the action installed after the arrow embeds in geometry: only advances the animation and kills the actor when the timer runs out.
Defined in soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c b/soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c
index 50ad2600b..6e45573d6 100644
--- a/soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c
+++ b/soh/src/overlays/actors/ovl_En_Arrow/z_en_arrow.c
@@ -19,7 +19,8 @@ void EnArrow_Draw(Actor* thisx, PlayState* play);
 
 void EnArrow_Shoot(EnArrow* this, PlayState* play);
 void EnArrow_Fly(EnArrow* this, PlayState* play);
-void func_809B45E0(EnArrow* this, PlayState* play);
+// LLM generated name (HIGH), was func_809B45E0: the action installed after the arrow embeds in geometry: only advances the animation and kills the actor when the timer runs out.
+void EnArrow_Stuck(EnArrow* this, PlayState* play);  // was func_809B45E0 [LLM:HIGH]
 void func_809B4640(EnArrow* this, PlayState* play);
 
 const ActorInit En_Arrow_InitVars = {
@@ -366,7 +367,7 @@ void EnArrow_Fly(EnArrow* this, PlayState* play) {
                     Audio_PlayActorSound2(&this->actor, NA_SE_IT_ARROW_STICK_CRE);
                 }
             } else if (this->touchedPoly) {
-                EnArrow_SetupAction(this, func_809B45E0);
+                EnArrow_SetupAction(this, EnArrow_Stuck);  // was func_809B45E0 [LLM:HIGH]
                 Animation_PlayOnce(&this->skelAnime, &gArrow2Anim);
 
                 if (this->actor.params >= ARROW_NORMAL_LIT) {
@@ -423,7 +424,7 @@ void EnArrow_Fly(EnArrow* this, PlayState* play) {
     }
 }
 
-void func_809B45E0(EnArrow* this, PlayState* play) {
+void EnArrow_Stuck(EnArrow* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
 
     if (DECR(this->timer) == 0) {
-- 
2.55.0

