From 8e89070fb08db17d538ba6366b6adde4a6ea5cb9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:48 +0000
Subject: [PATCH 1839/3799] soh: rename func_80A69EC0 ->
 EnHorseLinkChild_SetupFollow

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters action 3 with the first animation, stopping the foal.
Defined in soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../ovl_En_Horse_Link_Child/z_en_horse_link_child.c   | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
index 64c5da0b4..27e25db1a 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
@@ -16,7 +16,8 @@ void EnHorseLinkChild_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80A69B7C: enters action 1 with the first animation, stopping the foal.
 void EnHorseLinkChild_SetupApproach(EnHorseLinkChild* this);  // was func_80A69B7C [LLM:HIGH]
-void func_80A69EC0(EnHorseLinkChild* this);
+// LLM generated name (HIGH), was func_80A69EC0: enters action 3 with the first animation, stopping the foal.
+void EnHorseLinkChild_SetupFollow(EnHorseLinkChild* this);  // was func_80A69EC0 [LLM:HIGH]
 void func_80A6A4DC(EnHorseLinkChild* this);
 void func_80A6A724(EnHorseLinkChild* this);
 
@@ -169,16 +170,16 @@ void EnHorseLinkChild_Init(Actor* thisx, PlayState* play) {
     this->eyeTexIndex = 0;
 
     if (gSaveContext.sceneLayer > 3) {
-        func_80A69EC0(this);
+        EnHorseLinkChild_SetupFollow(this);  // was func_80A69EC0 [LLM:HIGH]
     } else if (play->sceneNum == SCENE_LON_LON_RANCH) {
         if (!Flags_GetEventChkInf(EVENTCHKINF_TALON_RETURNED_FROM_CASTLE)) {
             Actor_Kill(&this->actor);
             return;
         }
         this->unk_2A0 = Flags_GetEventChkInf(EVENTCHKINF_INVITED_TO_SING_WITH_CHILD_MALON);
-        func_80A69EC0(this);
+        EnHorseLinkChild_SetupFollow(this);  // was func_80A69EC0 [LLM:HIGH]
     } else {
-        func_80A69EC0(this);
+        EnHorseLinkChild_SetupFollow(this);  // was func_80A69EC0 [LLM:HIGH]
     }
 
     this->actor.home.rot.z = this->actor.world.rot.z = this->actor.shape.rot.z = 0;
@@ -312,7 +313,7 @@ void EnHorseLinkChild_Approach(EnHorseLinkChild* this, PlayState* play) {
     }
 }
 
-void func_80A69EC0(EnHorseLinkChild* this) {
+void EnHorseLinkChild_SetupFollow(EnHorseLinkChild* this) {
     this->action = 3;
     this->animationIdx = 0;
     this->actor.speedXZ = 0.0f;
-- 
2.55.0

