From 747fec684c143b7d7a1c186344cc7aded9a2542e Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:04:00 +0000
Subject: [PATCH 1922/3799] soh: rename func_809FE040 -> EnDu_AdvanceDanceAnim

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: steps an eight-entry animation ring at each animation end -- the body of Darunia's dance, cue 7
Defined in soh/src/overlays/actors/ovl_En_Du/z_en_du.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Du/z_en_du.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Du/z_en_du.c b/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
index 1d9daabd8..edb8dd68e 100644
--- a/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
+++ b/soh/src/overlays/actors/ovl_En_Du/z_en_du.c
@@ -272,7 +272,8 @@ void EnDu_GetCueEndPos(CsCmdActorCue* csAction, Vec3f* dst) {
     dst->z = csAction->endPos.z;
 }
 
-void func_809FE040(EnDu* this) {
+// LLM generated name (HIGH), was func_809FE040: steps an eight-entry animation ring at each animation end -- the body of Darunia's dance, cue 7
+void EnDu_AdvanceDanceAnim(EnDu* this) {
     s32 animationIndices[] = {
         ENDU_ANIM_8, ENDU_ANIM_8, ENDU_ANIM_8, ENDU_ANIM_8, ENDU_ANIM_9, ENDU_ANIM_10, ENDU_ANIM_10, ENDU_ANIM_13,
     };
@@ -525,7 +526,7 @@ void func_809FE890(EnDu* this, PlayState* play) {
             }
         }
         if (this->unk_1EA == 7) {
-            func_809FE040(this);
+            EnDu_AdvanceDanceAnim(this);  // was func_809FE040 [LLM:HIGH]
         }
         if (this->unk_1EA == 8) {
             func_809FE104(this);
-- 
2.55.0

