From 43f0c402bf688c257d8f25d69c9cb10b0f86d65d Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:18 +0000
Subject: [PATCH 2250/3799] soh: rename func_80B0DB00 -> EnSw_FallToGround

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the wall skulltula death: tumbles under gravity, bouncing with dust rings until it settles
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index 36affcd9b..cae19999c 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -22,7 +22,8 @@ void func_80B0DC7C(EnSw* this, PlayState* play);
 s32 EnSw_StickToSurface(EnSw* this, PlayState* play, s32);  // was func_80B0C0CC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0D3AC: puffs dust while the delay runs, then grows and falls along the stored basis until it finds a surface to cling to
 void EnSw_DropIn(EnSw* this, PlayState* play);  // was func_80B0D3AC [LLM:HIGH]
-void func_80B0DB00(EnSw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B0DB00: the wall skulltula death: tumbles under gravity, bouncing with dust rings until it settles
+void EnSw_FallToGround(EnSw* this, PlayState* play);  // was func_80B0DB00 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0D878: the gold skulltula death: throws EffectSsDeadDb sparks, then spawns EN_SI as its token and kills the actor
 void EnSw_DieAsToken(EnSw* this, PlayState* play);  // was func_80B0D878 [LLM:HIGH]
 
@@ -363,7 +364,7 @@ s32 EnSw_CheckDamage(EnSw* this, PlayState* play) {
                 this->actor.shape.shadowScale = 16.0f;
                 this->actor.gravity = -1.0f;
                 this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
-                this->actionFunc = func_80B0DB00;
+                this->actionFunc = EnSw_FallToGround;  // was func_80B0DB00 [LLM:HIGH]
             }
 
             GameInteractor_ExecuteOnEnemyDefeat(&this->actor);
@@ -655,7 +656,7 @@ void EnSw_DieAsToken(EnSw* this, PlayState* play) {
     }
 }
 
-void func_80B0DB00(EnSw* this, PlayState* play) {
+void EnSw_FallToGround(EnSw* this, PlayState* play) {
     Actor_MoveXZGravity(&this->actor);
     this->actor.shape.rot.x += 0x1000;
     this->actor.shape.rot.z += 0x1000;
-- 
2.55.0

