From d5db15807b46f73ed31dcccf5439c3a32b5968f9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:33:16 +0000
Subject: [PATCH 0970/3799] soh: rename func_80878300 -> BgGateShutter_Open

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: slides the gate along -x toward -89 with the metal-gate sfx, then arms the 30-frame settle timer.
Defined in soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c       | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c b/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
index 503233d09..727d3e297 100644
--- a/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
+++ b/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
@@ -18,7 +18,8 @@ void BgGateShutter_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_8087828C: idle action: watches openingState and dispatches to the open or close slide, arming the shared 2-frame delay first.
 void BgGateShutter_WaitToMove(BgGateShutter* this, PlayState* play);  // was func_8087828C [LLM:HIGH]
-void func_80878300(BgGateShutter* this, PlayState* play);
+// LLM generated name (HIGH), was func_80878300: slides the gate along -x toward -89 with the metal-gate sfx, then arms the 30-frame settle timer.
+void BgGateShutter_Open(BgGateShutter* this, PlayState* play);  // was func_80878300 [LLM:HIGH]
 void func_808783AC(BgGateShutter* this, PlayState* play);
 void func_808783D4(BgGateShutter* this, PlayState* play);
 
@@ -69,17 +70,17 @@ void BgGateShutter_Destroy(Actor* thisx, PlayState* play) {
 void BgGateShutter_WaitToMove(BgGateShutter* this, PlayState* play) {
     if (this->openingState == 1 && !Flags_GetInfTable(INFTABLE_SHOWED_ZELDAS_LETTER_TO_GATE_GUARD)) {
         this->unk_178 = 2;
-        this->actionFunc = func_80878300;
+        this->actionFunc = BgGateShutter_Open;  // was func_80878300 [LLM:HIGH]
     } else if (this->openingState == 2) {
         this->unk_178 = 2;
-        this->actionFunc = func_80878300;
+        this->actionFunc = BgGateShutter_Open;  // was func_80878300 [LLM:HIGH]
     } else if (this->openingState < 0) {
         this->unk_178 = 2;
         this->actionFunc = func_808783D4;
     }
 }
 
-void func_80878300(BgGateShutter* this, PlayState* play) {
+void BgGateShutter_Open(BgGateShutter* this, PlayState* play) {
     Actor* thisx = &this->dyna.actor;
 
     if (this->unk_178 == 0) {
-- 
2.55.0

