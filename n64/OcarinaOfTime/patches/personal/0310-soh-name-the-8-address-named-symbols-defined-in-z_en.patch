From 72459484221f45053a4450361afd710cc28db928 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:25 +0000
Subject: [PATCH 310/488] soh: name the 8 address-named symbols defined in
 z_en_kakasi.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 8 are defined in soh/src/overlays/actors/ovl_En_Kakasi/z_en_kakasi.c.

  * func_80A8F28C -> EnKakasi_ReturnToRest
    deduced, confidence HIGH: stops the dance animation and eases the
    scarecrow's three rotation axes back to the pose it was spawned with;
    every non-dancing action calls it each frame.

  * func_80A8F320 -> EnKakasi_Dance
    deduced, confidence HIGH: turns each ocarina note into a dance move --
    spin on A, faster or slower playback on the C-up and C-down notes, lean
    on C-left and C-right -- ramping up to a jump once eight notes have been
    played; its argument makes it pick random notes instead, for the
    playback.

  * func_80A8F660 -> EnKakasi_SetupIdle
    deduced, confidence HIGH: restarts the looping animation and picks the
    greeting text for Link's age and whether the scarecrow's song has been
    recorded, then installs the idle action.

  * func_80A8F75C -> EnKakasi_Idle
    deduced, confidence HIGH: holds the rest pose and offers talk while the
    player is close and in front, routing an accepted conversation either to
    the song scene or back to the idle setup.

  * func_80A8F8D0 -> EnKakasi_WaitForSongEnd
    deduced, confidence HIGH: dances to each note while the player has the
    ocarina out, and when it is put away either starts the "I remember it"
    text or returns to the idle setup.

  * func_80A8F9C8 -> EnKakasi_Talk
    deduced, confidence HIGH: waits for the textbox to be advanced, then
    starts the one-point cutscene and the scarecrow's-song playback prompt.

  * func_80A8FAA4 -> EnKakasi_WaitForPlayback
    deduced, confidence HIGH: dances through the scarecrow's-song playback
    and, once it finishes, opens the closing text and reframes the camera.

  * func_80A8FBB8 -> EnKakasi_FinishTalk
    deduced, confidence HIGH: closes the last textbox, ends the camera shot,
    hands control back to the player and returns to the idle setup.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../actors/ovl_En_Kakasi/z_en_kakasi.c        | 66 +++++++++++--------
 1 file changed, 37 insertions(+), 29 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Kakasi/z_en_kakasi.c b/soh/src/overlays/actors/ovl_En_Kakasi/z_en_kakasi.c
index 0f5ec3d80..3ce328f06 100644
--- a/soh/src/overlays/actors/ovl_En_Kakasi/z_en_kakasi.c
+++ b/soh/src/overlays/actors/ovl_En_Kakasi/z_en_kakasi.c
@@ -15,12 +15,18 @@ void EnKakasi_Destroy(Actor* thisx, PlayState* play);
 void EnKakasi_Update(Actor* thisx, PlayState* play);
 void EnKakasi_Draw(Actor* thisx, PlayState* play);
 
-void func_80A8F660(EnKakasi* this, PlayState* play);
-void func_80A8F75C(EnKakasi* this, PlayState* play);
-void func_80A8F8D0(EnKakasi* this, PlayState* play);
-void func_80A8F9C8(EnKakasi* this, PlayState* play);
-void func_80A8FBB8(EnKakasi* this, PlayState* play);
-void func_80A8FAA4(EnKakasi* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A8F660: restarts the looping animation and picks the greeting text for Link's age and whether the scarecrow's song has been recorded, then installs the idle action.
+void EnKakasi_SetupIdle(EnKakasi* this, PlayState* play);  // was func_80A8F660 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80A8F75C: holds the rest pose and offers talk while the player is close and in front, routing an accepted conversation either to the song scene or back to the idle setup.
+void EnKakasi_Idle(EnKakasi* this, PlayState* play);  // was func_80A8F75C [LLM:HIGH]
+// LLM generated name (HIGH), was func_80A8F8D0: dances to each note while the player has the ocarina out, and when it is put away either starts the "I remember it" text or returns to the idle setup.
+void EnKakasi_WaitForSongEnd(EnKakasi* this, PlayState* play);  // was func_80A8F8D0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80A8F9C8: waits for the textbox to be advanced, then starts the one-point cutscene and the scarecrow's-song playback prompt.
+void EnKakasi_Talk(EnKakasi* this, PlayState* play);  // was func_80A8F9C8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80A8FBB8: closes the last textbox, ends the camera shot, hands control back to the player and returns to the idle setup.
+void EnKakasi_FinishTalk(EnKakasi* this, PlayState* play);  // was func_80A8FBB8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80A8FAA4: dances through the scarecrow's-song playback and, once it finishes, opens the closing text and reframes the camera.
+void EnKakasi_WaitForPlayback(EnKakasi* this, PlayState* play);  // was func_80A8FAA4 [LLM:HIGH]
 
 static ColliderCylinderInit sCylinderInit = {
     {
@@ -78,10 +84,11 @@ void EnKakasi_Init(Actor* thisx, PlayState* play) {
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
 
     Actor_SetScale(&this->actor, 0.01f);
-    this->actionFunc = func_80A8F660;
+    this->actionFunc = EnKakasi_SetupIdle;  // was func_80A8F660 [LLM:HIGH]
 }
 
-void func_80A8F28C(EnKakasi* this) {
+// LLM generated name (HIGH), was func_80A8F28C: stops the dance animation and eases the scarecrow's three rotation axes back to the pose it was spawned with; every non-dancing action calls it each frame.
+void EnKakasi_ReturnToRest(EnKakasi* this) {
     this->unk_1A4 = 0;
     this->skelanime.playSpeed = 0.0f;
     this->unk_1A8 = this->unk_1AC = 0;
@@ -92,7 +99,8 @@ void func_80A8F28C(EnKakasi* this) {
     Math_SmoothStepToS(&this->actor.shape.rot.z, this->rot.z, 5, 0x2710, 0);
 }
 
-void func_80A8F320(EnKakasi* this, PlayState* play, s16 arg) {
+// LLM generated name (HIGH), was func_80A8F320: turns each ocarina note into a dance move -- spin on A, faster or slower playback on the C-up and C-down notes, lean on C-left and C-right -- ramping up to a jump once eight notes have been played; its argument makes it pick random notes instead, for the playback.
+void EnKakasi_Dance(EnKakasi* this, PlayState* play, s16 arg) {
     s16 ocarinaNote = play->msgCtx.lastOcaNoteIdx;
     s16 currentFrame;
 
@@ -170,7 +178,7 @@ void func_80A8F320(EnKakasi* this, PlayState* play, s16 arg) {
     }
 }
 
-void func_80A8F660(EnKakasi* this, PlayState* play) {
+void EnKakasi_SetupIdle(EnKakasi* this, PlayState* play) {
     f32 frameCount = Animation_GetLastFrame(&object_ka_Anim_000214);
 
     Animation_Change(&this->skelanime, &object_ka_Anim_000214, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
@@ -190,20 +198,20 @@ void func_80A8F660(EnKakasi* this, PlayState* play) {
             this->unk_196 = TEXT_STATE_EVENT;
         }
     }
-    this->actionFunc = func_80A8F75C;
+    this->actionFunc = EnKakasi_Idle;  // was func_80A8F75C [LLM:HIGH]
 }
 
-void func_80A8F75C(EnKakasi* this, PlayState* play) {
+void EnKakasi_Idle(EnKakasi* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
-    func_80A8F28C(this);
+    EnKakasi_ReturnToRest(this);  // was func_80A8F28C [LLM:HIGH]
     SkelAnime_Update(&this->skelanime);
     this->camId = SUBCAM_NONE;
     if (Actor_ProcessTalkRequest(&this->actor, play)) {
         if (this->unk_196 == TEXT_STATE_EVENT) {
-            this->actionFunc = func_80A8F9C8;
+            this->actionFunc = EnKakasi_Talk;  // was func_80A8F9C8 [LLM:HIGH]
         } else {
-            this->actionFunc = func_80A8F660;
+            this->actionFunc = EnKakasi_SetupIdle;  // was func_80A8F660 [LLM:HIGH]
         }
     } else {
         s16 yawTowardsPlayer = this->actor.yawTowardsPlayer - this->actor.shape.rot.y;
@@ -220,7 +228,7 @@ void func_80A8F75C(EnKakasi* this, PlayState* play) {
                         this->unk_19A = 0;
                         this->unk_1B8 = 0.0;
                         player->stateFlags2 |= PLAYER_STATE2_NEAR_OCARINA_ACTOR;
-                        this->actionFunc = func_80A8F8D0;
+                        this->actionFunc = EnKakasi_WaitForSongEnd;  // was func_80A8F8D0 [LLM:HIGH]
                         return;
                     }
                     if (this->actor.xzDistToPlayer < 80.0f) {
@@ -233,7 +241,7 @@ void func_80A8F75C(EnKakasi* this, PlayState* play) {
     }
 }
 
-void func_80A8F8D0(EnKakasi* this, PlayState* play) {
+void EnKakasi_WaitForSongEnd(EnKakasi* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     if (play->msgCtx.ocarinaMode == OCARINA_MODE_04 && play->msgCtx.msgMode == MSGMODE_NONE) {
@@ -245,20 +253,20 @@ void func_80A8F8D0(EnKakasi* this, PlayState* play) {
             this->actor.textId = 0x4077;
             this->unk_196 = TEXT_STATE_EVENT;
             Message_StartTextbox(play, this->actor.textId, NULL);
-            this->actionFunc = func_80A8F9C8;
+            this->actionFunc = EnKakasi_Talk;  // was func_80A8F9C8 [LLM:HIGH]
         } else {
             OnePointCutscene_EndCutscene(play, this->camId);
             this->camId = SUBCAM_NONE;
-            this->actionFunc = func_80A8F660;
+            this->actionFunc = EnKakasi_SetupIdle;  // was func_80A8F660 [LLM:HIGH]
         }
     } else if (play->msgCtx.ocarinaMode == OCARINA_MODE_01) {
-        func_80A8F320(this, play, 0);
+        EnKakasi_Dance(this, play, 0);  // was func_80A8F320 [LLM:HIGH]
         player->stateFlags2 |= PLAYER_STATE2_NEAR_OCARINA_ACTOR;
     }
 }
 
-void func_80A8F9C8(EnKakasi* this, PlayState* play) {
-    func_80A8F28C(this);
+void EnKakasi_Talk(EnKakasi* this, PlayState* play) {
+    EnKakasi_ReturnToRest(this);  // was func_80A8F28C [LLM:HIGH]
     SkelAnime_Update(&this->skelanime);
     Player_SetCsActionWithHaltedActors(play, NULL, 8);
 
@@ -271,13 +279,13 @@ void func_80A8F9C8(EnKakasi* this, PlayState* play) {
         play->msgCtx.msgMode = MSGMODE_PAUSED;
         Player_SetCsActionWithHaltedActors(play, NULL, 8);
         Message_StartOcarinaSunsSongEnabled(play, OCARINA_ACTION_SCARECROW_LONG_PLAYBACK);
-        this->actionFunc = func_80A8FAA4;
+        this->actionFunc = EnKakasi_WaitForPlayback;  // was func_80A8FAA4 [LLM:HIGH]
     }
 }
 
-void func_80A8FAA4(EnKakasi* this, PlayState* play) {
+void EnKakasi_WaitForPlayback(EnKakasi* this, PlayState* play) {
     if (play->msgCtx.ocarinaMode != OCARINA_MODE_0F) {
-        func_80A8F320(this, play, 1);
+        EnKakasi_Dance(this, play, 1);  // was func_80A8F320 [LLM:HIGH]
         return;
     }
 
@@ -293,7 +301,7 @@ void func_80A8FAA4(EnKakasi* this, PlayState* play) {
             this->unk_196 = TEXT_STATE_EVENT;
             Message_StartTextbox(play, this->actor.textId, NULL);
         }
-        this->actionFunc = func_80A8FBB8;
+        this->actionFunc = EnKakasi_FinishTalk;  // was func_80A8FBB8 [LLM:HIGH]
         OnePointCutscene_EndCutscene(play, this->camId);
         this->camId = SUBCAM_NONE;
         this->camId = OnePointCutscene_Init(play, 2260, -99, &this->actor, CAM_ID_MAIN);
@@ -301,15 +309,15 @@ void func_80A8FAA4(EnKakasi* this, PlayState* play) {
     }
 }
 
-void func_80A8FBB8(EnKakasi* this, PlayState* play) {
-    func_80A8F28C(this);
+void EnKakasi_FinishTalk(EnKakasi* this, PlayState* play) {
+    EnKakasi_ReturnToRest(this);  // was func_80A8F28C [LLM:HIGH]
     SkelAnime_Update(&this->skelanime);
 
     if (this->unk_196 == Message_GetState(&play->msgCtx) && Message_ShouldAdvance(play)) {
         Camera_SetFinishedFlag(play->cameraPtrs[this->camId]);
         Message_CloseTextbox(play);
         Player_SetCsActionWithHaltedActors(play, NULL, 7);
-        this->actionFunc = func_80A8F660;
+        this->actionFunc = EnKakasi_SetupIdle;  // was func_80A8F660 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

