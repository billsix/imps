From 3981d54eed3d2d08882c0000e7905137000608ee Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:05 +0000
Subject: [PATCH 085/488] soh: name the 16 address-named symbols defined in
 z_demo_sa.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 16 are defined in soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c.

  * func_8098E480 -> DemoSa_Blink
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.91

  * func_8098E6EC -> DemoSa_SetStartPosRotFromCue
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.84

  * func_8098E7FC -> DemoSa_InitChamberOfSages
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.97

  * func_8098E86C -> DemoSa_CsForestMedallion_SpawnDoorWarp
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 1.00

  * func_8098E8C8 -> DemoSa_CsForestMedallion_SpawnMedallion
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.90

  * func_8098E944 -> DemoSa_CsForestMedallion_Rise
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 1.00

  * func_8098EA3C -> DemoSa_CsForestMedallion_EndRise
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.81

  * func_8098EA68 -> DemoSa_CsForestMedallion_CheckStartRaisingArms
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.85

  * func_8098EB00 -> DemoSa_CsForestMedallion_HoldArmsUp
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.88

  * func_8098ECF4 -> DemoSa_InitTrialOrSealingGanon
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.95

  * func_8098EDB0 -> DemoSa_SageMagic_LowerEyes
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.97

  * func_8098EE08 -> DemoSa_Sfx_WhiteOut
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 1.00

  * func_8098EE28 -> DemoSa_SageMagic_SpawnLightBall
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 1.00

  * func_8098F390 -> DemoSa_InitUnused
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.87

  * func_8098F420 -> DemoSa_InitCredits
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.85

  * func_8098F83C -> DemoSa_InitBridge
    zeldaret/oot (same decomp, same ROM address); body verified against
    upstream at 0.92

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../overlays/actors/ovl_Demo_Sa/z_demo_sa.c   | 92 +++++++++++--------
 1 file changed, 54 insertions(+), 38 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c b/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
index a898a8950..7603bbae9 100644
--- a/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
+++ b/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
@@ -105,7 +105,8 @@ void DemoSa_Destroy(Actor* thisx, PlayState* play) {
     SkelAnime_Free(&this->skelAnime, play);
 }
 
-void func_8098E480(DemoSa* this) {
+// Name from zeldaret/oot (was func_8098E480): adopted upstream's name for this address; body verified to match upstream at 0.91. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_Blink(DemoSa* this) {
     s16* eyeIndex = &this->eyeIndex;
     s16* blinkTimer = &this->blinkTimer;
 
@@ -184,7 +185,8 @@ s32 func_8098E6A0(DemoSa* this, PlayState* play, u16 arg2, s32 arg3) {
     return 0;
 }
 
-void func_8098E6EC(DemoSa* this, PlayState* play, s32 actionIdx) {
+// Name from zeldaret/oot (was func_8098E6EC): adopted upstream's name for this address; body verified to match upstream at 0.84. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_SetStartPosRotFromCue(DemoSa* this, PlayState* play, s32 actionIdx) {
     CsCmdActorCue* npcAction = DemoSa_GetNpcAction(play, actionIdx);
 
     if (npcAction != NULL) {
@@ -214,14 +216,16 @@ void func_8098E76C(DemoSa* this, AnimationHeader* animHeaderSeg, u8 arg2, f32 tr
     Animation_Change(&this->skelAnime, animHeaderSeg, playbackSpeed, unk0, fc, arg2, transitionRate);
 }
 
-void func_8098E7FC(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098E7FC): adopted upstream's name for this address; body verified to match upstream at 0.97. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_InitChamberOfSages(DemoSa* this, PlayState* play) {
     SkelAnime_InitFlex(play, &this->skelAnime, &gSariaSkel, &gSariaWaitArmsToSideAnim, NULL, NULL, 0);
     this->actor.shape.yOffset = -10000.0f;
     DemoSa_SetEyeIndex(this, SARIA_EYE_HALF);
     DemoSa_SetMouthIndex(this, SARIA_MOUTH_CLOSED2);
 }
 
-void func_8098E86C(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098E86C): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_SpawnDoorWarp(DemoSa* this, PlayState* play) {
     Vec3f* world = &this->actor.world.pos;
     f32 posX = world->x;
     f32 posY = world->y;
@@ -230,7 +234,8 @@ void func_8098E86C(DemoSa* this, PlayState* play) {
     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_DOOR_WARP1, posX, posY, posZ, 0, 0, 0, WARP_SAGES);
 }
 
-void func_8098E8C8(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098E8C8): adopted upstream's name for this address; body verified to match upstream at 0.90. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_SpawnMedallion(DemoSa* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     f32 posX = player->actor.world.pos.x;
     f32 posY = player->actor.world.pos.y + 80.0f;
@@ -242,7 +247,8 @@ void func_8098E8C8(DemoSa* this, PlayState* play) {
     }
 }
 
-void func_8098E944(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098E944): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_Rise(DemoSa* this, PlayState* play) {
     this->actor.shape.yOffset += (250.0f / 3.0f);
 }
 
@@ -269,19 +275,21 @@ void func_8098E9EC(DemoSa* this, PlayState* play) {
         if ((npcAction != NULL) && (npcAction->action == 2)) {
             this->action = 2;
             this->drawConfig = 1;
-            func_8098E86C(this, play);
+            DemoSa_CsForestMedallion_SpawnDoorWarp(this, play);
         }
     }
 }
 
-void func_8098EA3C(DemoSa* this) {
+// Name from zeldaret/oot (was func_8098EA3C): adopted upstream's name for this address; body verified to match upstream at 0.81. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_EndRise(DemoSa* this) {
     if (this->actor.shape.yOffset >= 0.0f) {
         this->action = 3;
         this->actor.shape.yOffset = 0.0f;
     }
 }
 
-void func_8098EA68(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098EA68): adopted upstream's name for this address; body verified to match upstream at 0.85. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_CheckStartRaisingArms(DemoSa* this, PlayState* play) {
     CsCmdActorCue* npcAction;
 
     if (play->csCtx.state != CS_STATE_IDLE) {
@@ -294,7 +302,8 @@ void func_8098EA68(DemoSa* this, PlayState* play) {
     }
 }
 
-void func_8098EB00(DemoSa* this, s32 arg1) {
+// Name from zeldaret/oot (was func_8098EB00): adopted upstream's name for this address; body verified to match upstream at 0.88. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_CsForestMedallion_HoldArmsUp(DemoSa* this, s32 arg1) {
     if (arg1 != 0) {
         Animation_Change(&this->skelAnime, &gSariaGiveForestMedallionStandAnim, 1.0f, 0.0f,
                          Animation_GetLastFrame(&gSariaGiveForestMedallionStandAnim), ANIMMODE_LOOP, 0.0f);
@@ -309,7 +318,7 @@ void func_8098EB6C(DemoSa* this, PlayState* play) {
         npcAction = play->csCtx.npcActions[6];
         if ((npcAction != NULL) && (npcAction->action == 2)) {
             this->action = 6;
-            func_8098E8C8(this, play);
+            DemoSa_CsForestMedallion_SpawnMedallion(this, play);
         }
     }
 }
@@ -323,20 +332,20 @@ void func_8098EBD8(DemoSa* this, PlayState* play) {
 }
 
 void func_8098EBF8(DemoSa* this, PlayState* play) {
-    func_8098E944(this, play);
+    DemoSa_CsForestMedallion_Rise(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098EA3C(this);
+    DemoSa_CsForestMedallion_EndRise(this);
 }
 
 void func_8098EC28(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098EA68(this, play);
+    DemoSa_CsForestMedallion_CheckStartRaisingArms(this, play);
 }
 
 void func_8098EC60(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
-    func_8098EB00(this, DemoSa_UpdateSkelAnime(this));
+    DemoSa_CsForestMedallion_HoldArmsUp(this, DemoSa_UpdateSkelAnime(this));
 }
 
 void func_8098EC94(DemoSa* this, PlayState* play) {
@@ -350,7 +359,8 @@ void func_8098ECCC(DemoSa* this, PlayState* play) {
     DemoSa_UpdateSkelAnime(this);
 }
 
-void func_8098ECF4(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098ECF4): adopted upstream's name for this address; body verified to match upstream at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_InitTrialOrSealingGanon(DemoSa* this, PlayState* play) {
     SkelAnime* skelAnime = &this->skelAnime;
     f32 frameCount = Animation_GetLastFrame(&gSariaSealGanonAnim);
 
@@ -362,7 +372,8 @@ void func_8098ECF4(DemoSa* this, PlayState* play) {
     DemoSa_SetMouthIndex(this, SARIA_MOUTH_CLOSED);
 }
 
-void func_8098EDB0(DemoSa* this) {
+// Name from zeldaret/oot (was func_8098EDB0): adopted upstream's name for this address; body verified to match upstream at 0.97. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_SageMagic_LowerEyes(DemoSa* this) {
     f32 curFrame = this->skelAnime.curFrame;
 
     if ((this->skelAnime.mode == 2) && (curFrame >= 32.0f)) {
@@ -371,11 +382,13 @@ void func_8098EDB0(DemoSa* this) {
     }
 }
 
-void func_8098EE08(void) {
+// Name from zeldaret/oot (was func_8098EE08): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_Sfx_WhiteOut(void) {
     Sfx_PlaySfxCentered2(NA_SE_SY_WHITE_OUT_T);
 }
 
-void func_8098EE28(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098EE28): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_SageMagic_SpawnLightBall(DemoSa* this, PlayState* play) {
     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_DEMO_6K, this->actor.world.pos.x,
                        (kREG(23) + 25.0f) + this->actor.world.pos.y, this->actor.world.pos.z, 0, 0, 0, 4);
 }
@@ -387,7 +400,7 @@ void func_8098EEA8(DemoSa* this, PlayState* play) {
         this->alpha = 0;
         this->actor.shape.shadowAlpha = 0;
         this->unk_1A0 = 0.0f;
-        func_8098EE08();
+        DemoSa_Sfx_WhiteOut();
     }
 }
 
@@ -426,7 +439,7 @@ void func_8098F050(DemoSa* this, PlayState* play) {
         this->unk_1A0 = kREG(5) + 10.0f;
         this->alpha = 255;
         if (this->unk_1A8 == 0) {
-            func_8098EE28(this, play);
+            DemoSa_SageMagic_SpawnLightBall(this, play);
             this->unk_1A8 = 1;
         }
         this->actor.shape.shadowAlpha = 0xFF;
@@ -441,7 +454,7 @@ void func_8098F0E8(DemoSa* this, PlayState* play) {
 void func_8098F118(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
     func_8098EEFC(this, play);
     func_8098E554(this, play);
 }
@@ -449,7 +462,7 @@ void func_8098F118(DemoSa* this, PlayState* play) {
 void func_8098F16C(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098EDB0(this);
+    DemoSa_SageMagic_LowerEyes(this);
     func_8098F050(this, play);
     func_8098E554(this, play);
 }
@@ -477,7 +490,8 @@ void DemoSa_DrawXlu(DemoSa* this, PlayState* play) {
     CLOSE_DISPS(play->state.gfxCtx);
 }
 
-void func_8098F390(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098F390): adopted upstream's name for this address; body verified to match upstream at 0.87. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_InitUnused(DemoSa* this, PlayState* play) {
     SkelAnime_InitFlex(play, &this->skelAnime, &gSariaSkel, &gSariaWaitArmsToSideAnim, NULL, NULL, 0);
     this->action = 10;
     this->drawConfig = 1;
@@ -486,10 +500,11 @@ void func_8098F390(DemoSa* this, PlayState* play) {
 void func_8098F3F0(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
 }
 
-void func_8098F420(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098F420): adopted upstream's name for this address; body verified to match upstream at 0.85. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_InitCredits(DemoSa* this, PlayState* play) {
     SkelAnime_InitFlex(play, &this->skelAnime, &gSariaSkel, &gSariaSitting3Anim, NULL, NULL, 0);
     this->action = 11;
     this->drawConfig = 0;
@@ -512,7 +527,7 @@ void func_8098F480(DemoSa* this) {
 }
 
 void func_8098F50C(DemoSa* this, PlayState* play) {
-    func_8098E6EC(this, play, 4);
+    DemoSa_SetStartPosRotFromCue(this, play, 4);
     this->action = 12;
     this->drawConfig = 2;
 }
@@ -575,7 +590,7 @@ void func_8098F714(DemoSa* this, PlayState* play) {
 void func_8098F734(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
     func_8098F480(this);
     func_8098F544(this);
 }
@@ -583,14 +598,14 @@ void func_8098F734(DemoSa* this, PlayState* play) {
 void func_8098F77C(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
     func_8098F654(this, play);
 }
 
 void func_8098F7BC(DemoSa* this, PlayState* play) {
     func_8098E5C8(this, play);
     DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
     func_8098F654(this, play);
 }
 
@@ -599,11 +614,12 @@ void func_8098F7FC(DemoSa* this, PlayState* play) {
 
     func_8098E5C8(this, play);
     sp1C = DemoSa_UpdateSkelAnime(this);
-    func_8098E480(this);
+    DemoSa_Blink(this);
     func_8098F610(this, sp1C);
 }
 
-void func_8098F83C(DemoSa* this, PlayState* play) {
+// Name from zeldaret/oot (was func_8098F83C): adopted upstream's name for this address; body verified to match upstream at 0.92. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c]
+void DemoSa_InitBridge(DemoSa* this, PlayState* play) {
     Vec3f* thisPos = &this->actor.world.pos;
 
     SkelAnime_InitFlex(play, &this->skelAnime, &gSariaSkel, &gSariaWaitOnBridgeAnim, NULL, NULL, 0);
@@ -639,7 +655,7 @@ void func_8098F984(DemoSa* this) {
 
 void func_8098F998(DemoSa* this, PlayState* play) {
     if (this->cueId == 4) {
-        func_8098E6EC(this, play, 1);
+        DemoSa_SetStartPosRotFromCue(this, play, 1);
         this->action = 17;
         this->drawConfig = 2;
         this->unk_1B0 = 0;
@@ -762,19 +778,19 @@ void DemoSa_Init(Actor* thisx, PlayState* play) {
 
     switch (this->actor.params) {
         case 2:
-            func_8098ECF4(this, play);
+            DemoSa_InitTrialOrSealingGanon(this, play);
             break;
         case 3:
-            func_8098F390(this, play);
+            DemoSa_InitUnused(this, play);
             break;
         case 4:
-            func_8098F420(this, play);
+            DemoSa_InitCredits(this, play);
             break;
         case 5:
-            func_8098F83C(this, play);
+            DemoSa_InitBridge(this, play);
             break;
         default:
-            func_8098E7FC(this, play);
+            DemoSa_InitChamberOfSages(this, play);
     }
 }
 
-- 
2.55.0

