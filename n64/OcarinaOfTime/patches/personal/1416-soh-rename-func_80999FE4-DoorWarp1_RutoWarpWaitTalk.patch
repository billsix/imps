From 8cded4987cc1f98842e4fec80635bc52bf4f3aa1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:17:39 +0000
Subject: [PATCH 1416/3799] soh: rename func_80999FE4 ->
 DoorWarp1_RutoWarpWaitTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: waits for that textbox to close, plays the warp sfx, restores the main camera and hands off to DoorWarp1_RutoWarpOut.
Defined in soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
index 4a8cb0303..90aab770e 100644
--- a/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
+++ b/soh/src/overlays/actors/ovl_Door_Warp1/z_door_warp1.c
@@ -32,7 +32,8 @@ void DoorWarp1_RutoWarpIdle(DoorWarp1* this, PlayState* play);
 void DoorWarp1_ChildWarpOut(DoorWarp1* this, PlayState* play);
 // LLM generated name (HIGH), was func_80999EE0: sets up the sub-camera framing Ruto and the warp and opens her farewell textbox.
 void DoorWarp1_RutoWarpStartTalk(DoorWarp1* this, PlayState* play);  // was func_80999EE0 [LLM:HIGH]
-void func_80999FE4(DoorWarp1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80999FE4: waits for that textbox to close, plays the warp sfx, restores the main camera and hands off to DoorWarp1_RutoWarpOut.
+void DoorWarp1_RutoWarpWaitTalk(DoorWarp1* this, PlayState* play);  // was func_80999FE4 [LLM:HIGH]
 void DoorWarp1_RutoWarpOut(DoorWarp1* this, PlayState* play);
 void DoorWarp1_AdultWarpIdle(DoorWarp1* this, PlayState* play);
 void func_8099A508(DoorWarp1* this, PlayState* play);
@@ -612,11 +613,11 @@ void DoorWarp1_RutoWarpStartTalk(DoorWarp1* this, PlayState* play) {
         this->rutoWarpState = WARP_BLUE_RUTO_STATE_TALKING;
         // TODO: Why was this disabled in rando?
         Message_StartTextbox(play, 0x4022, NULL);
-        DoorWarp1_SetupAction(this, func_80999FE4);
+        DoorWarp1_SetupAction(this, DoorWarp1_RutoWarpWaitTalk);  // was func_80999FE4 [LLM:HIGH]
     }
 }
 
-void func_80999FE4(DoorWarp1* this, PlayState* play) {
+void DoorWarp1_RutoWarpWaitTalk(DoorWarp1* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_NONE) {
         Audio_PlaySfxGeneral(NA_SE_EV_LINK_WARP, &this->actor.projectedPos, 4, &gSfxDefaultFreqAndVolScale,
                              &gSfxDefaultFreqAndVolScale, &gSfxDefaultReverb);
-- 
2.55.0

