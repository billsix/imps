From e41a25949c183c94ad394fdd7f3fd727ef548577 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:23:22 +0000
Subject: [PATCH 1494/3799] soh: rename func_80A56874 ->
 EnHeishi4_DyingGuardOfferTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: offers talk within 100 units and routes an accepted conversation either back to the idle or into the dying speech.
Defined in soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
index b908e57e7..6ae9c8bd5 100644
--- a/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
+++ b/soh/src/overlays/actors/ovl_En_Heishi4/z_en_heishi4.c
@@ -21,7 +21,8 @@ void EnHeishi4_PickKakarikoText(EnHeishi4* this, PlayState* play);  // was func_
 void func_80A56B40(EnHeishi4* this, PlayState* play);
 // LLM generated name (HIGH), was func_80A56614: the night-market guard's line picker, choosing from the face-reaction set, the scene and the time of day.
 void EnHeishi4_PickMarketText(EnHeishi4* this, PlayState* play);  // was func_80A56614 [LLM:HIGH]
-void func_80A56874(EnHeishi4* this, PlayState* play);
+// LLM generated name (HIGH), was func_80A56874: offers talk within 100 units and routes an accepted conversation either back to the idle or into the dying speech.
+void EnHeishi4_DyingGuardOfferTalk(EnHeishi4* this, PlayState* play);  // was func_80A56874 [LLM:HIGH]
 void func_80A56900(EnHeishi4* this, PlayState* play);
 void func_80A56994(EnHeishi4* this, PlayState* play);
 void func_80A56A50(EnHeishi4* this, PlayState* play);
@@ -238,13 +239,13 @@ void EnHeishi4_DyingGuardIdle(EnHeishi4* this, PlayState* play) {
             this->unk_282 = TEXT_STATE_DONE;
             osSyncPrintf(VT_FGCOL(BLUE) " ☆☆☆☆☆ 返事なし ☆☆☆☆☆ \n" VT_RST);
         }
-        this->actionFunc = func_80A56874;
+        this->actionFunc = EnHeishi4_DyingGuardOfferTalk;  // was func_80A56874 [LLM:HIGH]
     } else {
         Actor_Kill(&this->actor);
     }
 }
 
-void func_80A56874(EnHeishi4* this, PlayState* play) {
+void EnHeishi4_DyingGuardOfferTalk(EnHeishi4* this, PlayState* play) {
     if (this->unk_284 != 0) {
         SkelAnime_Update(&this->skelAnime);
     }
-- 
2.55.0

