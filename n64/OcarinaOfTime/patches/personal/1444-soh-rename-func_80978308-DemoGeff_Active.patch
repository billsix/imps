From 052b5e0195dc02ef9a6a95b094714d4a0c875c49 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:19:23 +0000
Subject: [PATCH 1444/3799] soh: rename func_80978308 -> DemoGeff_Active

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: sActionFuncs[1]: finds and follows the Demo_Gt and refreshes the rubble scale every frame.
Defined in soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
index 48989598d..ef5c21370 100644
--- a/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
+++ b/soh/src/overlays/actors/ovl_Demo_Geff/z_demo_geff.c
@@ -19,7 +19,8 @@ void DemoGeff_Draw(Actor* thisx, PlayState* play);
 void DemoGeff_SetupRubble(DemoGeff* this, PlayState* play);  // was func_80978030 [LLM:HIGH]
 
 void func_809783D4(DemoGeff* this, PlayState* play);
-void func_80978308(DemoGeff* this, PlayState* play);
+// LLM generated name (HIGH), was func_80978308: sActionFuncs[1]: finds and follows the Demo_Gt and refreshes the rubble scale every frame.
+void DemoGeff_Active(DemoGeff* this, PlayState* play);  // was func_80978308 [LLM:HIGH]
 
 void func_809784D4(DemoGeff* this, PlayState* play);
 void func_80978344(DemoGeff* this, PlayState* play);
@@ -35,7 +36,7 @@ static DemoGeffInitFunc sInitFuncs[] = {
 
 static DemoGeffActionFunc sActionFuncs[] = {
     func_809783D4,
-    func_80978308,
+    DemoGeff_Active,  // was func_80978308 [LLM:HIGH]
 };
 
 static DemoGeffDrawFunc sDrawFuncs[] = {
@@ -165,7 +166,7 @@ void DemoGeff_FollowDemoGt(DemoGeff* this, PlayState* play) {
     }
 }
 
-void func_80978308(DemoGeff* this, PlayState* play) {
+void DemoGeff_Active(DemoGeff* this, PlayState* play) {
     DemoGeff_FindDemoGt(this, play);  // was func_809781FC [LLM:HIGH]
     DemoGeff_FollowDemoGt(this, play);  // was func_809782A0 [LLM:HIGH]
     DemoGeff_SetupRubble(this, play);  // was func_80978030 [LLM:HIGH]
-- 
2.55.0

