From b6422147ee2a9540cbe5adbf408c4f158159d737 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:56:45 +0000
Subject: [PATCH 2917/3799] soh: rename func_80B3F59C -> EnXc_HandleCue0Actions

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: reacts to each new action on cue 0, remembering the last one it handled
Defined in soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c b/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
index 6e633adf0..cbb79c5b2 100644
--- a/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
+++ b/soh/src/overlays/actors/ovl_En_Xc/z_en_xc.c
@@ -1500,7 +1500,8 @@ static s32 D_80B41DAC = 1;
 
 SHIP_SAVESTATE_DEFINE(EnXc, EN_XC_SHIP_SAVESTATE_FIELDS)
 
-void func_80B3F59C(EnXc* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B3F59C: reacts to each new action on cue 0, remembering the last one it handled
+void EnXc_HandleCue0Actions(EnXc* this, PlayState* play) {
     CsCmdActorCue* npcAction = EnXc_GetCsCmd(play, 0);
 
     if (npcAction != NULL) {
@@ -1600,7 +1601,7 @@ void EnXc_ActionFunc49(EnXc* this, PlayState* play) {
 void EnXc_Kneel(EnXc* this, PlayState* play) {
     EnXc_AnimIsFinished(this);
     EnXc_BgCheck(this, play);
-    func_80B3F59C(this, play);
+    EnXc_HandleCue0Actions(this, play);  // was func_80B3F59C [LLM:HIGH]
     EnXc_SetPosRotFromCue(this, play, 4);  // was func_80B3C588 [LLM:HIGH]
     func_80B3F754(this, play);
 }
@@ -1608,13 +1609,13 @@ void EnXc_Kneel(EnXc* this, PlayState* play) {
 void EnXc_ActionFunc51(EnXc* this, PlayState* play) {
     EnXc_AnimIsFinished(this);
     EnXc_BgCheck(this, play);
-    func_80B3F59C(this, play);
+    EnXc_HandleCue0Actions(this, play);  // was func_80B3F59C [LLM:HIGH]
     EnXc_MoveAlongCue(this, play, 4);  // was func_80B3C620 [LLM:HIGH]
     func_80B3F7BC(this, play);
 }
 
 void EnXc_ActionFunc52(EnXc* this, PlayState* play) {
-    func_80B3F59C(this, play);
+    EnXc_HandleCue0Actions(this, play);  // was func_80B3F59C [LLM:HIGH]
 }
 
 void func_80B3FA08(EnXc* this, PlayState* play) {
-- 
2.55.0

