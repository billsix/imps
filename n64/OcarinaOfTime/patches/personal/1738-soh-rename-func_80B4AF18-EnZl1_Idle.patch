From bb47dbebb9baf05093444ca7759c39a784714795 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:07 +0000
Subject: [PATCH 1738/3799] soh: rename func_80B4AF18 -> EnZl1_Idle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: tracks the player with her head and offers talk once he is level with her or below, running the collision check each frame.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index d9c073e69..bf2a05246 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -16,7 +16,8 @@ void EnZl1_Draw(Actor* thisx, PlayState* play);
 
 // LLM generated name (HIGH), was func_80B4AE18: drives Zelda's blink on a randomised timer and picks the eye and mouth textures, holding a fixed sideways glance during the first frames of one animation.
 void EnZl1_UpdateFace(EnZl1* this);  // was func_80B4AE18 [LLM:HIGH]
-void func_80B4AF18(EnZl1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B4AF18: tracks the player with her head and offers talk once he is level with her or below, running the collision check each frame.
+void EnZl1_Idle(EnZl1* this, PlayState* play);  // was func_80B4AF18 [LLM:HIGH]
 void func_80B4B010(EnZl1* this, PlayState* play);
 void func_80B4B240(EnZl1* this, PlayState* play);
 void func_80B4B8B4(EnZl1* this, PlayState* play);
@@ -110,12 +111,12 @@ void EnZl1_Init(Actor* thisx, PlayState* play) {
         frameCount = Animation_GetLastFrame(&gChildZelda1Anim_00438);
         Animation_Change(&this->skelAnime, &gChildZelda1Anim_00438, 1.0f, 0.0f, frameCount, ANIMMODE_LOOP, 0.0f);
         this->actor.textId = 0x703D;
-        this->actionFunc = func_80B4AF18;
+        this->actionFunc = EnZl1_Idle;  // was func_80B4AF18 [LLM:HIGH]
     } else if (Flags_GetEventChkInf(EVENTCHKINF_OBTAINED_ZELDAS_LETTER)) {
         frameCount = Animation_GetLastFrame(&gChildZelda1Anim_00438);
         Animation_Change(&this->skelAnime, &gChildZelda1Anim_00438, 1.0f, 0.0f, frameCount, ANIMMODE_LOOP, 0.0f);
         this->actor.textId = 0x703C;
-        this->actionFunc = func_80B4AF18;
+        this->actionFunc = EnZl1_Idle;  // was func_80B4AF18 [LLM:HIGH]
     } else {
         this->actor.textId = 0xFFFF;
         this->actionFunc = func_80B4B010;
@@ -146,7 +147,7 @@ void EnZl1_UpdateFace(EnZl1* this) {
     }
 }
 
-void func_80B4AF18(EnZl1* this, PlayState* play) {
+void EnZl1_Idle(EnZl1* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s32 pad;
 
-- 
2.55.0

