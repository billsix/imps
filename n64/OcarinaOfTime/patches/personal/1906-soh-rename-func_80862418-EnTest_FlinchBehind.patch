From bffc125532ed2cbc98155103ec58389c25a86460 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1906/3799] soh: rename func_80862418 -> EnTest_FlinchBehind

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the behind-hit twin of EnTest_FlinchFront, minus its jump-up-at-a-wall escape
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index c7f2db9f1..ad0c9c7fe 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -57,7 +57,8 @@ void EnTest_StopAndBlock(EnTest* this, PlayState* play);
 void EnTest_IdleFromBlock(EnTest* this, PlayState* play);
 // LLM generated name (HIGH), was func_808621D4: decays the knockback, then blocks, jumps back or jumps up depending on the wall and the player's swing
 void EnTest_FlinchFront(EnTest* this, PlayState* play);  // was func_808621D4 [LLM:HIGH]
-void func_80862418(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_80862418: the behind-hit twin of EnTest_FlinchFront, minus its jump-up-at-a-wall escape
+void EnTest_FlinchBehind(EnTest* this, PlayState* play);  // was func_80862418 [LLM:HIGH]
 void EnTest_Stunned(EnTest* this, PlayState* play);
 void func_808628C8(EnTest* this, PlayState* play);
 void func_80862E6C(EnTest* this, PlayState* play);
@@ -1264,10 +1265,10 @@ void EnTest_SetupFlinchBehind(EnTest* this) {
     this->unk_7C8 = 9;
     this->actor.speedXZ = -2.0f;
     Actor_SetColorFilter(&this->actor, 0x4000, 0xFF, 0, 8);
-    EnTest_SetupAction(this, func_80862418);
+    EnTest_SetupAction(this, EnTest_FlinchBehind);  // was func_80862418 [LLM:HIGH]
 }
 
-void func_80862418(EnTest* this, PlayState* play) {
+void EnTest_FlinchBehind(EnTest* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     Math_SmoothStepToF(&this->actor.speedXZ, 0.0f, 1.0f, 0.1f, 0.0f);
-- 
2.55.0

