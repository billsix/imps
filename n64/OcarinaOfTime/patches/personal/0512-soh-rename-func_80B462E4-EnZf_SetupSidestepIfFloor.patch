From 217fa64eb276693fa4523aacb0e39872c511fbfb Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 15:02:53 +0000
Subject: [PATCH 512/784] soh: rename func_80B462E4 ->
 EnZf_SetupSidestepIfFloor

Name source: zeldaret/oot (same decomp, same ROM address).
Body verified against upstream at 0.84.
Defined in soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c.
---
 soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c b/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
index 9f5f1dc44..2d5af8eff 100644
--- a/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
+++ b/soh/src/overlays/actors/ovl_En_Zf/z_en_zf.c
@@ -30,7 +30,8 @@ void EnZf_JumpForward(EnZf* this, PlayState* play);
 // Name from zeldaret/oot (was func_80B4604C): adopted upstream's name for this address; body verified to match upstream at 0.81. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Zf/z_en_zf.c]
 void EnZf_SetupTurnTowardsPlayer(EnZf* this);
 void func_80B46098(EnZf* this, PlayState* play);
-void func_80B462E4(EnZf* this, PlayState* play);
+// Name from zeldaret/oot (was func_80B462E4): adopted upstream's name for this address; body verified to match upstream at 0.84. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Zf/z_en_zf.c]
+void EnZf_SetupSidestepIfFloor(EnZf* this, PlayState* play);
 void func_80B463E4(EnZf* this, PlayState* play);
 void EnZf_SetupSlash(EnZf* this);
 void EnZf_Slash(EnZf* this, PlayState* play);
@@ -866,7 +867,7 @@ void EnZf_ApproachPlayer(EnZf* this, PlayState* play) {
         if (sp48 == this->curPlatform) {
             if (!Actor_IsFacingPlayer(&this->actor, 0x11C7)) {
                 if (Rand_ZeroOne() > 0.5f) {
-                    func_80B462E4(this, play);
+                    EnZf_SetupSidestepIfFloor(this, play);
                 } else {
                     EnZf_SetupIdle(this);
                 }
@@ -880,7 +881,7 @@ void EnZf_ApproachPlayer(EnZf* this, PlayState* play) {
                 }
             } else {
                 if (this->unk_3F8) {
-                    func_80B462E4(this, play);
+                    EnZf_SetupSidestepIfFloor(this, play);
                 } else if (Rand_ZeroOne() < 0.1f) {
                     EnZf_SetupIdle(this);
                 }
@@ -1021,7 +1022,7 @@ void func_80B46098(EnZf* this, PlayState* play) {
 
             if (Actor_IsFacingPlayer(&this->actor, 30 * 0x10000 / 360)) {
                 if (Rand_ZeroOne() > 0.8f) {
-                    func_80B462E4(this, play);
+                    EnZf_SetupSidestepIfFloor(this, play);
                 } else {
                     EnZf_SetupApproachPlayer(this, play);
                 }
@@ -1034,7 +1035,7 @@ void func_80B46098(EnZf* this, PlayState* play) {
 }
 
 // Conditional setup function
-void func_80B462E4(EnZf* this, PlayState* play) {
+void EnZf_SetupSidestepIfFloor(EnZf* this, PlayState* play) {
     if ((this->actor.params < ENZF_TYPE_LIZALFOS_MINIBOSS_A) /* miniboss */ ||
         Actor_TestFloorInDirection(&this->actor, play, 40.0f, (s16)(this->actor.shape.rot.y + 0x3FFF)) ||
         Actor_TestFloorInDirection(&this->actor, play, -40.0f, (s16)(this->actor.shape.rot.y + 0x3FFF))) {
-- 
2.55.0

