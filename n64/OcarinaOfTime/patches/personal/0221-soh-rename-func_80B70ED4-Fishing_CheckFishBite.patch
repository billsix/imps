From 09398b11dccbf9a3db678c9dfacbff0a55da17f9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 13:35:36 +0000
Subject: [PATCH 221/225] soh: rename func_80B70ED4 -> Fishing_CheckFishBite

Name source: deduced from the code (confidence HIGH).
Defined in soh/src/overlays/actors/ovl_Fishing/z_fishing.c.

Provenance comments accompany the definition and every header declaration,
so the name can be re-derived or overturned later. See
tasks/reference/ocarina/decomp-renaming.md.
---
 soh/src/overlays/actors/ovl_Fishing/z_fishing.c | 13 +++++++------
 1 file changed, 7 insertions(+), 6 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Fishing/z_fishing.c b/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
index c28e18e42..8d4f452a0 100644
--- a/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
+++ b/soh/src/overlays/actors/ovl_Fishing/z_fishing.c
@@ -2780,7 +2780,8 @@ void Fishing_SplashBySize2(Fishing* this, PlayState* play) {
     }
 }
 
-void func_80B70ED4(Fishing* this, Input* input) {
+// LLM generated name (HIGH), was func_80B70ED4: computes lure/line distance to the fish to detect a bite
+void Fishing_CheckFishBite(Fishing* this, Input* input) {
     Vec3f lineVec;
     Vec3f sp28;
     f32 lineLengthSQ;
@@ -3113,7 +3114,7 @@ void Fishing_UpdateFish(Actor* thisx, PlayState* play2) {
                 this->unk_194 = 500.0f;
             }
 
-            func_80B70ED4(this, input);
+            Fishing_CheckFishBite(this, input);
 
             if (this->actor.xzDistToPlayer < (250.0f * playerSpeedMod)) {
                 this->fishStateNext = this->fishState = 0;
@@ -3134,7 +3135,7 @@ void Fishing_UpdateFish(Actor* thisx, PlayState* play2) {
                 this->unk_190 = 1.0f;
                 this->unk_194 = 2000.0f;
             }
-            func_80B70ED4(this, input);
+            Fishing_CheckFishBite(this, input);
 
             if (this->actor.xzDistToPlayer < (250.0f * playerSpeedMod)) {
                 this->fishStateNext = this->fishState = 0;
@@ -3180,7 +3181,7 @@ void Fishing_UpdateFish(Actor* thisx, PlayState* play2) {
             }
 
             if (sLureEquipped == FS_LURE_SINKING) {
-                func_80B70ED4(this, input);
+                Fishing_CheckFishBite(this, input);
             } else {
                 this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
             }
@@ -3217,7 +3218,7 @@ void Fishing_UpdateFish(Actor* thisx, PlayState* play2) {
                 }
 
                 if (sLureEquipped == FS_LURE_SINKING) {
-                    func_80B70ED4(this, input);
+                    Fishing_CheckFishBite(this, input);
                 } else {
                     this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
                 }
@@ -4064,7 +4065,7 @@ void Fishing_UpdateFish(Actor* thisx, PlayState* play2) {
                 this->fishState = 10;
                 this->fishStateNext = 10;
             } else {
-                func_80B70ED4(this, input);
+                Fishing_CheckFishBite(this, input);
                 if (this->actor.xzDistToPlayer < (100.0f * playerSpeedMod)) {
                     this->fishStateNext = this->fishState = 0;
                     this->unk_1A4 = 500;
-- 
2.55.0

