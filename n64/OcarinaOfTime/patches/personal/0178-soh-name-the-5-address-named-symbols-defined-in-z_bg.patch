From e7e9b2e5cebaebd781e285a5d1cddbdac0200a93 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:13 +0000
Subject: [PATCH 178/488] soh: name the 5 address-named symbols defined in
 z_bg_jya_ironobj.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 5 are defined in soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c.

  * func_808992D8 -> BgJyaIronobj_SetupWaitForHit
    deduced, confidence HIGH: one-line setup that installs the wait-for-hit
    action; called from Init.

  * func_808992E8 -> BgJyaIronobj_WaitForHit
    deduced, confidence HIGH: the only action: waits for an AC hit from an
    Iron Knuckle (ACTOR_EN_IK), then spawns the pillar or throne debris per
    params bit 0, plays NA_SE_EN_IRONNACK_BREAK_PILLAR and drops
    collectibles.

  * D_808994D8 -> sDebrisYawOffsets
    deduced, confidence HIGH: binary-angle offsets added to the yaw toward
    the Iron Knuckle, indexed by its attack state, to aim the debris burst.

  * D_808994E0 -> sPillarDebrisScales
    deduced, confidence HIGH: feeds the scale parameter of
    EffectSsKakera_Spawn in BgJyaIronobj_SpawnPillarParticles (10th
    argument); the in-tree comment already groups it under "// Pillar".

  * D_80899510 -> sThroneDebrisScales
    deduced, confidence HIGH: the throne counterpart of sPillarDebrisScales
    -- same values, feeding the scale parameter in
    BgJyaIronobj_SpawnThoneParticles.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c     | 35 +++++++++++--------
 1 file changed, 20 insertions(+), 15 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
index 9314006de..8a5bc5ae4 100644
--- a/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
+++ b/soh/src/overlays/actors/ovl_Bg_Jya_Ironobj/z_bg_jya_ironobj.c
@@ -16,8 +16,10 @@ void BgJyaIronobj_Init(Actor* thisx, PlayState* play);
 void BgJyaIronobj_Destroy(Actor* thisx, PlayState* play);
 void BgJyaIronobj_Update(Actor* thisx, PlayState* play);
 void BgJyaIronobj_Draw(Actor* thisx, PlayState* play);
-void func_808992D8(BgJyaIronobj* this);
-void func_808992E8(BgJyaIronobj* this, PlayState* play);
+// LLM generated name (HIGH), was func_808992D8: one-line setup that installs the wait-for-hit action; called from Init.
+void BgJyaIronobj_SetupWaitForHit(BgJyaIronobj* this);  // was func_808992D8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808992E8: the only action: waits for an AC hit from an Iron Knuckle (ACTOR_EN_IK), then spawns the pillar or throne debris per params bit 0, plays NA_SE_EN_IRONNACK_BREAK_PILLAR and drops collectibles.
+void BgJyaIronobj_WaitForHit(BgJyaIronobj* this, PlayState* play);  // was func_808992E8 [LLM:HIGH]
 
 void BgJyaIronobj_SpawnPillarParticles(BgJyaIronobj* this, PlayState* play, EnIk* enIk);
 void BgJyaIronobj_SpawnThoneParticles(BgJyaIronobj* this, PlayState* play, EnIk* enIk);
@@ -59,15 +61,18 @@ static ColliderCylinderInit sCylinderInit = {
     { 30, 150, 0, { 0, 0, 0 } },
 };
 
-static s16 D_808994D8[] = { 0x8700, 0x4000, 0xC000, 0x0000 };
+// LLM generated name (HIGH), was D_808994D8: binary-angle offsets added to the yaw toward the Iron Knuckle, indexed by its attack state, to aim the debris burst.
+static s16 sDebrisYawOffsets[] = { 0x8700, 0x4000, 0xC000, 0x0000 };  // was D_808994D8 [LLM:HIGH]
 
 // Pillar
-static s16 D_808994E0[] = { 5, 8, 11, 14, 17, 20, 23, 26 };
+// LLM generated name (HIGH), was D_808994E0: feeds the scale parameter of EffectSsKakera_Spawn in BgJyaIronobj_SpawnPillarParticles (10th argument); the in-tree comment already groups it under "// Pillar".
+static s16 sPillarDebrisScales[] = { 5, 8, 11, 14, 17, 20, 23, 26 };  // was D_808994E0 [LLM:HIGH]
 static s16 D_808994F0[] = { 18, 26, 34, 42, 50, 60, 70, 80 };
 static s16 D_80899500[] = { 48, 42, 36, 32, 28, 24, 20, 16 };
 
 // Throne
-static s16 D_80899510[] = { 5, 8, 11, 14, 17, 20, 23, 26 };
+// LLM generated name (HIGH), was D_80899510: the throne counterpart of sPillarDebrisScales -- same values, feeding the scale parameter in BgJyaIronobj_SpawnThoneParticles.
+static s16 sThroneDebrisScales[] = { 5, 8, 11, 14, 17, 20, 23, 26 };  // was D_80899510 [LLM:HIGH]
 static s16 D_80899520[] = { 18, 26, 34, 42, 50, 60, 70, 80 };
 static s16 D_80899530[] = { 48, 42, 36, 32, 28, 24, 20, 16 };
 
@@ -112,7 +117,7 @@ void BgJyaIronobj_SpawnPillarParticles(BgJyaIronobj* this, PlayState* play, EnIk
         return;
     }
     osSyncPrintf("¢ attack_type(%d)\n", enIk->unk_2FF);
-    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + D_808994D8[enIk->unk_2FF - 1];
+    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + sDebrisYawOffsets[enIk->unk_2FF - 1];  // was D_808994D8 [LLM:HIGH]
 
     for (i = 0; i < 8; i++) {
         Actor* actor =
@@ -147,10 +152,10 @@ void BgJyaIronobj_SpawnPillarParticles(BgJyaIronobj* this, PlayState* play, EnIk
         vel.x = 2.0f * ((Rand_ZeroOne() * 6.0f) - 3.0f) + (Rand_ZeroOne() * sins * 8.0f * temp_f22);
         vel.y = (Rand_ZeroOne() * 8.0f) - 3.0f;
         vel.z = 2.0f * ((Rand_ZeroOne() * 6.0f) - 3.0f) + (Rand_ZeroOne() * coss * 8.0f * temp_f22);
-        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899500[j & 7], 4, 0, D_808994E0[j & 7], 0, 5,
+        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899500[j & 7], 4, 0, sPillarDebrisScales[j & 7], 0, 5,  // was D_808994E0 [LLM:HIGH]
                              D_808994F0[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);
         if (Rand_ZeroOne() < 0.26f) {
-            func_80033480(play, &pos, 200.0f, 1, D_808994E0[j & 7] * 4 + 60, D_808994E0[j & 7] * 4 + 80, 1);
+            func_80033480(play, &pos, 200.0f, 1, sPillarDebrisScales[j & 7] * 4 + 60, sPillarDebrisScales[j & 7] * 4 + 80, 1);  // was D_808994E0 [LLM:HIGH]
         }
     }
 }
@@ -175,7 +180,7 @@ void BgJyaIronobj_SpawnThoneParticles(BgJyaIronobj* this, PlayState* play, EnIk*
         return;
     }
     osSyncPrintf("¢ attack_type(%d)\n", enIk->unk_2FF);
-    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + D_808994D8[enIk->unk_2FF - 1];
+    rotY = Actor_WorldYawTowardActor(&this->dyna.actor, &enIk->actor) + sDebrisYawOffsets[enIk->unk_2FF - 1];  // was D_808994D8 [LLM:HIGH]
     for (i = 0; i < 8; i++) {
         Actor* actor =
             Actor_Spawn(&play->actorCtx, play, ACTOR_BG_JYA_HAHENIRON, this->dyna.actor.world.pos.x,
@@ -206,10 +211,10 @@ void BgJyaIronobj_SpawnThoneParticles(BgJyaIronobj* this, PlayState* play, EnIk*
         vel.x = 2.0f * (Rand_ZeroOne() * 6.0f - 3.0f) + (Rand_ZeroOne() * sins * 8.0f * temp_f22);
         vel.y = Rand_ZeroOne() * 8.0f - 3.0f;
         vel.z = 2.0f * (Rand_ZeroOne() * 6.0f - 3.0f) + (Rand_ZeroOne() * coss * 8.0f * temp_f22);
-        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899530[j & 7], 4, 0, D_80899510[j & 7], 0, 5,
+        EffectSsKakera_Spawn(play, &pos, &vel, &pos, -350, unkArg5, D_80899530[j & 7], 4, 0, sThroneDebrisScales[j & 7], 0, 5,  // was D_80899510 [LLM:HIGH]
                              D_80899520[j & 7], -1, OBJECT_JYA_IRON, gObjectJyaIronDL_000880);
         if (Rand_ZeroOne() < 0.26f) {
-            func_80033480(play, &pos, 200.0f, 1, D_80899510[j & 7] * 4 + 60, D_80899510[j & 7] * 4 + 80, 1);
+            func_80033480(play, &pos, 200.0f, 1, sThroneDebrisScales[j & 7] * 4 + 60, sThroneDebrisScales[j & 7] * 4 + 80, 1);  // was D_80899510 [LLM:HIGH]
         }
     }
 }
@@ -224,7 +229,7 @@ void BgJyaIronobj_Init(Actor* thisx, PlayState* play) {
     BgJyaIronobj_InitCylinder(this, play);
     CollisionHeader_GetVirtual(sCollisionHeaders[thisx->params & 1], &colHeader);
     this->dyna.bgId = DynaPoly_SetBgActor(play, &play->colCtx.dyna, &this->dyna.actor, colHeader);
-    func_808992D8(this);
+    BgJyaIronobj_SetupWaitForHit(this);  // was func_808992D8 [LLM:HIGH]
 }
 
 void BgJyaIronobj_Destroy(Actor* thisx, PlayState* play) {
@@ -234,11 +239,11 @@ void BgJyaIronobj_Destroy(Actor* thisx, PlayState* play) {
     DynaPoly_DeleteBgActor(play, &play->colCtx.dyna, this->dyna.bgId);
 }
 
-void func_808992D8(BgJyaIronobj* this) {
-    this->actionFunc = func_808992E8;
+void BgJyaIronobj_SetupWaitForHit(BgJyaIronobj* this) {
+    this->actionFunc = BgJyaIronobj_WaitForHit;  // was func_808992E8 [LLM:HIGH]
 }
 
-void func_808992E8(BgJyaIronobj* this, PlayState* play) {
+void BgJyaIronobj_WaitForHit(BgJyaIronobj* this, PlayState* play) {
     static BgJyaIronobjIkFunc particleFunc[] = { BgJyaIronobj_SpawnPillarParticles, BgJyaIronobj_SpawnThoneParticles };
     Actor* actor;
     Vec3f dropPos;
-- 
2.55.0

