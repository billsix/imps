From 0548ac3272b2a3ce482007f7cf2805e975f55fd8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:30:03 +0000
Subject: [PATCH 1594/3799] soh: rename func_80A4E648 -> EnGs_UpdateTalking

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: offers talk while the stone is on screen, picking the Mask of Truth line or the plain one, and drives the resulting conversation through EnGs_UpdateTalkState.
Defined in soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
index 733d442f0..f2e2c7db2 100644
--- a/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
+++ b/soh/src/overlays/actors/ovl_En_Gs/z_en_gs.c
@@ -174,7 +174,8 @@ void EnGs_CheckOcarinaSong(EnGs* this, PlayState* play) {
     }
 }
 
-void func_80A4E648(EnGs* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A4E648: offers talk while the stone is on screen, picking the Mask of Truth line or the plain one, and drives the resulting conversation through EnGs_UpdateTalkState.
+void EnGs_UpdateTalking(EnGs* this, PlayState* play) {
     s16 sp26;
     s16 sp24;
 
@@ -562,7 +563,7 @@ void EnGs_Update(Actor* thisx, PlayState* play2) {
         CollisionCheck_SetOC(play, &play->colChkCtx, &this->collider.base);
     }
     this->actionFunc(this, play);
-    func_80A4E648(this, play);
+    EnGs_UpdateTalking(this, play);  // was func_80A4E648 [LLM:HIGH]
 }
 
 void EnGs_Draw(Actor* thisx, PlayState* play) {
-- 
2.55.0

