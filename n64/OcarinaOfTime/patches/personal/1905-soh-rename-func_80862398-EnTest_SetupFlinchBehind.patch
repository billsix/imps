From 762a823c568bd24d0f1020a5e0f0cfe4a7e55da8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1905/3799] soh: rename func_80862398 ->
 EnTest_SetupFlinchBehind

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: plays gStalfosFlinchFromHitBehindAnim with NA_SE_EN_STAL_DAMAGE and a knockback of -2
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index ad954bdd6..c7f2db9f1 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -29,7 +29,8 @@ void EnTest_SetupSlashDown(EnTest* this);
 void EnTest_SetupTurnToPlayer(EnTest* this);  // was func_80860BDC [LLM:HIGH]
 void EnTest_SetupIdleFromBlock(EnTest* this);
 void EnTest_SetupRecoil(EnTest* this);
-void func_80862398(EnTest* this);
+// LLM generated name (HIGH), was func_80862398: plays gStalfosFlinchFromHitBehindAnim with NA_SE_EN_STAL_DAMAGE and a knockback of -2
+void EnTest_SetupFlinchBehind(EnTest* this);  // was func_80862398 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80862154: plays gStalfosFlinchFromHitFrontAnim with NA_SE_EN_STAL_DAMAGE and a knockback of -2
 void EnTest_SetupFlinchFront(EnTest* this);  // was func_80862154 [LLM:HIGH]
 void EnTest_SetupStopAndBlock(EnTest* this);
@@ -1257,7 +1258,7 @@ void EnTest_FlinchFront(EnTest* this, PlayState* play) {
     }
 }
 
-void func_80862398(EnTest* this) {
+void EnTest_SetupFlinchBehind(EnTest* this) {
     Animation_PlayOnce(&this->skelAnime, &gStalfosFlinchFromHitBehindAnim);
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_STAL_DAMAGE);
     this->unk_7C8 = 9;
@@ -1701,7 +1702,7 @@ void EnTest_UpdateDamage(EnTest* this, PlayState* play) {
                     func_808630F0(this, play);
                     Enemy_StartFinishingBlow(play, &this->actor);
                 } else {
-                    func_80862398(this);
+                    EnTest_SetupFlinchBehind(this);  // was func_80862398 [LLM:HIGH]
                 }
             }
         }
-- 
2.55.0

