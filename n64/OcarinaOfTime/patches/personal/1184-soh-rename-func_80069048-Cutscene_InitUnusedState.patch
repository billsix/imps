From 3d04685ea5e6539024caeff6ae566d131c63f7ec Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:55:36 +0000
Subject: [PATCH 1184/3799] soh: rename func_80069048 ->
 Cutscene_InitUnusedState

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: zeroes the two write-only cutscene globals around an empty twenty-iteration loop; nothing anywhere calls it, so "init" is inferred from the shape alone.
Defined in soh/src/code/z_demo.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/z_demo.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/src/code/z_demo.c b/soh/src/code/z_demo.c
index 13969c13c..22aa4dccb 100644
--- a/soh/src/code/z_demo.c
+++ b/soh/src/code/z_demo.c
@@ -2200,7 +2200,8 @@ void Cutscene_SetupScripted(PlayState* play, CutsceneContext* csCtx) {
     }
 }
 
-void func_80069048(PlayState* play) {
+// LLM generated name (GUESS), was func_80069048: zeroes the two write-only cutscene globals around an empty twenty-iteration loop; nothing anywhere calls it, so "init" is inferred from the shape alone.
+void Cutscene_InitUnusedState(PlayState* play) {
     s16 i;
 
     D_8015FCCC = 0;
-- 
2.55.0

