From 8067860bd72063c28e61ed60f8c6c5f8aadbf26b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:49:36 +0000
Subject: [PATCH 2778/3799] soh: rename func_800358DC -> Actor_SpawnEnPart

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spawns an EN_PART child carrying the caller's speed, spin, timer and display list
Defined in soh/src/code/z_actor.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                     | 4 ++--
 soh/src/code/z_actor.c                      | 3 ++-
 soh/src/overlays/actors/ovl_En_Mb/z_en_mb.c | 2 +-
 3 files changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 10d95c017..425bfddbe 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -554,8 +554,8 @@ u8 Actor_ApplyDamage(Actor* actor);
 void Actor_SetDropFlag(Actor* actor, ColliderInfo* colBody, s32 freezeFlag);
 void Actor_SetDropFlagJntSph(Actor* actor, ColliderJntSph* colBody, s32 freezeFlag);
 void Actor_GetPitchYawBetween(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3);  // was func_80035844 [LLM:HIGH]
-Actor* func_800358DC(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused,
-                     PlayState* play, s16 params, s32 arg8);
+Actor* Actor_SpawnEnPart(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused,
+                     PlayState* play, s16 params, s32 arg8);  // was func_800358DC [LLM:HIGH]
 void func_800359B8(Actor* actor, s16 arg1, Vec3s* arg2);
 s32 Flags_GetEventChkInf(s32 flag);
 void Flags_SetEventChkInf(s32 flag);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 0e0dde958..d31c20ad3 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -4911,7 +4911,8 @@ void Actor_GetPitchYawBetween(Vec3f* arg0, Vec3f* arg1, Vec3s* arg2, s32 arg3) {
 /**
  * Spawns En_Part (Dissipating Flames) actor as a child of the given actor.
  */
-Actor* func_800358DC(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused, PlayState* play,
+// LLM generated name (HIGH), was func_800358DC: spawns an EN_PART child carrying the caller's speed, spin, timer and display list
+Actor* Actor_SpawnEnPart(Actor* actor, Vec3f* spawnPos, Vec3s* spawnRot, f32* arg3, s32 timer, s16* unused, PlayState* play,
                      s16 params, s32 arg8) {
     EnPart* spawnedEnPart;
 
diff --git a/soh/src/overlays/actors/ovl_En_Mb/z_en_mb.c b/soh/src/overlays/actors/ovl_En_Mb/z_en_mb.c
index 0a8ae62e9..6e53ad465 100644
--- a/soh/src/overlays/actors/ovl_En_Mb/z_en_mb.c
+++ b/soh/src/overlays/actors/ovl_En_Mb/z_en_mb.c
@@ -894,7 +894,7 @@ void EnMb_ClubAttack(EnMb* this, PlayState* play) {
             if (!CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0)) {
                 Camera_AddQuake(&play->mainCamera, 2, 0x19, 5);
             }
-            func_800358DC(&this->actor, &effSpawnPos, &this->actor.world.rot, flamesParams, 20, flamesUnused, play, -1,
+            Actor_SpawnEnPart(&this->actor, &effSpawnPos, &this->actor.world.rot, flamesParams, 20, flamesUnused, play, -1,  // was func_800358DC [LLM:HIGH]
                           0);
             EnMb_SetupClubWaitAfterAttack(this);
         }
-- 
2.55.0

