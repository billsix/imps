From b7b7c2a669af058c107a345a9b48cad01dc2cc8b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:19:14 +0000
Subject: [PATCH 3463/3799] soh: rename D_80B871F4 -> sBurnOrigin

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the zero vector ItemShield_Burn transforms
Defined in soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Item_Shield/z_item_shield.c    | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
index 40136f643..7a50fdc97 100644
--- a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
+++ b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
@@ -146,7 +146,7 @@ void ItemShield_OfferUntilHit(ItemShield* this, PlayState* play) {
 
 // LLM generated name (HIGH), was func_80B86CA8: spawns eight staggered EffectSsFireTail flames, tips the shield over once it lands, shrinks it away and kills the actor.
 void ItemShield_Burn(ItemShield* this, PlayState* play) {
-    static Vec3f D_80B871F4 = { 0.0f, 0.0f, 0.0f };
+    static Vec3f sBurnOrigin = { 0.0f, 0.0f, 0.0f };  // was D_80B871F4 [LLM:HIGH]
     static f32 D_80B87200[] = { 0.3f, 0.6f,  0.9f, 1.0f,  1.0f, 1.0f,  1.0f, 1.0f,
                                 1.0f, 0.85f, 0.7f, 0.55f, 0.4f, 0.25f, 0.1f, 0.0f };
     static f32 D_80B87240[] = { 1.0f, 1.0f, 1.0f, 1.0f, 1.0f, 1.0f, 1.0f, 0.8f,
@@ -160,10 +160,10 @@ void ItemShield_Burn(ItemShield* this, PlayState* play) {
 
     for (i = 0; i < 8; i++) {
         temp = 15 - this->unk_19E[i];
-        D_80B871F4.x = this->unk_1A8[i].x;
-        D_80B871F4.y = this->unk_1A8[i].y + (this->actor.shape.yOffset * 0.01f) + (D_80B87200[temp] * -10.0f * 0.2f);
-        D_80B871F4.z = this->unk_1A8[i].z;
-        EffectSsFireTail_SpawnFlame(play, &this->actor, &D_80B871F4, D_80B87200[temp] * 0.2f, -1, D_80B87240[temp]);
+        sBurnOrigin.x = this->unk_1A8[i].x;  // was D_80B871F4 [LLM:HIGH]
+        sBurnOrigin.y = this->unk_1A8[i].y + (this->actor.shape.yOffset * 0.01f) + (D_80B87200[temp] * -10.0f * 0.2f);  // was D_80B871F4 [LLM:HIGH]
+        sBurnOrigin.z = this->unk_1A8[i].z;  // was D_80B871F4 [LLM:HIGH]
+        EffectSsFireTail_SpawnFlame(play, &this->actor, &sBurnOrigin, D_80B87200[temp] * 0.2f, -1, D_80B87240[temp]);  // was D_80B871F4 [LLM:HIGH]
         if (this->unk_19E[i] != 0) {
             this->unk_19E[i]--;
         } else if (this->timer > 16) {
-- 
2.55.0

