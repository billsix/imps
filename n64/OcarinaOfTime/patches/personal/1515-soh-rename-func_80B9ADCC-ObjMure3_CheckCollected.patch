From 054ac7a66498273a8c4b29c888ef56962005d603 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:24:25 +0000
Subject: [PATCH 1515/3799] soh: rename func_80B9ADCC ->
 ObjMure3_CheckCollected

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: watches the spawned rupees each frame, setting the group's switch flag as soon as the player picks one up and marking each one gone when its actor dies.
Defined in soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
index fce6b9de3..617497b97 100644
--- a/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
+++ b/soh/src/overlays/actors/ovl_Obj_Mure3/z_obj_mure3.c
@@ -126,7 +126,8 @@ void ObjMure3_DespawnRupees(ObjMure3* this, PlayState* play) {
     }
 }
 
-void func_80B9ADCC(ObjMure3* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80B9ADCC: watches the spawned rupees each frame, setting the group's switch flag as soon as the player picks one up and marking each one gone when its actor dies.
+void ObjMure3_CheckCollected(ObjMure3* this, PlayState* play) {
     s16 count = sRupeeCounts[(this->actor.params >> 13) & 7];
     s32 i;
 
@@ -187,7 +188,7 @@ void func_80B9AFEC(ObjMure3* this) {
 }
 
 void func_80B9AFFC(ObjMure3* this, PlayState* play) {
-    func_80B9ADCC(this, play);
+    ObjMure3_CheckCollected(this, play);  // was func_80B9ADCC [LLM:HIGH]
     if (Math3D_Dist1DSq(this->actor.projectedPos.x, this->actor.projectedPos.z) >= SQ(1450.0f)) {
         this->actor.flags &= ~ACTOR_FLAG_UPDATE_CULLING_DISABLED;
         ObjMure3_DespawnRupees(this, play);  // was func_80B9ACE4 [LLM:HIGH]
-- 
2.55.0

