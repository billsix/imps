From 9f5cb992d168cdc5af8cf38272647ef6491d5a06 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:17:03 +0000
Subject: [PATCH 3383/3799] soh: rename D_80A6D510 -> sWanderAnimKinds

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: maps each animation onto the wander behaviour that drives it
Defined in soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
index ec2b8111d..8bf13a0dc 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Normal/z_en_horse_normal.c
@@ -380,11 +380,11 @@ void EnHorseNormal_PlayAnimSfx(EnHorseNormal* this) {
 
 void EnHorseNormal_Wander(EnHorseNormal* this, PlayState* play) {
     static s32 sWanderNextAnims[] = { 0, 1, 4, 5, 6, 2, 3 };  // was D_80A6D4F4 [LLM:HIGH]
-    static s32 D_80A6D510[] = { 0, 0, 2, 2, 1, 1, 1, 3, 3 };
+    static s32 sWanderAnimKinds[] = { 0, 0, 2, 2, 1, 1, 1, 3, 3 };  // was D_80A6D510 [LLM:HIGH]
     s32 phi_t0 = this->animationIdx;
     s32 pad;
 
-    switch (D_80A6D510[this->animationIdx]) {
+    switch (sWanderAnimKinds[this->animationIdx]) {  // was D_80A6D510 [LLM:HIGH]
         case 0:
             EnHorseNormal_PlayAnimSfx(this);  // was func_80A6BD7C [LLM:HIGH]
             this->actor.speedXZ = 0.0f;
@@ -454,7 +454,7 @@ void EnHorseNormal_Wander(EnHorseNormal* this, PlayState* play) {
             Animation_Change(&this->skin.skelAnime, sAnimations[this->animationIdx], EnHorseNormal_GetAnimPlaySpeed(this), 0.0f,  // was func_80A6B30C [LLM:HIGH]
                              Animation_GetLastFrame(sAnimations[this->animationIdx]), ANIMMODE_ONCE, -3.0f);
         } else {
-            switch (D_80A6D510[this->animationIdx]) {
+            switch (sWanderAnimKinds[this->animationIdx]) {  // was D_80A6D510 [LLM:HIGH]
                 case 0:
                     if (Rand_ZeroOne() < 0.25f) {
                         this->unk_218 = 1.0f;
-- 
2.55.0

