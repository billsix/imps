From 0ed3e53cb85b6a79c8af8926b4c0ca49c0266b4f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:26:56 +0000
Subject: [PATCH 1557/3799] soh: rename func_80A2F608 -> EnGb_UpdateColliders

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: places the body collider 25 units in front of the actor and each bottle collider at its own offset, all rotated into the actor's frame.
Defined in soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c b/soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c
index 05a649591..6e3dc152e 100644
--- a/soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c
+++ b/soh/src/overlays/actors/ovl_En_Gb/z_en_gb.c
@@ -228,7 +228,8 @@ void EnGb_Destroy(Actor* thisx, PlayState* play) {
     DynaPoly_DeleteBgActor(play, &play->colCtx.dyna, this->dyna.bgId);
 }
 
-void func_80A2F608(EnGb* this) {
+// LLM generated name (HIGH), was func_80A2F608: places the body collider 25 units in front of the actor and each bottle collider at its own offset, all rotated into the actor's frame.
+void EnGb_UpdateColliders(EnGb* this) {
     s32 i;
     Vec3f sp48;
     Vec3f sp3C;
@@ -412,7 +413,7 @@ void EnGb_Update(Actor* thisx, PlayState* play2) {
     SkelAnime_Update(&this->skelAnime);
     this->actionFunc(this, play);
     this->dyna.actor.textId = this->textId;
-    func_80A2F608(this);
+    EnGb_UpdateColliders(this);  // was func_80A2F608 [LLM:HIGH]
     CollisionCheck_SetOC(play, &play->colChkCtx, &this->collider.base);
 
     for (i = 0; i < ARRAY_COUNT(this->bottlesColliders); i++) {
-- 
2.55.0

