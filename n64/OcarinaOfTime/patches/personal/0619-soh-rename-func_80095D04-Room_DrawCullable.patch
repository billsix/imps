From cc55a131d1dd337fe80afb38d3ab71c63563092c Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 15:21:13 +0000
Subject: [PATCH 619/784] soh: rename func_80095D04 -> Room_DrawCullable

Name source: zeldaret/oot (same decomp, same ROM address).
Structure verified against upstream at 0.94.
Defined in soh/src/code/z_room.c.
---
 soh/src/code/z_room.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/code/z_room.c b/soh/src/code/z_room.c
index d48c31cd4..b000893db 100644
--- a/soh/src/code/z_room.c
+++ b/soh/src/code/z_room.c
@@ -15,7 +15,8 @@
 
 // Name from zeldaret/oot (was func_80095AB4): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_room.c]
 void Room_DrawNormal(PlayState* play, Room* room, u32 flags);
-void func_80095D04(PlayState* play, Room* room, u32 flags);
+// Name from zeldaret/oot (was func_80095D04): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.94. [oot: https://github.com/zeldaret/oot/blob/main/src/code/z_room.c]
+void Room_DrawCullable(PlayState* play, Room* room, u32 flags);
 void func_80096F6C(PlayState* play, Room* room, u32 flags);
 
 Vec3f D_801270A0 = { 0.0f, 0.0f, 0.0f };
@@ -41,7 +42,7 @@ s32 OTRfunc_800973FC(PlayState* play, RoomContext* roomCtx);
 void (*sRoomDrawHandlers[])(PlayState* play, Room* room, u32 flags) = {
     Room_DrawNormal,
     func_80096F6C,
-    func_80095D04,
+    Room_DrawCullable,
 };
 
 void func_80095AA0(PlayState* play, Room* room, Input* arg2, UNK_TYPE arg3) {
@@ -96,7 +97,7 @@ typedef struct struct_80095D04 {
 } struct_80095D04; // size = 0x10
 
 // Room Draw Polygon Type 2
-void func_80095D04(PlayState* play, Room* room, u32 flags) {
+void Room_DrawCullable(PlayState* play, Room* room, u32 flags) {
     PolygonType2* polygon2;
     PolygonDlist2* polygonDlist;
     struct_80095D04 spB8[SHAPE_SORT_MAX];
-- 
2.55.0

