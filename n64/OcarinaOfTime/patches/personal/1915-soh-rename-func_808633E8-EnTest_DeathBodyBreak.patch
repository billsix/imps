From fa7079c2a2fc5526eb9ce231c2fc13f597cf51f1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:12 +0000
Subject: [PATCH 1915/3799] soh: rename func_808633E8 -> EnTest_DeathBodyBreak

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: scatters the body-break parts, drops a random collectible, decrements the parent's count and kills the actor
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index 65be6c72c..9f4937ee0 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -70,7 +70,8 @@ void EnTest_DeathFallBackwards(EnTest* this, PlayState* play);  // was func_8086
 // LLM generated name (HIGH), was func_8086318C: waits out the forwards death animation, thumping at frames 10 and 25, then allocates the body break
 void EnTest_DeathFallForwards(EnTest* this, PlayState* play);  // was func_8086318C [LLM:HIGH]
 void EnTest_Recoil(EnTest* this, PlayState* play);
-void func_808633E8(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_808633E8: scatters the body-break parts, drops a random collectible, decrements the parent's count and kills the actor
+void EnTest_DeathBodyBreak(EnTest* this, PlayState* play);  // was func_808633E8 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80862FA8: plays gStalfosFallOverBackwardsAnim with NA_SE_EN_STAL_DEAD; the higher params fall through to EnTest_SetupBreakApart instead
 void EnTest_SetupDeathFallBackwards(EnTest* this, PlayState* play);  // was func_80862FA8 [LLM:HIGH]
 
@@ -1569,7 +1570,7 @@ void EnTest_DeathFallBackwards(EnTest* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->timer = (Rand_ZeroOne() * 10.0f) + 10.0f;
         this->unk_7C8 = 7;
-        EnTest_SetupAction(this, func_808633E8);
+        EnTest_SetupAction(this, EnTest_DeathBodyBreak);  // was func_808633E8 [LLM:HIGH]
         BodyBreak_Alloc(&this->bodyBreak, 60, play);
     }
 
@@ -1599,7 +1600,7 @@ void EnTest_DeathFallForwards(EnTest* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->timer = (Rand_ZeroOne() * 10.0f) + 10.0f;
         this->unk_7C8 = 7;
-        EnTest_SetupAction(this, func_808633E8);
+        EnTest_SetupAction(this, EnTest_DeathBodyBreak);  // was func_808633E8 [LLM:HIGH]
         BodyBreak_Alloc(&this->bodyBreak, 60, play);
     }
 
@@ -1643,7 +1644,7 @@ void EnTest_Rise(EnTest* this, PlayState* play) {
     }
 }
 
-void func_808633E8(EnTest* this, PlayState* play) {
+void EnTest_DeathBodyBreak(EnTest* this, PlayState* play) {
     this->actor.params = STALFOS_TYPE_1;
 
     if (BodyBreak_SpawnParts(&this->actor, &this->bodyBreak, play, this->actor.params)) {
-- 
2.55.0

