From 7789c6c5ba23d478728d69ae0e55ac2fca6903a2 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:10:57 +0000
Subject: [PATCH 2044/3799] soh: rename func_80AB6A38 -> EnNiw_FollowPath

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: walks the setup path waypoint by waypoint, hopping toward each with EnNiw_HopTowardTarget
Defined in soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
index 96d6cc9eb..980116860 100644
--- a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
@@ -28,7 +28,8 @@ void EnNiw_Airborne(EnNiw* this, PlayState* play);  // was func_80AB63A8 [LLM:HI
 void EnNiw_WaitToBeLifted(EnNiw* this, PlayState* play);  // was func_80AB6450 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80AB6570: the default idle: offers itself to be carried, or starts down its path for the params 0xA river cucco
 void EnNiw_Idle(EnNiw* this, PlayState* play);  // was func_80AB6570 [LLM:HIGH]
-void func_80AB6A38(EnNiw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB6A38: walks the setup path waypoint by waypoint, hopping toward each with EnNiw_HopTowardTarget
+void EnNiw_FollowPath(EnNiw* this, PlayState* play);  // was func_80AB6A38 [LLM:HIGH]
 void func_80AB6BF8(EnNiw* this, PlayState* play);
 void func_80AB6D08(EnNiw* this, PlayState* play);
 void func_80AB6EB4(EnNiw* this, PlayState* play);
@@ -513,7 +514,7 @@ void EnNiw_Idle(EnNiw* this, PlayState* play) {
             this->actor.speedXZ = 4.0f;
             this->unk_300 = 0.0f;
             this->unk_2FC = 0.0f;
-            this->actionFunc = func_80AB6A38;
+            this->actionFunc = EnNiw_FollowPath;  // was func_80AB6A38 [LLM:HIGH]
             return;
         }
     }
@@ -597,7 +598,7 @@ void EnNiw_Idle(EnNiw* this, PlayState* play) {
     EnNiw_AnimateLimbs(this, play, tmp);  // was func_80AB5BF8 [LLM:HIGH]
 }
 
-void func_80AB6A38(EnNiw* this, PlayState* play) {
+void EnNiw_FollowPath(EnNiw* this, PlayState* play) {
     Path* path;
     Vec3s* pointPos;
     f32 pathDiffX;
-- 
2.55.0

