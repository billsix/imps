From fb90f4bd8f0a9f7079befc691f6395bd45ed44a0 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:28:09 +0000
Subject: [PATCH 2433/3799] soh: rename func_80ADEECC -> EnPoh_Damage

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: rides the knockback out, then flees or dissolves according to remaining health
Defined in soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
index 24d1fe598..d955a8221 100644
--- a/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
+++ b/soh/src/overlays/actors/ovl_En_Poh/z_en_poh.c
@@ -28,7 +28,8 @@ void EnPoh_Idle(EnPoh* this, PlayState* play);
 // LLM generated name (HIGH), was func_80ADEC9C: closes on the player, turning to face them and speeding up for the strike
 void EnPoh_Approach(EnPoh* this, PlayState* play);  // was func_80ADEC9C [LLM:HIGH]
 void EnPoh_Attack(EnPoh* this, PlayState* play);
-void func_80ADEECC(EnPoh* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ADEECC: rides the knockback out, then flees or dissolves according to remaining health
+void EnPoh_Damage(EnPoh* this, PlayState* play);  // was func_80ADEECC [LLM:HIGH]
 void func_80ADF894(EnPoh* this, PlayState* play);
 void EnPoh_ComposerAppear(EnPoh* this, PlayState* play);
 void func_80ADEF38(EnPoh* this, PlayState* play);
@@ -310,7 +311,7 @@ void EnPoh_SetupDamage(EnPoh* this) {
     this->colliderCyl.base.acFlags &= ~AC_ON;
     this->actor.speedXZ = 5.0f;
     Actor_SetColorFilter(&this->actor, 0x4000, 0xFF, 0, 0x10);
-    this->actionFunc = func_80ADEECC;
+    this->actionFunc = EnPoh_Damage;  // was func_80ADEECC [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_80ADE368: morphs to the flee animation, turns away from the player and arms the 200-frame retreat
@@ -581,7 +582,7 @@ void EnPoh_Attack(EnPoh* this, PlayState* play) {
     }
 }
 
-void func_80ADEECC(EnPoh* this, PlayState* play) {
+void EnPoh_Damage(EnPoh* this, PlayState* play) {
     Math_StepToF(&this->actor.speedXZ, 0.0f, 0.5f);
     if (SkelAnime_Update(&this->skelAnime)) {
         if (this->actor.colChkInfo.health != 0) {
@@ -961,7 +962,7 @@ void func_80AE067C(EnPoh* this) {
         this->lightColor.g = CLAMP_MAX((s16)(this->lightColor.g + 5), 255);
         temp_var = this->lightColor.b + 5;
         this->lightColor.b = CLAMP_MAX(temp_var, 225);
-    } else if (this->actionFunc == func_80ADEECC) {
+    } else if (this->actionFunc == EnPoh_Damage) {  // was func_80ADEECC [LLM:HIGH]
         if (this->actor.colorFilterTimer & 2) {
             this->lightColor.r = 0;
             this->lightColor.g = 0;
@@ -1025,7 +1026,7 @@ void EnPoh_UpdateLiving(Actor* thisx, PlayState* play) {
     CollisionCheck_SetOC(play, &play->colChkCtx, &this->colliderCyl.base);
     CollisionCheck_SetOC(play, &play->colChkCtx, &this->colliderSph.base);
     Actor_SetFocus(&this->actor, 42.0f);
-    if (this->actionFunc != func_80ADEECC && this->actionFunc != func_80ADF574) {
+    if (this->actionFunc != EnPoh_Damage && this->actionFunc != func_80ADF574) {  // was func_80ADEECC [LLM:HIGH]
         if (this->actionFunc == func_80ADF894) {
             this->actor.shape.rot.y = this->actor.world.rot.y + 0x8000;
         } else {
-- 
2.55.0

