From 5936539d99668953386d5690c71966da836c59f8 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:54:05 +0000
Subject: [PATCH 1152/3799] soh: rename func_80B176E0 -> EnTakaraMan_SetupIdle

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: restarts the looping idle animation and, when the player has not yet paid, arms the 10-rupee offer text and its choice state.
Defined in soh/src/overlays/actors/ovl_En_Takara_Man/z_en_takara_man.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Takara_Man/z_en_takara_man.c        | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Takara_Man/z_en_takara_man.c b/soh/src/overlays/actors/ovl_En_Takara_Man/z_en_takara_man.c
index 6cdaf3c99..99bcaceee 100644
--- a/soh/src/overlays/actors/ovl_En_Takara_Man/z_en_takara_man.c
+++ b/soh/src/overlays/actors/ovl_En_Takara_Man/z_en_takara_man.c
@@ -21,7 +21,8 @@ void EnTakaraMan_Update(Actor* thisx, PlayState* play);
 void EnTakaraMan_Draw(Actor* thisx, PlayState* play);
 void EnTakaraMan_Destroy(Actor* thisx, PlayState* play);
 
-void func_80B176E0(EnTakaraMan* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B176E0: restarts the looping idle animation and, when the player has not yet paid, arms the 10-rupee offer text and its choice state.
+void EnTakaraMan_SetupIdle(EnTakaraMan* this, PlayState* play);  // was func_80B176E0 [LLM:HIGH]
 void func_80B1778C(EnTakaraMan* this, PlayState* play);
 void func_80B17B14(EnTakaraMan* this, PlayState* play);
 void func_80B17934(EnTakaraMan* this, PlayState* play);
@@ -81,13 +82,13 @@ void EnTakaraMan_Init(Actor* thisx, PlayState* play) {
     thisx->room = -1;
     thisx->world.rot.y = thisx->shape.rot.y = -0x4E20;
     thisx->targetMode = 1;
-    this->actionFunc = func_80B176E0;
+    this->actionFunc = EnTakaraMan_SetupIdle;  // was func_80B176E0 [LLM:HIGH]
 }
 
 void EnTakaraMan_Destroy(Actor* thisx, PlayState* play) {
 }
 
-void func_80B176E0(EnTakaraMan* this, PlayState* play) {
+void EnTakaraMan_SetupIdle(EnTakaraMan* this, PlayState* play) {
     f32 frameCount = Animation_GetLastFrame(&object_ts_Anim_000498);
 
     Animation_Change(&this->skelAnime, &object_ts_Anim_000498, 1.0f, 0.0f, (s16)frameCount, ANIMMODE_LOOP, -10.0f);
@@ -188,14 +189,14 @@ void func_80B17A6C(EnTakaraMan* this, PlayState* play) {
 
 void func_80B17AC4(EnTakaraMan* this, PlayState* play) {
     if (Message_GetState(&play->msgCtx) == TEXT_STATE_DONE && Message_ShouldAdvance(play)) {
-        this->actionFunc = func_80B176E0;
+        this->actionFunc = EnTakaraMan_SetupIdle;  // was func_80B176E0 [LLM:HIGH]
     }
 }
 
 void func_80B17B14(EnTakaraMan* this, PlayState* play) {
     if (this->dialogState == Message_GetState(&play->msgCtx) && Message_ShouldAdvance(play)) {
         Message_CloseTextbox(play);
-        this->actionFunc = func_80B176E0;
+        this->actionFunc = EnTakaraMan_SetupIdle;  // was func_80B176E0 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

