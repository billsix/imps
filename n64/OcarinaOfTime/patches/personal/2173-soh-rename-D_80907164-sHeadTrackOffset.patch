From 8a4c6e28b7fd75e0d20fecab491c1f4b565cd3c7 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:15:36 +0000
Subject: [PATCH 2173/3799] soh: rename D_80907164 -> sHeadTrackOffset

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the offset limb 10 of the second skeleton records into the same field limb 15 fills from the origin in the first; the limb is head-adjacent but not confirmed
Defined in soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c      | 2 +-
 soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c | 3 ++-
 2 files changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
index 6e5913ecf..5e3f948bb 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
@@ -2786,7 +2786,7 @@ void BossGanon2_PostLimbDraw2(PlayState* play, s32 limbIndex, Gfx** dList, Vec3s
 
         CLOSE_DISPS(play->state.gfxCtx);
     } else if (limbIndex == 10) {
-        Matrix_MultVec3f(&D_80907164, &this->unk_1B8);
+        Matrix_MultVec3f(&sHeadTrackOffset, &this->unk_1B8);  // was D_80907164 [LLM:GUESS]
     }
 }
 
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
index 602cc117e..7e42d6765 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2_data.c
@@ -305,7 +305,8 @@ static s8 sLimbPosIndices2[] = {  // was D_80907144 [LLM:HIGH]
     0xFF, 0xFF, 0xFF, 0x02, 0x0C, 0x0D, 0x0E, 0x09, 0x0A, 0x0B, 0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00,
 };
 
-static Vec3f D_80907164 = { 800.0f, 420.0f, 100.0f };
+// LLM generated name (GUESS), was D_80907164: the offset limb 10 of the second skeleton records into the same field limb 15 fills from the origin in the first; the limb is head-adjacent but not confirmed
+static Vec3f sHeadTrackOffset = { 800.0f, 420.0f, 100.0f };  // was D_80907164 [LLM:GUESS]
 
 static void* sEyeTextures[] = {
     gGanonEyeOpenTex,
-- 
2.55.0

