From 7929775a7fa142098c1250c5cd4834c205c814dd Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:18 +0000
Subject: [PATCH 1008/3799] soh: rename func_80B86F68 ->
 ItemShield_DetachFromPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 1 first action: copies position and orientation out of the player's shieldMf, gives the shield an upward hop, and hands off to the burning state.
Defined in soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
index 938d92b5e..40136f643 100644
--- a/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
+++ b/soh/src/overlays/actors/ovl_Item_Shield/z_item_shield.c
@@ -17,7 +17,8 @@ void ItemShield_Destroy(Actor* thisx, PlayState* play);
 void ItemShield_Update(Actor* thisx, PlayState* play);
 void ItemShield_Draw(Actor* thisx, PlayState* play);
 
-void func_80B86F68(ItemShield* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B86F68: the params 1 first action: copies position and orientation out of the player's shieldMf, gives the shield an upward hop, and hands off to the burning state.
+void ItemShield_DetachFromPlayer(ItemShield* this, PlayState* play);  // was func_80B86F68 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B86BC8: the params 0 action: offers GI_SHIELD_DEKU from its perch and, on an AC hit, launches the shield into the dropped state.
 void ItemShield_OfferUntilHit(ItemShield* this, PlayState* play);  // was func_80B86BC8 [LLM:HIGH]
 
@@ -77,7 +78,7 @@ void ItemShield_Init(Actor* thisx, PlayState* play) {
 
         case 1:
             ActorShape_Init(&this->actor.shape, 0.0f, NULL, 0.0f);
-            ItemShield_SetupAction(this, func_80B86F68);
+            ItemShield_SetupAction(this, ItemShield_DetachFromPlayer);  // was func_80B86F68 [LLM:HIGH]
             this->unk_19C |= 2;
             for (i = 0; i < 8; i++) {
                 this->unk_19E[i] = 1 + 2 * i;
@@ -187,7 +188,7 @@ void ItemShield_Burn(ItemShield* this, PlayState* play) {
     }
 }
 
-void func_80B86F68(ItemShield* this, PlayState* play) {
+void ItemShield_DetachFromPlayer(ItemShield* this, PlayState* play) {
     s32 pad;
     Player* player = GET_PLAYER(play);
     MtxF* shield = &player->shieldMf;
-- 
2.55.0

