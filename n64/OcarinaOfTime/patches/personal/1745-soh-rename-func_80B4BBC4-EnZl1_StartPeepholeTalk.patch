From 7e5e4fa3619fec26565762a807d0a6bd11aca332 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:08 +0000
Subject: [PATCH 1745/3799] soh: rename func_80B4BBC4 ->
 EnZl1_StartPeepholeTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: starts her looping surprised animation, halts the actors, plays Link's surprise voice and opens the first line of the window conversation.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index 58c7f29d5..3912bdeac 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -24,7 +24,8 @@ void EnZl1_StartCourtyardCutscene(EnZl1* this, PlayState* play);  // was func_80
 void EnZl1_CourtyardCutscene(EnZl1* this, PlayState* play);  // was func_80B4B240 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B4B8B4: plays the animation the current actor cue calls for and keeps Zelda on the cue's path.
 void EnZl1_CsPlayCueAnimation(EnZl1* this, PlayState* play);  // was func_80B4B8B4 [LLM:HIGH]
-void func_80B4BBC4(EnZl1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B4BBC4: starts her looping surprised animation, halts the actors, plays Link's surprise voice and opens the first line of the window conversation.
+void EnZl1_StartPeepholeTalk(EnZl1* this, PlayState* play);  // was func_80B4BBC4 [LLM:HIGH]
 void func_80B4BC78(EnZl1* this, PlayState* play);
 void func_80B4BF2C(EnZl1* this, PlayState* play);
 
@@ -396,7 +397,7 @@ void EnZl1_CsPlayCueAnimation(EnZl1* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
     EnZl1_MoveActor(this, play);  // was func_80B4B874 [LLM:HIGH]
     if (play->csCtx.state == CS_STATE_IDLE) {
-        this->actionFunc = func_80B4BBC4;
+        this->actionFunc = EnZl1_StartPeepholeTalk;  // was func_80B4BBC4 [LLM:HIGH]
         return;
     }
 
@@ -432,7 +433,7 @@ void EnZl1_CsPlayCueAnimation(EnZl1* this, PlayState* play) {
     }
 }
 
-void func_80B4BBC4(EnZl1* this, PlayState* play) {
+void EnZl1_StartPeepholeTalk(EnZl1* this, PlayState* play) {
     s32 pad;
     f32 frameCount = Animation_GetLastFrame(&gChildZelda1Anim_00438);
     Player* player = GET_PLAYER(play);
-- 
2.55.0

