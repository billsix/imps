From e1acde98e08afe58ad63cc95cb7905161b06b2d5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:32:03 +0000
Subject: [PATCH 1631/3799] soh: rename func_8096F1D4 ->
 DemoEc_UpdateEyesAfterFrame32

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: holds eye texture 3 for the first 32 frames of the animation and runs the ordinary blink afterwards.
Defined in soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c b/soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c
index c014d98eb..a07577cb8 100644
--- a/soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c
+++ b/soh/src/overlays/actors/ovl_Demo_Ec/z_demo_ec.c
@@ -798,7 +798,8 @@ void DemoEc_InitKingZora(DemoEc* this, PlayState* play) {
     DemoEc_SetEyeTexIndex(this, 3);
 }
 
-void func_8096F1D4(DemoEc* this) {
+// LLM generated name (HIGH), was func_8096F1D4: holds eye texture 3 for the first 32 frames of the animation and runs the ordinary blink afterwards.
+void DemoEc_UpdateEyesAfterFrame32(DemoEc* this) {
     f32 currentFrame = this->skelAnime.curFrame;
 
     if (currentFrame <= 32.0f) {
@@ -851,7 +852,7 @@ void func_8096F378(DemoEc* this, PlayState* play) {
     s32 animDone = DemoEc_UpdateSkelAnime(this);
 
     DemoEc_MoveActor(this, play);  // was func_8096D594 [LLM:HIGH]
-    func_8096F1D4(this);
+    DemoEc_UpdateEyesAfterFrame32(this);  // was func_8096F1D4 [LLM:HIGH]
     DemoEc_UpdateBgFlags(this, play);
     func_8096F26C(this, animDone);
 }
-- 
2.55.0

