From 3196121c78bf789bfa8742b94654572e6ba65bd9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:12 +0000
Subject: [PATCH 1914/3799] soh: rename func_8086318C ->
 EnTest_DeathFallForwards

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: waits out the forwards death animation, thumping at frames 10 and 25, then allocates the body break
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index 2abe6a113..65be6c72c 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -67,7 +67,8 @@ void EnTest_CircleAroundPlayer(EnTest* this, PlayState* play);  // was func_8086
 void EnTest_BreakApart(EnTest* this, PlayState* play);  // was func_80862E6C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80863044: waits out the backwards death animation, thumping at frame 15, then allocates the body break
 void EnTest_DeathFallBackwards(EnTest* this, PlayState* play);  // was func_80863044 [LLM:HIGH]
-void func_8086318C(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_8086318C: waits out the forwards death animation, thumping at frames 10 and 25, then allocates the body break
+void EnTest_DeathFallForwards(EnTest* this, PlayState* play);  // was func_8086318C [LLM:HIGH]
 void EnTest_Recoil(EnTest* this, PlayState* play);
 void func_808633E8(EnTest* this, PlayState* play);
 // LLM generated name (HIGH), was func_80862FA8: plays gStalfosFallOverBackwardsAnim with NA_SE_EN_STAL_DEAD; the higher params fall through to EnTest_SetupBreakApart instead
@@ -1588,13 +1589,13 @@ void EnTest_SetupDeathFallForwards(EnTest* this, PlayState* play) {
 
     if (this->actor.params <= STALFOS_TYPE_CEILING) {
         this->actor.flags &= ~ACTOR_FLAG_ATTENTION_ENABLED;
-        EnTest_SetupAction(this, func_8086318C);
+        EnTest_SetupAction(this, EnTest_DeathFallForwards);  // was func_8086318C [LLM:HIGH]
     } else {
         EnTest_SetupBreakApart(this, play);  // was func_80862DBC [LLM:HIGH]
     }
 }
 
-void func_8086318C(EnTest* this, PlayState* play) {
+void EnTest_DeathFallForwards(EnTest* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->timer = (Rand_ZeroOne() * 10.0f) + 10.0f;
         this->unk_7C8 = 7;
-- 
2.55.0

