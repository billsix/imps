From 1fd6ebdaf4035ade53c12aeb08c791f7a19bda21 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:58:38 +0000
Subject: [PATCH 1220/3799] soh: rename func_8088E90C ->
 BgHidanSima_UpdateCollider

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: places the two JntSph collider spheres in world space along the platform's facing each frame.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c    | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
index 9be8eb1cb..114370647 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Sima/z_bg_hidan_sima.c
@@ -24,7 +24,8 @@ void BgHidanSima_Sink(BgHidanSima* this, PlayState* play);  // was func_8088E6D0
 void BgHidanSima_WaitToSlide(BgHidanSima* this, PlayState* play);  // was func_8088E760 [LLM:HIGH]
 // LLM generated name (HIGH), was func_8088E7A8: sweeps the platform up to 200 units along its facing on a sine curve while looping the fire-pillar sfx, then returns to the pause.
 void BgHidanSima_Slide(BgHidanSima* this, PlayState* play);  // was func_8088E7A8 [LLM:HIGH]
-void func_8088E90C(BgHidanSima* this);
+// LLM generated name (HIGH), was func_8088E90C: places the two JntSph collider spheres in world space along the platform's facing each frame.
+void BgHidanSima_UpdateCollider(BgHidanSima* this);  // was func_8088E90C [LLM:HIGH]
 
 const ActorInit Bg_Hidan_Sima_InitVars = {
     ACTOR_BG_HIDAN_SIMA,
@@ -198,7 +199,7 @@ void BgHidanSima_Slide(BgHidanSima* this, PlayState* play) {
     Actor_PlaySfx_Flagged(&this->dyna.actor, NA_SE_EV_FIRE_PILLAR - SFX_FLAG);
 }
 
-void func_8088E90C(BgHidanSima* this) {
+void BgHidanSima_UpdateCollider(BgHidanSima* this) {
     ColliderJntSphElement* elem;
     s32 i;
     f32 cos = Math_CosS(this->dyna.actor.world.rot.y + 0x8000);
@@ -225,7 +226,7 @@ void BgHidanSima_Update(Actor* thisx, PlayState* play) {
         }
         this->dyna.actor.world.pos.y = this->dyna.actor.home.pos.y - ((1.0f - cosf(temp * (M_PI / 20))) * 5.0f);
         if (this->actionFunc == BgHidanSima_Slide) {  // was func_8088E7A8 [LLM:HIGH]
-            func_8088E90C(this);
+            BgHidanSima_UpdateCollider(this);  // was func_8088E90C [LLM:HIGH]
             CollisionCheck_SetAT(play, &play->colChkCtx, &this->collider.base);
         }
     }
-- 
2.55.0

