From 977e2c5470aba8e03cbb7c266d32996ef8e1d55f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:14:11 +0000
Subject: [PATCH 2142/3799] soh: rename func_80967AD0 -> Demo6K_UpdateStreaks

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the params 0 update: adds a streak per frame while cue 1 is on action 2 and respawns each as it reaches the floor
Defined in soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c b/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
index 1eab0bcb5..5c98c3066 100644
--- a/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
+++ b/soh/src/overlays/actors/ovl_Demo_6K/z_demo_6k.c
@@ -39,7 +39,8 @@ void Demo6K_FlyToTarget(Demo6K* this, PlayState* play);  // was func_809674E0 [L
 void Demo6K_RiseAndFade(Demo6K* this, PlayState* play);  // was func_8096784C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80967A04: seeds one of the sixteen falling streaks with its x, height, fall rate, color index and scale
 void Demo6K_InitStreak(Demo6K* this, s32 i);  // was func_80967A04 [LLM:HIGH]
-void func_80967AD0(Demo6K* this, PlayState* play);
+// LLM generated name (HIGH), was func_80967AD0: the params 0 update: adds a streak per frame while cue 1 is on action 2 and respawns each as it reaches the floor
+void Demo6K_UpdateStreaks(Demo6K* this, PlayState* play);  // was func_80967AD0 [LLM:HIGH]
 void func_80967DBC(Demo6K* this, PlayState* play);
 void func_80967F10(Demo6K* this, PlayState* play);
 void func_80967FFC(Actor* thisx, PlayState* play);
@@ -110,7 +111,7 @@ void Demo6K_Init(Actor* thisx, PlayState* play) {
     switch (params) {
         case 0:
             this->drawFunc = func_809688C4;
-            this->initActionFunc = func_80967AD0;
+            this->initActionFunc = Demo6K_UpdateStreaks;  // was func_80967AD0 [LLM:HIGH]
             Actor_SetScale(&this->actor, 1.0f);
 
             for (i = 0; i < 16; i++) {
@@ -480,7 +481,7 @@ void Demo6K_InitStreak(Demo6K* this, s32 i) {
     this->unk_234[i] = Rand_ZeroFloat(0.02f) + 0.01f;
 }
 
-void func_80967AD0(Demo6K* this, PlayState* play) {
+void Demo6K_UpdateStreaks(Demo6K* this, PlayState* play) {
     s32 i;
 
     if ((play->csCtx.state != CS_STATE_IDLE) && (play->csCtx.npcActions[1] != NULL)) {
-- 
2.55.0

