From 63e3f001bce3a54788a8eaea416e5af5c9174666 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:37:35 +0000
Subject: [PATCH 1695/3799] soh: rename func_80045508 ->
 Camera_CheckEyeAtCorner

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: runs the background check both ways between the at and eye points and classifies the result, reporting whether the two surfaces meet at a corner the swing logic has to peek around.
Defined in soh/src/code/z_camera.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/z_camera.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/code/z_camera.c b/soh/src/code/z_camera.c
index ed0e2736f..5729e4170 100644
--- a/soh/src/code/z_camera.c
+++ b/soh/src/code/z_camera.c
@@ -820,7 +820,8 @@ Vec3f* Camera_BGCheckCorner(Vec3f* dst, Vec3f* linePointA, Vec3f* linePointB, Ca
  * 3 ?
  * 6 if the angle between the polys is greater than 120 degrees
  */
-s32 func_80045508(Camera* camera, VecSph* diffSph, CamColChk* eyeChk, CamColChk* atChk, s16 checkEye) {
+// LLM generated name (HIGH), was func_80045508: runs the background check both ways between the at and eye points and classifies the result, reporting whether the two surfaces meet at a corner the swing logic has to peek around.
+s32 Camera_CheckEyeAtCorner(Camera* camera, VecSph* diffSph, CamColChk* eyeChk, CamColChk* atChk, s16 checkEye) {
     Vec3f* at = &camera->at;
     Vec3f* eye = &camera->eye;
     Vec3f* eyeNext = &camera->eyeNext;
@@ -1335,7 +1336,7 @@ void func_80046E20(Camera* camera, VecSph* eyeAdjustment, f32 minDist, f32 arg3,
     VecSph newEyeAdjustment;
     VecSph sp40;
 
-    temp_v0 = func_80045508(camera, eyeAdjustment, &atEyeColChk, &eyeAtColChk, !anim->unk_18);
+    temp_v0 = Camera_CheckEyeAtCorner(camera, eyeAdjustment, &atEyeColChk, &eyeAtColChk, !anim->unk_18);  // was func_80045508 [LLM:HIGH]
 
     switch (temp_v0) {
         case 1:
-- 
2.55.0

