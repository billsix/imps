From 13218ee0c3fe5e990d10376ea3be1bff986af727 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:35:17 +0000
Subject: [PATCH 0999/3799] soh: rename func_809937B4 ->
 DemoTreLgt_StartAnimation

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: switches to DEMO_TRE_LGT_ACTION_ANIMATE and loads the age-appropriate SkelCurve animation from the chest's current frame.
Defined in soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c b/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
index 23048cbb2..9618c0b48 100644
--- a/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
+++ b/soh/src/overlays/actors/ovl_Demo_Tre_Lgt/z_demo_tre_lgt.c
@@ -15,7 +15,8 @@ void func_80993848(DemoTreLgt* this, PlayState* play);
 void DemoTreLgt_SetupWait(DemoTreLgt* this);  // was func_80993754 [LLM:HIGH]
 // LLM generated name (HIGH), was func_8099375C: sActionFuncs[DEMO_TRE_LGT_ACTION_WAIT]: watches the parent chest's skelanime and starts the light animation on frame 10.
 void DemoTreLgt_Wait(DemoTreLgt* this, PlayState* play);  // was func_8099375C [LLM:HIGH]
-void func_809937B4(DemoTreLgt* this, PlayState* play, f32 currentFrame);
+// LLM generated name (HIGH), was func_809937B4: switches to DEMO_TRE_LGT_ACTION_ANIMATE and loads the age-appropriate SkelCurve animation from the chest's current frame.
+void DemoTreLgt_StartAnimation(DemoTreLgt* this, PlayState* play, f32 currentFrame);  // was func_809937B4 [LLM:HIGH]
 
 typedef struct {
     /* 0x00 */ f32 startFrame;
@@ -79,11 +80,11 @@ void DemoTreLgt_Wait(DemoTreLgt* this, PlayState* play) {
     EnBox* treasureChest = (EnBox*)this->actor.parent;
 
     if (treasureChest != NULL && Animation_OnFrame(&treasureChest->skelanime, 10.0f)) {
-        func_809937B4(this, play, treasureChest->skelanime.curFrame);
+        DemoTreLgt_StartAnimation(this, play, treasureChest->skelanime.curFrame);  // was func_809937B4 [LLM:HIGH]
     }
 }
 
-void func_809937B4(DemoTreLgt* this, PlayState* play, f32 currentFrame) {
+void DemoTreLgt_StartAnimation(DemoTreLgt* this, PlayState* play, f32 currentFrame) {
     SkelAnimeCurve* skelCurve = &this->skelCurve;
     s32 pad[2];
 
-- 
2.55.0

