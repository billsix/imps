From d586246b58bc5cf2ce47461bff96ada379c6f5c1 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:35 +0000
Subject: [PATCH 418/488] soh: name the 5 address-named symbols defined in
 z_en_partner.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 5 are defined in soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c; updating their references touches 2 files in total.

  * D_80854A40 -> sBombSpawnOffset
    deduced, confidence HIGH: the offset in front of the partner a bomb is
    placed at

  * D_808547A4 -> sBombchuSparkVelocity
    deduced, confidence HIGH: the velocity of the sparks the bombchu trail
    spawns

  * D_808547B0 -> sBombchuSparkAccel
    deduced, confidence HIGH: the acceleration those sparks are given

  * D_808547BC -> sBombchuSparkPrimColor
    deduced, confidence HIGH: the prim colour of those sparks

  * D_808547C0 -> sBombchuSparkEnvColor
    deduced, confidence HIGH: the env colour of those sparks

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../actors/ovl_En_Partner/z_en_partner.c        | 17 +++++++++++------
 .../overlays/actors/ovl_player_actor/z_player.c | 14 +++++++-------
 2 files changed, 18 insertions(+), 13 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c b/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
index 20e52b3c5..7f6be9026 100644
--- a/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
+++ b/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
@@ -303,7 +303,8 @@ void UseBombs(Actor* thisx, PlayState* play, u8 started) {
     }
 }
 
-static Vec3f D_80854A40 = { 0.0f, 40.0f, 45.0f };
+// LLM generated name (HIGH), was D_80854A40: the offset in front of the partner a bomb is placed at
+static Vec3f sBombSpawnOffset = { 0.0f, 40.0f, 45.0f };  // was D_80854A40 [LLM:HIGH]
 
 void UseHammer(Actor* thisx, PlayState* play, u8 started) {
     EnPartner* this = (EnPartner*)thisx;
@@ -353,11 +354,15 @@ void UseBombchus(Actor* thisx, PlayState* play, u8 started) {
     }
 }
 
-static Vec3f D_808547A4 = { 0.0f, 0.5f, 0.0f };
-static Vec3f D_808547B0 = { 0.0f, 0.5f, 0.0f };
+// LLM generated name (HIGH), was D_808547A4: the velocity of the sparks the bombchu trail spawns
+static Vec3f sBombchuSparkVelocity = { 0.0f, 0.5f, 0.0f };  // was D_808547A4 [LLM:HIGH]
+// LLM generated name (HIGH), was D_808547B0: the acceleration those sparks are given
+static Vec3f sBombchuSparkAccel = { 0.0f, 0.5f, 0.0f };  // was D_808547B0 [LLM:HIGH]
 
-static Color_RGBA8 D_808547BC = { 255, 255, 100, 255 };
-static Color_RGBA8 D_808547C0 = { 255, 50, 0, 0 };
+// LLM generated name (HIGH), was D_808547BC: the prim colour of those sparks
+static Color_RGBA8 sBombchuSparkPrimColor = { 255, 255, 100, 255 };  // was D_808547BC [LLM:HIGH]
+// LLM generated name (HIGH), was D_808547C0: the env colour of those sparks
+static Color_RGBA8 sBombchuSparkEnvColor = { 255, 50, 0, 0 };  // was D_808547C0 [LLM:HIGH]
 
 void UseDekuStick(Actor* thisx, PlayState* play, u8 started) {
     EnPartner* this = (EnPartner*)thisx;
@@ -376,7 +381,7 @@ void UseDekuStick(Actor* thisx, PlayState* play, u8 started) {
                 this->stickWeaponInfo.tip = this->actor.world.pos;
                 this->stickWeaponInfo.tip.y += 7.0f;
 
-                EffectSsDust_SpawnDrawFlags0Life(play, &this->stickWeaponInfo.tip, &D_808547A4, &D_808547B0, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
+                EffectSsDust_SpawnDrawFlags0Life(play, &this->stickWeaponInfo.tip, &sBombchuSparkVelocity, &sBombchuSparkAccel, &sBombchuSparkPrimColor, &sBombchuSparkEnvColor,  // was func_8002836C [LLM:HIGH]
                               200.0f, 0, 8);
 
                 CollisionCheck_SetAT(play, &play->colChkCtx, &this->collider.base);
diff --git a/soh/src/overlays/actors/ovl_player_actor/z_player.c b/soh/src/overlays/actors/ovl_player_actor/z_player.c
index 0a641a4f4..455d081ca 100644
--- a/soh/src/overlays/actors/ovl_player_actor/z_player.c
+++ b/soh/src/overlays/actors/ovl_player_actor/z_player.c
@@ -11528,11 +11528,11 @@ void Player_UpdateCamAndSeqModes(PlayState* play, Player* this) {
     }
 }
 
-static Vec3f D_808547A4 = { 0.0f, 0.5f, 0.0f };
-static Vec3f D_808547B0 = { 0.0f, 0.5f, 0.0f };
+static Vec3f sBombchuSparkVelocity = { 0.0f, 0.5f, 0.0f };  // was D_808547A4 [LLM:HIGH]
+static Vec3f sBombchuSparkAccel = { 0.0f, 0.5f, 0.0f };  // was D_808547B0 [LLM:HIGH]
 
-static Color_RGBA8 D_808547BC = { 255, 255, 100, 255 };
-static Color_RGBA8 D_808547C0 = { 255, 50, 0, 0 };
+static Color_RGBA8 sBombchuSparkPrimColor = { 255, 255, 100, 255 };  // was D_808547BC [LLM:HIGH]
+static Color_RGBA8 sBombchuSparkEnvColor = { 255, 50, 0, 0 };  // was D_808547C0 [LLM:HIGH]
 
 void Player_UpdateBurningDekuStick(PlayState* play, Player* this) {
     f32 temp;
@@ -11556,7 +11556,7 @@ void Player_UpdateBurningDekuStick(PlayState* play, Player* this) {
         this->unk_85C = temp;
     }
 
-    EffectSsDust_SpawnDrawFlags0Life(play, &this->meleeWeaponInfo[0].tip, &D_808547A4, &D_808547B0, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
+    EffectSsDust_SpawnDrawFlags0Life(play, &this->meleeWeaponInfo[0].tip, &sBombchuSparkVelocity, &sBombchuSparkAccel, &sBombchuSparkPrimColor, &sBombchuSparkEnvColor,  // was func_8002836C [LLM:HIGH]
                   temp * 200.0f, 0, 8);
 }
 
@@ -15035,7 +15035,7 @@ s32 Player_ActionHandler_7(Player* this, PlayState* play) {
     return 1;
 }
 
-static Vec3f D_80854A40 = { 0.0f, 40.0f, 45.0f };
+static Vec3f sBombSpawnOffset = { 0.0f, 40.0f, 45.0f };  // was D_80854A40 [LLM:HIGH]
 
 void Player_Action_808502D0(Player* this, PlayState* play) {
     struct_80854190* sp44 = &D_80854190[this->meleeWeaponAnimation];
@@ -15089,7 +15089,7 @@ void Player_Action_808502D0(Player* this, PlayState* play) {
                 Vec3f shockwavePos;
                 f32 sp2C;
 
-                shockwavePos.y = func_8083973C(play, this, &D_80854A40, &shockwavePos);
+                shockwavePos.y = func_8083973C(play, this, &sBombSpawnOffset, &shockwavePos);  // was D_80854A40 [LLM:HIGH]
                 sp2C = this->actor.world.pos.y - shockwavePos.y;
 
                 Math_ScaledStepToS(&this->actor.focus.rot.x, Math_Atan2S(45.0f, sp2C), 800);
-- 
2.55.0

