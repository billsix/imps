From adc05060c5e84d870471e171d3a00f908fb390e6 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:27:41 +0000
Subject: [PATCH 1568/3799] soh: rename func_80A3F060 -> EnGo_TrackPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: feeds the player's position to Npc_TrackPoint at the per-variant Y offset, disabling tracking entirely for the states that must not turn their head.
Defined in soh/src/overlays/actors/ovl_En_Go/z_en_go.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Go/z_en_go.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
index e762a4bb9..73e163ff7 100644
--- a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
+++ b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
@@ -398,7 +398,8 @@ f32 EnGo_GetPlayerTrackingYOffset(EnGo* this) {
     }
 }
 
-void func_80A3F060(EnGo* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80A3F060: feeds the player's position to Npc_TrackPoint at the per-variant Y offset, disabling tracking entirely for the states that must not turn their head.
+void EnGo_TrackPlayer(EnGo* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s16 trackingMode;
 
@@ -1044,7 +1045,7 @@ void EnGo_Update(Actor* thisx, PlayState* play) {
     func_80A3F0E4(this);
     func_80A3F908(this, play);
     this->actionFunc(this, play);
-    func_80A3F060(this, play);
+    EnGo_TrackPlayer(this, play);  // was func_80A3F060 [LLM:HIGH]
 }
 
 void EnGo_DrawCurledUp(EnGo* this, PlayState* play) {
-- 
2.55.0

