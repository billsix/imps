From f1728cdc064b5cc3cd3d4484ec53c123bef904be Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:39:39 +0000
Subject: [PATCH 1063/3799] soh: rename func_808C17C8 ->
 BossDodongo_SpawnEffect

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: scans the specialEffects array for a free BossDodongoEffect slot and fills in its position, velocity, acceleration, scale and alpha.
Defined in soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
index 10b020716..a440ceaa8 100644
--- a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
@@ -269,7 +269,8 @@ void BossDodongo_UpdateLavaTexture(void* arg0, void* floorTex, s32 arg2, f32 arg
     Gfx_TextureCacheDelete(sLavaWavyTex);
 }
 
-void func_808C17C8(PlayState* play, Vec3f* arg1, Vec3f* arg2, Vec3f* arg3, f32 arg4, s16 arg5) {
+// LLM generated name (HIGH), was func_808C17C8: scans the specialEffects array for a free BossDodongoEffect slot and fills in its position, velocity, acceleration, scale and alpha.
+void BossDodongo_SpawnEffect(PlayState* play, Vec3f* arg1, Vec3f* arg2, Vec3f* arg3, f32 arg4, s16 arg5) {
     s16 i;
     BossDodongoEffect* eff = (BossDodongoEffect*)play->specialEffects;
 
@@ -1173,7 +1174,7 @@ void BossDodongo_Update(Actor* thisx, PlayState* play2) {
                 sp54.x = sinf(sp4C) * sp50 + (-890.0f);
                 sp54.y = -1513.76f;
                 sp54.z = cosf(sp4C) * sp50 + (-3304.0f);
-                func_808C17C8(play, &sp54, &sp6C, &sp60, ((s16)Rand_ZeroFloat(2.0f)) + 6, 0x50);
+                BossDodongo_SpawnEffect(play, &sp54, &sp6C, &sp60, ((s16)Rand_ZeroFloat(2.0f)) + 6, 0x50);  // was func_808C17C8 [LLM:HIGH]
             }
         }
 
-- 
2.55.0

