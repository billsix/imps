From a956d45cfdb3f0210ed19cdd97f2298b28f11c9e Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:33:16 +0000
Subject: [PATCH 0972/3799] soh: rename func_808783D4 -> BgGateShutter_Close

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the mirror of the open slide: moves along +x to 91 and then arms the same settle timer.
Defined in soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c         | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c b/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
index f0a536e7d..a50477f43 100644
--- a/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
+++ b/soh/src/overlays/actors/ovl_Bg_Gate_Shutter/z_bg_gate_shutter.c
@@ -22,7 +22,8 @@ void BgGateShutter_WaitToMove(BgGateShutter* this, PlayState* play);  // was fun
 void BgGateShutter_Open(BgGateShutter* this, PlayState* play);  // was func_80878300 [LLM:HIGH]
 // LLM generated name (HIGH), was func_808783AC: waits out the 30-frame timer set by the two slide actions, clears openingState and returns to the idle action.
 void BgGateShutter_Settle(BgGateShutter* this, PlayState* play);  // was func_808783AC [LLM:HIGH]
-void func_808783D4(BgGateShutter* this, PlayState* play);
+// LLM generated name (HIGH), was func_808783D4: the mirror of the open slide: moves along +x to 91 and then arms the same settle timer.
+void BgGateShutter_Close(BgGateShutter* this, PlayState* play);  // was func_808783D4 [LLM:HIGH]
 
 const ActorInit Bg_Gate_Shutter_InitVars = {
     ACTOR_BG_GATE_SHUTTER,
@@ -77,7 +78,7 @@ void BgGateShutter_WaitToMove(BgGateShutter* this, PlayState* play) {
         this->actionFunc = BgGateShutter_Open;  // was func_80878300 [LLM:HIGH]
     } else if (this->openingState < 0) {
         this->unk_178 = 2;
-        this->actionFunc = func_808783D4;
+        this->actionFunc = BgGateShutter_Close;  // was func_808783D4 [LLM:HIGH]
     }
 }
 
@@ -103,7 +104,7 @@ void BgGateShutter_Settle(BgGateShutter* this, PlayState* play) {
     }
 }
 
-void func_808783D4(BgGateShutter* this, PlayState* play) {
+void BgGateShutter_Close(BgGateShutter* this, PlayState* play) {
     Actor* thisx = &this->dyna.actor;
 
     if (this->unk_178 == 0) {
-- 
2.55.0

