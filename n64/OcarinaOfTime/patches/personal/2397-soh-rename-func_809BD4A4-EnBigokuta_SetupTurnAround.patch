From 47e9d9ae7b583e2b1bb4c03b01294f9736c69f1e Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:25:26 +0000
Subject: [PATCH 2397/3799] soh: rename func_809BD4A4 ->
 EnBigokuta_SetupTurnAround

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: records the opposite heading in world.rot.x and morphs to the idle loop while it turns
Defined in soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c   | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
index d136c9a1d..02a88fcb9 100644
--- a/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
+++ b/soh/src/overlays/actors/ovl_En_Bigokuta/z_en_bigokuta.c
@@ -332,7 +332,8 @@ void EnBigokuta_SetupBounce(EnBigokuta* this) {
     this->actionFunc = func_809BDF34;
 }
 
-void func_809BD4A4(EnBigokuta* this) {
+// LLM generated name (HIGH), was func_809BD4A4: records the opposite heading in world.rot.x and morphs to the idle loop while it turns
+void EnBigokuta_SetupTurnAround(EnBigokuta* this) {
     Animation_MorphToLoop(&this->skelAnime, &object_bigokuta_Anim_0014B8, -5.0f);
     this->unk_195 = true;
     this->actor.world.rot.x = this->actor.shape.rot.y + 0x8000;
@@ -543,7 +544,7 @@ void func_809BDC08(EnBigokuta* this, PlayState* play) {
     if (this->unk_198 == 0) {
         func_809BD768(this);
     } else if (this->unk_196 == 0) {
-        func_809BD4A4(this);
+        EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
     } else if (this->unk_195) {
         phi_v0 = this->actor.yawTowardsPlayer - this->actor.shape.rot.y;
         if (phi_v0 < 0) {
@@ -552,7 +553,7 @@ void func_809BDC08(EnBigokuta* this, PlayState* play) {
         if (phi_v0 < 0x4100 && phi_v0 > 0x3F00) {
             if (Rand_ZeroOne() < 0.6f) {
                 this->unk_196 = 0;
-                func_809BD4A4(this);
+                EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
             }
             this->unk_195 = false;
         }
@@ -618,7 +619,7 @@ void func_809BE180(EnBigokuta* this, PlayState* play) {
     }
     if (this->unk_196 == 0) {
         if (this->actor.colChkInfo.health != 0) {
-            func_809BD4A4(this);
+            EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
         } else {
             func_809BD658(this);
         }
@@ -749,7 +750,7 @@ void func_809BE798(EnBigokuta* this, PlayState* play) {
         }
         Actor_SetPlayerKnockbackLargeNoDamage(play, &this->actor, 10.0f, this->actor.world.rot.y + effectRot, 5.0f);
         if (this->actionFunc == func_809BDC08) {
-            func_809BD4A4(this);
+            EnBigokuta_SetupTurnAround(this);  // was func_809BD4A4 [LLM:HIGH]
             this->unk_196 = 40;
         } else if (this->actionFunc == func_809BE3E4) {
             if ((effectRot * this->unk_194) > 0) {
-- 
2.55.0

