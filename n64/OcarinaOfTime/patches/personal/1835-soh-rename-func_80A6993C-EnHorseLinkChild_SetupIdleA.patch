From dc7d1759498cbc7f8cbed8ce75771bf0eb9278a5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:48 +0000
Subject: [PATCH 1835/3799] soh: rename func_80A6993C ->
 EnHorseLinkChild_SetupIdleAnim

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters action 2 with one of the two standing animations, stopping the foal.
Defined in soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
index ea2c13498..a4ee15acd 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
@@ -211,7 +211,8 @@ void EnHorseLinkChild_CycleAnim(EnHorseLinkChild* this, PlayState* play) {
     }
 }
 
-void func_80A6993C(EnHorseLinkChild* this, s32 newAnimationIdx) {
+// LLM generated name (HIGH), was func_80A6993C: enters action 2 with one of the two standing animations, stopping the foal.
+void EnHorseLinkChild_SetupIdleAnim(EnHorseLinkChild* this, s32 newAnimationIdx) {
     this->action = 2;
     this->actor.speedXZ = 0.0f;
 
@@ -280,7 +281,7 @@ void func_80A69C18(EnHorseLinkChild* this, PlayState* play) {
         distFromLink = Actor_WorldDistXZToActor(&this->actor, &GET_PLAYER(play)->actor);
 
         if (distFromLink > 1000.0f) {
-            func_80A6993C(this, 0);
+            EnHorseLinkChild_SetupIdleAnim(this, 0);  // was func_80A6993C [LLM:HIGH]
             return;
         } else if ((distFromLink < 1000.0f) && (distFromLink >= 300.0f)) {
             newAnimationIdx = 4;
@@ -293,7 +294,7 @@ void func_80A69C18(EnHorseLinkChild* this, PlayState* play) {
             this->actor.speedXZ = 2.0f;
             this->unk_1F0 = 0;
         } else {
-            func_80A6993C(this, 1);
+            EnHorseLinkChild_SetupIdleAnim(this, 1);  // was func_80A6993C [LLM:HIGH]
             return;
         }
 
-- 
2.55.0

