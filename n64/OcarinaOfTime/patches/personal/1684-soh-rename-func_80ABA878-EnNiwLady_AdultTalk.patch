From 8faef283b2253994b3db104da1397c1b0fe4b644 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:36:45 +0000
Subject: [PATCH 1684/3799] soh: rename func_80ABA878 -> EnNiwLady_AdultTalk

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: offers talk in the adult era and routes the accepted conversation into whichever trade prompt the current line calls for.
Defined in soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
index 314f1876a..31eb83347 100644
--- a/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
+++ b/soh/src/overlays/actors/ovl_En_Niw_Lady/z_en_niw_lady.c
@@ -19,7 +19,8 @@ void EnNiwLady_SetupCuccoCount(EnNiwLady* this, PlayState* play);  // was func_8
 void func_80ABAD38(EnNiwLady* this, PlayState* play);
 // LLM generated name (HIGH), was func_80ABA778: picks the adult-era line from how far the Pocket Egg and Cojiro trade has progressed and installs the adult talk action.
 void EnNiwLady_SetupAdultTalk(EnNiwLady* this, PlayState* play);  // was func_80ABA778 [LLM:HIGH]
-void func_80ABA878(EnNiwLady* this, PlayState* play);
+// LLM generated name (HIGH), was func_80ABA878: offers talk in the adult era and routes the accepted conversation into whichever trade prompt the current line calls for.
+void EnNiwLady_AdultTalk(EnNiwLady* this, PlayState* play);  // was func_80ABA878 [LLM:HIGH]
 void func_80ABA9B8(EnNiwLady* this, PlayState* play);
 void func_80ABAB08(EnNiwLady* this, PlayState* play);
 void func_80ABAC00(EnNiwLady* this, PlayState* play);
@@ -367,10 +368,10 @@ void EnNiwLady_SetupAdultTalk(EnNiwLady* this, PlayState* play) {
         }
     }
     this->actor.textId = sTradeItemTextIds[this->unk_27A];
-    this->actionFunc = func_80ABA878;
+    this->actionFunc = EnNiwLady_AdultTalk;  // was func_80ABA878 [LLM:HIGH]
 }
 
-void func_80ABA878(EnNiwLady* this, PlayState* play) {
+void EnNiwLady_AdultTalk(EnNiwLady* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
     s8 playerExchangeItemId;
 
-- 
2.55.0

