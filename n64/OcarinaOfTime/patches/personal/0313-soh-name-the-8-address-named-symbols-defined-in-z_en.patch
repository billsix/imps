From a960bba22aed6893dd2f597f016e1cf7d0389a3b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:25 +0000
Subject: [PATCH 313/488] soh: name the 8 address-named symbols defined in
 z_en_skj.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 8 are defined in soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c.

  * func_80AFEDF8 -> EnSkj_IsPlayerEngaging
    deduced, confidence HIGH: returns 1 when the player is within the
    actor's current engage distance and using an item, or is standing inside
    its facing cone; the fight states use it to decide whether to keep
    going.

  * func_80AFF1F0 -> EnSkj_SetupPickNextFightAction
    deduced, confidence HIGH: plays the hit animation and installs
    SKJ_ACTION_PICK_NEXT_FIHGT_ACTION, matching the file's other
    EnSkj_Setup* wrappers.

  * func_80AFF2A0 -> EnSkj_SetupWaitForLandAnim
    deduced, confidence HIGH: recentres the actor, stops it and plays the
    landing animation before installing SKJ_ACTION_WAIT_FOR_LAND_ANIM.

  * func_80AFFE24 -> EnSkj_SetupOfferHeartPiece
    deduced, confidence HIGH: installs SKJ_ACTION_UNK14, the state that
    hands the player the Saria's Song heart piece.

  * func_80AFFE44 -> EnSkj_OfferHeartPiece
    deduced, confidence HIGH: re-offers GI_HEART_PIECE until the player
    takes it, then moves on to the post-song state.

  * D_80B01678 -> sCylinderInit
    deduced, confidence HIGH: the ColliderCylinderInitType1 passed to
    Collider_SetCylinderType1 in Init.

  * D_80B01EA0 -> sTalkRequested
    deduced, confidence HIGH: the in-tree comment on its declaration reads
    "gets set if ACTOR_FLAG_TALK is set"; three states assign
    Actor_ProcessTalkRequest's result to it and the rest read it to see
    whether the player started a conversation this frame.

  * func_80AFF334 -> EnSkj_SetupLookAround
    deduced, confidence HIGH: widens the engage cone and distance, arms the
    400-frame battle exit timer and plays the look-left-right animation
    before installing SKJ_ACTION_RESET_FIGHT. Not named for that action
    because EnSkj_SetupResetFight already exists in this file and installs
    SKJ_ACTION_SARIA_SONG_IDLE instead.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c | 72 ++++++++++---------
 1 file changed, 40 insertions(+), 32 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c b/soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c
index b53c9f975..1fe53db1e 100644
--- a/soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c
+++ b/soh/src/overlays/actors/ovl_En_Skj/z_en_skj.c
@@ -16,8 +16,10 @@ void EnSkj_Draw(Actor* thisx, PlayState* play);
 void EnSkj_SariasSongShortStumpUpdate(Actor* thisx, PlayState* play);
 void EnSkj_OcarinaMinigameShortStumpUpdate(Actor* thisx, PlayState* play);
 
-void func_80AFF2A0(EnSkj* this);
-void func_80AFF334(EnSkj* this);
+// LLM generated name (HIGH), was func_80AFF2A0: recentres the actor, stops it and plays the landing animation before installing SKJ_ACTION_WAIT_FOR_LAND_ANIM.
+void EnSkj_SetupWaitForLandAnim(EnSkj* this);  // was func_80AFF2A0 [LLM:HIGH]
+// LLM generated name (HIGH), was func_80AFF334: widens the engage cone and distance, arms the 400-frame battle exit timer and plays the look-left-right animation before installing SKJ_ACTION_RESET_FIGHT. Not named for that action because EnSkj_SetupResetFight already exists in this file and installs SKJ_ACTION_SARIA_SONG_IDLE instead.
+void EnSkj_SetupLookAround(EnSkj* this);  // was func_80AFF334 [LLM:HIGH]
 
 void EnSkj_CalculateCenter(EnSkj* this);
 void EnSkj_OcarinaGameSetupWaitForPlayer(EnSkj* this);
@@ -33,7 +35,8 @@ void EnSkj_SetupTalk(EnSkj* this);
 void EnSkj_SetupMaskTrade(EnSkj* this);
 void EnSkj_SetupWrongSong(EnSkj* this);
 void EnSkj_SetupAfterSong(EnSkj* this);
-void func_80AFFE24(EnSkj* this);
+// LLM generated name (HIGH), was func_80AFFE24: installs SKJ_ACTION_UNK14, the state that hands the player the Saria's Song heart piece.
+void EnSkj_SetupOfferHeartPiece(EnSkj* this);  // was func_80AFFE24 [LLM:HIGH]
 void EnSkj_SetupPostSariasSong(EnSkj* this);
 void EnSkj_JumpFromStump(EnSkj* this);
 void EnSkj_SetupWaitForLandAnimFinish(EnSkj* this);
@@ -41,7 +44,8 @@ void EnSkj_SetupWalkToPlayer(EnSkj* this);
 void EnSkj_SetupWaitForMaskTextClear(EnSkj* this);
 void EnSkj_SetupWaitForTextClear(EnSkj* this);
 void EnSkj_SetupDie(EnSkj* this);
-void func_80AFF1F0(EnSkj* this);
+// LLM generated name (HIGH), was func_80AFF1F0: plays the hit animation and installs SKJ_ACTION_PICK_NEXT_FIHGT_ACTION, matching the file's other EnSkj_Setup* wrappers.
+void EnSkj_SetupPickNextFightAction(EnSkj* this);  // was func_80AFF1F0 [LLM:HIGH]
 void EnSkj_OfferNextRound(EnSkj* this, PlayState* play);
 void EnSkj_SetupAskForMask(EnSkj* this, PlayState* play);
 f32 EnSkj_GetItemXzRange(EnSkj* this);
@@ -77,7 +81,8 @@ void EnSkj_WaitInRange(EnSkj* this, PlayState* play);
 void EnSkj_WaitForSong(EnSkj* this, PlayState* play);
 void EnSkj_AfterSong(EnSkj* this, PlayState* play);
 void EnSkj_SariaSongTalk(EnSkj* this, PlayState* play);
-void func_80AFFE44(EnSkj* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AFFE44: re-offers GI_HEART_PIECE until the player takes it, then moves on to the post-song state.
+void EnSkj_OfferHeartPiece(EnSkj* this, PlayState* play);  // was func_80AFFE44 [LLM:HIGH]
 void EnSkj_ChangeModeAfterSong(EnSkj* this, PlayState* play);
 void EnSkj_StartMaskTrade(EnSkj* this, PlayState* play);
 void EnSkj_WaitForLanding(EnSkj* this, PlayState* play);
@@ -177,7 +182,8 @@ const ActorInit En_Skj_InitVars = {
     NULL,
 };
 
-static ColliderCylinderInitType1 D_80B01678 = {
+// LLM generated name (HIGH), was D_80B01678: the ColliderCylinderInitType1 passed to Collider_SetCylinderType1 in Init.
+static ColliderCylinderInitType1 sCylinderInit = {  // was D_80B01678 [LLM:HIGH]
     {
         COLTYPE_NONE,
         AT_ON | AT_TYPE_ENEMY,
@@ -266,7 +272,7 @@ static EnSkjActionFunc sActionFuncs[] = {
     EnSkj_WaitForSong,
     EnSkj_AfterSong,
     EnSkj_SariaSongTalk,
-    func_80AFFE44,
+    EnSkj_OfferHeartPiece,  // was func_80AFFE44 [LLM:HIGH]
     EnSkj_ChangeModeAfterSong,
     EnSkj_StartMaskTrade,
     EnSkj_WaitForLanding,
@@ -288,7 +294,8 @@ static InitChainEntry sInitChain[] = {
     ICHAIN_F32(targetArrowOffset, 30, ICHAIN_STOP),
 };
 
-static s32 D_80B01EA0; // gets set if ACTOR_FLAG_TALK is set
+// LLM generated name (HIGH), was D_80B01EA0: the in-tree comment on its declaration reads "gets set if ACTOR_FLAG_TALK is set"; three states assign Actor_ProcessTalkRequest's result to it and the rest read it to see whether the player started a conversation this frame.
+static s32 sTalkRequested; // gets set if ACTOR_FLAG_TALK is set, was D_80B01EA0 [LLM:HIGH]
 
 void EnSkj_ChangeAnim(EnSkj* this, u8 index) {
     f32 endFrame = Animation_GetLastFrame(sAnimationInfo[index].animation);
@@ -439,7 +446,7 @@ void EnSkj_Init(Actor* thisx, PlayState* play2) {
             this->actor.colChkInfo.damageTable = &sDamageTable;
             this->actor.colChkInfo.health = 10;
             Collider_InitCylinder(play, &this->collider);
-            Collider_SetCylinderType1(play, &this->collider, &this->actor, &D_80B01678);
+            Collider_SetCylinderType1(play, &this->collider, &this->actor, &sCylinderInit);  // was D_80B01678 [LLM:HIGH]
             ActorShape_Init(&this->actor.shape, 0.0f, ActorShadow_DrawCircle, 40.0f);
             Actor_SetScale(thisx, 0.01f);
             this->actor.textId = this->textId = 0;
@@ -582,7 +589,7 @@ s32 EnSkj_CollisionCheck(EnSkj* this, PlayState* play) {
     s16 yawDiff;
     Vec3f effectPos;
 
-    if (!((this->unk_2D3 == 0) || (D_80B01EA0 != 0) || !(this->collider.base.acFlags & AC_HIT))) {
+    if (!((this->unk_2D3 == 0) || (sTalkRequested != 0) || !(this->collider.base.acFlags & AC_HIT))) {  // was D_80B01EA0 [LLM:HIGH]
         this->collider.base.acFlags &= ~AC_HIT;
         if (this->actor.colChkInfo.damageEffect != 0) {
             if (this->actor.colChkInfo.damageEffect == 0xF) {
@@ -612,7 +619,7 @@ s32 EnSkj_CollisionCheck(EnSkj* this, PlayState* play) {
                     if (this->dodgeResetTimer == 0) {
                         this->dodgeResetTimer = 60;
                     }
-                    func_80AFF1F0(this);
+                    EnSkj_SetupPickNextFightAction(this);  // was func_80AFF1F0 [LLM:HIGH]
                     return 1;
                 }
                 EnSkj_SetupDie(this);
@@ -627,7 +634,8 @@ s32 EnSkj_CollisionCheck(EnSkj* this, PlayState* play) {
     return 0;
 }
 
-s32 func_80AFEDF8(EnSkj* this, PlayState* play) {
+// LLM generated name (HIGH), was func_80AFEDF8: returns 1 when the player is within the actor's current engage distance and using an item, or is standing inside its facing cone; the fight states use it to decide whether to keep going.
+s32 EnSkj_IsPlayerEngaging(EnSkj* this, PlayState* play) {
     s16 yawDiff;
 
     if (this->actor.xzDistToPlayer < this->unk_2EC) {
@@ -676,7 +684,7 @@ void EnSkj_Fade(EnSkj* this, PlayState* play) {
     if (this->actor.velocity.y <= 0.0f) {
         if (this->actor.bgCheckFlags & BGCHECKFLAG_GROUND_TOUCH) {
             this->actor.bgCheckFlags &= ~2;
-            func_80AFF2A0(this);
+            EnSkj_SetupWaitForLandAnim(this);  // was func_80AFF2A0 [LLM:HIGH]
         }
     }
 }
@@ -727,8 +735,8 @@ void EnSkj_SariasSongKidIdle(EnSkj* this, PlayState* play) {
             }
         }
     } else {
-        if (func_80AFEDF8(this, play) != 0) {
-            func_80AFF334(this);
+        if (EnSkj_IsPlayerEngaging(this, play) != 0) {  // was func_80AFEDF8 [LLM:HIGH]
+            EnSkj_SetupLookAround(this);  // was func_80AFF334 [LLM:HIGH]
         }
     }
 }
@@ -747,7 +755,7 @@ void EnSkj_WaitForDeathAnim(EnSkj* this, PlayState* play) {
     }
 }
 
-void func_80AFF1F0(EnSkj* this) {
+void EnSkj_SetupPickNextFightAction(EnSkj* this) {
     EnSkj_ChangeAnim(this, SKJ_ANIM_HIT);
     EnSkj_SetupAction(this, SKJ_ACTION_PICK_NEXT_FIHGT_ACTION);
 }
@@ -765,7 +773,7 @@ void EnSkj_PickNextFightAction(EnSkj* this, PlayState* play) {
     }
 }
 
-void func_80AFF2A0(EnSkj* this) {
+void EnSkj_SetupWaitForLandAnim(EnSkj* this) {
     EnSkj_CalculateCenter(this);
     this->actor.speedXZ = 0.0f;
     EnSkj_ChangeAnim(this, SKJ_ANIM_LAND);
@@ -780,7 +788,7 @@ void EnSkj_WaitForLandAnim(EnSkj* this, PlayState* play) {
     }
 }
 
-void func_80AFF334(EnSkj* this) {
+void EnSkj_SetupLookAround(EnSkj* this) {
     this->unk_2C8 = 0x2000;
     this->battleExitTimer = 400;
     this->unk_2EC = 600.0f;
@@ -791,7 +799,7 @@ void func_80AFF334(EnSkj* this) {
 void EnSkj_ResetFight(EnSkj* this, PlayState* play) {
     if (this->battleExitTimer == 0) {
         EnSkj_SetupResetFight(this);
-    } else if (func_80AFEDF8(this, play) != 0) {
+    } else if (EnSkj_IsPlayerEngaging(this, play) != 0) {  // was func_80AFEDF8 [LLM:HIGH]
         this->battleExitTimer = 600;
         EnSkj_SetupStand(this);
     }
@@ -839,7 +847,7 @@ void EnSkj_Fight(EnSkj* this, PlayState* play) {
 
         this->skelAnime.playSpeed = (yawDistToPlayer < 0) ? -(1.0f + phi_f14) : (1.0f + phi_f14);
 
-    } else if (func_80AFEDF8(this, play) != 0) {
+    } else if (EnSkj_IsPlayerEngaging(this, play) != 0) {  // was func_80AFEDF8 [LLM:HIGH]
         this->backflipFlag = 1;
         EnSkj_Backflip(this);
     } else {
@@ -913,7 +921,7 @@ void EnSkj_WaitInRange(EnSkj* this, PlayState* play) {
         EnSkj_TurnPlayer(sSmallStumpSkullKid.skullkid, player);
         Message_StartOcarinaSunsSongDisabled(play, OCARINA_ACTION_CHECK_SARIA);
         EnSkj_SetupWaitForSong(this);
-    } else if (D_80B01EA0 != 0) {
+    } else if (sTalkRequested != 0) {  // was D_80B01EA0 [LLM:HIGH]
         player->actor.world.pos.x = sSmallStumpSkullKid.skullkid->actor.world.pos.x;
         player->actor.world.pos.y = sSmallStumpSkullKid.skullkid->actor.world.pos.y;
         player->actor.world.pos.z = sSmallStumpSkullKid.skullkid->actor.world.pos.z;
@@ -1018,7 +1026,7 @@ void EnSkj_SetupAfterSong(EnSkj* this) {
 }
 
 void EnSkj_AfterSong(EnSkj* this, PlayState* play) {
-    if (D_80B01EA0 != 0) {
+    if (sTalkRequested != 0) {  // was D_80B01EA0 [LLM:HIGH]
         EnSkj_SetupTalk(this);
     } else {
         Actor_OfferTalk(&this->actor, play, EnSkj_GetItemXzRange(this));
@@ -1035,7 +1043,7 @@ void EnSkj_SariaSongTalk(EnSkj* this, PlayState* play) {
         if (Flags_GetItemGetInf(ITEMGETINF_16)) {
             EnSkj_SetupWaitInRange(this);
         } else {
-            func_80AFFE24(this);
+            EnSkj_SetupOfferHeartPiece(this);  // was func_80AFFE24 [LLM:HIGH]
             if (GameInteractor_Should(VB_GIVE_ITEM_FROM_SKULL_KID_SARIAS_SONG, true, this)) {
                 Actor_OfferGetItem(&this->actor, play, GI_HEART_PIECE, EnSkj_GetItemXzRange(this),
                                    EnSkj_GetItemYRange(this));
@@ -1044,11 +1052,11 @@ void EnSkj_SariaSongTalk(EnSkj* this, PlayState* play) {
     }
 }
 
-void func_80AFFE24(EnSkj* this) {
+void EnSkj_SetupOfferHeartPiece(EnSkj* this) {
     EnSkj_SetupAction(this, SKJ_ACTION_UNK14);
 }
 
-void func_80AFFE44(EnSkj* this, PlayState* play) {
+void EnSkj_OfferHeartPiece(EnSkj* this, PlayState* play) {
     if (Actor_HasParent(&this->actor, play) ||
         !GameInteractor_Should(VB_GIVE_ITEM_FROM_SKULL_KID_SARIAS_SONG, true, this)) {
         this->actor.parent = NULL;
@@ -1192,7 +1200,7 @@ void EnSkj_SetupWrongSong(EnSkj* this) {
 }
 
 void EnSkj_WrongSong(EnSkj* this, PlayState* play) {
-    if (D_80B01EA0 != 0) {
+    if (sTalkRequested != 0) {  // was D_80B01EA0 [LLM:HIGH]
         EnSkj_SetupWaitForTextClear(this);
     } else {
         Actor_OfferTalk(&this->actor, play, EnSkj_GetItemXzRange(this));
@@ -1293,7 +1301,7 @@ void EnSkj_Update(Actor* thisx, PlayState* play) {
     Vec3f dropPos;
     EnSkj* this = (EnSkj*)thisx;
 
-    D_80B01EA0 = Actor_ProcessTalkRequest(&this->actor, play);
+    sTalkRequested = Actor_ProcessTalkRequest(&this->actor, play);  // was D_80B01EA0 [LLM:HIGH]
 
     this->timer++;
 
@@ -1332,7 +1340,7 @@ void EnSkj_Update(Actor* thisx, PlayState* play) {
     EnSkj_CollisionCheck(this, play);
     Collider_UpdateCylinder(&this->actor, &this->collider);
 
-    if ((this->unk_2D3 != 0) && (D_80B01EA0 == 0)) {
+    if ((this->unk_2D3 != 0) && (sTalkRequested == 0)) {  // was D_80B01EA0 [LLM:HIGH]
         CollisionCheck_SetAT(play, &play->colChkCtx, &this->collider.base);
 
         if (this->actor.colorFilterTimer == 0) {
@@ -1349,7 +1357,7 @@ void EnSkj_Update(Actor* thisx, PlayState* play) {
 void EnSkj_SariasSongShortStumpUpdate(Actor* thisx, PlayState* play) {
     EnSkj* this = (EnSkj*)thisx;
 
-    D_80B01EA0 = Actor_ProcessTalkRequest(&this->actor, play);
+    sTalkRequested = Actor_ProcessTalkRequest(&this->actor, play);  // was D_80B01EA0 [LLM:HIGH]
 
     if (BREG(0) != 0) {
         DebugDisplay_AddObject(this->actor.world.pos.x, this->actor.world.pos.y, this->actor.world.pos.z,
@@ -1491,7 +1499,7 @@ void EnSkj_WaitForPlayback(EnSkj* this, PlayState* play) {
 }
 
 void EnSkj_FailedMiniGame(EnSkj* this, PlayState* play) {
-    if (D_80B01EA0) {
+    if (sTalkRequested) {  // was D_80B01EA0 [LLM:HIGH]
         this->actionFunc = EnSkj_WaitForNextRound;
     } else {
         Actor_OfferTalk(&this->actor, play, 26.0f);
@@ -1528,7 +1536,7 @@ void EnSkj_WaitForOfferResponse(EnSkj* this, PlayState* play) {
 }
 
 void EnSkj_WonOcarinaMiniGame(EnSkj* this, PlayState* play) {
-    if (D_80B01EA0) {
+    if (sTalkRequested) {  // was D_80B01EA0 [LLM:HIGH]
         if (GameInteractor_Should(VB_GIVE_ITEM_FROM_OCARINA_MEMORY_GAME, true, this)) {
             this->actionFunc = EnSkj_WaitToGiveReward;
         }
@@ -1589,7 +1597,7 @@ void EnSkj_CleanupOcarinaGame(EnSkj* this, PlayState* play) {
 void EnSkj_OcarinaMinigameShortStumpUpdate(Actor* thisx, PlayState* play) {
     EnSkj* this = (EnSkj*)thisx;
 
-    D_80B01EA0 = Actor_ProcessTalkRequest(&this->actor, play);
+    sTalkRequested = Actor_ProcessTalkRequest(&this->actor, play);  // was D_80B01EA0 [LLM:HIGH]
     this->timer++;
 
     this->actor.focus.pos.x = 1230.0f;
-- 
2.55.0

