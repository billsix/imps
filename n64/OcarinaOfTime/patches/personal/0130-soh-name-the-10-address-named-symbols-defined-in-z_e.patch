From 8b374781a96be2477dc3da98d4c3c5242b125d1b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:09 +0000
Subject: [PATCH 130/488] soh: name the 10 address-named symbols defined in
 z_en_ik.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 10 are defined in soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c.

  * func_80A77034 -> EnIk_UpdateBgCheckInfo
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 0.95

  * func_80A77148 -> EnIk_SetupCsAction0
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A77434 -> EnIk_SetupCsAction4
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A77474 -> EnIk_SetupCsAction5
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A7748C -> EnIk_CsAction3
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A774BC -> EnIk_CsAction4
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A779DC -> EnIk_HandleCsCues
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 0.95

  * func_80A77AEC -> EnIk_CsAction0
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A77B0C -> EnIk_CsAction1
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * func_80A77B3C -> EnIk_CsAction2
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c | 68 ++++++++++++---------
 1 file changed, 39 insertions(+), 29 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c b/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
index a0b745874..ddb17a160 100644
--- a/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
+++ b/soh/src/overlays/actors/ovl_En_Ik/z_en_ik.c
@@ -61,15 +61,21 @@ void EnIk_Die(EnIk* this, PlayState* play);
 void EnIk_UpdateEnemy(Actor* thisx, PlayState* play);
 // Name from zeldaret/oot (was func_80A76798): adopted upstream's name for this address; body verified to match upstream at 0.82. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
 void EnIk_DrawEnemy(Actor* thisx, PlayState* play);
-void func_80A7748C(EnIk* this, PlayState* play);
-void func_80A774BC(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A7748C): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_CsAction3(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A774BC): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_CsAction4(EnIk* this, PlayState* play);
 void func_80A774F8(EnIk* this, PlayState* play);
 // Name from zeldaret/oot (was func_80A77844): adopted upstream's name for this address; body verified to match upstream at 0.82. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
 void EnIk_CsDrawDefeat(EnIk* this, PlayState* play);
-void func_80A779DC(EnIk* this, PlayState* play);
-void func_80A77AEC(EnIk* this, PlayState* play);
-void func_80A77B0C(EnIk* this, PlayState* play);
-void func_80A77B3C(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A779DC): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_HandleCsCues(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A77AEC): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_CsAction0(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A77B0C): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_CsAction1(EnIk* this, PlayState* play);
+// Name from zeldaret/oot (was func_80A77B3C): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_CsAction2(EnIk* this, PlayState* play);
 // Name from zeldaret/oot (was func_80A77ED0): adopted upstream's name for this address; body verified to match upstream at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
 void EnIk_CsDrawNothing(EnIk* this, PlayState* play);
 // Name from zeldaret/oot (was func_80A77EDC): adopted upstream's name for this address; body verified to match upstream at 0.81. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
@@ -1077,7 +1083,8 @@ void EnIk_SpawnAxeSmoke(EnIk* this, PlayState* play, Vec3f* pos) {
     }
 }
 
-void func_80A77034(EnIk* this, PlayState* play) {
+// Name from zeldaret/oot (was func_80A77034): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 0.95. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_UpdateBgCheckInfo(EnIk* this, PlayState* play) {
     Actor_UpdateBgCheckInfo(play, &this->actor, 75.0f, 30.0f, 30.0f, 5);
 }
 
@@ -1112,7 +1119,8 @@ f32 EnIk_curFrame(Actor* thisx) {
     return this->skelAnime.curFrame;
 }
 
-void func_80A77148(EnIk* this) {
+// Name from zeldaret/oot (was func_80A77148): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_SetupCsAction0(EnIk* this) {
     this->action = 0;
     this->drawMode = 0;
     this->actor.shape.shadowAlpha = 0;
@@ -1172,28 +1180,30 @@ void EnIk_SetupCsAction3(EnIk* this, PlayState* play) {
     this->actor.shape.shadowAlpha = 0xFF;
 }
 
-void func_80A77434(EnIk* this, PlayState* play) {
+// Name from zeldaret/oot (was func_80A77434): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_SetupCsAction4(EnIk* this, PlayState* play) {
     this->action = 4;
     this->drawMode = 2;
     func_80A772A4(this);
     this->actor.shape.shadowAlpha = 0xFF;
 }
 
-void func_80A77474(EnIk* this, PlayState* play) {
+// Name from zeldaret/oot (was func_80A77474): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Ik/z_en_ik.c]
+void EnIk_SetupCsAction5(EnIk* this, PlayState* play) {
     this->action = 5;
     this->drawMode = 0;
     this->actor.shape.shadowAlpha = 0;
 }
 
-void func_80A7748C(EnIk* this, PlayState* play) {
-    func_80A77034(this, play);
-    func_80A779DC(this, play);
+void EnIk_CsAction3(EnIk* this, PlayState* play) {
+    EnIk_UpdateBgCheckInfo(this, play);
+    EnIk_HandleCsCues(this, play);
 }
 
-void func_80A774BC(EnIk* this, PlayState* play) {
+void EnIk_CsAction4(EnIk* this, PlayState* play) {
     EnIk_UpdateSkelAnime(this);
-    func_80A77034(this, play);
-    func_80A779DC(this, play);
+    EnIk_UpdateBgCheckInfo(this, play);
+    EnIk_HandleCsCues(this, play);
 }
 
 void func_80A774F8(EnIk* this, PlayState* play) {
@@ -1273,7 +1283,7 @@ void EnIk_CsDrawDefeat(EnIk* this, PlayState* play) {
     CLOSE_DISPS(gfxCtx);
 }
 
-void func_80A779DC(EnIk* this, PlayState* play) {
+void EnIk_HandleCsCues(EnIk* this, PlayState* play) {
     CsCmdActorCue* npcAction = EnIk_GetNpcAction(play, 4);
     u32 action;
     u32 currentNpcAction;
@@ -1284,7 +1294,7 @@ void func_80A779DC(EnIk* this, PlayState* play) {
         if (action != currentNpcAction) {
             switch (action) {
                 case 1:
-                    func_80A77148(this);
+                    EnIk_SetupCsAction0(this);
                     break;
                 case 2:
                     EnIk_SetupCsAction1(this, play);
@@ -1299,10 +1309,10 @@ void func_80A779DC(EnIk* this, PlayState* play) {
                     EnIk_SetupCsAction3(this, play);
                     break;
                 case 6:
-                    func_80A77434(this, play);
+                    EnIk_SetupCsAction4(this, play);
                     break;
                 case 7:
-                    func_80A77474(this, play);
+                    EnIk_SetupCsAction5(this, play);
                     break;
                 default:
                     osSyncPrintf("En_Ik_inConfrontion_Check_DemoMode:そんな動作は無い!!!!!!!!\n");
@@ -1313,27 +1323,27 @@ void func_80A779DC(EnIk* this, PlayState* play) {
     }
 }
 
-void func_80A77AEC(EnIk* this, PlayState* play) {
-    func_80A779DC(this, play);
+void EnIk_CsAction0(EnIk* this, PlayState* play) {
+    EnIk_HandleCsCues(this, play);
 }
 
-void func_80A77B0C(EnIk* this, PlayState* play) {
-    func_80A77034(this, play);
-    func_80A779DC(this, play);
+void EnIk_CsAction1(EnIk* this, PlayState* play) {
+    EnIk_UpdateBgCheckInfo(this, play);
+    EnIk_HandleCsCues(this, play);
 }
 
-void func_80A77B3C(EnIk* this, PlayState* play) {
+void EnIk_CsAction2(EnIk* this, PlayState* play) {
     s32 sp24;
 
     sp24 = EnIk_UpdateSkelAnime(this);
     func_80A76C14(this);
-    func_80A77034(this, play);
-    func_80A779DC(this, play);
+    EnIk_UpdateBgCheckInfo(this, play);
+    EnIk_HandleCsCues(this, play);
     EnIk_HandleEnemyChange(this, play, sp24);
 }
 
 static EnIkActionFunc sActionFuncs[] = {
-    func_80A77AEC, func_80A77B0C, func_80A77B3C, func_80A7748C, func_80A774BC, func_80A774F8,
+    EnIk_CsAction0, EnIk_CsAction1, EnIk_CsAction2, EnIk_CsAction3, EnIk_CsAction4, func_80A774F8,
 };
 
 void EnIk_Update(Actor* thisx, PlayState* play) {
-- 
2.55.0

