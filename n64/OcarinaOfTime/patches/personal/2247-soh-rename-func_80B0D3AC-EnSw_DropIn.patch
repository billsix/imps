From 7fea48abc35894e9f296398604c28b7dda1246d5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:18 +0000
Subject: [PATCH 2247/3799] soh: rename func_80B0D3AC -> EnSw_DropIn

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: puffs dust while the delay runs, then grows and falls along the stored basis until it finds a surface to cling to
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index e4178cd06..2a8a6d1ca 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -19,7 +19,8 @@ void func_80B0E728(EnSw* this, PlayState* play);
 void func_80B0DC7C(EnSw* this, PlayState* play);
 // LLM generated name (HIGH), was func_80B0C0CC: probes ahead of and behind the skulltula for a surface to cling to and, when told to, moves it onto the hit and re-bases it
 s32 EnSw_StickToSurface(EnSw* this, PlayState* play, s32);  // was func_80B0C0CC [LLM:HIGH]
-void func_80B0D3AC(EnSw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B0D3AC: puffs dust while the delay runs, then grows and falls along the stored basis until it finds a surface to cling to
+void EnSw_DropIn(EnSw* this, PlayState* play);  // was func_80B0D3AC [LLM:HIGH]
 void func_80B0DB00(EnSw* this, PlayState* play);
 void func_80B0D878(EnSw* this, PlayState* play);
 
@@ -498,14 +499,14 @@ void EnSw_SpawnLandingDust(EnSw* this, PlayState* play, s32 cnt) {
 void EnSw_SetupDropIn(EnSw* this, PlayState* play) {
     if (((this->actor.params & 0xE000) >> 0xD) == 4) {
         this->unk_38C = 0;
-        this->actionFunc = func_80B0D3AC;
+        this->actionFunc = EnSw_DropIn;  // was func_80B0D3AC [LLM:HIGH]
     } else {
         this->unk_38C = 10;
-        this->actionFunc = func_80B0D3AC;
+        this->actionFunc = EnSw_DropIn;  // was func_80B0D3AC [LLM:HIGH]
     }
 }
 
-void func_80B0D3AC(EnSw* this, PlayState* play) {
+void EnSw_DropIn(EnSw* this, PlayState* play) {
     if (this->unk_38C != 0) {
         if ((this->unk_38C & 4) != 0) {
             EnSw_SpawnSpawnDust(this, play, 5);  // was func_80B0CF44 [LLM:HIGH]
-- 
2.55.0

