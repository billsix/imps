From 747b4572c1363547cec55fb8926109b9a2996691 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:43:30 +0000
Subject: [PATCH 1795/3799] soh: rename func_80028A54 ->
 EffectSsDust_SpawnRing5

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the same twenty-puff ring with drawFlags 5.
Defined in soh/src/code/z_effect_soft_sprite_old_init.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h                           |  2 +-
 soh/src/code/z_effect_soft_sprite_old_init.c      |  3 ++-
 soh/src/overlays/actors/ovl_En_Horse/z_en_horse.c | 10 +++++-----
 3 files changed, 8 insertions(+), 7 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 7de348dee..5f179a514 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -258,7 +258,7 @@ void EffectSsDust_SpawnDrawFlags0Default(PlayState* play, Vec3f* pos, Vec3f* vel
 void EffectSsDust_SpawnDrawFlags1Default(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel, Color_RGBA8* primColor,
                    Color_RGBA8* envColor);  // was func_80028858 [LLM:HIGH]
 void EffectSsDust_SpawnRing4(PlayState* play, f32 randScale, Vec3f* srcPos);  // was func_80028990 [LLM:HIGH]
-void func_80028A54(PlayState* play, f32 randScale, Vec3f* srcPos);
+void EffectSsDust_SpawnRing5(PlayState* play, f32 randScale, Vec3f* srcPos);  // was func_80028A54 [LLM:HIGH]
 void EffectSsKiraKira_SpawnSmallYellow(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel);
 void EffectSsKiraKira_SpawnSmall(PlayState* play, Vec3f* pos, Vec3f* velocity, Vec3f* accel,
                                  Color_RGBA8* primColor, Color_RGBA8* envColor);
diff --git a/soh/src/code/z_effect_soft_sprite_old_init.c b/soh/src/code/z_effect_soft_sprite_old_init.c
index 3da87d0a1..fb5621556 100644
--- a/soh/src/code/z_effect_soft_sprite_old_init.c
+++ b/soh/src/code/z_effect_soft_sprite_old_init.c
@@ -223,7 +223,8 @@ void EffectSsDust_SpawnRing4(PlayState* play, f32 randScale, Vec3f* srcPos) {
     }
 }
 
-void func_80028A54(PlayState* play, f32 randScale, Vec3f* srcPos) {
+// LLM generated name (HIGH), was func_80028A54: the same twenty-puff ring with drawFlags 5.
+void EffectSsDust_SpawnRing5(PlayState* play, f32 randScale, Vec3f* srcPos) {
     s32 i;
     Vec3f pos;
     Vec3f velocity;
diff --git a/soh/src/overlays/actors/ovl_En_Horse/z_en_horse.c b/soh/src/overlays/actors/ovl_En_Horse/z_en_horse.c
index 802836082..a78e0d7df 100644
--- a/soh/src/overlays/actors/ovl_En_Horse/z_en_horse.c
+++ b/soh/src/overlays/actors/ovl_En_Horse/z_en_horse.c
@@ -1652,7 +1652,7 @@ void EnHorse_LowJump(EnHorse* this, PlayState* play) {
         this->stateFlags &= ~ENHORSE_JUMPING;
         this->actor.gravity = -3.5f;
         this->actor.world.pos.y = this->actor.floorHeight;
-        func_80028A54(play, 25.0f, &this->actor.world.pos);
+        EffectSsDust_SpawnRing5(play, 25.0f, &this->actor.world.pos);  // was func_80028A54 [LLM:HIGH]
         EnHorse_JumpLanding(this, play);
     }
 }
@@ -1728,7 +1728,7 @@ void EnHorse_HighJump(EnHorse* this, PlayState* play) {
         this->stateFlags &= ~ENHORSE_JUMPING;
         this->actor.gravity = -3.5f;
         this->actor.world.pos.y = this->actor.floorHeight;
-        func_80028A54(play, 25.0f, &this->actor.world.pos);
+        EffectSsDust_SpawnRing5(play, 25.0f, &this->actor.world.pos);  // was func_80028A54 [LLM:HIGH]
         EnHorse_JumpLanding(this, play);
     }
 }
@@ -2230,7 +2230,7 @@ void EnHorse_CsJump(EnHorse* this, PlayState* play, CsCmdActorCue* action) {
         this->actor.gravity = -3.5f;
         this->actor.velocity.y = 0;
         this->actor.world.pos.y = this->actor.floorHeight;
-        func_80028A54(play, 25.0f, &this->actor.world.pos);
+        EffectSsDust_SpawnRing5(play, 25.0f, &this->actor.world.pos);  // was func_80028A54 [LLM:HIGH]
         this->animationIdx = ENHORSE_ANIM_GALLOP;
         Animation_PlayOnceSetSpeed(&this->skin.skelAnime, sAnimationHeaders[this->type][this->animationIdx],
                                    sPlaybackSpeeds[6]);
@@ -2750,7 +2750,7 @@ void EnHorse_FleePlayer(EnHorse* this, PlayState* play) {
 void EnHorse_BridgeJumpInit(EnHorse* this, PlayState* play) {
     f32 y;
 
-    func_80028A54(play, 25.0f, &this->actor.world.pos);
+    EffectSsDust_SpawnRing5(play, 25.0f, &this->actor.world.pos);  // was func_80028A54 [LLM:HIGH]
     this->action = ENHORSE_ACT_BRIDGE_JUMP;
     this->stateFlags |= ENHORSE_JUMPING;
     this->animationIdx = ENHORSE_ANIM_HIGH_JUMP;
@@ -2821,7 +2821,7 @@ void EnHorse_CheckBridgeJumpLanding(EnHorse* this, PlayState* play) {
         this->stateFlags &= ~ENHORSE_JUMPING;
         this->actor.gravity = -3.5f;
         this->actor.world.pos.y = sBridgeJumps[this->bridgeJumpIdx].pos.y;
-        func_80028A54(play, 25.0f, &this->actor.world.pos);
+        EffectSsDust_SpawnRing5(play, 25.0f, &this->actor.world.pos);  // was func_80028A54 [LLM:HIGH]
         EnHorse_JumpLanding(this, play);
         Audio_PlaySfxGeneral(NA_SE_EV_HORSE_LAND, &this->actor.projectedPos, 4, &gSfxDefaultFreqAndVolScale,
                              &gSfxDefaultFreqAndVolScale, &gSfxDefaultReverb);
-- 
2.55.0

