From e588a442f44772390cfe7415dc5dc54ebef195f4 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:17:02 +0000
Subject: [PATCH 3373/3799] soh: rename D_808547B0 -> sBombchuSparkAccel

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the acceleration those sparks are given
Defined in soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c | 5 +++--
 soh/src/overlays/actors/ovl_player_actor/z_player.c   | 4 ++--
 2 files changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c b/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
index 924ba2496..4113c0955 100644
--- a/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
+++ b/soh/src/overlays/actors/ovl_En_Partner/z_en_partner.c
@@ -359,7 +359,8 @@ void UseBombchus(Actor* thisx, PlayState* play, u8 started) {
 
 // LLM generated name (HIGH), was D_808547A4: the velocity of the sparks the bombchu trail spawns
 static Vec3f sBombchuSparkVelocity = { 0.0f, 0.5f, 0.0f };  // was D_808547A4 [LLM:HIGH]
-static Vec3f D_808547B0 = { 0.0f, 0.5f, 0.0f };
+// LLM generated name (HIGH), was D_808547B0: the acceleration those sparks are given
+static Vec3f sBombchuSparkAccel = { 0.0f, 0.5f, 0.0f };  // was D_808547B0 [LLM:HIGH]
 
 static Color_RGBA8 D_808547BC = { 255, 255, 100, 255 };
 static Color_RGBA8 D_808547C0 = { 255, 50, 0, 0 };
@@ -381,7 +382,7 @@ void UseDekuStick(Actor* thisx, PlayState* play, u8 started) {
                 this->stickWeaponInfo.tip = this->actor.world.pos;
                 this->stickWeaponInfo.tip.y += 7.0f;
 
-                EffectSsDust_SpawnDrawFlags0Life(play, &this->stickWeaponInfo.tip, &sBombchuSparkVelocity, &D_808547B0, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
+                EffectSsDust_SpawnDrawFlags0Life(play, &this->stickWeaponInfo.tip, &sBombchuSparkVelocity, &sBombchuSparkAccel, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
                               200.0f, 0, 8);
 
                 CollisionCheck_SetAT(play, &play->colChkCtx, &this->collider.base);
diff --git a/soh/src/overlays/actors/ovl_player_actor/z_player.c b/soh/src/overlays/actors/ovl_player_actor/z_player.c
index 125f022c9..d9e653503 100644
--- a/soh/src/overlays/actors/ovl_player_actor/z_player.c
+++ b/soh/src/overlays/actors/ovl_player_actor/z_player.c
@@ -11547,7 +11547,7 @@ void Player_UpdateCamAndSeqModes(PlayState* play, Player* this) {
 }
 
 static Vec3f sBombchuSparkVelocity = { 0.0f, 0.5f, 0.0f };  // was D_808547A4 [LLM:HIGH]
-static Vec3f D_808547B0 = { 0.0f, 0.5f, 0.0f };
+static Vec3f sBombchuSparkAccel = { 0.0f, 0.5f, 0.0f };  // was D_808547B0 [LLM:HIGH]
 
 static Color_RGBA8 D_808547BC = { 255, 255, 100, 255 };
 static Color_RGBA8 D_808547C0 = { 255, 50, 0, 0 };
@@ -11574,7 +11574,7 @@ void Player_UpdateBurningDekuStick(PlayState* play, Player* this) {
         this->unk_85C = temp;
     }
 
-    EffectSsDust_SpawnDrawFlags0Life(play, &this->meleeWeaponInfo[0].tip, &sBombchuSparkVelocity, &D_808547B0, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
+    EffectSsDust_SpawnDrawFlags0Life(play, &this->meleeWeaponInfo[0].tip, &sBombchuSparkVelocity, &sBombchuSparkAccel, &D_808547BC, &D_808547C0,  // was func_8002836C [LLM:HIGH]
                   temp * 200.0f, 0, 8);
 }
 
-- 
2.55.0

