From 424090766da88b311f45125b5812387dc1a96fd2 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:09:29 +0000
Subject: [PATCH 1305/3799] soh: rename func_808BC8B8 ->
 BgTreemouth_WaitForPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the child-era idle: enables targeting once the player is facing it from close enough, and starts either the first-meeting or the enter-the-tree choice cutscene.
Defined in soh/src/overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c    | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c b/soh/src/overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c
index ccda2091b..4bf9f9bc2 100644
--- a/soh/src/overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c
+++ b/soh/src/overlays/actors/ovl_Bg_Treemouth/z_bg_treemouth.c
@@ -21,7 +21,8 @@ void func_808BC6F8(BgTreemouth* this, PlayState* play);
 void func_808BC80C(BgTreemouth* this, PlayState* play);
 void func_808BC864(BgTreemouth* this, PlayState* play);
 void BgTreemouth_DoNothing(BgTreemouth* this, PlayState* play);
-void func_808BC8B8(BgTreemouth* this, PlayState* play);
+// LLM generated name (HIGH), was func_808BC8B8: the child-era idle: enables targeting once the player is facing it from close enough, and starts either the first-meeting or the enter-the-tree choice cutscene.
+void BgTreemouth_WaitForPlayer(BgTreemouth* this, PlayState* play);  // was func_808BC8B8 [LLM:HIGH]
 void func_808BC9EC(BgTreemouth* this, PlayState* play);
 void func_808BCAF0(BgTreemouth* this, PlayState* play);
 
@@ -78,7 +79,7 @@ void BgTreemouth_Init(Actor* thisx, PlayState* play) {
     Actor_SetFocus(thisx, 50.0f);
 
     if ((gSaveContext.sceneLayer < 4) && !LINK_IS_ADULT) {
-        BgTreemouth_SetupAction(this, func_808BC8B8);
+        BgTreemouth_SetupAction(this, BgTreemouth_WaitForPlayer);  // was func_808BC8B8 [LLM:HIGH]
     } else if (LINK_IS_ADULT || (gSaveContext.sceneLayer == 7)) {
         this->unk_168 = 0.0f;
         BgTreemouth_SetupAction(this, BgTreemouth_DoNothing);
@@ -144,7 +145,7 @@ void func_808BC864(BgTreemouth* this, PlayState* play) {
     }
 }
 
-void func_808BC8B8(BgTreemouth* this, PlayState* play) {
+void BgTreemouth_WaitForPlayer(BgTreemouth* this, PlayState* play) {
     if ((!Flags_GetEventChkInf(EVENTCHKINF_DEKU_TREE_OPENED_MOUTH)) || LINK_IS_ADULT) {
         if (!LINK_IS_ADULT) {
             if (Flags_GetEventChkInf(EVENTCHKINF_MET_DEKU_TREE)) {
@@ -197,7 +198,7 @@ void func_808BC9EC(BgTreemouth* this, PlayState* play) {
         } else {
             play->csCtx.segment = gDekuTreeAskAgainCs;
             play->csCtx.frames = 0;
-            BgTreemouth_SetupAction(this, func_808BC8B8);
+            BgTreemouth_SetupAction(this, BgTreemouth_WaitForPlayer);  // was func_808BC8B8 [LLM:HIGH]
         }
     }
 }
-- 
2.55.0

