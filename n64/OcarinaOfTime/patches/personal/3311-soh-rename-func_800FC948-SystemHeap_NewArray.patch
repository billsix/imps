From 1b8933a4833cebfcf8258c140bee95273811f828 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 00:13:26 +0000
Subject: [PATCH 3311/3799] soh: rename func_800FC948 -> SystemHeap_NewArray

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: allocates the array when the caller passes NULL, then constructs every element
Defined in soh/src/code/system_heap.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/code/system_heap.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/soh/src/code/system_heap.c b/soh/src/code/system_heap.c
index 442259264..75b48de80 100644
--- a/soh/src/code/system_heap.c
+++ b/soh/src/code/system_heap.c
@@ -132,7 +132,8 @@ void SystemHeap_DestructArray(void* blk, u32 nBlk, s32 blkSize, arg3_800FC8D8 ar
     }
 }
 
-void* func_800FC948(void* blk, u32 nBlk, u32 blkSize, arg3_800FC948 arg3) {
+// LLM generated name (HIGH), was func_800FC948: allocates the array when the caller passes NULL, then constructs every element
+void* SystemHeap_NewArray(void* blk, u32 nBlk, u32 blkSize, arg3_800FC948 arg3) {
     uintptr_t pos;
 
     if (blk == NULL) {
-- 
2.55.0

