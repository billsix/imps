From 4433137b2e5919099a5d0cd614a0d4cc1766731b Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:10:58 +0000
Subject: [PATCH 2051/3799] soh: rename func_80AB70F8 -> EnNiw_SwarmWindUp

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: holds for the cutscene timer, then arms the crow that precedes the swarm
Defined in soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
index 5c63dd454..8d060d897 100644
--- a/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
+++ b/soh/src/overlays/actors/ovl_En_Niw/z_en_niw.c
@@ -36,7 +36,8 @@ void EnNiw_Held(EnNiw* this, PlayState* play);  // was func_80AB6BF8 [LLM:HIGH]
 void EnNiw_FlutterAfterRelease(EnNiw* this, PlayState* play);  // was func_80AB6D08 [LLM:GUESS]
 // LLM generated name (HIGH), was func_80AB6EB4: rises until y passes 400 and then kills the actor
 void EnNiw_FlyAway(EnNiw* this, PlayState* play);  // was func_80AB6EB4 [LLM:HIGH]
-void func_80AB70F8(EnNiw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80AB70F8: holds for the cutscene timer, then arms the crow that precedes the swarm
+void EnNiw_SwarmWindUp(EnNiw* this, PlayState* play);  // was func_80AB70F8 [LLM:HIGH]
 void func_80AB714C(EnNiw* this, PlayState* play);
 void func_80AB7204(EnNiw* this, PlayState* play);
 void func_80AB7290(EnNiw* this, PlayState* play);
@@ -781,17 +782,17 @@ void EnNiw_SetupSwarmCutscene(EnNiw* this, PlayState* play) {
     OnePointCutscene_Init(play, 2290, -99, &this->actor, CAM_ID_MAIN);
     this->timer5 = 100;
     this->unk_2A2 = 1;
-    this->actionFunc = func_80AB70F8;
+    this->actionFunc = EnNiw_SwarmWindUp;  // was func_80AB70F8 [LLM:HIGH]
 }
 
 // LLM generated name (HIGH), was func_80AB70A0_nocutscene: the port's cutscene-free variant of EnNiw_SetupSwarmCutscene; renamed with it so its name does not cite a symbol that no longer exists
 void EnNiw_SetupSwarmNoCutscene(EnNiw* this, PlayState* play) {
     this->timer5 = 10;
     this->unk_2A2 = 1;
-    this->actionFunc = func_80AB70F8;
+    this->actionFunc = EnNiw_SwarmWindUp;  // was func_80AB70F8 [LLM:HIGH]
 }
 
-void func_80AB70F8(EnNiw* this, PlayState* play) {
+void EnNiw_SwarmWindUp(EnNiw* this, PlayState* play) {
     this->sfxTimer1 = 100;
 
     if (this->timer5 == 0) {
-- 
2.55.0

