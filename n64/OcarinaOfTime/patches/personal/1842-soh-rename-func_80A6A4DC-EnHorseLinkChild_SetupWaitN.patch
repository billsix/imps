From 35a0216059619acf58dbc4b2700dad770fe36461 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:47:48 +0000
Subject: [PATCH 1842/3799] soh: rename func_80A6A4DC ->
 EnHorseLinkChild_SetupWaitNearPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: enters action 5 with a randomly chosen standing animation and clears the debug trigger register.
Defined in soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../ovl_En_Horse_Link_Child/z_en_horse_link_child.c   | 11 ++++++-----
 1 file changed, 6 insertions(+), 5 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
index c37b6771a..f7529c3db 100644
--- a/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
+++ b/soh/src/overlays/actors/ovl_En_Horse_Link_Child/z_en_horse_link_child.c
@@ -18,7 +18,8 @@ void EnHorseLinkChild_Draw(Actor* thisx, PlayState* play);
 void EnHorseLinkChild_SetupApproach(EnHorseLinkChild* this);  // was func_80A69B7C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80A69EC0: enters action 3 with the first animation, stopping the foal.
 void EnHorseLinkChild_SetupFollow(EnHorseLinkChild* this);  // was func_80A69EC0 [LLM:HIGH]
-void func_80A6A4DC(EnHorseLinkChild* this);
+// LLM generated name (HIGH), was func_80A6A4DC: enters action 5 with a randomly chosen standing animation and clears the debug trigger register.
+void EnHorseLinkChild_SetupWaitNearPlayer(EnHorseLinkChild* this);  // was func_80A6A4DC [LLM:HIGH]
 void func_80A6A724(EnHorseLinkChild* this);
 
 const ActorInit En_Horse_Link_Child_InitVars = {
@@ -374,7 +375,7 @@ void EnHorseLinkChild_Follow(EnHorseLinkChild* this, PlayState* play) {
 
     if (((Flags_GetEventChkInf(EVENTCHKINF_INVITED_TO_SING_WITH_CHILD_MALON)) && (DREG(53) != 0)) ||
         ((play->sceneNum == SCENE_LON_LON_RANCH) && (gSaveContext.cutsceneIndex == 0xFFF1))) {
-        func_80A6A4DC(this);
+        EnHorseLinkChild_SetupWaitNearPlayer(this);  // was func_80A6A4DC [LLM:HIGH]
     } else {
         this->unk_2A0 = Flags_GetEventChkInf(EVENTCHKINF_INVITED_TO_SING_WITH_CHILD_MALON);
     }
@@ -445,7 +446,7 @@ void EnHorseLinkChild_Follow(EnHorseLinkChild* this, PlayState* play) {
     }
 }
 
-void func_80A6A4DC(EnHorseLinkChild* this) {
+void EnHorseLinkChild_SetupWaitNearPlayer(EnHorseLinkChild* this) {
     this->action = 5;
     this->animationIdx = Rand_ZeroOne() > 0.5f ? 0 : 1;
     DREG(53) = 0;
@@ -475,7 +476,7 @@ void func_80A6A5A4(EnHorseLinkChild* this, PlayState* play) {
                 Animation_Change(&this->skin.skelAnime, sAnimations[this->animationIdx], sAnimPlaySpeeds[this->animationIdx],  // was D_80A6AF64 [LLM:HIGH]
                                  0.0f, Animation_GetLastFrame(sAnimations[this->animationIdx]), ANIMMODE_ONCE, -5.0f);
             } else {
-                func_80A6A4DC(this);
+                EnHorseLinkChild_SetupWaitNearPlayer(this);  // was func_80A6A4DC [LLM:HIGH]
             }
         }
     }
@@ -540,7 +541,7 @@ void func_80A6A7D0(EnHorseLinkChild* this, PlayState* play) {
                 this->actor.speedXZ = 2.0f;
                 this->unk_1F0 = 0;
             } else {
-                func_80A6A4DC(this);
+                EnHorseLinkChild_SetupWaitNearPlayer(this);  // was func_80A6A4DC [LLM:HIGH]
                 return;
             }
         }
-- 
2.55.0

