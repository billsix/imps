From 8a0edcdeee0c0d61112a96cf2456236840517d1a Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:39:21 +0000
Subject: [PATCH 1721/3799] soh: rename func_808B3A34 ->
 BgSpot15Rrbox_ClearRiddenBgId

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: forgets whichever bg actor the crate was last resting on.
Defined in soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c       | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
index 5af5fd7fe..ae231d804 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot15_Rrbox/z_bg_spot15_rrbox.c
@@ -81,7 +81,8 @@ void BgSpot15Rrbox_RotatePoint(Vec3f* arg0, Vec3f* arg1, f32 arg2, f32 arg3) {
     arg0->z = (arg1->z * arg3) - (arg1->x * arg2);
 }
 
-void func_808B3A34(BgSpot15Rrbox* this) {
+// LLM generated name (HIGH), was func_808B3A34: forgets whichever bg actor the crate was last resting on.
+void BgSpot15Rrbox_ClearRiddenBgId(BgSpot15Rrbox* this) {
     this->bgId = BG_ACTOR_MAX;
 }
 
@@ -127,7 +128,7 @@ void BgSpot15Rrbox_Init(Actor* thisx, PlayState* play) {
 
     BgSpot15Rrbox_InitDynaPoly(this, play, &gLonLonMilkCrateCol, DPM_UNK);  // was func_808B3960 [LLM:HIGH]
     Actor_ProcessInitChain(&this->dyna.actor, sInitChain);
-    func_808B3A34(this);
+    BgSpot15Rrbox_ClearRiddenBgId(this);  // was func_808B3A34 [LLM:HIGH]
     if (Flags_GetSwitch(play, (this->dyna.actor.params & 0x3F))) {
         func_808B44B8(this, play);
         this->dyna.actor.world.pos = sSpawnPositions[sNextSpawnIndex];  // was D_808B45C4 [LLM:HIGH]
@@ -150,7 +151,7 @@ s32 BgSpot15Rrbox_TrySnapToCheckedPoint(BgSpot15Rrbox* this, PlayState* play, s3
     Vec3f actorPosition;
     Vec3f actorScale;
 
-    func_808B3A34(this);
+    BgSpot15Rrbox_ClearRiddenBgId(this);  // was func_808B3A34 [LLM:HIGH]
 
     actorScale.x = sBoxGroundCheckPoints[arg2].x * (this->dyna.actor.scale.x * 10.0f);
     actorScale.y = sBoxGroundCheckPoints[arg2].y * (this->dyna.actor.scale.y * 10.0f);
@@ -181,7 +182,7 @@ f32 BgSpot15Rrbox_GetFloorHeight(BgSpot15Rrbox* this, PlayState* play) {
     f32 returnValue = BGCHECK_Y_MIN;
     s32 bgId;
 
-    func_808B3A34(this);
+    BgSpot15Rrbox_ClearRiddenBgId(this);  // was func_808B3A34 [LLM:HIGH]
     for (i = 0; i < ARRAY_COUNT(sBoxGroundCheckPoints); i++) {
         scale.x = sBoxGroundCheckPoints[i].x * (actor->scale.x * 10.0f);
         scale.y = sBoxGroundCheckPoints[i].y * (actor->scale.y * 10.0f);
-- 
2.55.0

