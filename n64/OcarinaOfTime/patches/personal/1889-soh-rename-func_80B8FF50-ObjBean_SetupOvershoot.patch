From 57d2a4c450f1782c333e4fc58f6a2276cb09a77f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:50:07 +0000
Subject: [PATCH 1889/3799] soh: rename func_80B8FF50 -> ObjBean_SetupOvershoot

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: installs the overshoot action at its starting angle.
Defined in soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
index 1650e14ce..c1ff2eea7 100644
--- a/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
+++ b/soh/src/overlays/actors/ovl_Obj_Bean/z_obj_bean.c
@@ -44,7 +44,8 @@ void ObjBean_SetupSproutDelay(ObjBean* this);  // was func_80B8FE00 [LLM:HIGH]
 void ObjBean_SetupSprout(ObjBean* this);  // was func_80B8FE6C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B8FEAC: grows the plant to its full height and width, then moves on to the overshoot.
 void ObjBean_Sprout(ObjBean* this, PlayState* play);  // was func_80B8FEAC [LLM:HIGH]
-void func_80B8FF50(ObjBean* this);
+// LLM generated name (HIGH), was func_80B8FF50: installs the overshoot action at its starting angle.
+void ObjBean_SetupOvershoot(ObjBean* this);  // was func_80B8FF50 [LLM:HIGH]
 void ObjBean_SetupGrowWaterPhase4(ObjBean* this);
 void func_80B8FF8C(ObjBean* this, PlayState* play);
 void func_80B90050(ObjBean* this, PlayState* play);
@@ -587,7 +588,7 @@ void ObjBean_Sprout(ObjBean* this, PlayState* play) {
     this->dyna.actor.scale.z = this->dyna.actor.scale.x;
     if (temp_v1) {
         if (this->timer <= 0) {
-            func_80B8FF50(this);
+            ObjBean_SetupOvershoot(this);  // was func_80B8FF50 [LLM:HIGH]
         }
     } else {
         this->timer = 1;
@@ -595,7 +596,7 @@ void ObjBean_Sprout(ObjBean* this, PlayState* play) {
     Actor_PlaySfx_Flagged(&this->dyna.actor, NA_SE_PL_PLANT_GROW_UP - SFX_FLAG);
 }
 
-void func_80B8FF50(ObjBean* this) {
+void ObjBean_SetupOvershoot(ObjBean* this) {
     this->actionFunc = func_80B8FF8C;
     ObjBean_SetDrawMode(this, BEAN_STATE_DRAW_LEAVES | BEAN_STATE_DRAW_SOIL);
     this->unk_1B6.x = 0x33E9;
@@ -752,7 +753,7 @@ void ObjBean_SetupGrowWaterPhase5(ObjBean* this) {
 void ObjBean_GrowWaterPhase5(ObjBean* this, PlayState* play) {
     this->transformFunc(this);
     if (this->timer <= 0) {
-        func_80B8FF50(this);
+        ObjBean_SetupOvershoot(this);  // was func_80B8FF50 [LLM:HIGH]
         this->dyna.actor.flags &= ~ACTOR_FLAG_UPDATE_CULLING_DISABLED;
     }
 }
-- 
2.55.0

