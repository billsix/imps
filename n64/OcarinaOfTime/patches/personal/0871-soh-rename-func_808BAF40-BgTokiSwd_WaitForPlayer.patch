From 4fecf601f63769bb5594e2b99aaa6b277a7e1d16 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:12:00 +0000
Subject: [PATCH 0871/3799] soh: rename func_808BAF40 ->
 BgTokiSwd_WaitForPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the initial action: fires the Master Sword reveal cutscene the first time the player faces the pedestal, and handles giving or taking the sword depending on age and Prelude of Light state.
Defined in soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c b/soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c
index ff7f2fbad..f1ac12615 100644
--- a/soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c
+++ b/soh/src/overlays/actors/ovl_Bg_Toki_Swd/z_bg_toki_swd.c
@@ -16,7 +16,8 @@ void BgTokiSwd_Destroy(Actor* thisx, PlayState* play);
 void BgTokiSwd_Update(Actor* thisx, PlayState* play);
 void BgTokiSwd_Draw(Actor* thisx, PlayState* play);
 
-void func_808BAF40(BgTokiSwd* this, PlayState* play);
+// LLM generated name (HIGH), was func_808BAF40: the initial action: fires the Master Sword reveal cutscene the first time the player faces the pedestal, and handles giving or taking the sword depending on age and Prelude of Light state.
+void BgTokiSwd_WaitForPlayer(BgTokiSwd* this, PlayState* play);  // was func_808BAF40 [LLM:HIGH]
 void func_808BB0AC(BgTokiSwd* this, PlayState* play);
 void func_808BB128(BgTokiSwd* this, PlayState* play);
 
@@ -76,7 +77,7 @@ void BgTokiSwd_Init(Actor* thisx, PlayState* play) {
 
     Actor_ProcessInitChain(&this->actor, sInitChain);
     this->actor.shape.yOffset = 800.0f;
-    BgTokiSwd_SetupAction(this, func_808BAF40);
+    BgTokiSwd_SetupAction(this, BgTokiSwd_WaitForPlayer);  // was func_808BAF40 [LLM:HIGH]
 
     if (LINK_IS_ADULT) {
         if (IS_RANDO) {
@@ -116,7 +117,7 @@ void BgTokiSwd_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyCylinder(play, &this->collider);
 }
 
-void func_808BAF40(BgTokiSwd* this, PlayState* play) {
+void BgTokiSwd_WaitForPlayer(BgTokiSwd* this, PlayState* play) {
     if (((Flags_GetEventChkInf(EVENTCHKINF_ENTERED_MASTER_SWORD_CHAMBER)) == 0) && (gSaveContext.sceneLayer < 4) &&
         Actor_IsFacingAndNearPlayer(&this->actor, 800.0f, 0x7530) && !Play_InCsMode(play)) {
         Flags_SetEventChkInf(EVENTCHKINF_ENTERED_MASTER_SWORD_CHAMBER);
-- 
2.55.0

