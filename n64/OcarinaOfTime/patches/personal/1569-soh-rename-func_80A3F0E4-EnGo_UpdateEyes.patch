From c55622233e57a344d16d7ea8a756abaf956eaba3 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:27:41 +0000
Subject: [PATCH 1569/3799] soh: rename func_80A3F0E4 -> EnGo_UpdateEyes

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: steps the eye index through its three frames on a randomised 30-60 frame cycle, the standard NPC blink shape.
Defined in soh/src/overlays/actors/ovl_En_Go/z_en_go.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Go/z_en_go.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
index 73e163ff7..16c2fa68f 100644
--- a/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
+++ b/soh/src/overlays/actors/ovl_En_Go/z_en_go.c
@@ -413,7 +413,8 @@ void EnGo_TrackPlayer(EnGo* this, PlayState* play) {
     Npc_TrackPoint(&this->actor, &this->interactInfo, 4, trackingMode);
 }
 
-void func_80A3F0E4(EnGo* this) {
+// LLM generated name (HIGH), was func_80A3F0E4: steps the eye index through its three frames on a randomised 30-60 frame cycle, the standard NPC blink shape.
+void EnGo_UpdateEyes(EnGo* this) {
     if (DECR(this->unk_214) == 0) {
         this->unk_216++;
         if (this->unk_216 >= 3) {
@@ -1042,7 +1043,7 @@ void EnGo_Update(Actor* thisx, PlayState* play) {
     }
 
     Actor_UpdateBgCheckInfo(play, &this->actor, 0.0f, 0.0f, 0.0f, 4);
-    func_80A3F0E4(this);
+    EnGo_UpdateEyes(this);  // was func_80A3F0E4 [LLM:HIGH]
     func_80A3F908(this, play);
     this->actionFunc(this, play);
     EnGo_TrackPlayer(this, play);  // was func_80A3F060 [LLM:HIGH]
-- 
2.55.0

