From b3a54c9ca95780f7f5e92313c7671f32f0ddb366 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:40:08 +0000
Subject: [PATCH 1740/3799] soh: rename func_80B4B240 ->
 EnZl1_CourtyardCutscene

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: runs that scene as a numbered sequence of animation frames, camera moves and textboxes.
Defined in soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
index 20a924d12..ebdca92ae 100644
--- a/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
+++ b/soh/src/overlays/actors/ovl_En_Zl1/z_en_zl1.c
@@ -20,7 +20,8 @@ void EnZl1_UpdateFace(EnZl1* this);  // was func_80B4AE18 [LLM:HIGH]
 void EnZl1_Idle(EnZl1* this, PlayState* play);  // was func_80B4AF18 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B4B010: on the first conversation it starts her turn-around animation, creates the sub-camera and whites the screen out to open the courtyard scene.
 void EnZl1_StartCourtyardCutscene(EnZl1* this, PlayState* play);  // was func_80B4B010 [LLM:HIGH]
-void func_80B4B240(EnZl1* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B4B240: runs that scene as a numbered sequence of animation frames, camera moves and textboxes.
+void EnZl1_CourtyardCutscene(EnZl1* this, PlayState* play);  // was func_80B4B240 [LLM:HIGH]
 void func_80B4B8B4(EnZl1* this, PlayState* play);
 void func_80B4BBC4(EnZl1* this, PlayState* play);
 void func_80B4BC78(EnZl1* this, PlayState* play);
@@ -197,7 +198,7 @@ void EnZl1_StartCourtyardCutscene(EnZl1* this, PlayState* play) {
         player->actor.world.pos = playerPos;
         player->actor.speedXZ = 0.0f;
         this->unk_1E2 = 0;
-        this->actionFunc = func_80B4B240;
+        this->actionFunc = EnZl1_CourtyardCutscene;  // was func_80B4B240 [LLM:HIGH]
         Audio_PlayFanfare(NA_BGM_APPEAR);
     } else {
         rotDiff = ABS(this->actor.yawTowardsPlayer - this->actor.shape.rot.y);
@@ -207,7 +208,7 @@ void EnZl1_StartCourtyardCutscene(EnZl1* this, PlayState* play) {
     }
 }
 
-void func_80B4B240(EnZl1* this, PlayState* play) {
+void EnZl1_CourtyardCutscene(EnZl1* this, PlayState* play) {
     Vec3f sp74 = { -427.0f, 108.0, 26.0 };
     Vec3f sp68 = { -340.0f, 108.0f, 98.0f };
     s32 pad;
-- 
2.55.0

