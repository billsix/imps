From 6c50e087e236a6498c4efb2de608d04a63f447db Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:15:50 +0000
Subject: [PATCH 0875/3799] soh: rename func_8098E554 -> DemoSa_ResetWhenCsEnds

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: latches sWasInCutscene while csCtx.state is not CS_STATE_IDLE, and on the return to idle calls DemoSa_SetupSageInvisible for the params == 2 instance.
Defined in soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c b/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
index 79c60bf60..33ee4e1da 100644
--- a/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
+++ b/soh/src/overlays/actors/ovl_Demo_Sa/z_demo_sa.c
@@ -160,7 +160,8 @@ void DemoSa_SetupSageInvisible(DemoSa* this) {
     this->unk_1A0 = 0.0f;
 }
 
-void func_8098E554(DemoSa* this, PlayState* play) {
+// LLM generated name (HIGH), was func_8098E554: latches sWasInCutscene while csCtx.state is not CS_STATE_IDLE, and on the return to idle calls DemoSa_SetupSageInvisible for the params == 2 instance.
+void DemoSa_ResetWhenCsEnds(DemoSa* this, PlayState* play) {
     u32* something = &D_80990108;
 
     if (play->csCtx.state == CS_STATE_IDLE) {
@@ -483,7 +484,7 @@ void DemoSa_SageMagic_CheckFadeOut(DemoSa* this, PlayState* play) {
 
 void DemoSa_Action_SageInvisible(DemoSa* this, PlayState* play) {
     DemoSa_SageMagic_CheckFadeIn(this, play);
-    func_8098E554(this, play);
+    DemoSa_ResetWhenCsEnds(this, play);  // was func_8098E554 [LLM:HIGH]
 }
 
 void DemoSa_Action_SageFade(DemoSa* this, PlayState* play) {
@@ -491,7 +492,7 @@ void DemoSa_Action_SageFade(DemoSa* this, PlayState* play) {
     DemoSa_UpdateSkelAnime(this);
     DemoSa_Blink(this);
     DemoSa_SageMagic_Fade(this, play);
-    func_8098E554(this, play);
+    DemoSa_ResetWhenCsEnds(this, play);  // was func_8098E554 [LLM:HIGH]
 }
 
 void DemoSa_Action_AwaitLightBall(DemoSa* this, PlayState* play) {
@@ -499,7 +500,7 @@ void DemoSa_Action_AwaitLightBall(DemoSa* this, PlayState* play) {
     DemoSa_UpdateSkelAnime(this);
     DemoSa_SageMagic_LowerEyes(this);
     DemoSa_SageMagic_CheckFadeOut(this, play);
-    func_8098E554(this, play);
+    DemoSa_ResetWhenCsEnds(this, play);  // was func_8098E554 [LLM:HIGH]
 }
 
 void DemoSa_DrawXlu(DemoSa* this, PlayState* play) {
-- 
2.55.0

