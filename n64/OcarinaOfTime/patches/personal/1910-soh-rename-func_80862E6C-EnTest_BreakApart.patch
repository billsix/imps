From d92ed5905572e8263c2abff467d962d374935b92 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1910/3799] soh: rename func_80862E6C -> EnTest_BreakApart

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: spawns the BodyBreak parts, then either reassembles at 10 health or drops its collectible and dies
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Test/z_en_test.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index c457f8b39..9dd79e1c6 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -63,7 +63,8 @@ void EnTest_FlinchBehind(EnTest* this, PlayState* play);  // was func_80862418 [
 void EnTest_Stunned(EnTest* this, PlayState* play);
 // LLM generated name (HIGH), was func_808628C8: strafes sideways around the player, reversing off walls and off missing floor for the ceiling type
 void EnTest_CircleAroundPlayer(EnTest* this, PlayState* play);  // was func_808628C8 [LLM:HIGH]
-void func_80862E6C(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_80862E6C: spawns the BodyBreak parts, then either reassembles at 10 health or drops its collectible and dies
+void EnTest_BreakApart(EnTest* this, PlayState* play);  // was func_80862E6C [LLM:HIGH]
 void func_80863044(EnTest* this, PlayState* play);
 void func_8086318C(EnTest* this, PlayState* play);
 void EnTest_Recoil(EnTest* this, PlayState* play);
@@ -1506,10 +1507,10 @@ void EnTest_SetupBreakApart(EnTest* this, PlayState* play) {
         Actor_ChangeCategory(play, &play->actorCtx, &this->actor, ACTORCAT_PROP);
     }
 
-    EnTest_SetupAction(this, func_80862E6C);
+    EnTest_SetupAction(this, EnTest_BreakApart);  // was func_80862E6C [LLM:HIGH]
 }
 
-void func_80862E6C(EnTest* this, PlayState* play) {
+void EnTest_BreakApart(EnTest* this, PlayState* play) {
     if (this->actor.child == NULL) {
         if (this->actor.home.rot.x == 0) {
             this->actor.home.rot.x = this->bodyBreak.count;
-- 
2.55.0

