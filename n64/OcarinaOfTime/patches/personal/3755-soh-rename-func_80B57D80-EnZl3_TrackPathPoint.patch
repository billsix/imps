From 4013f4c39ee392b6370598abe4deadb7f9b34d79 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 02:01:54 +0000
Subject: [PATCH 3755/3799] soh: rename func_80B57D80 -> EnZl3_TrackPathPoint

Name source: DEDUCED from the code (confidence GUESS) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: aims the NpcInteractInfo tracking at a point on the current path heading rather than at the player, snaps shape.rot.y once the error drops under 0x320, and returns the remaining error
Defined in soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c.

Tagged [LLM:GUESS] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
index 93eaf6063..3abf4f249 100644
--- a/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
+++ b/soh/src/overlays/actors/ovl_En_Zl3/z_en_zl3.c
@@ -2134,7 +2134,8 @@ void EnZl3_UpdatePathYaw(EnZl3* this, PlayState* play) {
     EnZl3_StepYawToCurrentPathPoint(this);  // was func_80B57240 [LLM:GUESS]
 }
 
-s32 func_80B57D80(EnZl3* this, PlayState* play) {
+// LLM generated name (GUESS), was func_80B57D80: aims the NpcInteractInfo tracking at a point on the current path heading rather than at the player, snaps shape.rot.y once the error drops under 0x320, and returns the remaining error
+s32 EnZl3_TrackPathPoint(EnZl3* this, PlayState* play) {
     s32 pad;
     s16* sp32 = &this->actor.shape.rot.y;
     NpcInteractInfo* interactInfo = &this->interactInfo;
@@ -2175,7 +2176,7 @@ void func_80B57EEC(EnZl3* this, PlayState* play) {
 }
 
 void func_80B57F1C(EnZl3* this, PlayState* play) {
-    if (func_80B57D80(this, play) == 0) {
+    if (EnZl3_TrackPathPoint(this, play) == 0) {  // was func_80B57D80 [LLM:GUESS]
         EnZl3_ChangeAnim(this, &gZelda2Anime2Anim_009BE4, 0, -8.0f, 0);  // was func_80B54E14 [LLM:HIGH]
         this->action = 34;
         this->unk_314 -= 1;
-- 
2.55.0

