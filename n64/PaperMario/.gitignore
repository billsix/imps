# The pristine upstream checkout that fetch.sh manages (never committed).
/PaperBoat/
# Build tree lives inside the checkout (PaperBoat/build/), but ignore any
# stray top-level ones too, plus the sibling working dir.
/build/
/build-cmake/
/bldInstall/
/runDir/
