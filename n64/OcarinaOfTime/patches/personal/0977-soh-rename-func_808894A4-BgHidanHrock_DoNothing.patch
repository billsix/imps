From 52781f67fa114e3995d1950cec60b5e9866ca9b2 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:33:17 +0000
Subject: [PATCH 0977/3799] soh: rename func_808894A4 -> BgHidanHrock_DoNothing

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: empty action, installed once the pillar has finished falling or when it starts already lowered.
Defined in soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c         | 9 +++++----
 1 file changed, 5 insertions(+), 4 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c b/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
index af121e404..e6f33c12c 100644
--- a/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
+++ b/soh/src/overlays/actors/ovl_Bg_Hidan_Hrock/z_bg_hidan_hrock.c
@@ -16,7 +16,8 @@ void BgHidanHrock_Draw(Actor* thisx, PlayState* play);
 
 void func_8088960C(BgHidanHrock* this, PlayState* play);
 void func_808896B8(BgHidanHrock* this, PlayState* play);
-void func_808894A4(BgHidanHrock* this, PlayState* play);
+// LLM generated name (HIGH), was func_808894A4: empty action, installed once the pillar has finished falling or when it starts already lowered.
+void BgHidanHrock_DoNothing(BgHidanHrock* this, PlayState* play);  // was func_808894A4 [LLM:HIGH]
 
 const ActorInit Bg_Hidan_Hrock_InitVars = {
     ACTOR_BG_HIDAN_HROCK,
@@ -115,7 +116,7 @@ void BgHidanHrock_Init(Actor* thisx, PlayState* play) {
     }
 
     if (Flags_GetSwitch(play, this->unk_16A)) {
-        this->actionFunc = func_808894A4;
+        this->actionFunc = BgHidanHrock_DoNothing;  // was func_808894A4 [LLM:HIGH]
         if (thisx->params == 0) {
             thisx->world.pos.y -= 2800.0f;
             thisx->uncullZoneForward = 3000.0f;
@@ -148,7 +149,7 @@ void BgHidanHrock_Destroy(Actor* thisx, PlayState* play) {
     Collider_DestroyTris(play, &this->collider);
 }
 
-void func_808894A4(BgHidanHrock* this, PlayState* play) {
+void BgHidanHrock_DoNothing(BgHidanHrock* this, PlayState* play) {
 }
 
 void func_808894B0(BgHidanHrock* this, PlayState* play) {
@@ -196,7 +197,7 @@ void func_8088960C(BgHidanHrock* this, PlayState* play) {
             }
         }
 
-        this->actionFunc = func_808894A4;
+        this->actionFunc = BgHidanHrock_DoNothing;  // was func_808894A4 [LLM:HIGH]
     }
 }
 
-- 
2.55.0

