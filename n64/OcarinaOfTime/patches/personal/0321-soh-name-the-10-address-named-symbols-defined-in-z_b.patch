From 4b6aa3158f713918e48502ced31ae899aefc71cb Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:26 +0000
Subject: [PATCH 321/488] soh: name the 10 address-named symbols defined in
 z_bg_spot02_objects.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 10 are defined in soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.c; updating their references touches 3 files in total.

  * D_808AD850 -> sLightningTextures
    deduced, confidence HIGH: the twelve texture frames the bolt effect
    steps through, bound to segment 8 by its draw function on the frame
    given by the same counter that kills the actor after twelve.

  * func_808AC8FC -> BgSpot02Objects_DoNothing
    deduced, confidence HIGH: the empty terminal action installed for the
    static variants and once a moving one has finished.

  * func_808AC908 -> BgSpot02Objects_WaitForExplosion
    deduced, confidence HIGH: the graveyard tombstone's idle: watches the
    cutscene for the actor cue that blows the royal tomb open, then plays
    the explosion sfx, sets the event flag and spawns the blast.

  * func_808ACA08 -> BgSpot02Objects_Explode
    deduced, confidence HIGH: runs the 25-frame explosion: at frame 20 it
    hides the tombstone and throws its debris, plays Link's hurt voice on
    the cutscene frame, and kills the actor at zero.

  * func_808ACAFC -> BgSpot02Objects_WaitForSwitch
    deduced, confidence HIGH: waits for the switch flag, then aims the
    attention camera and starts raising the block.

  * func_808ACB58 -> BgSpot02Objects_Rise
    deduced, confidence HIGH: raises the block 255 units above home with the
    wall-moving sfx, ending on the stone-door stop sfx.

  * func_808ACC34 -> BgSpot02Objects_LightningUpdate
    deduced, confidence HIGH: the params 3 variant: steps the lightning
    frame counter while the cutscene cue is active, kills itself after
    twelve frames, and plays the thunder sfx on two fixed cutscene frames.

  * func_808ACCB8 -> BgSpot02Objects_LightningDraw
    deduced, confidence HIGH: draws the bolt at the cue's position and
    rotation, fading its prim and env colours from white through yellow to
    blue across the twelve frames of sLightningTextures.

  * func_808AD3D4 -> BgSpot02Objects_IceUpdate
    deduced, confidence HIGH: the params 4 variant: on its cutscene cue it
    plays the ice-explosion sfx, runs a 32-frame counter and then kills
    itself.

  * func_808AD450 -> BgSpot02Objects_IceDraw
    deduced, confidence HIGH: draws that effect at the cue's position,
    interpolating between the cue's previous and current rotation across its
    frame range.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../TimeSavers/timesaver_hook_handlers.cpp    |  4 +-
 .../z_bg_spot02_objects.c                     | 78 +++++++++++--------
 .../z_bg_spot02_objects.h                     |  4 +-
 3 files changed, 48 insertions(+), 38 deletions(-)

diff --git a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
index 0b1470818..160ac9166 100644
--- a/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
+++ b/soh/soh/Enhancements/TimeSavers/timesaver_hook_handlers.cpp
@@ -1054,8 +1054,8 @@ void TimeSaverOnActorInitHandler(void* actorRef) {
                 if (innerActor->id == ACTOR_BG_SPOT02_OBJECTS && innerActor->params == 2 &&
                     (CVarGetInteger(CVAR_ENHANCEMENT("TimeSavers.SkipMiscInteractions"), IS_RANDO))) {
                     BgSpot02Objects* bgSpot02 = static_cast<BgSpot02Objects*>(innerActorRef);
-                    if (bgSpot02->actionFunc == func_808ACC34) {
-                        bgSpot02->actionFunc = func_808AC908;
+                    if (bgSpot02->actionFunc == BgSpot02Objects_LightningUpdate) {  // was func_808ACC34 [LLM:HIGH]
+                        bgSpot02->actionFunc = BgSpot02Objects_WaitForExplosion;  // was func_808AC908 [LLM:HIGH]
                         GameInteractor::Instance->UnregisterGameHook<GameInteractor::OnActorUpdate>(bgSpot02UpdateHook);
                         GameInteractor::Instance->UnregisterGameHook<GameInteractor::OnSceneInit>(bgSpot02KillHook);
                         bgSpot02UpdateHook = 0;
diff --git a/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.c b/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.c
index 5da98c7b2..59a5d0fee 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.c
@@ -14,18 +14,28 @@ void BgSpot02Objects_Init(Actor* thisx, PlayState* play);
 void BgSpot02Objects_Destroy(Actor* thisx, PlayState* play);
 void BgSpot02Objects_Update(Actor* thisx, PlayState* play);
 void BgSpot02Objects_Draw(Actor* thisx, PlayState* play);
-void func_808ACCB8(Actor* thisx, PlayState* play);
-void func_808AD450(Actor* thisx, PlayState* play);
-
-void func_808AC8FC(BgSpot02Objects* this, PlayState* play);
-void func_808AC908(BgSpot02Objects* this, PlayState* play);
-void func_808ACA08(BgSpot02Objects* this, PlayState* play);
-void func_808ACAFC(BgSpot02Objects* this, PlayState* play);
-void func_808ACB58(BgSpot02Objects* this, PlayState* play);
-void func_808ACC34(BgSpot02Objects* this, PlayState* play);
-void func_808AD3D4(BgSpot02Objects* this, PlayState* play);
-
-static void* D_808AD850[] = {
+// LLM generated name (HIGH), was func_808ACCB8: draws the bolt at the cue's position and rotation, fading its prim and env colours from white through yellow to blue across the twelve frames of sLightningTextures.
+void BgSpot02Objects_LightningDraw(Actor* thisx, PlayState* play);  // was func_808ACCB8 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808AD450: draws that effect at the cue's position, interpolating between the cue's previous and current rotation across its frame range.
+void BgSpot02Objects_IceDraw(Actor* thisx, PlayState* play);  // was func_808AD450 [LLM:HIGH]
+
+// LLM generated name (HIGH), was func_808AC8FC: the empty terminal action installed for the static variants and once a moving one has finished.
+void BgSpot02Objects_DoNothing(BgSpot02Objects* this, PlayState* play);  // was func_808AC8FC [LLM:HIGH]
+// LLM generated name (HIGH), was func_808AC908: the graveyard tombstone's idle: watches the cutscene for the actor cue that blows the royal tomb open, then plays the explosion sfx, sets the event flag and spawns the blast.
+void BgSpot02Objects_WaitForExplosion(BgSpot02Objects* this, PlayState* play);  // was func_808AC908 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808ACA08: runs the 25-frame explosion: at frame 20 it hides the tombstone and throws its debris, plays Link's hurt voice on the cutscene frame, and kills the actor at zero.
+void BgSpot02Objects_Explode(BgSpot02Objects* this, PlayState* play);  // was func_808ACA08 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808ACAFC: waits for the switch flag, then aims the attention camera and starts raising the block.
+void BgSpot02Objects_WaitForSwitch(BgSpot02Objects* this, PlayState* play);  // was func_808ACAFC [LLM:HIGH]
+// LLM generated name (HIGH), was func_808ACB58: raises the block 255 units above home with the wall-moving sfx, ending on the stone-door stop sfx.
+void BgSpot02Objects_Rise(BgSpot02Objects* this, PlayState* play);  // was func_808ACB58 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808ACC34: the params 3 variant: steps the lightning frame counter while the cutscene cue is active, kills itself after twelve frames, and plays the thunder sfx on two fixed cutscene frames.
+void BgSpot02Objects_LightningUpdate(BgSpot02Objects* this, PlayState* play);  // was func_808ACC34 [LLM:HIGH]
+// LLM generated name (HIGH), was func_808AD3D4: the params 4 variant: on its cutscene cue it plays the ice-explosion sfx, runs a 32-frame counter and then kills itself.
+void BgSpot02Objects_IceUpdate(BgSpot02Objects* this, PlayState* play);  // was func_808AD3D4 [LLM:HIGH]
+
+// LLM generated name (HIGH), was D_808AD850: the twelve texture frames the bolt effect steps through, bound to segment 8 by its draw function on the frame given by the same counter that kills the actor after twelve.
+static void* sLightningTextures[] = {  // was D_808AD850 [LLM:HIGH]
     object_spot02_objects_Tex_0096B0, object_spot02_objects_Tex_00A2B0, object_spot02_objects_Tex_00AEB0,
     object_spot02_objects_Tex_00BAB0, object_spot02_objects_Tex_00C6B0, object_spot02_objects_Tex_00D2B0,
     object_spot02_objects_Tex_00DEB0, object_spot02_objects_Tex_00EAB0, object_spot02_objects_Tex_00F6B0,
@@ -66,22 +76,22 @@ void BgSpot02Objects_Init(Actor* thisx, PlayState* play) {
 
             if (thisx->params == 0) {
                 if (Flags_GetSwitch(play, this->unk_16B)) {
-                    this->actionFunc = func_808AC8FC;
+                    this->actionFunc = BgSpot02Objects_DoNothing;  // was func_808AC8FC [LLM:HIGH]
                     thisx->world.pos.y += 255.0f;
                 } else {
-                    this->actionFunc = func_808ACAFC;
+                    this->actionFunc = BgSpot02Objects_WaitForSwitch;  // was func_808ACAFC [LLM:HIGH]
                 }
 
                 CollisionHeader_GetVirtual(&object_spot02_objects_Col_012BA4, &colHeader);
             } else if (thisx->params == 1) {
-                this->actionFunc = func_808AC8FC;
+                this->actionFunc = BgSpot02Objects_DoNothing;  // was func_808AC8FC [LLM:HIGH]
                 CollisionHeader_GetVirtual(&object_spot02_objects_Col_0128D8, &colHeader);
                 thisx->flags |= ACTOR_FLAG_IGNORE_POINTLIGHTS;
             } else {
                 if (play->sceneNum == SCENE_GRAVEYARD) {
-                    this->actionFunc = func_808AC908;
+                    this->actionFunc = BgSpot02Objects_WaitForExplosion;  // was func_808AC908 [LLM:HIGH]
                 } else {
-                    this->actionFunc = func_808AC8FC;
+                    this->actionFunc = BgSpot02Objects_DoNothing;  // was func_808AC8FC [LLM:HIGH]
                 }
 
                 CollisionHeader_GetVirtual(&object_spot02_objects_Col_0133EC, &colHeader);
@@ -99,8 +109,8 @@ void BgSpot02Objects_Init(Actor* thisx, PlayState* play) {
         case 3:
             this->unk_16A = 0;
             Actor_ChangeCategory(play, &play->actorCtx, thisx, ACTORCAT_ITEMACTION);
-            this->actionFunc = func_808ACC34;
-            thisx->draw = func_808ACCB8;
+            this->actionFunc = BgSpot02Objects_LightningUpdate;  // was func_808ACC34 [LLM:HIGH]
+            thisx->draw = BgSpot02Objects_LightningDraw;  // was func_808ACCB8 [LLM:HIGH]
 
             if (Flags_GetEventChkInf(EVENTCHKINF_DESTROYED_ROYAL_FAMILY_TOMB)) {
                 Actor_Kill(thisx);
@@ -111,8 +121,8 @@ void BgSpot02Objects_Init(Actor* thisx, PlayState* play) {
             this->timer = -12;
             this->unk_170 = 0xFFFF;
             Actor_ChangeCategory(play, &play->actorCtx, thisx, ACTORCAT_ITEMACTION);
-            this->actionFunc = func_808AD3D4;
-            thisx->draw = func_808AD450;
+            this->actionFunc = BgSpot02Objects_IceUpdate;  // was func_808AD3D4 [LLM:HIGH]
+            thisx->draw = BgSpot02Objects_IceDraw;  // was func_808AD450 [LLM:HIGH]
             break;
     }
 }
@@ -123,10 +133,10 @@ void BgSpot02Objects_Destroy(Actor* thisx, PlayState* play) {
     DynaPoly_DeleteBgActor(play, &play->colCtx.dyna, this->dyna.bgId);
 }
 
-void func_808AC8FC(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_DoNothing(BgSpot02Objects* this, PlayState* play) {
 }
 
-void func_808AC908(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_WaitForExplosion(BgSpot02Objects* this, PlayState* play) {
     static Vec3f zeroVec = { 0.0f, 0.0f, 0.0f };
     Vec3f pos;
 
@@ -141,11 +151,11 @@ void func_808AC908(BgSpot02Objects* this, PlayState* play) {
         pos.y = this->dyna.actor.world.pos.y + 30.0f;
         pos.z = (Math_CosS(this->dyna.actor.shape.rot.y) * 50.0f) + this->dyna.actor.world.pos.z;
         EffectSsBomb2_SpawnLayered(play, &pos, &zeroVec, &zeroVec, 70, 30);
-        this->actionFunc = func_808ACA08;
+        this->actionFunc = BgSpot02Objects_Explode;  // was func_808ACA08 [LLM:HIGH]
     }
 }
 
-void func_808ACA08(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_Explode(BgSpot02Objects* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
 
     // The visual effects play the same way whether in rando or not, we just don't want
@@ -173,18 +183,18 @@ void func_808ACA08(BgSpot02Objects* this, PlayState* play) {
     }
 }
 
-void func_808ACAFC(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_WaitForSwitch(BgSpot02Objects* this, PlayState* play) {
     if (Flags_GetSwitch(play, this->unk_16B)) {
         Actor_SetFocus(&this->dyna.actor, 60.0f);
         OnePointCutscene_Attention(play, &this->dyna.actor);
-        this->actionFunc = func_808ACB58;
+        this->actionFunc = BgSpot02Objects_Rise;  // was func_808ACB58 [LLM:HIGH]
     }
 }
 
-void func_808ACB58(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_Rise(BgSpot02Objects* this, PlayState* play) {
     if (Math_StepToF(&this->dyna.actor.world.pos.y, this->dyna.actor.home.pos.y + 255.0f, 1.0f)) {
         Audio_PlayActorSound2(&this->dyna.actor, NA_SE_EV_STONEDOOR_STOP);
-        this->actionFunc = func_808AC8FC;
+        this->actionFunc = BgSpot02Objects_DoNothing;  // was func_808AC8FC [LLM:HIGH]
     } else {
         Actor_PlaySfx_Flagged(&this->dyna.actor, NA_SE_EV_WALL_MOVE_SP - SFX_FLAG);
     }
@@ -206,7 +216,7 @@ void BgSpot02Objects_Draw(Actor* thisx, PlayState* play) {
     Gfx_DrawDListOpa(play, dLists[thisx->params]);
 }
 
-void func_808ACC34(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_LightningUpdate(BgSpot02Objects* this, PlayState* play) {
     if (play->csCtx.state != 0 && play->csCtx.npcActions[0] != NULL && play->csCtx.npcActions[0]->action == 2) {
         this->unk_16A++;
 
@@ -220,7 +230,7 @@ void func_808ACC34(BgSpot02Objects* this, PlayState* play) {
     }
 }
 
-void func_808ACCB8(Actor* thisx, PlayState* play) {
+void BgSpot02Objects_LightningDraw(Actor* thisx, PlayState* play) {
     BgSpot02Objects* this = (BgSpot02Objects*)thisx;
     f32 rate;
     s32 pad;
@@ -262,7 +272,7 @@ void func_808ACCB8(Actor* thisx, PlayState* play) {
         gDPSetPrimColor(POLY_XLU_DISP++, 0, 0, redPrim, greenPrim, bluePrim, 255);
         gDPSetEnvColor(POLY_XLU_DISP++, redEnv, greenEnv, blueEnv, 255);
         gSPMatrix(POLY_XLU_DISP++, MATRIX_NEWMTX(play->state.gfxCtx), G_MTX_NOPUSH | G_MTX_LOAD | G_MTX_MODELVIEW);
-        gSPSegment(POLY_XLU_DISP++, 0x08, SEGMENTED_TO_VIRTUAL(D_808AD850[this->unk_16A]));
+        gSPSegment(POLY_XLU_DISP++, 0x08, SEGMENTED_TO_VIRTUAL(sLightningTextures[this->unk_16A]));  // was D_808AD850 [LLM:HIGH]
         gDPPipeSync(POLY_XLU_DISP++);
         gSPDisplayList(POLY_XLU_DISP++, object_spot02_objects_DL_0126F0);
         gDPPipeSync(POLY_XLU_DISP++);
@@ -271,7 +281,7 @@ void func_808ACCB8(Actor* thisx, PlayState* play) {
     CLOSE_DISPS(play->state.gfxCtx);
 }
 
-void func_808AD3D4(BgSpot02Objects* this, PlayState* play) {
+void BgSpot02Objects_IceUpdate(BgSpot02Objects* this, PlayState* play) {
     if (play->csCtx.state != 0 && play->csCtx.npcActions[2] != NULL && play->csCtx.npcActions[2]->action == 2) {
         if (this->timer == 2) {
             Audio_PlayActorSound2(&this->dyna.actor, NA_SE_IT_EXPLOSION_ICE);
@@ -285,7 +295,7 @@ void func_808AD3D4(BgSpot02Objects* this, PlayState* play) {
     }
 }
 
-void func_808AD450(Actor* thisx, PlayState* play) {
+void BgSpot02Objects_IceDraw(Actor* thisx, PlayState* play) {
     BgSpot02Objects* this = (BgSpot02Objects*)thisx;
     s32 pad;
     f32 lerp;
diff --git a/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.h b/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.h
index cf5152a7e..5149bcac1 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.h
+++ b/soh/src/overlays/actors/ovl_Bg_Spot02_Objects/z_bg_spot02_objects.h
@@ -19,7 +19,7 @@ typedef struct BgSpot02Objects {
     /* 0x0172 */ u16 unk_172;
 } BgSpot02Objects; // size = 0x0174
 
-void func_808ACC34(BgSpot02Objects* bgSpot02Objects, PlayState* play);
-void func_808AC908(BgSpot02Objects* bgSpot02Objects, PlayState* play);
+void BgSpot02Objects_LightningUpdate(BgSpot02Objects* bgSpot02Objects, PlayState* play);  // was func_808ACC34 [LLM:HIGH]
+void BgSpot02Objects_WaitForExplosion(BgSpot02Objects* bgSpot02Objects, PlayState* play);  // was func_808AC908 [LLM:HIGH]
 
 #endif
-- 
2.55.0

