From 08f836f46133c9b8dfa2c5b6c7b6f51b3ef8ff0f Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:49:49 +0000
Subject: [PATCH 1117/3799] soh: rename func_80026400 -> Gfx_SetFogFadeOpa

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: the same POLY_OPA fog setup over a quarter-turn instead, so the effect fades monotonically; this is the variant z_actor.c uses for an actor's colour filter.
Defined in soh/src/code/z_eff_ss_dead.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/include/functions.h      | 2 +-
 soh/src/code/z_actor.c       | 2 +-
 soh/src/code/z_eff_ss_dead.c | 3 ++-
 3 files changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/include/functions.h b/soh/include/functions.h
index 789dc03eb..8456e4830 100644
--- a/soh/include/functions.h
+++ b/soh/include/functions.h
@@ -203,7 +203,7 @@ void EffectSpark_Destroy(void* thisx);
 s32 EffectSpark_Update(void* thisx);
 void EffectSpark_Draw(void* thisx, GraphicsContext* gfxCtx);
 void Gfx_SetFogPulseOpa(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3);  // was func_80026230 [LLM:HIGH]
-void func_80026400(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3);
+void Gfx_SetFogFadeOpa(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3);  // was func_80026400 [LLM:HIGH]
 void func_80026608(PlayState* play);
 void func_80026690(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3);
 void func_80026860(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3);
diff --git a/soh/src/code/z_actor.c b/soh/src/code/z_actor.c
index 53174f65f..327866a97 100644
--- a/soh/src/code/z_actor.c
+++ b/soh/src/code/z_actor.c
@@ -2798,7 +2798,7 @@ void Actor_Draw(PlayState* play, Actor* actor) {
         if (actor->colorFilterParams & 0x2000) {
             func_80026860(play, &color, actor->colorFilterTimer, actor->colorFilterParams & 0xFF);
         } else {
-            func_80026400(play, &color, actor->colorFilterTimer, actor->colorFilterParams & 0xFF);
+            Gfx_SetFogFadeOpa(play, &color, actor->colorFilterTimer, actor->colorFilterParams & 0xFF);  // was func_80026400 [LLM:HIGH]
         }
     }
 
diff --git a/soh/src/code/z_eff_ss_dead.c b/soh/src/code/z_eff_ss_dead.c
index 0c2fc84e2..7ec9b7aed 100644
--- a/soh/src/code/z_eff_ss_dead.c
+++ b/soh/src/code/z_eff_ss_dead.c
@@ -27,7 +27,8 @@ void Gfx_SetFogPulseOpa(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3)
     CLOSE_DISPS(play->state.gfxCtx);
 }
 
-void func_80026400(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3) {
+// LLM generated name (HIGH), was func_80026400: the same POLY_OPA fog setup over a quarter-turn instead, so the effect fades monotonically; this is the variant z_actor.c uses for an actor's colour filter.
+void Gfx_SetFogFadeOpa(PlayState* play, Color_RGBA8* color, s16 arg2, s16 arg3) {
     Gfx* displayListHead;
     f32 cos;
 
-- 
2.55.0

