From 02f550dc858e450c681d83465bccc2cb1b2d2e47 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:19:18 +0000
Subject: [PATCH 2251/3799] soh: rename func_80B0DC7C -> EnSw_Burn

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: sparks for ten frames on the ground, then drops a random collectible and kills the actor
Defined in soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
index cae19999c..da7841ab0 100644
--- a/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
+++ b/soh/src/overlays/actors/ovl_En_Sw/z_en_sw.c
@@ -17,7 +17,8 @@ void EnSw_Cling(EnSw* this, PlayState* play);  // was func_80B0D590 [LLM:HIGH]
 void func_80B0E90C(EnSw* this, PlayState* play);
 void func_80B0E9BC(EnSw* this, PlayState* play);
 void func_80B0E728(EnSw* this, PlayState* play);
-void func_80B0DC7C(EnSw* this, PlayState* play);
+// LLM generated name (HIGH), was func_80B0DC7C: sparks for ten frames on the ground, then drops a random collectible and kills the actor
+void EnSw_Burn(EnSw* this, PlayState* play);  // was func_80B0DC7C [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0C0CC: probes ahead of and behind the skulltula for a surface to cling to and, when told to, moves it onto the hit and re-bases it
 s32 EnSw_StickToSurface(EnSw* this, PlayState* play, s32);  // was func_80B0C0CC [LLM:HIGH]
 // LLM generated name (HIGH), was func_80B0D3AC: puffs dust while the delay runs, then grows and falls along the stored basis until it finds a surface to cling to
@@ -671,7 +672,7 @@ void EnSw_FallToGround(EnSw* this, PlayState* play) {
         this->actor.bgCheckFlags &= ~1;
 
         if (this->unk_38A == 0) {
-            this->actionFunc = func_80B0DC7C;
+            this->actionFunc = EnSw_Burn;  // was func_80B0DC7C [LLM:HIGH]
             this->unk_394 = 10;
         } else {
             this->actor.velocity.y = ((this->unk_38A--) * 8.0f) * 0.5f;
@@ -682,7 +683,7 @@ void EnSw_FallToGround(EnSw* this, PlayState* play) {
     }
 }
 
-void func_80B0DC7C(EnSw* this, PlayState* play) {
+void EnSw_Burn(EnSw* this, PlayState* play) {
     Vec3f velAndAccel = { 0.0f, 0.5f, 0.0f };
     Vec3f pos = { 0.0f, 0.0f, 0.0f };
 
-- 
2.55.0

