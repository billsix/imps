# Source language of this docs-only carrier's upstreams (one lang for all repos
# in the manifest). Read by ../../../tools/check_comment_only_streams.sh to pick
# the comment-only prover:
#   c            -> tools/prove_comment_only.sh   (gcc -fpreprocessed engine)
#   rust|kotlin  -> tools/prove_comment_only_strip.py  (comment-stripper)
kotlin
