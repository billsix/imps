# Derived by the scripts on every run -- the scripts are the record, not their
# output. Delete a line here if you decide that table should be tracked.
inventory.tsv
rename_table.tsv
work/
__pycache__/
