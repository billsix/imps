From 2df008da7953ff0dcd801e9e0a36c8659d6e92c9 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 19:26:32 +0000
Subject: [PATCH 0807/3799] soh: rename func_808B7478 ->
 BgSpot17Funen_DrawSmoke

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: installed as actor.draw at init (the InitVars draw slot is NULL): billboards to the camera and draws gCraterSmokeConeDL with a two-axis scrolling texture.
Defined in soh/src/overlays/actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c         | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c b/soh/src/overlays/actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c
index 0f27a1930..19300ab69 100644
--- a/soh/src/overlays/actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c
+++ b/soh/src/overlays/actors/ovl_Bg_Spot17_Funen/z_bg_spot17_funen.c
@@ -13,7 +13,8 @@ void BgSpot17Funen_Init(Actor* thisx, PlayState* play);
 void BgSpot17Funen_Destroy(Actor* thisx, PlayState* play);
 void BgSpot17Funen_Update(Actor* thisx, PlayState* play);
 void func_808B746C(Actor* thisx, PlayState* play);
-void func_808B7478(Actor* thisx, PlayState* play);
+// LLM generated name (HIGH), was func_808B7478: installed as actor.draw at init (the InitVars draw slot is NULL): billboards to the camera and draws gCraterSmokeConeDL with a two-axis scrolling texture.
+void BgSpot17Funen_DrawSmoke(Actor* thisx, PlayState* play);  // was func_808B7478 [LLM:HIGH]
 
 const ActorInit Bg_Spot17_Funen_InitVars = {
     ACTOR_BG_SPOT17_FUNEN,
@@ -45,14 +46,14 @@ void BgSpot17Funen_Destroy(Actor* thisx, PlayState* play) {
 void BgSpot17Funen_Update(Actor* thisx, PlayState* play) {
     BgSpot17Funen* this = (BgSpot17Funen*)thisx;
 
-    this->actor.draw = func_808B7478;
+    this->actor.draw = BgSpot17Funen_DrawSmoke;  // was func_808B7478 [LLM:HIGH]
     this->actor.update = func_808B746C;
 }
 
 void func_808B746C(Actor* thisx, PlayState* play) {
 }
 
-void func_808B7478(Actor* thisx, PlayState* play) {
+void BgSpot17Funen_DrawSmoke(Actor* thisx, PlayState* play) {
     s32 pad;
 
     OPEN_DISPS(play->state.gfxCtx);
-- 
2.55.0

