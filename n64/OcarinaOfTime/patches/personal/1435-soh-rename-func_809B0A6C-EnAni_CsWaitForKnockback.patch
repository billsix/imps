From 44183f19d1ff83a5b06a3e289be4405b20e72600 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 22:18:57 +0000
Subject: [PATCH 1435/3799] soh: rename func_809B0A6C ->
 EnAni_CsWaitForKnockback

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: cutscene step 0: loops the current animation until actor cue 2, then plays the knockback animation, drops the shadow and advances the cutscene step.
Defined in soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
index 98e982dfe..710ce02a4 100644
--- a/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
+++ b/soh/src/overlays/actors/ovl_En_Ani/z_en_ani.c
@@ -31,7 +31,8 @@ void EnAni_AdultIdle(EnAni* this, PlayState* play);  // was func_809B07F8 [LLM:H
 void func_809B0988(EnAni* this, PlayState* play);
 void func_809B0994(EnAni* this, PlayState* play);
 void func_809B0A28(EnAni* this, PlayState* play);
-void func_809B0A6C(EnAni* this, PlayState* play);
+// LLM generated name (HIGH), was func_809B0A6C: cutscene step 0: loops the current animation until actor cue 2, then plays the knockback animation, drops the shadow and advances the cutscene step.
+void EnAni_CsWaitForKnockback(EnAni* this, PlayState* play);  // was func_809B0A6C [LLM:HIGH]
 
 const ActorInit En_Ani_InitVars = {
     ACTOR_EN_ANI,
@@ -233,7 +234,7 @@ void func_809B0A28(EnAni* this, PlayState* play) {
     }
 }
 
-void func_809B0A6C(EnAni* this, PlayState* play) {
+void EnAni_CsWaitForKnockback(EnAni* this, PlayState* play) {
     if (SkelAnime_Update(&this->skelAnime)) {
         this->skelAnime.curFrame = 0.0f;
     }
@@ -256,7 +257,7 @@ void EnAni_Update(Actor* thisx, PlayState* play) {
     if ((play->csCtx.state != CS_STATE_IDLE) && (play->csCtx.npcActions[0] != NULL)) {
         switch (this->unk_2AA) {
             case 0:
-                func_809B0A6C(this, play);
+                EnAni_CsWaitForKnockback(this, play);  // was func_809B0A6C [LLM:HIGH]
                 break;
             case 1:
                 func_809B0A28(this, play);
-- 
2.55.0

