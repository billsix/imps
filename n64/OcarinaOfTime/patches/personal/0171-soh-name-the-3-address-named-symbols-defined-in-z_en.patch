From 731dc3ca08088aa4769dd0841292aa2f74a22d42 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Tue, 8 Sep 2026 03:25:13 +0000
Subject: [PATCH 171/488] soh: name the 3 address-named symbols defined in
 z_en_fish.c

SoH carries the Ocarina of Time decomp, in which several thousand symbols are
still named after their ROM address. This commit names every such symbol DEFINED
IN ONE FILE, so the work can be reviewed a file at a time.

The file names the unit; it does not bound it. Each rename is applied wherever
the symbol occurs -- the declaration in the header, the definition, and every
call site -- so this commit reaches into whatever other files use these symbols
and leaves no dangling reference. It is a pure rename: no behaviour changes, and
the only edits that are not renames are the provenance comments described below.

Two name sources are used, and every name says which it came from:

  * zeldaret/oot -- SoH's decomp IS zeldaret/oot at the same ROM addresses, so
    where oot has already named a symbol, that name is adopted verbatim. The
    definition gets a "Name from zeldaret/oot" comment and every declaration a
    short "// was <addr> [oot]" tag.

  * DEDUCED -- oot leaves a great many symbols address-named too, so those
    names are inferred from the body and its callers, and are marked as OURS
    rather than passed off as upstream. The definition gets
    "LLM generated name (HIGH|GUESS), was <addr>: <reason>" and the short tag
    reads "[LLM:HIGH]" or "[LLM:GUESS]". A GUESS is tagged at every call site
    as well, not only the declaration, so a reader who meets the name anywhere
    knows it is inferred. "git grep '[LLM:'" lists every place the decomp
    leans on an inference.

The justification recorded for each individual name follows, one entry per
symbol, in the order the renames were made. Nothing is summarised away: these
are the per-commit reasons from the unsquashed history, with the boilerplate
that was identical in every one of them hoisted into the text above.

All 3 are defined in soh/src/overlays/actors/ovl_En_Fish/z_en_fish.c.

  * D_80A17010 -> sJabuCutsceneFish
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * D_80A17014 -> sJabuCutsceneOffsetY
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

  * D_80A17018 -> sJabuCutsceneOffsetYSpeed
    zeldaret/oot (same decomp, same ROM address); structure verified against
    upstream at 1.00

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
---
 .../overlays/actors/ovl_En_Fish/z_en_fish.c   | 45 ++++++++++---------
 1 file changed, 24 insertions(+), 21 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Fish/z_en_fish.c b/soh/src/overlays/actors/ovl_En_Fish/z_en_fish.c
index c48398c97..697c1469b 100644
--- a/soh/src/overlays/actors/ovl_En_Fish/z_en_fish.c
+++ b/soh/src/overlays/actors/ovl_En_Fish/z_en_fish.c
@@ -34,9 +34,12 @@ void EnFish_Unique_SetupSwimIdle(EnFish* this);
 void EnFish_Unique_SwimIdle(EnFish* this, PlayState* play);
 
 // Used in the cutscene functions
-static Actor* D_80A17010 = NULL;
-static f32 D_80A17014 = 0.0f;
-static f32 D_80A17018 = 0.0f;
+// Name from zeldaret/oot (was D_80A17010): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Fish/z_en_fish.c]
+static Actor* sJabuCutsceneFish = NULL;
+// Name from zeldaret/oot (was D_80A17014): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Fish/z_en_fish.c]
+static f32 sJabuCutsceneOffsetY = 0.0f;
+// Name from zeldaret/oot (was D_80A17018): adopted upstream's name for this address; structure verified to match upstream (identifiers and constants differ, upstream having since renamed them) at 1.00. [oot: https://github.com/zeldaret/oot/blob/main/src/overlays/actors/ovl_En_Fish/z_en_fish.c]
+static f32 sJabuCutsceneOffsetYSpeed = 0.0f;
 
 static ColliderJntSphElementInit sJntSphElementsInit[1] = {
     {
@@ -108,25 +111,25 @@ void EnFish_BeginRespawn(EnFish* this) {
 void EnFish_SetCutsceneData(EnFish* this) {
     Actor* thisx = &this->actor;
 
-    if (D_80A17010 == NULL) {
-        D_80A17010 = thisx;
+    if (sJabuCutsceneFish == NULL) {
+        sJabuCutsceneFish = thisx;
         Actor_SetScale(thisx, 0.01f);
         thisx->draw = EnFish_Draw;
         thisx->shape.rot.x = 0;
         thisx->shape.rot.y = -0x6410;
         thisx->shape.rot.z = 0x4000;
         thisx->shape.yOffset = 600.0f;
-        D_80A17014 = 10.0f;
-        D_80A17018 = 0.0f;
+        sJabuCutsceneOffsetY = 10.0f;
+        sJabuCutsceneOffsetYSpeed = 0.0f;
         thisx->flags |= ACTOR_FLAG_UPDATE_CULLING_DISABLED;
         EnFish_SetOutOfWaterAnimation(this);
     }
 }
 
 void EnFish_ClearCutsceneData(EnFish* this) {
-    D_80A17010 = NULL;
-    D_80A17014 = 0.0f;
-    D_80A17018 = 0.0f;
+    sJabuCutsceneFish = NULL;
+    sJabuCutsceneOffsetY = 0.0f;
+    sJabuCutsceneOffsetYSpeed = 0.0f;
 }
 
 void EnFish_Init(Actor* thisx, PlayState* play) {
@@ -586,19 +589,19 @@ void EnFish_Cutscene_FlopOnGround(EnFish* this, PlayState* play) {
     f32 sp24 = Math_SinS(this->slowPhase);
     f32 sp20 = Math_SinS(this->fastPhase);
 
-    D_80A17014 += D_80A17018;
+    sJabuCutsceneOffsetY += sJabuCutsceneOffsetYSpeed;
 
-    if (D_80A17014 <= 1.0f) {
-        D_80A17014 = 1.0f;
+    if (sJabuCutsceneOffsetY <= 1.0f) {
+        sJabuCutsceneOffsetY = 1.0f;
 
         if (Rand_ZeroOne() < 0.1f) {
-            D_80A17018 = (Rand_ZeroOne() * 3.0f) + 2.0f;
+            sJabuCutsceneOffsetYSpeed = (Rand_ZeroOne() * 3.0f) + 2.0f;
             Audio_PlayActorSound2(&this->actor, NA_SE_EV_FISH_LEAP);
         } else {
-            D_80A17018 = 0.0f;
+            sJabuCutsceneOffsetYSpeed = 0.0f;
         }
     } else {
-        D_80A17018 -= 0.4f;
+        sJabuCutsceneOffsetYSpeed -= 0.4f;
     }
 
     this->skelAnime.playSpeed = ((sp24 + sp20) * 0.5f) + 2.0f;
@@ -612,7 +615,7 @@ void EnFish_Cutscene_WiggleFlyingThroughAir(EnFish* this, PlayState* play) {
 
     this->actor.shape.rot.x -= 500;
     this->actor.shape.rot.z += 100;
-    Math_StepToF(&D_80A17014, 0.0f, 1.0f);
+    Math_StepToF(&sJabuCutsceneOffsetY, 0.0f, 1.0f);
     this->skelAnime.playSpeed = ((sp28 + sp24) * 0.5f) + 2.0f;
     SkelAnime_Update(&this->skelAnime);
 }
@@ -667,7 +670,7 @@ void EnFish_UpdateCutscene(EnFish* this, PlayState* play) {
     progress = Environment_LerpWeight(csAction->endFrame, csAction->startFrame, play->csCtx.frames);
 
     this->actor.world.pos.x = (endPos.x - startPos.x) * progress + startPos.x;
-    this->actor.world.pos.y = (endPos.y - startPos.y) * progress + startPos.y + D_80A17014;
+    this->actor.world.pos.y = (endPos.y - startPos.y) * progress + startPos.y + sJabuCutsceneOffsetY;
     this->actor.world.pos.z = (endPos.z - startPos.z) * progress + startPos.z;
 
     this->actor.floorHeight =
@@ -743,12 +746,12 @@ void EnFish_RespawningUpdate(EnFish* this, PlayState* play) {
 void EnFish_Update(Actor* thisx, PlayState* play) {
     EnFish* this = (EnFish*)thisx;
 
-    if ((D_80A17010 == NULL) && (this->actor.params == FISH_DROPPED) && (play->csCtx.state != 0) &&
+    if ((sJabuCutsceneFish == NULL) && (this->actor.params == FISH_DROPPED) && (play->csCtx.state != 0) &&
         (play->csCtx.npcActions[1] != NULL)) {
         EnFish_SetCutsceneData(this);
     }
 
-    if ((D_80A17010 != NULL) && (&this->actor == D_80A17010)) {
+    if ((sJabuCutsceneFish != NULL) && (&this->actor == sJabuCutsceneFish)) {
         EnFish_UpdateCutscene(this, play);
     } else if (this->respawnTimer > 0) {
         this->respawnTimer--;
@@ -764,4 +767,4 @@ void EnFish_Draw(Actor* thisx, PlayState* play) {
     Gfx_SetupDL_25Opa(play->state.gfxCtx);
     SkelAnime_DrawSkeletonOpa(play, &this->skelAnime, NULL, NULL, NULL);
     Collider_UpdateSpheres(0, &this->collider);
-}
\ No newline at end of file
+}
-- 
2.55.0

