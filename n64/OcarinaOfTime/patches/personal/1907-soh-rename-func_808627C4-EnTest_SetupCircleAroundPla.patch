From ba65a282104ca44a222082c24e489d385d3514a5 Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 23:03:11 +0000
Subject: [PATCH 1907/3799] soh: rename func_808627C4 ->
 EnTest_SetupCircleAroundPlayer

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: morphs to gStalfosSidestepAnim at +/-4 speed with world.rot.y a quarter-turn off shape.rot.y -- the AI's usual dodge
Defined in soh/src/overlays/actors/ovl_En_Test/z_en_test.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 .../overlays/actors/ovl_En_Test/z_en_test.c   | 29 ++++++++++---------
 1 file changed, 15 insertions(+), 14 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
index ad0c9c7fe..598764019 100644
--- a/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
+++ b/soh/src/overlays/actors/ovl_En_Test/z_en_test.c
@@ -34,7 +34,8 @@ void EnTest_SetupFlinchBehind(EnTest* this);  // was func_80862398 [LLM:HIGH]
 // LLM generated name (HIGH), was func_80862154: plays gStalfosFlinchFromHitFrontAnim with NA_SE_EN_STAL_DAMAGE and a knockback of -2
 void EnTest_SetupFlinchFront(EnTest* this);  // was func_80862154 [LLM:HIGH]
 void EnTest_SetupStopAndBlock(EnTest* this);
-void func_808627C4(EnTest* this, PlayState* play);
+// LLM generated name (HIGH), was func_808627C4: morphs to gStalfosSidestepAnim at +/-4 speed with world.rot.y a quarter-turn off shape.rot.y -- the AI's usual dodge
+void EnTest_SetupCircleAroundPlayer(EnTest* this, PlayState* play);  // was func_808627C4 [LLM:HIGH]
 
 void EnTest_WaitGround(EnTest* this, PlayState* play);
 void EnTest_WaitAbove(EnTest* this, PlayState* play);
@@ -355,7 +356,7 @@ void EnTest_ChooseRandomAction(EnTest* this, PlayState* play) {
         case 3:
         case 4:
         case 7:
-            func_808627C4(this, play);
+            EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
             break;
 
         case 2:
@@ -385,7 +386,7 @@ void EnTest_ChooseAction(EnTest* this, PlayState* play) {
             case 5:
             case 6:
             case 8:
-                func_808627C4(this, play);
+                EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                 break;
 
             case 2:
@@ -416,7 +417,7 @@ void EnTest_ChooseAction(EnTest* this, PlayState* play) {
                         if (this->actor.isTargeted) {
                             EnTest_SetupSlashDown(this);
                         } else {
-                            func_808627C4(this, play);
+                            EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                         }
                     } else {
                         EnTest_SetupSlashDown(this);
@@ -508,7 +509,7 @@ void EnTest_Idle(EnTest* this, PlayState* play) {
                 if (Rand_ZeroOne() > 0.7f && player->meleeWeaponAnimation != 0x11) {
                     EnTest_SetupJumpBack(this);
                 } else {
-                    func_808627C4(this, play);
+                    EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                 }
                 return;
             }
@@ -523,13 +524,13 @@ void EnTest_Idle(EnTest* this, PlayState* play) {
                     if (Actor_IsTargeted(play, &this->actor)) {
                         EnTest_SetupJumpslash(this);
                     } else {
-                        func_808627C4(this, play);
+                        EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                     }
                 } else {
                     if (Rand_ZeroOne() > 0.3f) {
                         EnTest_SetupWalkAndBlock(this);
                     } else {
-                        func_808627C4(this, play);
+                        EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                     }
                 }
             } else {
@@ -675,7 +676,7 @@ void EnTest_WalkAndBlock(EnTest* this, PlayState* play) {
             } else if (player->heldItemAction != PLAYER_IA_NONE) {
                 if (this->actor.isTargeted) {
                     if ((play->gameplayFrames % 2) != 0) {
-                        func_808627C4(this, play);
+                        EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                         return;
                     }
 
@@ -702,7 +703,7 @@ void EnTest_WalkAndBlock(EnTest* this, PlayState* play) {
                     if (this->actor.isTargeted) {
                         EnTest_SetupSlashDown(this);
                     } else {
-                        func_808627C4(this, play);
+                        EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                     }
                 } else {
                     EnTest_SetupSlashDown(this);
@@ -987,7 +988,7 @@ void EnTest_SlashDownEnd(EnTest* this, PlayState* play) {
                 if (this->actor.isTargeted) {
                     EnTest_SetupSlashDown(this);
                 } else if ((play->gameplayFrames % 2) != 0) {
-                    func_808627C4(this, play);
+                    EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
                 } else {
                     EnTest_SetupJumpBack(this);
                 }
@@ -995,7 +996,7 @@ void EnTest_SlashDownEnd(EnTest* this, PlayState* play) {
                 EnTest_SetupSlashDown(this);
             }
         } else {
-            func_808627C4(this, play);
+            EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
         }
     }
 }
@@ -1209,7 +1210,7 @@ void EnTest_IdleFromBlock(EnTest* this, PlayState* play) {
             if (this->actor.xzDistToPlayer < 500.0f) {
                 EnTest_ChooseAction(this, play);
             } else {
-                func_808627C4(this, play);
+                EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
             }
         }
     }
@@ -1352,7 +1353,7 @@ void EnTest_Stunned(EnTest* this, PlayState* play) {
 }
 
 // a variation of sidestep
-void func_808627C4(EnTest* this, PlayState* play) {
+void EnTest_SetupCircleAroundPlayer(EnTest* this, PlayState* play) {
     if (Actor_OtherIsTargeted(play, &this->actor)) {
         EnTest_SetupSidestep(this);  // was func_80860EC0 [LLM:GUESS]
         return;
@@ -1618,7 +1619,7 @@ void EnTest_Recoil(EnTest* this, PlayState* play) {
         } else if (((play->gameplayFrames % 2) != 0) && (this->actor.params != STALFOS_TYPE_CEILING)) {
             EnTest_SetupJumpBack(this);
         } else {
-            func_808627C4(this, play);
+            EnTest_SetupCircleAroundPlayer(this, play);  // was func_808627C4 [LLM:HIGH]
         }
     }
 }
-- 
2.55.0

