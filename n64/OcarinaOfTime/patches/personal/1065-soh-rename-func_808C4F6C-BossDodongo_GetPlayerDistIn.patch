From b7b0ba9c80c845649f1dbb892f28f229b4aa92fa Mon Sep 17 00:00:00 2001
From: William Emerison Six <billsix@gmail.com>
Date: Mon, 7 Sep 2026 21:39:39 +0000
Subject: [PATCH 1065/3799] soh: rename func_808C4F6C ->
 BossDodongo_GetPlayerDistInFront

Name source: DEDUCED from the code (confidence HIGH) -- zeldaret/oot
leaves this symbol address-named too, so there is no upstream name to adopt.
Reason: rotates the player offset into the boss's facing frame and returns the forward distance when the player is within 150 units laterally and 100 to 2000 units ahead, or -1 otherwise.
Defined in soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c.

Tagged [LLM:HIGH] at the definition, every declaration and every call site, so a
reader meeting the name anywhere knows it is inferred.
---
 soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
index 353a0f995..8e2d59237 100644
--- a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
@@ -43,7 +43,8 @@ void BossDodongo_Damaged(BossDodongo* this, PlayState* play);
 void BossDodongo_UpdateDamage(BossDodongo* this, PlayState* play);
 void BossDodongo_PlayerPosCheck(BossDodongo* this, PlayState* play);
 void BossDodongo_PlayerYawCheck(BossDodongo* this, PlayState* play);
-f32 func_808C4F6C(BossDodongo* this, PlayState* play);
+// LLM generated name (HIGH), was func_808C4F6C: rotates the player offset into the boss's facing frame and returns the forward distance when the player is within 150 units laterally and 100 to 2000 units ahead, or -1 otherwise.
+f32 BossDodongo_GetPlayerDistInFront(BossDodongo* this, PlayState* play);  // was func_808C4F6C [LLM:HIGH]
 f32 func_808C50A8(BossDodongo* this, PlayState* play);
 void BossDodongo_DrawEffects(PlayState* play);
 void BossDodongo_UpdateEffects(PlayState* play);
@@ -1029,7 +1030,7 @@ void BossDodongo_Update(Actor* thisx, PlayState* play2) {
         this->unk_1C8--;
     }
 
-    temp_f0 = func_808C4F6C(this, play);
+    temp_f0 = BossDodongo_GetPlayerDistInFront(this, play);  // was func_808C4F6C [LLM:HIGH]
 
     if (temp_f0 > 0.0f) {
         this->unk_1A4 = temp_f0;
@@ -1398,7 +1399,7 @@ void BossDodongo_Draw(Actor* thisx, PlayState* play) {
     BossDodongo_DrawEffects(play);
 }
 
-f32 func_808C4F6C(BossDodongo* this, PlayState* play) {
+f32 BossDodongo_GetPlayerDistInFront(BossDodongo* this, PlayState* play) {
     f32 xDiff;
     f32 zDiff;
     f32 sp2C;
-- 
2.55.0

