# The upstream checkout is disposable — recreated by fetch.sh; only the
# scripts, patches/, and docs here are versioned.
/checkout/

# Autotools build artifacts (dash builds in-tree with ./autogen.sh &&
# ./configure && make), plus anything the podman gate leaves behind.
*.tar
